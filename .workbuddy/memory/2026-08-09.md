# 2026-08-09 工作日志

## 死代码删除安全扫描（只读调查）

- **背景**: 用户要清理 51 个疑似死接口 + 4 个死组件 + 3 个死埋点配置，删之前需逐个体检防止 build 崩。
- **已完成**: 输出 `docs/plans/2026-08-08-删除安全扫描.md`。
  - 51 个死接口: A=21, B=29, D=1
  - 4 个死组件: 全 A（admin-secondary-nav 需同步清理 app-shell/index.ts barrel 的 re-export 行）
  - 3 个死埋点: 全 A（需配套 migration 调整 CHECK 约束）
  - 补充 4 个"待确认"接口按用户指示归 D
- **关键结论**:
  - B 类保留 8 个 _shared.ts + handlers.ts + _admin-list.ts + production/_shared，被存活代码复用不能删。
  - 特殊: `p1-case-library-and-violations.test.ts` 同时测 2 死 + 3 活接口，只能改不能删。
  - 清单备注"violations/visual-tags 的 lib 被 /api/violations 复用"经 grep 验证不成立，lib 只被 3 个死接口引用，可连带清理。
  - publish-drafts 全系列(7个)A，可连带删 _archive.ts + lib/publish-drafts/*。
- **未完成线索**: 实际删除操作未执行（本次只读）。删后建议跑 build + test 验证。

## 死代码删除安全扫描——细化复查（只读，覆盖上条结论）

- **背景**: 上条记录范围含 3 死埋点 + 4 待确认接口，数字口径与本次用户任务不可比。本次严格按用户指定范围「51 死接口 + 4 死组件」逐个做连带依赖检查（4 个并行只读子代理采集原始 import 数据，主代理归并归类）。
- **交付**: `docs/plans/2026-08-08-删除安全扫描.md`（一张表 + A/B/C/D 汇总 + helper 白名单 + 连带死代码附表）。
- **本次准确归类**:
  - 51 接口: A=17, B=10, C=24, D=0
  - 4 组件: A=1(toast.tsx), B=3(admin-top-nav / admin-secondary-nav / motion-card)
  - 合计 55: A=18, B=10, C=27, D=0
- **与上条记录的差异**: 上条「4 组件全 A」有误——admin-top-nav/admin-secondary-nav/motion-card 各有同名 test import 其 export，应为 B；仅 toast.tsx 零引用为 A。上条数字 A=21/B=29/D=1 因范围不同不可比，以本次为准。
- **关键结论（与上条一致部分）**: 10 个 _shared/_admin-list helper 被存活代码复用不能删（附表一）；p1-case-library-and-violations.test.ts 共享 test 需人工确认；violations/visual-tags lib 仅被 3 死路由引用可连带清理；publish-drafts 系列删后 _archive.ts 成死代码。
- **新增注意点**: #53 admin-secondary-nav 删组件必须同步删 `app-shell/index.ts` 第 9 行死 re-export 否则编译失败；#5 ai-features/[feature_key] 的 route-legacy.test.ts 用 readFileSync 读源码做正则断言，删 route 必连带删该 test。
- **未完成线索**: 仍为只读，删除未执行。

## 合并两版扫描报告（00:49）

- **背景**: 上两条记录对应两轮独立调查——第一轮(我,A/B/D,含3埋点+4待确认D)和第二轮(对方,A/B/C/D,仅51接口+4组件)。对方 Write 覆盖了我的磁盘版本，git 找不回（untracked），但我的内容在对话历史完整保留。
- **已完成**: 按用户选择"合并两版"，以我的 A/B/D 为骨架，补入对方 4 处细节（api-path-validation.ts 3 处正则残留、topics/page.test.ts mock、route-css-loading.test.ts、几个备注），纠正对方 2 处误判（dashboard-alerts 应归 D 非A；case-library/analytics 应归 A 非C——analytics 不 import case-library/_shared）。
- **分类口径说明**: 对方把"仅 test 引用"单列 B 类；用户原始标准 A = "只有自己.test引用"，已含此情况，故合并版维持 A/B/D，A 类注明连带删 test。对方"4 组件 1A+3B"是 B 类细分口径，合并版按用户标准记为 4A（admin-secondary-nav 备注清 barrel + route-css-loading.test.ts）。
- **交付**: `docs/plans/2026-08-08-删除安全扫描.md` 已为合并版，末尾有"与历史版本差异说明"附注。
