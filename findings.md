# 个人成长页后端重构发现

- GitHub 最新 `main` 已包含上一轮成长页 hydration 契约和测试；原主目录的大量修改主要来自本地分支指针落后。
- 施工基线固定为 `origin/main@73bdd4ed`，分支为 `codex杜/growth-contract-v1`。
- 现有加载器仍会在真实报告少于 3 条时调用 `generateVirtualReports`，并把虚拟报告混入能力、建议、趋势等结果。
- 现有 `src/lib/growth-page.ts` 已有能力六维的主要计算，可作为唯一口径重构起点。
- 7 个指定成长组件除自身外无引用；`/api/growth-advice` 也只剩自身实现，无活跃调用。
- 正式前端仍有旧口径 B，本轮不重写该 UI；新契约由后端直接提供，下一棒删除旧计算。
- 线上主库 30 天真实样本分布可找到 0 条、2 条、13 条用户，未找到 3–9 条用户；对应空态、低可信度、高可信度均已跑通。
- 线上抽样用户的脚本契约均正确返回 `none`；低/高样本用户的实名对标均为 `ok`，且只有 `name/dimensionValue/scriptSnippet` 三个字段。
- 线上暂未找到“有日报但无实名对标”的自然样本；`fallback_team_avg` 与结构化脚本 `ok` 状态由纯规则测试覆盖，未伪造线上实测结论。
