- [CX] 文档治理评估：保留《运维排查手册》合理；《后端与数据说明》和《架构与性能总纲》可收缩，但不能按原计划直接删除。14 条后端内容需按通用硬规矩、权限/生命周期规则、模块接口契约、发布脚本说明分流；删除前必须迁移全部高价值内容并修正 AGENTS、代码治理手册、全站模块地图、运维手册及历史计划中的引用。
- [CX] 已为 Claude Code 编写文档治理执行提示词：明确当前后端第三节实际为 15 条规则，优先复用选题库接口文档与反馈流文档，保留性能红线/测量流程，先备份再归档，禁止改代码、误删和自动 push。
- [CX] 处理文档治理断链：将指标权威文档从 `docs/archive/数据口径.md` 恢复至 `docs/数据口径.md`，修正其个人成长数据契约路径；同步将活跃的 `网站体检` Skill 从已下线性能总纲切换到模块地图“性能约束”，历史日志/归档快照保留原貌。
- [CX] 文档治理补漏：发现 `AGENTS.md` 只迁入少数数据库规则，未完整承接已下线《后端与数据说明》第三节。已完整备份现行 AGENTS，并补入数据库客户端与 service role 鉴权、缺字段降级、伪事务回滚、四类“找不到”分诊、成员生命周期/数据范围/发布卡点等全部 Agent 强制规则；待提交推送。
- [CX] 继续收口 `AGENTS.md` 第二节：补成按场景分诊的开工清单，明确性能/loader、接口/数据/权限、数据库/migration、故障、纠错和项目事实的权威文档组合；新增实际读文件声明格式、已下线入口禁作施工依据、跨领域拆分和高风险调用链清单要求；已备份整理前版本，待提交推送。
- [CX] 21:14 修复无团队账号入口：`JoinBanner` 从纯通知改为页面可见横幅，保留入团申请/撤销申请待办；补了服务端渲染测试，确认未分配和待审核两种状态都能看到 CTA。
- [CX] 21:45 修复成员架构假入口：头像菜单里的“成员与团队架构”改为按真实权限显示；无 `manage_members` 时不再给可点击链接，改为明确提示“需权限”，补了源码级回归测试并通过。
- [CX] 21:55 排查“负责人点成员与团队架构进不去”：根因确认是菜单假入口链路未收口——`/admin/modules` 服务端放行条件始终是 `manage_members=true`（company_owner/legacy owner 通过固定权限映射满足），但 `src/components/nav-bar-items.ts` 之前会把“成员管理”暴露给所有能进管理中心的人，造成无该权限账号点进去再被页面守卫拦回。已做最小修复：主导航仅对 `manage_members` 账号显示 `/admin/modules`，不扩大 company_owner 范围；补回归测试并通过 33 项目标测试。
- [An] 22:00 入团横幅视觉体验与响应式重构：1. 将 `<JoinBanner />` 移入 `<main>` 最前列，彻底根治 Fixed 顶栏遮挡与挤压错位；2. 废弃高饱和渐变与侧边生硬印迹，重构为纯净纸感白卡（四边均匀 1px 细线 + 32px 浅底微胶囊图标），与全站卡片完全统一；3. 解耦通知中心常态静默推送，消灭铃铛常驻红点噪点；4. 移动端采用内联单行排版并将高度压缩至 64px 以内保证首屏数据露出版面；测试通过。

- [CX] 22:22 修复电脑端头像菜单“成员与团队架构”点击无效：线上当前部署与 `main` 一致，`/admin/modules` 对当前会话可正常放行，故仅将已授权菜单项从 Next 客户端 `Link` 改为原生同源链接，确保点击由浏览器直接跳转；未修改角色、权限、团队或集团范围，25 项目标测试与 TypeScript 检查通过，待随本次提交上线验证。
- [CX] 22:32 修复集团模式入口不可见：无集团资格时 `GroupModeSettingsControl` 原先直接不渲染，导致系统设置没有“公司模式 / 集团模式”说明；现统一显示当前公司模式和“未获集团资格 / 需集团资格”，仅保留有资格账号的进入/退出按钮，不改资格、范围或接口。23 项目标测试与 TypeScript 检查通过，待随本次提交上线验证。
