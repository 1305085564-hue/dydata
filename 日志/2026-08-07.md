[CX] 11:02 修复本地 `/dashboard` 因 Supabase refresh token 已失效仍返回“暂时无法确认账号状态”的循环：middleware 改用 `auth.getUser()` 做真实校验，并将明确失效会话清 cookie 后跳回登录页；已用单测和 localhost HTTP/浏览器验证。
[CX] 16:32 推进权限 V2 契约收口：成员生命周期代码与测试已去掉 group 依赖，新增 `20260807103000_permission_v2_contract_closure.sql` 覆盖 `visible_user_ids`、履约 RPC、case/publish scope 和豁免 SELECT/RPC；同步收口了三份说明文档。数据库变更还未实际执行，待后续按 SQL Editor/迁移校验。
[CX] 21:11 修复 `/dashboard` 顶部日期日历历史日期误显示红点：首屏 loader 增加本月已提交日期轻量查询，页面按本月日期去重传给日历；已补 `dashboard-page` 回归测试并通过。
[An] 21:20 优化首页历史记录编辑弹窗：DialogContent 扩至 sm:max-w-5xl，DashboardForm 重构为左【截图+基础】右【核心+补充】的 2x2 网格，适配 768px+ 容器查询一屏全览。
[An] 21:25 精准收口：还原主页 (VideoSubmitForm) 原始布局；将 md:grid-cols-2 左右双列精准应用于历史记录编辑弹窗 (DashboardForm)。
[An] 21:30 历史记录编辑弹窗排版优化：将文案框移至左列消除“左短右长高低脚”，将点赞/评论/分享/收藏优化为 2x2 网格并去除冗余解释文案。
[CC] 22:49 根治历史记录【修改日报】弹窗：① 弹窗叠弹窗——history-list 删掉内部编辑弹窗，行点击/铅笔统一走 onReportOpen；② 行点击秒关跳回上传页的预存根因——production-control-system 用 `账号-日期` 作 key，handleHistoryReportOpen/openSubmittedDate 开弹窗时改日期触发整面板重挂清状态，已改为开弹窗不动日期/账号；③ dashboard-form 重排为顶部通栏(账号/日期/状态+截图导入)+左右双列(基础信息|核心数据)+底部通栏(互动发布 3×2)，全程压缩纵向密度；④ 删除 dashboard.css 永不命中的 @container 死代码。Playwright 实测双入口单层弹窗、左右列 340/340 齐平、900px 视口基本一屏；npm test 1091 全过。改动未 push。
