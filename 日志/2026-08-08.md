[CX] 01:04 修复 Codex 启动前反复 `Reconnecting...`：确认当前链路走 `cc-switch` 的 `custom` provider，给 `/Users/mac/.codex/config.toml` 和当前运行时 provider 配置补了 `supports_websockets = false`，doctor 已显示 active provider 的 Responses WebSocket 关闭，HTTPS 回退生效；已留备份，未改模型与 base_url。
