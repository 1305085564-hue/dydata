# 2026-08-30

[CX] 2026-08-30：按用户要求将 Topics V3 的产品对齐、五批改为三包的历史、Antigravity 前端改造与返工、Codex 接口收口、手动录入恢复、验收结果、遗留问题和下一步边界集中更新到 `docs/plans/2026-08-29-Topics-V3-五批进度.md`。该进度文档作为后续选题库优化的唯一交接入口；卡片数据取舍和最终视觉统一仍待后续确认，三条进货入口已固定为内部自动入库、管理员批量导入、页面手动录入。`.commandcode/` 仅含通用偏好规则，没有本项目执行记录，未改动；旧版 `docs/reference/选题库接口文档.md` 已在进度文档中标记为历史资料，避免后续误按 V2 契约施工。
[CX] 2026-08-30：交接文档首版及后续修订已推送 main，并核对本地与远端一致；未触碰 `.commandcode/`、凭据文件或其他无关未提交改动。
[CX] 2026-08-30：真实页面发现 `/api/topics/active` 的最近成片查询因 `videos` 与 `sub_topics` 存在两条外键关系而返回 Supabase `PGRST201`；已指定 `videos.topic_id` 关系修复，新增回归测试，修复后的线上底层查询返回 200。`npm test` 1243/1243、lint 0 error（74 条既有 warning）、tsc/build 通过；代码提交 `431f3e73` 已推送 main，云端页面刷新待部署后复验。此次问题记录为“契约测试通过但生产页面首屏未逐请求复验”，不能再笼统称全部接口已打通。
[CX] 2026-08-30：完成 Topics 首屏性能收口：服务端页面复用已确认的身份/权限直接注入首屏真实数据，客户端首屏不再重复请求 active/options/pool/my_claims；保留 bootstrap API 兜底，筛选与刷新请求收敛，topics API 复用请求级用户上下文。首次进入、搜索、刷新均用本地生产模式登录态实测通过；未宣称生产性能通过。
[ZC] 2026-08-30：为 ZCode 首次配置用户级 MCP（此前 ~/.zcode/cli/config.json 不存在，仅有内置能力）。对照 Claude Code（filesystem/antigravity/playwright/notion）与 Codex（node_repl/playwright/computer-use 已禁用）后，只装增量项：notion（http://mcp.notion.com/mcp）和 antigravity（npx @ask-llm/antigravity-mcp），写入 ~/.zcode/cli/config.json 的 mcp.servers，JSON 校验通过；playwright、node_repl、computer-use、filesystem 与 ZCode 内置 browser-use/computer-use/文件工具重复，未装。另发现仓库 .agents/mcp_config.json 文件名不合规（ZCode 只读 .agents/mcp.json），从未生效，未照搬。需重启会话或到 Settings → MCP 确认连接；notion 若要求 OAuth 需在该页面授权。
[CX] 2026-08-30 10:40：完成全站公共加载链路第一批体检，覆盖 Topics、Dashboard、admin/content、admin/collaboration 的静态调用链与本地生产模式真实登录浏览器；结论不是全站从零重构，而是公共 profile/accounts/scope 重复读取叠加各页局部问题。实测 Topics 首屏 FMP 1.12s 且 0 个 topics API，Dashboard FMP 0.88s但提前拉岗位成员；Topics 详情接口 828–951ms且 detail/works 重复查作品；admin/content 首屏后全量状态 GET 因 400 个视频 ID 返回 431并被前端静默吞掉；协作页重复权限上下文且同月日报至少查三次。报告见 `docs/reference/全站公共加载链路体检-2026-08-30.md`，模块地图与性能台账已同步；未改业务代码、数据库或权限，未宣称 Vercel 生产通过。
[CX] 2026-08-30 10:52：完整修复 `/admin/content` topic-library-status 的 HTTP 431：页面改用固定 URL 的 POST JSON 发送最多 400 个视频 ID，服务端保留 GET 兼容、`review_content` 鉴权和 150 条查库分批，新增正文校验与失败可见提示；未改数据库/RLS/权限模型。回归测试先失败后通过，全量 `npm test` 1250/1250、eslint、diff check、生产构建通过；本地生产模式真实登录复测 20 条和 400 条请求均 POST 200、无控制台错误，400 条约 1.24s 仍记为性能债。未部署、未宣称 Vercel 生产通过，现有 Antigravity 改动未触碰。
[CX] 2026-08-30 11:16：恢复 `docs/架构与性能总纲.md`，已把“建立可验证、可演进的全站加载与性能控制机制”写入总目标并接入项目文档分诊；完成静态 + 登录态浏览器双层门禁。`gate:static` 通过（项目测试 1255/1255、脚本测试 2/2、tsc、eslint 0 error、生产构建），最终 `gate:browser` 四页硬规则全部通过；FCP 约为 Topics 0.71s、Dashboard 0.23s、admin/content 0.64s、collaboration 0.67s，admin/content 最慢接口 2.91s、collaboration 0.94s 作为性能债报警。门禁已修正链式请求等待，关闭 trace/截图/视频/失败页面快照/登录态持久化；失败调试产物已从工作区移入废纸篓且未进 Git。仅本地生产构建，未部署、未宣称 Vercel 通过；现有 Antigravity 改动保持原样。
[CX] 2026-08-30 12:33：完成性能架构文档归位清理：模块地图删除通用性能红线和排查流程，仅保留页面预算与缓存事实；代码治理手册将 loader 章节收回文件位置/页面例外；运维手册与 README 改指架构与性能总纲；已完成的双层门禁设计从 `docs/plans/` 移至 `docs/reference/`。历史体检、性能台账和 archive 原稿保留，未改代码/数据库/权限，未提交推送。
[CX] 2026-08-30 13:05：完成剩余历史文档处理：体检报告、性能台账和门禁验收记录均补充“仅历史记录、不构成当前施工授权”边界；现行总纲补充 archive 原稿追溯路径；核对旧性能入口和旧门禁设计路径，活动文档无残留，archive 原稿保留原样。
[CX] 2026-08-30 13:06：清除性能台账底部已过期的“浏览器全链路请求数仍待补测”占位行，并在体检报告说明“下一阶段建议”是当时记录，已标完成项不再视为待办；历史实测数字与 archive 原稿未改。
[CX] 2026-08-30 13:13：按用户要求将阿禅账号改回短密码；已通过 Supabase Auth 实际登录核验，本地 gitignored 测试账号记录同步更新，凭据未写入 Git。
[ZC] 2026-08-30：完成全站 12 页 + 13 个抽屉/弹窗加载速度实测（本地生产构建 + company_owner 登录态，脚本 `scripts/perf-full-sweep.mjs` 未提交可复跑）。结论：所有页面骨架首屏/完整加载均达标，慢点集中在 hydration 后自动追加重接口与弹层内并行慢接口——`/admin/content` full list 冷缓存 3.92s 且 topic-library-status 重复触发 2 次（1.94s+0.88s）、`/admin/videos` list 3.43s、协作页 person 接口单击重复请求 2 次、Topics 详情抽屉三接口 1.28–1.54s、`/api/exemptions/pending` 全站固定 0.79–0.93s、dashboard 挂载即拉 operator-members 0.90s、growth trend/leaderboard 1.07–1.13s、rewrite bootstrap 1.78s。无 4xx/5xx、无控制台错误。台账已更新；仅本地生产构建，未宣称 Vercel 通过，未改业务代码。
[ZC] 2026-08-30：按阿禅拍板完成 sweep 后四项性能修复并复测通过：①协作页人卡 hover 预取与点击共享进行中 Promise，person 请求 2→1 次；②admin/content full list 改列表上方「加载全量列表」显式按钮（删自动加载 effect），topic-library-status 按视频 ID 签名去重 + AbortController，页面请求数 5→2、判定转为通过；③Topics 详情抽屉作品统一读 detail.works（服务端本就内含相同参数查询），删 /works 冗余请求与 worksError 死代码，请求 3→2；④导航豁免角标延后 2.5s 拉取 + 60s 模块缓存（服务端注入会阻塞 shell TTFB，评估后放弃）。1255 测试/tsc/lint/生产构建全过，真实登录复测行为正确；detail/claims/person/attribution 接口自身 1.3–1.9s 延迟属服务端既有债务未动。改动文件含未提交的 Antigravity 外观改动（TopicHubV2.tsx、TopicPoolExplorer.tsx），为避免混入他人未提交工作，先向阿禅呈现三拨改动清单再统一处理。台账与模块地图已同步；仅本地生产构建，未宣称 Vercel 通过。
[ZC] 2026-08-30：经阿禅确认，将工作树中三拨未提交改动分 6 个逻辑批次推送 main：ff51e6e3 Antigravity 选题库外观微调+审计报告、f26f7978 Codex 的 431 修复、8edd0b57 双层性能门禁、9b95e820 四项性能修复、c2b17f19 文档、14b2ddb9 sweep 脚本。工作树仅剩本地配置残留（.agents/mcp_config.json、.commandcode/、supabase/.temp），未入库。已推送待 Vercel 部署，线上复验 431 修复与性能修复效果后方可在生产环境下结论。
