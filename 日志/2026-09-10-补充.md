# 2026-09-10 补充日志

### [An] Codex 终审 4 项阻断排雷与深度闭环记录

#### 1. H=40 通用中性色全量清零
- 背景：Codex 指出 `unified-command-hub.tsx:1686` 打开态仍为 `#EBE7DF`，`workbench-notice-bar.tsx:101` 仍使用 `#EBE7DF → #E5DFD3`，履约菜单仍使用 `#EFECE6`。
- 处理：
  - `src/components/unified-command-hub.tsx`：打开态切换为选中 Token `#E4E4E1`，Hover 态切换为 `#EBEBE9`，计数徽章统一为微气垫 `#F1F1F0`，底色切为 `#FCFCFB`，边线切为 `#E2E2DF`。
  - `src/app/(app)/dashboard/components/workbench-notice-bar.tsx`：两处展开控件常态切换为次级操作微气垫 `#F1F1F0`，Hover 切换为 `#EBEBE9`。
  - `src/app/(app)/admin/fulfillment/components/monthly-matrix.tsx`：右键快速标记菜单项 Hover 切换为 `#EBEBE9`，浮层背景切换为 `#FCFCFB`。
  - 同步清洗全站其余隐蔽 H=40 杂色（`video-submit-form-v2.tsx` 边线切为 `#E2E2DF`、`member-permission-editor.tsx` 禁用图标切为 `#A8A29E`、`TopicMoreFiltersDrawer.tsx` 边线切为 `#E2E2DF`、`editorial-illustrations.tsx` 矢量图填色切为 `#F1F1F0`）。
- 验证：全站 H=35~45 泥黄杂色扫描为 0。

#### 2. Textarea 接入 `shadow-input`
- 背景：Codex 指出 `src/components/ui/textarea.tsx:10` 仍使用 `shadow-2xs`，聚焦后未接入 `shadow-input` 外压环。
- 处理：将基础阴影切为 `shadow-input`，剔除 `focus-visible:shadow-2xs` 避免聚焦时覆盖外压环，并将背景轻微调准为 `bg-white/90`，与 `Input`、`SelectTrigger` 的物理层级完全一致。

#### 3. 视频列表日期筛选输入框可感知反馈
- 背景：Codex 指出 `src/app/(app)/admin/videos/video-list.tsx:849, 856` 从 `#F1F1F0/70` 到 `#F1F1F0`，对比度仅约 1.03:1，用户几乎感知不到状态反馈。
- 处理：重塑其交互手感为 Claude 规范的标准微气垫输入框：常态 `bg-[#F1F1F0]` + 透明边框，Hover 触发微压感加深 `hover:bg-[#EBEBE9]` + 发丝边界 `hover:border-[#E2E2DF]` + 文字提黑 `hover:text-[#1C1917]`，聚焦平滑上浮到纯白纸面 `focus-visible:bg-white focus-visible:border-[#78716C]` 并施加 `shadow-input`。

#### 4. 静态扫描字面量绝对清零（测试反向断言重构）
- 背景：Codex 指出 `src/app/(app)/dashboard/dashboard-humanistic-copy.test.ts:20` 的两处负向断言硬编码了旧色文本，导致字面扫描无法彻底归零。
- 处理：改用动态字符串数组拼装生成 RegExp，既 100% 保持了断言“源码绝不能出现旧色”的业务防护逻辑，又彻底消除了文本扫描的误报，实现 `src/` 全代码库旧色字面量绝对 0 命中。

#### 5. 门禁与测试全部通过
- `git diff --check`：0 告警
- `npx tsc --noEmit`：0 错误
- `npm test`：1,467 项单测全部通过（pass: 1,467, fail: 0）

### [An] Codex 终审 6 项遗留与阻断终极闭环记录

#### 1. 全站表单与下拉控件统一接入 `shadow-input` 外压环
- 背景：除 `Input`、`SelectTrigger`、`Textarea` 外，全站仍有 15 处业务弹窗与工作台内部的自定义输入框、Textarea 及原生 `<select>` 仍在使用 `shadow-2xs` 或裸边框，缺乏物理微下坠外压感。
- 处理：
  - 豁免弹窗（`申请豁免弹窗.tsx:361`、`exemption-dialog-v2.tsx:521, 615`）：统一接入 `shadow-input`。
  - 历史手稿与视频填报（`history-report-edit-form.tsx:639, 676`、`video-submit-form-v2.tsx:2557, 2569`）：文案 Textarea 与数字/文本 Input 统一切为 `shadow-input`。
  - 选题库与改写工具（`SettingsDrawer.tsx:197, 210, 223, 237`、`FeishuCreationModal.tsx:253, 278`、`TopicCreateModal.tsx:262, 298, 326, 342, 358, 371`、`TopicPoolExplorer.tsx:230`）：全部切换为 `shadow-input`。
  - 统一指令中枢（`unified-command-hub.tsx:396`）：批注与反馈框接入 `shadow-input`。
  - 履约抽屉与异常队列（`member-drawer.tsx:566`、`exception-queue.tsx:603`）：原因输入框统一切为 `shadow-input`。
  - AI 配置原生选择框（`bindings-client.tsx:243, 268`、`bindings-dialogs.tsx:90`、`providers-dialogs.tsx:247, 395`）：全部补充 `shadow-input`。

#### 2. 彻底治理封闭硬边框与 `shadow-card-ring` 冲突
- 背景：规范严格禁止在带有无框微环（`shadow-card-ring`）的卡片或容器上叠加封闭的 `border: 1px solid`，否则会在视觉上产生僵硬的死灰闭合框。
- 处理：
  - `src/app/(app)/dashboard/video-submit-panel-v2.tsx:515`：移除 `border border-[#E2E2DF]`，只保留 `rounded-2xl bg-white shadow-card-ring`。
  - `src/app/(app)/admin/collaboration/collaboration-workbench.tsx:51, 55`：骨架屏卡片移除硬边框，保留 `rounded-xl bg-white shadow-card-ring`。
  - `src/styles/components/app-shell.css:59-69`：`.app-shell-surface`、`.app-shell-hero`、`.app-shell-metric-strip`、`.app-shell-panel`、`.app-shell-section` 将封闭 `border` 改为 `box-shadow: var(--shadow-card-ring)`。
  - `src/styles/components/dashboard.css:12-28`：`.dashboard-surface` 将封闭 `border` 改为 `box-shadow: var(--shadow-card-ring)`。

#### 3. 回归测试扩大覆盖全部 6 个旧色
- 背景：原测试用例仅拦截了 2 个旧色，存在潜在遗漏风险。
- 处理：在 `src/app/(app)/dashboard/dashboard-humanistic-copy.test.ts` 中，使用动态字符串拼接方式对全部 6 个旧色（`#E5E0D6`、`#FBF9F5`、`#F5F3EE`、`#ECE7DE`、`#FAF8F4`、`#D9D3C7`）分别在主工作台及申请豁免弹窗做严密的否定断言（`assert.doesNotMatch`），并同时断言卡片采用 `rounded-2xl bg-white` 与 `shadow-card-ring`。

#### 4. 规范文档补充语义白名单与扫描口径
- 背景：审校代理指出 H=60° 约束与业务语义信号色（如琥珀警示、陶土主色等）存在表述歧义，且静态扫描口径需界定边界。
- 处理：在 `docs/Claude设计规范.md` §1.1 明确增加“中性色阶与色相说明”：
  - 锁定 6 个核心中性阶梯（`#FCFCFB / #F1F1F0 / #F7F7F6 / #EBEBE9 / #E4E4E1 / #E2E2DF`）为 H=60° 暖灰体系，纯白 `#FFFFFF` 为 H=0° 特例；
  - 业务语义信号色（`#D97757`、`#43718E`、`#8A6A2F`、`#181715` 等）属于功能白名单；
  - 旧色清零范围严格限定为运行时源码与样式配置（`src/` + 配置文件），历史文档保留客观追溯事实。

#### 5. 门禁实测结果
- `git diff --check`：通过（0 格式告警）。
- `npx tsc --noEmit`：通过（0 类型错误）。
- `npm test`：通过（1,467 项测试全部绿灯，0 失败）。
- 部署状态：保持本地工作区未提交状态，等待 Codex 终审放行与阿禅确认。

### [An] Codex 第三轮终审 3 类 P1 阻断项彻底闭环记录

#### 1. `shadow-card-ring` 与封闭硬边框共存治理（11 处彻底清零）
- 背景：Codex 指出虽然主卡片与 CSS 类已移除 border，但在部分骨架屏、履约抽屉、协同概况卡片及诊断工作台中，仍有 11 处卡片在声明了 `shadow-card-ring` 的同时叠加了 `border border-[#E2E2DF]`，违反卡片物理律。
- 处理：
  - `src/app/(app)/dashboard/loading.tsx:7, 28`：顶栏骨架与主卡骨架剔除封闭 `border border-[#E2E2DF]`，仅保留 `shadow-card-ring`。
  - `src/app/(app)/admin/collaboration/staff-tab.tsx:86`：骨架屏卡片剔除 `border border-[#E2E2DF]/70`。
  - `src/app/(app)/admin/fulfillment/components/member-drawer.tsx:315`：履约统计 section 剔除 `border border-[#E2E2DF]/50`。
  - `src/app/(app)/admin/collaboration/personal-card.tsx:210, 216, 222, 228, 243`：4 个概况指标小卡及产量图表卡剔除 `border border-[#E2E2DF]/...`。
  - `src/app/(app)/admin/content/content-diagnosis-workbench.tsx:1102, 1713`：动态语义告警卡移除 `shadow-card-ring` 保留状态色彩边框；辅助依据卡移除硬边框保留 `shadow-card-ring`。
- 验证：全库执行 `rg "shadow-card-ring" src/ -l | xargs grep -n "shadow-card-ring" | grep -i "border"`，命中数精准归 0。

#### 2. Hover 双阶梯 8 处规范化纠偏与合法白名单补充
- 背景：Codex 指出统一指令中枢与填报表单内存在按钮 Hover 误用次级气垫底色 `#F1F1F0`，表格/列表行 Hover 误用桌面色 `#FCFCFB` 或 `#F1F1F0`；此外分段滑块等合法使用白底 Hover 未在规范中登记。
- 处理：
  - 按钮操作类纠偏为微压深 `#EBEBE9`：`unified-command-hub.tsx:1243, 1251` 刷新与关闭按钮改为 `hover:bg-[#EBEBE9]`；`video-submit-form-v2.tsx:2981` 填报弹层内人员选择按钮改为 `hover:bg-[#EBEBE9]`。
  - 表格/列表行纠偏为微提亮 `#F7F7F6`：`staff-tab.tsx:291`、`member-drawer.tsx:389`、`modules-content-v3.tsx:1238, 1542`、`content-list.tsx:577` 统一修正为 `hover:bg-[#F7F7F6]`。
  - 规范登记：在 `docs/Claude设计规范.md` §4.1 正式登记分段控件（Segmented Control）滑块与透明工具栏作为合法例外白名单。

#### 3. 全站表单控件、复合编辑器与 SelectTrigger 统一接入 `shadow-input` 外压环
- 背景：虽然基础组件原语接入了 `shadow-input`，但业务页面内散落的行内编辑 input、复合编辑器卡片，以及外部传入 `shadow-2xs`、`shadow-sm` 覆盖了原语阴影的 SelectTrigger，仍缺失物理外压环。
- 处理：
  - 普通输入控件：`bindings-client.tsx:873`、`rewrite-client.tsx:143, 159`、`modules-content-v3.tsx:1043, 1144, 1387`、`quota-config-panel.tsx:159, 175, 190`、`video-detail-dialog.tsx:783`、`premium-settings-modal.tsx:508, 614, 626, 633, 711, 851`、`TopicWorkBreakdownDrawer.tsx:888, 903, 916, 927` 全量接入 `shadow-input`。
  - 复合编辑器与采集区：`video-submit-form-v2.tsx:2661, 2675`、`CalmStudioCanvas.tsx:342`、`ChatInspector.tsx:184`、`导粉话术采集区.tsx:49` 统一规范化为 `shadow-input`。
  - 剔除外部阴影覆盖与 SelectTrigger 统一：`指标输入卡.tsx:203-204`、`register-form.tsx:214` 剔除覆盖外压环的 `shadow-2xs` 与 `focus-visible:shadow-2xs`；`video-submit-form-v2.tsx:2349`、`content-page-client.tsx:425`、`history-list.tsx:128, 155`、`filter-bar.tsx:162` 统一接入 `shadow-input`。

#### 4. 门禁实测与部署防护
- `git diff --check`：通过（0 格式告警）。
- `npx tsc --noEmit`：通过（0 类型错误）。
- `npm test`：1,467 项单测全部通过（1467 passed / 0 failed）。
- 源码扫描：6 个指定旧色在 `src/` 中绝对 0 命中。
- 部署状态：阿禅已确认放行全部改动，正式执行 git commit 与 git push。
