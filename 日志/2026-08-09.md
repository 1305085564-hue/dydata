# 2026-08-09 日志

## [CC] 死代码删除安全扫描（只读调查）

- **背景**: 用户要删 51 个疑似死接口 + 4 个死组件，删前需逐个做连带依赖检查防 build 崩。
- **已完成**: 输出 `docs/plans/2026-08-08-删除安全扫描.md`。4 个并行只读子代理采集原始 import 数据，主代理归并归类。
- **归类结果（55 个）**: A=18（可直接删）/ B=10（删文件+连带删测试）/ C=27（不能整删·要拆，保留 _shared helper）/ D=0。
- **核心红线**: 10 个 `_shared.ts`/`_admin-list.ts` helper 被存活生产代码复用，删对应 route.ts 时必须保留（见交付文档附表一）。
- **特殊处理点**:
  - #53 admin-secondary-nav 删组件须同步删 `app-shell/index.ts` 第 9 行死 re-export，否则编译失败。
  - #5 ai-features/[feature_key] 的 route-legacy.test.ts 用 readFileSync 读源码断言，删 route 必连带删该 test。
  - #24/#25 case-library/inbox、counts 共享 `p1-case-library-and-violations.test.ts`，删两 route 后需人工确认整删或部分删。
- **当前状态**: 只读调查完成，交付文档已就绪。
- **未完成线索**: 实际删除操作未执行。删后建议跑 build + test 验证。
2026-08-09 12:00 [CX] 视频复盘减法：下线 ContentDetailDialog 抽屉和复盘台资产筛选/大表，首屏改今日待盘队列；归因舱保留草稿保存并新增复制建议；站内下发/员工已读写操作用开关短路，员工侧反馈卡入口返回空；previous 复测接口放宽到草稿/已下发并返回 source；build 与 npm test 通过，lint 仍卡在仓库既有 17 个 error。
2026-08-09 [CX] 视频复盘减法收尾：确认无生产引用后删除孤儿归属编辑组件与仅被测试调用的反馈卡访问范围 helper；员工端 viewed 收到 delivery_disabled 改提示“站内下发已暂停”，不再误报确认失败；待办记录 previous source confirmed 映射和 copy/previous 非 happy path 测试。
2026-08-09 [CX] 视频复盘减法收尾验证：npm test 1036/1036 通过；改动文件 ESLint 0 error；生产构建完成并生成 Next 产物；两项已删除标识全仓精确检索均为空。
2026-08-09 12:35 [CX] 视频复盘减法独立止损复核：未通过，`content-observations` 仍有管理端路由和库实现，且全仓文档仍含已删组件路径；按红线未继续清尾巴。复核测试当前 1035/1036 通过，失败的是已删除诊断脚本结构的陈旧无障碍断言（`src/lib/a11y-responsive-integration.test.ts`）。
2026-08-09 12:45 [An] 视频复盘减法·前端视觉重构完成：1. 诊断队列卡片流与轻量异常胶囊条上线，排序切换与全量素材库二级入口就绪；2. 纯化行动色，暖橙 #D97757 仅保留给主 CTA「复制建议」，装饰橙全面替换为中性/石青色；3. 下发表单折叠降级为「如需网站内留档可展开」，主推一键复制建议；4. 台词黄金三段式（前3s钩子/中段承接/尾部号召）与段落定位联动就绪；5. 全流程兜底态补齐（归因待数据齐、缺数据、今日待复盘已清完）。全仓 1036/1036 测试通过，build 成功，改动文件 ESLint 0 error。
2026-08-09 13:24 [CX] 视频复盘归因算法上锁：补齐阈值、缺数据、排序、舍入与复制文案兜底测试；confirmed 卡保持未下发的 draft 来源标签，但优先读取人工确认 payload，避免旧草稿覆盖确认结论；`npm test` 1050/1050 通过，改动文件 ESLint 0 error，未改归因阈值/映射表、迁移或下发短路。
2026-08-09 14:20 [An] 视频复盘·归因舱 A+ 改造完成：1. 抽取共享队列计算与排序至 `src/lib/review-queue.ts`，严格锁定打分与排序权重，新增 4 项单元测试（1054/1054 全过）；2. 归因舱顶部内嵌流水线导航条（「第 n / N 条」、上一条/下一条原地刷新切换、队首队尾正确禁用、J/K 快捷键且输入态完全隔离防劫持）；3. 内嵌可收起/展开的紧凑今日待盘队列轨（≤64px、中性高亮、自动滚动居中、1280 悬浮抽屉防挤压、≥1536 停靠排版推开正文两栏 ≥560px、收起满宽消灭 85% 留白）；4. `npm run build`、`npm test`（1054/1054）、`npm run lint`（改动文件 0 error）全部通过，未 push。
2026-08-09 14:40 [An] 视频复盘·列表页态势网格改造完成：1. 单列通栏重构为响应式紧凑卡片墙（窄屏1列 / md 2列 / xl 3列 / 2xl 4列），单卡竖向结构完整容纳状态badge、标题、作者时间、指标异常pills与复盘引导；2. 1280x800 首屏免滚直达 ≥6 张卡，1920 宽屏自然铺满消灭 85% 留白死区；3. 逻辑红线守住：review-queue.ts 零改动、归因舱 A+ 零改动、后端接口零改动、整卡单一可点击元素满足无障碍；4. 清理次要文字「直接去盘」橙色误用为中性色；5. `npm run build` 成功、`npm test` 1054/1054 全过、ESLint 改动文件 0 error，本地未 push。
2026-08-09 15:40 [An] 视频复盘·定制化对比表格重构完成：1. 彻底重写 `src/app/(app)/admin/content/content-list.tsx` 为高密度定制化对比表格；2. 固定核心列（状态灯、视频标题/创作者、发布时间、播放量、涨粉、复盘）+ 切换区二选一（「互动数据」：点赞/评论/分享/收藏；「完播数据」：互动率/2s跳出/5s完播/均播/完播）；3. 所有数字列右对齐并采用 tabular-nums 等宽对齐与大数/百分比/均播秒数单位缩写，null 安全显示「—」；4. 本地 state 支持全列表头点击快速排序（默认发布时间降序，首点降序，再点升序）；5. 表头 sticky 吸顶、行紧凑（py-2.5），1280×800 首屏可直观对比 15+ 行；6. 严格坚守橙色纪律（唯一行动变橙 #D97757，分段控件与排序保持中性冷灰）；7. 零动 review-queue.ts（git diff 为空）、归因舱 A+ 零动、后端零动；8. `npm run build` 成功、`npm test` 1054/1054 全过、ESLint 改动文件 0 错，真实浏览器 Chromium 截图（1280 与 1920）已采集。
2026-08-09 15:55 [An] 视频复盘·宽屏 14 列全展开与留白消除完成：1. 宽屏 (≥1536px) 隐藏切换器，自动全展开 14 项指标列（点赞/评论/分享/收藏/互动率/2s跳出/5s完播/均播/完播）；2. 窄屏 (<1536px) 保留分段切换器（互动率归入互动组）；3. 视频标题列设为 `w-auto` 弹性吸收所有富余横向空间，表格 `w-full` 彻底吃满容器消除右侧空白；4. 骨架屏与空态 colSpan={15} 全自适应；5. `npm run build` 成功、`npm test` 1054/1054 全过，真实浏览器 1920 与 1280 截图已保存。
2026-08-09 16:05 [An] 视频复盘·标题列宽收敛与全表比例均衡优化：1. 针对 1920 宽屏标题列占比过宽问题，将标题列宽收敛至 `w-[220px] 2xl:w-[280px]`（占比从约 42% 降至约 17% 的舒适阅读区）；2. 横向富余空间均匀分配给 12 个指标列与时间列（宽屏下指标列加宽至 `2xl:w-22 ~ 2xl:w-28`，内边距增至 `2xl:px-3`），数字纵向对齐更清晰、呼吸感更强；3. `npm run build` 成功、`npm test` 1054/1054 全过，已更新 1920 真实浏览器截图。
2026-08-09 16:10 [An] 视频复盘·单行极致紧凑与行高降维优化：1. 标题与创作者从双行拆解重组为单行「标题 · 账号名」（去除冗余用户名，表头同步更新为「视频标题 / 账号」）；2. 全表单元格纵向内边距收敛为 `py-2`，单行高度压降至约 32px；3. 1280x800 首屏一屏可直观对比 20+ 行视频数据，1920 宽屏可同屏对比近 30 行，纵向扫描效率大幅提升；4. 骨架屏同步压为单行高度；5. `npm run build` 成功、`npm test` 1054/1054 全过，真实浏览器 1920 与 1280 截图已刷新。
