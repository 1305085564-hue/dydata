# 2026-09-26

[CX] 2026-09-26：按阿禅要求将 B 档拆为两份独立代理实施方案：`docs/plans/2026-09-26-B1应用行为与权限实施方案.md`（OCR 原值恢复、岗位管理只读视频复盘权限、异常处罚类型清理）和 `docs/plans/2026-09-26-B2数据库与维护性欠账实施方案.md`（P1 DB/RPC 只读盘点与候选清理、A-21/B-16 到期观察、维护性缺陷、大文件拆分）。B1 权限项冻结写权限和公司范围；B2 数据删除项要求生产只读证据，A-21/B-16 未到 2026-10-18 只可准备不可裁定。仅新增方案文档，未派代理、未改代码、未回写待办。

[CCmd] 2026-09-26 10:11：`/growth`（导航名「数据分析」）按阿禅拍板**整页删除**，从原定「待重做」改为直接下线。删除 25 个文件 + 1 个测试（页面 8 个、`components/growth/*` 7 个、`components/charts/*` 4 个、`loaders/growth-page` 3 个、`lib/growth-page` 2 个、`lib/趋势图` 2 个、`/api/dashboard/trend`、`trend-leaderboard` 孤儿测试）。榜单组件 `components/leaderboard/leaderboard.tsx` 与 `/api/dashboard/leaderboard` **保留待接入口**，已在 `content-feedback-removal.test.ts` 加守卫防误删；「排行榜接回前端入口」计入 `docs/待办清单.md`（硬约束以该条为准：入口并入数据管理、取数改用 `resolveCollaborationScope`、不得再用文案冒充成长反馈、接入时补真实浏览器验收）。
[CCmd] 2026-09-26 10:11：随之清理的连带引用：工作台「复盘 / 成长复盘」两条失效入口当时被临时改指 `/admin/collaboration`（⚠️ 该处理已被后续审查判定为「把数据管理冒充成长反馈」，最终处理见本文件末尾收口条）；`revalidatePath("/growth")` ×3、导航「管理中心 → 数据分析」、middleware 的 `isGrowthRoute` 与 matcher、手机底栏 `includes("growth")` 判定、埋点路径白名单、性能脚本目标、7 个测试文件同步收口。**顺带删除存量死代码**：`shouldAutoRedirectToGrowthAfterSubmit` 与 `shouldAutoRedirectAfterSubmitRef` 早已只写不读（「提交日报后自动跳转成长复盘」实际从未生效），测试却仍在替它背书，本次一并清除 3 项测试。
[CCmd] 2026-09-26 10:11：行为变更（需知晓）：普通组员（`member`）**不再看到「管理中心」分组**——该组原有 6 个子项全部有权限门槛，`/growth` 是唯一全员常驻项；组员现只看到「工作台 / 选题库 / 数据管理」，已写入模块地图「分组按子项权限显隐」。验证：`npx tsc --noEmit` 0 错；`npm test` 1767/1767 通过；`npm run lint` 0 error（改动文件仅余存量 warning）。文档同步：模块地图（总览/连累/导航/第四节废弃表）、待办清单、工程运行事实（loader 清单/冻结条/性能预算表/性能债）、数据口径（第 9 节标废弃）、权限与安全说明（登录闸机页面清单）、命名对照表；`docs/reference/个人成长数据契约.md` 归档到 `docs/archive/`。未提交、未 push。
[CCmd] 2026-09-26 10:35：阿禅反馈「系统设置页面很多功能失效」，按只读方式核查两个设置面，**未改任何代码、未写线上库**。① `/admin/settings`（导航「系统设置」）**功能正常**：浏览器实测 5 项阈值输入改动后 `保存阈值配置` 由灰转可点，走 `PATCH /api/admin/settings/thresholds`；产量目标读 `get_daily_quota` RPC + `daily_quota_config`，写走 `POST /api/daily-quota-config`。线上库只读核对：两表存在、RPC 返回 6、`created_by→profiles` 外键在、RLS 已开、`company_owner` 同时具备 `manage_system` 与 `manage_fulfillment`（`src/lib/permission-contract.ts:59-71`），写权限链路通。注意点：线上 `system_settings` **无 `video_review_thresholds` 行**，故页面显示系统默认 30/50/30/5/1000（不是配置丢失）；`保存阈值配置` 初始为灰是「无改动不启用」的设计，非故障。② **真失效在「账号与设置」弹窗 → 「系统参数配置」标签页**（`src/components/premium-settings-modal.tsx:154-155`）：`pushEnabled` 与 `remindHour` 是纯前端 `useState`，全仓库 grep 无任何读取方、无接口调用；浏览器实测关掉开关并刷新页面后复位为「开 + 11:15」，即改完不保存、刷新即失效。该标签页文案承诺「配置日常催交、违规提醒和周月报统计参数」，实际既无违规提醒也无周月报参数。**真实催交总开关另在 `/admin/fulfillment`（「飞书每日提醒开关」，当前 checked）**，写 `system_settings.feishu_fulfillment_reminder_enabled`（线上值 `true`，经 `/api/admin/system/settings`）——两处开关互不相通，弹窗里关掉不影响实际推送，属伪接。③ 「矩阵账号管理」标签页文案写「可新增、解绑或重命名」，但实测仅有 `新账号/修改账号名/修改备注/切换为该账号`，**无解绑入口**。线索：本地已有 dev server 占用 3000（PID 82193，非本次启动），本次仅只读点击，未触发任何保存。未提交、未 push。
[CCmd] 2026-09-26 11:10：按审查结论对 `/growth` 下线批次做**收口修正**（推翻 10:11 的临时处理）。① 工作台提交后的两条失效入口**整体删除**：`video-submit-panel-v2.tsx` 与 `video-submit-form-v2.tsx` 里的「数据管理」按钮，连同 `handleGoToDataManagement`（原「复盘 / 成长复盘」）一并移除，不再把 `/admin/collaboration` 冒充成成长反馈；保留「留在工作台 / 修改今日数据 / AI 检查样本质量 / 挑选明日选题」四个真实可用操作。② 改写仍承诺「成长复盘」的活跃文案：`src/app/layout.tsx` 三处 description（default / openGraph / twitter）由「抖音数据日报、运营分析与成长复盘平台」改为「抖音数据日报与运营分析平台」；`src/app/opengraph-image.tsx` 副标去掉「· 成长复盘」；工作台落款副标「数据沉淀与成长复盘」→「数据沉淀与运营复盘」（连带更新锁它的 `dashboard-humanistic-copy.test.ts`）；`editorial-illustrations.tsx` 适用注释去掉已删页面。③ 新增 `src/lib/growth-removal.test.ts` 五项定向回归：页面与专属实现保持删除、活跃代码零 `/growth` 路由字符串、工作台不得出现 `/admin/collaboration` 或「成长复盘」、产品元数据不得再写成长复盘、排行榜只保留文件且页面与导航均未引用（明确本批未接入）。④ 文档与待办同步：模块地图废弃表改为「失效入口已删除 / 不得冒充成长反馈」并写明排行榜当前无入口、本批未接入；待办硬约束由三条改四条（新增「不得用文案冒充成长反馈」与「接入时补真实浏览器验收」）。**排行榜本批未接入，未改任何排行榜代码、RPC、migration 或 RLS。**
[CCmd] 2026-09-26 11:23：`/growth` 下线批次**已提交并推送**。commit `3030bad279d38639702a3e0c364b9c16f6b47751`（短 `3030bad2`；63 files changed，+279 / −6945），push `4374a33e..3030bad2 main -> main`，本地 HEAD 与 `origin/main` 一致；提交信息 `refactor(growth): complete page removal cleanup`（依仓库惯例带 Co-Authored-By 尾注）。门禁留痕：`npm run gate:static` exit 0（test + create-test-users + tsc + lint + build）；`npm run gate:browser` exit 0（真实登录态，四个代表页 1 passed / 36.7s）；`git diff --cached --check` 通过；`git diff --cached --name-only` 63 个文件逐项核对，未混入其他 AI 文件。全程未用 `git add -A` / `git add .` / `git add -u`。**未取证项**：日报提交成功态没有跑真实浏览器（浏览器门禁只覆盖四个页面首屏，走不到提交成功这一步），已写入 `docs/待办清单.md`，不得用静态检查冒充通过。**工作区遗留（未暂存、未提交）**：`docs/plans/2026-09-26-文案助手处置方案.md`、`docs/plans/2026-09-26-管理中心系统设置清理实施方案.md`、`docs/reviews/2026-09-26-全站产品深度审计与瘦身路线.md`、`日志/2026-09-26-补充.md`（[CX]）；同期另有代理在同一工作区做文案助手下线（已暂存一批 `rewrite` 删除并改动 `nav-bar-items.ts` 等），与本批次互不干涉。
[CX] 2026-09-26 11:35：文案助手 `/content-tools/rewrite` 按阿禅拍板**下线并删除功能代码**。阿禅 2026-09-26 明确作废 2026-08-26 的「重做前不可当废代码删」条款（该条款记录于模块地图 100/106/223、工程运行事实 26/75、待办清单 39/40）；决策记录与逐条审查对账见 `docs/plans/2026-09-26-文案助手处置方案.md`（v3，含第十三节施工结果）。① **入口**：`nav-bar-items.ts` / `nav-bar.tsx` / `nav-bar-client.tsx` 三处移除 `use_ai_copy` 入口块与 `showAiCopywriting` 传参。② **页面**：`/content-tools/rewrite` 改为「已下线」提示页，保留登录与权限 redirect；**middleware 采用方案甲、零改动**（matcher 与 `isProtectedAppRoute` 均保留，避免审查 P0-2 的「删保护却留 matcher」矛盾）。③ **删代码 43 个文件 / 9185 行生产 + 1944 行测试**：`src/lib/rewrite/`(12)、`src/app/api/rewrite/`(13)、`src/app/api/content-tools/`(4)、`src/components/content-tools/`(14)。④ **连带清理**：`api-path-validation.ts` 5 条 UUID 规则、`usage-events/shared.ts` 的 `rewrite_generate` 事件与 `/content-tools` 埋点前缀、`api-rate-limit.ts` 的 `rewrite/generate` 10/分规则、`perf-full-sweep.mjs` 两个扫描目标、`layout.tsx` 注释、`mobile-tab-bar.tsx` 的 `includes("rewrite")` 判定。⑤ **测试护栏反转为「断言不存在」**（不是整段删）：`nav-bar-items.test.ts`、`usage-events/shared.test.ts`、`a11y-responsive-integration.test.ts`、`content-feedback-removal.test.ts`、`windows-adaptation-second-batch.test.ts`；另修 3 个引用已删文件的测试（`permission-architecture.test.ts`、`api-rate-limit.test.ts`、`api-path-validation.test.ts`）——后 3 个与 `mobile-tab-bar.tsx` 是 v2 方案漏项，因原 grep 模式未覆盖裸 `rewrite` 而漏检，施工中补出。⑥ **数据库零动作**：17 张 `rewrite_*` 与 `ai_*` 配置表全部原地保留（不删/不改/不停用）；`rewrite_model_views`/`rewrite_model_routes` 仍由 AI 配置中枢读写，`ai_providers` 系列仍由截图 OCR 使用，**严禁清理**。⑦ `use_ai_copy` 权限键保留在固定角色模板中（已无页面用例，待后续批次）。**实测**（现有 dev server :3000，未登录 GET）：`/content-tools/rewrite` → 307 跳 `/login`（保护未削弱）、`/dashboard` → 307、`/api/rewrite/generate` 与 `/api/content-tools/rewrite/bootstrap` 与 `/api/rewrite/documents/*` 与 `/api/rewrite/skills` → 全部 404、`/api/admin/ai-config` → 401（存在且需鉴权，共享模型配置未误伤）。静态：`tsc` 与 `lint` 在本次 18 个改动文件上零问题、`npm test` 1716/1722 通过。**未取证（不得用静态检查冒充通过）**：三身份导航显隐、提示页登录态渲染、归档账号拦截、截图 OCR 冒烟、`npm run gate:browser`——因同期另一代理的 `src/lib/video-review-thresholds.ts` 处于半成品状态、生产构建被其阻断（4 个构建错误全部指向该文件）而无法执行；待该文件收口后需补跑 `npm run gate:browser`。文档同步：模块地图（总览行/模块段/限流/导航子项数/第四节废弃表）、工程运行事实（领域目录/下线事实/性能债）、待办清单（冻结条与历史会话条关闭、051-062 条改写）、权限与安全说明（`use_ai_copy` 两处）、命名对照表（C5/D4/D5/K5 与权限表）。**已提交并推送**：commit `a94cbb7b`（68 files changed，+480 / −11256），push `af21b578..a94cbb7b main -> main`，本地 HEAD 与 `origin/main` 一致；提交信息 `refactor(rewrite): 下线文案助手，删除功能代码并保留数据表`（带 Co-Authored-By 尾注）。**提交方式**：因暂存区同时存在另一代理的 7 个删除（系统设置批次：`admin/settings/*`、`api/admin/settings/thresholds/*`、`api/daily-quota-config`、`loaders/admin-settings-page*`），采用**路径限定提交**（`git commit -- <显式路径>`）只提交本次 68 个文件，未用 `git add -A`，对方暂存状态未被改动。**push 前已知风险**：`gate:browser` 未能执行，属用户明确授权后推送。审计报告 `docs/reviews/2026-09-26-全站产品深度审计与瘦身路线.md` 属另一代理的未跟踪文件，**本次未改动**，以免混入他人提交；「待拍板事项 2（文案改写）」已由本次落地，以模块地图的现行事实为准。
[CCmd] 2026-09-26 12:05：`docs/plans/2026-09-26-管理中心系统设置清理实施方案.md` **方案 A 执行完成**（阿禅「方案 a，执行吧」）。**未提交、未 push**；未改任何历史 migration、未删任何数据库对象或数据行。
**[阶段1 冻结范围 · 线上只读证据]** `system_settings` 只有两个 key：`feishu_fulfillment_reminder_enabled`（`true`）、`topics_feishu_workspace_url`；**从未写入过旧阈值 key**，故按方案 §3 把样本下限固定为默认 `play_count = 1000`，其余四项沿用代码默认 30/50/30/5。`daily_quota_config` 2 行（`2026-07-14`→6、`2026-07-07`→4，`created_by` 均为 NULL）；`get_daily_quota('2026-09-26')` = 6；`created_by→profiles` 外键在、两表 RLS 已开。
**[阶段2 代码闭环]** 删除 7 个文件：两个面板（`thresholds-config-panel.tsx`、`quota-config-panel.tsx`）、`lib/loaders/admin-settings-page.ts` + 其测试、`api/admin/settings/thresholds/route.ts` + `route.test.ts`、`api/daily-quota-config/route.ts`。改写：`admin/settings/page.tsx` 改为**保留 `manage_system` 闸机的重定向页**（未登录→`/login`，无权限→`/admin`，有权→`/admin/fulfillment`）；`video-review-thresholds.ts` 删掉 DB key 常量、`parseVideoReviewThresholds`、`normalizeVideoReviewThresholds`，只留**代码内固定常量 `VIDEO_REVIEW_RULE_THRESHOLDS`**（`DEFAULT_` 前缀一并改掉，避免再被当成可配置项），测试改为「固定规则闸门」；`review-queue.ts` / `review-queue.test.ts` / `content-list.tsx` 跟随改用固定常量，`content-list.tsx` 删除阈值接口请求与可配置 state，样本不足降灰改读固定 `play_count`；导航 `nav-bar-items.ts` 移除 `/admin/settings` 项（**`/admin/ai-config` 保留**），`nav-bar-items.test.ts` 断言反转为「系统设置不显示、AI 配置仍显示」。**刻意保留不动**：`route-permissions.ts` 的 `/admin/settings: ["manage_system"]`、`admin/layout.tsx` 的标题与 owner-only 判定（旧址拒绝卡文案仍归它），保证旧址闸机不削弱。
**[阶段4 定向验收 · 全部有真实证据]** ① 方案 §6 三条命令：定向测试 32/32 通过；`npx tsc --noEmit --pretty false` 0 错；验收 grep（`daily-quota-config|get_daily_quota|video_review_thresholds|视频复盘与素材库异常警戒阈值|产量目标管理` 外加 `admin-settings-page|ThresholdsConfigPanel|QuotaConfigPanel`）在 `src/` **零命中**——为让这条闸门今后任何命中都等于真实回归，特意把注释里残留的字面键名改写掉。② 全量 `npm test` 1715/1715 通过；`npm run lint` 0 error（47 条存量 warning）；**`npm run build` exit 0**。③ HTTP 实测：`/api/daily-quota-config` → 404、`/api/admin/settings/thresholds` → 404、`/api/admin/system/settings` → 401（保留且需鉴权）。④ 真实浏览器（owner 阿禅，只读操作）：访问 `/admin/settings` 最终落地 `/admin/fulfillment`；导航「管理中心」只剩 发布管理/成员管理/AI 配置；`/admin/content` 加载正常（异常提醒 263 条、最需关注、「直接去盘」成功打开视频工作舱 `5d13784e`、URL 带 `videoId`）、**样本不足降灰 25 格仍在作用**、控制台无报错；`/admin/fulfillment` 飞书开关读取 `checked=true` 与线上库一致。⑤ 真实角色：组长（`admin`，有 `manage_fulfillment` 但**无** `manage_system`）访问 `/admin/settings` 得到拒绝卡（「仅对企业管理员（公司所有者）开放」）、**没有被重定向进发布管理**，导航只有 发布管理/成员管理——权限边界未被本次重定向削弱。
**[未取证项 · 不得用静态检查冒充]** 飞书开关的**保存腿**没有在真实页面点击：拨动它会真的改线上 `feishu_fulfillment_reminder_enabled`，进而改变每日催交推送行为，故只做读取核对；保存链路改由 10 项单元测试背书（`api/admin/system/settings/route.test.ts` + `fulfillment-settings.test.ts`：POST 写入开关、缺表明确失败、只允许公司所有者、无权限不发起请求）全通过。
**[数据库零动作]** 不删 `daily_quota_config` 表、`get_daily_quota(date)` RPC、2 行历史规则，不删 `system_settings`（飞书开关与选题库工作区链接仍在用），不改历史 migration。
**[文档回写]** `docs/全站模块地图.md`：总览表 系统设置 行改「⛔ 重定向」、原「系统设置」节改写为退役说明、地基层接口目录移除 `/api/daily-quota-config`、第四节废弃表新增退役行（含「不要按本行删数据库对象」警告）、顶部核对范围补本次、管理中心子项数 4→3；`docs/工程运行事实.md`：loader 例外清单移除 `admin/settings`；`docs/待办清单.md`：新增「系统设置退役的数据库残留（待单独批准）」。
**[给后续代理的两条线索]** ① `a94cbb7b` 之后工作区只剩本批次改动，未混入其他 AI 文件。② [CX] 11:35 记录的「构建被 `video-review-thresholds.ts` 半成品阻断、导致三身份导航/提示页/`gate:browser` 未取证」——该文件已随本批次收口，`npm run build` 现已 exit 0，那条未取证项可以补跑了。
[CX] 2026-09-26 12:10：独立复核方案 A：定向导航/权限/视频复盘测试 32/32 通过，`npx tsc --noEmit --pretty false` 通过，`npm run build` 通过；旧接口、旧配置键、旧面板和“素材库异常阈值/产量目标”现行代码引用为 0。发现并修正发布管理筛选栏残留文案，将“系统设置中统一配置”改为“发布管理中统一配置”。未提交、未 push；未做线上飞书开关保存、数据库清理或全量浏览器门禁。
[CX] 2026-09-26 12:15：按阿禅授权执行数据库清理前置核对：生产库旧 `video_review_thresholds` key 不存在，`daily_quota_config` 与 `get_daily_quota(date)` 存在且无其他数据库对象依赖。新增 migration `20260926121500_remove_retired_system_settings.sql`，准备删除旧 key、函数和表；保留 `system_settings` 表及现行设置。
[An] 2026-09-26 16:50：按阿禅要求将 **`[ 🏆 账号排行 ]` 按钮还原回控制栏右侧**（与达人/运营/文案/剪辑同处一行右端），同时**保留体验极佳的大画幅中心弹窗（Center Modal）**。实测弹窗宽度（972px+）舒展完整、全指标无水平挤压、Esc 秒关、工作流零中断；全量 1716 项测试通过、tsc 0 错。
[CCmd] 2026-09-26 17:52：阿禅问「800 行以上是否必须拆」，遂对 4 个最大文件做只读结构体检并核对 09-16/09-17 既有可维护性专项，**结论已记入 `docs/待办清单.md` 新增的「P2 代码可维护性欠账（2026-09-26 结构体检，待 Codex 评估）」，本项交 [CX] 评估后再动，阿禅明确「不用你（CCmd）做」**。体检要点：① 「800 行必须拆」无权威依据，且与 09-16 专项「不以行数为验收／不按行数上限继续拆分」的既定边界相反；② 依赖方向反向的真实范围比预想大——`src/lib` 5 个生产文件 import `src/app`（即专项注明「归权限专项」的那 5 个）、`src/components/premium-settings-modal.tsx` 1 处、另 4 处反向 import 在测试里；③ 测试方式问题最大：324 个测试文件中 48 个读源码文本做字符串断言、真实渲染/操作测试 0 个；④ 4 文件体检分级＝`unified-command-hub.tsx` 该拆（291 行零 React 依赖纯逻辑 + 3 DTO，含 `lib` 反向依赖，已有 8 条测试）、`video-submit-form-v2.tsx` 与 `modules-content-v3.tsx` 可选（仅展示层能干净切，state/handler 层切不干净）、`lib/topics/service.ts` 不该拆（单一「选题库」域，34 导出函数职责一致，硬拆要改 16 处调用点）；⑤ 09-16 专项 4C 拆出的 `use-content-comparison.ts` 已随 `fc829049`（视频复盘与素材库合并）整体删除，说明按行数拆的收益不稳定。**全程只读，未改任何代码、未跑功能测试，故「有无未登记的功能 bug」未取证**，不得据此判定代码无缺陷。
[CCmd] 2026-09-26 18:00：按阿禅要求更新 `docs/reviews/2026-09-26-全站产品深度审计与瘦身路线.md`，反映**成长分析已整页删除、文案助手已下线**的现状。改动 8 处（纯文档，未动任何代码）：① 文首新增「执行进展（2026-09-26 更新）」段；② 总览表两行核心状态改为「⛔ 已下线 / 已整页删除」；③ 第 2 节成长分析裁决改为「已执行—整页删除」并补执行结果 7 条（入口摘除、25 文件删除、`growth-removal.test.ts` 护栏、组员不再见「管理中心」、排行榜保留并入数据管理、日报提交成功态未取证）；④ 第 3 节文案助手裁决改为「已执行—下线并删功能代码」，注明**实际走的是原方案第三条兜底「直接关停入口」，未走「极简重构」**，补执行结果 7 条（含 17 张数据表原地保留、middleware 零改动、`use_ai_copy` 权限键遗留）；⑤⑥ 两处 mermaid 改写（第三梯队「推倒重做区」→「已下线区（已出清）」、Phase1 标注已完成 2/3）；⑦ 第一阶段清单 1/2 打勾、3 保留待办；⑧ 待拍板表 1/2 改为已拍板已执行、末段「下一步建议」改写。**核实依据**：`/growth` 目录已不存在、`/content-tools/rewrite/page.tsx` 为「已下线」提示页、`nav-bar-items.ts` 已无 growth/rewrite/文案 入口、`leaderboard-tab.tsx` 存在、提交 `3030bad2` 与 `a94cbb7b` 均已在 origin/main。**刻意未改（登记待阿禅定夺）**：总览表「系统设置」行仍为「🟡 需收口」，但该页已于 2026-09-26 退役（改重定向，见 `cf2da41d`）；第 9 节「AI 配置中枢」仍写「保留文案与 OCR 必要的模型 Key 配置」，其中「文案」所指的文案助手已下线。
[CCmd] 2026-09-26 18:05：按阿禅要求「其他文档同步更新」做了全库文档对齐审计。**结论：§1 当前事实入口对两个页面的同步其实已经做过**（模块地图 / 工程运行事实 / 数据口径 / 权限与安全说明 / 全站命名对照表 / 待办清单 均已标「已下线·已删除」，`项目事实.md`、`可维护性维护入口.md` 零命中）——不是没做，是 dispersed 在不同提交里。**本轮实际修了 2 份仍有残留的当前事实文档**：① `docs/页面功能/数据管理/数据管理.md`（最后核对 09-24 → **09-26**）：L265「这些标准可能在**系统设置**里被调整」改为「固定在代码内 `VIDEO_REVIEW_RULE_THRESHOLDS`、无页面入口」；L444「**系统设置**：指标标准影响本页面告警」改为标明 2026-09-26 已退役为受 `manage_system` 保护的重定向。② `docs/权限与安全说明.md`：L259 `manage_system` 说明从「能否改系统参数」改为「仅守住闸机（无系统参数可改，实际把关 `/admin/ai-config`）」，Q1 标题改为「打不开 `manage_system` 保护的页面」并加前置说明「打开 `/admin/settings` 落到发布管理是预期行为、不是权限故障」。**核实依据**：`src/lib/video-review-thresholds.ts:10` 存在 `VIDEO_REVIEW_RULE_THRESHOLDS`、`src/lib/route-permissions.ts:16/24` 的 `manage_system` 同时守 `/admin/settings` 与 `/admin/ai-config`；全部为文档改动，未动代码。**按规则未动（`docs/文档维护入口.md` §2/§4.5：reviews/plans 是证据不是当前事实、归档须单独确认）**：3 份带日期的历史报告仍把两个页面写成在役——`docs/reference/真实链路验收-管理端-2026-08-28.md:72/173`（导航含 `/growth`、系统维护、文案助手）、`docs/reference/ux-product-page-audit-2026-08-28.md:30/96/189`（员工入口含 `/growth`）、`docs/reviews/2026-09-22-数据上传心流直觉化-Antigravity方案-WorkBuddy审查.md:223`（提到已被删的「成长复盘」按钮）；**是否加时点说明或移入 `docs/archive/` 待阿禅确认**，本轮只报告不改写，避免篡改历史证据。另记一处**同文件内但属别的待办**未动：`数据管理.md` L211/L433 的「投流 / 活动干预」属已登记的「异常处罚类型废弃项清理」（待排期全仓扫描），不在本轮范围。
[CCmd] 2026-09-26 19:17：按阿禅指令修复历史 `062_ai_rewrite_unified_view.sql` 空库重放阻塞（历史记录见 `日志/2026-09-13.md`）。**改动**：删除整个 `rewrite_length_preset` UNION 分支（15 行，含 SELECT 与 FROM），并把 `rewrite_fixed_mode` 分支 metadata 里残留的 `'length_preset_id', rfm.length_preset_id` 一并删除（否则验收 grep 无法 exit 1；该 view 全仓零消费方）；feature / rewrite_model_view / rewrite_mode / rewrite_workflow / rewrite_fixed_mode 五个分支结构、GRANT、rollback 注释均未动。**验收**：`rg -n "rewrite_length_presets|rewrite_length_preset|length_preset_id" <062>` → exit 1 ✅；本文件 `git diff --check` → exit 0 ✅（全仓 `git diff --check` exit 2 的两处属他人未提交改动：`src/app/api/dashboard/leaderboard/route.ts`、本日志文件，非本任务）；`npx supabase db reset --local` → **BLOCKED**（本机 Docker daemon 未运行，原样错误 `failed to connect to the docker API at unix:///var/run/docker.sock`，未伪称通过）。**替代取证**：用本机 PostgreSQL 16 在 scratchpad 起隔离库重放真实文件（039-041 + 044/045/046/047 + 051 + 062）：修复前 `ERROR: relation "public.rewrite_length_presets" does not exist`（复现历史阻塞），修复后错误位移为 `ERROR: relation "public.rewrite_workflows" does not exist`。**由此暴露范围外事实（需阿禅/主窗口扩权）**：051 除 `rewrite_length_presets` 外还 DROP 了 `rewrite_fixed_modes`（051:11）与 `rewrite_workflows`（051:20），而 062 的 `rewrite_workflow`、`rewrite_fixed_mode` 两个分支仍引用它们——**本次授权禁止删这两个分支，故只能消除 3 个阻塞中的 1 个，全量 `db reset` 仍不可能通过**；另 `20260629000000_rewrite_skills_and_documents.sql:640/649` 无 `to_regclass` 守卫直接从 `rewrite_fixed_modes` 取数，空库重放同样必失败（静态确认，未做全链重放）。**边界**：仅暂存 `supabase/migrations/062_ai_rewrite_unified_view.sql`（`git diff --cached --name-only` 只有它），未 commit、未 push；未改 044/046/051、未新增 migration、未处理线上数据库、未改 SQL 逻辑迁就本地环境；隔离库已停止并从 scratchpad 清理。
[CCmd] 2026-09-26 21:40：按阿禅 6 批次指令完成「依赖方向收口 + 测试方式治理 + 大文件拆分设计」，共 6 个独立 commit 已 push main（`1f940b22` D-01 权限核心反向依赖 / `f64cb9fc` D-02 管理员纯策略 / `60bba90c` D-03 账户 action / `0a88c65c` T-01 跨层纯业务测试 / `19effef5` T-02 测试治理清单+首批改造 / `cbfc9f0b` S-01 拆分方案；origin/main == 本地 HEAD）。**交付**：① `src/lib` 5 个生产文件 + `premium-settings-modal.tsx` 全部不再 import `src/app`（旧入口保留兼容：`auth-helper.ts` 纯 re-export、`权限管理.ts` re-export、dashboard/actions.ts 与 app/actions/auth.ts 走最小接线）；② 测试反向依赖：`groupPendingApprovals` 及其类型/`formatShortDate` 迁入 `src/lib/exemption-approvals.ts`，`src/lib` 不再 import `@/components/*`；③ 新增 `docs/plans/2026-09-26-测试方式治理分批清单.md`（71 个读源码测试文件：A 保留 51 / B 可改 5 / C 需渲染 15）与 `docs/plans/2026-09-26-代码可维护性大文件拆分方案.md`（4 个大文件只读设计，`service.ts` 结论为不拆）。**过程决策（需知悉）**：① D-03 实测 Turbopack **禁止**在 `"use server"` 文件里 `export ... from`（报 Only async functions are allowed，且整模块导出表失效）→ 按指令备选方案改用最小接线 async 包装函数；② T-02 复核后把 `rate-limit` 形态断言**从 B 改判 A 并保留**（性能形态运行期不可观测，删除即净损失保护）；③ T-01/T-02 的 5 个转换逐条做了**变异检查**（改坏生产行为 → 断言必须红），M1–M5 全部验红且文件逐字还原。**门禁（全绿）**：`npm test` **1718/1718**；`npm run lint` **0 error / 47 warning**（其中仅 1 条落在本批文件 `unified-command-hub.tsx:293`，已用 HEAD 版本复核为预存）；`npx tsc --noEmit` 0 错；`npm run build` exit 0。**未做/待办**：C 类 15 个仍需渲染/浏览器，本批未引入任何浏览器框架；4 个大文件的拆分**只出方案未实施**；`supabase/migrations/062_ai_rewrite_unified_view.sql`（上一批）仍**只暂存未提交**，等阿禅对「051 另删 rewrite_fixed_modes / rewrite_workflows、062 仍引用」的范围决策；`docs/待办清单.md` 的「5 处反向依赖 / 48 个源码测试 / 4 个大文件」三条本轮未回填（该文件有他人未提交改动，避免混入）。
[CCmd] 2026-09-26 20:40：062 空库重放修复**触发停止条件、停止施工**。线上只读核对（`db.gcrhhxaopomtposmahsw.supabase.co:5432/postgres`，psql 会话 `default_transaction_read_only=on`，未打印凭据）：三张表**均仍存在**——`rewrite_fixed_modes` 2 行 / `rewrite_length_presets` 3 行 / `rewrite_workflows` 1 行；视图 `ai_unified_config_view` 当前 6 类共 20 行（feature 7 / rewrite_fixed_mode 2 / rewrite_length_preset 3 / rewrite_mode 3 / rewrite_model_view 4 / rewrite_workflow 1）。**根因（不是 062）**：线上 `schema_migrations` 记 051=`leader_daily_reports`（2026-05-06 `d9d585fb` 已推送线上）；本地 `051_cleanup_deprecated_tables.sql` 是 2026-07-03 `4dca8a64` 新增的、**占用了已被线上占用的版本号 051**，2026-07-25 `8903039e` 又把线上已应用的 leader 迁移改名成 `051b_leader_daily_reports.sql`（R100）。结果：该清理迁移**永远不会被 push 到线上**（版本 051 已存在），但在空库/新环境会按序执行 → 「线上有表、空库无表」的漂移，空库重放必失败（先前孤岛 PG16 重放实测：原 062 报 `rewrite_length_presets` 不存在，改后顺延报 `rewrite_workflows`、`rewrite_fixed_modes`）。**本任务改动：0 个文件**（停止条件命中，未碰 `20260629000000`）。工作树里 062 的半修**仍只暂存未提交**（只删了 1/3 个分支，且与线上现状相反），建议**不要提交**，等版本撞号决策。**验收**：① 非 docs 应用代码引用三表 = 0 命中（`rg src scripts tests supabase/seed.ts *.ts *.json *.mjs` 退出 1）✅；② `npx supabase db reset --local` = **BLOCKED**（Docker 守护进程未运行：`failed to connect to the docker API at unix:///var/run/docker.sock`），无退出码；③ `git diff --check` 退出 2，但两条均为他人预存改动（`src/app/api/dashboard/leaderboard/route.ts:107`、`日志/2026-09-26.md:25`）。**待阿禅拍板**：把废弃表清理改成**新前向 migration**（本任务禁区，需授权）→ 线上真正删表后，062 与 20260629000000 删分支才是两个环境都正确的做法；否则只能二选一（保线上现状 / 保空库可重放）。
[CCmd] 2026-09-26 23:05：废弃表清理的空库重放修复**已完成并推送**（commit `9820ecb8`，origin/main 已核验）。**线上只读核对**：三张废弃表仍存在（2/3/1 行，2026-04-14~15 后零写入）→ 触发停止条件；但顺藤摸到**根因**：本地 `051_cleanup_deprecated_tables.sql` 占用线上已占用的版本号 051（线上 051=leader_daily_reports），该清理**从未落到线上**却在空库执行，造成「线上有表、空库无表」漂移 → 062/20260629000000 空库重放必失败。**改动（历史 migration 直接修改＝本任务授权例外）**：① 062 删掉 3 个引用已删表的分支，保留 3 个存活分支 + GRANT + rollback 注释；② 20260629000000 删除 §8.2 从废弃表搬数据的旧分支（线上早已搬完，不伪造空数据）；③ **新增前向迁移 `20260926130000_drop_deprecated_rewrite_tables`**：先重建视图去引用 → 动态摘外键 → 按序删表（`rewrite_workflow_steps` 必须先于 `rewrite_workflows`，前者 RLS 策略 EXISTS 引用后者——这条是**重放实测抓到的真实阻塞点**）；不用 CASCADE、空库 no-op 幂等、含表定义来源＋10 行数据快照回滚 recipe；④ 新增静态测试 8 条钉住上述不变量。**验证**：Docker 未安装 → `npx supabase db reset --local` 仍 **BLOCKED**（无退出码），改用隔离 PG 集群重放真实迁移文件——空库链路 040→044→045→046→047→051→062→新迁移 全通过、三张表最终不存在、视图只剩 3 存活分支；线上升级链路（先造出 6 分支视图 + 4 外键 + 数据的线上现状）→ 迁移成功、表删、视图留、外键归零、引用方列保留、可重复执行幂等。`npm test` **1726/1726**、`tsc` 0 错、`eslint` 0 报错、本批 `git diff --check` 0、非 docs 应用代码对四张表零引用。**范围说明**：为实现授权内的三表清理，额外删掉同为 051 废弃清单成员、应用零引用且休眠的 `rewrite_workflow_steps`（否则必须改写/删除存活表上的 RLS 策略——安全性决策，未擅自做）。仍有 `rewrite_variants` / `sop_review_scores` 两张 051 废弃表留在线上，无任何引用，待后续单独授权。**待上线**：仓库无 CI、无自动 db push，本迁移**不会自动作用于线上**，需另行执行 `supabase db push`。
 [CX] 2026-09-26：按阿禅要求把 A 档三项任务拆成独立弱模型施工单，方案见 `docs/plans/2026-09-26-A档三项任务实施方案.md`。A1 改为只读复核与逐条决策清单（真冲突已按现行口径为 0，疑似错绑不准直接改绑）；A2 为 `/admin/videos` 到期观察后决定删除或延期；A3 为日报提交成功态真实浏览器验收。三项均冻结了文件范围、验收命令、停止条件和未覆盖项，未改代码、未回写待办、未提交推送。
[CCmd] 2026-09-26 23:40：按「push，一起清」完成收口。**扩展清理范围到 051 全部 6 张废弃表**（新增 sop_review_scores 4 行历史评分 / rewrite_variants 0 行空表；两张依赖自闭环、应用零引用），并删掉 `20260629000000` 里「重建 rewrite_variants 空表 + 索引 + 策略 + 授权」的段落（否则空库重放会在 051 删表后又把空表造回来）。commit `aa85e172` 已 push（origin/main 一致）。**线上已执行**：`psql -1 -f 20260926130000_drop_deprecated_rewrite_tables.sql`（单事务，exit 0：CREATE VIEW / GRANT / DO）+ `npx supabase migration repair --status applied 20260926130000`（账本已记）。**注：没有用 `supabase db push`**——`migration list` 显示本地还有 10 个未上线迁移（`20260903170000`/`20260904010000`/`20260926121500` 等），db push 会把它们一起写进线上，超出授权范围；改为只应用本迁移并官方同步账本。**线上前后核对**：前置 6 张表存在、4 条指向它们的外键、视图 20 行；后置 **6 张表全部不存在**、外键 0、视图 14 行（正好少了被删表那 6 行）、视图只剩 3 个存活分支（feature / rewrite_mode / rewrite_model_view）；存活对象未受伤（rewrite_model_routes 4 行可查、引用方 2 列保留、workflow_step_id 列保留）。**验证**：隔离 PG 集群重放两链路全绿——空库链路 040→…→050→051→062→20260629000000→新迁移，六表最终不存在；线上升级链路（用改前 `58794003` 版 062 造出 6 分支视图 + 外键 + 数据）→ 迁移成功、六表删、视图留 3 分支、幂等。`npm test` 1727/1727；`tsc` 0 错；本批 `git diff --check` 0。**未验证**：`npx supabase db reset --local` 仍 BLOCKED（未安装 Docker）；未做带登录态的接口/浏览器冒烟（被删对象零应用引用、视图零消费方，风险低）；`supabase db push` 实体未执行（故其余 10 个待上线迁移状态不变）。
[CCmd] 2026-09-27 00:10：按「做吧」完成 051 版本号正名（commit `c267308e` 已 push，origin/main 一致）。**发现并修掉第三个隐患**：`supabase migration list` 报 `Skipping migration 051b_leader_daily_reports.sql... (file name must match pattern "<timestamp>_name.sql")` —— 2026-07-25 那次改名把**线上真正应用的 051（组长日报表）变成了本地不可见文件**（CLI 只认 <数字版本>_name.sql），而占着 051 号的 `051_cleanup_deprecated_tables.sql` 从未在线上生效、却被 CLI 按版本号当成「已应用」→ 本地 051 ↔ 线上 051 内容对不上，且空库重放长期缺 `leader_daily_reports` 表（应用侧零引用，未爆出来）。**改动**：`051b_leader_daily_reports.sql` → 改名回 `051_leader_daily_reports.sql`（R100，内容一字未动）；删除占号的 `051_cleanup_deprecated_tables.sql`（其清理内容已由已上线的 20260926130000 承担，两边幂等）；20260926130000 头部补记正名；静态测试新增「051 已正名 / 无不合规迁移文件名 / 不会有迁移在删表后再重建废弃表」守卫（测试 11 条全绿）。**验证**：`migration list` 已无 Skipping 警告、051 双列对齐、051b 消失；隔离 PG 集群重放 23/23（空库链路改跑 051 组长日报文件，六张废弃表最终全部不存在、视图 3 分支）；`npm test` 1737/1737；`tsc` 0 错；本批 `git diff --check` 0。**线上无需任何动作**（本次只改仓库文件与注释，未新增待应用迁移）。**遗留**：本地仍有 **19 个**未上线迁移（他人/历史在途，未动）；`supabase/migrations/补丁/` 下有两个 2026-05-19 的手工 patch SQL，CLI 会忽略子目录，属预存遗留未处理；`supabase db reset --local` 仍 BLOCKED（未装 Docker）。
[CCmd] 2026-09-26 22:55：按阿禅「执行 B2 方案」完成 **B2-1 阶段 0 生产只读盘点**（阿禅选定范围＝只做 B2-1；全程只读 `SELECT`，未 DDL/DML、未新增 migration、未改待办、未提交未推送）。**产出**：`docs/plans/2026-09-26-B2-1数据库对象只读盘点矩阵.md`（对象矩阵＋依赖解除顺序＋回滚 recipe＋停止条件判定）与原始输出 `docs/plans/cleanup-batches/2026-09-26-b2-1-readonly-raw.txt`（4 段查询 / 1333 行，含方法自检控制项）。**关键事实（与 P1 草案有 4 处差异）**：① 待办要清的 7 张表**生产只有 3 张存在**（`knowledge_cases` 0 行 / `smart_alert_claims` 0 行 / `violation_test_records` **40 行**，最后写入 2026-05-28）；`case_taxonomies`、`knowledge_case_taxonomy_links`、`knowledge_case_status_logs`、`case_usages` **从未建成**——账本里的 `20260628113000` 只是 2026-08-19 用管理 API「记账」未真执行，`20260819130700` 只补建了基表，全仓无一处 `drop table` → 阶段 1 必须 `if exists`，且「空库重放」与「线上存量升级」两条路径都要验。② 8 个候选 RPC **无一可判零调用**：`pg_stat_statements`（2026-05-26 起 123 天）记 `admin_sidebar_badges_summary` **12608 次**、`admin_pending_submissions_today` 96、`admin_cockpit_summary` 91、`admin_analytics_first_screen` 50；其余 4 个读数为 0，但该窗口 `max=5000` / 现存 4873 / `pg_stat_statements_info.dealloc=8`（**已淘汰 8 条**）→「0 次」只能是下限。③ **草案漏掉 v1→v2 函数内依赖**：`admin_cockpit_summary` 与 `admin_pending_submissions_today` 现为 `..._v2` 的 `language sql` 包装，删除必须**先 v1 后 v2**；SQL 内联使 v2 计数恒为 0，**不能用 v2=0 当无调用证据**。④ `violation_test_records` 不该与 `smart_alert_claims` 同档：前者有数据、挂现役触发器 `trg_violation_pass_rate` 维护 `violation_cases.pass_count/fail_count`（7 个案例计数非零：pass 22 / fail 18），删表后两列**冻结在最后值**。**独立复核结论**：B-16 的 14 个候选与 2026-09-20「6 保留 / 7 观察 / 1 补链」归类**完全一致**；`track_functions=all`（09-20 开启）6 天窗口内 19 个候选**全部 0 调用**（本次基线），但 6 天＜待办要求的 4 周，B2-2 仍须等 2026-10-18。A-21：`audit_logs` 中 `action='cleanup_observation_a21_claim'` **0 行**（全表 438 行、最新 2026-09-23），本批无法区分「确实没人调」与「计数器未上线」→ 需另核 Vercel 部署。**停止条件命中**（保留期未确认 ＋ 存在外部触发／未解释数据）→ **不进入阶段 1、不写清理 migration、不删任何对象或数据**。**保留项复核通过**：`violation_cases`（29 行、19 条 `purpose='conversion'` 在役、最近写入 2026-09-10）、`review_violations` 权限键、middleware `/violations/:path*` 均维持保留结论。**意外发现（登记、未改判）**：`admin_first_screen_perf_regressions` 方案定为保留却 123 天零数据库层调用记录；`case_library_inbox` / `case_library_inbox_counts` 各 19 次调用却不属任何候选名单；候选表 ACL 对 `anon` 全权（RLS 为唯一闸门）。**待阿禅决定 4 项**（矩阵第十节）：`violation_test_records` 40 行留否、`smart_alert_claims` 可否直删、`knowledge_cases` 可否直删（先解释那 20 次外部读取）、8 个 RPC 是否一律推迟到 2026-10-18。**未验证/未做**：`npx supabase db reset --local` 未跑（本批零 migration 改动，无重放对象）；B2-2/B2-3/B2-4 均未启动（按方案规定不得从一个子项自动续做下一个）。
[CCmd] 2026-09-27 00:20：补记上一条——051 正名实际落在两个提交：`c267308e`（改名+删除+测试守卫）与 `715d0ccc`（补齐旧路径 051b 的删除，上一个提交漏带该路径，否则全新克隆里会同时存在两个 051 文件、静态测试会红）。已用 `git archive HEAD` 快照复核：HEAD 中无不合规迁移文件名、`migration list` 无 Skipping 警告、051 双列对齐；静态测试 11/11。
[CCmd] 2026-09-26 23:35：按阿禅对 B2-1 四问的决定执行**阶段 1**（已授权两项），并完成 `knowledge_cases` 读取来源追查。**阿禅决定**：40 行测试记录不留、`smart_alert_claims` 空表可删（→ 已交付删除 migration）、`knowledge_cases` 先查清读取（→ 已查清）、8 个 RPC 推迟到 2026-10-18。**交付**：① `supabase/migrations/20260926230000_retire_orphan_case_tables.sql`（**只新增**，可执行部分仅 3 条语句：`DROP TABLE IF EXISTS violation_test_records` → `DROP FUNCTION IF EXISTS update_violation_pass_rate()` → `DROP TABLE IF EXISTS smart_alert_claims`；无 CASCADE、全 `IF EXISTS`、空库 no-op 幂等；40 行数据**逐行留档**在回滚 recipe）；② `src/lib/orphan-case-tables-migration.test.ts`（7 条静态守卫，表名从迁移推导以保持 `src/` 零引用）。**连带处置（需知晓）**：`update_violation_pass_rate()` 的**唯一**调用方是被删表上的触发器，表删后即成"函数体引用已删表"的孤儿函数，故同批 `DROP FUNCTION`——属方案明确要求的"DROP 前处理函数依赖"，不是顺手清理；回滚须表与函数**成对恢复**。**验证（本机 Docker 不可用、`supabase db reset --local` 无法执行，改用 `initdb` 新建隔离 PG16 集群重放真实迁移文件）**：路径 A 空库重放 = exit 0 + 三条 NOTICE 均"不存在，跳过" + 无残留 + **幂等**；路径 B 线上存量升级（脚手架 → 真实 `055_violation_system.sql` → 真实 `20260823020000_audit_logs_smart_alert_dedupe.sql` → 灌入删除前 40 行真实快照 → 执行本迁移）= 两表与函数消失、`violation_cases` **7 行 / 4 策略原样未动**、7 个案例 pass/fail（4/1、0/5、1/5、5/1、2/4、6/0、4/2）**冻结在删除前值且与生产基准逐行一致**（删除前这些值是触发器在重放中真实算出来的，顺带证明快照 40 行的 22/18 分布正确），再跑一次仍 exit 0。**顺带修正两处计数笔误**（重放暴露）：`violation_test_records` 是 **3 条策略**（不是 4）、`knowledge_cases` 是 **7 外键 / 5 索引**（不是 9/6），已同步改正矩阵文档与迁移注释。**`knowledge_cases` 追查结论**：该表**自 2026-08-19 补建至今 `n_tup_ins=0`，一行都从未写入**；那 20 次读取只有一种形态——PostgREST 分页列表查询（`where account_id=$1 limit/offset` + exact count），每次只返回"计数 0"、**从未返回过任何数据行**；该形态对应 `f76d2387^:src/lib/violations/read-model.ts` 的通用分页函数，即**已删除的避坑案例模块自身**在"表已建、模块未删"那几天（08-19→08-22）发的查询，另有 1 次 `to_regclass` 核查探针。**残留不确定性**：统计视图无时间维度且 `pg_stat_statements` 已淘汰 8 条，无法证明"此刻无人读" → 已留复检基线（`calls=20` / `seq_scan=82` / `idx_scan=3`，并记录"自己跑 `count(*)` 会污染 `seq_scan`"这个坑），并入 B2-2（2026-10-18）同一趟复检；**是否删除 `knowledge_cases` 仍待阿禅据此再定**。**验收**：`npm test` **1752/1752**；`npx tsc --noEmit` 0 错；本批文件 `eslint` 0 错；`git diff --check` 0；`src/` 对两张表仍零引用；`deprecated-rewrite-tables-migration.test.ts` 的两条全局规则（迁移文件名合规 / 建表迁移必须排在删表之前）已复核通过；其余 4 个 `readdirSync` 的迁移测试均按具体文件名过滤、不受新增文件影响。**边界**：**未对生产执行任何 DDL/DML**（只读复核确认两张表仍存在、40 行、触发器函数仍为 1）；未改历史 migration、未回写待办、未 push；隔离 PG16 集群已停止并释放端口 55432。**未取证**：`smart_alert_claims` 那 1 次读取来源、A-21 计数器是否已在 Vercel 部署、`admin_first_screen_perf_regressions` 零调用记录原因。**待上线**：本迁移需另行授权后 `supabase db push`（或单迁移应用 + `migration repair`）。
[CCmd] 2026-09-26 23:55：阿禅「做吧」→ B2-1 阶段 1 迁移**已上线并完成闭环验证**。**顺序**：先推仓库（`715d0ccc..5fc445ca main -> main`，含 `2854df59` 盘点矩阵 + `5fc445ca` 迁移与守卫），**再用 `psql -1` 单事务上线本迁移**，最后 `npx supabase migration repair --status applied 20260926230000` 同步账本。**明确没用 `supabase db push`**——本地另有 19 个未上线迁移，db push 会把它们一起写进线上、超出授权范围。**上线前只读基线**：两表均在（`violation_test_records` 40 行 / `smart_alert_claims` 0 行）、触发器函数 1、策略 3、两张表入向外键均为 **0**、该版本未记账。**执行回显**：`DROP TABLE` / `DROP FUNCTION` / `DROP TABLE`，exit 0，未用 CASCADE。**上线后核对（全部与隔离库重放预测逐行一致）**：`violation_test_records`、`smart_alert_claims`、`update_violation_pass_rate()` **三者均不存在**（策略 0 / 触发器 0）；`violation_cases` **29 行、4 策略、pass 22 / fail 18 完全不变**；7 个案例 pass/fail 冻结为 4/1、0/5、1/5、5/1、2/4、6/0、4/2（与预测逐行一致）；旁证对象 `script_usage_records`、`violation_events` 仍在。**PostgREST 侧复核**：`/rest/v1/violation_test_records` 与 `/rest/v1/smart_alert_claims` 均 **HTTP 404**（`pgrst_ddl_watch` 已自动重载 schema cache），`/rest/v1/violation_cases` 仍 **HTTP 200**。**账本**：`schema_migrations` 记为 `20260926230000 retire_orphan_case_tables`；`migration list` 该版本本地/远程双列对齐；其余 19 个未上线迁移状态未变（未碰）。**回滚路径**：`055_violation_system.sql` 重建表 + 函数，40 行用迁移内快照回灌。**文档回写**：矩阵文档第十三节新增「线上执行结果」表（上线前后逐项对照）、状态行与附录 C 同步更新，并如实写明未跑 `gate:browser`（本批不涉及页面）。**遗留**：`knowledge_cases` **仍在线上**（未授权删除，读取来源已查清、复检基线已留，待阿禅定）；8 个候选 RPC 仍在，等 2026-10-18。
[CCmd] 2026-09-26 23:16：按 `docs/plans/2026-09-26-A档三项任务实施方案.md` 执行 **A1 岗位归属双表只读复核**（只读，未写生产数据、未建 migration、未改代码）。**结论**：真冲突 = **0**、两表署名不一致 = **0**（均复现 09-22 口径）；旧数字 `9/188/227/250/247` **全部失效**，当前真实规模为「标题不符 **50** 条」，需业务裁定的仅 **16** 条（9 条建议「人工确认置空」+ 7 条「无法判断」），另 34 条是任一侧为占位标题【视频提交/无/空】，标题比对不构成错绑证据，建议保留仅登记。候选侧：27 条日报有同名候选共 44 条候选行，其中指标完全一致的仅 5 条且**全部已被其他日报占用**，故无一条满足「指标匹配 + 未被占用」，改绑建议 0 条。**重要发现（非本轮所为）**：2026-09-22 19:40–19:55 已发生过一次 **200 条生产写入**（195 改绑 + 5 置空，方案原文未记录），本轮用线上数据逐条验证 `rebind-194/-v2/-rollback-*` 三支脚本仍 **100% 覆盖且零漂移**（194 条=189 仍为改后值+5 已置空，6 条全为改后值）；该批写入**是否在业务授权范围内无法取证**（`audit_logs` 无归属变更留痕），建议阿禅确认。**底账核对**：无 video_id 日报 **466**（=底账 461 + 5 条置空），逐 id 对账 0 丢失、0 篡改、含署名 176 条原样未动。**产物**（`output/` 已 gitignore）：`history-attribution-repair/a1-readonly-baseline-20260926-2239.md`、`a1-suspect-rebind-20260926-2239.csv`（67 行 / 50 条日报）、`a1-readonly-queries-20260926-2239.sql`、`a1-export-suspect-rebind-20260926-2239.sql`、`a1-future-write-template-20260926-2239.sql`。**验收**：`test -s` 两条 exit 0、方案文件 `git diff --check` exit 0、只读 SQL 与导出 exit 0；生产写入 0 条；BLOCKED 无。
[CCmd] 2026-09-26 23:16：**A2 `/admin/videos` 旧入口到期复核**：决策 = **保留旧入口 + 延期复核（新日期 2026-10-27）**，未改任何代码（本单改动文件 **0**）。**三条独立理由**：① 观察到**真实访问**——用生产库 `usage_events`（真实证据，不是 git grep）查到 2026-09-21～27 共 **5 次** `/admin/videos` page_view：1 次是阿禅本人（09-22 10:10）、4 次是自动化浏览器巡检（09-26 17:07–17:15，每次后紧跟一条 `/admin/content`，正是重定向落地特征）；② `"/admin/videos"` 这个**路径字符串是现役 API 的权限判定键**（`api/admin/videos/list/route.ts:63`、`api/admin/video-assets/[videoId]/route.ts:43`、`route-permissions.ts:21`、`admin-scoped-video.ts:43/53`），「只服务旧页面的权限兼容代码」证明失败 → 不删；③ 同目录 `patch-24h-dialog.tsx` 被 `/admin/content` 的 `content-detail-dialog.tsx:59` import，删目录会打断统一入口。**BLOCKED/未覆盖**：观察期「报错」与「用户反馈」两维度**无证据来源**（`vercel logs` 返回 No logs found、库内无 HTTP 访问日志表、`middleware.ts` 无留痕、无反馈通道）；`docs/archive` 09-20 验收报告记录的「实测 200+meta-refresh ≠ 宣称 308」差异本轮仍未取证（未登录请求被 middleware 提前拦为 307）。**验收**：定向测试 **9/9 通过**（exit 0）、`npx tsc --noEmit` exit 0、`git diff --check` exit 2 但唯一命中是他人预存改动（`api/dashboard/leaderboard/route.ts:107`）。报告：`output/admin-videos-retirement/a2-old-entry-review-20260926-2255.md`。
[CCmd] 2026-09-26 23:16：**A3 日报提交成功态真实浏览器验收**：判定 **FAIL**——四项操作里 3 项正常、**「AI 检查样本质量」必然 404**。真实登录（受控 env 回退链取 `ai-test@dydata.local`，凭据未落盘、未打印；本地生产构建连生产库）→ 切「作品异常」（截图非必填）→ 填 6 个必填指标+标题+文案+题材标签 → 提交成功。**成功态证据**：正确显示「今日创作已成功立卷」「归属日期：2026-09-26」；**无**「复盘/成长复盘」失效入口、无 `/growth` 死链；四个操作包围盒均 >0 无错位空位；导航已无「数据分析」。**逐项**：① AI 检查样本质量 ❌ 点击后 `AI 分析中…` loading，随后 `POST /api/dashboard/sample-quality-check` **404 `{"error":"日报不存在"}`** + toast「AI 检查未完成，不影响您直接提交」；② 查看并修改 ✅ 成功态关闭进编辑态、字段与按钮可见；③ 留在工作台 ✅ 回工作台且**无重复提交**（3 次提交后库里仍只有 1 日报/1 视频/1 快照）；④ 去选题库挑选明日选题 ✅ URL 进 `/topics`。**根因（预存缺陷，非本轮也非并发改动引入）**：`video-submit-form-v2.tsx:1254` 传 `submittedVideo.id`（**视频 id**），而 `sample-quality-check/route.ts:264-268` 按 `daily_reports.id` 查、471 行返 404 —— 两边 id 域不同必然失败；该行自 2026-08-25 `cda7272e` 即存在，其他会话未提交的重构未碰它。**按方案停止条件（出现 404）停止、未修复**，另开修复单（建议改由提交接口返回 `daily_report_id`）。**生产数据**：只用独立测试沙箱账号，标题带 `【A3验收测试】`，已用 `output/dashboard-submit-acceptance/a3-cleanup.sql`（快照→单事务删除→事务内核验）清理，**最终线上残留 0**（日报 0 / 视频 0）。**产物**：新增 `tests/performance/日报提交成功态.spec.ts`（未动任何业务代码）；报告 + 5 张截图 + 证据 JSON 在 `output/dashboard-submit-acceptance/`。**命令退出码**：`npm run build` **0**、定向 Playwright **1**（预期内失败：四项都跑完、证据收齐，仅末段断言因 404 判不通过）、`tsc` **0**、`eslint` **0**、`git diff --check` **2**（他人预存 `leaderboard/route.ts:107`）。**未跑全量 gate、未提交、未推送。**

## B1-3 异常处罚类型废弃项清理（2026-09-26 收尾）
- [CX] 唯一入口收口到 `src/lib/video-anomaly.ts`：`VIDEO_ABNORMAL_STATUS_VALUES` 只剩 abnormal/异常/限流/删稿；新增 `RETIRED_VIDEO_ANOMALY_STATUS_VALUES`（投流/活动干预）+ `isRetiredVideoAnomalyStatus()` + `findRetiredVideoAnomalyInput()` 写入守卫；旧英文枚举走同一中文标签映射，旧行不炸不丢。
- [CX] 写入口：`/api/video-submit` validation 对 anomaly_status/punish_type 带下线值明确 400 拒绝（不静默归一化）；工作台下拉早已只剩限流/删稿，无需再动。
- [CX] 读兼容：normalize 仍把旧值判异常；`classifyVideoAnomalyBucket` 删 boosted 桶（旧行归入异常桶）；提醒条删「投流/活动干预」分项；列表徽标/详情抽屉徽标走 `resolveVideoStatusLabel()` + `isRetiredVideoAnomalyStatus()`。
- [CX] 类型：`AnomalyStatus` 去掉投流/活动干预（历史兼容读保留在 video-anomaly 映射层）。DB CHECK 约束仍允许旧值（历史 migration 不改），收紧需前向 migration。
- [CX] 生产只读核对：videos.anomaly_status（限流23/删稿12/投流0/活动干预0）、videos.punish_type（paid_boost/campaign_intervention 0 条）。历史数据无改写。
- [CX] 验证：定向 54 项 + 全量 1756 项全绿、tsc 0 错。`rg 投流|活动干预 src supabase` 剩余命中逐条已解释（历史兼容读/写入守卫/测试/旧 migration）。
[CCmd] 2026-09-27 00:30：**本方案已全部完成（收口）**。范围与结果：① `062_ai_rewrite_unified_view.sql` 去掉 3 个引用已删表的分支（保留 3 个存活分支 + GRANT + rollback 注释）；② `20260629000000_rewrite_skills_and_documents.sql` 去掉 §8.2 数据搬运分支与 rewrite_variants 建表/策略/授权段；③ 新增并**已在线上执行** `20260926130000_drop_deprecated_rewrite_tables.sql`（先重建视图→摘外键→按序删 051 全部 6 张废弃表，幂等、不用 CASCADE、带回滚 recipe）；④ `051` 版本号正名（组长日报回 051、占号清理文件删除）。**线上终态**（只读复核）：6 张废弃表全部不存在；`ai_unified_config_view` 只剩 3 个分支（feature/rewrite_mode/rewrite_model_view）、14 行；残留外键 0；存活对象未受伤（rewrite_model_routes 4 行、引用方列保留）；账本记 `20260926130000 = drop_deprecated_rewrite_tables`。**提交**：`9820ecb8` → `aa85e172` → `e3dfe1f5` → `c267308e` → `715d0ccc`，均 push（origin/main 与本地一致）。**验收**：`npm test` 1737/1737、`tsc` 0 错、隔离集群重放 23/23（空库链路 + 线上升级链路）、`migration list` 无 Skipping 警告且 051 双列对齐。**遗留（不阻塞）**：`supabase db reset --local` 因本机无 Docker 仍 BLOCKED；本地 19 个未上线迁移维持原状；`supabase/migrations/补丁/` 两个手工 patch SQL 属预存遗留未处理；`docs/待办清单.md` 中「依赖方向 / 测试方式 / 超大文件」三条未回填（该文件有他人未提交改动，避免混入）。
[CCmd] 2026-09-26 23:50：阿禅「1 可以」→ **知识案例库家族 5 张表已清理并上线**。**授权链**：阿禅先要求查清那 20 次外部读取 → 我追查完毕（读取来自**已删除的避坑案例模块自身**的分页列表查询、且该表**自建表至今 `n_tup_ins=0` 一行都未写入**）→ 阿禅据此授权删除。**交付**：① `supabase/migrations/20260926233000_retire_knowledge_case_tables.sql`（**只新增**，可执行部分仅 5 条删表语句，按外键依赖序：`knowledge_case_taxonomy_links` → `knowledge_case_status_logs` → `case_usages` → `case_taxonomies` → `knowledge_cases`；全 `IF EXISTS`、无 CASCADE、空库 no-op 幂等）；② `src/lib/knowledge-case-tables-migration.test.ts`（7 条守卫：表名从迁移推导、**断言子表先于父表删除**、不摘函数、不连带 `case_library_*`、建表迁移须在前、回滚 recipe 完整、只允许 `DROP TABLE`）。**关键设计（为什么删满 5 张而不是只删线上那 1 张）**：子表对基表有外键，只删基表在**生产**能过（子表本就不存在）但在**空库/新环境**重放 `20260628113000` 会先建出子表、随后 DROP 基表因外键报错 → 两环境终态漂移（本仓库近期反复出现的一类缺陷）。**一处被实测否掉的设计（重要留痕）**：初版判断 2 个 SQL 语言函数（`append_knowledge_case_status_log`、`knowledge_case_snapshot`）会被 `pg_depend` 登记、**必须先摘掉**才能删表，据此写了 2 条 `DROP FUNCTION`；隔离库**反证路径**（新环境现场 + 故意不摘函数、只删表）实测 **exit 0、无依赖报错** —— 引号体（`AS $$...$$`）SQL 函数在 PostgreSQL **不在创建时解析函数体、因此不登记依赖**。据此**删掉那 2 条语句**，迁移收敛为纯 5 条删表，函数清理改登记为遗留。**验证（隔离 PG16 三路径）**：A 空库 = exit 0 + 5 条 NOTICE + **幂等**；B1 新环境现场（真实 DDL 建 5 表 + 从 `20260628113000` 原样抽取的 7 个引用它们的函数）= **删表 exit 0**（证明函数不阻塞）、5 表归零、函数原样保留；C 生产现状（仅基表）= exit 0 + 4 条 no-op + 基表删除成功。B1 中遗留函数失效已实测（`knowledge_case_snapshot` 报 `relation "public.knowledge_cases" does not exist`）。**线上执行**：推 `d6fc489a` → `psql -1` 单事务（回显 4 条 no-op + 5 次 `DROP TABLE`，exit 0）→ `supabase migration repair --status applied 20260926233000`；**仍未用 `supabase db push`**。**线上后置**：5 张家族表全部不存在；函数/策略/视图引用这 5 表均为 **0**（无悬挂引用）；存活对象未受伤（`violation_cases` 29 行 / 4 策略、`script_usage_records` 53 行、`violation_events` 在）；PostgREST 侧 `/rest/v1/knowledge_cases` **404**、`violation_cases` 与 `script_usage_records` 仍 **200**；账本记 `20260926233000 retire_knowledge_case_tables`、`migration list` 双列对齐。**验收**：`npm test` **1763/1763**、`tsc` 0 错、本批文件 `eslint` 0 错、三个迁移守卫测试文件 25/25、`git diff --cached --check` 0、`src/` 对 5 张表零引用。**未取证/未解释（如实登记）**：上线前复检 `knowledge_cases.seq_scan` 由 82 升至 86 **无法归因**（无新增受追踪语句、无活跃会话、无写入；对象已删故问题消解）。**遗留（需另立施工单）**：家族服务层函数 7 个（只在未真正执行过的 `20260628113000` 里创建、线上不存在、应用零引用，空库重放时会引用已删表而失效）；`case_library_*` 四个 RPC **线上现役**、属 B2-2 观察范围，本迁移刻意不碰。隔离 PG16 集群已停止、端口 55432 已释放。
[CCmd] 2026-09-27 00:05：按阿禅「做吧」修复一处**账本与线上不一致**（并回写待办）。**发现**：`docs/待办清单.md` 记「系统设置退役的数据库残留已清理」，线上对象确实已删（`daily_quota_config` 表、`get_daily_quota(date)` 函数、旧 `video_review_thresholds` key 都不存在），但 `supabase_migrations.schema_migrations` **没有 `20260926121500` 这个版本**——`migration list` 显示该版本"本地有、远端空"。后果：将来任何 `supabase db push` 都会试图再执行它一次。**处置**：先逐条核对 `20260926121500_remove_retired_system_settings.sql` 的三项效果在线上**是否真的已生效**（① `system_settings` 里 `video_review_thresholds` 行数 = **0**；② `get_daily_quota` 函数数 = **0**；③ `daily_quota_config` = **不存在**；附带确认该迁移**不该删**的 `system_settings` 表仍在，且只剩 `feishu_fulfillment_reminder_enabled`、`topics_feishu_workspace_url` 两个现役 key）→ 全部成立后执行 `npx supabase migration repair --status applied 20260926121500`（**只登记账本，未执行任何 DDL**）。**结果**：账本记 `20260926121500 = remove_retired_system_settings`；`migration list` 该版本**双列对齐**（与 `20260930000000` 无关，另两个我加的版本 `20260926230000`/`20260926233000` 亦对齐）。**文档回写**：`docs/待办清单.md` 该条目补上本次复核结论（三项效果均生效 + 账本已补记），并同步更新 P1 清理库存与 2026-10-18 复核两节（见上一条）。**⚠️ 过程失误如实登记**：上一笔提交 `45cb7ea8` 时，索引里已存在**其他代理预先 `git add` 的 12 个文件**（视频复盘 / 投稿校验 / 异常类型 / 数据口径等），我未先查 `git diff --cached` 就 `git commit`，导致它们随我的提交一起提交并推送——**内容未丢未覆盖，仅归属记在我的提交信息下**。已用 `tsc` 0 错、`npm test` 1763/1763、`npm run lint` 0 error（47 存量 warning）、`npm run build` exit 0 复核 main 健康；未回滚（回滚会把该代理的成果从 main 撤掉，损害更大）。本次提交起改为**先查索引、并用 `git commit -- <路径>` 只提交指定文件**。
[CCmd] 2026-09-27 01:05：按「1 2 做吧，3 发给我」完成。**① 未上线迁移盘点与对齐**：体检 20 个本地单侧迁移后发现——它们的**内容基本早已在线上生效**（函数/列/表探针逐条确认），真正缺的是「本地版本号 ↔ 线上账本」的映射；直接 `db push` 会重跑带未守卫插入/`DELETE`/`DROP TABLE` 的语句，故不采用。处置：（a）**补做唯一真正缺失的** `20260904120000_revoke_fulfillment_write_rpc_authenticated_grant`（线上 4 个履约写 RPC 仍对该角色开放；已确认 4 个调用点全走 service-role 客户端，故收回 authenticated 执行权不影响功能），单事务 exit 0 + 账本已记；（b）按「内容已在线上」补账本 16 个版本；（c）3 处版本号对齐改名并 push：`20260907082607_writer_certifications→20260907130047_*`、`20260907100000_daily_report_data_source→20260907130052_*`（线上账本 name 里嵌着本地号即证据）、`076_visual_tags→20260926234000_visual_tags`（本地 076 重复号，线上 076 只记了 feishu，该文件永远无法被 CLI 应用；全文幂等，应用后仅补账本）。**最终 `migration list`**：本地单侧仅剩 **1**（`20260901120000`，有意保留）、线上单侧 3、无 Skipping 警告。提交 `090c8ccd`。**② 待办清单回填**（commit `e504a080`）：062 重放项标记完成并按事实修正「不要用新增前向迁移」的判断（该判断只对 062 未修成立）；补一条结构体检三欠账收口记录（依赖方向 / 测试方式 / 超大文件）。注：原三条欠账条目已被其他 agent 按文件约定移出，故以收口记录形式回填。**③ 本地重放命令已交付阿禅自行执行**（本机无 Docker）。**两条待决策（未擅自动手）**：㈠ `20260901120000` 的 EXCLUDE 约束需要 `btree_gist`（线上未装），整份执行还会复活已被后续迁移删掉的旧 `re_review_exemption_request_atomically` → 若要这道数据完整性护栏，建议单独抽一条只加约束的前向迁移；㈡ **安全加固建议**：`mark_fulfillment_status(_batch)` / `remove_fulfillment_mark` 是 `SECURITY DEFINER` 且**内部不查权限**，却仍带 `PUBLIC`(`=X/postgres`) 与 `anon` 的 EXECUTE 授权（`handle_fulfillment_appeal` 已收干净），建议 `revoke execute ... from anon, public`——属权限口径改动，等阿禅确认。
[CCmd] 2026-09-27 01:30：履约写 RPC 执行权收口**已按迁移方式做掉并上线**（commit `a8fc275a`，账本记 `20260926235000`）。起因：阿禅问「那三行直接在网页跑就行吧」——能生效，但网页执行不入仓库/账本，空库重建会因 Supabase 默认权限再次放开，故改成迁移文件。线上只读核对原状：`mark_fulfillment_status` / `_batch` / `remove_fulfillment_mark` 是 SECURITY DEFINER 且内部不查权限，ACL 仍带 `=X/postgres`(PUBLIC) 与 `anon=X/postgres`（20260904120000 只收了 authenticated 的显式授权，对 PUBLIC 路径无效）→ 任何调用者含未登录都能执行履约写操作。处置：`revoke execute ... from anon, public`（4 个函数，含已收干净的 `handle_fulfillment_appeal` 幂等再收一次）。执行后 4 个函数 ACL 只剩 postgres + service_role，`anon` 不可执行（实测 anon_ok 全为 f）。零风险依据：全仓 4 个调用点全走 service-role 客户端（`requireAdminServiceClient` → `createAdminClient`）。`migration list`：本地单侧仍只剩 `20260901120000`（待决策），无 Skipping 警告。
