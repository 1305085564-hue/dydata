- 01:29:00 [An] 完成协作管理 (/admin/collaboration) 工业级 UI 重构与二次升维：整合控制舱（恢复 L1 纯白 `bg-white` 纸感底板，解决与 L0 背景 `zinc-50` 撞色模糊问题）并增加 `<` `>` 快捷翻月按键；切除大黄框，改造成右侧极轻静默芯片与归属明细弹窗；补齐表格列头排序高亮指示箭头 (`↓` / `↑`)；完成 PersonalCard 5:4 黄金比例内部弹性自适应与试做暗夜 Tooltip 悬浮框；重构表格展开亲密层级（移除父行与展开内容中间的分割线并共享 `bg-zinc-50/90` 衬底，使其归属一体，同时在最底部拉开 `pb-5 border-b` 隔断下行）；在《美学规范.md》中正向写入中心弹窗 `5:4` 黄金比例规则及【焦点与内容色彩分离准则】。
- 2026-07-29 20:04:29 [CX] 修复首页数据上传页“选发布时间后无法上传/无反应”：发布时间只更新 `published_at`，不再反写 `bizDate`；同时把新表单默认归属日期改回当天，避免默认昨天 19:00 的发布时间把日报写到前一天；已用定向 tests、eslint 和 `npm run build` 验证通过。
- 2026-07-29 [CX] 批量修复 5 个低难度 bug：①补全站 not-found.tsx + global-error.tsx（风格复用 RouteErrorState）；②为 admin/topics/violations 三大路由补 error.tsx（参照 dashboard/growth 已有模式）；③导出按钮增加日期反向校验（开始>结束时禁止导出并提示）；④dialog.tsx DialogFooter 关闭按钮英文→中文“关闭”；⑤NetworkStatusBar z-50→z-[60] 修复与 NavBar 层级冲突。TypeScript + eslint 均通过，无新增错误。
- 2026-07-29 [CX] 返工修复 6 个问题：①global-error.tsx 框架约定（加 <html><body>）；②日志引号误改还原；③不 stage supabase/.temp/cli-latest；④选题池多选 topic_id（buildPoolQueryOptions + loadTopicPool + test 更新）；⑤useFormField 空值检查（context default null + 顺序检查）；⑥topics/page.tsx as any 移除 + TopicSortMode 类型。验证：eslint（旧 unused vars 除外，无 new any error）、tsx test pass、npm run build pass。风险：无；#10 sortMode 默认值未改。
- 2026-07-29 [CX] 一次性修复剩余复杂 bug（A权限安全组 + B提交数据污染 + C时区 + D sortMode）：①#1 豁免审批 review 加 requireOwnerOrAdminActor 鉴权；②#7 middleware catch 改 fail-closed 重定向登录；③#15 auth/callback 已有 next 校验（sanitizeNextPath）保留；④#16 register-teams 改只返回默认团队最小公开字段；⑤#3 权限申请按钮接入 /api/exemptions/apply 真实 API；⑥#17 豁免申请 pending 防重 409；⑦#2 curve/retention screenshot_url 按 screenshot_type 分取；⑧#11 uploaded_at 刷新；⑨#23 Number("") 空值报错；⑩#21 formKey 已存在（账号切换 key 重建）；⑪#5/#12 today 统一 formatShanghaiDateOnly；⑫#10 默认 sortMode 改为 priority 与文案一致。验证：git diff --check 0、eslint 0 error（旧 unused warning 6 条）、tsx review+video tests 26 pass、npm run build 0。未完全修复：#3 权限申请用豁免接口是权宜（业务语义是豁免不是权限 key 申请，无独立 permission request 表）；#21 ocrValues 状态仍在父组件，form key 已存在但账号切换时 ocr 残留风险仍需 Codex 主线验收。禁止 stage/push。
