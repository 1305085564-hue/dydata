# 2026-07-14

- 完成最轻量使用埋点：新增 `usage_events` migration、`/api/usage-events` 接口、登录后页面 `page_view`、8 个核心动作埋点，并补充 `docs/plans/2026-07-14-使用埋点方案.md` 查询模板；不引第三方 SDK，不做可视化界面。
- 已将 `20260714120000_add_usage_events.sql` 安全落到线上 Supabase：先确认线上缺表，再用隔离 workdir 排除无关 `051_leader_daily_reports.sql` 后单独 push；随后用临时测试用户真实登录 `dydata.cc` 验证 `/api/usage-events` 可写入线上 `usage_events`，验证后已清理测试数据与用户。
- 修复视频审核产量对账数据看板页面（`/video-review`）的 500 Internal Server Error 问题。对 SSR 的数据库 RPC、表格查询及 Storage 签名调用添加全局 `try-catch` 容错，并增加前端 Alert 状态横幅进行异常提示，保障页面稳定性和可诊断性。
- 完成 `/dashboard` 与 `/video-review` 白屏/卡顿专项修复：定位到本地双 `next dev` 互踩 `.next/dev` 导致 `ChunkLoadError`，以及 `video-review` 子页默认预取 4 个兄弟路由 `_rsc`、首页未收口 loader、历史 `publish_drafts` 截图误走违规截图校验导致 404。已将 `/video-review` 首页收口到 `src/lib/loaders/video-review-page.ts`、案例首屏限 24 条、submission 截图改批量签名、tabs 关闭默认预取、截图路由放行本人/approved `publish_drafts` 历史图，并更新 `docs/架构与性能总纲.md`、`docs/全站模块地图.md`。本地真实登录态复测 `/dashboard`、`/video-review`、`/video-review/submit`、`/video-review/exemption`、`/video-review/manage -> /admin/content` 均可打开且控制台无报错。
