# 2026-08-30 选题库性能收口补充

## 目标

解决 `/topics` 首屏进入慢、同一页面重复拉取数据的问题，不改变卡片指标、视觉层级、权限口径或数据库结构。

## 代码改动

- `src/app/(app)/topics/page.tsx`：服务端完成 active team membership 与权限确认后，直接准备并注入首屏数据；权限与飞书地址并行读取。
- `src/app/api/topics/bootstrap/route.ts`：新增首屏聚合读取兜底接口。
- `src/lib/topics/service.ts`：新增 `loadTopicLibraryBootstrap`，并行准备团队动态、母题、首屏选题和当前用户在写 ID。
- `src/components/topics-v2/TopicHubV2.tsx`：消费服务端首屏数据；无首屏数据时才请求 bootstrap；开发模式请求去重；刷新不再重复读取母题。
- `src/app/api/topics/_shared.ts`：topics API 复用请求级用户上下文，并增加可选性能分项日志。
- 新增/更新首屏聚合、页面注入和接口守卫回归测试。

## 实测证据

- 本地生产模式、已登录代号账号：进入 `/topics` 首屏 `0` 个 `/api/topics/*` 请求，页面直接显示真实选题和团队动态。
- 输入搜索条件：只产生 1 个 `/api/topics/pool` 请求。
- 点击刷新：只产生 `/api/topics/active` 与 `/api/topics/pool` 两个请求，不再拉 `/options`。
- 服务端首屏数据查询分项约 `0.08–0.15s`；筛选接口的登录/权限约 `0.3s`，选题池查询约 `0.1s`。该证据来自本机生产模式，不等同于生产环境通过。

## 验收

- 定向 Topics/页面/服务测试：通过。
- `npm test`：1246/1246 通过。
- `npm run lint`：0 errors，74 条仓库既有 warnings。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过，78 个静态页生成成功。

## 未覆盖与边界

- 未做生产登录态浏览器性能宣称；生产只需在部署后按同样方法复测。
- 未改数据库、migration、RLS、卡片指标、视觉设计或业务数据。
- 本机仍有与本任务无关的未提交 Antigravity 视觉改动、临时配置和审计文件，未混入本次性能提交。
