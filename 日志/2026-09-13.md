# 2026-09-13

- 10:15 [CX] 选题库全量审查修复已完成本地应用层收口：Topics 只按 `profiles.team_id` 隔离，作者或同团队 `review_content` 管理员可编辑/软移出，跨团队拒绝；飞书保持“复制→标记在写→打开”且失败不报假成功；pool/J-K/筛选与抽屉旧数据竞态已收口。
- 10:16 [CX] 指标口径已统一：单作品取全部快照最高值，“最高播放”统计全部真实作品，3 万只用于达标数/文案；“尚无作品/暂未达标”和不限 7 天的当前在写人数已统一到卡片、表格、抽屉。
- 10:17 [CX] 新增 `20260912120000_topics_security_metrics_hardening.sql`：active 同团队 RLS、收回浏览器直接写权限、pool RPC 仅 service role、严格 writing 状态、从全量在库题左连接聚合、软移出与审计原子 RPC；PostgreSQL 15 最小真实库执行成功，权限矩阵、无作品有人写、多快照低播放和软移出审计均通过。
- 10:18 [CX] 线上变更前只读证据：旧 `topics_pool_aggregates` 仍授权 PUBLIC/anon/authenticated；Topics 三表仍向 anon/authenticated 暴露直接写权限；claim 当前 `cancelled=11/completed=1/writing=20`，其中 19 条 writing 超过 7 天；处女题 `00fd1e7c-4f06-4378-ae01-640c625eb747` 有当前写作但旧 RPC 漏出，低播放题 `59a74c11-7e85-454d-b8cf-ca57a7a63d3c` 当前最高 1993；线上尚未执行本次 migration。
- 10:19 [CX] 本地验证：Topics 定向测试 106/106、全量测试 1507/1507、凭据脚本 2/2、TypeScript、Topics 模块 lint、Next.js production build、`gate:browser` 1/1 通过；`gate:static` 仅被范围外既有 `src/lib/animations.ts:229` 拦截。
- 10:20 [CX] `npx supabase db reset` 在本次 migration 前被历史 `062_ai_unified_config_view.sql` 引用缺失的 `public.rewrite_length_presets` 阻断，未扩范围修改；已用隔离 PostgreSQL 15 实际执行本次 migration 补足本模块证据。
- 10:21 [CX] 已删除零运行时调用的 `FeishuCreationModal.tsx`、`claim-toggle.ts` 及其测试；旧 `/claim` 路由因 Vercel 精确 30 天指标需要付费观测能力，暂保留并在代码/接口文档标记迁移期废弃。
- 10:50 [CC] 数据上传页三份 UX 审计（TRAE/luna/gemini）合并完成：23 条发现、19 条成立，产出 `docs/plans/数据上传页面UX修复施工规格-2026-09-13.md`（16 任务，[CX]10/[An]6）。P0 为 OCR 失败后空白指标落 0 污染日报。
- 10:55 [CC] 阿禅拍板 5 项决策并已写入规格：①低置信度不阻断提交改警告条；②留存 4 项可留空、必填为 7 项（核心3+互动4）；③新建草稿 key 改为随账号更新（原 useState 冻结）；④提交按钮去 disabled 改抖动定位（T16 入册），快捷键常驻与云端加载态立待办；⑤空态文案改"管理员或组长"。待办清单已同步补遗留体验项。
- 15:34 [CX] 选题库兼容应用与两份前向 migration 已分顺序上线，生产 RPC 只保留 `service_role` 执行权，Topics 三表已收回 anon/authenticated 直接写权，active+同 `team_id` RLS 与新 claim CHECK 已实查生效；旧状态已收敛为 `writing/cancelled/completed`。
- 15:34 [CX] 真实生产闭环已验收：普通成员、同团队管理员、跨团队管理员权限矩阵符合合同；软移出/恢复后 2 作品、5 claim 与 2 条审计保留；飞书成功路径完成复制、持久化 writing 和 HTTPS 跳转，无 Confirm；处女题卡片显示“尚无作品/1人在写”，低播放题在卡片、表格、抽屉均显示最高 1,993 与“暂未达标”。
- 15:34 [CX] 最终回归：Topics 定向 98/98、全仓测试 1516/1516、TypeScript、Topics 模块 lint、Next.js production build 全部通过。全仓 `gate:static` 仍只受范围外 `src/lib/animations.ts:229` 阻断；全量 `supabase db reset` 仍被历史 `062_ai_unified_config_view.sql` 缺失 `rewrite_length_presets` 阻断，两项均未冒充为已通过。
- 15:52 [CC] T1（空白指标落 0）代码审查完成：Codex 汇报属实（状态机 14/14、validation 30/30、tsc 0；gate:static/diff --check 失败点均在范围外预存问题），六项改动五层落地、警告条与门禁符合规格。但发现 1 个重大问题：edit 模式 null 契约在路由层断裂——route.ts 对 edit 调 `buildEditSubmissionContract`（edit-detail.ts:209-215 要求 12 项指标全为有限数字），validation.ts 放行的 edit-null 实际到不了落库；新回归路径=编辑今日作品清空留存指标→发 null→400，validation.test.ts 的 edit-null 用例只测单函数属假阳性。另有 3 个清理项：提交状态机残留 `pendingSlotConfirmations`/`missingRequiredFields` 死字段、route.ts 导粉落库条件冗余重复、canSubmit 与 summary.reason 文案分叉（线上不可见）。待返工，未改代码。
- 16:05 [CC] 选题库任务审查结案：按标准 A（任务范围完成即通过）判定通过。Codex 汇报逐项属实：FeishuCreationModal 零运行时调用、三表写权已收回 anon/authenticated、RPC 仅 service_role、Topics 契约测试 105/105、lint 与 062 两处阻塞确系范围外预存问题。两项预存欠账已记入 `docs/待办清单.md` P2；同时修正计划文档中"前向迁移收口 062"的错误方向（重放时 062 先炸，前向迁移救不了 db reset，应直接编辑 062 删 UNION 分支，动手前先查线上 rewrite_length_presets 是否实际存活）。
- 17:02 [CX] T1 P0 返工完成：`/api/video-submit/edit-detail` 读取与 `/api/video-submit` edit 保存契约已统一为 7 项必填指标必须为数字、4 项留存指标可为 null；validation 不再放行 edit 必填 null，避免进入写入层后报数据库错。新增/更新 edit-detail、route-core、validation 回归，定向测试、TypeScript、生产构建、本批 diff check 通过；`gate:static` 仍仅被范围外既有 `src/lib/animations.ts:229` lint 阻断，全仓 diff check 仍被范围外 AGENTS.md 和 2026-09-12 日志空白阻断。
- 23:15 [An] 数据上传页第三批视觉/响应式任务（T7、T9、T11、T12、T13、T14）完成：移动端指标网格改为 2 列、重试按钮移动端隐藏、删除触控区 36px；未入团队空态卡落成；导粉话术左线去橙色改为中性灰；异常态截图槽位增加选填徽标与提示文案；截图槽位追加上传格式/8MB限制说明；指标网格增加桌面端分组标题。TypeScript、Next.js 生产构建通过，5 档浏览器（320/375/768/1024/1440）实走查均符合规格。
- 23:40 [An] 数据上传页第三批返工收口：回滚原图 hover 浮窗越界项（彻底移除浮窗及外层 wrapper，恢复原 Dialog 结构）；确认状态判定与无障碍属性等属前置历史/Codex 未动；保留本批合法 T7/T9/T11/T12/T13/T14；tsc 与改动组件 lint 均为 0 error；如实汇报 gate:static 整体失败（退出码 1，2 个范围外 error）。
