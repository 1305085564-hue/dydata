# 2026-09-13

- 10:15 [CX] 选题库全量审查修复已完成本地应用层收口：Topics 只按 `profiles.team_id` 隔离，作者或同团队 `review_content` 管理员可编辑/软移出，跨团队拒绝；飞书保持“复制→标记在写→打开”且失败不报假成功；pool/J-K/筛选与抽屉旧数据竞态已收口。
- 10:16 [CX] 指标口径已统一：单作品取全部快照最高值，“最高播放”统计全部真实作品，3 万只用于达标数/文案；“尚无作品/暂未达标”和不限 7 天的当前在写人数已统一到卡片、表格、抽屉。
- 10:17 [CX] 新增 `20260912120000_topics_security_metrics_hardening.sql`：active 同团队 RLS、收回浏览器直接写权限、pool RPC 仅 service role、严格 writing 状态、从全量在库题左连接聚合、软移出与审计原子 RPC；PostgreSQL 15 最小真实库执行成功，权限矩阵、无作品有人写、多快照低播放和软移出审计均通过。
- 10:18 [CX] 线上变更前只读证据：旧 `topics_pool_aggregates` 仍授权 PUBLIC/anon/authenticated；Topics 三表仍向 anon/authenticated 暴露直接写权限；claim 当前 `cancelled=11/completed=1/writing=20`，其中 19 条 writing 超过 7 天；处女题 `00fd1e7c-4f06-4378-ae01-640c625eb747` 有当前写作但旧 RPC 漏出，低播放题 `59a74c11-7e85-454d-b8cf-ca57a7a63d3c` 当前最高 1993；线上尚未执行本次 migration。
- 10:19 [CX] 本地验证：Topics 定向测试 106/106、全量测试 1507/1507、凭据脚本 2/2、TypeScript、Topics 模块 lint、Next.js production build、`gate:browser` 1/1 通过；`gate:static` 仅被范围外既有 `src/lib/animations.ts:229` 拦截。
- 10:20 [CX] `npx supabase db reset` 在本次 migration 前被历史 `062_ai_unified_config_view.sql` 引用缺失的 `public.rewrite_length_presets` 阻断，未扩范围修改；已用隔离 PostgreSQL 15 实际执行本次 migration 补足本模块证据。
- 10:21 [CX] 已删除零运行时调用的 `FeishuCreationModal.tsx`、`claim-toggle.ts` 及其测试；旧 `/claim` 路由因 Vercel 精确 30 天指标需要付费观测能力，暂保留并在代码/接口文档标记迁移期废弃。
- 15:34 [CX] 选题库兼容应用与两份前向 migration 已分顺序上线，生产 RPC 只保留 `service_role` 执行权，Topics 三表已收回 anon/authenticated 直接写权，active+同 `team_id` RLS 与新 claim CHECK 已实查生效；旧状态已收敛为 `writing/cancelled/completed`。
- 15:34 [CX] 真实生产闭环已验收：普通成员、同团队管理员、跨团队管理员权限矩阵符合合同；软移出/恢复后 2 作品、5 claim 与 2 条审计保留；飞书成功路径完成复制、持久化 writing 和 HTTPS 跳转，无 Confirm；处女题卡片显示“尚无作品/1人在写”，低播放题在卡片、表格、抽屉均显示最高 1,993 与“暂未达标”。
- 15:34 [CX] 最终回归：Topics 定向 98/98、全仓测试 1516/1516、TypeScript、Topics 模块 lint、Next.js production build 全部通过。全仓 `gate:static` 仍只受范围外 `src/lib/animations.ts:229` 阻断；全量 `supabase db reset` 仍被历史 `062_ai_unified_config_view.sql` 缺失 `rewrite_length_presets` 阻断，两项均未冒充为已通过。
