[CX] 16:52 DYData 页面权限扩充完成：固定权限、导航过滤、成员管理边界、组员个人导出/分析与只读岗位管理、岗位归属写入 403 已落地并覆盖定向/全量测试；未覆盖真实线上账号与生产权限数据验收，未改数据库/migration/RLS。
[CX] 16:56 顶栏待办与通知中心故障修复：点击后先打开 UnifiedCommandHub 再异步刷新 notifications/审批计数，并将抽屉改为固定定位避免脱离 nav 后跑出视口；定向测试、ESLint、tsc、diff check 通过，本地浏览器只读验证可打开且 notifications/exemptions 返回 200。
[CX] 16:59 主线程复核页面权限扩充：补回普通组长可归档/恢复本团队组员、收紧组员访问 /admin 根路径；npm test、tsc、lint、build、diff check 均通过，未做真实账号浏览器验收。
[CX] 17:09 全站命名决策完成：执行 C1/C2/C3，将 view_analytics 展示名统一为“数据分析”，/admin/settings 导航、metadata、页面标题和无权限态统一为“系统设置”，并同步代码治理手册与命名对照表；生产库残留键和 /violations matcher 暂不混入本次。
[An] 17:40 首页申请豁免支持跨月补录：为豁免弹窗补充月份切换导航，放开跨月历史未交日期选择限制，修复「一键全选（七日）」在月初跨月选取上月末漏交作品，支持全量生效豁免跨月透传；定向单测与 tsc 检查全部通过。
[An] 17:44 首页豁免弹窗 UI 与交互体验升级：落地天数统计、一键清空、跨月标签点击直达月份日历、今日醒目标定、催交记录温和提示与动态原因引导；移除已选日期外层嵌套边框盒；单测与 tsc 均通过。
[CX] 17:49 顶栏行动中枢改造：新增轻量 summary 与统一 ActionItem，接入通知待办、豁免审批、权限申请和归属异常，顶栏改用缓存优先红点并支持失败保留；定向测试 19 项、ESLint、tsc、build 与本地组员账号桌面/390px 只读浏览器验收通过，未覆盖真实管理员/老板账号和生产部署 SHA，发布管理暂保零查询适配器。
[An] 18:01 修复首页日历翻阅历史月份（如8月）提交状态绿点缺失问题：首屏 loader 增加近 60 天已提交日期极简直查，并在日历/豁免弹窗打开时即时补齐历史活动，确保月初跨月翻看历史日历时绿点状态完整展示。
[CX] 18:42 顶栏行动中枢待办空列表回归修复：定位为 summary Top 8 被审批项挤满，导致普通通知待办有计数但无展示项；改为待办/审批分栏保底选择 topItems，补回归测试并通过定向测试、ESLint、tsc、diff check 和本地只读浏览器复验，未做真实写操作。
[CX] 19:22 行动中枢黑盒解释层：待办与审批卡片新增「详情」渐进展开（这是什么/为什么出现/优先级/下一步、处理影响），纯前端展示不动写链路；定向测试 17 项、tsc、ESLint 通过，本地测试账号只读浏览器复验待办/审批两页签展开解释均可见，summary 200、无控制台错误、除页面访问埋点外无业务写请求。
[CX] 19:30 行动中枢解释层按用户口径重做：移除每条待办/审批卡片的「详情」展开（三处：普通待办/实时审批/缓存审批），改为「待办」「审批」Tab 按钮悬停 Tooltip 解释这两个概念本身（getActionTabExplanation），删除展开 state 与旧解释函数并同步测试；定向测试 7 项、tsc、ESLint 通过，本地测试账号只读浏览器复验两 Tab hover 解释均正确、卡片无详情按钮、无业务写请求。
[CX] 19:40 修复提交豁免后申请按钮冻结：定位为三层整人级拦截（前端按钮 disabled、Server Action 任意 pending 拒绝、pendingDates 未传真实数据），与 REST API 的日期级防重口径不一致；改为日期级去重——loader 返回 pending 日期集合并传入弹窗锁定已申请日期，Server Action 只拒日期重叠并提示具体日期，停笔调养按钮解冻恢复可再次申请，提交成功后本地乐观合并 pending 日期；新增「日期不重叠可提交」「重叠被拒」回归用例，定向 5+18 项、tsc、eslint、diff check 通过，本地测试账号只读复验按钮可点弹窗可开且无写请求（该账号无 pending，有 pending 场景由单测覆盖），未改 migration。
[CX] 19:42 同类豁免冻结逻辑一次性清理：未挂载的老链路（申请豁免弹窗/QuickExemptionButton/DashboardWorkspaceHeader）同步改为日期级口径——移除触发按钮 disabled 与打开守卫、去掉 pendingDates 假回退 [today]、header 透传真实 pending 日期、提交成功后本会话乐观合并新申请日期并由日历锁定，按钮文案恢复「申请豁免」并以描述提示「其他日期仍可申请」；补渲染级回归用例锁住不再整人冻结，定向 3+9 项、tsc、eslint、diff check 通过；该链路当前无页面挂载，实机验收不适用。
[CX] 20:03 修复发布时间选择器下拉被截断：原因为 PublishedAtPicker 使用 absolute 定位，被外层 Framer Motion height 动画容器临时 overflow:hidden 裁剪，导致日历下半部分显示不全；改为 React createPortal 挂到 document.body，fixed 定位并跟随 trigger 位置，超出视口时内部滚动；tsc、eslint 通过，本地测试账号只读复验 9 月日历 31 天全部可见且无控制台错误。
[An] 20:28 依阿禅指示还原日历视觉调整：完全回滚豁免弹窗与日历网格样式，保持原有视觉呈现不变；同步修正 layout 单测中对豁免解冻标题的断言，1293 项全量测试绿灯通过。
[An] 21:12 升级审批撤回为行动中枢弹窗内嵌悬浮横幅：彻底移除远端右下角 Toast，改在行动中枢弹窗底部悬浮呈现（紧贴视线中心），配备真实的 5s→1s 每秒动态递减倒计时与线性微进度条，点击撤回即刻插回原卡片并支持离开时安全 flush 落库；tsc 与全量 1295 项测试绿灯通过。
[An] 21:22 落地审批历史决策查看与随时改判能力：在行动中枢审批 Tab 新增「待处理 / 已处理」子控制器；支持拉取历史已审批记录，卡片直接提供「改为拒绝 / 改为通过」按钮；接入 /api/exemptions/history 与 /api/exemptions/re-review 接口实现决策修改与月度考核自动重算闭环；tsc 与全量 1296 项测试绿灯通过。
[An] 21:32 升级行动中枢顶部为平级三栏 Segmented Controller：彻底消除内层 Sub-Tab 嵌套套娃，顶栏直观平铺「待办 | 审批 | 已处理」三栏并配备独立徽标；升级快捷键为 1-3 顺畅直达；tsc 与全量 1296 项测试绿灯通过。
[CX] 21:09 已在终端配置 `codex-dy`：执行后进入 `/Users/mac/Projects/dydata` 并启动 Codex；`.zshrc` 语法与别名识别验证通过，配置备份为 `/Users/mac/.zshrc.bak-20260901-210920`。
[CX] 22:06 全量审查 Antigravity 行动中枢、豁免历史改判及跨月/发布时间修复：tsc 通过，npm test 1296/1296 通过；发现改判状态与 Grant/Profile 非原子、service-role RPC 调用/用户级清理范围错误、审批分类缺失导致请假/豁免混并、撤回失败无回滚且卸载 flush 不可靠、事件未驱动 Dashboard 重算等 P0/P1，结论不允许直接 push。
[CX] 23:14 完成行动中枢/豁免历史改判/跨月链路 P0-P2 全部修复（本地未提交）：新增原子改判 RPC migration（`20260901120000_re_review_exemption_atomically.sql`，锁定申请行 FOR UPDATE、禁止自审/归档/跨范围、拒绝只停当前 request 的 grant、通过只恢复当前 request 的 grant、Profile 投影按剩余 active grant 重算、附 btree_gist 排除约束防跨标签页重复提交）；重写 `/api/exemptions/re-review` 改用登录会话调 RPC；Pending/History/ActionCenter 补齐 exemption_category；审批严格按分类分组并按完整日期并集折叠天数；Undo 改为多条并行可分别撤回 + 失败恢复原卡片 + sendBeacon/keepalive flush；dashboard 监听改判事件刷新月历与实发/应发；修复上海时区近 60 天少一天；移除无效 batchProcessing。tsc、npm test 1323/1323、git diff --check 全部通过，**未 push**，等阿禅确认。
[CX] 2026-09-02：近期高风险项目复盘后修复历史日报编辑的静默不同步缺陷：编辑详情已唯一确认的视频现在通过 `video_id + account_id` 精确更新，不再按发布时间猜测；视频同步失败向用户返回明确错误；无效视频编号被拒绝。全量测试 1330/1330、tsc、git diff --check 通过。未改数据库/权限，未单独提交。
[CX] 2026-09-02：完成岗位管理专项高风险复审并修复两类口径缺陷：①运营/达人爆款判定的历史样本改为同账号全历史有效播放日报，不再受责任人补录归属或只查上月影响；缺失播放量不计样本；②岗位月报默认月份与翻月上限统一按 Asia/Shanghai 计算。共享数据集仍严格区分 historyRows 与紧邻 previousRows，避免环比误把历史累计当上月。新增 5 个回归用例，npm test 1335/1335、tsc、lint、diff check 通过。npm run build 受当前环境 Turbopack 子进程绑定端口 Operation not permitted 失败，非 TypeScript/代码错误；未 push。
