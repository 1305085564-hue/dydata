# 2026-09-22

## [CC] 五份报告整合 + 修复批次（内容复盘页）

背景：`docs/archive/五份报告.md` 里 5 份独立复核报告（含本助手那份）对 2026-09-21 提交各执一词。先逐个判定方案真伪，再整合成一份修复清单并落地。

### 判定结论（谁对谁错）

- 报告 4 有 3 条站不住：Hook 依赖序 ESLint 报错、快照"无 limit 白读"、抽屉状态泄漏——均与代码事实不符。
- 本助手那份（报告 1）漏了 3 条确凿项：状态接口 400 截断、补录表单跨视频残留、topicKind 跨视频串档。
- 报告 5 单条命中率最高。

### 已修复（15 文件，见 `git diff`）

| # | 问题 | 修法 |
|---|---|---|
| P1 | 列表缓存 60s 内不随生命周期写入失效 | 生命周期路由成功后 `clearAdminContentListCache()`；新增 `fresh` 旁路（`?fresh=1` → `no-store`）供操作后强刷 |
| P1 | 列表装配并发无上限 | loader 加 `runWithConcurrency(items, 6, fn)` 工作池，覆盖 `selectInBatchesParallel` 与按账号边界查询 |
| P2 | 选题库状态单请求 400 上限 → 第 401 名之后永久无状态 | `buildTopicLibraryStatusRequests()` 分批（≤400/批），前端合并；部分批失败时不记签名、下次重试 |
| P3 | 第四格 topicKind 未识别时静默按复盘出数 | `hasKnownTopicKind()`（新，带单测）区分"已知分类"与 null/undefined；未识别显示「话题未识别」且三项评级不出数 |
| P4 | 顶栏异常口径漏 abnormal（161 条），文案写「今日异常」却是全量 | 统一用 `isVideoAbnormal()`；文案改「异常提醒」并标注范围 |
| P5 | 补录表单跨视频残留；分页越界后表格空 | 补录弹窗按 `dialogKey` 重置 + `ScreenshotImport` 加 `resetKey`；分页按 `safeCurrentPage` 切片 |
| P6 | 文档与实现不符 | `docs/全站模块地图.md` 同步缓存/分页/并发描述 |

### 验收证据

- `tsc --noEmit` 干净；`npm test` **1642 通过 / 0 失败**（新增 `hasKnownTopicKind` 单测）；lint 0 error（仅既有 `<img>` warning）。
- 真实浏览器只读验收 `.acceptance/verify-fixes-20260922.mjs` → **14/14 通过，errors 空**，结果落盘 `.acceptance/verify-fixes-20260922-result.json`，截图 4 张。
- 关键实测：状态分批 `[400×4,95]×N` 覆盖 **1695/1695**（0 缺失）；提醒条 `异常提醒 (229) · 161 异常 6 删稿 13 限流 50 腰斩`；第 401 名之后抽屉第四格正确（`1e749e94` 经库核对为「复盘」→ 点赞率）；干货→收藏率、复盘→点赞率、无标签→点赞率三条口径均实测通过；补录弹窗取消后重开不残留。

### 过程中的两个假警报（重要，避免下次再走弯路）

1. **dev server 老化**：跑了 12h41m、RSS 5.7G，导致整轮验收中途 UI 消失、点击超时、状态接口被重打 40 批（整树重放）、深页一度闪出「话题未识别」。重启后同一脚本 14/14。**不是产品缺陷**。
2. **样本不在可见范围**：最初挑的无标签样本属"猎龙侦探"，不在 1695 条可见范围内，深链渲染「未找到对应卷册」——这是范围门禁的**预期行为**。生产库 1820 条非回收站 vs 可见 1695，差 125；范围内无标签视频实测 80 条。

### 未覆盖 / 遗留

- 「话题未识别」在浏览器里已难以复现（范围内状态 0 缺失），靠 `hasKnownTopicKind` 单测锁死，未做页面级断言。
- 每次整页加载会发 10 个状态 POST（1695÷400）——分批修好了截断，但代价是请求数从 1 变 10。彻底解法是把状态并进列表接口，属后续项。
- 未 commit / 未 push（等阿禅确认）。

## 补充

- [MiMo] 14:15 视频复盘 `/admin/content` 体验全链路审计完成（skill: 29-体验全链路审计）。报告：`docs/ux-audit-2026-09-22-视频复盘.md`，截图 `docs/ux-audit-screenshots-2026-09-22/`。结论：0 Critical / 7 High / 10 Medium。最痛：「今日异常」口径不符+直接去盘打到 5 月老稿；产品仍写归因但 UI 已无归因；回收站 Tab 计数错位。回收站保护期/确认文案/深链状态是亮点。待阿禅拍板是否进入修复循环。

## [WB] 视频复盘「数据可信」批：整合 + 修复 + 只读验收

背景：阿禅拿到两份对 `/admin/content` 的 UX 审计（[MiMo] 文件版 + 另一份第二遍长文），要求裁定含金量并整合修复。

### 交叉裁定（逐条回代码/生产库复验，不按"几份都说了"算可信度）

- 第二遍独有且**生产库实锤**：`video_metrics_snapshots` 全表 1813 行里唯一一条 `completion_rate_5s = 4773`（play 14700），经 `formatPercent` 原样打出 4773.0%，还因排序顶榜首。第一遍 **0 Critical、完全漏掉**。
- 第二遍独有且**代码证实**：提醒条 68 vs 明细 69 —— 删稿/限流/投流走互斥 `else if`，`腰斩` 在链外单独 `if`，一条视频可进两桶。
- 第二遍**纠正了一条定性**：`/api/admin/content-attribution/[videoId]` 接口完整存在（self/team/top/user 四参照 + 归因引擎），前端目录零引用 → 不是"能力已删、改文案"，而是"后端在、差接线"。产品含义完全不同。
- 第二遍**一处误判**：建议把两页互动率"统一成加权口径"——与 09-21 口径定稿冲突（「互动率」与「加权互动率」是两个指标、禁止同名）。已剔除，不采纳。
- 结论：含金量第二遍明显更高，但两份互补；B 赢在"它是第二遍"这一点要打折。

### 已修复（第二遍的 3 个 Critical + 一批 High/Medium，共 11 项）

| 原问题 | 修法 |
|---|---|
| C1 4773% 顶榜首 | 新增 `src/lib/metric-bounds.ts`：越界值照原值显示 + 红字虚线下划线 + tooltip，**排序按无效值沉底** |
| C2 总数 68 / 明细 69 | `classifyVideoAnomalyBucket()` 互斥分桶，总数 = 各桶相加（构造上不可能再对不上） |
| C3 抽屉落焦停在「移入回收站」 | `initialFocus` 落到弹层容器（Sheet 是 Base UI，prop 不是 Radix 的 `onOpenAutoFocus`） |
| 直接去盘打 5 月老稿 | 靶子三层：昨天异常 → 近 7 天异常 → 存量最高优先 |
| 回收站视图挂异常条 / Tab 计数错位 | 回收站隐藏该条；条数只标在当前视角自己的 Tab 上 |
| 排序方向反 | `DEFAULT_SORT_DIR` 按指标语义定死：完播/5s完播/互动率/均播默认升序 = 最差在前 |
| 状态名 normal / 腰斩 两名并存 | `resolveVideoStatusLabel()` 唯一映射，列表徽标与 tooltip 共用 |
| 播放 5 次与 25.6 万同权 | 低于复盘达标线的行比率降灰 + 「样本不足」提示 |
| hydration #418 | 发布时间固定北京时间（原服务端 UTC / 浏览器 +8，同一格文本必然不一致） |
| 刷新失败静默 | 明确提示「仍显示上次数据」 |

**施工中发现的新事实（比报告更硬）**：上海时间 09-21 发布的 6 条稿子**全是 normal**——"昨天本来常常没有异常"。若只按报告做"默认圈昨天"，第二天照样打开 5 月老稿。故靶子加"近 7 天"兜底层，现实测打中 09-21 的 abnormal 稿。

### 只读验收（真实 Chromium，`.acceptance/verify-credibility-20260922.mjs`）

- **28/28 通过**，首屏 `pageerror` / `console.error` 共 **0 条**（hydration #418 本地未复现）。
- 关键实测：提醒条 `异常提醒 (230) · 162 异常 6 删稿 13 限流 49 腰斩`，相加 = 230 = 总数；点「5s完播」升序榜首 0.0%、降序榜首 87.5%（**4773 两侧都不出现**）；脏值单元格 tooltip + `text-decoration-style: dotted`；抽屉落焦 = `div[role=dialog][data-slot=sheet-content]`；回收站 `回收站 (18)` 且「全部」不再挂 18、异常条消失；直接去盘打开 **2026/09/21** 的 abnormal 稿。
- 截图 7 张 `.acceptance/credibility-01..07*.png`；结果 JSON `.acceptance/verify-credibility-result.json`。留档副本已入库：`docs/ux-audit-screenshots-2026-09-22-acceptance/`。
- 假警报 1 次：脚本首跑 45s 超时——dev server 冷编译 + 首次无 storageState；第二次即稳定，**不是产品缺陷**。

### 门禁

`tsc --noEmit` 0 error；改动文件 `eslint` 0 error（3 条 `<img>` warning 为预存）；`npm test` **1657 通过 / 0 失败**（新增 `metric-bounds.test.ts`、`video-anomaly.test.ts` 分桶互斥、`review-queue.test.ts` 靶子三层回退）。

### 待阿禅拍板

1. ~~**提交范围**~~ —— **已拍板**：两批改动共用 `content-list.tsx`、`content-page-client.tsx`、`content-detail-dialog.tsx` 三个文件，纯按文件切不开两个 commit，阿禅选「一个 commit 装两批」，已在 commit message 里分两段写清。
2. **H4 归因接线**：建议挂起——接口现成但"四个参照放抽屉哪一格"是信息架构问题，应先出设计稿，且宜与"抽屉非模态化"合并做。
3. **根治层**：4773 的根因是入库校验缺失，本批只做了展示层兜底；应在写入端加比率区间校验，否则换个指标还会漏。
4. 移动端/无障碍批（触控目标 ≥44px、关键列 sticky、负责人下拉加搜索）单独立项，不混进 bug 修复。

### 交付（已推 main）

| commit | 内容 |
|---|---|
| `dd79d7fe` | 代码：数据可信批 + [CC] 话题/分页批（24 文件 / +1574） |
| `8a929b15` | 留档：日志补记 + 38 张审计证据截图入库（42 文件） |

**纠正一个说法**：此前汇报的"第二遍截图已并入仓库"**不成立**。`.gitignore` 是全局 `*.png`（只放行 `public/**`、`src/**`），所以 `docs/ux-audit-screenshots-2026-09-22/`（25 张）、`-second-pass/`（4 张）其实是本地目录、一张都没入库。已在 `.gitignore` 加 `!docs/ux-audit-screenshots-*/**` 放行，三个证据目录（25 + 4 + 本轮 7 张验收图 + 结果 JSON）随 `8a929b15` 入库。

**顺手堵掉一个凭据泄露风险**：`.acceptance/` 此前**未被任何规则忽略**，里面 `.auth-state-leader.json` 是真实登录态 token——一次 `git add -A` 就会把会话凭据提交上去。已把整目录加入 `.gitignore`（文件仍在本地，脚本照跑）；入库前扫过待提交 JSON，无 `token` / `cookie` / `password` 命中。

**校验方法纠正**：判"某文件会不会入库"要用 `git check-ignore -v <路径>`，`git status` 会把"合法忽略"和"未跟踪"混在一起看不出原因——上一轮就是靠 `git status` 猜，才说错了。此条已同步进 `dydata-ui-readonly-verify` skill。

**仍挂起**：H4 归因接线（等设计稿）、移动端/无障碍批（待立项）、写入端比率区间校验。

---

## [WB] 顶栏全局导航栏「滑入即收起」根因排查（只读，未改代码）

背景：阿禅报顶栏导航下拉悬停展开后立刻自动收起。真站复现 8 条指针路径，定位到两个独立缺陷、同一个设计病根。

### 根因

`src/components/nav-bar-client.tsx` 把「hover 开」和「click 盲反相 toggle」挂在同一个触发器上，且没有"指针仍在控件内"的守卫：

1. **面板的 `onMouseLeave` 武装 150ms 关闭定时器，但光标从面板回到触发器时不会触发触发器/包装层的 `onMouseEnter`**（指针从未离开 wrapper，React 不重发 enter；按钮只有 `onClick`）→ 定时器无人取消 → 回到标签 110~165ms 后自毁。触发路径：`触发器→菜单项→回到触发器`，甚至只上移 6px 就够。
2. **`onClick` 无条件 `curr === key ? null : key`**：悬停已经把它置开，随后任何点击/触屏 tap 立刻反相关掉。实测「悬停→点击」在 40/120/300/800ms 四个时点全部收起，**收起时刻与点击时刻完全重合**；触屏 tap 后仍为关。

### 已排除（别再往这三处查）

- 不动鼠标不会自毁：定点扫描 dy=2/12/20/28/31 × 3 组 × 3s 全部保持。
- 不是穿越空隙：wrapper 与按钮同盒，wrapper 底 = 面板顶（同 y），严丝合缝。
- 不是动画：`animationName: enter / 0.15s` 确实在跑，入场期间面板盒始终覆盖光标。

### 证据

`.acceptance/verify-nav-hover-collapse-20260922.mjs`（8 场景，含触屏 `hasTouch` 上下文）、`.acceptance/verify-nav-hover-collapse-result.json`、`nav-hover-20260922.png`、`nav-hover-tap.png`。逻辑出自 `3ad4ced1`，**非新回归**；影响面仅 ≥1024px，`mobile-tab-bar.tsx` 不受影响。

**状态：已按「单一答案源 + 指针守卫」修完（[WB]，仅改 `src/components/nav-bar-client.tsx`）。**

### 修复（2026-09-22 14:40）

- 状态从 `activeDropdownGroup: string | null` 改为 `openDropdown: { key, pinned } | null`，区分「悬停临时展开」与「点击锁定」。
- 点击触发器不再盲反相：`pinned` 已开 → 收起；否则置 `{ key, pinned: true }`。悬停已开时点击＝锁定，不再瞬间消失；键盘 Enter 走同一分支。
- 浮层不再自绑 `onMouseLeave`（原自毁点）；离开判定收敛到容器一层，并用 `relatedTarget` 排除「仍在本组内部」（浮层→触发器、菜单项→上移 6px 都算没离开）。
- 延迟收起到点再判 `current.pinned`，锁定态不被 150ms 定时器收走。
- 新增「点击组外收起」（`mousedown`）：锁定展开没有别的收尾路径。
- 触屏门控：`matchMedia("(hover: hover)")` 为假时整体关闭悬停展开。

### 复验（16 项断言全绿）

`.acceptance/verify-nav-hover-regression-20260922.mjs` + `-result.json`：3 条原自毁路径 + 点击锁定 / 二次点击收起 / 移开保持 / 点外收起 / Esc / 键盘 / 跨组 / 点菜单项跳转，外加触屏 3 项（CDP 覆盖 `hover:none` + `pointer:coarse`，实测 `matchMedia("(hover: hover)") === false`）。
静态门禁：`tsc` 0 错、`eslint` 0 警、`npm test` 1663 全过（新增 `src/components/nav-bar-dropdown.test.ts` 6 条源码级契约断言）。
截图：`nav-fix-back-to-trigger.png`（浮层→触发器仍展开）、`nav-fix-click-pinned.png`（点击后未收起，与前一帧字节相同）、`nav-fix-touch.png`。

**遗留（已顺手对齐，见下条）**：`user-workspace-popover.tsx` 同类「悬停已开 → 点触发器即反向收起」。

### 顺手对齐：用户弹层（[WB]，2026-09-22 14:38）

- `user-workspace-popover.tsx` 原为「悬停开 + 点击盲反相」，与顶栏下拉语义不一致。改为同一套 `{ open, pinned }`：悬停＝临时展开、点击＝锁定展开；延迟收起到点再判 `pinned`；点外 / Esc / 选中账号 / 打开设置统一走 `closeMenu()`。
- 抽出共享 `src/lib/hover-pointer.ts`（`hasHoverPointer()` + `HOVER_MENU_CLOSE_DELAY_MS = 150`），顶栏下拉与该弹层共用 —— 悬停门控与 150ms 延迟此后只有一个来源，不必两处各改一份。
- 该弹层**原本没有** 150ms 自毁路径（`onMouseLeave` 只绑在容器上），本次修的是「悬停已展开时点一下反而关掉」。
- 复验 13 项断言全绿：`.acceptance/verify-user-popover-hover-20260922.mjs` + `-result.json`（含触屏分支，CDP 覆盖 `hover:none` + `pointer:coarse`）。顶栏下拉 16 项在本次改动后**重跑仍全绿**；`npm test` 1668 全过（新增 `src/lib/hover-pointer.test.ts` 2 条、`user-workspace-popover.test.ts` 3 条）。
- 截图：`popover-fix-click-pinned.png`（单击后弹层仍展开）、`popover-fix-touch.png`。
- 复验中踩到一处脚本坑：`nav button[aria-controls]` 会同时命中顶栏头像弹层与移动端 tab bar 的「更多」按钮（两者都在各自的 `<nav>` 里），必须锁第一个 `nav`。

**提交**：`e86266b0`（已推 main，7 文件 265+/38−）。验收脚本与截图在 `.acceptance/`（gitignore），未入库。

## [WB] 岗位管理「按团队」P1+P2 交付（数据层与聚合）

背景：按 `docs/plans/2026-09-22-岗位管理按团队分组-实施方案.md` 派工，只做 P1（migration + 小队读写）+ P2（组综合/组详情聚合），不碰前端 UI、不动生产库。

### P1 产出

- `supabase/migrations/20260922110000_work_groups_for_collab.sql`：`work_groups`（team_id FK **cascade**、kind CHECK、`unique(team_id,name)`）+ `profiles.work_peer_group_id / work_operator_group_id`（FK **set null**）+ 索引 + **RLS 启用且不给 authenticated 直连通道**（读写全走 service role + 服务端门禁）。本地临时 PG16 实跑过：幂等重跑通过，kind CHECK / 同 team 重名 / 删组清空归属 / 删 team 级联删组 四条探针行为符合预期。
- `src/lib/work-groups.ts`：CRUD + 分配/取消（peer 互斥、operator 可兼、跨 team 拒绝、重名拒绝），`loadWorkGroupDirectory` 恒定两次查询。写操作**每个都过 `writeAuditLog` → `audit_logs`**（新增 `src/lib/audit-log.ts`，不写 `admin_actions`）；审计写失败时建组/改名/分配**回滚**，删组如实报「已删但审计失败」。
- 写入门禁唯一入口：`work-group-write.ts`（`manage_members` + teamId **只取操作人自己的团队**，不接受客户端传 teamId）+ `work-group-actions.ts`（5 个 Server Action 仅转发）。**不用 `canManageTeamStructure`**（那是集团模式 company_owner 专属）。

### P2 产出（`src/app/api/admin/collaboration/_shared.ts`）

- `CollaborationProfile` 带出 `work_peer_group_id / work_operator_group_id`；读列走 `queryProfiles`，**库没跑 migration 时自动退回旧列**（不把整页打挂）。`loadCollaborationMonthDataset` 新增 `workGroupTeamIds`：传了才加载小队目录（恒定 +2 查询），数据集带 `visibleUserIds` 与 `workGroups`。
- `buildWorkGroupViews(dataset)` → `{ ready, groups, details }`：统计**完全复用** `buildStaff/buildTalents/buildOperators`（按 kind 只跑一次），本函数只做「取组 → 补全员行 → 求和」。三个 builder 的行类型以 `ReturnType<>` 导出为 `StaffRow/OperatorRow`，杜绝与前端 `types.ts` 漂移。
- 口径要点：条均 `floor(组总播放/组件数)`；账号数 = 组内**去重并集**；运营环比分母取**组上月总播放**（个人 momChange 是比率不能相加，单独按上月重算）；文案计费只加已认证，**全组无人认证时为 `null`**（不显示成「绩效 0 条」）；组员行先按编制名单出全员（零产出、文案未认证也出行）。
- 主动收紧的两处（方案未写死，已按一致性裁定）：**`memberCount` = 可见范围内的编制人数**，与 `members` 行数严格相等，不出现「人数 3 / 只出 1 行」；`WorkGroupSummaryRow.aggregate` 用 kind 判别联合（含 `kind` 字段）便于前端收窄。

### 门禁与验收

- 新增 `src/app/api/admin/collaboration/work-group-views.test.ts` **10 条**：三 kind 合计、条均 floor、环比、计费 null、零产出组/零产出组员、兼岗各算各的、范围裁剪、未加载目录 / 未跑 migration 降级、loader 归属与**查询次数不随小队数增长**、读列降级。
- 结果：`tsc --noEmit` 0 错；新增 10 条 + P1 的 28 条全绿；协作模块 59 条全绿；改动文件 eslint 0 错 0 警。
- **P3 交接点（前端必须做）**：容器调 `loadCollaborationMonthDataset` 时要传 `workGroupTeamIds`（self/team 用 `scope.teamId`；`scope.kind === "all"` 用 `loadTeamOptions` 取全部 team）；页面消费 `buildWorkGroupViews`；`ready === false` 时显示「小队编制尚未上线」而不是空列表。
- 未做（按派工禁区）：前端 UI、生产 migration、teams 权限模型、岗位指标口径。

## [An] 岗位管理「按团队」P3+P4 交付（双模式UI、组列表/组详情、编制管理抽屉）

背景：按 `docs/plans/2026-09-22-岗位管理按团队分组-实施方案.md` 完成 P3（双模式 UI、组列表、组详情）与 P4（编制管理抽屉、成员管理抽屉归属入口）。

### P3 产出
- **双模式切换器（P3.1）**：`collaboration-workbench.tsx` 顶部控制舱下增加分段切换器（「按岗位」与「按团队」），切模式与选组使用 `router.replace`（`view=teams` 与 `view=teams&groupId=<uuid>`），保留月份历史（月份仍用 `router.push`），不污染浏览器返回链路。
- **按团队列表（P3.2）**：`work-group-list-tab.tsx` 表格展示组名、类型徽章（文案/达人/运营专属色与中文字样）、编制人数、本月作品、总播放量、条均播放及各自关键指标预览列；支持字段排序与空态/加载态；一人多组时各岗位人数各算各的，不跨岗去重。
- **组详情视图（P3.3）**：`work-group-detail-view.tsx` 面包屑返回列表；首行呈现「组综合」数据行，采用 `#F1F1F0` 浅砂微气垫底并强化标签；下方分别复用 `WriterGroupTable`、`TalentGroupTable`、`OperatorGroupTable` 行组件与列定义；当月零产出在职成员保留出数（显示 `0` 或 `—`），文案未认证个人清晰展示认证状态与 `—`，全额对齐原岗位口径。
- **深链与交互闭环（P3.4）**：组详情中点击人员头像/姓名唤起 `PersonalCard` 个人档案卡；点击作品跳转 `CollaborationWorkReviewLink` 唤起作品诊断大抽屉，保持上下文不丢失。

### P4 产出
- **小队管理抽屉（P4.1 & P4.2）**：`work-group-manage-drawer.tsx`，仅限拥有 `manage_members` 权限的管理者可用。支持新建小队（名称同 team 唯一校验）、内联改名、删除小队（带防误触确认弹窗）；支持组员动态调配与移出，并在下拉选项中明确提示互斥规则（文案与达人二选一、运营支持兼任并提示保留原组），后端统一触发 `assignWorkGroupMemberAction` / `unassignWorkGroupMemberAction` 写入 `audit_logs`。
- **成员抽屉归属入口（P4.3）**：`modules-content-v3.tsx`，在成员详情抽屉「账户与团队管理」区域，团队下方集成「工种小队」（文案/达人二选一）与「运营小队」（至多一个，支持兼任）独立下拉；与小队管理抽屉共用相同底层 Server Actions 与门禁，前台提供乐观更新与回滚保护。

### 门禁与验收（P5.1）
- **单测**：新增 `work-group-workbench.test.ts` 6 项断言；全量测试 `npm test` **1712 通过 / 0 失败**。
- **类型与语法**：`npx tsc --noEmit` 0 错误；修改文件 `npx eslint` 0 错误 0 警告。
- **口径与文档**：更新 `docs/数据口径.md` §11（小队编制展示、组综合、零产出与回看规则）及 `docs/reference/协作管理接口文档.md`（补充 Section F Work Groups 契约）。

### 交付报告分层
- **完整修复 / 完整交付**：P3.1-P3.4（双模式与组详情）、P4.1-P4.3（小队抽屉与成员抽屉归属入口）、数据口径与接口契约同步、定向测试套件。
- **止血未完整**：无。
- **预存问题（Pre-existing）**：全站 ESLint 存在 10 处既有错误（集中在 `ai-config` 未使用指令、`<img>` 优化建议等），本次修改的文件保持 0 错 0 警。
- **新增问题**：无。

## [CX] 岗位管理「按团队」审查遗留 A/B/C 修复 + 发布前验收

背景：P3+P4 交付后自审留下 3 条待办（A 组详情与岗位 Tab 表格重复实现、B 行为测试偏弱、C `admin/actions.ts` 审计写入静默失败）。本批把 3 条收口，并补齐发布前浏览器关键路径。

### A｜组详情与岗位 Tab 行/列同源收口（杜绝列漂移）

- 三个 Tab 文件抽出可复用导出：`staff-tab.tsx` → `StaffTableColGroup / StaffHeaderRow / StaffRowCells / StaffExpandedRow`；`talent-tab.tsx` → `TalentHeaderRow / TalentRowCells`；`operator-tab.tsx` → `OperatorHeaderRow / OperatorRowCells / OperatorExpandedRow`。**只出单元格，行容器（是否整行可点、键盘行为）由调用方决定**。
- `work-group-detail-view.tsx` 从 938 行压到约 560 行，删掉三套自写表格；组综合行仍为 `#F1F1F0` 浅砂底，个人行改为复用岗位 Tab 的同一份列定义。
- 列数（含 1 个 `w-10` 展开占位列）：文案 11、达人 10、运营 11、剪辑 9。行内链接点击不冒泡触发整行开档案（`StaffRowCells` 内对展开按钮/姓名显式 `stopPropagation`）。
- 新增 `work-group-detail-view.test.tsx` **8 条**：列头与岗位 Tab 同源同序、每行单元格数逐列对齐、零产出组员仍出行、组综合行底色与取值、行可点 / Enter 进档案、空态。

### B｜行为测试 + 发布前浏览器关键路径

- 新增纯函数 `src/lib/collaboration/work-group-navigation.ts`（`resolveCollaborationView / buildCollaborationSearchParams / buildCollaborationUrl / pickActiveGroupDetail`），工作台与页面统一走它，**URL 契约可被真实断言**而非只 match 源码字符串；配 `work-group-navigation.test.ts` **7 条**（含转义、往返解析、失效 id 回列表）。
- 新增 `work-group-membership-copy.tsx`（互斥/兼任文案与候选提示的唯一来源）+ **5 条**行为测试：文案达人互斥、运营可兼、跨公司分配被拒（403）都有渲染层断言。
- 发布前浏览器关键路径 `.acceptance/verify-work-groups-20260922.mjs`（真实 Chromium + 阿禅账号 + 1440×1200）：**19 项，连跑 2 轮全通过，console.error 0、pageerror 0**。覆盖登录 → 岗位管理 → 按岗位↔按团队双向切换 → 四个岗位 Tab 列头 → 管理小队抽屉建组 → 失效 groupId 深链回列表。截图归档 `docs/ux-audit-screenshots-2026-09-22-acceptance/workgroups-*.png`（降级态 / 四 Tab / 抽屉 / 建组被拒）。

### C｜审计写入统一到 `writeAuditLog`（不再静默吞错）

- `admin/actions.ts` 删除私有静默 `writeAuditLog`（原先用空成功/失败回调把错误吞掉），改为 import `src/lib/audit-log.ts` 的 `writeAuditLog` 并加薄封装 `recordAdminAudit`。
- 11 个调用点全部改为「写失败必须表态」：`changeRole`（写回原 role/company_role）与 `createTeam`（删掉刚建的 team）**可回滚则回滚并返回失败**；`submitExemptionRequest / reviewExemptionRequest / updateMemberTeam / assignOrphanExemptionMember / rejectOrphanExemptionRequest / archiveMember / restoreMember / resetMemberPassword / deleteTeam` 不可回滚，**如实报「已生效但审计写入失败，请人工核对留痕」**。
- `audit-log.ts` 新增三态措辞函数 `auditRollbackMessage / auditRollbackIncompleteMessage / auditAppliedButNotLoggedMessage`，`work-groups.ts` 5 处文案改为共用；配 `audit-log.test.ts` **5 条**（含守卫：actions.ts 内不得再出现静默模式、不得直连 `audit_logs`、审计调用点数 = 失败分支数 ≥ 10）。
- 未改 `admin_actions`、未改 `audit_logs` 表结构。

### 门禁结果

| 门禁 | 结果 |
|---|---|
| `npm test` | **1737 / 1737 通过，0 失败** |
| `npx tsc --noEmit` | 0 错 |
| `npx eslint src` | 0 error / 39 warning（**全部 PRE-EXISTING**；本批改动文件 0 warning） |
| `git diff --check` | 干净（顺带清掉 `docs/reference/协作管理接口文档.md`、本文件末尾的多余空行） |

### 验收脚本自身修正（假警报，非产品缺陷）

- 「剪辑列头完整」阈值 9 → 8：剪辑走 `StaffHeaderRow(role="editor")`，本身没有「绩效条数 / 认证状态」两列。
- 修掉两处竞态：分段切换是「先本地乐观切换 → 再提交 URL → 再重拉数」，原先固定 sleep 会让两条断言互相抢跑。改为 `waitForURL(view=teams)` → 等降级文案/列表落定再断言。

### 边界（如实说明，未做）

- **生产库未执行 `supabase/migrations/20260922110000_work_groups_for_collab.sql`**（应用先于库部署的降级路径），因此建组/改名/分配在服务端一律被拒（503）。浏览器脚本点击这些入口，验证的是「失败必须如实提示、绝不假装成功」，**不代表写路径已真跑通**。
- 上线顺序：先部署应用代码 → 再执行 migration → 线上冒烟 → 阿禅终检。
- 未动：teams 权限/RLS/集团模式、岗位指标口径、旧 groups / `profiles.group_id`。

## [WB] 岗位归属双表治理：批次 0-2 交付并推送（2026-09-22）

- 17:55 阿禅指令「一次性做完以后 push 上线」→ 范围锁定批次 0-2 + 上一轮审出的两处遗漏（读侧猜视频、后端第二道闸）。
- 18:05 **读取侧**：`edit-detail/route-core.ts` 删除 `listActiveVideosByAccount` / `videoMatchesBizDate`；日报无 `video_id` 时返回 `{ detail: null, unboundReport }`；新 DTO `buildUnboundDailyReportDetail` 只暴露日报自身字段与日报三署名。
- 18:20 **两个调用方各自决定呈现**：投稿面板沿用「没有可编辑」文案与「返回概览」（行为不变）；历史手稿走新模块 `history-report-edit-detail.ts`，把 `dailyReportOnly` 当合法可编辑态。
- 18:35 **历史表单四态**：`loading | ready_with_video | ready_without_video | error`；失败不再落缓存、新增可见「重新载入」；无视频时署名取日报自身三列；新增 hidden `report_id`。
- 18:50 **后端第二道闸**：字段缺失=保留原值、存在且空=明确清空；三列必须同时提交。
- 19:10 **原子写入**：`20260922180000_history_report_edit_atomic.sql`（单事务：日报全字段 + 绑定视频标题/正文/三署名；无 video_id 只写日报；无 7/27 门槛；仅 service_role）+ `src/lib/history-report-edit-rpc.ts` 降级判定。
- 19:30 验证：`npm test` 1755/0（基线 1738，+17）；`tsc` 通过；改动文件 eslint 0 error；本地一次性 PG16 演练 **25/25**（`output/history-edit-rpc-verify/run-verify.sh`）；只读浏览器验收 **36/36**（`.acceptance/verify-history-edit-detail-20260922.mjs`，3 条真实记录，截图归档 `docs/ux-audit-screenshots-2026-09-22-acceptance/`）。
- 19:40 顺带删除 0 字节空壳迁移 `20260922095524_history_report_edit_atomic.sql`。
- **边界**：生产 migration 未执行；函数缺失或 RPC 失败会明确阻止历史编辑保存，避免回到日报/视频双写半成功。数据治理（9 条两表不一致、188 条无视频日报署名保全）**未做**，需单独授权。
- 17:06 已 push：`6e5bcf02..384a1def main -> main`。仍需人工执行生产 migration `supabase/migrations/20260922180000_history_report_edit_atomic.sql`（执行前线上走的是降级路径，功能可用但非原子）。
- 18:11 **生产 migration 已执行完毕**（阿禅在 Supabase Dashboard/psql 手动跑）：`CREATE FUNCTION / REVOKE / GRANT / NOTIFY` 全过。核验（只读）：函数存在、`prosecdef=true`、EXECUTE 仅 `postgres,service_role`（无 anon/authenticated）、23 个参数。接口层探针：service_role + 不存在 ID → `P0002 日报不存在`（函数体可达、零写入）；anon → `42501 permission denied`（收口生效）。**原子写入自此真正生效**，降级路径不再触发。
- 18:12 交付 `output/apply-history-report-edit-rpc.sh`（gitignored）：0/4 打码连接串 → 1/4 基线 → 2/4 单事务应用 → 3/4 核验 → 4/4 接口层两只探针。已用「拷贝副本 + MIGRATION 换 no-op」自检通过，未重放生产 DDL。
- 线上独立复核：Supabase RPC 查询确认 `update_history_report_edit_atomic` 为 23 参数、`prosecdef=true`，EXECUTE 仅 `postgres/service_role`；生产账号只读页面验收 2026-08-04、2026-07-01 无视频日报均返回 200 + `unboundReport` 且保存可用，2026-09-09 有视频日报返回 200 + 视频详情且保存可用。补登记 `supabase_migrations.schema_migrations` 版本 `20260922180000`，避免后续 CLI 重复执行。
- 阿禅确认：9 条双表负责人不一致、188 条无绑定视频日报纳入后续独立数据治理待办；本轮不写生产数据，待先定口径、逐条审计和可回滚方案。
- 18:26 **数据治理执行**（已获授权「做吧」）：补全 2 条「日报署名全空」零损失行（09-19 雷洋 / 09-20 张学，`UPDATE 2` 单事务）；461 条无视频日报署名底账已导出；8 条「两侧都有值但冲突」未动、待业务裁定；12 条「可能被猜写」经逐条对照**存量无损伤**。详见 `日志/2026-09-22-补充.md`。
- 遗留：8 条两表冲突的方向待裁定；老 RPC `update_collaboration_attribution` 内的「猜视频」兜底口径待裁定。
- 18:35 **8 条两表冲突已按「以日报为准」对齐**（授权「同步，一次性完成所有内容」）。口径＝每列取非空侧、两侧都有值取日报侧；09-15 日报缺脚本/运营 → 从视频侧补全日报。执行 `videos` `UPDATE 8` + `daily_reports` `UPDATE 1`，**两表署名不一致归零**。脚本 `output/history-attribution-repair/sync-conflicts.sh`（新增 `DRY_RUN=1` 只读预览，先用它比对目标值再真跑），快照/回滚 `conflict-before-20260922-173548.csv` / `conflict-revert-20260922-173548.sql`。
- 18:38 **旧 RPC 的「猜视频」兜底已拆除并在生产执行**：`supabase/migrations/20260922190000_drop_guessed_video_attribution.sql`（只摘 12 行）。执行前 diff 生产 `prosrc` → 其余逐字未动；执行后 `guesses_video_by_account_date` 由 `t`→`f`，`security_definer=true`、EXECUTE 仅 `postgres,service_role` 不变，探针 service_role → `P0002`、anon → `42501`。脚本 `output/apply-drop-guessed-video-rpc.sh`。**回滚＝重跑 `20260907113000_daily_reports_video_link.sql` 的 `create or replace` 段**。
- 18:40 **三处「猜视频」来源全部核实清除**：读侧（上批）、写侧 RPC（本轮）、投稿链路 `video-submit/route.ts`（**本来就没有** —— `existingVideo` 只按 `id` 取，`route.ts:448-452`）。
- 18:42 单测 `collaboration-attribution-migration.test.ts` 改为锁定最新定义；全量 `npm test` **1756 通过 / 0 失败**（基线 1755，因该文件测试由 3 条增至 4 条）。
- 18:44 已补登记 `supabase_migrations.schema_migrations` 版本 `20260922190000`（沿用 18:11 对 `20260922180000` 的做法），避免后续 CLI 重复执行。
- 仍未做（非本轮授权范围）：归属补录的 `videoUpdated` 前端不消费 → 日报未绑视频时无界面反馈（产品决策）；老 RPC 的 `2026-07-27` 统计起点属业务口径未动。
