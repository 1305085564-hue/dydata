[CX] 2026-07-20 视频复盘/素材库删除能力盘点：当前运行中的复盘工作流为 /admin/content，素材库为 /admin/videos，二者共用 videos 主表；复盘结论在 content_feedback_cards，截图路径在 video_metrics_snapshots，尚无作品回收站或管理员删除接口。旧 /video-review 使用独立 work_submissions 产量凭证表且已归档，但迁移与 DELETE 路由仍允许成员删除当天记录，和现行产品规则冲突。group_leader 不是纯残留，仍参与业务角色、团队数据范围、复盘入口及分组管理；回收站方案必须只授予 admin/owner，并将移除组长模型列为独立治理项。建议 videos 增加回收站软删除字段，所有正常查询/RPC 统一过滤，永久删除只允许 owner 且回收满 30 天，并保留复盘/审计历史。
[CX] 2026-07-20 作品回收站后端闭环已完成：新增 videos 生命周期迁移和服务端 PATCH `/api/admin/videos/[videoId]/lifecycle`；仅 owner/team_admin 可在范围内回收/恢复，group_leader/member 拒绝，永久删除仅 owner 且回收满 30 天，改为 purged 墓碑并原子写 audit_logs。正常管理、驾驶舱、分析、提醒、话题等 videos 查询已过滤 active；反馈卡保留并返回作品生命周期标记。work_submissions DELETE 路由及 authenticated RLS 删除权限已封禁。未运行迁移、未操作线上数据、未提交或 push。
[CX] 2026-07-20 回收站验收阻断修复：/admin/videos 数据层与列表接口支持 trash，owner/team_admin 才可读取，trash 首屏绕开 active RPC；video-submit 新建作品失败时改走 service_role 受限回滚 RPC，回滚失败不再静默；purged 作品重复 purge 仅重试本系统截图清理，不重复写审计。未执行迁移、未操作线上数据、未提交或 push。
[An] 2026-07-20 作品回收站前端与操作交互已全部完成：支持回收站 Tab 角色防御与 UI 显示，列表/详情页移入回收站、恢复、30 天永久删除限制及二次确认弹窗交互全部闭环；看板历史复盘卡原作品已回收/已删除状态 Badge 显示支持。静态类型及 986 个单元测试全部通过。不修改数据库迁移，不提交，不 push。
[An] 2026-07-20 回收站列表在 trash 视图下已隐藏 24h播放量/互动率/涨粉，只保留必需 6 列；统一了永久删除 Dialog 内文案；清除了未使用类型导入；986 个单元测试及 ESLint 校验已通过。
[CX] 2026-07-20 作品回收站最终前端验收收尾：移除 video-page-client.tsx 中遗留的未使用 AdminVideosView 类型导入；针对性 ESLint 与 git diff --check 通过。未执行迁移、未操作线上数据、未提交或 push；全量 tsc 仍由既有 .next 路由辅助类型错误阻断。
[CX] 2026-07-20 修复视频复盘归因舱遗漏：该全屏工作台此前未接收回收站权限，导致 owner/team_admin 看不到入口；现已在标题栏增加移入回收站操作，成功后关闭并刷新列表，并补回归测试。未执行迁移、未操作线上数据。
