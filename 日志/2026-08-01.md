- 2026-08-01 继续收尾 [CX]：补回生产已执行的 `20260707090000` 历史占位迁移，避免迁移台账断裂；确认生产存在 9 条未登记的旧迁移，未混入本次 AI 变更。单独执行 `20260731193000_ai_feature_control_plane.sql` 成功，5 个归档功能在 `ai_feature_bindings` 与 `ai_feature_config` 均为 `is_enabled=false/lifecycle_state=archived`，归档快照为 5+5，事务函数和 PostgREST 新字段验收通过；迁移版本已登记 applied。应用代码已提交，未 push。
- 2026-08-01 发布前验收 [CX]：复核生产两张 AI 配置表的新字段、5 个归档渠道和 10 条快照均正确；80 项 AI 定向测试与相关 ESLint 通过。全量测试仍有既有导航栏无障碍断言失败；本次 AI 改造提交准备推送。
- 2026-08-01 发布完成 [CX]：`main` 已推送至远端提交 `9f5cb2c6`，远端核对一致；`supabase/.temp/cli-latest` 的 CLI 自动换行改动未纳入提交。
- 2026-08-01 清理完成 [CX]：生产仅执行 `20260801103000_remove_dead_ai_features.sql`，未夹带 9 条更早未登记历史脚本；删除团队报告分析、旧版 AI 洞察、单视频分析、成长诊断及 5 个旧 AI 配置的程序、配置记录、归档快照和旧结果。生产核验：9 个删除项在配置表、绑定表、归档表均为 0；首页截图识别仍为 active/enabled；成长页面、现行视频复盘、智能预警和飞书催交保留。应用代码与模块地图已同步。
- 2026-08-01 推送完成 [CX]：数据库迁移提交 `73f7d300` 与应用清理提交 `08fcfe63` 已推送 `main`；远端核对为 `08fcfe63808dfa8857d22c71a945d708c8ef9f1c`。全量测试 1051 项中 1044 项通过、7 项为既有导航栏无障碍断言失败；定向 AI 测试 37 项和相关 ESLint 通过；TypeScript 仍有既有 `.next`/settings/sidebar-badges/topics 等错误。
- 2026-08-01 UI优化完成 [An]：完成后台 AI 配置页 `/admin/ai-config` 的 UI/UX 体验修复。在 `bindings-client.tsx` 中为“截图识别”添加“首页核心”高亮 Badge 与专属阻断警告弹窗；增加“已清理功能说明”静态告示区；优化状态 Badge 视觉分层与图文按钮；完成术语中文化降维及表格横向溢出响应式保护。定向 ESLint 校验通过。
- 2026-08-01 选题库视觉重构完成 [An]：根据用户反馈精精准优化：取消非选中胶囊在 Hover 时伪装成白底 Active 卡片的误导设计；将非选中胶囊在 Hover 时的透明灰阶直接加深为 `hover:bg-zinc-200 hover:text-zinc-900`；而真正当前选中的 Tab 依然独占纯白浮凸卡片（`bg-white shadow-2xs border-zinc-200/90`），选中与 Hover 态形成 100% 极其清晰的辨识对比！
