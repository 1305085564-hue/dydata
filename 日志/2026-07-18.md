# 2026-07-18

- [09:40] [CX] 处理 owner 再次无法登录问题：确认 Supabase Auth 账号存在、未禁用，profiles 仍为 `owner / active`；根因是仓库脚本残留 owner 旧测试密码，运行 `scripts/reset-admin-password.mjs` 会把 owner 密码覆盖回旧值。已按阿禅指定新值重置 owner 密码并用 Supabase 登录接口验证通过（日志不记录密码明文）。防复发：`reset-admin-password.mjs` 改为必须显式传 `DYDATA_RESET_EMAIL` / `DYDATA_RESET_PASSWORD`，且重置 owner 必须额外设置 `DYDATA_CONFIRM_OWNER_RESET=YES`；`test-supabase-auth.mjs` 与 `ux-audit-runner-v2.mjs` 改为只读本机 `.env.ai-test.local` / 环境变量中的 AI 测试账号，不再硬编码 owner 邮箱密码。验证：仓库 `rg` 未发现旧/新密码明文残留；定向 eslint 通过；AI 测试账号登录脚本通过；已新增全局记忆边界，禁止后续为测试擅自重置 owner 或硬编码 owner 密码。
- [09:48] [CX] 排查登录关联页面：确认忘记密码提交会向 Supabase recover 发出 200 请求，但随后立即跳回 `/login` 且不保留可见成功提示；`next` 回跳参数在登录页进入忘记密码/注册时丢失；登录失败直接透出 Supabase 英文错误；注册链路存在 Auth 用户已创建但 profile/account/join request 后续失败时不回滚的半账号风险；重置密码成功后未显式退出 recovery 会话。未改业务代码；只跑浏览器复现与登录相关定向测试，未创建账号、未改密码。
- [10:08] [CX] 完成登录关联页面修复：忘记密码成功后留在当前页展示稳定提示，真实发送失败显示中文错误；登录、注册、找回、重置全链路保留安全 `next` 回跳；登录错误不再透出 Supabase 英文；重置页无会话时直接提示链接失效，成功后显式退出 recovery 会话；注册资料、默认账号或入团申请失败时补偿删除本次 Auth 用户并退出会话，清理失败仅记服务端日志并提示联系管理员。新增 20 项认证回归测试全部通过，浏览器确认回跳和无效链接页面无控制台错误，`npm run build` 通过；未创建账号、未发送真实重置邮件、未改线上数据。
