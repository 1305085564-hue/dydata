# 2026-07-18

- [09:40] [CX] 处理 owner 再次无法登录问题：确认 Supabase Auth 账号存在、未禁用，profiles 仍为 `owner / active`；根因是仓库脚本残留 owner 旧测试密码，运行 `scripts/reset-admin-password.mjs` 会把 owner 密码覆盖回旧值。已按阿禅指定新值重置 owner 密码并用 Supabase 登录接口验证通过（日志不记录密码明文）。防复发：`reset-admin-password.mjs` 改为必须显式传 `DYDATA_RESET_EMAIL` / `DYDATA_RESET_PASSWORD`，且重置 owner 必须额外设置 `DYDATA_CONFIRM_OWNER_RESET=YES`；`test-supabase-auth.mjs` 与 `ux-audit-runner-v2.mjs` 改为只读本机 `.env.ai-test.local` / 环境变量中的 AI 测试账号，不再硬编码 owner 邮箱密码。验证：仓库 `rg` 未发现旧/新密码明文残留；定向 eslint 通过；AI 测试账号登录脚本通过；已新增全局记忆边界，禁止后续为测试擅自重置 owner 或硬编码 owner 密码。
