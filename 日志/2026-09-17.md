# 2026-09-17

- 00:10 [CX] 启动项目可维护性提升专项第 0 批：确认用户授权六批连续实施、定向测试、精确提交，并在第 5 批满足发布条件后合入推送 main；禁区为不执行数据库 migration、不改真人角色或生产业务数据、不携带其他任务改动。基线 HEAD 694dba3b、分支 main、远端 SSH；`npx tsx --test "src/app/api/video-submit/*.test.ts" "src/app/api/exemptions/apply/route.test.ts" "src/app/api/exemptions/review/route.test.ts" "src/app/(app)/dashboard/actions.test.ts"` 109/109 通过，`npx tsc --noEmit --pretty false` 通过。记录写入 `output/maintenance/00-基线.md`（output 被忽略，仅本地证据），总进度更新为第 0 批本地通过；Sentry/Vercel 外部权限和第 4 批实页基线待对应批次核实。
- 00:28 [CX] 完成项目可维护性提升专项第 1 批本地施工：新增 `src/lib/observed-mutation.ts`、`src/lib/sentry/capture-mutation-error.ts` 与测试，三条写接口 `/api/video-submit`、`/api/exemptions/apply`、`/api/exemptions/review` 接入服务器诊断头、阶段日志和 5xx 固定分类 Sentry 捕获；保留原状态码、JSON、写入顺序和补偿逻辑，不执行数据库 migration、不做生产写入。验收：定向 `npx tsx --test ...` 118/118 通过，`npx tsc --noEmit --pretty false` 通过，目标 `npx eslint ...` 通过；`output/maintenance/01-验收.md` 为本地证据，Sentry 外部收件和生产可观测待第 5 批核实。
- 14:10 [CX] 进入第 5 批发布恢复与整体收口：从远端 main 0cd9dcdc 建立隔离候选 codex/maintenance-release-20260917，按第 1–4 批逐个摘取 12 个维护性提交，未携带权限专项脏文件或 migration。核对发现第 3 批真实写入、第 4 批两项实页场景、Sentry 收件和 Vercel Ready 仍缺证；vercel ls 返回 fetch failed，E2E 登录环境变量缺失。补维护入口、工程事实链接、旧测试路径文档引用和收口进度；未推送、未执行数据库 migration 或生产写入。
