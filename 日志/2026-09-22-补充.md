# 2026-09-22 补充日志

## [WB] 岗位署名双表：数据治理执行（18:20–18:30）

授权：阿禅「做吧」（承接「9 条两表不一致对齐、188 条无视频日报署名保全」）。

### 一、只读复核（与批次 0 基线对比）

| 口径 | 批次 0 基线 | 本次实测 | 差异原因 |
| --- | --- | --- | --- |
| 两表署名不一致 | 9 | **10** | 期间新增 1 条保存 |
| ├ 日报三列全空 / 视频侧有值 | — | 2 | |
| └ 两侧都有值但不同 | — | 8 | |
| 无视频日报总数 | 461 | 461 | — |
| ├ 带署名 | 176 | 176 | — |
| └ 三列全空 | — | 285 | |
| 无视频日报 × 同日恰好 1 条视频 | 12（口径：active 视频） | 18（口径：全部视频） | 口径不同，非数据变化 |
| └ 其中带署名 | 12 | 12 | |

### 二、三个新的数据层事实（决定了「不能靠推理定方向」）

1. **`videos` 没有「署名来源」列**，无法区分某条署名是投稿时录入还是归属补录写入。
2. **`audit_logs` 没有归属变更动作**。全表动作分布里与归属相关的只有历史遗留的 `update_report` × 4，其余是角色/权限/激励/封禁类。归属补录（290 次）没有留下可追溯记录。
3. **两表都没有 `updated_at`**（`daily_reports` / `videos` 只有 `created_at`）。

→ 结论：**没有任何证据链能判定「某条冲突署名哪一侧后写入 / 谁写的」**。方向只能由业务口径裁定。

### 三、已执行：补全 2 条零损失行

判据：`daily_reports` 三列全为 `NULL`，绑定视频三列至少一列有值 → 复制视频侧到日报侧，**不覆盖任何已有数据**。

| report_id | 日期 | 作品 | 补入 | 视频侧 |
| --- | --- | --- | --- | --- |
| `9ae2fb12-3567-429a-9739-0bf5ba6fccce` | 2026-09-19 | 新股全场MVP | 雷洋 / 雷洋 / 雷洋 | 同 |
| `466d0f27-ff2c-4753-af20-c5db6f75d7e1` | 2026-09-20 | 板块之间分歧转一致 | 张学 / 张学 / 张学 | 同 |

这两条正是「视频复盘里有、岗位管理里查不到」的同一类（岗位管理读 `daily_reports`）。执行后该类不一致归零。

- 执行：`output/history-attribution-repair/apply.sh`（4 步：目标清单 → 快照 → 单事务应用 → 复核），结果 `UPDATE 2`
- 快照：`before-targets-<ts>.csv`、`revert-source-<ts>.csv`、`revert-<ts>.sql`（回滚即把三列置回 NULL）
- 复核：目标类不一致 **0**；两表不一致剩余 **8**

### 四、触发器前置核查（关键，避免误伤）

`daily_reports` 上两个非内部触发器，触发条件绑定了列序号：

| 触发器 | 时机 | 绑定列 | 行为 |
| --- | --- | --- | --- |
| `preserve_daily_report_manual_source` | BEFORE UPDATE | 列 38 = `data_source` | 旧值为 `manual` 时钉住 `manual` |
| `trg_daily_reports_clear_fulfillment_absent` | AFTER UPDATE/DELETE | 列 2,3 = `user_id`, `report_date` | 删除同人同日的 `status='absent'` 履约记录 |

本次 UPDATE 的 SET 只含三个署名列 → **两个触发器都不触发**，`data_source` 与履约标记未被改动。
（列序号用 `ordinal_position` 反查确认；`tgtype` 19 = ROW|BEFORE|UPDATE，21 = ROW|AFTER|UPDATE|DELETE。）

### 五、署名保全：底账已落，未改写任何数据

`output/history-attribution-repair/inventory-no-video-reports.csv` —— 461 条无视频日报的 `id / user_id / account_id / report_date / data_source / 三列署名` 全量导出，可回滚。

对「历史上会被猜中写回」的那批（无视频日报 + 同日恰好 1 条视频 + 带署名，12 条）逐条对照日报侧与视频侧：

- **11 条完全一致**
- 仅 1 条不一致：2026-09-06 付洋洋（日报三列）vs 严一鸣 / 黄玉清 / 严一鸣（视频侧）

→ **没有被猜写坏**。该风险的危害面是前瞻性的（代码已修），存量无损伤。故不改写、不迁移。

### 六、未完成：8 条「两侧都有值但冲突」

清单与观察见方案文档 §4.4。要点：

- 5 条（08-18、09-05 大A祖训、09-06、09-15、09-18）日报侧信息量更大（区分出独立剪辑人 / 脚本人），视频侧把该列复制成另一个人；按「不销毁区分度」方向应是日报 → 视频。
- 3 条（08-07、09-04、09-05 柚子选股法）完全对称 —— 日报侧复制成 A、视频侧复制成 B，无偏向。
- **署名驱动文案计费与运营绩效，误改有实际成本 → 不自动对齐。**

### 七、被否证的假设（记录下来，避免下次重犯）

进入本轮时的直觉：「视频侧是批量粗放赋值、日报侧更细」。对照实测：

| 口径 | 三列全同占比 |
| --- | --- |
| 冲突所在账号 · 视频侧 | 255 / 317 = **80.4%** |
| 冲突所在账号 · 日报侧 | 158 / 211 = **74.9%** |
| 全库 · 视频侧（active） | 1596 / 1840 = 86.7% |
| 全库 · 日报侧（有绑定视频） | 1111 / 1323 = 84.0% |

两侧粗糙度基本相同（差约 5 个百分点），**不构成判定依据**。该直觉作废，未用于任何写操作。

### 八、方法学踩坑

1. **明细查询漏过滤条件会跑成笛卡尔积**：`join lateral ... on true` 取「同日视频」时忘了加 `same_day_videos = 1`，输出被放大成「每条日报 × 全部同日视频」（200+ 行），差点据此得出「大面积不一致」的错误结论。**先跑聚合计数确认桶大小，再跑明细。**
2. **`$TS）` 写法在中文相邻处会炸**：bash 在 UTF-8 locale 下把全角括号算进变量名 → `TS: unbound variable`。变量后紧跟中文标点一律写 `${TS}`。
3. 汇总口径的「同日」定义会显著改变桶大小（active-only vs 全部视频：12 vs 18）。**报数字时必须带上口径。**

### 九、完成：8 条「两侧都有值但冲突」已按「以日报为准」对齐（授权「同步，一次性完成」）

**口径（合并语义）**：每一列取非空的那一侧；两侧都非空时取日报侧。

- 依据：其中 5 条（08-18、09-05 大A祖训、09-06、09-15、09-18）日报侧信息量更大（区分出了独立的剪辑人或脚本人）；另 3 条对称无偏向 → 统一走同一口径，避免逐条二义。
- 特例：09-15「揭秘，板块之间的分歧转一致」日报脚本/运营为 `NULL` → 取视频侧值**补全日报**（不制造新的 NULL），与 4.2 的两条同构。

执行：`videos` 侧 `UPDATE 8`；`daily_reports` 侧 `UPDATE 1`（仅 09-15）。核验 **不一致 = 0**（全表扫描同样为 0）。

- 脚本：`output/history-attribution-repair/sync-conflicts.sh` —— 新增 `DRY_RUN=1` 只读预览（先打清单与落快照、不写库）；本次先用它比对目标值，确认无误后才真跑。
- 快照 / 回滚：`conflict-before-20260922-173548.csv`、`conflict-revert-20260922-173548.sql`（一条 `-f` 回退两侧原值）。
- 触发器：本次只写三个署名列 → `daily_reports` 两个触发器均未触发；`videos` 表无触发器（已核）。

**保留的疑点（已执行，但记下来）**：09-04「尾盘选股法」与 09-05「柚子选股法」在两侧**交叉串位**（一侧脚本=严一鸣 / 张皓宇，另一侧正好相反）→ 说明其中一侧**整体错位**，而非简单漏填。两表都没有 `updated_at`、`audit_logs` 也没有归属变更动作，无法判定偏移发生在哪一侧。本次按裁定口径执行，保留单命令回滚。

### 十、完成：旧 RPC 的「猜视频」兜底已拆除并在生产执行

`public.update_collaboration_attribution`（协作补录）里原有一段：日报没有 `video_id` 时，按「同账号 + 同业务日期」取 `published_at/uploaded_at` 最新的那条 active 视频覆盖署名 —— **与读侧完全同构**，命中唯一一条时会把署名写到另一条作品上。

- 新 migration：`supabase/migrations/20260922190000_drop_guessed_video_attribution.sql`，只摘掉那 12 行。
- **执行前 diff**：生产 `prosrc` 与新定义逐行对比 → 只少那 12 行、多 1 行注释，其余逐字未动。
- **已在生产执行**（脚本 `output/apply-drop-guessed-video-rpc.sh`，4 步）：`guesses_video_by_account_date` 由 `t` → `f`；`security_definer=true` 与 EXECUTE 仅 `postgres,service_role` 保持不变；探针 service_role → `P0002`（可达且零写入）、anon → `42501`（收口有效）。已补登记 `supabase_migrations.schema_migrations` 版本 `20260922190000`（否则 CLI 会重复执行）。
- 回滚：重跑 `20260907113000_daily_reports_video_link.sql` 里的 `create or replace` 段。
- 测试：`src/lib/collaboration-attribution-migration.test.ts` 改为锁定最新定义，含「不得再出现 `account_id = v_report.account_id` / `timezone('Asia/Shanghai'`」反向断言。
  **坑**：新 migration 的**注释**里引用了被删掉的旧写法，反向断言前必须先剥掉 `--` 注释行，否则误报。

**三处「猜视频」来源全部核实**：

| 来源 | 结论 |
| --- | --- |
| 读侧 `edit-detail/route-core.ts` | 已删（`20260922180000` 批） |
| 写侧 RPC `update_collaboration_attribution` | 本轮已删并执行 |
| 投稿主链路 `video-submit/route.ts` | **本来就没有** —— `existingVideo` 只按 `id` 取（`route.ts:448-452`） |

### 十一、本轮发现（未处理，留给产品决策）

归属补录返回的 `videoUpdated` **前端不消费**（全仓仅 `handlers.ts:114` 与其单测出现该 key）→ 日报未绑定视频时，「视频那侧没同步」不会被告知用户。拆掉兜底**不产生新提示**（原本猜不到时同样是静默），故无 UX 回归；要不要补提示属产品决策。

### 十二、更正：8 条里 5 条是「日报绑错视频」而非署名冲突 → 已回滚（18:47）

复核第十节的「交叉串位」时发现：8 条里有 **5 条的日报标题与其绑定视频的标题完全不符** —— 根本不是同一条作品。

| 日报日期 | 日报标题 | 被它绑定的视频 | 真正对应的作品（同账号同名视频） |
| --- | --- | --- | --- |
| 08-07 | 选股只看强不强，别纠结高不高 | 炒股不需要复杂的技术，也能赚到米 | 选股只看强不强…（08-11 · 小芸姐） |
| 09-04 | 短线挖掘方法 | 尾盘选股法 | 短线挖掘方法（09-03 · 张皓宇） |
| 09-05 | 炒谷要背的6句口诀，每天开盘前默念一遍 | 大A祖训 | 炒谷要背的6句口诀…（09-04 · 小芸姐） |
| 09-05 | 尾盘选股法 | 柚子选股法 | 尾盘选股法（09-04 · 张皓宇） |
| 09-06 | 揭秘龙头股4大节点，看懂离悟道不远了 | 09.07盘前：外围AI硬件大涨… | 揭秘龙头股4大节点…（09-05 · 曾文聪） |

**判据**：这 5 条日报的三列署名，与「同账号标题同名视频」的署名**逐列完全相同**（三重独立吻合，不是相似度推测）。例：09-04 日报（严一鸣/张皓宇/严一鸣）= 09-03「短线挖掘方法」视频；而它绑的 09-04「尾盘选股法」是 张皓宇/张皓宇/严一鸣。

- **结论修正**：「09-04 ↔ 09-05 交叉串位」错位的是**日报的 `video_id` 绑定**，不是署名。日报与视频的署名各自都是**它自己那条作品**的真实值 —— 这也解释了为何看起来像镜像：两条作品本就分属两种分工（一条严一鸣选题、一条张皓宇选题），绑错后才像互换。
- **我上一轮的误判**：按「以日报为准」把日报署名同步到视频 → 等于把 A 作品署名写到 B 作品上，属**污染**。**已整体回滚**：`output/history-attribution-repair/rollback-misbound-videos.sh`（执行前预检「当前值是否等于上一轮改后值」，不符即中止；`UPDATE 5`；逐条核验全 OK；附反向回滚 SQL 与快照 CSV）。
- **保留 3 条**（标题与视频一致 = 真冲突，绑定正确）：08-18、09-15、09-18，上一轮对齐结果不变。
- **口径修正（本次最重要的一条）**：比对两表署名**必须先限定 `btrim(daily_reports.title) = btrim(videos.video_title)`**。按此口径重算：**真冲突 = 0**。不加上这个条件，227 条绑错配对会被误算成冲突 —— 上一轮就是被它骗了，才会误判成「有一侧整体错位」并动手改数据。

### 十三、新发现（未处理）：全库 227 条日报绑定了「标题不符」的视频

- 1323 条有绑定视频的日报中：标题一致 1073、不符 250；不符里 **227 条能在同账号找到标题同名视频**（= 疑似绑错），另 23 条找不到（可能只是标题写法不同）。
- 涉 **30 个账号**；按月：3月 9 / 4月 23 / 5月 **109** / 6月 26 / 7月 4 / 8月 25 / 9月 31。250 条里 236 条是「同日」绑定（日报日期 = 视频发布日）→ 指向旧「按账号 + 日期猜/选视频」的绑定方式。
- 227 条中仅 **5 条**同时署名不一致（即上节那 5 条），其余 222 条署名恰好一致（同账号作品多由同一批人承担）→ **长期未被发现**。
- **风险**：这些日报一旦被编辑保存，会把日报署名同步到**那条错误的视频**上 —— `video_id` 已有值，不走「无视频」分支，**四态改造救不了**，属残留污染通道。
- **未处理原因**：修正绑定需业务确认三件事 —— ①「日报标题 = 视频标题」是否等价于「同一条作品」；②同日有多条视频时该绑哪条；③置空还是改绑。且存在链式占用（正确的同名视频可能已被另一条日报绑走），不是单点改写。
