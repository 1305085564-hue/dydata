# 2026-08-29 未入团成员权限止血

## [CX] 2026-08-29 03:46

- 服务端：在 `src/app/api/topics/_shared.ts` 增加 `TEAM_MEMBERSHIP_REQUIRED` 稳定错误码、统一 403 响应和 active team membership 守卫；13 个 `/api/topics*` 路由全部接入，匿名 401 与 archived 原有 403 行为保持分离。
- 服务端：`/api/video-submit` 在解析请求和任何写入前检查 `membership_status=active` 且 `team_id` 非空；`/api/exemptions/apply` 在重复检查和插入前拒绝未入团，确认被拒时申请行数不增加；dashboard Server Action 使用相同文案“请先申请加入团队”。
- 权限基础：未知或缺失 `membership_status` 不再由权限信息默认归一成 active，避免带团队但状态不明的账号绕过新守卫。
- 页面配合：`/topics` 服务端先判断团队归属后再挂载 `TopicHubV2`；详情页新增服务端 layout 拦截；TopicHubV2 和详情页消费稳定 403 并显示申请引导；dashboard 未满足 active team membership 时不渲染 `VideoSubmitPanelV2`，继续使用既有 `JoinBanner` 申请/审核状态。
- 测试：新增 13 路由 × 4 身份守卫矩阵、日报提交守卫、豁免 REST/Server Action 无写入、dashboard/page gate、客户端 403 契约测试。
- 验收：`npx tsx --test ...` 定向 35/35；`npm test` 1166/1166；`npm run lint` 通过（0 error，仓库既有 61 warnings）；`npx tsc --noEmit` 通过；`npm run build` 通过；`npm audit --audit-level=high` 显示 0 vulnerabilities；`git diff --check` 通过。
- 边界：本次未改数据库、migration、RLS、Auth、middleware、设计规范和验收文档；未执行生产真实链路，以上测试与构建均为本地结果。孤立 pending 豁免队列、测试凭据治理、M-01 重复月历刷新、测试账号矩阵、生产环境真实链路复跑留后续批次。

# 批次 2 凭据止血与污染样本清理

## [CX] 2026-08-29 10:51

### 凭据与会话

- 停点确认已收到：用户确认轮换账号#1–#3，并要求同步替换本机保存的测试账号秘密。
- 只读匹配确认 3 个代号均对应真实 Auth/Profile 账号：账号#1 为 `company_owner`，账号#2 的文档“组长”标签对应数据库 `member`，账号#3 为 `member` 且未分配团队。
- 3 个账号均生成随机新密码并完成 Auth 更新；本机 `docs/reference/测试账号.md` 已同步替换，文件权限收紧为 `600`，未输出或提交秘密。
- 3 个账号均通过临时新凭据登录后立即执行客户端 `global signOut`，全局会话撤销完成；使用轮换前旧凭据逐个登录均被拒绝。未用旧凭据建立业务会话。
- 首次尝试把用户 ID 传给 SDK 的 admin sign-out 接口时被 Supabase 拒绝，未写本机秘密文件；随后按 SDK 要求改为临时新会话调用客户端全局退出，并重新轮换一组新密码，最终状态以第二次完整验收结果为准。

### 脚本脱敏

- 修改 `scripts/create-test-users.ts`：账号邮箱和密码改从 `DYDATA_TEST_*` 环境变量或本地 gitignored `docs/reference/测试账号.md` 读取。
- 删除创建成功日志中的 ID、邮箱、密码以及错误详情输出；新增 `--dry-run`，dry-run 只报告代号、凭据是否加载和角色映射，不连接 Auth、不写 `profiles`。
- 组员按 `member/company_role=member` 写入；组长测试样本按 `admin/company_role=admin` 双写，移除非法 `leader` 技术值。
- 新增 `scripts/create-test-users.test.ts`，覆盖 dry-run 无服务端凭据运行、敏感值不出现在输出、无明文密码字面量和无 `leader` 写入。

### 污染数据只读定位与清理

- 只读扫描覆盖 `daily_reports`、`videos`、`video_metrics_snapshots`、`video_tags`、`script_usage_records`、脚本/话术、内容片段、资产和违规记录；发现一组精确命中，均为账号#3在 `2026-07-30` 的测试提交。
- 用户确认“精确删除”后，严格按子表到主表顺序执行并复核：
  - `video_tags`：删除 4 条，剩余 0 条；
  - `video_metrics_snapshots`：删除 1 条，剩余 0 条；
  - `videos`：删除 `45eb9154-28e9-5ed4-a274-592ffe167c1a`，剩余 0 条；
  - `daily_reports`：删除 `554d539e-de22-4cf0-b0dc-8425e0a29f5e`，剩余 0 条；
  - `script_usage_records`：精确扫描无命中 ID，未执行无目标删除。
- 关联 Storage bucket 为 `submission-screenshots`，删除并复核 2 个精确对象：
  - `7195257f-6e3a-4ece-93cc-208bac4d4ab2/35253fae-4a4d-490b-b3d3-bc4371500aac/screenshot_1/2026-07-30T11-10-34-278Z-73d75b22-2902-43b7-9c11-2de2e4894f9f.jpg`
  - `7195257f-6e3a-4ece-93cc-208bac4d4ab2/35253fae-4a4d-490b-b3d3-bc4371500aac/screenshot_2/2026-07-30T11-15-02-124Z-0a1854b5-0604-41b8-868b-ddcf88a38ac2.png`
- 审计日志没有删除；没有做全表模糊删除、批量更新或 Storage 前缀删除。该样本原本参与日报历史和视频指标/标签统计，删除后不再参与这些测试历史口径，属于本次止血的明确副作用。

### 批次 2 验收

- `npx tsx --test scripts/create-test-users.test.ts`：2/2 通过。
- `npx tsx scripts/create-test-users.ts --dry-run`：通过；未执行 Auth 或 `profiles` 写入。
- `npm test`：1166/1166 通过。
- `npm run lint`：0 error，60 个仓库既有 warning。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；Next.js 16.3.3 仅保留既有 middleware 命名弃用提示。
- `npm audit --audit-level=high`：0 vulnerabilities。
- `git diff --check`：通过。
- 未执行生产真实链路；未把本地测试、Auth 管理操作或 localhost 构建结论写成生产通过。

# 批次 3 孤立 pending 豁免申请收口

## [CX] 2026-08-29 11:30

### 只读核验与方案边界

- 只读确认存在唯一一条 pending 且申请人当前 `profiles.team_id` 为空、`membership_status=active` 的归属异常申请；快照团队仍为原团队，处理对象使用账号#3代号，不记录邮箱、密码或 Token。
- 代码新增 `/api/exemptions/orphan` 只读明细接口：仅公司所有者可见具体行；普通管理员只获得待处理计数和主入口提示；不在 `/admin/fulfillment` 增加孤立申请入口。
- 孤立集合严格排除普通 `/api/exemptions/pending` 当前可见队列，避免同一申请在两个队列重复出现；申请人当前团队、快照团队和 scope 均在服务端重新读取，不信任页面卡片快照。

### 按确认执行的真实本地数据处理

- 用户在最终停点选择“1：分配团队 + 员工重提交”。仅在本地已登录管理端对该唯一异常行执行：分配到原快照团队；重新核验目标仍是未入团 active 成员；原 pending 申请结束为 `rejected`，保留申请人和快照字段；写入稳定 action `reject_orphan_exemption_request` 及固定审计说明“归属已修正，请重新提交”。
- 直接只读复核：账号#3当前团队已恢复为原快照团队且仍为 active；该申请不再 pending；同一申请人 pending 数为 0；审计行存在；数据库未增加 `review_note` 字段、未执行 migration。
- 账号#3重新提交未代为执行：这是用户选择方案中的后续业务动作，避免在没有新的业务申请内容和必要确认时制造豁免业务数据。

### 三处一致性核验

- 管理模块：处理后“待归属申请”消失；公司所有者仍能看到正常团队成员入口，普通管理员不获得异常明细。
- 通知中心：待审申请计数为 0，显示当前范围内没有待审批；无孤立处理按钮。
- 履约月历/汇总：`GET /api/admin/fulfillment/calendar?year=2026&month=8` 返回 200，成员数与统计均为 37，无空团队成员，月历内 pending 豁免数和统计 pending 数均为 0；页面无孤立入口，浏览器控制台无 error。

### 批次 3 验收与边界

- 定向孤立申请/管理模块/相关契约测试：22/22 通过。
- `npm test`：1181/1181 通过。
- `npm run lint`：0 error，60 个仓库既有 warning。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；Next.js 16.3.3 仅保留既有 middleware 命名弃用提示。
- 本批未改数据库、migration、RLS、Auth、角色体系、验收文档；未触碰 `/api/exemptions/pending` 过滤逻辑；未跑生产真实链路。
- 止血未完整项：既有数据库没有 `review_note` 列，因此无法写入字段级审核备注，改为固定审计 detail；后续结构治理需单独批次评估。孤立申请的 owner 专属完整队列、原申请 rejected 留痕和员工重提交闭环已按本批方案落地，员工实际重提交留给业务后续动作。
- 后续批次仍未开始：批次 4 角色语义统一；批次 5 可丢弃夹具与真实写链验收；以及凭据治理后续、测试账号矩阵、生产环境真实链路复跑。

## 批次 4：角色语义与账号#2角色修正

- 代码层新增统一角色标签：member 显示“组员”，admin 显示“组长 · 管理”，company_owner 显示“公司所有者”，archived 显示“已归档”；导航、移动端抽屉、设置弹窗、成员管理、权限提示和管理助手均使用同一 helper。
- 账号#2先做只读唯一性核对，再从管理端既有“切换身份”入口执行角色变更；未使用直接 SQL。变更后核对 role=admin、company_role=admin，membership_status=active 和团队归属保持不变。
- 账号#1仍为 company_owner，账号#3仍为 member；三条本地测试账号均唯一匹配 Auth 与 profile。账号#2的 change_role 审计记录存在。
- 有效权限按现有 company_role 解析为 admin：具备日常管理权限，不获得公司所有者的集团/系统能力，也不额外获得 manage_members。
- 页面核验：账号#2角色操作已从“提升为组长 · 管理”切换为“降为组员”，说明角色展示和刷新结果已生效；浏览器控制台无新增 error。
- 本地 gitignored 测试账号文档已将账号#2数据库核实角色同步为 admin，文件权限保持 600；未在日志、提交或交付报告中记录凭据。
- 批次 4验收：定向角色测试 20/20；npm test 1185/1185；npm run lint 0 error、60 条既有 warning；npx tsc --noEmit 通过；npm run build 通过；git diff --check 通过。
- 未覆盖：生产环境真实链路未跑；批次 5可丢弃夹具、账号矩阵和真实写链验收尚未开始；既有 lint warning 未在本批扩大处理。

# 批次 5：前一日业务数据口径、可丢弃夹具与本地真实验收

## [CX] 2026-08-29 15:14

### 先纠正业务口径

- 本批最终采用业务确认的时间语义：上传日为 `2026-08-29`，日报归属业务日为前一日 `2026-08-28`；管理侧按周/月汇总，不把“当天是否上传”当成业务完成标准。
- 前一轮准备阶段曾把 T+0 当作验收主线，这与本业务口径不一致；没有把该错误方案写成验收通过，也没有保留同日业务日报。准备阶段产生的 4 个截图对象已按精确路径清理，随后重新按前一日业务日完成本批验收。
- 代码和夹具契约固定要求上传日的前一日业务日；历史补录使用合法历史日期，发布时间默认归属日前一天 19:00，已存在的发布时间保持不变。

### 账号矩阵

- 批次 5账号#4：团队 A在职组员，用于同账号/同业务日重复提交与审批夹具。
- 批次 5账号#5：团队 B在职组员，用于跨团队边界验收。
- 批次 5账号#6：先按团队 A组员创建，再通过既有归档链路完成封禁；最终为 `member/member`、`archived`、团队归属为空，并有成员变更审计。
- 既有账号#1–#3的角色、状态和团队归属核验未发生变化。没有新团队、没有移动既有成员、没有写入业务夹具以外的数据。
- 凭据只保存在本地 gitignored 测试账号文件，权限为 `600`；源码、日志、提交和报告不记录邮箱、密码、Token 或 Auth ID。

### 真实浏览器与数据库证据

- 账号#4在次日页面选择业务日 `2026-08-28`，提交日报后页面进入“该日期已提交”；服务端请求为 `POST /api/video-submit 200`。数据库核对到该业务日一条日报、一条视频，指标和发布时间与页面历史记录一致。
- 使用两个保留的创建表单进行同账号同业务日重复提交：第一次为 `200`，第二次为 `409`；第二次没有新增日报或视频。为补齐这一契约，`/api/video-submit`在新建模式写入前增加日报/稳定视频记录的重复检查，编辑模式仍进入原有绑定校验流程。
- 历史编辑在页面完成并刷新，业务日期保持 `2026-08-28`，历史修正标题和内容仍然显示；未把次日上传日期错误地写成业务归属日期。
- OCR真实页面验收覆盖一次成功、一次低置信度失败、重新上传恢复，以及已保存截图在后续编辑状态中的复用；服务端只记录结构化结果，报告不输出外部错误明细中的敏感值。
- 豁免链路使用账号#4在 `2026-08-29` 创建申请，账号#1在管理端完成单条审批；通知中心待审卡片归零，数据库形成一条申请和一条授予，履约月度汇总与日历同步变化。另完成两条批量全成功，以及两条并发制造陈旧选择后的批量部分失败；刷新后待审数归零，已处理项不复现。
- 网络失败分支随后用同一类本地待审批夹具真实验证：临时让当前浏览器进入离线状态，点击“通过”后页面显示“网络连接异常，请重试”，待审卡片仍保留；恢复网络后再次点击，审批请求返回 `200`，卡片消失，刷新后没有复现。离线状态只在当前本地浏览器短时存在，验证结束已恢复在线。
- 本轮清理前收集到：日报 2、视频 2、24h 快照 2、标签 4、豁免申请 5、Storage 9；没有话术使用记录，因为测试指标未触发导粉话术写入。

### 精确清理与核验

- 清理使用固定 run_id 清单，不使用模糊条件；顺序为：`script_usage_records → video_tags → video_metrics_snapshots → videos → daily_reports → exemption_grants → exemption_requests → storage`。
- 清理器逐个 ID核验父子表归零，Storage逐个对象核验不存在；清理后脱敏计数为所有上述业务表 0、Storage 残留 0。认领没有被删除，只做非活动状态核对。
- 账号、Profile、归档审计和账号矩阵文件保留；未清理历史业务数据，未执行全表删除或批量写操作。

### 代码与测试

- 新增 `src/lib/batch5-fixture-contract.ts` 及测试：日期口径、run_id、Storage路径边界、批量全成功/部分失败/网络失败状态机、403/409结构化契约。
- 新增 `src/lib/fixture-account-matrix.ts` 及测试；新增账号矩阵施工脚本，重复 Auth/Profile 或基线异常时停止，不覆盖、不 upsert、不删除既有账号。
- 新增 `scripts/batch5-fixture-run.ts`：安全 dry-run、精确清单、固定清理顺序、逐组归零核验；本地清单权限 `600`。
- 新增视频新建重复冲突 helper 与测试；真实浏览器复验到第二次请求 `409`。
- 批次 5定向测试：12/12；`npm test`：1197/1197；`npm run lint`：0 error、60 条仓库既有 warning；`npx tsc --noEmit`：通过；`npm run build`：通过。构建仅有既有 middleware 命名弃用提示。

### 分层结论与未覆盖项

- 完整修复：本地账号矩阵、前一日业务日真实提交、历史日期语义、重复提交 409及不重复落库、OCR成功/失败/恢复、单条和批量豁免审批、月度履约同步、run_id精确清理和代码/全量测试均完成。
- 止血未完整：本地真实网络失败/恢复分支已完成，但本地真实结果不能替代生产验收；未在生产执行任何写操作。
- 预存问题：批次 3的账号#3员工重新提交仍未由业务实际执行；账号#2按现有权限模型不具备 `manage_members`，这不是本批新增问题；仓库既有 lint warning 未扩大处理。
- 新增问题：未发现新的业务数据残留或账号基线变化。
- 留给后续批次：凭据轮换/撤会话脚本治理、孤立申请队列深化、角色语义后续治理、测试账号矩阵补齐、M-01重复月历刷新、生产环境真实链路复跑；本批没有修改验收文档、数据库结构、RLS、Auth 设计或 middleware。

## 业务口径文件更新

- 用户确认“数据不是看当天有没有发，而是按周/月看，数据在第二天上传”后，正式更新 `docs/数据口径.md` 第 6 节。
- 新口径区分业务日 D 与上传时点 D+1：`daily_reports.report_date` 用于周/月聚合，`uploaded_at` 仅记录实际上传时间；基础信息与 24h 数据可以在次日同一次补录中提交，不产生第二条日报。
- 保留 T+0/T+1 作为观察阶段术语，但明确 T+0 不等于“当天必须提交”，T+1 不等于“新增第二条数据”。历史验收报告保留原始记录，不事后改写。
