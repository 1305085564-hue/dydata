[CX] 00:18 Desktop 表单按 b139b4f9 还原左侧垂直双截图与分工、右侧指标/标题/文案/提交栏；保留移动端独立组件与 OCR 链路。npx tsc --noEmit、npm test（1092/1092）、npm run build 全部通过；真实浏览器因无登录会话未执行表单截图验收；已提交本地 commit，未推送 main。
[CX] 02:32 修复 /dashboard 数据台加载失败：page.tsx 恢复通过 DashboardDataContainer 服务端加载 accounts/todayReports/monthSubmittedDates 等首屏数据；VideoSubmitPanelV2 对齐当前提交状态与豁免函数合同，账号切换回调重新生效；VideoSubmitFormV2 截图区接回现有稳定槽位组件。验证：npx tsc --noEmit、dashboard 相关 20 个测试、npm run build 均通过；未连接真实登录会话做浏览器实测。
[CX] 02:35 处理 /dashboard Webpack dev runtime 报 `__webpack_modules__[moduleId] is not a function`：确认生产 build 与源码已正常，错误来自旧 dev 进程 Fast Refresh/hot-update 缓存污染；停止 PID 94924，将 `.next/dev` 备份为 `.next/dev-broken-20260825-023440` 后重启 `npm run dev`。验证：新 dev 下 `/dashboard` 多次 200，`/api/dashboard/activity`、`/api/dashboard/operator-members`、`/api/dashboard/watch-overview`、`/api/exemptions/pending` 均 200，未再出现该 runtime 错误。
[CX] 03:41 修复 /dashboard V2 双栏比例与伸展关系：桌面截图槽固定为 290px 左栏内的双 105px 卡片；截图与团队分工保留约 24px 呼吸间隔；团队分工卡和右侧文案卡在各自内容满足后共同向下伸展并保持底边对齐；题材与形式默认摘要、按需展开。真实登录态浏览器实测：间隔 23px、两侧底边误差 0px、无横向溢出、控制台无错误或警告。验证：相关 48 个测试通过、`npx tsc --noEmit` 通过、`git diff --check` 通过；未提交、未推送。
[CX] 04:37 完成第一批 A 数据/状态层修复：首屏返回本月日报、默认账号创建后本次请求重查、活动查询错误不降级为空、历史日期状态与报告合并规则集中到纯状态 helper，并保留审批结果通知从 loader 经页面编排层传递。验证：dashboard page/activity/panel-state 目标测试 18/18 通过，目标文件 lint 无错误；全仓 `npm test`/`npx tsc --noEmit` 仍受其他代理未提交的豁免 action、豁免日历和缺失 logic 文件影响；未提交、未推送。
[CX] 04:45 完成第一批 B /api/video-submit 服务端校验与编辑安全：正常提交强制互动/完播截图、归属与确认、合法话题标签和导粉话术；异常提交允许无截图但要求文案；编辑要求完整 DTO、沿用原 video_id、缺截图时复用已确认旧资产并保留 rollbackSafely 错误日志。验证：video-submit 目录测试 35/35、npx tsc --noEmit、git diff --check 通过；未提交、未推送。
[CX] 04:45 完成第一批 C 请假/豁免契约与日期修复：V2 提供 range/category/dates/reason 对象回调契约；dashboard action 保留 leave/waive 分类、pending 防重和可见错误结果并返回 submittedDates；日历统一使用上海日期 helper，合并首屏与异步提交日期，阻断已交/请假/豁免/未来日期。验证：TZ=Asia/Shanghai 定向 25/25 通过，npx tsc --noEmit、目标 ESLint、git diff --check 通过；未提交、未推送。跨实例数据库级 pending 唯一约束仍需后续 migration/生产验证，本批未改数据库/RLS。
[CX] 05:18 主控完成 /dashboard 今日提交工作台业务闭环收口与审查：整合历史日期状态、活动错误阻断、审批通知展示、默认账号重查、豁免 Server Action 契约、上海日期 helper、截图错误名映射和 /api/video-submit 服务端校验；完整视频编辑因缺少可安全回填的前端 DTO 暂停保存以防覆盖旧值，历史日报编辑仍可用。验证：相关 tsx 测试 81/81、npx tsc --noEmit、git diff --check 通过；目标 ESLint 0 错 8 警告；真实登录态浏览器非写入验收通过基础填写、截图缺失禁提交、历史空态、豁免面板、桌面/移动无横向溢出和无 Console error；未提交、未推送。
- 09:53 [CX] 今日提交工作台完整视频编辑闭环：新增受鉴权的编辑详情 DTO/读取接口与前端全量回填，编辑保存固定原 video_id，保留旧截图和选题关联，并在详情冲突、缺失或不完整时明确阻断；全量 npm test 1138/1138、npx tsc --noEmit、npm run build、git diff --check 通过。真实浏览器已验证工作台可渲染且无运行时错误；两个可选账号当日均无已提交记录，未能进入“修改今日数据”实际回填/保存步骤，待有当日记录后补做只读验收；未提交、未推送。
- [CC] 11:20 下线"推流曲线/留存曲线"识别能力（前置清理，百度 OCR 接入前）：ocr-screenshot 删除 curve 类型识别、detectScreenshotType/分类重试、retention 分析段（segment_summary/bounce_peak_time/replay_peak_time），保留留存 4 数字指标；类型改由槽位直定 screenshot_1→data、screenshot_2→retention；两表单 buildOcrSummary 删曲线/分析分支；video-submit 的 retention_screenshot_url 改由 asset_role=screenshot_2 写入、curve_screenshot_url 不再写入新值（列与历史保留）；route.test.ts 曲线用例删除/改写（10 用例全绿）；维护手册新增"截图识别口径"。未动批改台读取展示，无 migration。验收：eslint 0 error（11 条预存 warning）、tsx --test 10 pass 0 fail、tsc --noEmit 全绿。未 push，待阿禅审查。注意：本文件同期有并行任务（dashboard rewrite）在同一工作区施工，video-submit/route.ts 出现过联合类型报错，已由并行任务自行修复，与本任务改动无残留冲突。

## [CC] 截图识别切换百度OCR · 第0步关账（约当前时间）
- 曲线/留存曲线识别能力清理验收通过：复跑 eslint(0错误)/tsx test(10 pass)/tsc(0 error) 全绿
- retention_screenshot_url 已改由 screenshot_2 槽位直写；curve_screenshot_url 停写保留历史
- 改皮肤任务确认完成后，与本次清理一并合并：feat/dashboard-rewrite-from-scratch --ff→main（19 commits），已 push origin/main
- 百度 OCR Key 实测通过（token/webimage 权限/识别准确/~1.3s），本地 .env.local 已配，线上 Vercel 环境变量未配
- 规划书已建：docs/plans/8.25-截图识别切换百度OCR-规划书.md；第1-5步（百度接入）未开始
- [CC] 编辑链路安全修复完成（未 commit/push）：POST /api/video-submit 编辑模式写入前经 edit-binding.ts 重新核对原视频/日期/日报/快照绑定（他人视频统一404、跨日期/不一致409、缺日报404、重复日报/快照409、缺快照422），编辑禁建新日报/新快照；follower_loss/homepage_visits/fan_play_ratio/cover_click_rate/avg_play_ratio 编辑时由服务端保留原值
- [CC] hasReusableConfirmedScreenshots 收紧：恰好两角色+合法站内URL+OCR asset 无重复+全部 confirmed，否则阻断提示重传；GET 编辑详情抽 route-core.ts 并新增 assigneeProfiles（仅按原记录三个责任人 ID 查 profiles，service role 前已完成归属校验）
- [CC] 草稿 key 隔离：新建/补交/编辑互不读写清除（video-submit-draft-key.ts，编辑 key 含 videoId）；新建态只读兼容旧共享 key；前端历史责任人显示 resolveAssigneeDisplay（归档→旧姓名+历史成员·不可选；无档案→历史责任人，不冒充本人），后端允许未修改的旧责任人保留原值、新指派仍限在职同队
- [CC] 新增测试：edit-binding.test(11)/route-core.test(7)/draft-key.test(6)/assignee-display.test(4)+edit-detail.test 扩展(截图复用5例)；全量 npm test 1167 pass、tsc 0 error、build 通过、eslint 定向 0 error(8 个 warning 均为改动前已存在)、git diff --check 干净；浏览器验收未做（本环境无浏览器工具且禁止读取凭据）
- [CX] 11:48 今日提交编辑链最终收口：历史责任人查询移除不存在的 `profiles.display_name`，只读 `id/name/membership_status`；归档旧责任人豁免改为按岗位判断，堵住同一归档成员跨岗位新指派绕过；5 个不可编辑快照指标连 `null` 也严格保留。新增 3 个先失败后通过的回归用例；相关测试 31/31、tsc、定向 ESLint、diff check 全绿；commit `57f2fc54` 已 push `origin/main`。本轮不改数据库/RLS、不写业务数据。
- [CX] 12:05 百度 OCR 项目第 1 批完成（未 commit/push）：新建 src/lib/baidu-ocr.ts 独立客户端（token 换取+内存缓存 30 天留 1 天余量提前刷新 / webimage base64 传参剥 data 头 / 前置校验 4M+边长 PNG/JPEG/WebP 探测 / 8 秒 AbortController 超时 / 错误码五类分法 BaiduOcrErrorType，110/111 自动刷新重试一次）；baidu-ocr.test.ts 12 用例全 mock 不发真实请求；.env.example 与项目事实.md 补 BAIDU_OCR_API_KEY/BAIDU_OCR_SECRET_KEY。验收：eslint 0 错误、tsx --test 12 pass、tsc 0 error；未改任何现有业务代码，接口改造留给第 2 批。
- [CX] 12:15 Vercel 部署核实：最新生产部署 dpl_H7F5dqFVv9hstaB7E6fsm2Jd11n9 创建于 11:49:51（紧跟 57f2fc54 的 push），readyState=READY，已挂生产别名 dydata.cc/dydata.vercel.app。结论：编辑链安全修复 57f2fc54 已上线生产；此前报告中『e632135c 含编辑链修复』说法有误，e632135c 是 OCR 第 0 步旧提交。此结论供批次 B 开工参考。
- [CX] 12:40 百度 OCR 项目第 2 批完成（未 commit/push）：/api/ocr-screenshot 主链路切百度通道——baidu-ocr 提字(baidu_ms)→文字模型结构化(structure_ms)，输出契约抽到 ocr-contract.ts 两通道共用，route.ts 统一再导出保持旧测试导入路径兼容；旧视觉链路保留为 runVisionOcrAttempt，resolveOcrScreenshotChannel 当前固定默认 baidu（后台开关第 3 批接入）；百度错误五类映射回现有 OcrErrorCode（QPS→RATE_LIMITED、TIMEOUT→AI_TIMEOUT、IMAGE_REJECTED→LOW_CONFIDENCE、其余→AI_CHANNEL_UNAVAILABLE），OCR 空文字行降级 failed 契约。新增 baidu-channel.test 6 用例；验收：定向 eslint 0 错误、ocr 相关 17 测试全绿、npm test 全量 1188 pass、tsc 0 error。模块地图已同步；浏览器真实截图链路验证留待第 4 批本地全链路验收。
- [CX] 14:30 百度 OCR 第 3 批（通道后台开关 + 模型绑定拆分）完成（未 commit/push）：①功能键拆分——feature-catalog 新增 `ocr_screenshot_structure`（截图识别·文字结构化），原 `ocr_screenshot` 收窄为「截图识别·看图回退」只服务 vision 链路；baidu-channel 结构化调用改用新键（STRUCTURE_FEATURE_KEY），两键模型绑定互不干扰。②通道开关——migration `20260825120000` 给 ai_feature_bindings 加 channel_settings jsonb + 种子新功能键 + manage_ai_feature_lifecycle 白名单补新键；resolveOcrScreenshotChannel 改异步直读 ai_feature_bindings.channel_settings（每次请求直读、保存即生效不发版，读库失败/缺省/非法值均兜底 baidu）；后台 AI 配置页「截图识别·看图回退」设置弹窗新增「识别通道」下拉（百度默认/视觉模型回退），save_feature_control 落 channel_settings。③可观测性——api_log detail 新增 channel 字段记录最终生效通道，ai_channel/ai_model 与 timings(baidu_ms/structure_ms) 照旧落库。测试同步适配不删用例：feature-catalog/feature-controls/feature-lifecycle/route/baidu-channel 共新增 8 例。验收：定向 eslint 0 错误（3 个 warning 均为预存）、验收命令 tsx --test 35 pass 0 fail、npm test 全量 1192 pass、tsc 0 error；migration 已在本地临时 Postgres 完整应用验证（建列/种子/label 更新/新旧键 archive→restore 往返全过，验证后已销毁临时库），未推远端 Supabase。禁区遵守：未动 .env.local、未 commit/push、未改前端提交表单。
- [CC] 百度OCR项目批次C验收通过（35测试全绿）；批次D实测：migration已推远端，20张真实截图 20/20 成功 p50=263ms p95=618ms；批次E上线完成：Vercel已配Key，commit 3884ebf4 已push main并部署Ready
- [CC] 线上端到端验收通过：生产接口真实截图识别成功（baidu_ms=746+structure_ms=2217≈3.4s，旧通道5-20s）；vision回退卡点实测通过：真切vision跑通(4.1s)→切回baidu生效(3.2s)，开关保存即生效无需发版

## 下午-晚间 [CC] 大图压缩 + AI调用旧机制清理（F3R3）

- 大图压缩上线：>3MB 截图识别前自动缩图转 JPEG（纯 JS 库 jimp，弃用 sharp——原生模块在 Vercel Serverless 加载失败导致过生产事故）。压后仍超 base64 4M 时逐轮降质缩尺寸（最多3轮）。生产实测 4MB 噪音图→压缩→百度正常接收
- 生产事故与修复记录：sharp 顶层 import 导致 /api/ocr-screenshot 全路由崩溃（返回 HTML 错误页），先改懒加载+serverExternalPackages 未根治，最终换 jimp 纯 JS 根治（commit bc8da80a → a8b76b6c → cfed2c1d）
- AI 调用旧机制清理完成（commit 56ad04d1）：callAi 移除第一代 ai_channels 渠道池 + 第二代 ai_feature_config 回退；顺位切换改为新体系完整排名（listRankedProviderKeyModels），绑定模型则只用绑定。净删 328 行
- 边界确认：ai_channels/ai_feature_config 表和数据保留（改写工作台仍是活租户：路由 FK + token 限制配置）；sidebar 角标 RPC 已硬编码 0 无需动
- 顺位切换结论（回答用户）：跨请求有熔断顺位切换；单次请求内现在也有完整顺位 failover（首选失败→顺位下一名→最后 env 兜底）
- 发现并告知用户：结构化模型线（api7 的 gpt-5.4-mini，用户自己换的顺位）单次 19-27 秒，是"识别慢"体感的主因；建议在场景路由换回 api1 线
- 平台限制备忘：Vercel 请求体上限 4.5MB，>4.5MB 图片在上传接口就 413（与本次压缩修复的 3-4.5MB 窗口是两个问题，前者未处理）
- 测试：本地 1194/1194 全过；生产 E2E：普通留存图 confirmed ✓、4MB 大图走压缩后百度正常接收 ✓
