# 2026-08-25 全站减法·第一批

## [CC] 背景
全站做代码与功能减法，目标：故障更少、更稳定、维护成本更低。阿禅拍板合并为两批（1+2 一起、3 第二批）。本批为批次一：清理死代码。

## [CC] 已完成（本地已改 + 已验证）
- 删除旧指挥台整条死链（入口 today-todo-drawer 早已不存在）：admin-cockpit、ai-alert-panel、ai-alert-card、queue-quick-preview、remind-log-dialog、join-request-review-list、admin-first-screen-loader（共 7 组件）
- 删除死 API：`/api/admin/dashboard-alerts` 全链 + `[alertId]/execute` + `ai-context`；`/api/admin/cockpit/queue-overview`；`/api/admin/cockpit/remind-logs`；`/api/smart-alert` 全链
- 删除死 lib：`src/lib/alert-sources/` 整目录；`smart-alert.ts`/`.test.ts`、`smart-alert-claim.ts`/`.test.ts`、smart-alert-claim-migration.test.ts
- 删除 `admin/modules/modules-content-v2.tsx`（线上用 v3）
- 保留：cockpit/_shared.ts（sidebar-badges、fulfillment 复用）、admin-first-screen-contract（first-screen-monitor 用）、join-request-actions.ts（v3 用）、modules-skeleton-v2（page.tsx 用）
- 残留清理：api-path-validation 删 dashboard-alerts 正则、api-rate-limit 删 smart-alert 豁免、飞书webhook 注释、首屏取数.test 删单测、a11y 测试改指 v3
- **修复 v3 回归**：抽屉关闭按钮补回 `aria-label="关闭成员权限详情"`（v2 有、v3 重构时丢）
- 验证：1153 测试全过；lint 前后 87 problems/10 errors 一致（本次零新增，均为历史遗留）；`npm run build` 成功

## [CC] 当前状态
- 本地已改，未提交未推送
- 待清理（留 DB migration，不在本批）：queue-overview RPC 仍读 violation_cases；admin_analytics_first_screen RPC 残留；smart_alert_claims 表

## [CC] 后续
- 批次二（第二批）：验证并删除剩余疑似废接口（collaboration attribution/talents 薄壳、content-observations、content-comparison 等），逐个按规矩来
- 不动清单（记入技术债）：rewrite 巨型文件、loaders 重构、topics/[id] 详情页 1158 行内联、今日上传新旧双版
