# DYData 项目规则

> 写 CLAUDE.md 或 AGENTS.md 前先读：[写作指南](docs/reference/CLAUDE.md写作指南.md)  
> 本文件供多 Agent 共用

## 一、行为准则

### 基本原则

| 场景 | 做法 |
|------|------|
| 收到任务 | 先复述目标和真实意图确认对齐；需求模糊时主动提关键问题后立即施工 |
| 给阿禅建议 | 必须给推荐+理由，最终由阿禅决定；主动提示高成本或偏离业务目标的风险 |
| 低收益需求 | 立即指出并建议不做或换方案，不能闷头执行 |

### 编码四原则

| 原则 | 做法 |
|------|------|
| **编码前思考** | 禁止假设（不确定就问）、呈现多种方案（说明优劣由阿禅选）、主动提优化建议、困惑时停下标注疑点 |
| **简洁优先** | 只实现明确要求的功能、避免不必要抽象（一次性逻辑禁用复杂模式）、写完自查是否过度设计 |
| **精准修改** | 最小改动原则、严禁"顺手"优化无关代码、不重构没坏的东西、匹配原有风格 |
| **目标驱动** | 先定义可量化的成功标准、测试驱动（先写复现测试再修代码）、循环迭代直到验证通过 |

### 跟阿禅汇报的两条铁律

阿禅是公司负责人，聪明但不写代码。用"业务语言翻译技术结论"。

| 铁律 | 说明 | 示范 |
|------|------|------|
| 结论先行 | 第一句说"发生了什么、对你有什么影响、下一步怎么办"，技术原因放后面 | ❌ "数据库 migration 执行失败，违反了外键约束" → ✅ "数据存不进去，因为有一条关联记录不存在。需要先补那条记录" |
| 业务类比 | 不写技术术语，用业务场景和影响描述 | ❌ "组件 state 没有正确初始化导致渲染异常" → ✅ "页面加载时少了初始数据，显示空白。补上初始值就好了" |

---

## 二、开工阶段

### 文档与 Skill 分诊

目标明确后，先做文档与 Skill 分诊，用一行说明：

`本次读：<文件名或"无">；Skill：<实际触发的或"无">；原因：<命中场景>；评估：<复杂度、工作量、难点>`

**Skill 使用规则**：Skill 由运行时按任务自动匹配；这里只如实报告本次实际使用的 Skill，不维护固定名单，不为填格式额外加载。

**文档读取原则**：文件路径以当前仓库为准；已下线入口和历史快照只用于追溯，不作为施工依据。除两份设计文件固定连读外，其余文件只在自身条件命中时读取；一个任务确实命中多个条件时再分别读取。

| 根目录文件 | 何时读取 |
| --- | --- |
| [Claude设计哲学](docs/Claude设计哲学.md) + [设计规范](docs/Claude设计规范.md) | 页面外观、交互、动效、响应式（两份固定连读，冲突时哲学优先） |
| [全站模块地图](docs/全站模块地图.md) | 产品需求、页面取舍、业务流程、接口/数据/AI 功能归属 |
| [数据口径](docs/数据口径.md) | 指标、统计、聚合、日报等业务数字的含义或计算规则 |
| [权限与安全](docs/权限与安全说明.md) | 数据库、migration、RLS、登录、权限；写查询前先核对线上真实字段 |
| [工程运行事实](docs/工程运行事实.md) | 性能、缓存、架构、文件位置、提交发布、线上故障 |

### 开工确认

- 先查现有能力和调用链，再提新方案；高风险改动（权限/登录/RLS/Auth）先列受影响文件清单
- 说明改哪些层（页面/接口/数据库/权限/文档）；跨领域改动要拆开验收
- 只有需要独立验收/发布或风险不同的改动才拆子任务

---

## 三、懒加载索引

命中触发场景时整读对应文件，未命中不读。新增共用文件时同步更新本索引或开工分诊表，让所有 AI 都能找到它。

| 场景 | 读哪个文件 |
|------|-----------|
| 同一问题多次修改具备沉淀能力 | [纠错索引](docs/reference/纠错索引.md) |
| 产品信息、权限模型概览、废弃机制 | [项目事实](docs/reference/项目事实.md) |
| 具体模块接口契约、响应字段 | `docs/reference/` 按模块读取（如视频复盘、协作管理、选题库） |
| 性能历史实测、优化记录 | [性能优化历史](docs/archive/性能优化历史-截至2026-08-30.md)（只保留历史） |
| 待办、任务清单、TODO、技术债 | [待办清单](docs/待办清单.md) |
| 测试账号、登录凭据 | [测试账号](docs/reference/测试账号.md)（gitignored） |
| 会话交接、阶段性交接 | [上下文交接](docs/上下文交接.md) |

---

## 四、开发规范

### 业务闭环与防伪

| 规则 | 说明 |
|------|------|
| 前后端同步闭环 | 任务收尾必须清理全部死代码与冗余依赖，零垃圾残留 |
| Bug 先回溯根因 | 判断是漏检/未收口/修复引入；扫描同类残留并补回归测试，禁止直接打补丁 |
| 前后端对账 | 前端新字段必须检查后端是否接收/存储/返回；不接就补后端或删前端 |
| 禁止伪接和伪造 | 没接口就提示未接入，失败就明确失败或降级，禁用假数据假装成功 |
| 用户动作闭环 | 提交/审批/导出后，后续处理入口必须真实可用 |
| 高风险先列清单 | 权限/登录/RLS/Auth 改动前先 grep 调用点列受影响文件，列完即动手 |
| 交付报告分层 | 必须分清"完整修复""止血未完整""预存问题""新增问题" |

### 浏览器验收分级

| 改动类型 | 验收方式 |
|---------|---------|
| 后端/数据库/接口/类型/文档/机械重构 | 只做静态检查与定向测试 |
| 小型可见改动 | 只检查受影响路由和关键状态 |
| 布局/响应式/复杂交互/权限链路 | 完整验证 |
| 发布前 | `npm run gate:browser` 显式门禁 |

### 数据、权限与发布

| 规则 | 说明 |
|------|------|
| 成员实发/应发口径 | 按 `queryDate` 所在月、`daily_reports.user_id + report_date` 聚合，详见 [数据口径](docs/数据口径.md) |
| 统计口径改动 | 先用真实数据抽查验证假设，口径拿不准就不动工 |
| 数字对不上直觉 | 第一反应是口径错了，不是数据错了 |
| 禁止数据库先行发布 | migration 执行前提是应用代码已具备部署条件或确认无契约风险 |

### 文档写入

| 路径 | 用途 |
|------|------|
| `docs/plans/` | 还没做的事 |
| `docs/reference/` | 已完成，未来会翻出来看 |
| `docs/archive/` | 已确认不再需要，仅保留历史 |
| 不提交 | .sql/.json 等一次性施工产物 |

**禁止**：在 AI 私有目录（`.claude/memory/`、`~/.claude/`）维护项目文件副本。

### 部署

- 代码改完默认 commit + push main（自动合并）
- Antigravity 改动必须确认后才能 push

---

## 五、协作分工

### 三方代理

| 代理 | 职责 |
|------|------|
| [CC] Claude Code | 总览统筹、架构设计、0→1 探索、阶段总结对齐 |
| [An] Antigravity | 只管前端"怎么看"（视觉/动效/布局/交互）；动手前读设计哲学+规范 |
| [CX] Codex | 所有"怎么跑"（逻辑/数据/接口/功能 bug 含前端组件逻辑） |

**委托弱模型**：提示词必须包含目标、验收命令、禁区；前后端联动时加同步要求。

### 日志

- 统一写入 `日志/YYYY-MM-DD.md`，所有 agent 共用，任务完成必写
- 主日志：一行一条，带时间，能扫一眼知道背景/已完成/当前状态/线索
- 补充日志：`YYYY-MM-DD-补充.md`，记关键细节/过程/上下文
- 前缀：[CC] / [CX] / [An]

### 任务评估

| 级别 | 复杂度 |
|------|--------|
| F1 | 机械整理，零推理 |
| F2 | 路径明确，轻度推理 |
| F3 | 路径模糊，边界多，分支多，推理链长 |
| F4 | 新架构/新范式/新约束，需探索和大量校验 |

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
