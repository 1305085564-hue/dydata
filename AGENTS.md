# DYData 项目规则

> 本文件供多 Agent 共用。

## 一、行为准则

一、摸索与对齐，收到任务/需求先复述目标和真实意图跟用户确认对齐。如果需求模糊禁止直接执行，主动补全完善真实目标，并最多提多个关键问题后立即施工。

二、阿禅不懂技术，需要选择时必须给出推荐和理由，最终由阿禅决定。主动提示高成本或偏离真实业务目标的需求风险。

三、阿禅提出的需求如果低收益高成本，或看似简单实则复杂、炫技大于实用，必须立即指出并建议不做或换方案，不能闷头执行。

四、跟阿禅说话，用"业务语言翻译技术结论"，不是"降低智商说话"。阿禅是公司负责人，聪明但不写代码。三条铁律：

1. **结论先行**：第一句说"发生了什么、对你有什么影响、下一步怎么办"，技术原因放后面
2. **用业务类比，不用技术术语**：不写"CI/CD pipeline build 阶段 TypeScript 类型检查未通过"，写"代码检查没通过，页面没更新，需要修一处问题，大概 10 分钟"
3. **不降智、不居高临下**：阿禅不需要被当小孩哄。像跟一个懂业务但不碰代码的合伙人汇报那样说话

| ❌ 不要这样 | ✅ 要这样 |
|---|---|
| "数据库 migration 执行失败，违反了外键约束" | "数据存不进去，因为有一条关联记录不存在。需要先补那条记录" |
| "组件 state 没有正确初始化导致渲染异常" | "页面加载时少了初始数据，显示空白。补上初始值就好了" |
| "需要你确认一下"（没说确认什么、为什么） | "有两个方案：A 快但可能影响旧数据，B 稳但多花半天。你选哪个？" |
| "已修复"（一句话结束） | "改好了。线上页面XX现在应该正常显示了，你刷新看看" |

------

## 二、开工阶段

目标明确后，正式动手前必须先做一次文档与 Skill 分诊，并用一行说明实际读取和使用的内容：
`本次读：<根目录文件，未读写“无”>；使用 Skill：<本次实际触发的 Skill，未触发写“无”>；原因：<命中场景>；评估：<复杂度、工作量、主要工作和难点>。`

Skill 由运行时按任务自动匹配；这里只如实报告本次实际使用的 Skill，不维护固定名单，不为填格式额外加载。

文件路径以当前仓库为准；已下线入口和历史快照只用于追溯，不作为施工依据。除两份设计文件固定连读外，其余文件只在自身条件命中时读取；一个任务确实命中多个条件时再分别读取。

| 根目录文件 | 何时读取 |
| --- | --- |
| `docs/Claude设计哲学.md` + `docs/Claude设计规范.md` | 页面外观、交互、动效、响应式；两份固定连读，冲突时设计哲学优先 |
| `docs/全站模块地图.md` | 产品需求、页面取舍、业务流程、模块范围，以及接口、数据、AI 功能的模块归属和前后端连累关系 |
| `docs/数据口径.md` | 指标、统计、聚合、日报、发布、协作等业务数字的含义或计算规则 |
| `docs/权限与安全说明.md` | 数据库、migration、RLS、登录、权限、数据范围、成员生命周期；写查询前先核对线上真实字段和已执行 migration |
| `docs/工程运行事实.md` | 页面加载、首屏、性能、缓存、架构、文件位置、模块边界、提交发布、工程治理、线上故障和部署异常 |

开工表只列一级方向；具体模块契约、项目事实、待办、测试账号和交接等细项，命中后按“懒加载索引机制”继续读取，不在此表展开。

开工时还要确认三件事：

- 先在代码中查现有能力和调用链，再提出新方案；高风险权限/登录/RLS/Auth 改动先列受影响文件清单。
- 说明本次是否改页面、接口、数据库、权限或文档；跨领域改动要拆开，分别验收和发布。
- 只有需要独立验收、独立发布或风险不同的改动，才拆子任务；简单搜索和机械整理不拆。

------

## 三、懒加载索引机制

命中触发场景时整读对应文件，未命中不读。新增共用文件时同步更新本索引或开工分诊表，让所有 AI 都能找到它。

- 同一问题多次修改具备沉淀能力 → `docs/reference/纠错索引.md`
- 需要产品信息、权限模型概览、废弃机制 → `docs/reference/项目事实.md`
- 具体模块接口契约、响应字段、模块专属规则 → 按模块读取对应 `docs/reference/` 文档（如视频复盘、协作管理、选题库、个人成长数据契约）；没有对应文档时以模块地图和代码为准
- 需要性能历史实测、优化记录 → 按需查 `docs/archive/性能优化历史-截至2026-08-30.md`；它只保留历史，不是施工入口
- 待办、任务清单、TODO、技术债 → `docs/待办清单.md`
- 测试账号、登录凭据 → `docs/reference/测试账号.md`（gitignored，本地存在）
- 会话结束 / 任务阶段性交接 / 用户要求写交接文档 → `docs/上下文交接.md`

------

## 四、开发规范

### 业务闭环与防伪规则

- 改动必须前后端同步闭环，任务收尾必须顺手清理全部派生死代码与冗余依赖，零垃圾残留。
- 前后端必须对账：前端新增/修改字段、参数、按钮行为时，必须检查后端是否接收、存储、返回；后端不接就补后端或删前端。
- 禁止伪接和伪造：没有真实接口就提示未接入，数据加载失败就明确失败或降级为空，禁止接到不相干接口或用假数据假装成功。
- 用户动作必须闭环：提交/审批/导出后，后续处理入口必须真实存在且可用，不能只靠提示信息就认为完成。
- 高风险链路（权限、登录、RLS、Auth 封禁）首次改动前，先 grep 调用点列出受影响文件清单，清单列完即动手。
- 交付报告必须分层：最终报告必须分清"完整修复""止血未完整""预存问题""新增问题"，禁止把临时方案说成完整完成。

### 浏览器验收分级

- 浏览器验证按风险分级；启动 `next dev`、`vite` 或出现 `localhost` 不自动触发。
- 后端、数据库、接口、类型、文档、机械重构和无可见变化的前端改动，默认只做静态检查与定向测试。
- 小型可见改动只检查受影响路由和关键状态；布局、响应式、复杂交互、运行时问题、权限链路和发布验收才做完整验证。
- `npm run gate:browser` 仅作高风险或发布前显式门禁；用户要求跳过时，报告未覆盖边界。

### 数据、权限与发布硬规矩

- 成员”本月实发 / 应发”按页面 `queryDate` 所在自然月、`daily_reports.user_id + report_date` 聚合：实发按作品数，应发按自然日扣除豁免/请假并按入职或创建时间裁剪；`monthly_published_days` 只作辅助天数，不代表登录或在线。详见 `docs/数据口径.md`。
- 统计口径类改动动手前，先用真实数据抽查验证假设（例：假设“运营=给别人账号干活”，先查库里自运营占比），口径拿不准就不动工。
- 数字对不上直觉时，第一反应是口径错了，不是数据错了。
- 禁止数据库先行发布：migration 执行的前提是对应应用代码已具备部署条件，或已确认无契约风险；不允许出现旧应用读已删表的风险窗口。

### 文档写入规则

写文档前先判断类型，路径不匹配时必须指出：

- `plans/` — 还没做的事
- `reference/` — 已完成，未来遇到相关问题会翻出来看
- `archive/` — 已确认不再需要，仅保留历史
- 不提交 — .sql/.json 等一次性施工产物

所有项目文档统一放在 `docs/` 目录下，日志放在 `日志/` 目录下。
禁止在任何 AI 私有目录（如 `.claude/memory/`、`~/.claude/`）维护项目文件副本。

### 部署禁令

- 代码改完默认 commit + push main（自动合并）
- Antigravity改动必须确认后才能 push

------

## 五、协作分工

### 三方代理

- [CC] Claude Code：总览与统筹，负责架构设计与 0→1 需求探索；与用户交互最多，负责阶段总结与对齐。
- [An] Antigravity：仅负责前端的「怎么看起来」——视觉风格、动效节奏、布局适配、交互手感；动手前读 `docs/Claude设计哲学.md` + `docs/Claude设计规范.md`；遇代码逻辑问题交 Codex。
- [CX] Codex：负责所有代码的「怎么跑起来」——逻辑、数据、接口、功能 bug（含前端组件逻辑）；遇审美需求交 Antigravity。
- 跨领域任务通过提示词交用户转派，简单改动可以直接说明。
- 委托弱模型时，提示词必须包含：目标、验收命令、禁区。涉及前后端联动时加同步要求。

### 日志规则

- 本项目所有日志统一写入 `日志/YYYY-MM-DD.md`，所有 agent 共用，按时间线记录关键结论，任务完成必写。
- 主日志：每次任务完成后追加，记结论/事件，一行一条，带时间（要能扫一眼知道：背景 / 已完成 / 当前状态 / 未完成线索）
- 补充日志：同目录 YYYY-MM-DD-补充.md，记关键细节/过程/上下文
- 日志前缀与上方代理简称一致（[CC] / [CX] / [An]）

### 任务评估

执行任务和派子代理之前都要先做这个评估

| 级别 | 复杂度 |
|------|--------|
| 1 | 简单：机械整理，零推理 |
| 2 | 普通：路径明确，轻度推理 |
| 3 | 复杂：路径模糊，边界多，分支多，推理链长 |
| 4 | 极难：新架构/新范式/新约束，需要探索和大量校验 |

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
