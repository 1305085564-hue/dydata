# 死代码清理记录：2026-07-14

## 清理背景
本次清理针对文档中标注的废弃功能以及未上线的半成品功能进行彻底的代码移除。

## 清理项与判断依据

### 1. `rewrite-v3` 的冗余代码及重定向壳
- **动作**：删除了 `src/components/content-tools/rewrite`（注意：这是**旧版本**的内容，并未在使用）和 `src/app/(app)/content-tools/rewrite-v3`（纯重定向页）。
- **依据**：经由代码库审查，正式页面 `(app)/content-tools/rewrite/page.tsx` 实际渲染的是 `RewriteWorkbenchV3`（存在于 `components/content-tools/rewrite-v3/`），旧目录 `rewrite` 没有任何页面引用；`/content-tools/rewrite-v3` 路由只做重定向，属于多余的空壳。

### 2. `sop_review_scores` 六维评分
- **动作**：无代码删除。
- **依据**：全局搜索未发现 `sop_review_scores` 的存在，该功能代码在之前的迭代中已经被完全剥离。

### 3. SOP 5 / 审核中心 / 全域矩阵
- **动作**：无代码删除。
- **依据**：全局搜索未发现相关调用，`dashboard-workspace-header.test.tsx` 更是显式断言了其在 UI 中不存在，属于完全没有实质代码留存的废弃需求。

### 4. 预览页 `/playground/navigation`
- **动作**：删除了 `src/app/playground/navigation` 目录。
- **依据**：该页面为开发预览页，不是正式业务入口。

### 5. `ai-channels` / `ai-features` 旧路由重定向壳
- **动作**：修改了 `src/middleware.ts`、`src/lib/analytics-access.ts` 及 `src/lib/analytics-access.test.ts`，移除了与它们有关的页面级路由重定向和访问权限判断。移除了 `src/components/admin-layout/admin-events.ts` 中废弃的事件常量。
- **依据**：前端页面已统一收口至 `ai-config`。注意并未删除对应的后台 API（内部其他 AI 接口如 `ai-config` 仍在使用 shared 部分的代码），仅清除前端壳和相关无效变量。

### 6. `admin/guidance` 和 `admin/advice` (运营建议/诊断动作)
- **动作**：删除了以下内容：
  - 前端路由：`src/app/(app)/admin/guidance/` 和 `src/app/(app)/admin/advice/`
  - Loader：`src/lib/loaders/guidance-page.ts` 及 `src/lib/loaders/admin-advice-page.ts` (含对应测试)
  - API：`src/app/api/advice-action/`、`src/app/api/advice-actions/`、`src/app/api/video-diagnose/`
  - 库逻辑：`src/lib/video-diagnose.ts` (含测试)
  - 组件：`src/app/(app)/dashboard/advice-panel.tsx`
  - 重定向/权限：修改了 `src/middleware.ts` 和 `src/lib/analytics-access.ts`
- **依据**：业务明确要求整体砍掉。全局验证均无别的页面调用或被 cron 依赖（Vercel 仅有两个独立 cron），`guidance` loader 虽查阅了 `daily_reports` 但只供自身使用，删除未影响主要日报链路。
