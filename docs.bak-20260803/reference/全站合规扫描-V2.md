# DYData 全站源码合规扫描报告 V2

> 扫描时间：2026-05-15
> 扫描范围：`src/app/**`、`src/components/**`、`src/lib/**`（仅 UI 相关 .tsx/.ts/.css）
> 规范依据：`docs/美学规范.md` + `docs/交互排版规范.md`（冲突时以后者为准）
> 已知例外 10 条已过滤，不计入违规

---

## 一、美学 P0 违规

### A1：彩色浅底面 `bg-(emerald|amber|rose|sky)-(50|100|200)`

| 文件 | 行号 | 违规代码 | 修复建议 |
|------|------|----------|----------|
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1006 | `bg-amber-50 ring-1 ring-inset ring-amber-200` | 改为中性底 `bg-zinc-50` + `ring-zinc-200`，状态用 dot 色区分 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1007 | `hover:bg-amber-50` | hover 态改 `hover:bg-zinc-50` |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1092 | `hover:bg-amber-50 hover:text-amber-800` | 改 `hover:bg-zinc-50 hover:text-zinc-800` |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1123 | `bg-amber-50 ring-1 ring-inset ring-amber-200` | 同 1006 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1124 | `hover:bg-amber-50 hover:ring-1 hover:ring-inset hover:ring-amber-200/60` | 改中性 hover |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1158 | `hover:bg-amber-50 hover:text-amber-800` | 同 1092 |
| `src/components/profile-edit-dialog.tsx` | 37 | `bg-amber-50 text-amber-700 border-amber-200` | 状态 chip 改中性底 |
| `src/components/submission/截图槽位卡.tsx` | 193 | `bg-emerald-50/90 text-emerald-700 ring-emerald-200` | 状态底面改中性 |
| `src/components/submission/截图槽位卡.tsx` | 194 | `bg-rose-50/90 text-rose-700 ring-rose-200` | 同上 |
| `src/components/submission/截图槽位卡.tsx` | 195 | `bg-amber-50/90 text-amber-700 ring-amber-200` | 同上 |
| `src/components/submission/截图槽位卡.tsx` | 308 | `bg-amber-50 px-2.5 py-1.5 text-amber-700 ring-amber-200/80` | 同上 |
| `src/components/submission/截图槽位卡.tsx` | 315 | `bg-rose-50 px-2.5 py-1.5 text-rose-700 ring-rose-200/80` | 同上 |
| `src/components/submission/指标输入卡.tsx` | 51 | `bg-emerald-50 text-emerald-700 ring-emerald-100` | 状态底面改中性 |
| `src/components/submission/指标输入卡.tsx` | 58 | `bg-amber-50 text-amber-700 ring-amber-100` | 同上 |
| `src/components/submission/指标输入卡.tsx` | 64 | `bg-rose-50 text-rose-700 ring-rose-100` | 同上 |
| `src/components/submission/截图槽位区.tsx` | 54 | `bg-amber-50 px-2.5 py-1 text-amber-700 ring-amber-200` | 同上 |

**统计**：38 行命中，涉及 5 个文件。重灾区：`video-submit-form.tsx`（14 行）、`截图槽位卡.tsx`（12 行）、`指标输入卡.tsx`（9 行）。

---

### A2：四族色 UI 元素 `bg-(emerald|amber|rose|sky)-(400|500|600)`

| 文件 | 行号 | 违规代码 | 修复建议 |
|------|------|----------|----------|
| `src/app/(app)/dashboard/video-submit-form.tsx` | 996 | `bg-emerald-500`（状态 dot） | 状态 dot 允许保留，但需确认是否为唯一语义标识 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1012 | `bg-amber-500`（状态 dot） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1080 | `bg-amber-500`（状态 dot） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1112 | `bg-emerald-500`（状态 dot） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1129 | `bg-amber-500`（状态 dot） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1146 | `bg-amber-500`（状态 dot） | 同上 |
| `src/components/submission/截图槽位卡.tsx` | 202 | `bg-emerald-500`（状态点） | 状态点允许，但 `animate-pulse` 需确认是否为骨架屏 |
| `src/components/submission/截图槽位卡.tsx` | 203 | `bg-rose-500 animate-pulse` | 非骨架屏使用 `animate-pulse`，P0 违规 |
| `src/components/submission/截图槽位卡.tsx` | 204 | `bg-amber-500`（状态点） | 允许 |
| `src/components/submission/截图槽位卡.tsx` | 277 | `bg-emerald-500`（成功标记） | 允许 |
| `src/components/submission/截图槽位卡.tsx` | 309 | `bg-amber-500`（状态 dot） | 允许 |
| `src/components/submission/截图槽位卡.tsx` | 317 | `bg-rose-500`（状态 dot） | 允许 |
| `src/components/submission/指标输入卡.tsx` | 28 | `bg-emerald-500` | 允许 |
| `src/components/submission/指标输入卡.tsx` | 34 | `bg-amber-500` | 允许 |
| `src/components/submission/指标输入卡.tsx` | 39 | `bg-rose-500` | 允许 |
| `src/components/submission/指标分组区.tsx` | 145 | `bg-sky-400/70`（分组竖线） | 分组标识允许，但建议统一为 zinc 系 |
| `src/components/submission/指标分组区.tsx` | 169 | `bg-emerald-400/70`（分组竖线） | 同上 |
| `src/components/submission/截图槽位区.tsx` | 55 | `bg-amber-500 animate-pulse` | 非骨架屏 pulse，P0 违规 |

**统计**：24 行命中，涉及 6 个文件。状态 dot 占多数，实际需整改：`截图槽位卡.tsx:203`（`animate-pulse`）、`截图槽位区.tsx:55`（`animate-pulse`）。

---

### A3：`transition-all` 未明确过渡属性

| 文件 | 行号 | 违规代码 | 修复建议 |
|------|------|----------|----------|
| `src/app/(app)/admin/components/admin-hub.tsx` | 121 | `transition-all duration-150` | 改为 `transition-[transform,background-color,border-color]` |
| `src/app/(app)/admin/components/admin-hub.tsx` | 135 | `transition-all duration-150`（ArrowRight） | 改为 `transition-[color,transform]` |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1004 | `transition-all duration-150` | 明确属性 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1063 | `transition-all duration-150` | 明确属性 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1121 | `transition-all duration-150` | 明确属性 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1205 | `transition-all duration-150` | 明确属性 |
| `src/app/page.tsx` | 151 | `transition-all duration-300` | 落地页允许破格，但建议明确 |
| `src/app/page.tsx` | 162 | `transition-all duration-300` | 同上 |
| `src/components/submission/截图槽位卡.tsx` | 258 | `transition-all duration-200` | 明确属性 |
| `src/components/submission/截图槽位卡.tsx` | 297 | `transition-all duration-150` | 明确属性 |
| `src/components/submission/截图槽位卡.tsx` | 324 | `transition-all duration-150` | 明确属性 |
| `src/components/submission/截图槽位卡.tsx` | 343 | `transition-all duration-200` | 明确属性 |

**统计**：13 行命中，涉及 4 个文件。`截图槽位卡.tsx` 4 处，`video-submit-form.tsx` 4 处。

---

### A4：`font-bold` / `font-black`（排除落地页）

**统计**：0 行命中。应用内无 `font-bold`/`font-black` 违规。

---

### A5：`text-[15px]` / `text-[16px]` 非规范七档字号

| 文件 | 行号 | 违规代码 | 修复建议 |
|------|------|----------|----------|
| `src/app/(app)/admin/permission-manager.tsx` | 601 | `text-[15px] font-medium`（页头标题） | 改 `text-[14px]` 或 `text-[13px]` |
| `src/app/(app)/admin/guidance/page.tsx` | 35 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/conversion-hub/tabs/scripts-tab.tsx` | 59 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/conversion-hub/tabs/violations-tab.tsx` | 44 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/conversion-hub/tabs/advice-tab.tsx` | 91 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/conversion-hub/weekly/view.tsx` | 89 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/conversion-hub/weekly/view.tsx` | 154 | `text-[15px] font-medium`（卡片标题） | 同上 |
| `src/app/(app)/admin/conversion-hub/weekly/view.tsx` | 202 | `text-[15px] font-medium`（空状态） | 同上 |
| `src/app/(app)/admin/conversion-hub/analytics/view.tsx` | 94 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/conversion-hub/analytics/view.tsx` | 226 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/content/content-detail-dialog.tsx` | 241 | `text-[16px] font-medium`（DialogTitle） | 改 `text-[14px]`，DialogTitle 默认已放大 |
| `src/app/(app)/admin/content/page.tsx` | 46 | `text-[15px] font-medium`（页头标题） | 改 `text-[14px]` |
| `src/app/(app)/admin/advice/advice-detail-dialog.tsx` | 134 | `text-[16px] font-medium`（DialogTitle） | 改 `text-[14px]` |
| `src/app/(app)/admin/advice/advice-detail-dialog.tsx` | 142 | `text-[15px] font-medium`（员工名） | 改 `text-[14px]` |
| `src/app/(app)/admin/advice/page.tsx` | 36 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/components/admin-hub.tsx` | 128 | `text-[15px] font-semibold`（卡片标题） | 同上 |
| `src/app/(app)/admin/videos/video-detail-dialog.tsx` | 180 | `text-[16px] font-medium`（DialogTitle） | 改 `text-[14px]` |
| `src/app/(app)/admin/videos/video-detail-dialog.tsx` | 188 | `text-[15px] font-medium`（视频标题） | 改 `text-[14px]` |
| `src/app/(app)/admin/videos/page.tsx` | 49 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/team-group-manager.tsx` | 272 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/ai-channels/ai-channels-client.tsx` | 542 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/modules/modules-content.tsx` | 117 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/modules/modules-content.tsx` | 130 | `text-[15px] font-medium`（页头标题） | 同上 |
| `src/app/(app)/admin/analytics/ai-insight.tsx` | 210 | `text-[16px] font-semibold text-white`（洞察卡片） | 评估是否为 Display 档，否的话改 `text-[14px]` |
| `src/app/(app)/_components/apply-join-dialog.tsx` | 57 | `text-[15px] font-semibold`（DialogTitle） | 改 `text-[14px]` |
| `src/components/ui/dialog.tsx` | 123 | `text-[16px] leading-none font-semibold`（DialogTitle 默认） | 全局组件，改 `text-[14px]` 影响面大，需评估 |

**统计**：26 行命中，涉及 19 个文件。`text-[15px]` 占 23 处，`text-[16px]` 占 3 处。重灾区：admin 各页面页头标题统一使用 `text-[15px]`，需批量整改。

---

### A6：外层超 16px 圆角 `rounded-3xl` / `rounded-[20px]` / `rounded-[24px]`

| 文件 | 行号 | 违规代码 | 修复建议 |
|------|------|----------|----------|
| `src/app/(app)/admin/ai-assistant/chat-panel.tsx` | 518 | `rounded-[20px]`（输入框外框） | 改 `rounded-xl`（16px）或 `rounded-lg`（12px） |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1315 | `rounded-[20px]`（输入框） | 改 `rounded-xl` |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1325 | `rounded-[20px]`（输入框） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1421 | `rounded-[20px]`（输入框） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1467 | `rounded-[20px]`（输入框） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1472 | `rounded-[20px]`（展示框） | 同上 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 1510 | `rounded-[20px]`（textarea） | 同上 |
| `src/app/(app)/violations/components/case-filters.tsx` | 138 | `rounded-[20px]`（搜索框） | 同上 |
| `src/components/submission/指标输入卡.tsx` | 167 | `rounded-[20px]`（数字输入框） | 同上 |
| `src/components/submission/提交成功卡.tsx` | 19 | `rounded-[20px]`（成功卡片） | 改 `rounded-xl` |

**统计**：10 行命中，涉及 5 个文件。`video-submit-form.tsx` 独占 6 处，输入框圆角统一超标。

---

### A7：异常 Focus 样式

| 文件 | 行号 | 违规代码 | 修复建议 |
|------|------|----------|----------|
| `src/app/(app)/admin/analytics/time-analysis.tsx` | 394 | `focus-visible:ring-2 focus-visible:ring-[#D97757]/40` | 暖橙 focus ring，改 `focus-visible:ring-zinc-950/10` |
| `src/components/admin-secondary-nav.tsx` | 144 | `focus-visible:ring-2 focus-visible:ring-[#D97757]/30` | 同上 |
| `src/app/(app)/dashboard/components/review-detail-modal.tsx` | 208 | `focus:border-zinc-950/20` | 异常 focus 边框，改标准 focus 样式 |
| `src/app/(app)/dashboard/components/workflow-dashboard.tsx` | 174 | `focus:border-zinc-950/20` | 同上 |
| `src/app/(app)/dashboard/components/workflow-dashboard.tsx` | 185 | `focus:border-zinc-950/20` | 同上 |
| `src/app/(app)/dashboard/components/workflow-dashboard.tsx` | 194 | `focus:border-zinc-950/20` | 同上 |
| `src/app/(app)/dashboard/components/leader-report.tsx` | 137 | `focus:border-zinc-950/20` | 同上 |
| `src/components/content-tools/rewrite/ConfigBar.tsx` | 157 | `focus:border-zinc-950` | 硬编码 focus 边框，需统一为 token |
| `src/components/content-tools/rewrite/ConfigBar.tsx` | 179 | `focus:border-zinc-950` | 同上 |
| `src/components/content-tools/rewrite/ConfigBar.tsx` | 201 | `focus:border-zinc-950` | 同上 |

**统计**：10 行命中，涉及 6 个文件。`focus:border-zinc-950/20` 5 处（workflow-dashboard 独占 3 处），暖橙 focus ring 2 处。

---

## 二、交互排版违规

### B1：弹窗/抽屉密度档错误

**全局 Dialog 默认密度**：`src/components/ui/dialog.tsx:56` 默认 `p-5`，规范要求低信息档弹窗用 `p-10`。

**DialogContent 显式 `p-0` 的文件**（内部自行控制密度，需人工复核）：

| 文件 | 内部密度 | 评估 |
|------|----------|------|
| `content-detail-dialog.tsx` | 内部多处 `p-4` | 详情弹窗内容多，但头部/底部密度偏低 |
| `advice-detail-dialog.tsx` | 内部多处 `p-4` | 同上 |
| `video-detail-dialog.tsx` | 内部多处 `p-4` | 同上 |
| `action-detail-drawer.tsx` | 内部 `p-4` | 详情类，密度应提升至 `p-6` 或 `p-8` |
| `申请豁免弹窗.tsx` | `p-0` | 复杂表单，需评估 |
| `video-submit-form.tsx`（删除确认） | `p-0` | 确认弹窗，允许 |
| `admin-panel-launcher.tsx` | `p-0` | 全屏面板，允许 |
| `action-hub.tsx` | `p-0` | 操作面板，允许 |
| `history-sidebar.tsx` | `p-0` | 侧边历史，允许 |
| `截图槽位卡.tsx`（预览） | `p-0` | 图片预览，允许 |

**核心问题**：`src/components/ui/dialog.tsx` 全局默认 `p-5`，不符合低信息档 `p-10` 规范。所有未显式覆盖 `p-0` 的 Dialog 都会继承此默认值。

---

### B2：`max-w-` 混档

**同屏混用统计**（关键页面）：

| 页面/文件 | 混用档位 | 问题 |
|-----------|----------|------|
| `dashboard/page.tsx` | `max-w-none`（AppShell） | 与内部 `max-w-5xl`/`max-w-6xl` 形成嵌套混档 |
| `video-submit-panel.tsx` | `max-w-6xl`（主面板）+ `max-w-5xl`/`max-w-6xl`（弹窗） | 同功能模块混用 |
| `violations/page.tsx` | `max-w-6xl` | 与 admin 页 `max-w-5xl` 不一致 |
| `violations/[id]/page.tsx` | `max-w-5xl` / `max-w-6xl` | 同一路由下不同状态混用 |
| `admin/loading.tsx` | `max-w-5xl` | 与 admin 内容区 `max-w-[1400px]` 不一致 |
| `growth/loading.tsx` | `max-w-6xl` | 与 growth 内容区不一致 |
| `app-shell.tsx` | 定义 `normal: max-w-5xl` / `wide: max-w-7xl` / `full: max-w-none` | 三档并存，需确认使用场景是否清晰 |

**max-w-[数字]px 硬编码统计**：

| 值 | 出现次数 | 典型文件 |
|----|----------|----------|
| `max-w-[220px]` | 4 | 图表占位、表格列 |
| `max-w-[200px]` | 3 | 表格列、导航栏 |
| `max-w-[768px]` | 2 | AI 聊天面板 |
| `max-w-[640px]` | 1 | AI 聊天消息 |
| `max-w-[440px]` | 2 | 登录框、弹窗 |
| `max-w-[320px]` | 2 | 表格列 |
| `max-w-[280px]` | 2 | 表格列 |
| `max-w-[260px]` | 2 | 表格列 |
| `max-w-[1760px]` | 1 | admin 全屏面板 |
| `max-w-[1400px]` | 1 | admin 主内容区 |

---

### B3：`tabular-nums` 缺失

**扫描方法**：`className` 含 `text-` + 数字场景，但不含 `tabular-nums`。

**统计**：2010 行命中，误报率极高。此规则需人工复核，无法纯自动化判定。

**已知合规场景**（已有 `tabular-nums`）：
- `video-submit-form.tsx:1015` 计数器
- `指标输入卡.tsx` 数字输入

**需重点检查的文件**（数字密集但可能缺失）：
- `dashboard/video-submit-form.tsx`（大量数字展示）
- `admin/analytics/*`（经营分析数字）
- `admin/videos/video-list.tsx`（视频数据表格）
- `dashboard/history-list.tsx`（历史数据）
- `components/charts/*`（图表数字）

**建议**：此维度不适合纯文本扫描，需结合运行时视觉检查。

---

## 三、弹出形态分布

### C1：Dialog / Drawer / Sheet 使用统计

**Dialog 使用文件清单**（20 个文件）：

| 文件 | 用途 | 形态评估 |
|------|------|----------|
| `src/app/(app)/_components/apply-join-dialog.tsx` | 申请加入 | Dialog 合理 |
| `src/app/(app)/admin/admin-panel-launcher.tsx` | 全屏管理面板 | Dialog 改全屏，合理 |
| `src/app/(app)/admin/advice/advice-detail-dialog.tsx` | 建议详情 | **详情类用 Dialog，应改 Drawer** |
| `src/app/(app)/admin/ai-assistant/action-detail-drawer.tsx` | 动作详情 | **名不副实：叫 drawer 实际用 Dialog** |
| `src/app/(app)/admin/ai-assistant/history-sidebar.tsx` | 历史记录（移动端） | Dialog 合理（移动端适配） |
| `src/app/(app)/admin/ai-rewrite/ai-rewrite-client.tsx` | 编辑配置 | Dialog 合理 |
| `src/app/(app)/admin/content/content-detail-dialog.tsx` | 内容详情 | **详情类用 Dialog，应改 Drawer** |
| `src/app/(app)/admin/data-manager.tsx` | 内容查看 | Dialog 合理 |
| `src/app/(app)/admin/videos/patch-24h-dialog.tsx` | 补录数据 | Dialog 合理 |
| `src/app/(app)/admin/videos/video-detail-dialog.tsx` | 视频详情 | **详情类用 Dialog，应改 Drawer** |
| `src/app/(app)/admin/豁免弹窗.tsx` | 设置豁免 | Dialog 合理 |
| `src/app/(app)/dashboard/dashboard-form.tsx` | 截图识别导入 | Dialog 合理 |
| `src/app/(app)/dashboard/history-list.tsx` | 修改日报 | Dialog 合理 |
| `src/app/(app)/dashboard/video-submit-form.tsx` | 删除确认 | Dialog 合理 |
| `src/app/(app)/dashboard/video-submit-panel.tsx` | 数据/趋势/排行榜/历史/编辑 | **5 个 Dialog，需评估是否过多** |
| `src/app/(app)/dashboard/申请豁免弹窗.tsx` | 申请豁免 | Dialog 合理（复杂表单） |
| `src/app/(app)/dashboard/添加账号弹窗.tsx` | 添加账号 | Dialog 合理 |
| `src/app/(app)/violations/components/test-record-form.tsx` | 追加测试记录 | Dialog 合理 |
| `src/components/profile-edit-dialog.tsx` | 编辑资料 | Dialog 合理 |
| `src/components/ui/confirm-dialog.tsx` | 通用确认 | Dialog 合理 |

**Drawer/Sheet 使用**：仅 `history-sidebar.tsx` 一处 import Drawer，但实际使用 Dialog。

**全站无真正的 Drawer/Sheet 组件使用**。

---

### C2：就地展开模式

**扫描方法**：`useState + open/show/visible` 且含 `form/detail/edit/config/setting`。

**统计**：0 行命中。

**已知就地展开场景**（需人工确认）：
- `src/app/(app)/admin/ai-assistant/` 内联配置表单
- `src/app/(app)/dashboard/video-submit-form.tsx` 复杂表单内部展开

此维度不适合纯文本扫描，需结合组件结构分析。

---

## 四、汇总统计 + 扫描覆盖度自评估

### 违规汇总

| 类别 | 命中行数 | 涉及文件数 | 严重程度 | 整改优先级 |
|------|----------|------------|----------|------------|
| A1 彩色浅底面 | 38 | 5 | P0 | 🔴 高 |
| A2 四族色 UI 元素 | 24 | 6 | P0（含 2 处 animate-pulse） | 🔴 高 |
| A3 transition-all | 13 | 4 | P0 | 🟡 中 |
| A4 font-bold/black | 0 | 0 | - | ✅ 合规 |
| A5 非规范字号 | 26 | 19 | P0 | 🔴 高 |
| A6 超 16px 圆角 | 10 | 5 | P0 | 🟡 中 |
| A7 异常 Focus | 10 | 6 | P0 | 🟡 中 |
| B1 弹窗密度 | 全局默认 p-5 | 1（全局） | B4 | 🔴 高 |
| B2 max-w 混档 | 122 行 | 多 | B5 | 🟡 中 |
| B3 tabular-nums | 2010（误报高） | - | B3 | 🟢 低（需人工） |
| C1 弹出形态 | 20 Dialog 文件 | 20 | B4 | 🔴 高（3 个详情类） |
| C2 就地展开 | 0 | - | - | 🟢 需人工 |

### 扫描覆盖度自评估

| 维度 | 覆盖状态 | 说明 |
|------|----------|------|
| 美学 P0（色彩/圆角/字号/过渡/字重） | ✅ 已扫 | 7 类全部执行 |
| 交互排版（密度/混档/数字字体） | ⚠️ 部分 | B1 需人工复核 Dialog 内部结构；B3 误报率过高 |
| 弹出形态 | ⚠️ 部分 | C1 已统计文件级分布；C2 就地展开无法纯文本扫描 |
| 动效白名单 | ❌ 未扫 | `hover:-translate-y` + `hover:shadow-sm` 列表/卡片违规未在本次 12 类中 |
| 页头结构（Eyebrow/辅助说明） | ❌ 未扫 | 需 AST 级分析或人工走查 |
| 信息密度三档落地 | ❌ 未扫 | 需运行时视觉检查 |
| 表格列宽 `flex-1` | ❌ 未扫 | 需专项扫描 |
| 分隔线 `border-t` | ❌ 未扫 | 需专项扫描 |
| 彩色面状底状态 chip | ❌ 未扫 | 与 A1 部分重叠，但列表密集场景未单独评估 |
| 落地页破格 | ✅ 已排除 | `src/app/page.tsx` 已排除 |

### 整改建议（按优先级）

**P0 - 立即整改**
1. **全局 Dialog 默认密度**：`src/components/ui/dialog.tsx:56` 的 `p-5` 改为 `p-10`，或按场景分档
2. **admin 页头字号统一**：批量将 `text-[15px]` 改为 `text-[14px]`（约 20 处）
3. **video-submit-form.tsx 彩色底面**：`bg-amber-50` / `hover:bg-amber-50` 批量改中性
4. **截图槽位卡 + 指标输入卡**：状态底面改中性，保留状态 dot 色

**P1 - 短期整改**
5. **video-submit-form.tsx 输入框圆角**：`rounded-[20px]` 批量改 `rounded-xl`
6. **transition-all 明确属性**：4 个文件 13 处
7. **详情类弹窗改 Drawer**：`content-detail-dialog.tsx`、`advice-detail-dialog.tsx`、`video-detail-dialog.tsx`
8. **action-detail-drawer.tsx 名不副实**：实际用 Dialog，需改名或改实现

**P2 - 中期评估**
9. **max-w 混档统一**：梳理 `app-shell.tsx` 三档定义，统一各页面使用
10. **focus 样式统一**：`focus:border-zinc-950/20` 和暖橙 focus ring 统一为规范 token
11. **tabular-nums 人工走查**：数字密集组件逐页确认

### 扫描局限声明

1. **B3（tabular-nums）**：2010 行命中绝大多数为误报（普通文本含 `text-` 前缀），此规则需运行时视觉检查或 AST 分析
2. **C2（就地展开）**：纯文本无法区分"内嵌表单展开"与"普通条件渲染"
3. **未覆盖维度**：动效白名单、页头结构、表格列宽、分隔线、彩色 chip 密集场景，需补充扫描
4. **例外过滤**：已应用 10 条已知例外，但可能存在遗漏的合法例外场景
