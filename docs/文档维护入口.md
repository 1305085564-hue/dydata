# 文档维护入口

> 目的：让文档少折腾、容易找到、知道是否仍然可信。
> 范围：只管当前施工会引用的文档；`docs/archive/` 默认只作历史追溯，不自动搬迁、不自动改写。
> 当前核对：2026-09-24（只核对文档分层和入口，没有重新核实各业务事实）。

## 1. 当前优先入口

“唯一优先入口”表示该主题的现行规则以此文件为准；其他文件如需提及，应链接回这里，不复制出另一份现行规则。

| 用途 | 唯一优先入口 | 什么时候读 |
| --- | --- | --- |
| 项目事实、废弃机制 | [项目事实](reference/项目事实.md) | 产品、权限范围、历史废弃机制 |
| 工程运行、部署、文件位置 | [工程运行事实](工程运行事实.md) | 性能、架构、发布、故障、文件位置 |
| 权限、安全、RLS | [权限与安全说明](权限与安全说明.md) | 登录、权限、数据库、migration、RLS |
| 指标和统计口径 | [数据口径](数据口径.md) | 数字、日报、聚合、统计 |
| 模块归属和流程 | [全站模块地图](全站模块地图.md) | 产品取舍、页面/接口/AI归属 |
| 当前待办 | [待办清单](待办清单.md) | 只看仍需继续处理的事项 |
| 维护专项代码入口 | [可维护性维护入口](reference/可维护性维护入口.md) | 诊断、定向测试、发布恢复 |

## 2. 文档放置规则

- `docs/plans/`：尚未完成的施工方案；完成后按下方规则迁移。
- `docs/reference/`：完成后仍需反复查阅的当前资料、入口和契约。
- `docs/archive/`：历史方案、历史报告和已不作为当前依据的材料。
- `docs/reviews/`：审查记录；结论生效后必须回写到对应当前入口，审查报告本身不替代当前事实文档。
- 不自动依据日期或“已完成”字样搬迁文件；只列候选，由负责人确认。

## 3. 当前文档的最低维护信息

不要给所有文档增加重复标签。目录和文件职责已能表达的内容不再重复：`plans` 是待施工，`archive` 是历史，`reviews` 是审查证据。

只对“当前有效事实入口”维护已有的 `最后核对代码日期` 或 `最后核对日期`：

- 文档内容因代码、线上事实或业务决策被实际核验时，更新日期；纯文字润色不更新。
- 更新日期只代表当次核对的范围，不代表整份文档每一项都重新验过；有局部范围时在文档正文说明。
- 不以固定天数自动判过期。日期较旧只提示负责人按任务需要复核，不把旧日期当作错误或自动归档依据。
- 新建文档只在目录不能表达用途时补充状态；不强制加“负责人”等目前没有明确归属机制的字段。

暂不要求一次性补齐 300 份历史文档；只维护本页第 1 节中的当前入口和后续新建的长期事实文档。

## 4. 维护动作

1. 改权限、路由、接口、数据库字段、部署方式或文件位置时，交付前按第 1 节逐项检查受影响入口；确实相关才更新，不相关则不改。
2. 施工完成后，更新对应入口的事实和核对日期；未做真实线上验证时，不把本地结果写成线上已生效。
3. 同一结论只保留一份当前原文，其他文档只链接它；历史文档保留追溯价值，但不作为施工依据。
4. 审查报告、施工方案中的结论只有在采纳并核实后，才回写当前入口；报告本身不自动成为规则。
5. 链接检查、路径对账和复核提醒只做报告；任何批量修复、归档或删除都必须单独确认。
6. 发现当前入口与代码或线上证据冲突时，记录冲突和证据；按项目验收规则确认哪个环境为准，再修正文档，不以脚本猜测代替判断。
7. 一个任务的文档闭环只需报告“更新了哪些入口 / 已核对无需更新哪些入口 / 未能核实哪些入口”，不要求每次扫描全库。

## 5. 本页边界

本页不替代项目事实、权限安全、数据口径、工程运行和模块地图；它只说明“到哪里找”和“怎么维护”。
