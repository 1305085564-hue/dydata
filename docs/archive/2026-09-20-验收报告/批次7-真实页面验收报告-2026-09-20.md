# 批次 7 真实页面验收报告（本地浏览器实测）

## 环境
- 目标环境：**本地 dev 服务器**（`next dev --webpack`），非 Vercel Preview/Production
- 域名：`http://localhost:3000`
- 部署 SHA：运行进程 cwd = 本仓库工作树，分支 `release/permission-compat-2026-09-19`（HEAD `4c59b0d6`）；已核实该 HEAD 包含 `58b854a2 / 1a73918f / f78dc410 / 6275bc74` 四个提交（`git merge-base --is-ancestor` 全部命中）。**注意：这四个提交仍未推送到 origin，不在 origin/main（`437aea5a`）上，故线上/预览环境不含本代码——本轮验的是本地跑的这份批次 7 代码。**
- 数据后端：本地 app 连接 **生产 Supabase 项目**（同一 RLS/权限判定），因此本轮全程**只读**，未创建/发送/切换/导入/确认/重放任何写请求。
- 验收时间：2026-09-20（Asia/Jakarta）
- 浏览器/窗口：Playwright 驱动的**真实 Chromium**（headless，1280×900），每账号独立 context，逐路由重新读取 URL/正文/网络/控制台并截图
- 使用账号类型：组员(member) / 组长·管理(admin) / 阿禅(company_owner)，凭据运行时从 `docs/reference/测试账号.md` 读取，报告内不含密码/Cookie/token
- 是否存在可回收测试 fixture：**否**（无专用 fixture，故一切写入类场景未执行）
- 证据文件（本地，含成员邮箱，属敏感，勿外发）：`.acceptance/evidence.json`、`.acceptance/rewrite.json`、`.acceptance/*.png`

## 结论
- 总体：**PARTIAL（部分通过）**——权限门禁与 Rewrite 主路径为真实浏览器实测通过；集团模式、跨公司负向、Topics 管理端 API 鉴权、写入类因只读纪律/无 fixture/无开关 UI 而未验证或阻断。
- 通过场景：member 管理页拒绝、member 人员明细过滤、member 无 Rewrite 权限跳转、admin 本公司成员范围、admin 系统设置拒绝、owner 默认本公司范围、Rewrite 页面加载+bootstrap+会话/技能列表、Rewrite 主路径 `/api/rewrite/*`（未命中旧路径）、Topics 员工端可见池。
- 阻断/未验证场景：见下表标注 未验证 / WRITE_ACCEPTANCE_BLOCKED / 后端授权未能由浏览器证明。
- 未出现"页面行为与接口状态冲突"的 FAIL；未出现把"未看到"写成"通过"。

## 场景证据

| 场景 | 账号/角色 | 操作 | 实际结果 | HTTP/页面证据 | 截图 | 结论 |
|---|---|---|---|---|---|---|
| member 首屏 | member | 登录后 /dashboard | 进入工作台，仅本人数据 | 200，未跳登录，0 控制台错误 | member___dashboard.png | PASS |
| member 成员管理拒绝 | member | 打开 /admin/modules | 显示"还没有「成员管理」权限…仅对组长·管理开放" | 200 + 应用内拒绝卡（非空白成功） | member___admin_modules.png | PASS（fail-closed 拒绝页） |
| member 系统设置拒绝 | member | 打开 /admin/settings | "还没有「系统设置」权限…仅对企业管理员（公司所有者）开放" | 200 + 拒绝卡 | member___admin_settings.png | PASS |
| member 素材库拒绝 | member | 打开 /admin/videos | "还没有「素材库」权限" | 200 + 拒绝卡 | member___admin_videos.png | PASS |
| member 人员明细过滤 | member | 打开 /admin/collaboration | 页面可进但 达人0/运营0/文案0/剪辑0，"本月还没有达人数据" | 200，无他人明细 | member___admin_collaboration.png | PASS（按固定权限过滤，无其他成员明细） |
| member 无 Rewrite 权限 | member | 打开 /content-tools/rewrite | 被重定向回 /dashboard，未见半成品/假数据 | finalUrl=/dashboard | member___content_tools_rewrite.png | PASS（跳转式拒绝） |
| admin 本公司成员范围 | admin | 打开 /admin/modules | 团队选择器仅"深圳二部(44)"，在职44/归档8，全部行 所属团队=深圳二部 | 200 | admin___admin_modules.png | PASS（本公司范围） |
| admin 跨公司隔离（负向） | admin | 尝试看其他公司 | 选择器只显示本公司一个团队；**未穷举切换/未直接探测越权接口** | — | — | 部分支持，未完全验证 |
| admin 系统设置拒绝 | admin | 打开 /admin/settings | 拒绝卡（仅 company_owner） | 200 + 拒绝卡 | admin___admin_settings.png | PASS |
| admin 互相归档/恢复/调配/升降 | admin | 成员操作 | **未执行**（生产写入风险，无 fixture） | — | — | WRITE_ACCEPTANCE_BLOCKED |
| owner 默认本公司 | owner | 打开 /admin/collaboration | 计数与单团队一致（达人28/文案28），未见全集团放大 | 200 | owner___admin_collaboration.png | PASS（默认本公司范围） |
| owner 集团模式 ON 全局 | owner | 进入集团模式 | 本环境**无集团模式开关 UI**，未触发 | — | — | 未验证 |
| owner 伪造 groupMode=true 不放大 | owner | 前端塞参数 | 未执行（本轮未构造伪造参数用例） | — | — | 未验证 |
| profiles.data_scope 旧值不改判定 | — | 观察页面 | 成员列表"数据范围"列统一显示"所属公司"；无法安全改值验证 | — | — | 未验证，不得推断通过 |
| Rewrite 页面加载 | admin/owner | 打开 /content-tools/rewrite | 完整渲染 WRITER V3、对话/技能/参数面板，无错误 | 200，0 控制台错误 | rewrite_admin.png | PASS |
| Rewrite bootstrap | admin/owner | 页面自触发 | GET /api/rewrite/bootstrap → 200 | 200 | — | PASS |
| Rewrite 会话/技能列表 | admin | 页面自触发 | GET /api/rewrite/conversations 200、/api/rewrite/skills 200，无接口错误 | 200 | — | PASS |
| Rewrite 主路径 vs 旧路径 | admin/owner | 记录页面网络 | 仅出现 `/api/rewrite/*`；**未出现 `/api/content-tools/rewrite/*`** | usesMain=true / usesLegacy=false | — | PASS（主流程已收口，兼容路径存在但页面不命中） |
| Rewrite 打开已有会话 | admin | 点开历史 | 该账号"历史记录 0 条"，无既有会话可读 | — | — | 未验证（无 fixture，禁写入） |
| Topics 员工端可见 | member | 打开 /topics | 选题池正常加载（八大母题体系、动态、最新在写文案） | 200，正文 ~12k 字符 | member___topics.png | PASS |
| Topics "正在写"语义 | member | 观察状态文案 | 页面出现"最新在写：《5分钟定生死》"等"在写"表述 | 200 | member___topics.png | 部分通过（观察到"在写"文案；未逐一校验 candidate/scripting→文案映射） |
| Topics 管理端 API 鉴权 | member | import/parse、import/confirm、toggle、evaluate | **未由浏览器证明**：均为 POST 写路由，只读纪律下不发起；GET→405 不能证鉴权 | — | — | 后端授权未能由浏览器证明 |
| Topics 管理端入口（review_content） | admin/owner | 找管理端选题页 | 仓库无独立 `/admin/topics-library` 页面路由（仅 API + 内容页内嵌），无法作为独立页面验收 | — | — | 未验证（路由形态与预期不同） |
| Topics 观察期 claim 保护 | — | 不触发/不重放 | 全程未触发 `/api/topics/sub-topics/[id]/claim` | — | — | 遵守（未执行写） |
| 视频快照映射 | — | — | 后端工具逻辑，本就不要求浏览器验收 | — | — | 不在本轮范围 |

## 阻断规则命中说明
- 部署 SHA：本地 HEAD 含四提交（已核实），但**线上/预览不含**（未推送）→ 若验收目标是"已部署环境"，则 Production/Preview 仍为 BLOCKED；本轮改在本地跑同一批次 7 代码，属阿禅指定的本地验收路径。
- 只看到应用内"需访问权限"拒绝页（HTTP 200 而非 403）：已如实记录为"页面级拒绝"，未据此宣称后端接口级授权；后端越权拦截需接口级证据（见下）。
- 需生产写入但无 fixture：admin 成员操作、Rewrite 新会话、Topics 管理端 API → WRITE_ACCEPTANCE_BLOCKED / 未执行。
- 未把任何"未看到"写成"通过"。

## 最终区分
1. **页面真实通过**：member 四类管理入口拒绝 + 人员明细过滤 + 无 Rewrite 权限跳转；admin 本公司成员范围 + 系统设置拒绝；owner 默认本公司；Rewrite 页面加载/bootstrap/会话/技能/主路径收口（未命中旧路径）；Topics 员工端可见。
2. **页面失败**：无。
3. **后端授权未能由浏览器证明**：Topics 管理端 API（import/confirm/toggle/evaluate）越权拦截、admin 成员操作的服务端门禁、Rewrite 旧路径兼容响应的服务端一致性——需接口级/RLS 级证据，浏览器只读无法安全证明。
4. **因环境/账号/部署缺失而 BLOCKED 或未验证**：集团模式 ON 全局、伪造 groupMode 不放大、data_scope 旧值不改判定、Rewrite 打开既有会话（无会话/无 fixture）、Topics 管理端页面路由形态。
5. **本轮明确未覆盖**：视频快照映射后端工具、migration、RLS 写路径、生产写入路径、观察期 claim、admin 互相归档/调配/升降、任何数据变更。

## 建议下一步（按优先级）
1. 若要验"已部署环境"：把批次 7 四提交推送并部署到明确环境，给出该环境部署 SHA + 域名后，用真实浏览器重跑（本轮为本地实跑，线上仍不含此码）。
2. 后端授权类（Topics 管理端 API、成员操作越权、Rewrite 兼容路径响应一致性）：走接口级只读探测或 RLS 实测（psql 只读），不要靠浏览器"入口隐藏"下结论。
3. 集团模式：需可用开关 UI 或受控只读事务复现，再验 ON/OFF 范围切换与伪造参数不放大。
