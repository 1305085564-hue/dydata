# 数据报表顶部云端基线补回方案

## 目标

以线上 `origin/main` 为基线，用最小改动把今天有效内容补回来，避免继续在早上本地大范围改动上解冲突。

## 决策

当前线上代码已经包含新的 dashboard 工作台结构，所以不再整页重写顶部，只补最关键链路：

- 数据报表顶部继续沿用线上工作台结构。
- 截图上传从本地 `blob:` 改成 Supabase Storage 公开 URL。
- 今日日期改为上海时区，减少跨时区误差。
- 补回豁免审核结果和团队待审批数据链路，后续 UI 可继续加提示。

## 已补文件

- `src/app/api/submission-screenshots/route.ts`
- `supabase/migrations/058_submission_screenshots_bucket.sql`
- `src/app/(app)/dashboard/video-submit-form.tsx`
- `src/lib/loaders/dashboard-page.ts`
- `src/lib/loaders/shared.ts`
- `src/app/(app)/dashboard/page.tsx`
- `src/app/(app)/dashboard/dashboard-content.tsx`
- `src/app/(app)/dashboard/production-control-system.tsx`
- `src/app/(app)/dashboard/components/data-report-stage.tsx`
- `src/app/(app)/dashboard/video-submit-panel.tsx`

## 验收

- `npm run build` 已通过。
- 构建仅保留线上已有提醒：`middleware` 命名弃用、部分图表容器宽高提示。

## 备份

覆盖云端基线前已保存完整 stash：

- `stash@{0}`：`backup-before-cloud-reset-2026-05-11`
