# 模块地图对账 · 差异清单

调查日期：2026-08-08
对账基准：`docs/plans/全站模块扫描-底稿.md`（2026-07-12 全量扫描）→ 当前 `src/` 代码（2026-08-08 核对）
背景：期间执行了"权限 V2"大改（`docs/plans/2026-08-05-权限机制重构-规格书.md`、`docs/plans/2026-08-07-权限V2契约断裂-根治施工任务书.md`）及若干模块增删。本文件只列差异，不重写整份地图。

---

## 一、页面差异

### 新增页面

- **【新增】协作管理 /admin/collaboration**
  - 底稿说：无此页面（底稿页面清单未收录）
  - 现在是：完整的管理页模块（数据看板 + 文案/剪辑/运营三岗位 tab + 个人卡片）
  - 证据：`src/app/(app)/admin/collaboration/page.tsx`；配套组件 `collaboration-workbench.tsx`、`operator-tab.tsx`、`staff-tab.tsx`、`personal-card.tsx`、`collaboration-data-container.tsx`

- **【新增】选题库 /topics、/topics/[id]**
  - 底稿说：无此页面
  - 现在是：选题库 V2 入口 + 详情页（带独立 layout）
  - 证据：`src/app/(app)/topics/page.tsx`、`src/app/(app)/topics/[id]/page.tsx`、`src/app/(app)/topics/layout.tsx`、`src/app/(app)/topics/topic-helpers.ts`

### 删除页面

- **【删除】/playground/navigation**
  - 底稿说：归类"其他"页
  - 现在是：整个 playground 目录不存在
  - 证据：Glob `src/app/**/playground/**` 无文件；`docs/全站模块地图.md` 第四节已标"已清理"

- **【删除】/content-tools/rewrite-v3**
  - 底稿说：登录后业务页（兼容重定向到 /content-tools/rewrite）
  - 现在是：rewrite-v3 目录不存在，仅保留 `/content-tools/rewrite`
  - 证据：Glob `src/app/(app)/content-tools/**` 仅见 `rewrite/page.tsx`，无 `rewrite-v3`

- **【删除】video-review 全部 5 个页面（/video-review、/archive、/exemption、/manage、/submit）**
  - 底稿说：5 个 video-review 页面均为"登录后业务页"
  - 现在是：整个 video-review 目录不存在（模块地图称"归档只读、页面已全部清除"）
  - 证据：Glob `src/app/(app)/video-review/**` 无文件

- **【删除】/admin/advice**
  - 底稿说：管理后台页，加载器 `admin-advice-page.ts`
  - 现在是：admin/advice 目录不存在
  - 证据：Glob `src/app/(app)/admin/advice/**` 无文件；`docs/全站模块地图.md` 第四节已标"已整体下线并清理"

- **【删除】/admin/guidance**
  - 底稿说：管理后台页，加载器 `guidance-page.ts`
  - 现在是：admin/guidance 目录不存在
  - 证据：Glob `src/app/(app)/admin/guidance/**` 无文件；`docs/全站模块地图.md` 第四节已标"已整体下线并清理"

---

## 二、接口差异

### 新增接口

- **【新增】协作管理接口群 /api/admin/collaboration/\***
  - 底稿说：无
  - 现在是：5 个接口（summary / operators / staff / person / attribution）
  - 证据：`src/app/api/admin/collaboration/summary/route.ts:5`、`operators/route.ts`、`staff/route.ts`、`person/route.ts`、`attribution/route.ts`

- **【新增】选题库接口群 /api/topics/\***
  - 底稿说：无
  - 现在是：16 个接口（active / options / pool / comparison / recommendations + sub-topics 全套：列表、详情、claim/claims/return/start-scripting/works/from-recommendation/replace-claim/suggest）
  - 证据：`src/app/api/topics/active/route.ts`、`options/route.ts`、`pool/route.ts`、`comparison/route.ts`、`recommendations/route.ts`、`sub-topics/route.ts`、`sub-topics/[id]/route.ts`、`sub-topics/[id]/claim/route.ts`、`sub-topics/[id]/claims/route.ts`、`sub-topics/[id]/from-recommendation/route.ts`、`sub-topics/[id]/return/route.ts`、`sub-topics/[id]/start-scripting/route.ts`、`sub-topics/[id]/works/route.ts`、`sub-topics/replace-claim/route.ts`、`sub-topics/suggest/route.ts`

- **【新增】视频生命周期 /api/admin/videos/[videoId]/lifecycle**
  - 底稿说：无（底稿只有 `/api/admin/video-assets/[videoId]`）
  - 现在是：PATCH 支持 trash / restore / purge
  - 证据：`src/app/api/admin/videos/[videoId]/lifecycle/route.ts:3,6,9`

- **【新增】视频复盘责任归属 /api/admin/content-attribution/[videoId]**
  - 底稿说：无
  - 现在是：独立的内容归因接口
  - 证据：`src/app/api/admin/content-attribution/[videoId]/route.ts`

- **【新增】反馈卡历史 /api/admin/content-feedback-cards/[videoId]/previous**
  - 底稿说：无（底稿只有 `/api/admin/content-feedback-cards/[videoId]`）
  - 现在是：取上一条反馈卡
  - 证据：`src/app/api/admin/content-feedback-cards/[videoId]/previous/route.ts`

- **【新增】系统阈值 /api/admin/settings/thresholds**
  - 底稿说：无（底稿 `/admin/settings` 走 `/api/daily-quota-config`）
  - 现在是：视频复盘与素材库异常警戒阈值
  - 证据：`src/app/api/admin/settings/thresholds/route.ts:9-17`

- **【新增】看板补充接口 /api/dashboard/operator-members、submission-calendar、watch-overview**
  - 底稿说：无
  - 现在是：被指派者选项、提交日历、看护总览
  - 证据：`src/app/api/dashboard/operator-members/route.ts:3`、`submission-calendar/route.ts`、`watch-overview/route.ts`

- **【新增】飞书事件回调 /api/feishu/event**
  - 底稿说：无
  - 现在是：飞书机器人事件回调（URL 验证 + 消息接收，Node runtime）
  - 证据：`src/app/api/feishu/event/route.ts:5,7`

- **【新增】使用事件 /api/usage-events**
  - 底稿说：无
  - 现在是：前端使用事件上报
  - 证据：`src/app/api/usage-events/route.ts:4`

- **【新增】截图文件 /api/submission-screenshots/file**
  - 底稿说：无（底稿只有 `/api/submission-screenshots`）
  - 现在是：独立的截图文件接口
  - 证据：`src/app/api/submission-screenshots/file/route.ts`

### 删除接口

- **【删除】advice 相关 4 个接口**
  - 底稿说：`/api/admin/advice-actions`、`/api/admin/advice-actions/[id]`、`/api/advice-action`、`/api/advice-actions/[id]`
  - 现在是：全部不存在（随 `/admin/advice` 页面下线）
  - 证据：Glob `src/app/api/admin/advice-actions/**`、`src/app/api/advice-action*` 无文件；`docs/全站模块地图.md` 第四节标"已同步清理"

- **【删除】video-diagnose 2 个接口**
  - 底稿说：`/api/video-diagnose`、`/api/video-diagnose/batch`
  - 现在是：全部不存在
  - 证据：Glob `src/app/api/video-diagnose/**` 无文件；模块地图第四节标"已同步清理"

- **【删除】/api/ai-insight、/api/ai/insight/single-video**
  - 底稿说：`/api/ai-insight`（POST 表 daily_reports）、`/api/ai/insight/single-video`
  - 现在是：均不存在（仅保留 `/api/ai/insight/period`）
  - 证据：Glob `src/app/api/ai-insight/**` 无文件；`src/app/api/ai/insight/` 下仅 `period/route.ts`

- **【删除】转化中心管理侧 3 个接口 /api/admin/conversion-hub/\***
  - 底稿说：`/api/admin/conversion-hub/advice`、`pipeline-counts`、`weekly`
  - 现在是：全部不存在（员工侧 `/api/conversion-hub/*` 仍在）
  - 证据：Glob `src/app/api/admin/conversion-hub/**` 无文件

- **【删除】growth 相关 3 个接口**
  - 底稿说：`/api/growth-advice`、`/api/growth/ai-insight`、`/api/growth/page-data`
  - 现在是：全部不存在；`/growth` 页面改为服务端组件 `GrowthDataContainer` 直查，不再走 API
  - 证据：Glob `src/app/api/growth/**`、`growth-advice/**` 无文件；Grep 全 `src` 无 `/api/growth` 引用；`src/app/(app)/growth/page.tsx:6,22-24`

- **【删除】/api/report**
  - 底稿说：`/api/report` GET 表 daily_reports
  - 现在是：不存在
  - 证据：Glob `src/app/api/report/**` 无文件

- **【删除】rewrite 文档/段落/技能详情一批接口**
  - 底稿说：`/api/rewrite/documents/[id]`（+ history / paragraphs / revisions）、`/api/rewrite/paragraphs/[id]/undo`、`/api/rewrite/paragraphs/user-edit`、`/api/rewrite/generate/patch`、`/api/rewrite/conversations/[id]/skills`（+ `[skillId]`）、`/api/rewrite/skills/[id]`
  - 现在是：`/api/rewrite/` 下仅剩 `conversations`、`generate`、`skills` 三个；其余全部不存在
  - 证据：Glob `src/app/api/rewrite/**` 仅返回 `conversations/route.ts`、`generate/route.ts`、`skills/route.ts`

- **【删除】video-review 专属接口 /api/work-submissions、/api/work-submissions/[id]、/api/work-screenshots/upload**
  - 底稿说：三个接口均存在，调用位置在 video-review 各页面
  - 现在是：全部不存在（video-review 页面已清；publish-drafts / exemptions 作为共享链路保留）
  - 证据：Glob `src/app/api/work-submissions/**`、`work-screenshots/**` 无文件
  - 备注：`docs/全站模块地图.md`（108-112 行）仍把 `app/api/work-submissions/` 列为 video-review 接口目录，与本对账结果不符，地图此处已过期

- **【删除】/api/violations/[id]/test**
  - 底稿说：POST 表 violation_cases
  - 现在是：不存在
  - 证据：Glob `src/app/api/violations/[id]/test/**` 无文件

---

## 三、权限相关差异（重点核对）

### 权限 lib 层

- **【删除】BusinessRole 体系 src/lib/business-role.ts（及测试）**
  - 底稿说：底稿未直接枚举 lib，但权限判断依赖 BusinessRole（team_admin / group_leader）体系
  - 现在是：整文件已删除，全仓无 `business-role*` 文件
  - 证据：Glob `src/lib/business-role*` 无文件；`docs/plans/2026-08-05-权限机制重构-规格书.md:108-113`（删除清单）

- **【新增】权限判断模块 src/lib/permission-utils.ts**
  - 底稿说：无此模块
  - 现在是：`hasPermission` / `hasAnyPermission`，owner 直通、其余看 `permissions` JSON
  - 证据：`src/lib/permission-utils.ts`（被 `src/lib/permissions.ts:5`、`src/lib/analytics-access.ts:1` 引用）

- **【改名/重构】数据范围 src/lib/data-access-scope.ts**
  - 底稿说：基于 access_level(1-4) + view_all_data 权限键 + groups/group_id 推导
  - 现在是：改为 `data_scope`(self/team/all) + `visibleUserIds` / `activeVisibleUserIds`，无 groups / access_level
  - 证据：`src/lib/data-access-scope.ts:9`（DataAccessScopeKind=DataScope）、`:43-50`（inferDataScope）、`:55`（select data_scope）、`:96-110`（all/team 分支）、`:128-129`（visibleUserIds/activeVisibleUserIds）

- **【改名/重构】路由守卫 src/lib/analytics-access.ts**
  - 底稿说：基于 businessRole 判断
  - 现在是：`canAccessAdminPath` 按 V2 权限键逐路径判断，新增 `/admin/collaboration` 守卫
  - 证据：`src/lib/analytics-access.ts:55-84`（manage_system / manage_members / view_analytics / review_content / manage_fulfillment / manage_videos）、`:70-72`（collaboration 走 view_analytics）

- **【改名/重构】团队管理 src/lib/team-management.ts**
  - 底稿说：含 group_leader 推导逻辑
  - 现在是：简化为只认 `manage_members` 权限，无 group_leader
  - 证据：`src/lib/team-management.ts:40-42`（isTeamAdmin = role=admin && manage_members）

- **【改名/重构】权限加载 src/lib/permissions.ts**
  - 底稿说：UserPermissionInfo 含 businessRole / groupId / ledGroupIds / accessLevel
  - 现在是：仅含 role / permissions / dataScope / teamId，读 `profiles.data_scope`
  - 证据：`src/lib/permissions.ts:9-16`（UserPermissionInfo）、`:30`（select data_scope）、`:14`（dataScope 字段）

### 权限 API 层

- **【新增】权限申请 /api/permission-requests/apply**
  - 底稿说：无
  - 现在是：员工申请权限时通知有 `manage_members` 的管理员（含 owner）
  - 证据：`src/app/api/permission-requests/apply/route.ts:87-91`（按 manage_members 过滤收件人）、`:128`（POST）

- **【删除】admin/permission-manager.tsx**
  - 底稿说：`src/app/(app)/admin/permission-manager.tsx` 是 `/api/admin/execute-tool`、`/api/admin/member-ai-suggestion` 的调用位置
  - 现在是：该文件不存在（两个接口的调用位置现仅剩 `modules-content-v2.tsx`）
  - 证据：Glob `src/app/(app)/admin/permission-manager*` 无文件；底稿接口清单第 72、81 行将其列为调用位置

### 权限相关既有接口的 V2 清理

- **【改名/重构】/api/admin/modules/team-management、/api/admin/fulfillment/calendar 等去除 groups 维度**
  - 底稿说：查询表含 groups（如 team-management 查 profiles/groups/teams；fulfillment/calendar 查 profiles/teams/groups）
  - 现在是：groups 表已物理删除（迁移 `20260805120000_permission_v2.sql`），这些接口不再查 groups；`modules-content-v2.tsx` 的分小组 UI 已移除
  - 证据：Grep `src/app/(app)/admin/modules/modules-content-v2.tsx` 无 `group_id`/`groupId`/`分小组`/`小组` 命中；`docs/plans/2026-08-07-权限V2契约断裂-根治施工任务书.md:19`（删除 groups 表 / profiles.group_id / access_level）

- **【改名/重构】src/types/index.ts 权限模型**
  - 底稿说：ADMIN_PERMISSION_KEYS / AI_PERMISSION_KEYS 分组 + BusinessRole
  - 现在是：PERMISSION_CATEGORIES 四类 11 键 + DataScope + DEFAULT_PERMISSIONS_BY_ROLE + profiles.data_scope 字段
  - 证据：`src/types/index.ts:9`（PERMISSION_CATEGORIES）、`:17`（DataScope）、`:52`（DEFAULT_PERMISSIONS_BY_ROLE）、`:83`（data_scope 字段）

---

说明：本次仅对账，未修改任何代码。
