# DYData P0 产品逻辑文档

> ⚠️ **历史文档，部分机制已废弃或未上线**。本文档保留早期产品设计，但实际落地情况已发生变化。读本文档时必须同时对照 `AGENTS.md` 的「废弃机制速查」和真实代码，不要直接按本文档执行。
>
> 当前已上线的真实批改台链路见文末 §当前已上线的批改台反馈链路。
>
> 目标：让管理体系的 5 个卡点在网站上真正跑起来，组长能管组、你能看全局。
> 前端：Claude Code | 后端：GPT-5.5/Codex | 产品逻辑：本文档

---

## 现状盘点

### 数据库（已建好）
| 表名 | 用途 | 状态 |
|------|------|------|
| `sop_daily_status` | 每人每天 5 个卡点的状态（IDLE/PENDING/SUBMITTED/APPROVED/REJECTED/OVERDUE） | ✅ 表已建，未接前后端 |
| `sop_checkpoint_submissions` | 每个卡点的实际提交内容（选题文本、文案文本、视频链接等） | ✅ 表已建，未接前后端 |
| `sop_review_scores` | 审核评分（6 维度：开头感染力/观点质量/合规红线/绩效钩子/昨日回顾/导粉话术） | ❌ **已废弃**。表仍在，但前后端零调用，批改台改用 `content_feedback_cards` |
| `groups` | 分组，已有 `leader_user_id` 字段 | ✅ 已建 |
| `profiles` | 用户，已有 `team_id`、`group_id` | ✅ 已建 |
| `submission_batch` | 数据填报批次（现有的提交流程） | ✅ 已建，卡点①在用 |

### 前端（部分搭好）
| 模块 | 状态 |
|------|------|
| 5 个节点的视觉框架（数据报表→早会→选题→脚本→成片） | ✅ 有了，但只是按钮，没有状态流转 |
| 数据填报表单（截图上传+指标录入+视频信息） | ✅ 基本打通 |
| 审核中心 Tab | ❌ 空壳，只有"暂无待审核数据"；当前 dashboard 未展示该入口 |
| 全域矩阵 Tab | ❌ 空壳；当前 dashboard 未展示该入口 |
| 卡点状态追踪 | ❌ 没做 |
| 组长看板 | ❌ 没做 |
| 组长日报 | ❌ 没做 |
| 异常标红 | ❌ 没做 |

---

## P0-1：卡点状态实时追踪（历史设计，未上线）

### 产品目标
每个达人的 5 个卡点状态，所有人实时可见。组长看自己组，你看全局。

### 数据流

```
达人操作                          系统自动                    组长/负责人看到
─────────                        ─────────                  ─────────────
① 填报数据 → 提交            
   → sop_daily_status.data_report_status = 'SUBMITTED'
   → sop_checkpoint_submissions 插入一条 DATA_REPORT 记录
                                                       → 卡点①变绿 ✅

② 早会（暂不需达人在网站操作）
   → sop_daily_status.morning_review_status = 'APPROVED'（组长手动确认或自动）
                                                       → 卡点②变绿 ✅

③ 提交选题 → 达人填写选题文本 → 提交
   → sop_daily_status.topic_status = 'SUBMITTED'
   → sop_checkpoint_submissions 插入一条 TOPIC 记录
                                                       → 卡点③变黄（待审核）🟡

④ 组长审核选题 → 通过
   → sop_daily_status.topic_status = 'APPROVED'
                                                       → 卡点③变绿 ✅

④' 组长审核选题 → 打回
   → sop_daily_status.topic_status = 'REJECTED'
   → sop_checkpoint_submissions.review_status = 'REJECTED'
                                                       → 卡点③变红 ❌ + 打回原因

⑤ 提交文案 → 达人填写文案 → 提交 → 组长审核（同上逻辑）
⑥ 提交成片 → 达人填视频链接 → 提交 → 组长审核（同上逻辑）
```

### 前端展示

**5 个节点的视觉改造**（历史设计，未上线；当前 dashboard 的 5 节点展示请以真实代码为准）

每个节点显示：
- 图标 + 名称 + 状态色
- 状态色规则：
  - 灰色 = IDLE（还没到这一步）
  - 蓝色闪烁 = PENDING（该做了，还没交）
  - 黄色 = SUBMITTED（已提交，等审核）
  - 绿色 = APPROVED（通过）
  - 红色 = REJECTED（被打回）
  - 红色脉冲 = OVERDUE（超时未交）

**全局看板**（所有人的卡点状态一览表）

```
         | 数据报表 | 早会 | 选题 | 文案 | 成片
─────────|─────────|─────|─────|─────|─────
阿禅     |   ✅    | ✅  | 🟡  | 灰  | 灰
张三     |   ✅    | ✅  | ✅  | 🟡  | 灰
李四     |   ❌    | 🔴  | 灰  | 灰  | 灰
```

### 需要新建的 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/sop/status` | GET | 查询某日所有人的卡点状态（按组过滤） |
| `/api/sop/status` | PATCH | 更新某个卡点的状态（提交/审核通过/打回） |
| `/api/sop/submit` | POST | 提交某个卡点的内容（选题/文案/成片） |

### 需要改的前端

| 组件 | 改动 |
|------|------|
| 5 个节点按钮 | 改成有状态色的流程节点，可点击进入对应卡点 |
| 新增 `SopStatusBoard` 组件 | 全局状态一览表（表格形式） |
| 数据填报提交后 | 自动把 `sop_daily_status.data_report_status` 改为 SUBMITTED |

---

## P0-2：组长看板（历史设计，未上线）

### 产品目标
组长打开网站，第一眼看到"我这组今天谁交了谁没交"。

### 数据流

```
组长登录 → 系统识别 group_id + role = 'admin'
→ 自动过滤只看本组成员
→ 展示组员的卡点状态 + 异常标红
```

### 前端展示

**组长看板页面结构**

```
┌─────────────────────────────────────────────────┐
│  第一组 · 今日概览           2026-05-06          │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─── 待处理 ──────────────────────────────┐   │
│  │ 🔴 李四 · 数据报表未交（超时 30 分钟）    │   │
│  │ 🟡 王五 · 选题待审核                     │   │
│  │ 🟡 赵六 · 文案待审核                     │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  ┌─── 组员状态 ────────────────────────────┐   │
│  │        | 报表 | 早会 | 选题 | 文案 | 成片 │   │
│  │ 张三   |  ✅  | ✅  | ✅  | ✅  | 🟡   │   │
│  │ 李四   |  ❌  | 🔴  | 灰  | 灰  | 灰   │   │
│  │ 王五   |  ✅  | ✅  | 🟡  | 灰  | 灰   │   │
│  │ 赵六   |  ✅  | ✅  | ✅  | 🟡  | 灰   │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  ┌─── 审核队列 ────────────────────────────┐   │
│  │ 王五 · 选题: "A股放量突破..." · [通过] [打回]│   │
│  │ 赵六 · 文案: "今天大盘..."    · [通过] [打回]│   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  ┌─── 今日数据汇总 ────────────────────────┐   │
│  │ 组均播放: 12,350 | 组均点赞: 856         │   │
│  │ 填报率: 3/4 (75%) | 审核通过率: 2/2      │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

### 权限逻辑

| 角色 | 能看到什么 |
|------|-----------|
| owner（你） | 所有组的看板，可切换组 |
| admin/组长 | 只看自己组 |
| member/达人 | 看不到组长看板 |

### 需要新建的 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/sop/leader-board` | GET | 返回本组今日所有成员的卡点状态 + 待审核列表 + 数据汇总 |

### 需要改的前端

| 组件 | 改动 |
|------|------|
| 审核中心 Tab | 改成组长看板，不再是空壳 |
| 导航 | 根据 role 动态显示/隐藏"审核中心"入口 |

---

## P0-3：组长日报（历史设计，未上线）

### 产品目标
组长每天 20:30 前在网站提交日报，你一页看 5 份，不用翻微信群。

### 数据库

**新建表 `leader_daily_reports`**

```sql
create table public.leader_daily_reports (
  id uuid primary key default gen_random_uuid(),
  leader_user_id uuid not null references public.profiles(id),
  group_id uuid not null references public.groups(id),
  report_date date not null,

  -- 固定 4 个字段（对应管理设计文档 §5.2）
  topic_feedback text,        -- 选题：今日组内选题方向亮点/问题（举例）
  opening_feedback text,      -- 开头：哪条开头写得好/哪条开头有问题
  script_feedback text,       -- 脚本文案：组内整体写作水平观察、典型问题
  video_feedback text,        -- 视频：成片质量观察、突出案例/待改进案例

  submitted_at timestamptz default now(),
  updated_at timestamptz default now(),

  unique (leader_user_id, report_date)
);
```

### 前端展示

**组长日报提交入口**（在组长看板底部或独立 Tab）

```
┌─────────────────────────────────────────────────┐
│  第一组 · 今日日报                 2026-05-06    │
├─────────────────────────────────────────────────┤
│                                                 │
│  1. 选题                                        │
│  ┌───────────────────────────────────────────┐  │
│  │ 今日组内选题方向亮点：张三的"A股放量突破"切入  │  │
│  │ 角度好，从散户心理切入而非纯技术分析...       │  │
│  │ 问题：李四选题偏娱乐化，跟财经复盘定位不符...  │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  2. 开头                                        │
│  ┌───────────────────────────────────────────┐  │
│  │ 王五今天的开头用了反问句，情绪张力强...       │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  3. 脚本文案                                    │
│  ┌───────────────────────────────────────────┐  │
│  │ ...                                        │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  4. 视频                                        │
│  ┌───────────────────────────────────────────┐  │
│  │ ...                                        │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│                              [保存草稿] [提交]   │
└─────────────────────────────────────────────────┘
```

**你的查看入口**（负责人视角）

```
┌─────────────────────────────────────────────────┐
│  组长日报一览                     2026-05-06     │
├─────────────────────────────────────────────────┤
│                                                 │
│  第一组 · 张组长           ✅ 已提交 20:15      │
│  > 选题：今日整体方向对，有 2 条切入角度新颖...   │
│  [展开查看全部]                                  │
│                                                 │
│  第二组 · 李组长           ✅ 已提交 20:28      │
│  > 选题：组内选题同质化严重，明天需要差异化...    │
│  [展开查看全部]                                  │
│                                                 │
│  第三组 · 王组长           🔴 未提交（超时）     │
│                                                 │
│  第四组 · 赵组长           ✅ 已提交 19:50      │
│  > 选题：今天全员选题质量不错...                  │
│  [展开查看全部]                                  │
│                                                 │
│  第五组 · 陈组长           🟡 草稿未提交        │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 需要新建的 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/leader-report` | GET | 查询日报（组长看自己的，owner 看全部） |
| `/api/leader-report` | POST | 提交/保存草稿 |
| `/api/leader-report` | PUT | 修改已提交的日报 |

---

## P0-4：异常自动标红（历史设计，未上线）

### 产品目标
异常情况不用人盯，系统自动推到组长和你的视野里。

### 异常判定规则

| 异常类型 | 触发条件 | 推给谁 |
|---------|---------|--------|
| 超时未交 | 卡点截止时间后仍未提交 | 组长 + 你 |
| 连续数据下滑 | 播放量连续 3 天下降（对比前 3 天均值） | 组长 |
| 断更 | 当日未提交数据且未申请豁免 | 组长 + 你 |
| 审核积压 | 待审核超过 30 分钟未处理 | 组长 |

### 时间节点（对应管理设计文档）

```
卡点① 数据上报  截止 11:15
卡点② 早会      截止 12:00
卡点③ 选题      截止 15:30
卡点④ 文案      截止 18:00
卡点⑤ 成片      截止 20:00
```

### 实现方式

**方案：定时任务 + 前端轮询**

1. 后端定时任务（每 5 分钟跑一次）：
   - 检查 `sop_daily_status` 中各卡点状态
   - 到了截止时间还是 IDLE/PENDING → 改为 OVERDUE
   - 查询近 3 天播放量，连续下滑 → 标记异常

2. 前端轮询（每 60 秒）：
   - 调 `/api/sop/alerts` 获取当前异常列表
   - 有新异常 → 顶部显示红色通知条

### 异常展示位置

| 位置 | 展示方式 |
|------|---------|
| 组长看板顶部 | 红色告警条，列出本组所有异常 |
| 你的全局看板 | 红色告警条，列出所有组的异常 |
| 5 个节点流程条 | 超时的节点红色脉冲动画 |
| 导航栏 | 异常数量角标（如"审核中心 3"） |

### 需要新建的 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/sop/alerts` | GET | 返回当前用户可见范围内的异常列表 |
| `/api/sop/check-overdue` | POST | 手动触发超时检查（定时任务调用） |

---

## 当前已上线的批改台反馈链路

以下内容反映代码真实状态，优先以此为准：

1. **触发**：系统依据播放涨跌幅（`play_change_signal`）等异常指标，在 `/admin/content` 列表中标记待复核视频。
2. **分析**：组长打开 `ContentDetailDialog`，在「分析」Tab 查看视频数据、截图、同账号上一条对比、AI 辅助分析结果。
3. **反馈**：组长在「反馈」Tab 填写两个字段：
   - **主要问题**（`confirmed.summary.one_line` / `problem_tags`）
   - **改进反馈**（`confirmed.actions.message_for_member`）
4. **下发**：点击「确认并下发」后，数据写入 `content_feedback_cards`，员工在 `/dashboard` 收到反馈。
5. **员工端**：员工查看反馈卡片，可回复「收到」或「申诉」。
6. **沉淀**：组长可对优秀/失败案例点击「标记为经验」，写入 `content_experience_marks`；不做自动全量沉淀。

> 关键约束：当前批改台**没有评分机制**，不要引入 `sop_review_scores` 或任何多维度打分字段。

## 实施顺序建议

```
第一步：卡点状态追踪（P0-1）
  → 后端：接通 sop_daily_status + sop_checkpoint_submissions 的 CRUD
  → 前端：改造 5 个节点组件，加入状态色
  → 前端：新增 SopStatusBoard 全局状态表
  → 验收：达人提交数据后，卡点①变绿；组长能看到全局状态表

第二步：组长看板（P0-2）
  → 后端：/api/sop/leader-board 接口
  → 前端：审核中心 Tab 改造成组长看板
  → 验收：组长登录后看到本组成员状态 + 待审核列表

第三步：审核流转（P0-1 的延伸）
  → 后端：卡点③④⑤的提交+审核 API
  → 前端：审核队列 + 通过/打回交互
  → 验收：达人提交选题 → 组长看到 → 通过/打回 → 达人看到结果

第四步：组长日报（P0-3）
  → 后端：建 leader_daily_reports 表 + API
  → 前端：组长日报提交表单 + 你的查看列表
  → 验收：组长能提交日报，你能看到 5 份

第五步：异常标红（P0-4）
  → 后端：定时任务 + /api/sop/alerts
  → 前端：告警条 + 角标 + 节点脉冲
  → 验收：超时自动标红，连续下滑自动告警
```

---

## 给 Claude Code 的指令要点

1. 所有 SOP 相关 API 统一用 `SUPABASE_SERVICE_ROLE_KEY`，不用 anon key
2. `sop_daily_status` 的状态流转要严格按 IDLE → PENDING → SUBMITTED → APPROVED/REJECTED 走，不能跳步
3. 组长看板的权限过滤：`group_id = (select group_id from profiles where id = auth.uid())`
4. 5 个节点组件要支持所有状态色，不要只做 IDLE 和 SUBMITTED
5. 异常检查用 Supabase Edge Function 或 Next.js API Route + cron，不要在前端算

## 给 GPT-5.5/Codex 的指令要点

1. 先跑通 P0-1 的后端 API，这是其他功能的基础
2. `sop_checkpoint_submissions` 的 `topic_text`、`script_text`、`video_url` 三个字段，不同卡点只填对应的字段，其他留 null
3. 审核打回时必须写 `rejection_reason`，前端强制校验
4. 异常检查定时任务用 Vercel Cron，间隔 5 分钟，接口兼容 `CRON_SECRET ?? REMIND_SECRET`
