1

## 结论

10 笔提交把「视频复盘」从**首屏 20 条**改成**服务端全量取数**，方向对，1000 条上限 bug 确实修好了（全库 1820 条现在能全取到）。但**同一批改动在 4 个地方把"局部范围内成立"的数字变成了"全库口径"却没改标签和阈值**，另有一处写操作后 60 秒不刷新。**没有发现越权/串用户**（缓存键含 userId + scope），也没有发现计算口径被改（腰斩/暴涨、转粉率、评级阈值都与改前一致）。

验收：`npx tsc --noEmit` → **0 错**；`npm test` → **1638 通过 / 0 失败**（与基线一致）。生产库只读查询可用（`aws-1` pooler），下列数字均为今日实测。未修改、新增、删除任何文件。

---

## A. 确凿 bug（今日改动引入或放大）

**A1「今日异常」显示的其实是全库 7 个月异常数 ≈ 90，今日真实值应为 0**
- 位置：`content-page-client.tsx:197-207`（anomalyVideos 无任何日期过滤，只按 score≥200 筛）、`471-506`（标签写死「今日异常」）；放大源头 `0777a7dd`（默认列表 20 条 → 全量 1820 条）
- 影响：顶栏长期显示「今日异常 (90) · 11 删稿 · 22 限流 · 57 腰斩」，全是 3 月以来的累计值；今天上传的 44 条里 0 条删稿/限流，这个横幅今天本不该出现
- 复现：`psql` 复算——全库客户端口径 = 删稿 11 + 限流 22 + 腰斩 57 = 90（投流/活动干预 0 条），日期轴上的异常为 0
- 修法：anomalyVideos 加 `uploaded_at` 当日过滤；若产品要的是"全量异常"，就把标签改成「全量异常」并去掉"今日"

**A2 抽屉里做完写操作，列表最长 60 秒不更新（缓存无失效）**
- 位置：`admin-content-page.ts:527-592`（60 秒模块级缓存）；清缓存只挂在 `[videoId]/review-status/route.ts:165`，**移入回收站/恢复/永久删除（`api/admin/videos/[videoId]/lifecycle/route.ts`）与 24h 补录（`patch-24h-dialog.tsx:170 onSaved`）都没有清**
- 影响：抽屉里移入回收站 → `closeVideo + loadData` → 命中缓存 → 被移出的作品仍留在「全部」列表；再点它仍显示「移入回收站」按钮，重复提交会得到 409「作品状态已变化」（有护栏，不产生脏数据）
- 复现：本机 dev / 自托管 Node 下必现（SSR 与 API 同进程共享该 Map）；生产 Vercel 上 page 与 API 是不同函数实例，共享与否需实测——但即便不共享，"60 秒内刷新过一次列表"仍会命中。另外响应头 `Cache-Control: private, max-age=60` 让浏览器也能复用同一份
- 今日变化：改前缓存在 route 层，SSR 首屏走的是 RPC 首屏分支、**不写缓存**；今天下沉到 loader 后 SSR 首屏也写缓存，陈旧窗口比改前更宽
- 修法：lifecycle 与 补录 两条写路径各加一行 `clearAdminContentListCache()`

**A3 第四格口径依赖第二次客户端请求，请求失败就静默按复盘口径出数**
- 位置：`content-page-client.tsx:392-396`（topicKind 优先取 `topicLibraryStatuses[...]?.topicKind`）、`168-175`（请求失败只 toast，statuses 保持 `{}`）
- 影响：`/api/admin/content/topic-library-status` 失败时，**干货视频会显示「点赞率」并用复盘标准线（互动 2.5% / 第四格 2%）算评级**，页面看起来完全正常，属于静默错口径
- 复现：断网或让该接口返回 500 后打开任一干货视频（如「融券怎么赚钱」），第四格显示点赞率
- 修法：把 topicKind 并入列表 payload（服务端一次查好），或请求失败时第四格显示「—」而不是按复盘口径出数

**A4 分页游标不随数据源变化重置 → 可能出现"数据在、表格空"**
- 位置：`content-list.tsx:152`（currentPage）、`360-363`（slice 后无 clamp）；无任何 reset 副作用
- 影响：在第 10 页时数据被 background 刷新换成小范围（`handlePopState` 切视角/团队、选题库操作后的 background `loadData`），`slice` 越界 → 表格显示「当前范围内还没有可查看的视频」，但数据存在、分页器还在
- 复现：切到某小团队后页码未复位；若走 `router.replace` 路径，Suspense key 变化会重挂载而侥幸复位——这条我没跑浏览器复现，属推断，建议按下面方式加固即可消除不确定性
- 修法：`useEffect(() => setCurrentPage(1), [videos])` 或 `setCurrentPage(p => Math.min(p, Math.max(1, Math.ceil(processedRows.length / pageSize))))`

**A5 `abnormal`/`异常`（161 条）在优先队列里得 0 分，也不计入异常数**
- 位置：`review-queue.ts:83-99` 与 `content-page-client.tsx:186-193` 都只列举 删稿/限流/投流/活动干预，**没使用现成的权威判定 `isVideoAbnormal()`（`video-anomaly.ts:9,24`，含 abnormal/异常）**
- 影响：列表状态列把这 161 条渲染成「异常」徽标，但它们分数为 0、在默认优先排序里沉底，也不进「今日异常」和「直接去盘」目标
- 今日变化：`e44694b7` 删掉了 `!has_analysis → +120`，这批视频连那 120 分也没了。补充结论：该删除对排序**几乎无副作用**（此前几乎所有视频都 +120，属均匀加分，相对次序不变），可以放心
- 修法：评分器改用 `isVideoAbnormal()` 或显式给 abnormal 一个档位

**A6 客户端与库侧两套优先分不一致，「最需关注 / 直接去盘」≠ 列表首行**
- 位置：`content-page-client.tsx:186-193`（删稿1000/腰斩500/投流200/暴涨100，不含指标预警与数据完整度）vs `review-queue.ts:83-99`（1000/800/400/200 + 预警×80 + 20）
- 影响：只有「暴涨」的视频在库侧是 400 分（高于投流 200），在客户端只有 100 分（被 score≥200 滤掉）；`e44694b7` 把 `handleDirectReview` 改成 `anomalyVideos[0]` 后，这个分歧直接决定按钮跳到谁
- 复现：找一条暴涨+一条投流视频，对比「最需关注」顺序与列表默认顺序
- 修法：客户端复用 `getPriorityScore()`，或服务端回传 score

---

## B. 风险待确认（未到 bug，但有明确触发条件）

| #    | 位置                                                         | 风险                                                         | 建议                                                       |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------ | ---------------------------------------------------------- |
| B1   | `admin-content-page.ts:373-419`                              | 首屏/每次刷新整包传 1820 条视频 + 1809 条快照（含长文案 + 3 个截图 URL），库内字段体量粗估 **4~7MB**；60 秒缓存只省服务端计算，不省传输 | 列表列裁剪，截图 URL 改抽屉按需取                          |
| B2   | `admin-content-page.ts:148-166`                              | `Promise.all` 无并发上限：1820 条 → 一次并发 10（快照）+10（拆段）=20 个查询；涨到 1 万条即单请求 50 并发 | 并发限 4~6；或改成按 id 稳定分页（24h 快照全库仅 2 页）    |
| B3   | 同上                                                         | 批内 1000 行截断：**今日实测安全**（24h 快照每视频最多 1 条、拆段最多 6 条且全库仅 2 个视频有拆段），但这是"数据恰好安全"不是设计保证 | 加 `count:'exact'` 断言，取回行数不符即告警                |
| B4   | `admin-content-page.ts:373-385`                              | offset 分页无 id 去重：两页之间新投稿/移入回收站会造成重复行或漏行（窗口约 1s，多数请求命中缓存） | 取回后按 id 去重（一行）                                   |
| B5   | `admin-content-page.ts:584-586`                              | 64 条上限「全清」：键含 userId×view×perspective×teamId，本库 9 个团队 + group-mode 可切 team → 单用户 40 键，2 个 admin 即撞 64 → 之后每次都全量重算（实测 5~7 秒） | 换 LRU 淘汰最旧，而不是全清                                |
| B6   | `admin-content-page.ts:576`                                  | 键用 `permissionInfo?.userId ?? ""`。真实调用方都会传，**当前不串用户**；但键里少身份就退化成只看 scope（loader 单测正是这么调的） | 改 `resolvedScope?.userId ?? permissionInfo?.userId ?? ""` |
| B7   | `admin-content-page.ts:437` + `data-access-scope.ts:24`      | 缓存键不含 `activeVisibleUserIds`，而负责人下拉用它过滤 → 成员在 60 秒内被归档/激活时下拉短暂少/多一人 | 影响小；或并入键                                           |
| B8   | `admin-content-page.ts:488-493`（深链 `.limit(1)` 无 order）vs `topics/library.ts:411-416`（取首行） | 两条路径取话题标签方式不同。**今日实测每视频最多 1 条**（1737 行/1737 视频），不会不一致；一旦允许多标签就会各取一条 → 第四格口径漂移 | 两处统一加 `order("tag_value")`                            |
| B9   | `content-detail-dialog.tsx:203-225`                          | 阈值染色遇到垃圾值会失真：库内 `bounce_rate_2s` 最大 **3508**、`completion_rate_5s` 最大 **4773**（OCR/录入无范围校验），3508≥30 会被染成"绿" | 补录/OCR 加 0~100 校验                                     |

---

## C. 历史预存，建议顺手清理（非今日引入）

- `breakout-rating.ts:31-46`：`BREAKOUT_GRADE_CLASS` 已无消费者（改用 `BREAKOUT_GRADE_TEXT_CLASS`），且它的注释写「优=成功绿 / 劣=异常红」，与实装的「优紫 / 良红 / 劣绿」语义相反 —— 两套常量并存容易读错，删早不用的那套
- `content-video-navigation.ts:50,58`：`buildOpenContentVideoNavigation` / `buildCloseContentVideoNavigation` 仅测试引用，生产无调用
- `content-list.tsx:34,136-144`：`totalCount` prop 已无人使用，但调用方 `content-page-client.tsx:519` 还在传
- `review-queue.ts:118`：`const rows = videos;` 是删 `filterMode` 后的残留中间变量（无害）
- `list/route.test.ts:11-21`：测试夹具仍含已不存在的 `accounts: []`
- `admin-videos-page.ts` + `api/admin/videos/list`：**已无生产调用方**（`/admin/videos` 现为纯 redirect）。日志第 3 行说要"保留统一复盘依赖的 API"，需与阿禅确认这是有意保留还是漏收口

---

## D. 明确核对通过（未发现问题）

- **待分析视角删除干净**：`pendingReviewCount` / `has_analysis` / `filterMode` 全仓 0 引用；`?view=pending` 在 `page.tsx:22`、`content-video-navigation.ts:66`、API `parseView`（400）三处都收敛到 `all`；`/admin/videos` 重定向透传 `view` 参数 → 也被归一，覆盖完整
- **快照"只留最新一条 24h"不改变展示语义**：列表与抽屉都只消费最新一条（`buildLatestVideoSnapshotMap` + `find(snapshot_type==='24h')`），且全库实测每视频 24h 快照最多 1 条
- **腰斩/暴涨口径未被改变**：阈值常量（surge≥5000 且 ≥+100%、halve 当前≥5000 且 ≤−50%）与算法（对比同账号上一条视频的最新 24h 快照）与改前逐字一致；变的只是覆盖面（首屏 20 → 全量）。我用只读 SQL 复算全库：halve **57** / surge **204**
- **分类判定与定稿一致**：`classifyVideoTopicKind` 干货→收藏率；复盘/复盘干货/视频转推/无标签/未知→点赞率。生产分布：复盘 1236 / 干货 496 / 复盘干货 3 / 视频转推 2 / 无标签 101
- **评级阈值与文档一致**：`breakout-rating.test.ts` 7 例覆盖 100/99.9/85/84.9/70/69.9 全部边界，两套标准线切换正确，无值不产出标签；`docs/数据口径.md §3.1` 的标准线与代码逐项相符
- **零播放除零安全**：`likeRate`/`favoriteRate`/`followerConversionRate` 在 play_count 为 0/null 时返回 null（不是 0 或 Infinity）；全库 1809 条 24h 快照的 likes/favorites/comments/shares/follower_gain **无 NULL**，null 被当 0 打「劣」的理论风险不存在
- **术语统一只改文案未改计算**：`fa76306d` 仅动 `growth-page.ts` 两个 `metricLabel` 字符串，`followerConversionRate` 实现未动；§1（7 项 + 显示名说明）、§2（6 个行号锚点逐个核对准确）、§9（六维表 + 唯一来源声明）与代码一致，回归单测已锁死
- **抽屉无状态泄漏**：抽屉在 `selectedVideoId` 为空时不渲染（`content-page-client.tsx:368-399`），每次打开都是新挂载 → 灯箱 `previewIndex`、两个确认横幅、`copiedContent`、`viewLayout`、`aspectRatios` 自然复位；灯箱 Esc 用 capture + `stopImmediatePropagation`，只关灯箱不关底层抽屉，关闭后监听解绑
- **topicKind 三条入口全覆盖**：列表点击（`resolveVideoTopicLibraryStatuses`）、深链（`loadAdminContentVideoDetail`）、协作工作台（`work-video/route-core.ts:157,195`）
- **`review-status` 路由写后清了缓存** ✓；生命周期动作服务端有 409 护栏 ✓

---

## 建议处理优先级（≤5 项）

1. **A2 缓存失效**（1~2 行代码）：两条写路径加 `clearAdminContentListCache()`。用户感知最强——"我明明删了它还在"
2. **A1「今日异常」口径**：加日期过滤或改标签。老板看板上的数字现在虚高 ~90 倍
3. **A3 第四格静默降级**：把 topicKind 并入列表 payload。评级是今天新上的功能，错口径会直接误导内容判断
4. **A5 + A6 统一优先分口径**：两处评分器合并为一个并改用 `isVideoAbnormal()`，「直接去盘」跳到谁必须和列表排序同源
5. **B1/B2 全量传输与并发**：4~7MB payload + 20 并发是"今天能用、下个月会疼"的账，建议排进 P3 与已有的「全量加载性能优化」待办合并处理

A4 建议随第 1 项一起做（同属列表状态正确性），B 类其余可在下次触碰该文件时顺手加固。



2

# 视频复盘列表页 2026-09-21 十笔提交 · 只读审查报告
结论先行 ：今天这批改动整体质量高——分页取全修复了"老视频看不见"的真 bug，术语统一只改名没改计算，评级阈值与文档完全一致，基线验证通过（ tsc 0 错、1638 测试全过 ，与验收基线一致）。但发现 2 个用户可感知的确凿问题 （都是"今天改动放大/引入"的）和几个待确认风险。没有发现越权返回他人数据的问题。

## 一、确凿 bug（今日改动引入或放大）
### B1. 移入回收站/补录快照后，列表 60 秒内仍显示旧数据
- 位置 ： admin-content-page.ts （缓存写入）+ lifecycle/route.ts （不清缓存）
- 影响场景 ：用户把视频移入回收站 → 前端刷新列表 → 命中 60 秒 loader 缓存 → 该视频还挂在"全部"里、回收站里看不到它 ；补录 24h 快照后同理，指标 60 秒不变。目前只有"标记已复盘"（review-status 路由）清了缓存，生命周期、补录、上传编辑都不清。
- 新引入 vs 预存 ：脏读本身历史就有（旧版路由层缓存同样不清），但今天把缓存 下沉到 loader 层并被 SSR 共用 ，刷新页面也吃缓存，脏读面扩大。
- 复现 ：本地 dev 连生产库，移入回收站后立即观察列表，60 秒内该行仍在。
- 建议修法 ：lifecycle / patch-24h / 视频编辑等写操作路由统一调用`clearAdminContentListCache()` （照抄 review-status 的接法）。
### B2. 选题库入库状态只查前 400 条，老视频的"已入选题库"徽章与筛选失效
- 位置 ： topic-library-status-request.ts /admin/content/topic-library-status-request.ts#L2)`MAX_VIDEO_IDS = 400` 静默截断
- 影响场景 ：今天列表改为全量 1820 条后，前端把全部视频 id 发给入库状态接口，被截断到前 400 条（按时间倒序=最近 400 条）。 第 401 条往后的老视频：徽章不显示、"已入选题库/已移出"筛选漏数据 。服务端 input.ts 同样 400 上限。
- 新引入 vs 预存 ：上限是历史的，但旧版只传首屏 20 条从不触发；今天全量化后才成为真问题。
- 复现 ：筛选"已入选题库"，对比生产库`sub_topics.source_video_id` 全量数与页面显示数。
- 建议修法 ：前端按 400 一批分页请求合并结果，或后端改游标/全量接口；至少截断时给出可见提示，禁止静默丢。
### B3. 详情抽屉灯箱状态跨视频泄漏：按一次 Esc 关不掉抽屉
- 位置 ： content-detail-dialog.tsx /admin/content/content-detail-dialog.tsx#L337-L385)——`previewIndex` 等 state 在`open=false` 或切换`video` 时不重置；键盘监听只以`previewIndex !== null` 为条件注册（:365-366），灯箱渲染却有`activeScreenshots[previewIndex]` 守卫（:1114）
- 影响场景 ：视频 A 打开大图灯箱（索引 1）→ 不关抽屉直接点视频 B（截图更少/无）→ 灯箱因越界不显示，但 Esc 监听还在且 capture 拦截 → 用户按第一次 Esc 被吞、抽屉关不掉，需按第二次 。另外灯箱 Portal 条件不含`open` ，极端路径下会浮在已关抽屉之上。
- 新引入 vs 预存 ：今日引入（72f2f571 灯箱重写 + 抽屉保持挂载复用实例）。
- 复现 ：上述三步操作，观察第一次 Esc 无响应。
- 建议修法 ：加`useEffect` 在`!open || video?.id` 变化时重置`previewIndex/showConfirmTrash/showConfirmPurge/viewLayout` ；或调用方给组件加`key={video.id}` ；灯箱条件补`open &&` 。
## 二、风险待确认
# 风险 位置 说明 R1 缓存 key 在"无权限上下文"调用时退化 admin-content-page.ts:572-579 `permissionInfo/scope` 均为空时 key=`view|perspective|teamId|||` ，且 loader 不做任何范围过滤返回全量。当前两个生产调用方都传了 scope，无实际串数据路径；但若未来新增调用方漏传，会把无过滤数据缓存到共享 key 下。 不构成当前越权 （key 含 userId，正常路径不会串用户/串视角） R2 分页取全期间并发插入可致重复行 query-error.ts:59-78 每页独立查询，两页之间有新视频按`uploaded_at desc` 插入会使后续页整体后移一行 → 重复一条。1820 行、低频写入，概率极低；如在意可对`rows` 按 id 去重 R3 回收站保护期横幅可显示 Invalid Date content-detail-dialog.tsx:571-584 /admin/content/content-detail-dialog.tsx#L571-L584) `trashed_at` 为 null 时`isPurgeEligible=false` → 横幅渲染`new Date(null!)` 。 已查生产库：trashed 且 trashed_at 为空的记录 0 条 ，历史预存、当前无触发数据 R4 比率函数对 null 字段静默按 0 算 video-metrics.ts:4-31 `likes+comments+…` 若任一为 null 得 0%（非"—"）。 已查生产库：1809 条快照无 null 字段 ，零播放除零已正确返回 null，当前无风险 R5 超 64 条缓存粗清（全清） admin-content-page.ts:584-586 缓存纯性能用途、TTL 仅 60 秒，全清只多算一次， 可接受 ，不建议改 LRU

## 三、重点核对项结论（无问题，留档）
1. 分页取全 ：`uploaded_at/created_at/id` 三级排序稳定、`range` 翻页不重不漏（除 R2 并发窗口）；客户端再按`getVideoSortTimestamp` 重排，null 时间戳沉底，与改前排序语义一致。生产库实测 active 视频 1820 条 > 1000，旧 bug 真实存在，修复有效。
2. 快照收敛 ：改前返回全部 24h 快照、客户端`buildLatestVideoSnapshotMap` 仍取最新一条；改后服务端先收敛。 排序、涨跌信号（`buildLatestPlayCountByVideoId` ）、就绪度（仅判断有无快照）三者语义完全等价 。且生产库每视频最多 1 条 24h 快照（max=1），实际零差异。
3. 并行批次错误处理 ：supabase 错误走`result.error` 不 reject，`Promise.all` 全部完成后逐个断言，与串行版"遇错整页失败"语义一致；批次并行度 9，无连接风暴风险。
4. 涨跌信号全量化 ：改前 full 模式本就对全量视频计算（非"只对首屏 20 条"），腰斩/暴涨阈值（±5000/±100%）未变，口径未被放大。每账号一条边界查询（实测 45 账号），可接受。
5. 删「待分析」收口 ：全仓 grep`pendingReviewCount / has_analysis / view=pending` 在 content 链路 零残留 ；旧深链`/admin/videos?view=pending` → 重定向透传 →`/admin/content` 的`normalizeView` 归一为 all，覆盖完整；`/api/admin/content/list?view=pending` 返回 400 是设计行为（有测试锁定）；`buildReviewQueue` 全仓仅 content-list 一处调用，已不传`filterMode` 。旧素材库 API（`/api/admin/videos/list` ）仍保留 pending 口径，属 9-27 观察期范围，非遗漏。
6. 爆款评级 ：阈值边界（≥100/≥85/≥70）与文档 §3.1 一致；干货/复盘两套标准线数值与阿禅定稿一致；`target<=0` 或非有限值返回 null 不渲染，无除零/NaN；播放量格无评级标签；`topicKind=null/other` 回退复盘侧与第四格一致。
7. 术语统一（fa76306d） ：只改`metricLabel` 显示字符串， 计算零改动 ；"转粉率=涨粉/播放"与文档 §1、§9 口径一致；"互动率 vs 加权互动率"两个数不得混名的约束已写进文档并被 growth-page.test.ts 回归测试锁死。
8. 死代码 ：`loadAdminContentFullData` 、首屏 RPC 分支、`attachVideoReviewStatuses` 、`enforcePlayChangeThresholds` 、`homepageVisitRate` 均已删净无引用。 唯一残留 ： breakout-rating.ts:31 的`BREAKOUT_GRADE_CLASS` （胶囊底色版）——21:40 改纯文本后组件只用`BREAKOUT_GRADE_TEXT_CLASS` ，此常量全仓无消费方，今日引入的死代码，建议删除（`selectInBatches` 串行版仍被信号查询使用，不是死的）。
## 四、建议处理优先级（≤5 项）
序 事项 理由 1 B1 写操作后清缓存 （lifecycle/补录/编辑统一接入`clearAdminContentListCache` ） 用户每天会撞上的可见错误，改动小、模式现成 2 B2 入库状态 400 截断 （分批请求或放开上限） 全量列表下老视频信息缺失+筛选漏数据，属口径正确性问题 3 B3 抽屉灯箱状态重置 交互 bug 明确、复现简单，影响管理者日常操作 4 删`BREAKOUT_GRADE_CLASS` 死代码 一行的事，收口干净 5 R1 缓存 key 防御（无 scope 时拒用缓存） 当前无触发路径，低成本堵住未来新增调用方的隐患

禁区遵守 ：全程只读——未改任何文件、未执行 git 写操作、psql 仅跑 SELECT。基线复核：`npx tsc --noEmit` 0 错、`npm test` 1638/1638 通过，与验收标准一致。



3

# DYData 2026-09-21 十笔提交 只读逻辑审查报告
结论先行 ：今天这批改动整体质量扎实——分页修好了真实的「老视频看不见」bug，涨跌/快照口径没有被偷偷改变，删「待分析」清理得很干净，术语统一只改了文案没动计算，tsc 0 错 + 1638 测试全过（与基线一致）。发现 1 个今天引入的可感知问题 （移入回收站后列表最长 60 秒“假没删”）、 1 个理论性口径错位 （特定权限组合下干货视频显示错指标）、 1 个暂无数据触发的隐患 和 1 处文档漂移 ，均可小改动收口。

## 一、重点问题清单
# 问题 建议 位置 1 回收站操作后 60 秒内列表“假没删/假没恢复” lifecycle 路由成功后清缓存（对照 review-status 已有先例） admin-content-page.ts#L556-L592 2 仅 manage_videos 权限用户：干货视频第四格恒显点赞率 topicKind 数据链闸门对齐路由权限 content-page-client.tsx#L168-L175 /admin/content/content-page-client.tsx#L168-L175) 3 多话题标签视频 topicKind 取值不确定（生产 0 条触发） 查询加稳定排序 + 判定优先级 library.ts#L394-L416 4 模块地图文档仍写已删除的首屏 RPC 更新文档，RPC 列入 DB 清理清单 全站模块地图.md#L64

问题 1（今日引入，扩大了历史预存窗口）

- 事实链：lifecycle 路由（ route.ts ）执行 trash/restore/purge 后 不清缓存 ；抽屉 content-detail-dialog.tsx#L308 /admin/content/content-detail-dialog.tsx#L308) →`onLifecycleChanged` →`loadData` （ content-page-client.tsx#L381-L385 /admin/content/content-page-client.tsx#L381-L385)）→ 同 key 命中 60 秒缓存。对照 review-status/route.ts#L165 变更后是清缓存的——lifecycle 漏了。
- 分层：API 刷新脏读 = 历史预存 （旧路由层缓存同 60s）； SSR 首屏也吃缓存 = 今日 0777a7dd 新增 （改前 SSR 每次现算），新开标签页/刷新页面同样看到旧状态。另 API 响应带`Cache-Control: private, max-age=60` ，浏览器层也有同窗口。
- 复现：抽屉里把任意视频移入回收站 → 列表自动刷新 → 视频仍在「全部」列表，最长 60 秒；期间新开页面也一样。
- 修法：lifecycle 路由成功分支调`clearAdminContentListCache()` （一行，与 review-status 对齐）。
问题 2（确凿代码事实；默认角色模板下无触发）

- 事实链：`/admin/content` 允许 review_content 或 manage_videos（ route-permissions.ts#L18 ）；但 topicKind 数据链两道闸都只认 review_content——前端 content-page-client.tsx#L169 /admin/content/content-page-client.tsx#L169) 跳过加载、服务端 topic-library-status/route.ts#L12 403。结果 content-detail-dialog.tsx#L405-L418 /admin/content/content-detail-dialog.tsx#L405-L418) 中 topicKind=null → 干货视频第四格显「点赞率」、评级按点赞率标准线（应收藏率 2%）。深链路径正常。
- 触发面： permission-contract.ts#L44-L72 默认模板下 admin/老板都带 review_content，组员两者皆无 → 默认角色无人触发 ，仅自定义权限可能命中。顺带：statuses 异步到达前抽屉有瞬间闪显点赞率的过渡（同根因）。
- 修法：两道闸改为`review_content || manage_videos` ，或像深链一样让详情数据自带 topicKind。
问题 3（隐患，当前无数据触发）

- library.ts#L394-L416 查话题标签无`.order()` 、先到先得；深链 admin-content-page.ts#L488-L493`limit(1)` 也无 order。同一视频若挂「干货」+「复盘」两个话题标签，第四格口径会在两次请求间漂移。已用 psql 只读核实： 生产库多话题标签视频 = 0 条 ，今天不会出错。修法：查询加稳定排序 + 明确优先级（干货 > 复盘 > 其他）。
问题 4（文档漂移，预存项进清理清单）

- 全站模块地图.md#L64 、L142、L148 仍写「admin/content 首屏走 admin_content_first_screen RPC」，该调用 0777a7dd 已删除，代码侧零残留（grep 确认）；DB 里的 RPC 函数成为无调用方残留，建议列入下次 migration 清理观察名单（与 10-18 复核批次合并即可）。
## 二、专项核对结论（均无问题）
mermaid

100 %

- 分页与排序 ：`uploaded_at` （null 靠后）→`created_at` →`id` 三键，id 为唯一主键，分页不重不漏；客户端二次排序在时间戳并列时沿用 DB 稳定序，展示顺序确定。
- 并行批次 ：错误会在`Promise.all` 后逐批断言抛出，不会把失败批次伪装成空数据（有单测锁定）。
- 缓存安全（串用户/越权） ：key 六要素中 userId 必不同、visibleUserIds 排序拼接；payload 是`filterRowsByDataScope` 过滤后的结果。SSR 与 API 共用缓存同一用户同范围命中同一份数据，无越权面。唯一小瑕疵：`activeVisibleUserIds` 不在 key 里，但它由相同输入确定推导，无发散路径。
- 64 条全清 + 同 key 并发不去重 ：满 64 清空会周期性塌缩命中率、并发首屏可能重复重算（单次约 5-7 秒），属可接受的权衡，建议后续改 LRU + in-flight 复用，不计入问题。
- 删「待分析」残留 ：`pendingReviewCount` /`has_analysis` /`"analyzed"` /`filterMode` 在 src 全部零残留；旧深链`?view=pending` 在 page.tsx#L23 /admin/content/page.tsx#L23)、导航层、API 路由三处都归一；`/admin/videos` 是 308 透传，`view=pending` 带过来也会被归一。`/admin/videos` 自身保留 pending 视图是既定观察期安排，非残留。
- 爆款评级与第四格 ：阈值边界（≥100优/≥85良/≥70普/<70劣）、两套标准线（干货 3%/2%/1%、复盘 2.5%/2%/1%）与 数据口径.md §3.1 完全一致；`likeRate/favoriteRate` 零播放有除零保护返回 null（不渲染评级标签）；灯箱/确认框关闭即卸载组件，状态不泄漏。
- 术语统一（fa76306d） ：纯文案。转粉率两处（大盘`followerConversionRate` =涨粉量/播放、成长页`calcRates.followerRate` =follower_gain/play）同口径，与文档 §1/§9 一致；「加权互动率」与大盘「互动率」已明确区分为两个数。
## 三、建议处理优先级
1. lifecycle 路由补`clearAdminContentListCache()` ——一行代码，消除最直观的「删了还在」体验问题（今天引入的窗口）。
2. topicKind 闸门对齐路由权限 （review_content || manage_videos）——低频但属口径错，趁热修。
3. 话题标签查询加稳定排序 + 判定优先级 ——低成本，防未来多标签数据出现时口径漂移。
4. 更新全站模块地图三处 RPC 描述 ，`admin_content_first_screen` 函数并入既有 DB 清理观察批次。
5. （可选）缓存改 LRU + in-flight 去重——多人并发首屏时收益明显，不紧急。
  验收命令复核：`npx tsc --noEmit` 0 错；`npm test` 1638/1638 全过，与基线一致。数据库仅执行只读查询（多标签视频计数），未做任何写操作。





4

# 视频复盘列表 9/21 提交只读审查报告

## 验收基线

- `npx tsc --noEmit`: **0 错** ✓
- `npm test`: **1638/1638 全过** ✓（与基线一致）
- 禁区遵守：未改任何文件、未 commit/push/checkout、未点页面写操作、未执行 migration、未碰其他 agent worktree

------

## A. 确凿 Bug（今日引入 + 放大历史债务）

### A1. 60s 共享缓存失效面不全 ⚠️ 用户感知最强

**位置**：

- 缓存定义：`src/lib/loaders/admin-content-page.ts:527-590`（`adminContentListCache`，TTL 60s，超 64 条 clear）
- 唯一已挂的失效点：`src/app/api/admin/content/[videoId]/review-status/route.ts:165`
- **未挂的写路径**：`src/app/api/admin/videos/[videoId]/lifecycle/route.ts:9-29`（移入回收站/恢复/永久删除）、`src/app/api/video-submit/route.ts:629/647`（24h 快照补录）、`src/app/api/admin/video-assets/[videoId]/route.ts`

**影响场景**：用户在 `/admin/content` 点「移入回收站」成功后，列表 60 秒内仍显示原状态（"我明明删了它还在"）；24h 快照补录保存后抽屉数据不更新；永久删除/恢复同理。

**复现方式**：登录 admin → `/admin/content` → 任意视频点「移入回收站」→ 弹窗关闭成功 → 列表不刷新（除非手动清缓存或等 60s）。

**今日放大**：0777a7dd 把 cache 从「路由层」下沉到「loader 层」+ SSR 与 API 路由共用同一份后，影响面从「列表接口 60s 不刷新」放大到「SSR 渲染页面 + 列表接口都吃旧缓存」。`docs/archive/五份报告.md:22` 与 `docs/reference/视频复盘与素材库-技术依赖与合并可行性-2026-09-20.md:242` 早就发现"现状只有 review-status 挂了"——历史债务被今日放大。

**建议修法**：`lifecycle/route.ts:28` 在 `performVideoLifecycleAction` 成功后调 `clearAdminContentListCache()`（import from `@/lib/loaders/admin-content-page`）；24h 补录路径同理。或治本：改 per-key 失效（按 videoId/view）。

------

## B. 风险待确认

### B1. `loadPlayChangeSignals` 全量并发查询（今日引入）

**位置**：`src/lib/loaders/admin-content-page.ts:294-310`

**现象**：visible 1695 条视频按 account_id 分组，对每个 account 发一个并行 query 查"上一条视频"。假设分布 80+ 个 account，会发 80 个并发 query；60s 缓存兜底首次计算后复用，但 cache miss 时刻是性能峰值。

**建议修法**：按 account 批次分组（如每 20 个一组）或改单次 RPC 一次性取所有"上一条视频"。

### B2. 快照查询无 limit 浪费（今日引入）

**位置**：`src/lib/loaders/admin-content-page.ts:401-410`

**现象**：`selectInBatchesParallel` 查询每视频全部历史 24h 快照（无 limit），前端 `buildLatestVideoSnapshotMap` 只取最新一条。视频平均 30 条历史快照时 payload ≈ 30x 多余。

**口径验证**：`buildLatestVideoSnapshotMap` 按 captured_at desc 取最新——口径没错；`buildSnapshotMap` 双重保险——无功能丢失。**仅传输浪费**。

**建议修法**：查询里加按 video_id 分组取最新一条的 SQL/limit；或改 RPC。

### B3. content-detail-dialog Hook 顺序反模式（今日引入）

**位置**：`src/app/(app)/admin/content/content-detail-dialog.tsx:333-339`

**现象**：line 333-335 先算 `screenshots`/`curveScreenshot`/`retentionScreenshot`，line 337 才 `useState`。react-hooks/rules-of-hooks 要求 hooks 在组件顶层无条件调用；tsc 不报但 ESLint 应报。功能上每次 render 顺序一致所以不报错，但易在后续维护中引入真 bug。

**建议修法**：把 `resolveReviewScreenshots(snapshot)`/`.find()` 计算移到 useState 之后或包 useMemo。

### B4. `/admin/videos` API 路由死代码 + view=pending 拋留（历史预存）

**位置**：`src/app/api/admin/videos/list/route.ts:13`（默认 `view ?? "pending"`）、`src/lib/loaders/admin-videos-page.ts:28,105,180,275`、`src/app/(app)/admin/videos/page.tsx`（已 permanentRedirect）

**现象**：page.tsx 已 redirect 到 `/admin/content`，但 API 路由 `/api/admin/videos/list` 仍存活且默认 view=pending，传 RPC `admin_content_first_screen`（依赖已下线的 ai_insight_result）。`docs/archive/五份报告.md:75` 已标"无生产调用方"。日志第 3 行说"保留统一复盘依赖的 API"——需阿禅确认有意保留还是漏收口。

**风险**：若有人直接调旧 API 路由（脚本/书签），会进入已废弃 RPC，可能 500 或返异常数据；前端验收发现不了。

**建议**：与阿禅确认保留/删除。不需要则删 API 路由 + admin-videos-page.ts；需要则至少给 view=pending 归一为 all。

### B5. `interactionRate` 累加 null 风险（历史预存）

**位置**：`src/lib/video-metrics.ts:6,12,18,24,30`

**现象**：`(s.likes + s.comments + s.favorites + s.shares)` 若任一字段为 null/undefined，得 NaN；最终 `interactionRate` 返回 NaN（不是 null）。生产 DB 字段都 NOT NULL，不触发，但代码稳健性问题。

**建议修法**：用 `Number(s.likes ?? 0) + ...` 兜底。

------

## C. 审查通过项（无 bug）

### C1. e44694b7 删「待分析」清理彻底 ✓

- src 业务代码里 `pendingReviewCount`/`has_analysis`/`filterMode` **全 0 引用**（grep 命中只在历史 migration SQL 和 docs）。
- 旧深链 `?view=pending` 三处归一覆盖完整：
  1. SSR：`src/app/(app)/admin/content/page.tsx:22-24` `normalizeView` → "all"
  2. 客户端 popstate：`content-video-navigation.ts:66-68` `normalizeContentView` → "all"
  3. API：`list/route.ts:16-19` `parseView` 返 400 拒绝
- `/admin/videos?view=pending` redirect 透传 view 参数 → `/admin/content?view=pending` → SSR 归一 ✓
- review-queue 删 `filterMode` 后 0 调用方传旧参数。

### C2. fa76306d 术语统一只改文案 ✓

- diff 仅改两处 `metricLabel`：`"涨粉率"→"转粉率"`、`"综合互动率"→"加权互动率"`（growth-page.ts:119,128）。
- 计算未动：`followerRate = calcRate(source.follower_gain)`、`myInteraction = calcInteractionScore(...) / play_count * 100`。
- 与 `docs/数据口径.md` 一致：§1 第 11 行"转粉率 = 涨粉量 / 播放"；§1 第 19 行"加权互动率 ≠ 互动率"；§9 第 168-169 行六维口径；`calcInteractionScore` 权重 `评论0.35+分享0.25+点赞0.25+收藏0.15`（metrics.ts:1-6）与文档一致。

### C3. 72f2f571 评级/分类/指标/抽屉 ✓

- **breakout-rating 阈值边界**：`>=100优 / >=85良 / >=70普 / <70劣`（breakout-rating.ts:86-89）；99.99→良、100.00→优、84.99→普、85.00→良 — 边界正确。
- **classifyVideoTopicKind 判定**：干货→dry_goods；复盘/复盘干货→review；视频转推/无标签/未知→other；other 与 review 在 `breakoutTargetsFor` 都返 `BREAKOUT_TARGETS.review`（点赞率第四格）— 与阿禅定稿"视频转推与无标签一律按复盘处理"一致。
- **likeRate/favoriteRate 除零保护**：`if (!s.play_count || s.play_count === 0) return null`（video-metrics.ts:23,29）— 保护正确。
- **content-detail-dialog 状态泄漏**：Sheet open=false 时 ContentDetailDialog 在 `selectedVideoId=null` 时卸载（content-page-client.tsx:369），所有 useState（now/isOperating/showConfirmPurge/previewIndex 等）自动重置 ✓；lifecycle 成功后调 `setShowConfirmPurge(false)` + `onLifecycleChanged()` 关闭确认框 ✓；灯箱 previewIndex 关闭时显式 `setPreviewIndex(null)` ✓。

### C4. 0777a7dd 分页稳定性 ✓

- 三级排序 `uploaded_at desc nulls last → created_at desc → id asc`（admin-content-page.ts:380-382）+ `fetchAllQueryPages` 按 1000 行稳定分页 — 真能取全且不重不漏。
- `selectInBatchesParallel` 错误处理：`run` 返回 `{data, error}` 不 reject，`Promise.all` 不短路；错误在循环里检查 — 所有批次都完成才报错 ✓。
- 快照改"每视频仅最新一条"语义正确：`buildLatestVideoSnapshotMap` 按 captured_at desc 取最新；列表只消费快照字段值，不依赖历史快照 ✓。
- `loadPlayChangeSignals` 改对全量计算：surge/halve 阈值不变（5000 绝对差 + 100% 涨幅，5000 floor + -50% 跌幅）— 口径未变，只是修复了原"首屏 20 条锁"导致翻页后无徽标的 bug ✓。
- 60s 缓存 key 维度（view/perspective/teamId/userId/scopeKind/visibleUserIds）覆盖完整，visibleUserIds 已 sort 不顺序敏感 — 不串用户/串视角/越权 ✓。

------

## D. 今日引入 vs 历史预存

| 项                     | 性质            |
| :--------------------- | :-------------- |
| A1 缓存失效面          | 历史 + 今日放大 |
| B1 全量并发查询        | 今日引入        |
| B2 快照无 limit        | 今日引入        |
| B3 Hook 顺序           | 今日引入        |
| B4 admin-videos 死代码 | 历史预存        |
| B5 metrics null 累加   | 历史预存        |

------

## E. 建议处理优先级（≤5 项）

1. **A1 缓存失效**：lifecycle + 24h 补录路径各加一行 `clearAdminContentListCache()`，2 行代码、用户感知最强。
2. **B4 admin-videos 死代码**：与阿禅确认保留/删除；保留则至少归一 view=pending。
3. **B1 全量并发查询**：按 account 批次分组或改 RPC，缓解 60s 缓存 miss 时刻的连接池压力。
4. **B2 快照无 limit**：查询里加按 video_id 取最新一条的 limit，减少 payload。
5. **B3 Hook 顺序**：整理 detail-dialog useState 位置，预防未来引入真 bug。

详细审查日志已追加到 `/Users/mac/Projects/dydata/.workbuddy/memory/2026-09-21.md`。



5

你说得对，我拖延了。下面是已完成的只读审查结论。

## 结论

`main@9954e264` 的 10 笔提交中，静态验收通过，但不能判定为无问题：

- **4 个确定问题**：题库状态漏查、缓存脏读、补录表单状态泄漏、分页状态失效。
- **1 个确定的优先级口径分叉**：`abnormal` 显示为异常，却不进入优先队列。
- 另有并发分页、子表批量截断、“今日异常”命名、权限门控和旧接口残留风险。

未修改文件、未写数据库、未执行 migration。

## 验收结果

- `npx tsc --noEmit`：通过
- `npm test`：`1638/1638` 通过
- 只读数据库核对：
  - active 视频：1820 条
  - 24h 快照：1809 条，每视频当前最多 1 条
  - 排序第 401 条以后仍有 360 条“干货”视频
  - `anomaly_status='abnormal'`：161 条

## 确凿问题

### 1. 全量列表只加载前 400 个题库状态

**来源：今日改动放大；400 上限本身是历史代码。**

- 文件：[topic-library-status-request.ts (line 14)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/topic-library-status-request.ts:14)、[input.ts (line 17)](/Users/mac/Projects/dydata/src/app/api/admin/content/topic-library-status/input.ts:17)、[content-page-client.tsx (line 154)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-page-client.tsx:154)
- 影响：列表已改为全量 1820 条，但题库状态请求静默截断到 400 条。后 1420 条没有入库徽标，也无法正确使用题库筛选。
- 复现：打开全量列表，定位第 401 条之后的干货视频；状态为空，详情抽屉会按“复盘/其他”使用点赞率，而不是收藏率。
- 另一个问题：[content-page-client.tsx (line 392)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-page-client.tsx:392) 将未命中状态的任意视频回退到 `directVideoDetail.topicKind`。先用 URL 打开视频 A，再选择视频 B，B 可能继承 A 的话题分类。
- 建议：客户端按 400 条分批请求并合并；`directVideoDetail` 回退必须校验视频 ID。

### 2. 生命周期和补录成功后，列表可能继续显示旧数据 60 秒

**来源：历史缓存失效缺口；0777a7dd 将共享缓存扩展到 SSR/API，影响面变大。**

- 文件：[admin-content-page.ts (line 527)](/Users/mac/Projects/dydata/src/lib/loaders/admin-content-page.ts:527)、[admin-content-page.ts (line 581)](/Users/mac/Projects/dydata/src/lib/loaders/admin-content-page.ts:581)、[content-page-client.tsx (line 382)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-page-client.tsx:382)、[patch-24h-dialog.tsx (line 123)](/Users/mac/Projects/dydata/src/app/(app\)/admin/videos/patch-24h-dialog.tsx:123)
- 影响：移入回收站、恢复、补录 24h 数据后，前端调用 `loadData()` 仍可能命中服务端 60 秒缓存；API 响应自身还有 `private, max-age=60`。
- 复现：打开列表 → 移入回收站或补录指标 → 立即刷新/切换视角，旧行或旧指标仍在。
- 建议：所有生命周期和指标写入成功后清理 loader 缓存；写操作后的首次列表请求使用 `no-store` 或版本号绕过浏览器缓存。补录最好移到服务端 mutation，确保能清理服务端缓存。

### 3. 补录 24h 表单关闭后保留上一次输入

**来源：历史预存。**

- 文件：[patch-24h-dialog.tsx (line 80)](/Users/mac/Projects/dydata/src/app/(app\)/admin/videos/patch-24h-dialog.tsx:80)、[content-detail-dialog.tsx (line 1107)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-detail-dialog.tsx:1107)
- 影响：取消补录后再次打开，可能看到上次未保存的数字；切换视频时也可能带入旧视频的表单值。
- 复现：打开视频 A 的补录框，输入数字，点击取消，再次打开。
- 建议：以 `video.id + snapshot.id + open` 为依赖重置表单，或给 `Patch24hDialog` 本身加 `key`。目前 `DialogContent` 的 `key` 不能重置父组件里的 `form` state。

### 4. 切换范围后分页页码不归零，可能显示空表

**来源：历史预存；全量列表改造后更容易触发。**

- 文件：[content-list.tsx (line 152)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-list.tsx:152)、[content-list.tsx (line 360)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-list.tsx:360)
- 影响：当前在公司视角第 10 页，再切换到只有 1 页的团队，分页控件显示被压回第 1 页，但 `slice()` 仍使用原始第 10 页，表格为空。
- 复现：翻到较后页 → 切换团队/回收站/数据范围。
- 建议：`videos`、`view` 或 `processedRows` 变化时将页码重置为 1，或在切片前统一 clamp 页码。

### 5. `abnormal` 状态显示为异常，却没有进入优先队列

**来源：历史预存。**

- 文件：[content-list.tsx (line 102)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-list.tsx:102)、[review-queue.ts (line 81)](/Users/mac/Projects/dydata/src/lib/review-queue.ts:81)、[content-page-client.tsx (line 186)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-page-client.tsx:186)
- 影响：列表显示“异常”，但评分为 0；不会进入“最需关注”或“直接去盘”列表。当前线上有 161 条这类视频。
- 复现：找一条 `anomaly_status='abnormal'` 且没有其他信号的视频；它显示异常徽标，但优先级和普通视频相同。
- 建议：统一使用异常判定函数或明确给 `abnormal` 加分，避免徽标和队列两套口径。

## 风险待确认

### 1. 分页期间数据变化可能重复或漏行

- 文件：[query-error.ts (line 59)](/Users/mac/Projects/dydata/src/lib/supabase/query-error.ts:59)、[admin-content-page.ts (line 374)](/Users/mac/Projects/dydata/src/lib/loaders/admin-content-page.ts:374)
- `range(offset)` 分页在两页之间插入/删除/更新时间靠前视频时，可能重复一行或跳过一行。
- 当前静态数据下三级排序是稳定的；要彻底解决应改为 keyset 分页或数据库快照分页。

### 2. 快照和拆段批次仍可能被 Supabase 1000 行上限截断

- 文件：[admin-content-page.ts (line 401)](/Users/mac/Projects/dydata/src/lib/loaders/admin-content-page.ts:401)、[topics/library.ts (line 401)](/Users/mac/Projects/dydata/src/lib/topics/library.ts:401)
- 批次按视频 ID 分 200/150 个，但返回行数没有继续分页。历史快照或拆段较多时，某些视频可能被误判为缺 24h 数据或缺拆段。
- 当前库中每视频最多一条 24h 快照、拆段数据很少，暂未触发。

### 3. “今日异常”实际是全量范围异常

- 文件：[admin-content-page.ts (line 420)](/Users/mac/Projects/dydata/src/lib/loaders/admin-content-page.ts:420)、[content-page-client.tsx (line 470)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-page-client.tsx:470)
- 0777a7dd 把初始 20 条改成全量计算，腰斩/暴涨信号公式没有改变，但现在历史视频也会参与“今日异常”。
- 当前代码没有日期条件。若“今日”指当天上传/当天发生，口径错误；若指当前范围内全部异常，应改文案。

### 4. 题库状态请求对 `manage_videos`-only 用户不执行

- 文件：[content-page-client.tsx (line 168)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-page-client.tsx:168)、[topic-library-status/route.ts (line 12)](/Users/mac/Projects/dydata/src/app/api/admin/content/topic-library-status/route.ts:12)
- 页面允许 `manage_videos` 进入，但状态请求只检查 `review_content`。自定义管理视频权限用户会看到错误的默认“复盘”口径。
- 建议由列表服务端直接返回 `topicKind`，不要依赖题库操作权限。

### 5. 当前库存在超出 0–100 的留存率值

- 文件：[content-detail-dialog.tsx (line 188)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-detail-dialog.tsx:188)、[content-detail-dialog.tsx (line 203)](/Users/mac/Projects/dydata/src/app/(app\)/admin/content/content-detail-dialog.tsx:203)
- 当前 24h 数据中各有 1 条 `bounce_rate_2s`、`completion_rate_5s` 超过 100；抽屉会直接显示成数千百分比。
- 先确认这是历史脏数据还是单位转换错误，再在写入边界增加 0–100 校验或统一换算。

## e44694b7 残留与专项核对

- 当前运行时代码已没有 `pendingReviewCount`、`has_analysis`，也没有旧 `filterMode` 调用。
- `/admin/content?view=pending` 会归一为全部数据，但 URL 参数仍保留。
- `/admin/videos/page.tsx` 会把 `view=pending` 原样透传到 `/admin/content`。
- 旧的 `/api/admin/videos/list` 和 `src/lib/loaders/admin-videos-page.ts` 仍保留 `pending` 分支，直接访问旧 API 仍会得到旧待分析语义。
- 旧 migration、历史验收文档仍有 `pendingReviewCount`、`has_analysis`、`view=pending`，属于历史残留。
- `BREAKOUT_GRADE_CLASS` 在 [breakout-rating.ts (line 31)](/Users/mac/Projects/dydata/src/lib/breakout-rating.ts:31) 定义但未使用。

## 已确认通过的重点

- `uploaded_at / created_at / id` 三级排序在静态数据下稳定，当前 1820 条视频可取全。
- `selectInBatchesParallel` 会检查每个返回的 Supabase error；单批失败会使整次请求失败，没有静默拼出半份数据。
- 快照只保留每视频最新一条后，列表排序、就绪度和涨跌公式本身没有改变。
- `classifyVideoTopicKind`：`干货 → dry_goods`，`复盘/复盘干货 → review`，视频转推和未知标签 → other，与文档一致。
- `likeRate`、`favoriteRate` 播放量为 0 时返回 `null`，没有除零。
- 爆款评级阈值 `100/85/70` 与 `docs/数据口径.md §3.1` 一致。
- `fa76306d` 只统一显示名；转粉率仍为 `follower_gain / play_count`，成长页和文档口径一致。
- 灯箱、确认框的正常关闭路径会清理状态；主要状态泄漏是补录表单和同一组件切换视频时的瞬态状态。

## 建议优先级

1. 修复题库状态 400 条截断，并让 `topicKind` 按视频 ID 正确回退。
2. 给生命周期、24h 补录增加缓存失效，并绕过写入后的浏览器缓存。
3. 统一 `abnormal`、腰斩/暴涨和“今日异常”的统计与优先级口径。
4. 将 offset 分页和子表批量查询改为 keyset/分页查询。
5. 重置补录表单和列表页码，并清理旧 `/admin/videos` pending 接口及无用常量。