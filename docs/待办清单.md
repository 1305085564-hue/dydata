# 待办清单

> 项目级待办和技术债，一两行一条，完成后打勾注明日期。

## 库存清理分批索引（2026-09-08）

> 本段把下方原始待办按本轮清库存优先级重排，方便 P0/P1 两个 agent 回看；任务事实仍以下方原始待办和对应日志为准。

### P0 先清

直接影响线上闭环、数据正确性或发布状态。本批建议交给高判断力 agent，涉及生产库时先核查、再确认、再执行。

- [ ] 日报↔视频存量数据修复（收尾中）：全历史 1419 条日报扫描完成（修复 PostgREST 1000 行截断 Bug）；近两周 168 + 老账 657 条唯一候选已绑定并写后复验归零（8 条跨日期争议候选剔除）；**剩余 334 孤儿 + 245 冲突待阿禅人工审阅**，清单见 `output/dydata-存量待人工处理清单-2026-09-08.csv`（类型/日期/账号/提交人/候选视频），冲突不得自动绑定，孤儿需逐条决定是否作废。
- 已完成（2026-09-08 收口，证据见 日志/2026-09-08.md 11:59/12:49）：migration `20260907113000` 与三条 20260823 幂等 migration 线上对账、4A-4C 生产版本对账、近两周 168 条绑定。

### P1 清理库存

明确是旧功能、死链、孤儿入口，适合分批核查后清理。本批可交给执行型 agent，但 DB/RPC/权限项只能先出证据和方案，不能擅自改生产库。

- [ ] 旧避坑案例 DB 清理：`violation_cases` **不是死表**（conversion-hub 话术链路在用，勿 drop）；真死的是 `knowledge_cases` 家族 5 表 + `violation_test_records` + `smart_alert_claims` 和孤儿 RPC。清理草案见 `docs/plans/2026-09-08-P1库存DB清理草案.md`，**待阿禅确认保留期后出 migration 执行**。
- [ ] middleware matcher 中 violations 路径、`review_violations` 权限键：核查结论为继续保留（豁免授权链路在用、matcher 有测试断言），随上条 DB 清理一并最终处置。
- [ ] 第三批保留项复核：`/api/remind`（无 vercel cron，疑外部/手动调度）待确认调度来源后再定去留；`/api/admin/first-screen-monitor` 现役保留；DashboardWorkspaceHeader 属另一会话 WIP，先协调再动。
- 已完成（2026-09-08 收口，证据见 日志/2026-09-08.md 12:40）：test-v2 注释删除、queue-overview 口径核查作废、`admin_sidebar_badges_summary` 并入草案、types 死类型删除、useRewriteV3Logic 死链口径作废。

### P2 补质量

不一定马上产生事故，但会降低稳定性、可维护性或后续验收可信度。

- [ ] 视频复盘 previous 接口：补齐 `confirmed` 状态到 `source` 的映射边角。
- [ ] 视频复盘建议复制/复测：补 happy path 以外的测试用例。
- [ ] lint 存量 11 errors（scripts/*、talent-tab.tsx、premium-settings-modal.tsx）。
- [ ] Sentry 接入评估。
- [ ] Web Vitals 上报评估。
- [ ] 性能专项（暂缓）。
- [ ] 全站真实场景补验（暂缓）。
- [ ] P3-UI 同类残留：`collaboration-workbench.tsx` L74 视频诊断抽屉加载态仍是黑底菊花（属诊断抽屉 WIP 区域，另行处理）。
- [ ] P3-UI 同类残留：`health-bar.tsx` L270/302/334 仍有 h-6 灰框 SelectTrigger，与灰框换微环同类（待后续统一精修）。

### P3 暂缓/另派

大工程或偏业务/数据状态机的事项，暂缓或另派。

- [ ] `/admin` 路由解耦。
- [ ] `/growth` 与 `/content-tools/rewrite` 待重做页面冻结。
- [ ] 文案助手历史会话完整交互（暂缓）。
- [ ] OCR ↺ 恢复识别值。

## 原始待办

- [ ] /admin 路由解耦：8个子路由分属3个分组却共用路径前缀，需先拆权限再迁移路由（2026-08-04 记录）
- [ ] 视频复盘 previous 接口：补齐 `confirmed` 状态到 `source` 的映射边角（2026-08-09 记录）
- [ ] 视频复盘建议复制/复测：补 happy path 以外的测试用例（2026-08-09 记录）
- [ ] 功能冗余清理遗留（2026-08-22 记录，避坑案例/经营分析/旧版文案工具已下线；2026-09-08 P1 核查修正，详见 `docs/plans/2026-09-08-P1库存DB清理草案.md`）：
  - [ ] DB 清理 migration：⚠️ `violation_cases` 非死表（conversion-hub 话术链路经 video-submit 仍在读写，勿 drop）；可清理项为 `knowledge_cases` 家族 5 表、`violation_test_records`、`smart_alert_claims` 与孤儿 RPC `admin_analytics_first_screen`、`admin_sidebar_badges_summary`、`admin_cockpit_summary` 家族——草案已出，确认保留期后执行
  - [ ] middleware matcher 中 violations 路径、`review_violations` 权限键：暂保留（豁免授权链路仍用键、matcher 有测试断言）；types 中 `ViolationCase` 等死类型 ✅2026-09-08 已删
  - [ ] lint 存量 11 errors（scripts/*、talent-tab.tsx、premium-settings-modal.tsx），与清理无关，另开任务修
- [ ] Sentry 接入评估：**未接入，未完成**。2026-08-22 评估结论——缺四项前置：① 未装 `@sentry/nextjs` 依赖；② 无 `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN` 等环境变量及 Vercel 部署配置；③ 隐私策略未定（beforeSend 脱敏规则、是否上报请求体/用户 id）；④ 验收方式未定（故意抛错→Sandbox 收到事件）。四项补齐前禁止以"已接入"名义上线半成品。
- [ ] Web Vitals 上报评估：**未接入，未完成**。Next 内置 `useReportWebVitals`/`reportWebVitals` 零成本可开，但需先决定上报目的地（自建 usage_events 表 or 外部平台）、指标口径和验收方式；当前无任何采集点。
- [ ] 全站死代码/无效性能审计（2026-08-26 三路扫描+逐条人工复核，分批执行中；第一、二批 ✅2026-08-26 上线，test-v2 与第二批幂等 migration 对账 ✅2026-09-08 收口，详见 日志/）：
  - [ ] 🔁 待重做页面冻结（2026-08-26 阿禅拍板）：`/growth`（成长分析）与 `/content-tools/rewrite`（AI 文案助手）两页后续整体重做，重做前只做故障修复、冻结性能重构/视觉优化类投入；下列 growth loader 陪算重构任务随之冻结，待重做方案一并解决
  - [x] 第一批·确认死接口（前端零调用方，已逐一核实）：`/api/dashboard/watch-overview`、`/api/admin/content-comparison/[videoId]`、`/api/admin/content-observations`（route+lib）、`/api/admin/next-day-review/batch`（AI 成本接口，白挂限流额度）；连带清理 `api-path-validation.ts` 中 work-submissions/comparison 失效正则 ✅2026-08-26 完成并上线（净删 1753 行，1141 测试全过）
  - [x] 第二批·跑着但结果没人用（浪费性能）✅2026-08-26 完成并上线，1137 测试全过：① sidebar-badges 轮询链整链删除（nav-bar-client fetch+路由+监控预算；RPC 留待 migration）；② topics active 接口 select 瘦身（去 hook/topics/topic_groups 无渲染 join，claims 改显式列）；③ topics 详情页删重复 claims 查询（myClaim 由 pool 接口判定）；④ dashboard 删 userRole/ownContentDirections/summary 死透传与计算、profiles 查询去掉 role 列；⑤ admin/content 删 workflowSummary 双份计算、accounts 数组查询、full 模式下两族只为死字段服务的批量扫描、reviewedCount/snapshotCount；⑥ growth 契约删 trend 白算（前端另有 /api/dashboard/trend）。保留：credibility/reportCount（成本可忽略、文档在案，是否上 UI 待产品决策）；growth loader 结构性重构（statusCards/pkPanel 等为 contract 陪算）另立任务
  - [ ] 第三批·动前必须确认（2026-09-08 只核查未删）：`/api/remind` 主路由（cron 鉴权 + claim 幂等在线，vercel.json 无 cron，疑外部/手动调度，保留）；`/api/admin/first-screen-monitor`（现役监控接口，有测试，保留）；DashboardWorkspaceHeader（无页面导入但被 a11y 触控热区测试读取源码，另一会话 WIP 区域，先协调再动）

- [ ] 文案助手历史会话完整交互（暂缓）：补真实登录态下的历史会话加载、撤销/重做、版本时光机、保存和长会话轮询验收；页面整体重做前只处理明确故障（2026-09-04）。
- [ ] 性能专项（暂缓）：复测 `/growth`、`/content-tools/rewrite`、`/admin/content`、协作和 `/dashboard` 的真实接口耗时，先确认瓶颈与业务预算，再决定是否优化，不把 timing warning 当功能 Bug（2026-09-04）。
- [ ] 全站真实场景补验（暂缓）：补低频查询、日期入口、成员调配后 pending 豁免、实发/应发逐日对账、移动端、长会话和真实员工账号视角；每项必须完成页面→接口→持久化→刷新消费者证据（2026-09-04）。
- [ ] OCR ↺ 恢复识别值（需按字段留存 OCR 原始值、与提交状态机联动、单独排期验收）（2026-09-04 记录）。
- [ ] 日报↔视频编辑链路根治（2026-09-07 曾梓翔重复上传致无法编辑事件暴露，当日已手工止血 3 条数据，见 日志/2026-09-07.md 22:43）：
  - [x] 删视频联动日报：trash/purge/restore 走事务 RPC `transition_video_lifecycle_with_report_link` 联动作废/恢复日报，前端提示连带条数（2026-09-08 上线，`2cfdb400`）
  - [x] 日报与视频直接关联：`daily_reports.video_id` 外键 + 编辑页优先按绑定定位、历史无绑定回退日期匹配（2026-09-08 上线，`2cfdb400`）
  - [x] 审查修复：video_id 唯一索引排除作废行、重传不复活旧日报、11 个业务读路径补 `is_void=false` 过滤（2026-09-08，见 日志/2026-09-08.md 00:41）
  - [x] **上线前置**：migration `20260907113000_daily_reports_video_link.sql` 已于 2026-09-08 线上对账确认执行，字段/索引/RPC/PostgREST schema cache 均可用
  - [ ] 存量数据修复：全历史扫描（1419 条）完成，825 条唯一候选（168+657）已分批绑定并复验归零；剩余 334 孤儿 + 245 冲突待人工审阅 `output/dydata-存量待人工处理清单-2026-09-08.csv`，冲突不得自动绑定
