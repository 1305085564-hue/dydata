# 待办清单

> 项目级待办和技术债，一两行一条，完成后打勾注明日期。

- [ ] /admin 路由解耦：8个子路由分属3个分组却共用路径前缀，需先拆权限再迁移路由（2026-08-04 记录）
- [ ] 视频复盘 previous 接口：补齐 `confirmed` 状态到 `source` 的映射边角（2026-08-09 记录）
- [ ] 视频复盘建议复制/复测：补 happy path 以外的测试用例（2026-08-09 记录）
- [ ] 功能冗余清理遗留（2026-08-22 记录，避坑案例/经营分析/旧版文案工具已下线）：
  - [ ] DB 清理 migration：violation_cases、knowledge_cases 等 6 张避坑案例表已无写入方；`admin_analytics_first_screen` RPC 已无调用方——确认保留期后统一清理
  - [ ] `queue-overview` RPC 仍读 violation_cases（历史遗留无害），驾驶台"案例沉淀"卡已删，RPC 本体待瘦身
  - [ ] middleware matcher 中 violations 路径、`review_violations` 权限键、types 中残留字符串：暂保留（豁免授权链路仍用键），后续随 DB 清理一并处理
  - [ ] lint 存量 11 errors（scripts/*、talent-tab.tsx、premium-settings-modal.tsx），与清理无关，另开任务修
