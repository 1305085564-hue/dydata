# 待办清单

> 项目级待办和技术债，只保留仍需继续处理的活项；已完成或已拆出独立计划的事项从这里移除，证据回看当日日志和对应 `docs/plans/` 文档。

## 当前剩余待办（2026-09-08）

### 2026-09-27 到期复核：旧素材库入口下线

- [ ] 复核 2026-09-21 至 2026-09-27 期间 `/admin/videos` 旧链接是否仍有真实访问、报错或用户反馈。
- [ ] 如果视频复盘统一入口运行稳定，删除旧页面兼容入口 `src/app/(app)/admin/videos/page.tsx`，并同步清理仅服务于该页面的权限兼容代码和测试。
- [ ] 保留统一视频复盘仍在使用的 API、回收站接口、数据库字段和标签投稿链路；本项只下线旧页面入口。
- [ ] 删除前确认没有未处理的旧链接依赖；删除后补一次旧链接访问结果记录。
- [ ] 如果观察期内出现访问或业务问题，不删除入口，记录问题并重新安排复核日期。

### 系统设置退役的数据库残留（已清理，2026-09-26）

- [x] `/admin/settings` 已于 2026-09-26 退役：页面、阈值面板、产量目标面板、首屏 loader 与 `/api/admin/settings/thresholds`、`/api/daily-quota-config` 全部删除，旧地址保留 `manage_system` 闸机后跳 `/admin/fulfillment`。清理 migration `20260926121500_remove_retired_system_settings.sql` 已删除 `daily_quota_config`、`get_daily_quota(date)` 和旧 `video_review_thresholds` key；`system_settings` 表本身保留，飞书履约开关与选题库工作区链接不受影响。

### 2026-10-18 到期复核

- [ ] 复核 A-21 与 B-16 7 个未匹配 RPC 的 4 周观察结果：A-21 查询 `audit_logs.action = 'cleanup_observation_a21_claim'`；RPC 查询 `pg_stat_user_functions` 的 7 个函数。若均为 0 次调用，再决定是否删除；任一有调用则保留。提醒日期：2026-10-18。
- [ ] 补充（2026-09-26 B2-1 盘点登记）：同一趟请一并复核 `case_library_inbox` / `case_library_inbox_counts` / `case_library_processed` / `case_library_actor_scope`，以及 P1 清单里那 8 个候选 RPC（`admin_analytics_first_screen`、`admin_sidebar_badges_summary`、`admin_cockpit_summary`(+`_v2`)、`admin_pending_submissions_today`(+`_v2`)、`admin_pending_videos_today`、`admin_pending_violations`）。本次独立复核与 2026-09-20「6 保留 / 7 观察 / 1 补链」归类**完全一致**；`track_functions` 6 天窗口内 19 个候选**全部 0 调用**（基线，记为本次读数）。两点注意：`admin_cockpit_summary` / `admin_pending_submissions_today` 现为 `..._v2` 的 `language sql` 包装（内联使 v2 计数恒为 0），**删除必须先 v1 后 v2**；`pg_stat_statements` 已淘汰 8 条，"0 次"只能是下限。查询模板见矩阵文档附录 A。

### P1 清理库存

- [x] **旧避坑案例 DB 清理（2026-09-26 已完成并上线）**：阿禅确认保留期后，新增并已在线上执行两张只新增的删除迁移——`20260926230000_retire_orphan_case_tables.sql`（删 `violation_test_records` 40 行、`smart_alert_claims` 空表，以及失去唯一调用方的触发器函数 `update_violation_pass_rate()`）、`20260926233000_retire_knowledge_case_tables.sql`（按外键依赖序删 `knowledge_cases` 家族 5 表）。两张均无 CASCADE、`IF EXISTS` 幂等、带回滚 recipe；执行后存活对象未受伤（`violation_cases` 29 行 / 4 策略、`script_usage_records` 53 行、`violation_events` 在），账本双列对齐。**原本生产不存在的 4 张子表也一并删掉**，是为了让空库重放与线上收敛到同一终态（只删基表会在新环境因外键报错）。证据：`docs/plans/2026-09-26-B2-1数据库对象只读盘点矩阵.md`（第十三、十四节）；`docs/plans/2026-09-08-P1库存DB清理草案.md` 的清单已按其执行完毕。
- [ ] **同上一项的剩余部分（不在本轮授权内，需另立施工单）**：① 家族服务层函数 7 个（`append_knowledge_case_status_log`、`knowledge_case_snapshot`、`sync_knowledge_case_taxonomies`、`recalculate_knowledge_case_usage_metrics`、`trg_recalculate_knowledge_case_usage_metrics`、`enrich_and_verify_case`、`request_case_supplement`）——只在从未真正执行过的 `20260628113000` 里创建、**线上不存在**、应用零引用，空库重放时会引用已删表而失效。② `case_library_inbox` / `case_library_inbox_counts` / `case_library_processed` / `case_library_actor_scope` **线上现役且有调用记录**，并入下方「2026-10-18 到期复核」判定。
- [x] `violation_cases` **不是死表**（conversion-hub 话术链路在用，勿 drop）；`review_violations` 权限键与 middleware `/violations/:path*` 复核后**维持保留**（2026-09-26 复核通过）。

### 岗位归属双表一致性治理（后续单独清理）

- [ ] 处理生产中 9 条日报与绑定视频三岗位负责人不一致记录：先确认以日报还是视频为准，再生成逐条决策清单；禁止直接按一侧批量覆盖。
- [ ] 处理历史手稿列表可触达的 188 条无绑定视频日报（其中 98 条带负责人）：先区分合法日报-only 与需要补绑定的记录；禁止按账号+日期猜视频，禁止无授权批量回填或清空负责人。
- [ ] 执行前补充生产只读基线、逐条审计、可回滚方案和真实角色验收；本事项不改投稿主链路、不新建第三张归属表。

### P2 全仓门禁欠账（2026-09-13 选题库审查发现，范围外预存问题）

- [x] `src/lib/animations.ts:229` 数字滚动动画钩子存在 `react-hooks/set-state-in-effect` lint 错误（历史提交 `2a7a9804` 引入），阻断 `npm run gate:static`。修法：消除 effect 内同步 `setValue`，改后顺手确认首页数字动画无回归，重跑 `gate:static` 退出 0 才算收口。（2026-09-19 已修复，`gate:static` 退出码 0）
- [ ] 空库重建 `npx supabase db reset` 在历史 `062_ai_rewrite_unified_view.sql` 处必然失败：051 已 `DROP rewrite_length_presets CASCADE`，062 视图仍引用该表。正确修法是**直接编辑 062 删除该 UNION 分支**（全仓 grep 确认 `ai_unified_config_view` 无应用代码消费，062 本就不可重放，回写无额外风险）；不要用"新增前向迁移"——重放时 062 先炸，新迁移轮不到执行。（2026-09-26 更新：文案助手已下线、`src/lib/rewrite/` 已删除，应用代码不再查询 `rewrite_length_presets`，本条退化为纯 migration 重放问题；线上该表是否仍存活仍需单独核实，但已不再阻塞应用层收口。）

### P3 暂缓/另派

- [x] 视频复盘"加载全量列表"性能优化：当前点击后需 5-7 秒才返回全部数据（约 1000 条），仅影响主动触发场景不阻首屏。需定位瓶颈（批次查询次数 / 网络往返 / 数据库索引），独立排期。（2026-09-21 随真分页重构收口：横幅与首屏 20 条锁删除，服务端分页取全修复 1000 条上限、批次并行、60 秒共享缓存；见分支 refactor/content-true-pagination）
- [ ] 数据上传页遗留体验项（2026-09-13 UX 修复批次未含）：① ⌘+Enter 快捷键提示目前仅在"信息已齐备"后显示，建议常驻；② 草稿恢复后截图槽位仅剩云端 assetUrl，需补"来自云端"加载态，避免用户怀疑图丢了。见 `docs/plans/数据上传页面UX修复施工规格-2026-09-13.md`。
- [ ] **排行榜接回前端入口（阿禅 2026-09-26 拍板）**：随 `/growth` 下线，排行榜 `src/components/leaderboard/leaderboard.tsx` 与取数接口 `/api/dashboard/leaderboard` 已移出并保留（`src/lib/content-feedback-removal.test.ts` 有防误删守卫，**勿当死代码清理**）。**当前没有任何前端入口；2026-09-26 收口批次没有完成排行榜接入**，`src/lib/growth-removal.test.ts` 锁住「只保留文件、尚未接入口」这一现状。落地时四条约束：① 入口并入「数据管理」（`/admin/collaboration`），不是另开的独立页；② 取数必须改用该模块的放宽范围 `resolveCollaborationScope`，**否则普通组员进去仍只看到自己一行**——现状即此（接口走通用 `visibleUserIds`，组员 `kind=self` 被过滤成单人榜），属 `/growth` 下线之前就存在的存量缺陷，接入口时一并修掉；③ 不要用任何入口或文案把「数据管理」冒充成成长反馈：工作台提交后的「复盘 / 成长复盘」失效入口已整体删除，接入时必须另起真实闭环，不能复用骗点击的文案；④ 排行榜真实浏览器验收（含普通组员视角）随接入一并做，不能只靠静态测试过关。
- [ ] **日报提交成功态的真实浏览器验证（2026-09-26 `/growth` 收口遗留）**：收口批次已删除提交成功后「复盘 / 成长复盘」两条失效入口，但**该画面没有跑过真实浏览器**——`npm run gate:browser` 只覆盖 `/topics`、`/dashboard`、`/admin/content`、`/admin/collaboration` 四个页面的**首屏**，走不到「日报提交成功」这一步。需在真实登录态下完整提交一次日报，确认三件事：① 成功态正常渲染、删掉按钮后没有错位空位；② 无 404、无控制台报错；③ 剩余四个操作（留在工作台 / 修改今日数据 / AI 检查样本质量 / 挑选明日选题）均可点可用。低风险但**未取证**，不得用静态检查冒充通过。
- [x] `/content-tools/rewrite` 待重做页面冻结：重做前只处理明确故障，冻结性能重构和视觉优化类投入。（2026-09-26 阿禅拍板改为下线并删除代码，「待重做冻结」不再适用，本条关闭）
- [x] 文案助手历史会话完整交互：补真实登录态下的历史会话加载、撤销/重做、版本时光机、保存和长会话轮询验收；页面整体重做前只处理明确故障。（2026-09-26 功能已下线、代码已删除，验收对象不复存在，本条关闭）
- [x] OCR ↺ 恢复识别值（已完成，2026-09-26）：按字段留存 OCR 原值（`ocrValue`/`ocrConfidenceLevel`）并与来源/置信度/草稿/提交联动；手改字段不被二次识别覆盖，右侧 ↺「恢复识别值」只还原该字段。见 `docs/reference/截图上传维护手册.md` 指标字段来源与 OCR 原值节。
- [x] 异常处罚类型废弃项清理（已完成，2026-09-26）：现役只剩「限流」「删稿」，`/api/video-submit` 明确拒绝下线值；旧行读兼容（进异常桶、中文标签照常）。DB 约束未收紧，收紧需前向 migration。唯一入口 `src/lib/video-anomaly.ts`。
- [ ] 岗位管理组员可看视频复盘抽屉（只读）：阿禅 2026-09-23 拍板，岗位管理「本公司范围」放宽批次（D-6）先保留 403 断点，后期组员点作品也要能打开视频复盘抽屉。后续批次需新增独立只读权限键（如 `view_video_review`）给组员，`/admin/content` 与 `work-video` 按它放行只读；**不得复用 `review_content`**（会连带激活「入库 / 移出选题库」按钮）。施工时同时确认 403 期间的提示文案对组员可理解。见 `docs/plans/2026-09-22-岗位管理对组员放开本公司范围-实施方案.md` 4.5 节。

### P2 全站墨色深度与暖墨油墨感收口（2026-09-25 视觉升级）

- [ ] 全站旧墨色硬编码统一收口：将全站存量组件中约 108 个文件、1000 余处硬编码旧墨色（`#1C1917` / `#292524`）统一替换为 Claude 官方原版墨色（标题浓墨 `#141413`、正文极深暖墨 `#1F1E1D`），并同步更新 `tokens.css` 基础变量与阴影 RGBA。本项为纯色值润色，不改字阶尺寸与排版骨架，零布局破坏风险。
- [ ] 核心模块排版与衬线体逐步推进：在墨色收口后，按高频核心业务页面（选题库、视频复盘、排行榜等）逐步审查并升级 H1/H2 衬线宋体（Serif）与表头字阶规范（≥13px），避免一刀切引入换行挤压风险。
