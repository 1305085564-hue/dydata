# 待办清单

> 项目级待办和技术债，只保留仍需继续处理的活项；已完成或已拆出独立计划的事项从这里移除，证据回看当日日志和对应 `docs/plans/` 文档。

## 当前剩余待办（2026-09-08）

### P1 清理库存

- [ ] 旧避坑案例 DB/RPC 清理：`violation_cases` **不是死表**（conversion-hub 话术链路在用，勿 drop）；待阿禅确认保留期后，按 `docs/plans/2026-09-08-P1库存DB清理草案.md` 出 migration，清理 `knowledge_cases` 家族 5 表、`violation_test_records`、`smart_alert_claims` 与孤儿 RPC。执行前同步复核 middleware `/violations/:path*` 与 `review_violations` 权限键，当前结论是保留。

### P2 全仓门禁欠账（2026-09-13 选题库审查发现，范围外预存问题）

- [x] `src/lib/animations.ts:229` 数字滚动动画钩子存在 `react-hooks/set-state-in-effect` lint 错误（历史提交 `2a7a9804` 引入），阻断 `npm run gate:static`。修法：消除 effect 内同步 `setValue`，改后顺手确认首页数字动画无回归，重跑 `gate:static` 退出 0 才算收口。（2026-09-19 已修复，`gate:static` 退出码 0）
- [ ] 空库重建 `npx supabase db reset` 在历史 `062_ai_rewrite_unified_view.sql` 处必然失败：051 已 `DROP rewrite_length_presets CASCADE`，062 视图仍引用该表。正确修法是**直接编辑 062 删除该 UNION 分支**（全仓 grep 确认 `ai_unified_config_view` 无应用代码消费，062 本就不可重放，回写无额外风险）；不要用"新增前向迁移"——重放时 062 先炸，新迁移轮不到执行。动手前先查线上：`src/lib/rewrite/shared.ts` 仍在查 `rewrite_length_presets`，说明线上库该表可能实际存活（051 删除未生效），需确认线上真实状态再决定 051/062 与文案助手代码的收口顺序。

### P3 暂缓/另派

- [ ] 数据上传页遗留体验项（2026-09-13 UX 修复批次未含）：① ⌘+Enter 快捷键提示目前仅在"信息已齐备"后显示，建议常驻；② 草稿恢复后截图槽位仅剩云端 assetUrl，需补"来自云端"加载态，避免用户怀疑图丢了。见 `docs/plans/数据上传页面UX修复施工规格-2026-09-13.md`。
- [ ] `/admin` 路由解耦：8 个子路由分属 3 个分组却共用路径前缀，需先拆权限再迁移路由。
- [ ] `/growth` 与 `/content-tools/rewrite` 待重做页面冻结：重做前只处理明确故障，冻结性能重构和视觉优化类投入。
- [ ] 文案助手历史会话完整交互：补真实登录态下的历史会话加载、撤销/重做、版本时光机、保存和长会话轮询验收；页面整体重做前只处理明确故障。
- [ ] OCR ↺ 恢复识别值：需按字段留存 OCR 原始值、与提交状态机联动、单独排期验收。
