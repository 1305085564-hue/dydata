# 待办清单

> 项目级待办和技术债，只保留仍需继续处理的活项；已完成或已拆出独立计划的事项从这里移除，证据回看当日日志和对应 `docs/plans/` 文档。

## 当前剩余待办（2026-09-08）

### P1 清理库存

- [ ] 旧避坑案例 DB/RPC 清理：`violation_cases` **不是死表**（conversion-hub 话术链路在用，勿 drop）；待阿禅确认保留期后，按 `docs/plans/2026-09-08-P1库存DB清理草案.md` 出 migration，清理 `knowledge_cases` 家族 5 表、`violation_test_records`、`smart_alert_claims` 与孤儿 RPC。执行前同步复核 middleware `/violations/:path*` 与 `review_violations` 权限键，当前结论是保留。

### P3 暂缓/另派

- [ ] `/admin` 路由解耦：8 个子路由分属 3 个分组却共用路径前缀，需先拆权限再迁移路由。
- [ ] `/growth` 与 `/content-tools/rewrite` 待重做页面冻结：重做前只处理明确故障，冻结性能重构和视觉优化类投入。
- [ ] 文案助手历史会话完整交互：补真实登录态下的历史会话加载、撤销/重做、版本时光机、保存和长会话轮询验收；页面整体重做前只处理明确故障。
- [ ] OCR ↺ 恢复识别值：需按字段留存 OCR 原始值、与提交状态机联动、单独排期验收。
