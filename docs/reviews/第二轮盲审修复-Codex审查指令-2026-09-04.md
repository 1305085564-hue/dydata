# Codex 审查任务：第二轮盲审后续修复批次

> 这是一份给 [CX] Codex 的只读审查指令。WorkBuddy 已完成修复但**未 commit/push**，工作区为修改状态。请你审查修复是否真正收口、是否引入新回归。

## 背景

WorkBuddy 于 2026-09-04 完成第二轮全站质量盲审（系统内部视角），报告在 `docs/reviews/第二轮全站质量盲审-系统内部视角-2026-09-04.md`。随后经产品授权（"p1 两个去掉，我刻意的，其他的补上"），对报告中的问题做了修复批次：删 R-02/R-03（产品有意设计），补 R-01/R-06/R-08/R-09/R-11（代码）+ R-07/R-10/观察项7（文档）。**修复未 commit/push**，工作区现状即待审对象。

## 纪律

- **只读不改**：本轮只审查，不修改任何代码/migration/文档/配置。
- 先读 `docs/reviews/第二轮全站质量盲审-系统内部视角-2026-09-04.md` 全文了解原问题与修复状态表。
- 发现问题必须给 `file:line` 证据 + 影响链路 + 复现/验证方式。
- 没有真实执行或代码证据的内容标"未验证"，禁止推测为通过。
- 按 AGENTS.md 开工前输出分诊行（本次读/使用 Skill/原因/评估）。

## 审查范围（9 个本轮修改文件）

用 `git status --short` 与 `git diff <file>` 看改动。本轮应审的 9 个文件：

1. `supabase/migrations/20260904120000_revoke_fulfillment_write_rpc_authenticated_grant.sql`（新增，R-01）
2. `src/lib/notifications/server.ts`（R-06）
3. `src/lib/topics/service.ts`（R-08/R-09）
4. `src/lib/topics/feishu-workspace.ts`（R-11）
5. `docs/权限与安全说明.md`（R-07/观察项7）
6. `docs/全站模块地图.md`（R-10）
7. `日志/2026-09-04.md`（日志，不深审内容）
8. `docs/reviews/第二轮全站质量盲审-系统内部视角-2026-09-04.md`（报告同步更新，不深审）
9. `.gitignore`（加 .next.old-* 忽略，卫生改动）

> **注意**：工作区另有 6 个**非本轮脏改动**（`src/app/(app)/dashboard/video-submit-form-v2.tsx` 及其 layout.test/panel、`docs/待办清单.md`、`scripts/Claude中文汉化.command` 删除、`docs/plans/第二轮全站质量盲审-真实用户视角-2026-09-04.md`）——**不在审查范围，不要审也不要碰**。

## 逐项审查要点

### R-01（P0）— fulfillment 写 RPC 撤 authenticated GRANT
- 看 `20260904120000_...sql`：4 条 REVOKE 的函数签名是否与既有 GRANT 完全一致：
  - `mark_fulfillment_status(uuid, date, text, text, uuid)`
  - `mark_fulfillment_status_batch(uuid[], date, text, text, uuid)`
  - `remove_fulfillment_mark(uuid, date, uuid)`
  - `handle_fulfillment_appeal(uuid, text, uuid)`
- 对照既有 GRANT 位置：`20260806090000_fix_fulfillment_drop_group_refs.sql:331-334`、`20260603070000_fulfillment_workbench_backend.sql:410-413`、`20260903153000_fix_fulfillment_appeal_drop_group_refs.sql:136`。
- **确认业务路径不受影响**：4 个 API route（`src/app/api/admin/fulfillment/{mark,bulk-mark,remove,appeal/handle}/route.ts`）都用 `auth.supabase.rpc()`，`auth.supabase` 来自 `requireAdminServiceClient`（`src/app/api/admin/cockpit/_shared.ts:28`）→ `createAdminClient()`（service role）。撤 authenticated GRANT 后业务路径仍可调（service_role 仍持有 EXECUTE）。
- grep 全范围 `mark_fulfillment_status` / `remove_fulfillment_mark` / `handle_fulfillment_appeal` 在 `src/`，确认无其他地方用 authenticated 会话直调（前端 usage-events 的 eventType 字符串不算直调）。
- `get_fulfillment_range` 读 RPC 未撤，评估是否合理（跨团队读影响有限）。
- migration 执行顺序：确认 `20260904120000` 晚于所有相关 GRANT migration，REVOKE 能正确覆盖。

### R-06 — notifications UTC fallback
- `src/lib/notifications/server.ts:17-21`：fallback 从 `new Date().toISOString().slice(0,10)` 改为 `formatShanghaiDateOnly()`。
- 确认 `formatShanghaiDateOnly` 来自 `src/lib/loaders/shared.ts:5`，import 路径与签名正确。
- 确认语义：原 UTC 日 → 上海日，消除北京 0-8 点跨 UTC 午夜去重键漂移。
- 唯一 emit 调用方 `src/app/api/permission-requests/apply/route.ts:117` 传了稳定 sourceId，fallback 仍为死路径，但改动让死路径也安全——确认这个判断。

### R-08/R-09 — topics service
- `src/lib/topics/service.ts`：两处 catch 加 `console.error`（`tryLoadTopicPoolAggregates` ~1234、`loadSubTopicClaimActivity` heatResult.get ~1745）。
- `service.ts:702-713`：`error.message.includes("sub_topic_claims_one_writing_per_user_topic")` → `error.code === "23505"`。
- 确认 `sub_topic_claims_one_writing_per_user_topic` 是该表**唯一**唯一索引（`20260829091000_topics_v3_writing_states.sql:35`），23505 在此 insert 上下文只会由它触发。
- 确认 23505 判定与项目既有约定一致（`dashboard/actions.ts:395`、`team-ai/suggestion/route.ts` 无关，看 `team-join/service.ts:151`、`exemptions/apply/route.ts:255`）。
- 检查是否有测试断言依赖旧索引名串（`service.test.ts` 用 fake client，应不受影响，但跑 `npm test` 确认 1416/1416）。
- **重点查同类遗漏**：grep `error.message.includes` 在 `src/`，确认其他用法是判列名/业务串（无标准 PG code，不应改），而非同类索引名串判定。

### R-11 — feishu-workspace SSRF
- `src/lib/topics/feishu-workspace.ts`：加 `isInternalOrLoopbackHost` + `BLOCKED_HOSTNAMES`。
- 审查黑名单覆盖：IPv4 私网（10./127./169.254./172.16-31./192.168./0./255.255.255.255）、IPv6 环回（::1）、云元数据 hostname（metadata.google.internal / metadata.azure.com / 169.254.169.254 / metadata.aws.internal）。
- **重点：IPv6 内网覆盖是否完整？** `fe80::/10`（链路本地）、`fc00::/7`（唯一本地）未覆盖——但 feishu-workspace 是 admin 配置飞书地址，几乎不可能是 IPv6 内网，评估是否需要补。
- 确认 `validateFeishuWorkspaceUrl` 在 protocol/hostname 校验后调用 `isInternalOrLoopbackHost`，顺序正确。
- 检查是否有 `validateFeishuWorkspaceUrl` 的测试需要更新（grep test）。

### 文档审查
- `docs/权限与安全说明.md`：顶部日期→2026-09-04、A 节 company_role 措辞改"已线上生效（2026-09-04 探测）"、B.5 `002_invite_rls.sql` 描述标废弃（src/ 无 invite_code 引用，准入由 team_join_request 审批承担）。确认措辞与代码现状一致。
- `docs/全站模块地图.md`：topics 段补 recent7dParticipants 全量口径明示。确认与 `loadRecent7dHeat` 顶部注释（"作品按全量成员统计"）一致。

## 验收命令（你跑确认）

```
npm test                                          # 预期 1416/1416
npx tsc --noEmit --pretty false                   # src 零错误（output/batch5 既有阻断单独保留）
npm run lint                                      # 我改的三个文件 0 error（全项目既有 42575 problems 非本次引入）
```

build 特殊处理：WorkBuddy safe-delete 会拦 `.next` 清理（4880 文件 ≥ 50 阈值，rm 也被拦），需先 `mv .next .next.old-$(date +%s)` 绕过再 `npm run build`（预期 Compiled successfully + 79/79 静态页，仅收尾清理 export-detail.json 被拦，非代码问题）。

## 未动项（不在审查范围，但了解背景）

- **R-04/R-05**（video-submit/exemption 多表写入事务边界）：走文档明示保守路线，未 RPC 化（工作量大需破坏性测试）。现有 API 层 `rollbackActions`/`cleanupCreated` 在 catch 路径补偿 + 唯一索引兜底重复。
- **R-07 invite_codes 表与 RLS 清理**：不可逆 schema，未动，只改了文档措辞。

## 输出

写一份审查报告到 `docs/reviews/第二轮盲审修复-Codex审查-2026-09-04.md`，格式：

```
# 第二轮盲审修复 - Codex 审查 - 2026-09-04

## 审查范围
（列出实际读了哪些文件、跑了哪些命令）

## 逐项审查结论
每项修复：结论（收口 / 未收口 / 引入新回归）+ 代码证据(file:line) + 验证方式 + 置信度

## 新发现的问题（如有）
问题描述 / 代码证据 / 影响链路 / 复现方式 / 建议处理方向

## 仍未验证区域
具体到模块/文件/命令，不能泛泛写。

## 总体结论
是否可以 commit/push，或需要先修什么。
```

不夸大，没证据标"未验证"。审查完追加一条 `[CX]` 日志到 `日志/2026-09-04.md`。
