# 更新日志

记录这个项目里已经上线或已确认要追踪的变更。只记结果，不记流水账。

## 2026-04-13

- 修复截图上传链路里 `video_tags` 的多关键词保存问题，支持 3 个内容标签各自独立保存。
- 修复 `video_tags` 旧 `ON CONFLICT` 写法在新约束下报错的问题，相关写入改为“删旧再插入”。
- 新增 [截图上传维护手册](./截图上传维护手册.md)，专门记录截图上传 / OCR / 提交链路的维护、排查和回归清单。
- Supabase 已推送 `043_video_tags_keyword_multi_value.sql`，数据库规则已支持关键词多值。

