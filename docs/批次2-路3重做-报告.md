# 批次 2 路 3（重做）整改报告

## 字体接入状态
- next/font 已早期接入，本次 SKIP（layout.tsx 行号 2/8/15 已有 Inter + JetBrains_Mono）

## tabular-nums 补全

### 文件 1：video-list.tsx
- 新增：3 处（第 213-215 行：24h播放量 / 互动率 / 涨粉）
- 跳过：0 处

### 文件 2：history-list.tsx
- 新增：0 处
- 跳过：全部已有 `font-mono tabular-nums`（桌面端表格 9 列 + 移动端卡片 5 处）

### 文件 3：video-analytics.tsx
- 新增：0 处
- 跳过：全部已有 `font-mono tabular-nums`（Top3 卡片 3 处 + 移动端 grid 2 处 + 桌面端表格 3 处）

### 文件 4：time-analysis.tsx
- 新增：0 处
- 跳过：全部已有 `font-mono tabular-nums`（KPI 卡片 4 处 + 时段详情 3 处）

### 文件 5：personnel-analysis.tsx
- 新增：0 处
- 跳过：全部已有 `font-mono tabular-nums`（均播对比 1 处 + 稳定性 1 处 + MetricBarCard value 1 处）

### 文件 6：hit-analyzer.tsx
- 新增：0 处
- 跳过：全部已有 `font-mono tabular-nums`（分页器 2 处 + KPI 卡片 5 处 + leadInsight 4 处 + distributionCards 1 处 + SummaryBucketCard 内联）

### 文件 7：ai-insight.tsx
- 新增：0 处
- 跳过：该组件无表格/数字 KPI，内容为文本标签、文案段落、实验列表和样本警告，无需要 tabular-nums 的数字渲染场景

### 文件 8：audit-log-list.tsx
- 新增：0 处
- 跳过：该组件为操作日志列表，时间列使用 `toLocaleString` 渲染日期时间字符串（非数字），ID 列使用 `slice(0, 8)` 截断展示（非等宽场景），无需要 tabular-nums 的数字渲染场景

## 总计
- 新增 tabular-nums：3 处（全部在 video-list.tsx）
- 跳过：其余 7 个文件已完整覆盖或无可加场景

## 验收
```
$ npx tsc --noEmit 2>&1 | grep -v ".test." | wc -l
0
```
非测试文件无新增 type error。
