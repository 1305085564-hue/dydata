# 今日提交工作台 v2 改进计划

## 一、已完成：提取逻辑层（前端的后端）

### 提取的核心模块

```
src/lib/dashboard-logic/
├── use-video-submit-form.ts    // 表单状态管理
├── use-exemption-calendar.ts   // 豁免日历逻辑  
├── use-ocr-upload.ts            // OCR 上传状态机
└── submission-status.ts         // 提交状态判断
```

**特点：**
- ✅ 零 UI 依赖
- ✅ 完整 TypeScript 类型
- ✅ 可独立测试
- ✅ 可复用

---

## 二、下一步：重构 UI 层（前端的前端）

### 架构原则

```
┌─────────────────────────────────────┐
│  UI 组件（Claude 风格）            │
│  - 遵循设计哲学                     │
│  - 唯一行动色 #D97757              │
│  - 留白四级语义                     │
└─────────────┬───────────────────────┘
              │
              │ 调用
              ↓
┌─────────────────────────────────────┐
│  逻辑层（Antigravity 提取）        │
│  - useVideoSubmitForm()            │
│  - useExemptionCalendar()          │
│  - useOcrUpload()                  │
└─────────────────────────────────────┘
```

### 重构优先级

#### P0 - 核心体验修复（必须做）

**1. 豁免申请弹窗重构**
- ✅ 逻辑层：`useExemptionCalendar` 已提取
- ❌ UI 层：需要重构成左右分栏布局

**对标 Antigravity 的设计：**
```
┌──────────────────────────────────────────┐
│  申请请假或豁免               [X]        │
├──────────────────────────────────────────┤
│                                          │
│  ┌────────────┐  ┌──────────────────┐  │
│  │            │  │  ● 申请类型       │  │
│  │   日历     │  │  ○ 请假 ○ 豁免   │  │
│  │            │  │                   │  │
│  │  [一键全选]│  │  已选日期：       │  │
│  │  （七日）  │  │  [2026-08-25]  x │  │
│  │            │  │                   │  │
│  │  图例：    │  │  申请原因：       │  │
│  │  ● 已交    │  │  [文本框 0/100]  │  │
│  │  ● 豁免    │  │                   │  │
│  │  ● 请假    │  │      [取消][提交] │  │
│  │  ● 未交    │  │                   │  │
│  └────────────┘  └──────────────────┘  │
└──────────────────────────────────────────┘
```

**改进点：**
- 左侧日历 + 右侧表单分栏
- 申请类型用分段控件（Segmented Control）
- 已选日期用标签卡片展示（可删除）
- 日历底部添加图例
- 一键全选近 7 天按钮
- 字数统计 0/100

**2. 截图槽位修正**
- ✅ 逻辑层：`useOcrUpload` 已支持 2 槽位
- ❌ UI 层：需要改成 2 个槽位

**修正方案：**
```
截图上传：
┌─────────────┐  ┌─────────────┐
│  完播截图   │  │  互动截图   │
│  [上传区]   │  │  [上传区]   │
│  识别：     │  │  识别：     │
│  - 播放量   │  │  - 点赞数   │
│  - 完播率   │  │  - 评论数   │
│  - 平均时长 │  │  - 转发数   │
└─────────────┘  └─────────────┘
```

**3. 日历图例**
- 在日历 Popover 底部添加状态图例
- 颜色语义：
  - 已交：绿色 `#6FAA7D`
  - 豁免：黄色 `#B98A54`
  - 请假：蓝色 `#43718E`
  - 未交：灰色 `#E5E0D6`

#### P1 - 交互优化（提升体验）

**4. 表单分组优化**
- 基础信息卡片
- 截图上传卡片（2 槽位）
- 数据指标卡片（3 组：播放/互动/导粉）
- 补充信息卡片

**5. 已选日期可视化**
- 豁免弹窗中，已选日期用标签展示
- 支持点击 x 删除单个日期

**6. 催交记录提示**
- 在豁免弹窗中显示催交次数
- 用警告色标记被催交多次的日期

#### P2 - 功能补充（如果需要）

**7. 团队协作模块**
- 侧边栏显示：文案、剪辑、运营
- 角色分配和权限控制

**8. 悬浮提示**
- 日历 hover 显示日期详情
- 表单字段 hover 显示说明

**9. 响应式优化**
- 移动端弹窗全屏显示
- 表单字段自动换行

---

## 三、实施计划

### 第 1 步：豁免弹窗重构（2 小时）
```typescript
// 新建文件
src/app/(app)/dashboard/v2/exemption-dialog-v2.tsx

// 使用逻辑层
import { useExemptionCalendar } from "@/lib/dashboard-logic/use-exemption-calendar";
import { getDateStatus } from "@/lib/dashboard-logic/submission-status";

// 实现左右分栏 UI
// 遵循 Claude 设计规范
```

### 第 2 步：截图槽位修正（1 小时）
```typescript
// 修改文件
src/app/(app)/dashboard/redesign/screenshot-upload.tsx

// 使用逻辑层
import { useOcrUpload } from "@/lib/dashboard-logic/use-ocr-upload";

// 改成 2 个槽位：screenshot_1, screenshot_2
```

### 第 3 步：日历图例（30 分钟）
```typescript
// 修改文件
src/app/(app)/dashboard/redesign/submission-calendar.tsx

// 底部添加图例
<div className="mt-4 flex items-center gap-4 text-[13px]">
  <div className="flex items-center gap-1">
    <div className="size-2 rounded-full bg-[#6FAA7D]" />
    <span>已交</span>
  </div>
  // ...更多图例
</div>
```

### 第 4 步：整合测试（1 小时）
- 替换到主路由
- 功能测试
- 响应式测试

**总计：约 4.5 小时**

---

## 四、验收标准

### 功能完整度
- [ ] 豁免弹窗：申请类型切换、一键全选、已选日期展示
- [ ] 截图上传：2 个槽位，OCR 自动填充
- [ ] 日历：状态图例清晰
- [ ] 表单：所有字段正常工作
- [ ] 提交：数据正确保存

### 设计规范
- [ ] 唯一行动色 #D97757
- [ ] 留白四级语义
- [ ] 数字右对齐 + tabular-nums
- [ ] 毛玻璃 + 漫反射阴影
- [ ] 知识分子文案

### 交互体验
- [ ] 豁免弹窗操作流畅
- [ ] OCR 识别反馈及时
- [ ] 表单验证提示清晰
- [ ] 状态切换平滑

---

## 五、下一步行动

阿禅，逻辑层已经提取完成。现在开始实施 P0 优先级的 UI 重构。

**我现在要做：**
1. 重构豁免申请弹窗（对标 Antigravity 布局）
2. 修正截图槽位（改成 2 个）
3. 添加日历图例

你确认开始吗？
