# 页面重写信息采集

请帮我采集 **[页面名称]** 页面的完整信息，用于前端重写。按以下结构输出：

## 1. 页面入口与路由

- 路由路径：`/xxx/xxx`
- 主页面文件：`src/app/.../page.tsx`
- 数据加载器：`src/lib/loaders/xxx.ts`（如果有）

## 2. 核心业务逻辑

请找到并总结以下逻辑文件的关键函数和状态机：

- 状态管理文件（如 `xxx-state.ts`、`xxx-store.ts`）
- 业务规则文件（如权限判断、数据验证、状态流转）
- 工具函数（如日期格式化、数据转换）

**输出格式**：

```typescript
// 文件路径：src/lib/xxx-state.ts
export function 核心函数名(参数类型) {
  // 简要说明这个函数做什么
  // 关键逻辑伪代码
}

// 状态流转规则
状态A -> 条件 -> 状态B
```

## 3. 数据结构与类型定义

找到这个页面涉及的所有 TypeScript 类型定义：

- 主要实体类型（如 `Video`、`Report`、`User`）
- 表单数据类型
- API 请求/响应类型
- 组件 Props 类型

**输出格式**：

```typescript
// 文件路径：src/types/xxx.ts
interface 核心类型名 {
  字段名: 类型; // 字段说明
}
```

## 4. API 接口清单

列出这个页面调用的所有 API：

- 端点路径：`POST /api/xxx`
- 请求体结构
- 响应体结构
- 错误处理方式

**输出格式**：

```
POST /api/dashboard/submit
请求: { account_id, video_url, metrics, ... }
响应: { data: Video, ai_tags: [] }
错误: { error: string }
```

## 5. 功能组件清单

列出当前实现用到的所有子组件：

- 组件名称
- 文件路径
- 主要功能
- 关键 Props

**输出格式**：

```
VideoSubmitForm
路径：src/app/.../video-submit-form.tsx
功能：视频数据提交表单，支持 OCR、AI 标签
Props：account, userId, mode, onSubmitted
```

## 6. 用户交互流程

描述用户在这个页面的完整操作路径：

1. 用户进入页面看到什么
2. 用户可以做哪些操作
3. 每个操作触发什么逻辑
4. 成功/失败后的反馈

**输出格式**：

```
流程 1：提交今日数据
1. 用户选择账号 -> 切换账号状态，加载该账号今日数据
2. 用户填写表单 -> 实时验证，显示错误提示
3. 用户上传截图 -> OCR 识别，自动填充指标
4. 用户点击提交 -> 调用 API，成功后刷新列表，失败弹 Toast
```

## 7. 状态与权限规则

- 哪些字段在什么条件下可编辑
- 哪些按钮在什么情况下显示/禁用
- 权限判断逻辑（admin/member/owner 的区别）

## 8. 边界情况处理

- 空状态（无数据时显示什么）
- 加载中状态
- 错误状态（网络错误、权限错误等）
- 表单验证规则（必填项、格式校验）

## 9. 依赖的共享组件

列出 `@/components` 下被引用的 UI 组件：

- Button、Input、Dialog 等基础组件
- 自定义业务组件（如 SubmissionCalendar）

## 10. 特殊交互与动画

- 是否有拖拽、排序功能
- 是否有动画效果（用了 framer-motion 的地方）
- 是否有实时预览、联动更新

------

**采集完成后，请将以上 10 个部分的信息整理成一份完整文档，保存为：**

```
docs/页面重写/[页面名称]-重写信息.md
```

采集时注意：

- 代码片段保留关键逻辑，省略实现细节
- 类型定义只保留核心字段，省略可选的次要字段
- API 接口写清楚请求/响应结构
- 交互流程用自然语言描述，不要贴大段代码