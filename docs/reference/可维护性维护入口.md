# 可维护性维护入口

本页只提供本专项的代码入口、定向验证、诊断查法和发布恢复索引；业务口径、权限规则和接口响应契约仍以各自的项目文档为准。生产发布状态与本轮未覆盖项见 output/maintenance/05-验收.md。

## 1. 领域入口

| 领域 | 代码入口 | 定向测试 / 说明 |
| --- | --- | --- |
| 写链路故障定位 | src/lib/observed-mutation.ts、src/lib/sentry/capture-mutation-error.ts；接入 /api/video-submit、/api/exemptions/apply、/api/exemptions/review | src/lib/observed-mutation.test.ts、src/app/api/video-submit/observation.test.ts 及三条写接口原有测试 |
| AI 配置优先级 | src/lib/ai-config/priority.ts；调用方为 src/lib/ai-config/key-patch.ts、src/lib/ai-config/swap-key-priority.ts | src/lib/ai-config/priority.test.ts；管理员 API 的兼容接线见 src/app/api/admin/ai-channels/_shared.ts |
| 上海日期 | src/lib/loaders/shared.ts:formatShanghaiDateOnly | src/lib/loaders/shared.test.ts、src/app/api/production/_shared.test.ts；不要在调用方重新实现时区换算 |
| Dashboard 提交 | src/lib/dashboard-submission-state.ts、src/app/(app)/dashboard/video-submit-form-model.ts | src/lib/dashboard-submission-state.test.ts、src/app/(app)/dashboard/video-submit-form-model.test.ts；表单展示和请求仍由页面组件负责 |
| 豁免申请 | 共享预检入口见 [豁免申请维护入口](豁免申请维护入口.md) | 预检只判重叠，不接管登录、写入、审批或日志 |
| 复杂组件 | src/app/(app)/admin/modules/member-ai-dialogs.tsx、src/app/(app)/admin/content/use-content-comparison.ts | 对应 member-ai-preview.test.ts、content-diagnosis-workbench-structure.test.ts；父组件保留业务请求和展示契约 |

## 2. 定向命令

| 场景 | 命令 |
| --- | --- |
| 第 1 批观测与写接口 | npx tsx --test src/lib/api-logger.test.ts src/lib/observed-mutation.test.ts src/lib/sentry/privacy.test.ts "src/app/api/video-submit/*.test.ts" "src/app/api/exemptions/apply/route.test.ts" "src/app/api/exemptions/review/route.test.ts" |
| 第 2 批模块边界 | npx tsx --test src/lib/ai-config/priority.test.ts src/lib/dashboard-submission-state.test.ts src/lib/maintenance-boundaries.test.ts src/lib/content-comparison-reference.test.ts src/lib/loaders/shared.test.ts src/app/api/production/_shared.test.ts src/lib/loaders/dashboard-activity.test.ts src/lib/loaders/dashboard-page.test.ts src/app/api/admin/ai-channels/_shared.test.ts src/app/api/admin/ai-config/route.test.ts |
| 第 3 批豁免预检 | npx tsx --test src/lib/exemption-application-precheck.test.ts "src/app/(app)/dashboard/actions.test.ts" src/app/api/exemptions/apply/route.test.ts src/lib/豁免.test.ts src/lib/豁免流程.test.ts |
| 第 4 批职责拆分 | npx tsx --test "src/app/(app)/dashboard/video-submit-form-model.test.ts" "src/app/(app)/dashboard/video-submit-form-state.test.ts" "src/app/(app)/dashboard/video-submit-form-v2-layout.test.ts" "src/app/(app)/admin/modules/member-ai-preview.test.ts" "src/app/(app)/admin/content/content-diagnosis-workbench-structure.test.ts" src/lib/content-comparison-members.test.ts |
| 类型和差异格式 | npx tsc --noEmit --pretty false；git diff --check |
| 最终候选发布门禁 | npm run gate:static；npm run gate:browser；git diff --check。只对最终候选运行，环境变量只记录已配置/缺失，不写入账号或凭据 |

## 3. 诊断查询

| 目标 | 查法 | 边界 |
| --- | --- | --- |
| 定位一次写请求 | 从响应头读取 x-dydata-request-id，在服务端日志按 requestId、route、stage、outcome 查同一条记录；detail.release 用于核对版本 | 不把请求体、SQL、员工信息或业务正文写入报告 |
| 判断写链路阶段 | 关注 auth、read、validate、scope、write-*、review-rpc、compensate、finalize 及 primaryStage | 日志或 Sentry 失败不能改变业务响应；补偿发生时同时保留主失败阶段 |
| 核对 Sentry 事件 | 按白名单标签 dydata.request_id、dydata.stage、dydata.outcome 筛选；事件收件需用真实项目查看权限确认 | 隐私过滤会丢弃请求参数、正文、用户信息和 breadcrumb；未收到事件不能写“线上可观测已完成” |
| 区分服务与数据库连通 | 访问 /api/health，再访问 /api/health?check=supabase | 健康接口只证明服务/数据库连通，不证明业务权限、数据口径或页面写入 |
| 核对版本与部署 | git rev-parse HEAD、git log -1 --format='%H %s'；有部署权限时再用 vercel inspect <deployment> | Ready、commit SHA、域名指向必须同时可见；缺任一项记为阻塞 |

## 4. 发布与恢复

| 场景 | 步骤 |
| --- | --- |
| 形成候选 | 从已核对的 origin/main 建隔离分支，按维护专项第 1→4 批逐个保留领域提交；只暂存本页、工程事实、进度和日志等约定文件，不带入权限任务脏改动 |
| 发布前 | 运行最终候选的 npm run gate:static、npm run gate:browser、git diff --check，保存退出码、候选 SHA、环境和未覆盖项 |
| Vercel 核对 | 用 vercel ls 找到候选 deployment，用 vercel inspect <deployment> 核对 Ready、commit SHA 和正式域名；promote 只用于已验收的同一构建 |
| 触发恢复 | 核心页面不可用、权限边界回归或持续异常时先停止测试/写入，保留请求编号和版本证据，再确认旧版本与当前数据库/权限状态兼容 |
| 应用恢复 | 优先在 Vercel Promote/Instant Rollback 到已记录的上一个 Ready deployment；未实际回滚只能记录“未演练” |
| 源码恢复 | 部署回退不等于源码回退；需要源码恢复时在干净分支逆序撤销本专项提交并重新验证，禁止 reset 共享工作区，也不撤销权限 migration |
| 数据恢复 | 代码回退不会撤销已产生的业务数据；只处理已登记且有安全回收入口的测试对象，真实数据异常先冻结写入并报告 |
