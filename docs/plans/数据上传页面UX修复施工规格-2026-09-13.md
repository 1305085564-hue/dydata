# 「数据上传」页面 UX 修复施工规格书（三方案整合版）

> 整合来源：TRAE 独立代码审计 + Luna 审计 + Gemini 审计。所有断言已逐条对照当前代码验证，本文以代码事实为准，两处与审计原文不同的结论已在「与审计原文的出入」一节说明。

## 执行纪律（施工前必读）

- 不理解需求、不优化、不加创意；照抄本规格（UI 像素级、逻辑照搬）。
- 只允许补类型、调命名、写注释。
- 每个任务改完先自查对应验收项，全部任务完成后对照「总验收清单」逐项打勾再交付。
- 禁止碰「禁碰清单」中的文件；禁止顺手重构无关代码。
- 交付报告必须按任务编号逐条说明：完整修复 / 部分修复 / 未做及原因。

## 背景（一段话）

数据上传页（/dashboard 视频提交表单）存在一个会污染业务数据的 P0：OCR 识别失败后槽位仍被标记为「已确认」，提交校验被绕过，空白指标在前端被转成 0、服务端静默补 0 落库。其余为错误提示隐藏、无障碍缺失、账号切换脱节、补交取消陷阱、键盘流跳错焦点、移动端密度、草稿保存间隔等问题。

## 分工归属（[CX]=Codex 逻辑，[An]=Antigravity 视觉）

- **[CX] 负责**：T1、T2、T3、T4、T5、T6、T8、T10、T15、T16（数据校验、状态联动、焦点流、草稿机制、输入规则）
- **[An] 负责**：T7、T9、T11、T12、T13、T14（响应式密度、空态卡、装饰线、说明文案、分组标题、选填徽标）
- **施工顺序**：[CX] 的 T1 单独先行、单独验收；其余 [CX] 任务一批；[An] 一批最后做。
- **文件冲突提醒**：`截图槽位区.tsx` 与 `指标分组区.tsx` 两方都会改——[An] 必须等 [CX] 的改动合入后再动这两个文件，禁止并行。

## 与审计原文的出入（以本规格为准）

1. **Luna P0 根因修正**：绕过校验的直接原因不是状态机本身，而是 [video-submit-form-v2.tsx](file:///Users/mac/Projects/dydata/src/app/(app)/dashboard/video-submit-form-v2.tsx) 中 `handleSlotUpload` 成功分支把 `confirmed` 写成 `Boolean(assetUrl)`——只要有图就为 true，导致状态机 [提交状态机.ts](file:///Users/mac/Projects/dydata/src/components/submission/提交状态机.ts) 第 141-150 行的 `!slot.confirmed` 过滤全部失效。修复以前端 confirmed 语义 + 指标非空校验 + 服务端拒绝 null 三层收口。
2. **Gemini H1 根因修正**：[workspace-picker.tsx](file:///Users/mac/Projects/dydata/src/components/workspace-picker.tsx) 第 135 行只写了 `dashboard-store`，而 [production-control-system.tsx](file:///Users/mac/Projects/dydata/src/app/(app)/dashboard/production-control-system.tsx) 第 73 行用的是组件本地 `useState`，且只监听 `dydata-dashboard-action` 事件、不订阅 store。移动端抽屉能生效是因为它额外派发了事件。修复方案采用「统一派发事件」而非 useSyncExternalStore 重写（改动面最小）。
3. **Gemini M2（历史弹窗复用主表单）本期不做**：属功能重构非 UX 修复，列入待办另立任务；本期只统一字段命名。

---

## 文件清单

### 修改（13 个）

| 文件 | 涉及任务 |
|---|---|
| `src/app/(app)/dashboard/video-submit-form-v2.tsx` | T1 T2 T3 T6 T8 T16 |
| `src/components/submission/提交状态机.ts` | T1 |
| `src/app/api/video-submit/validation.ts` | T1 |
| `src/app/api/video-submit/stability.ts` | T1 |
| `src/app/api/video-submit/route.ts` | T1 |
| `src/lib/dashboard-logic/use-video-submit-form.ts` | T1 |
| `src/components/submission/截图槽位区.tsx` | T2 T3 T7 T12 T13 |
| `src/components/submission/指标输入卡.tsx` | T3 |
| `src/components/submission/指标分组区.tsx` | T3 T7 T10 T14 |
| `src/components/workspace-picker.tsx`、`src/components/user-workspace-popover.tsx`、`src/components/premium-settings-modal.tsx`、`src/components/mobile-more-drawer.tsx`、`src/lib/dashboard-store.ts` | T4 |
| `src/app/(app)/dashboard/video-submit-panel-v2.tsx` | T5 |
| `src/components/submission/metric-focus-flow.ts`、`src/components/submission/metric-focus-flow.test.ts`、`src/app/(app)/dashboard/video-submit-form-v2.tsx` | T6 |
| `src/hooks/use-form-draft.ts` | T8 |

### 新建（1 个）

| 文件 | 涉及任务 |
|---|---|
| `src/app/(app)/dashboard/no-team-placeholder.tsx` | T9 |

### 禁碰

- `src/app/(app)/dashboard/history-list.tsx`、`src/app/(app)/dashboard/actions.ts`、所有 migration/SQL、`docs/Claude设计哲学.md`、`docs/Claude设计规范.md`、导航壳层组件（`nav-bar-client.tsx` 只读参考）。
- 历史编辑弹窗 `history-report-edit-form.tsx` 仅允许 T15 列出的命名改动，禁止结构重构。

---

## 任务明细

### T1【P0】[CX] 空白指标不得落 0；识别不确定时警告但不拦路

**决策记录（2026-09-13 阿禅拍板）**：① 选 B——低置信度/待核对**不阻断提交**，改为警告条提示核对；② 选 B——留存 4 项**允许留空**（落 null 不落 0），核心+互动 7 项必填（此前口头沟通误说成 8 项，以 7 项为准：播放量/涨粉/转粉 + 点赞/评论/转发/收藏）。主防线从"槽位确认门禁"改为"指标完整性门禁"。

**改动 1a — 前端槽位 confirmed 语义**（`video-submit-form-v2.tsx`，`handleSlotUpload` 成功分支的 `updateSlotsState` 内）：

把
```typescript
confirmed: Boolean(assetUrl),
```
改为
```typescript
confirmed: data.slot_status === "confirmed" && !data.requires_manual_confirmation,
```
失败 catch 分支（`status: uploadedAssetUrl ? "pending_confirm" : "failed"` 处）同步把 `confirmed: Boolean(uploadedAssetUrl)` 改为 `confirmed: false`。

说明：此项只影响徽标与警告条显示（见 1f），不再是提交门禁——门禁职责移交给 1c 的指标完整性。用户显式确认入口仍是「手输」按钮（`onManualFill` 回调），保持不动。

**改动 1b — 收紧提交时自动解除失败态**（`handleSubmit` 内 L1855-1856）：

把
```typescript
const canAutoResolve1 = slots.screenshot_1.status === "failed" && Boolean(slots.screenshot_1.assetUrl) && (Number(fields.play_count.value || 0) > 0 || hasManualEdit);
const canAutoResolve2 = slots.screenshot_2.status === "failed" && Boolean(slots.screenshot_2.assetUrl) && (Number(fields.completion_rate.value || 0) > 0 || hasManualEdit);
```
改为（两个条件同构，仅 key 不同）：
```typescript
const canAutoResolve1 = slots.screenshot_1.status === "failed" && Boolean(slots.screenshot_1.assetUrl) && hasManualEdit;
const canAutoResolve2 = slots.screenshot_2.status === "failed" && Boolean(slots.screenshot_2.assetUrl) && hasManualEdit;
```

**改动 1c — 状态机：指标完整性成为主门禁，槽位确认降级为警告**（`提交状态机.ts`）：

1. `SubmissionIssueSummary` 增加两个字段：
```typescript
missingRequiredMetrics: EditableMetricKey[];   // 阻断提交
unconfirmedSlots: SubmissionSlotRole[];        // 仅警告，不阻断
```
2. 必填指标常量（模块顶部导出，供服务端对齐时复制）：
```typescript
export const REQUIRED_METRIC_KEYS: EditableMetricKey[] = [
  "play_count", "follower_gain", "follower_convert",
  "likes", "comments", "shares", "favorites",
];
```
留存 4 项（`avg_play_duration`、`bounce_rate_2s`、`completion_rate_5s`、`completion_rate`）**任何状态下均可留空**。空判定：`state.fields[key].value.trim() === ""`。`missingRequiredMetrics` 无论 normal/abnormal 都按上述 7 项检查。
3. **解除槽位确认阻断**：`pendingSlotConfirmations` 不再计入 `totalIssueCount`、不再参与 `canSubmit` 判定（保留字段本身，改名为 `unconfirmedSlots` 继续输出，供警告条消费）；`missingRequiredSlots`（完全没传图）与 `failedRequiredSlots`（上传失败）维持阻断不变。
4. `firstIssueAnchor` 顺序改为：slots 类 → `"metrics"` → meta → topicTag；reason 文案：`"请补全 N 项必填指标（留空不再视为 0）"`。
5. `canSubmit()` 函数同步：移除 pending 阻断分支，新增 `missingRequiredMetrics.length > 0 → { ok: false, reason: "请补全必填指标" }`。
6. `getSubmissionStage` 中"待确认"档改为仅由 `missingRequiredSlots/failedRequiredSlots` 触发，pending 槽位不再产生"待确认"阶段。

**改动 1d — 前端空值发 null 不发 0**（`use-video-submit-form.ts` 新增导出，`video-submit-form-v2.tsx` 提交体使用）：

```typescript
export function parseMetricOrNull(value: string): number | null {
  const trimmed = value.trim();
  if (!trimmed) return null;
  const parsed = Number(trimmed);
  return Number.isFinite(parsed) ? parsed : null;
}
```
`handleSubmit` 的 `metrics:` 对象里 11 项全部改用 `parseMetricOrNull`；`follower_loss: 0` 保持不变；`script_text` 判断条件里的 `parseMetric(fields.follower_convert.value) > 0` 保持 `parseMetric` 不动。

**改动 1e — 服务端拒绝必填项 null**（`stability.ts` + `validation.ts`）：

`normalizeMetrics` 返回类型改为各键 `number | null`，新增 `normalizeNumberOrNull`（不动原函数以免波及其他调用方）。在 `validateVideoSubmitPayload` 内、`metricBoundary` 校验之后追加：

```typescript
if (mode !== "edit") {
  const missing = REQUIRED_METRIC_KEYS.filter((k) => normalizedMetrics[k] === null);
  if (missing.length > 0) {
    return { ok: false, error: "指标数值不完整，空白项不会被记为 0，请补全后提交" };
  }
}
```
`REQUIRED_METRIC_KEYS` 与 1c 前端 7 项完全一致，定义在 `validation.ts` 顶部。edit 模式放行 null（服务端已有 `mergePreservedEditSnapshotFields` 保留原值）。`route.ts` 的 `snapshotPayload` 各指标键直接透传 normalized 值，禁止再补 0（当前实现即透传，仅需确认无新增兜底）。

**改动 1f — 识别不确定警告条（不阻断）**（`video-submit-form-v2.tsx` 指标区顶部，`指标分组区` 渲染之前）：

当 `issueSummary.unconfirmedSlots.length > 0` 时渲染：

```tsx
<div className="mb-2 flex items-center gap-2 rounded-lg bg-[#FFFBEB] px-3 py-2 text-[12px] text-[#92400E]" role="status">
  <AlertTriangle className="size-3.5 shrink-0" />
  {issueSummary.unconfirmedSlots.length} 张截图识别未确认，请对照原图核对指标后提交
</div>
```
`AlertTriangle` 从 `lucide-react` 导入（若文件已导入其他图标则并入同一 import）。桌面端保留 hover 查看原图能力（已有），不加确认按钮。

**验收**：见总清单 F1-F4b。

### T2【P1】[CX] OCR 失败原因不再被通用文案吞掉

`截图槽位区.tsx` 底栏（L399-409）warning 分支的固定文案改为：

```tsx
{isError ? (
  slot.error || "未识别到图片内容，请点击重新上传"
) : slot.error ? (
  <span className="lg:hidden">{slot.error}</span>
  <span className="hidden lg:inline">{slot.error} · 请核对右侧指标</span>
) : (
  <><span className="lg:hidden">已留存，请核对右侧指标</span><span className="hidden lg:inline">截图已就位，请直接在右侧核对或补全指标</span></>
)}
```
外层容器加 `title={slot.error ?? undefined}`。

### T3【P1】无障碍：指标 label 绑定 + 上传卡键盘可达

**3a** `指标输入卡.tsx`：`<Label>` 加 `htmlFor={`metric-${field.key}`}`；`<Input>` 加 `id={`metric-${field.key}`}`。

**3b** `截图槽位区.tsx` 空槽位卡片 div（L175 起）：加 `role="button"`、`tabIndex={0}`、`aria-label={`${item.title}截图，点击选择文件`}`、`onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); if (slot.status === "empty") slotInputRefs.current[item.role]?.click(); } }}`（仅 empty 态响应）。

**3c** 三个操作按钮补 `aria-label`：重新识别 `"重新识别{item.shortTitle}"`、手输 `"手动填写{item.shortTitle}指标"`、删除 `"删除{item.shortTitle}"`。

**3d** 状态徽标外层容器加 `aria-live="polite"`。

### T4【P1】桌面端切账号表单联动

`dashboard-store.ts` 新增：

```typescript
export function selectDashboardAccount(accountId: string) {
  setDashboardAccount(accountId);
  window.dispatchEvent(
    new CustomEvent("dydata-dashboard-action", {
      detail: { key: "set-account", accountId },
    }),
  );
}
```
`workspace-picker.tsx` L135、`user-workspace-popover.tsx` L170、`premium-settings-modal.tsx` L757 三处把 `setDashboardAccount(account.id)` 替换为 `selectDashboardAccount(account.id)`；`mobile-more-drawer.tsx` 已有手动派发，改为调用 `selectDashboardAccount` 并删除其重复的 `dispatchEvent` 块（保留其余 detail 逻辑若有）。`production-control-system.tsx` 的事件处理器已存在（L101-105），不改。

### T5【P1】[CX] 补交取消后回到今天

`video-submit-panel-v2.tsx`：`onCancel`（L893-896）与「返回概览」按钮（L865）两处，在 `setRequestedMode(null)` 后追加：

```typescript
if (activeBizDate !== today) {
  onActiveBizDateChange?.(today);
}
```
（`onActiveBizDateChange` 为既有 prop，PCS 已传入 `setActiveBizDate`。）

### T6【P1】[CX] 键盘流：末项指标 → 标题 → 文案

`metric-focus-flow.ts`：`MetricFocusTarget` 类型扩为 `EditableMetricKey | "video_title" | "content"`；`getNextMetricFocusTarget` 末项返回值 `"content"` → `"video_title"`。

`指标分组区.tsx` `handleKeyDown`：`next === "content"` 分支改为 `next === "video_title"` 时调 `onCompleteMetrics?.()`（语义不变，由调用方决定落点）。

`video-submit-form-v2.tsx` L2644：`onCompleteMetrics={() => contentTextareaRef.current?.focus()}` 改为 `onCompleteMetrics={() => document.getElementById("video_title")?.focus()}`。

`video_title` 输入框（L2679）加 `onKeyDown={(e) => { if (e.key === "Enter") { e.preventDefault(); contentTextareaRef.current?.focus(); } }}`。

`metric-focus-flow.test.ts`：L29-31 与 L55 两处断言 `"content"` 改为 `"video_title"`。

### T7【P1】[An] 移动端密度

**7a** `指标分组区.tsx` 三处网格 `grid grid-cols-4 gap-1.5 sm:gap-3` → `grid grid-cols-2 gap-1.5 sm:grid-cols-4 sm:gap-3`。

**7b** `截图槽位区.tsx` 顶栏：「重新识别」按钮 className 前缀加 `hidden sm:inline-flex`（手输、删除保留；重试依赖内存中 file，刷新后本就不显示，移动端损失可接受）。删除按钮移动端触控区已是 `size-7`（28px），改为 `size-9 sm:size-5.5`（36px）。

### T8【P1】草稿即时保存 + 保存状态可见

`use-form-draft.ts`：删除 30 秒 `setInterval` 块，替换为防抖保存：

```typescript
useEffect(() => {
  if (isEmpty(formData)) return;
  if (lastSavedRef.current && deepEqual(lastSavedRef.current, formData)) return;
  const timer = setTimeout(() => {
    try {
      const entry: DraftEntry<T> = { data: formData, savedAt: new Date().toISOString() };
      localStorage.setItem(key, JSON.stringify(entry));
      lastSavedRef.current = formData;
      setLastSavedAt(new Date());
      setHasDraft(true);
    } catch {
      feedbackToast.warning("草稿保存失败：浏览器存储空间不足或已禁用");
    }
  }, 1000);
  return () => clearTimeout(timer);
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [key, isEmpty, ...deps]);
```
`feedbackToast` 从 `@/components/ui/feedback-toast` 导入；catch 内失败提示用 `ref` 去重（同一会话只提示一次，避免刷屏）。另在 mount effect 内追加关页面前冲刷：

```typescript
function flush() {
  if (isEmpty(formData) || (lastSavedRef.current && deepEqual(lastSavedRef.current, formData))) return;
  try {
    localStorage.setItem(key, JSON.stringify({ data: formData, savedAt: new Date().toISOString() }));
  } catch {}
}
window.addEventListener("pagehide", flush);
return () => window.removeEventListener("pagehide", flush);
```
（formData 经 ref 透传避免闭包过期：`const formDataRef = useRef(formData); formDataRef.current = formData;`）

`video-submit-form-v2.tsx`：底部提交行左侧提示区（L2868-2875 绿色「信息已齐备」分支旁、及禁用分支旁统一位置）追加常驻微提示，仅当 `!isSubmitted && lastSavedAt`：

```tsx
<span className="text-[11px] text-[#A8A29E] tabular-nums">
  已自动保存 {lastSavedAt.getHours().toString().padStart(2, "0")}:{lastSavedAt.getMinutes().toString().padStart(2, "0")}
</span>
```

**追加改动（决策③，2026-09-13 拍板）— 新建草稿 key 跟随账号**：`video-submit-form-v2.tsx` L987-993 的 `createDraftStorageKey` 当前用 `useState` 一次性冻结，切号后仍写旧账号的 key。改为：

```typescript
const createDraftStorageKey = useMemo(
  () =>
    resolveVideoSubmitCreateDraftStorageKey({
      userId,
      accountId: account?.id ?? null,
      bizDate: today,
    }),
  [account?.id, userId, today],
);
```
效果：给 A 号填一半切到 B 号，B 号是干净表单；切回 A 号，草稿横幅出现可恢复。补交/编辑 key（`buildVideoSubmitDraftKey`）本就含 accountId 且走 useMemo，不动。

### T9【P2】未入团队空态

新建 `no-team-placeholder.tsx`（"use client"）：

```tsx
export function NoTeamPlaceholder() {
  return (
    <div className="mx-auto max-w-md py-24 text-center">
      <div className="mx-auto mb-4 flex size-12 items-center justify-center rounded-xl bg-[#F1F1F0]">
        <Users className="size-5 text-[#78716C]" />
      </div>
      <h2 className="font-serif text-[17px] font-semibold tracking-tight text-[#1C1917]">静候团队接引</h2>
      <p className="mt-2 text-[13px] leading-6 text-[#78716C]">
        你的账号还未加入任何创作团队，暂时无法提交数据。
        请联系管理员将你拉入工作组后再回到本页。
      </p>
    </div>
  );
}
```
`dashboard-content.tsx` L33-35 的 `return null` 改为 `return <NoTeamPlaceholder />`。图标从 `lucide-react` 导入 `Users`。正文第二句固定为（决策⑤：组长和管理员都有拉人权限）：`请联系管理员或组长将你拉入团队后再回到本页。`

### T10【P2】[CX] 整数指标禁止小数

`指标分组区.tsx`：`CORE_ITEMS` 中 `play_count` 的 `step: "0.01"` 删除并改为 `step: "1"`；`follower_gain`、`follower_convert` 及互动组（likes/comments/shares/favorites）全部显式加 `step: "1"`。留存组 step 不动。

### T11【P2】导粉话术装饰线去橙色

`导粉话术采集区.tsx` L36：`bg-[#D97757]` → `bg-[#E2E2DF]`。

### T12【P2】[An] 异常态截图「选填」语义

`截图槽位区.tsx`：解构参数 `screenshotsRequired: _screenshotsRequired = true` 改回 `screenshotsRequired = true` 并实际使用。空槽位标题行（L240 附近）在 `!screenshotsRequired` 时渲染：

```tsx
<span className="ml-1 rounded-full bg-[#F1F1F0] px-1.5 py-0.5 text-[10px] font-normal text-[#78716C]">选填</span>
```
描述文案 `item.description` 在 `!screenshotsRequired` 时替换为 `"异常提交可不带截图"`。

### T13【P2】上传限制说明

`截图槽位区.tsx` 两槽位网格容器（L148 的 grid div）之后追加一行：

```tsx
<div className="pt-1 text-center text-[10.5px] text-[#A8A29E] lg:text-left">
  支持拖拽 / ⌘V 粘贴 · JPG / PNG / WebP · 单张 ≤ 8MB
</div>
```
（8MB 与 `submission-screenshots/route.ts` 的 `MAX_FILE_SIZE` 一致。）

### T14【P2】指标分组标题

`指标分组区.tsx` 三个网格各自上方（网格 div 前）插入：

```tsx
<div className="mb-1 hidden text-[10.5px] font-medium tracking-wide text-[#A8A29E] sm:block">核心数据</div>
```
三组文案依次为 `核心数据`、`互动数据`、`留存数据`。`sm:block` 以下隐藏，避免挤占移动端纵向空间（移动端已有 2 列分行 + T7 密度改善）。

### T15【P2】[CX] 历史编辑弹窗：命名对齐 + 草稿保护

`history-report-edit-form.tsx`：L95 `"涨粉"`→`"涨粉数"`、L99 `"点赞"`→`"点赞数"`（其余 label 若与主表单不一致一并对齐，仅改字符串）。草稿：以 `useFormDraft` 接入，key 为 `dydata:draft:history-edit:${id}`，提交成功与弹窗关闭时 `clearDraft()`；弹窗打开时若 `hasDraft` 显示一行「检测到未保存的修改，恢复 / 丢弃」文字按钮（样式同主表单草稿横幅，`text-[12px]`）。

### T16【P1】[CX] 提交按钮不再"哑巴禁用"（决策④，TRAE 审计补录）

现状：`video-submit-form-v2.tsx` L2895 `disabled={isSubmitting || !canActuallySubmit}`，导致 L1866-1881 已实现的"抖动 + 滚动定位问题处"逻辑永远触发不了。

改为：
```tsx
disabled={isSubmitting}
```
并给禁用样式分支补 `aria-disabled={!canActuallySubmit || undefined}`；`triggerSubmit`（L2059）开头追加：

```typescript
if (!canActuallySubmit) {
  setHasAttemptedSubmit(true);
  triggerFormShake();
  scrollToIssueAnchor(issueSummary.firstIssueAnchor);
  return;
}
```
（`issueSummary` 经 ref 透传，与 `canSubmitRef` 同法。）视觉不变（仍灰态），但点击有反馈：抖动 + 滚到问题处 + 左侧灰色提示同步显示 reason。快捷键路径（L2082 cmd+Enter）经 `triggerSubmit` 自动获得同样行为，无需另改。

---

## UI 视觉规格汇总（供走查比对）

| 位置 | 规格 |
|---|---|
| 草稿时间微提示 | `text-[11px] text-[#A8A29E] tabular-nums` |
| 选填徽标 | `rounded-full bg-[#F1F1F0] px-1.5 py-0.5 text-[10px] text-[#78716C]` |
| 上传限制行 | `text-[10.5px] text-[#A8A29E]`，移动端居中、lg 左对齐 |
| 指标分组标题 | `text-[10.5px] font-medium text-[#A8A29E] tracking-wide`，仅 sm+ |
| 移动端指标网格 | `grid-cols-2`（<640px）→ `sm:grid-cols-4`，gap `1.5/3` |
| 删除按钮触控区 | `size-9`（36px）移动端，`sm:size-5.5` 桌面 |
| 空态占位卡 | 容器 `max-w-md py-24`，标题 `font-serif 17px #1C1917`，正文 `13px #78716C` |
| 导粉话术装饰线 | `w-[2px] bg-[#E2E2DF]`（原橙色） |

色值全部沿用现有 token（#D97757 仅保留给主提交按钮等行动核心；#E2E2DF 发丝线；#78716C 次级文本；#A8A29E 弱提示；#F1F1F0 底纸）。

## 逻辑规格补充约束

- T1 各层缺一不可：前端 confirmed 语义（1a/1b）、指标门禁 + 槽位警告降级（1c）、前端 null + 服务端 null 拒绝（1d/1e）、警告条 UI（1f）。只改任何一层都不算完成。
- 编辑模式（mode==="edit"）服务端保留 null 透传与 `mergePreservedEditSnapshotFields` 行为不变；前端编辑态指标回填后一般非空，不受 1c 门禁误伤（1c 读的是当前表单值）。
- `useFormDraft` 目前全项目仅视频提交表单使用（T15 将新增历史编辑弹窗一个调用方），改动前 grep 确认无其他调用方。
- 不改数据库结构、不改 OCR 接口、不改 `/api/video-submit` 的请求体字段名（只改值语义 0→null）。

## 总验收清单

**功能测试（npm test 覆盖 + 手工）**
- F1：`npx tsx --test src/components/submission/metric-focus-flow.test.ts` 通过（含改后断言）。
- F2：为 T1c 在 `提交状态机` 测试文件（若无则新建 `提交状态机.test.ts`）补 4 条用例：① pending_confirm+confirmed=false **不**阻断（`canSubmit.ok===true`，`unconfirmedSlots` 含该槽位）；② 7 项必填指标任一留空 → 阻断且 reason 含「补全」；③ 留存 4 项全空但 7 项必填齐全 → 放行；④ failed 槽位（无图）→ 阻断。
- F3：为 T1e 补服务端用例：create 模式 metrics.play_count 为 null 时 `validateVideoSubmitPayload` 返回 ok:false；completion_rate 为 null 返回 ok:true；edit 模式 null 返回 ok:true。
- F4：浏览器走查 /dashboard：上传一张无法识别的图 → 槽位显示具体错误原因 → 指标区顶部出现黄色警告条（N 张截图识别未确认）→ 提交按钮灰态但**可点击**，点击后表单抖动并滚到指标区，提示「请补全 N 项必填指标」→ 填全 7 项必填（留存留空）→ 提示变「信息已齐备」→ 提交请求体中无因留空产生的 0，留存空项为 null。
- F5：桌面端顶部导航切账号，表单归属账号即时变化（Network 面板确认后续上传请求 account_id 为新账号）；切回原账号草稿横幅出现（T8 追加改动）。
- F6：日历选过去日期进入补交 → 点「取消」→ 页面回到今天的概览。
- F7：11 项指标全键盘录入，末项回车落「视频标题」，标题回车落「文案正文」。
- F8：表单填一半等 2 秒刷新，草稿横幅出现且含「已自动保存 HH:mm」；关浏览器标签页再开，数据仍在。
- F9：信息未齐时点灰色提交按钮 → 抖动 + 滚动定位到第一个问题区域；⌘+Enter 同样触发（T16）。

**视觉走查（浏览器，320/375/768/1024/1440）**
- V1：320px 指标两列、无横向溢出、删除按钮触控 ≥36px。
- V2：橙色仅出现在主提交按钮与 OCR 强调态，导粉话术左线为中性灰。
- V3：异常状态切换后截图卡出现「选填」徽标与替代文案。

**无障碍**
- A1：`指标输入卡` 11 个输入框 labelCount≥1（DevTools Accessibility 面板或 `for/id` 检查）。
- A2：Tab 可聚焦空槽位卡，Enter/Space 打开文件选择；重试/手输/删除按钮有 aria-label。

**边界 case**
- B1：localStorage 禁用（隐私模式）下填表不崩溃，控制台无未捕获异常。
- B2：edit 模式提交不被 T1 门禁误拦（编辑今日作品改 1 个指标提交成功）。
- B3：OCR 成功但置信度低（pending_confirm）→ 黄色警告条出现但**不阻断提交**（决策①B）；7 项必填齐全时可直接提交成功。

**代码检查**
- C1：`npm run gate:static` 退出码 0。
- C2：`git diff --check` 干净；diff 不触碰禁碰清单文件。

## 参考与约束

**只读参考**：`截图槽位区.tsx` 现有徽标三态配色（L277-294）；`video-submit-form-v2.tsx` 草稿横幅（L1132-1159）；`mobile-more-drawer.tsx` 事件派发写法（L65-70）；`docs/Claude设计规范.md` 单点聚光灯原则（T11 依据）。

**必须复用**：`feedbackToast`、`cn`、`dashboard-store` 现有 emitChange 机制、`mergePreservedEditSnapshotFields`（不得绕过）。

**禁用方案**：不得用 `useSyncExternalStore` 重写 PCS 账号状态（T4 选事件派发）；不得给 `parseMetric` 原函数改默认值（新增 `parseMetricOrNull`，避免波及其他调用方）；不得在 route.ts 写入层补 0 掩盖校验问题。

---

## 转派提示词（直接复制给对应代理）

### 给 Codex（第一批，只发这一条）

> 阅读 `docs/plans/数据上传页面UX修复施工规格-2026-09-13.md`，只执行标注 [CX] 的任务，分两批：
> 第一批只做 T1（P0），完成后停下，等我验收。
> 第二批做 T2、T3、T4、T5、T6、T8、T10、T15、T16。
> 发布顺序约束：T1 的服务端改动（validation/stability/route）必须先于或随前端同批发布，禁止前端单独先发。
> 验收命令：`npm run gate:static` 退出码 0；规格书「总验收清单」中 F1-F9、A1-A2、B1-B3、C1-C2 逐条自查。
> 禁区：不做 [An] 任务；禁碰清单文件不动；`截图槽位区.tsx` 只做 T2/T3 的改动，T7/T12/T13 留给 Antigravity。
> 交付报告按任务编号逐条写：完整修复 / 部分修复 / 未做及原因。

### 给 Antigravity（第二批，等 Codex 全部合入后再发）

> 阅读 `docs/plans/数据上传页面UX修复施工规格-2026-09-13.md`，只执行标注 [An] 的任务：T7、T9、T11、T12、T13、T14。
> 前置条件：Codex 对 `截图槽位区.tsx`、`指标分组区.tsx` 的改动已合入，先 `git pull` 再动工。
> 验收命令：`npm run gate:static` 退出码 0；总清单 V1-V3 逐条走查（320/375/768/1024/1440 五档）。
> 禁区：不碰 [CX] 任务；UI 按规格书色值与 class 照抄，不加创意。
