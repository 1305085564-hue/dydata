# 阿禅美学法典 V1.2 · 合规整改进度

**起始分支**：`v1.2-upgrade`（从 `main` 分出）
**参照**：`/Users/mac/Projects/dydata/v1.2-issues-batch-1.md`（Kimi 合规扫描报告）
**整改范围**：移除黑色滥用、字号非档、状态点缺环、刺眼彩色等 V1.2 合规问题
**禁区**：见本文末尾「禁区清单」

---

## 批次进度总览

| 批次 | 状态 | Commit | 改动量 | Build |
|-----|------|--------|--------|-------|
| P0 核心（text-zinc-900 / 黑按钮 / 字号 / 状态点） | ✅ 已提交 | `1fedf55` | 45 文件 / ~95 处 | 零错误 |
| P1 中频通用（submission-calendar / hover:shadow / border-t / font-mono） | ✅ 已提交 | `6bd4fe8` | 58 文件 / ~189 处 | 零错误 |
| P2 小众专项（admin-secondary-nav 蓝色 / 渐变 / 毛玻璃残留等） | ✅ 已提交 | `0de5b73` | 22 文件 / ~75 处 | 零错误 |

---

## P0 已完成内容（Commit `1fedf55`）

### P0-1 `text-zinc-900` → `text-zinc-800`（29 处 / 9 文件）
- `dashboard/components/` 下 9 个文件全量替换
- 遵循 V1.2 第三章主文字档 `zinc-800`

### P0-2 黑按钮去塑料感（5 处 / 4 文件）
文件：`admin/generate-invite-button.tsx` / `team-manager.tsx` / `permission-manager.tsx` × 2 / `team-group-manager.tsx`
- 删 `hover:shadow-sm`
- 保留 `hover:-translate-y-[1px] active:translate-y-0`
- 依据 V1.2 第十八章《黑色激活态塑料感禁区》

### P0-3 字号分档整改（~40 处）
分档映射（法典 V1.2 7 档）：
- Display 32 / **Section 24** / **Title 18** / **Body-L 14** / Body 13 / Label 13 / Caption 12 / Eyebrow 10

具体操作：
- A 档（16 处）：`text-[16px] font-semibold tracking-tight text-zinc-800` h2/h3 模块标题 → `text-[18px] font-medium tracking-tight text-zinc-800`
- B 档（12 处）：无 `tracking-tight` 变种 h2/h3/h4 → 同上
- C 档（7 处）：弹窗区块标题 `text-base font-semibold text-foreground` → `text-[18px] font-medium text-foreground`
- D 档（2 处）：`text-[17px] sm:text-[18px]` → 统一 `text-[18px] font-medium`
- E 档（7 处）：`text-[15px]` h3 → Title；textarea/列表强调 → Body-L 14
- 数据大数（~14 处）：`text-[16px] font-semibold tabular-nums` → `text-[24px] font-semibold tabular-nums tracking-tight`（hit-analyzer / time-analysis / personnel-analysis / analytics-modal-panel / 视频结论卡-单卡）
- hit-analyzer 两处标题去 tabular-nums 恢复 Title：`{leadInsight?.title}` / "筛选文案样本"
- 空状态文案 → Body-L 14 / Title 调整 3 处

### P0-4 状态点合规（~12 处 / 8 文件）
核心：`dashboard/components/status-theme.ts`
- `STATUS_THEME` 非 Idle 档（PENDING/SUBMITTED/APPROVED/REJECTED/OVERDUE）dot 统一补 `ring-1 ring-white`
- IDLE 档保持 `bg-zinc-300` 无外圈（法典第十六章）
- `severityDot()` / `statusDot()` / `signalMap` 动态映射全部补 ring

直接位置：
- `hit-analyzer.tsx:966/970` `size-2.5` (10px) → `h-2 w-2` (8px) + `ring-1 ring-white`
- `global-matrix.tsx:36` 补 ring
- `leader-report.tsx:113` / `owner-report-list.tsx:110` / `capability-grid.tsx:82` 动态 dot 函数内补 ring
- `RewriteInput.tsx:121` / `RewriteWorkbench.tsx:114` 小状态点补 ring

### 附带
- `ui/badge.tsx` default variant `bg-zinc-800 text-white` → `bg-zinc-100 text-zinc-800`（根除徽章黑色滥用，继承阿禅之前未提交的改动）

---

## P0 验收（法典第二十章 8 条 grep，当前状态）

```bash
# [1] font-bold/extrabold/black
grep -rn "font-bold\|font-extrabold\|font-black" src/ --include="*.tsx"
# 当前：3 处（均为代码注释里字面量 "无 font-black"，合法保留）

# [2] text-zinc-900
grep -rn "text-zinc-900" src/ --include="*.tsx" | grep -v "/ui/"
# 当前：0 ✅

# [5] 黑色+shadow 塑料感
grep -rn "bg-zinc-800.*shadow\|bg-zinc-900.*shadow" src/ --include="*.tsx" | grep -v "/ui/"
# 当前：0 ✅

# [6] 刺眼深色 bg-{hue}-400~900
grep -rnE "bg-(rose|emerald|amber|sky|orange|red)-(400|500|600|700|800|900)" src/ --include="*.tsx" | grep -v "/ui/\|/demo/"
# 当前：5 处 ← P1 submission-calendar 处理

# [8] 黑色滥用 bg-zinc-800/900/950
grep -rn "bg-zinc-800\|bg-zinc-900\|bg-zinc-950" src/ --include="*.tsx" | grep -v "/ui/\|/demo/"
# 当前：40 处（大部分为头像 / 合法破格 / nav-bar，需人工甄别）
```

---

## P1 已完成内容（Commit `6bd4fe8`）

### P1-A submission-calendar 重灾专项（~20 处 / 1 文件）
文件：`src/components/submission/submission-calendar.tsx`

映射：
- submitted/waive → 绿 `bg-[#6FAA7D]/10 text-[#6FAA7D] border-[#6FAA7D]/30`；状态点 `bg-[#6FAA7D] ring-1 ring-white`
- leave → 橙 `bg-[#D99E55]/10 text-[#D99E55] border-[#D99E55]/30`
- missing/unsubmitted → 红 `bg-[#C9604D]/10 text-[#C9604D] border-[#C9604D]/30`
- future → `bg-zinc-50 text-zinc-400 border-zinc-200`
- Today：`ring-2` → `ring-1 ring-[V1色]/30`
- 毛玻璃 `bg-white/82` `border-white/70/75` → `bg-white border-zinc-200`
- `bg-slate-100/80-90` → `bg-zinc-50` / `bg-zinc-100`
- `text-[9px]` → `text-[10px]`

### P1-B hover:shadow-md 移除（6 处 / 5 文件）
全部删除（含 2 处笔误 `hover:shadow-md]"`）。

### P1-C border-t border-zinc-100 → 留白（8 处改 / 14 处里 6 处功能性保留）
**改为 `mt-6` 留白**（法典 7.2）：
- `personnel-analysis.tsx:353` / `视频结论卡-单卡.tsx:85/106/194/224` / `hit-analyzer.tsx:601` / `workflow-dashboard.tsx:216` / `leader-report.tsx:138`

**功能性保留**（表格行分隔 / 折叠面板 header-body 分隔 / 弹窗大 section 分隔）：
- `assistant-detail-sections.tsx:82` / `action-detail-drawer.tsx:267/286` / `analytics-sections.tsx:152` / `review-detail-modal.tsx:155` / `owner-report-list.tsx:122`

### P1-D tabular-nums 全量补 font-mono（152 处 / 50 文件）
法典 14.3「数字 / ID / 图表数值 **全站强制** `tabular-nums font-mono`」。
脚本批量补：所有 className 字串中只有 `tabular-nums` 无 `font-mono` 的，统一改为 `font-mono tabular-nums`。

### P1-E 注释里 font-black 字面量清理（3 处）
- `dashboard-workspace-header.tsx:31` / `leader-report.tsx:16` / `owner-report-list.tsx:29`
  删除注释里描述"无 font-black"的残留字面量，避免 grep 误报。

---

## P1 验收 grep（提交后状态）

```bash
hover:shadow-md 残留              0 ✅
border-t border-zinc-100 残留     6（功能性保留，已记录）
tabular-nums 无 font-mono 残留    0 ✅
font-bold/extrabold/black 残留    0 ✅
submission-calendar 彩色残留       0 ✅
bg-xxx-400~900 刺眼深色残留        0 ✅
```

---

## P2 已完成内容（第三批次）

### P2-A admin-secondary-nav 蓝色/渐变/毛玻璃
- `src/components/admin-secondary-nav.tsx`
- 卡片从蓝色渐变 / 蓝色投影 / 毛玻璃改为白底、zinc 边框、V1 主色 `#D97757` hover / active
- 图标容器从 `border-white/70 bg-white/85` 改为 `border-zinc-200 bg-zinc-50`
- 文案颜色从 CSS 变量改为 `text-zinc-800` / `text-zinc-500`

### P2-B 渐变背景清理（~5 处）
- `src/components/charts/chart-skeleton.tsx` 容器渐变改为 `bg-zinc-50`
- 骨架屏扫光 `bg-gradient-to-r from-transparent via-white/45 to-transparent` 保留，属加载态合法破格
- 本批次实际工作区里 `chat-panel` / `top-scripts-banner` 无未提交渐变 diff，按当前仓库残留验收

### P2-C 超小字号 text-[9px] → text-[10px]（~12 处）
- 全部 `text-[9px]` 改为 `text-[10px]`
- 涉及 action drawer、rewrite 配置/输出、nav 下拉、截图槽位卡、指标输入卡等

### P2-D `dark:` 模式死代码清理
- `src/components/pk/pk-selector.tsx` 清理 `dark:`、毛玻璃、白色透明边框

### P2-E 半透明色残留 `hover:bg-zinc-50/50` 等
- 批量将 `bg-zinc-100/70` 等半透明输入/选择器底色改为 `bg-zinc-50`
- 涉及 admin 邀请码、权限、团队、分组、豁免弹窗、dashboard 提交表单、register 表单等

### P2-F 其他 bg-zinc-800/900/950 逐个甄别（40 处残留）
- 头像（合法，保留）
- nav-bar 活跃态（需看法典判定）
- ai-insight:202 等合法破格锚点（保留）
- 其他滥用（改 V1 四色或浅灰）

### P2 验收 grep（提交前状态）

```bash
dark: 残留                         0 ✅
text-[9px] 残留                    0 ✅
hover:shadow-md 残留               0 ✅
bg-zinc-50/50 残留                 0 ✅
bg-zinc-100/70 残留                0 ✅
border-slate 残留                  0 ✅
bg-xxx-400~900 刺眼深色残留        0 ✅
gradient 容器残留                  0（仅 chart-skeleton 扫光合法保留）
```

---

## 禁区清单（整改过程勿碰）

1. **头像 `bg-zinc-800`**：nav-bar / chat-panel / 所有 avatar 组件
2. **合法破格锚点**：
   - `admin/analytics/ai-insight.tsx:202` 黑卡 + 内部 `text-[16px] text-white` 白字（210 行）
   - `admin-sidebar` 左侧 2px 橙条激活指示
3. **shadcn ui/ 基元**：`src/components/ui/*.tsx` 除 `badge.tsx`（阿禅手动改过）外不动
4. **Idle 档状态点**：`h-1.5 w-1.5 bg-zinc-200/300` 无 ring，保留
5. **demo 目录**：`src/app/demo/**` 离线示例，全部跳过
6. **字号档位严格对齐**：V1.2 7 档，不允许中间值（17/16/15/11）除破格区外
7. **不新增/修改 Skill 文件、hooks 外部文件**：只改 src/ 下的 .tsx / .ts 样式

---

## 验证命令（每批次 commit 前必跑）

```bash
# 1. Build 零错误
npm run build 2>&1 | grep -E "error|Error|Failed|Compiled"

# 2. 法典第二十章 8 条验收
bash docs/scripts/v1.2-lint.sh  # TODO: 可选创建

# 3. 对比 commit 前后差异
git diff --stat HEAD~1
```

---

## 上下文断点恢复指南

如果上下文被压缩/丢失，从这个文件恢复工作的步骤：
1. 读本文件（`docs/plans/2026-05-11-阿禅美学法典V1.2-合规整改进度.md`）
2. 读 Kimi 报告（`v1.2-issues-batch-1.md`）作为补充
3. 运行 `git log --oneline -5` 看已提交的批次
4. 运行法典第二十章 8 条 grep 看当前残留
5. 对照"P1/P2 待办清单"继续

---

**最后更新**：P3 批次完成后（commit `a4dff7e`）
**下一行动**：全站合规整改完成，可合并到 main
