# 5 · 最终回归验收

> 阶段目标：确认前四阶段修复在自动测试、本地真实链路、生产部署和真实角色上闭环。
> 当前状态：进行中，静态与受控本地浏览器回归通过；R-5A-01 已在本地修复但尚未发布，5A 仍暂不批准。

---

# 一、施工包

## 5A · 全站最终回归验收

- 目标：确认前四阶段修复在自动测试、本地真实链路、生产部署和真实角色上闭环。
- 包含问题：全部已修项。
- 不包含范围：不临场扩大修复范围；发现新问题只记录、定级和决定是否回退。
- 预计影响模块：全站核心页面、API、数据库、Vercel、真实角色账号。
- 依赖关系：前四阶段完成或明确 BLOCKED。
- 风险等级：高。
- 当前状态：进行中，已完成静态门禁、本地生产页面扫查、生产代表路由和三类真实角色只读验收；写入链路、归档角色和新增 migration 仍未形成完整证据。
- 开工前计划：按总清单第十节自动回归测试、E 区真实链路口径、F 区响应式视口口径分层验收。
- 完成标准：所有已修 P0/P1 有自动回归测试；生产 SHA/migration/真实角色证据齐全；未覆盖项全部列入风险。
- 当前进度：前四阶段的本地修复均已有代码/测试记录；本轮已完成 1410 项自动测试、TypeScript、Lint、Next build、本地生产页面扫查、受控本地浏览器首屏门禁、线上代表路由和老板/组长/组员只读矩阵。R-5A-01 已恢复 v3 实际依赖的历史读写路由，补齐技能响应适配、新建会话 ID 解析，并补上技能注入和技能列表的可见范围、已发布版本校验；本地生产构建和未登录路由 smoke 通过，尚未发布到生产。
- 下一步：发布 R-5A-01 后重新验收历史会话加载、撤销/重做、版本和保存；由有权限的发布流程确认 `20260903153000`、`20260903170000` 执行及 schema cache；补归档成员和必要写入链路验收后再决定是否批准 5A。

---

# 二、验收矩阵

| 证据层 | 记录要求 | 当前状态 |
| --- | --- | --- |
| 代码 diff | `HEAD=46a02a60`，与 `origin/main` 一致；工作区保留既有死代码清理等未提交改动，未由本轮撤销；本轮新增 rewrite 安全修复仍未提交。 | 已核对 |
| 自动测试 | `npm run gate:static` 退出码 0；1410/1410 测试、测试用户脚本 2/2、TypeScript、ESLint、Next build 79/79 通过。 | 通过 |
| 数据库/migration | 1E 的 `admin_first_screen_perf_events` RLS 已有用户确认；`20260903153000` 和 `20260903170000` 是否生产执行、是否刷新 schema cache 未独立确认。`supabase migration list` 因 `LegacyDbConfigConnectTempRoleError`（临时登录角色不存在）失败，不能据此判断已执行或未执行。 | 未验证 / BLOCKED |
| 本地真实页面/API | 本地生产服务已扫查 11 个页面、主要抽屉/弹窗和 Topics 详情；`gate:browser` 受控登录态四页 1/1 通过，rewrite 4 个历史 GET 路由未登录 smoke 统一进入 401 鉴权，未执行真实写入。 | 代码/本地浏览器/未登录 smoke 通过，写入未验收 |
| 生产部署 | Vercel 部署 `dpl_AM5BtzEQaeBcr5BkfgWXprjwWcX4` 状态 Ready，别名含 `https://dydata.cc`；生产 `/api/health?check=supabase` 返回 `status=ok`、`supabase=up`。当前生产对合法 UUID 的 4 个新路由仍返回 404，符合修复尚未提交/部署的边界。 | 服务通过，修复待发布复验 |
| 真实角色 | 老板、组长、组员已做生产只读矩阵；权限允许/拒绝符合现行角色文档。归档成员没有可用账号，未验收。 | 部分通过 |
| 未验证边界 | 归档成员登录/旧会话失效、审批/成员生命周期/选题写入/日报提交写入、2C RPC 生产执行、schema cache、rewrite 修复发布后的历史会话撤销/重做/保存；Chrome 调试器返回 `Debugger unattached`。 | 已列出 |

---

# 三、发布与回滚记录

| 日期 | 阶段/施工包 | commit SHA | Vercel 状态 | Supabase migration | 生产验证 | 回滚点 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-03 | 5A 大回归 | `46a02a60`（本地与 origin 一致） | `dpl_AM5BtzEQaeBcr5BkfgWXprjwWcX4` Ready，别名 `dydata.cc` | 1E 已由用户确认；`20260903153000`、`20260903170000` 未独立核实 | 线上代表路由及老板/组长/组员只读通过；rewrite 历史会话发现 4 个 404 | 暂不回退；先修复新发现断链并补数据库证据 |
| 2026-09-03 | R-5A-01 本地修复 | 未提交（工作区） | 未发布，生产仍为上一部署 | 未涉及 | 本地 build、路由挂载和未登录 401 smoke 通过；生产未复验 | 暂不回退；发布后复验历史会话 |

发布记录必须保留代码和数据库两条线：有 commit 不代表 migration 已执行，有 migration 文件不代表生产已应用，有 Vercel Ready 不代表真实角色验收通过。

---

# 四、阶段完成记录

```md
## YYYY-MM-DD HH:mm · 最终回归记录

- 执行者：
- 覆盖阶段：
- 覆盖施工包：
- 自动测试结果：
- 本地真实链路：
- 生产部署 SHA：
- Supabase migration 状态：
- 真实角色验收：
- 已确认完成：
- 未发布：
- 未验收：
- BLOCKED：
- 新发现问题：
- 是否需要回退前置阶段：
- 下一步：
```

## 2026-09-03 20:34 · 最终回归记录

- 执行者：Codex（承接前序 5A 浏览器验收记录并做独立静态/部署核对）
- 覆盖阶段：1A-1E、2A-2F、3A-3C、4A-4C 的回归证据收口；未把本轮变成临场修复批次
- 覆盖施工包：前四阶段全部；重点复核 P0/P1 数据安全、跨页读取、权限边界、响应式/浮层修复和死代码清理
- 自动测试结果：`npm run gate:static` 退出码 0；1407/1407 测试、测试用户脚本 2/2、`tsc`、`lint`、Next build 78/78 通过
- 本地真实链路：本地生产构建已扫查 11 个页面、主要抽屉/弹窗和 Topics 详情；未执行写入。前序 H2 生产复测已确认日报标题与 `videos.video_title` 同步，测试数据已还原
- 生产部署 SHA：本地 `HEAD=46a02a60`，`origin/main` 一致；Vercel 当前部署 Ready 并绑定 `dydata.cc`，但 CLI 未提供 Git source SHA，版本映射仍需独立确认
- Supabase migration 状态：1E 的 RLS migration 有用户确认；`20260903153000_fix_fulfillment_appeal_drop_group_refs.sql` 与 `20260903170000_admin_grant_exemption_rpc.sql` 的生产执行和 schema cache 均未独立确认。CLI 失败原因为临时登录角色连接错误，不是 migration 结论
- 真实角色验收：老板可进入核心页面；组长可进入管理核心页面，AI 配置/系统设置按权限拒绝；组员的 dashboard/growth/topics 可用，文案工具及管理页按权限拒绝，岗位管理仅能看自己的数据；归档成员未验收
- 已确认完成：静态门禁、生产服务健康、代表路由首屏、已覆盖角色的只读权限边界；前序 H2/M1/M2 记录已完成相应复测/单测收口
- 未发布：当前工作区仍有未提交的死代码清理和文档/测试改动；本轮未提交、未 push
- 未验收：所有真实写入链路、归档角色、两条新增 migration 的生产状态、schema cache、rewrite 历史会话完整功能
- BLOCKED：migration CLI 的数据库临时角色配置阻塞独立状态确认；Chrome 调试器本轮无法重新附着，未将本轮算作新的交互证据
- 新发现问题：`[P1 待修]` `src/components/content-tools/rewrite-v3/useRewriteV3Logic.ts` 仍请求 `/api/rewrite/conversations/:id/skills`、`/api/rewrite/documents/:id/{paragraphs,history,revisions}`，当前代码路由树不存在这些路径；生产使用合法 UUID 请求时均返回 404，前序真实角色验收已观察到 4 个 404 和控制台错误。影响历史会话技能、段落、撤销/重做和版本时光机加载，需单独修复后复测
- 性能告警（不阻断本次静态/只读结论）：线上前序记录中 `/growth` 榜单约 739ms、文案工具 bootstrap 约 1649ms、`/admin/content` 题库状态约 1210ms、协作成员卡约 1451ms、`/dashboard` 约 688ms；按现行规则记为预存性能问题，不伪装为通过
- 是否需要回退前置阶段：暂不回退。当前阻断是新增接口断链和验收证据缺口，不足以证明前四阶段需要整体回退
- 下一步：先处理 rewrite 断链；再按授权发布流程确认两条 migration、刷新 schema cache，补归档角色和必要写入链路，完成后重新判定 5A

### 新问题登记：R-5A-01

- 等级：P1，本地已修复，生产待发布复验
- 现象：历史文案会话初始化时请求 4 个当前不存在的 API 路径，生产返回 404，并产生控制台错误。
- 证据：源码调用位于 `src/components/content-tools/rewrite-v3/useRewriteV3Logic.ts`；当前路由树仅有 rewrite conversations、generate、skills 和 content-tools rewrite bootstrap/messages；对合法 UUID 访问上述 4 路径均返回 404。
- 影响：历史会话的激活技能、画布段落、撤销/重做状态、版本时光机无法加载；新会话主页面是否受影响需按真实会话继续确认。
- 本地处理：恢复 v3 实际仍调用的 conversation skills、document paragraphs/history/revisions、paragraph user-edit 和 skill management handlers；新增 `rewrite-api-contract.ts` 适配嵌套技能响应，并从现行 `{ data: { conversationId } }` 契约读取新会话 ID。
- 本地证据：rewrite 契约测试通过；相关 9 个定向测试通过；`npm run gate:static` 通过（1407/1407、测试用户脚本 2/2、tsc、lint、Next build 79/79）；本地生产 4 个 GET 路由未登录请求均返回 401 而非 404。
- 生产边界：修复尚未提交或部署，Chrome 调试器本轮未连接，生产历史会话加载、撤销/重做、版本和保存仍未验收；发布后必须重新做完整浏览器/Network/Console 验收。

## 2026-09-03 21:26 · R-5A-01 修复回归记录

- 执行者：Codex
- 覆盖阶段：5A 阻断项 R-5A-01
- 覆盖施工包：rewrite v3 历史会话加载、历史操作和技能管理的 API 契约修复
- 自动测试结果：rewrite 定向测试 9/9；`npm run gate:static` 退出码 0，1407/1407 测试、测试用户脚本 2/2、TypeScript、ESLint、Next build 79/79 通过
- 本地真实链路：生产构建列出 7 个 v3 仍依赖 handler 模块；4 个历史 GET 路由未登录请求均返回 401，未执行写入
- 代码修复：恢复 7 个仍被 v3 调用的 route handler；新增技能响应扁平化适配；修正新建会话从 `data.conversationId` 读取 ID；技能管理使用 `manage_system` 权限或私有技能本人权限
- 生产部署 SHA：未提交、未部署；生产仍是旧部署，不能宣称线上问题已消失
- Supabase migration 状态：未涉及，本轮仍无法独立核实 `20260903153000`、`20260903170000` 与 schema cache
- 真实角色验收：未新增；Chrome 调试器未连接
- 已确认完成：本地代码、定向测试、静态门禁、生产构建和未登录路由挂载 smoke
- 未发布：本次所有 rewrite 代码与测试改动
- 未验收：生产历史会话加载、撤销/重做、版本时光机、保存、归档成员和真实写入链路
- BLOCKED：生产发布及登录态浏览器复验需要后续发布/调试器条件；migration 状态仍受 Supabase CLI 临时登录角色错误阻塞
- 新发现问题：未新增
- 是否需要回退前置阶段：不需要
- 下一步：发布后用登录态历史会话验证 4 个 GET 返回 2xx、无控制台错误，再验证撤销/重做、版本和保存；随后继续 migration、归档成员和写入链路验收

## 2026-09-03 21:50 · 5A 大回归继续核验

- 执行者：Codex
- 覆盖阶段：R-5A-01 安全边界补强、静态门禁、受控本地浏览器门禁、生产/本地路由 smoke
- 代码审查：确认技能注入使用 service role，已在写入前校验当前用户可使用的启用技能；指定版本必须属于该技能且 `published_at` 非空。其他用户私有技能返回 403，错配/未发布版本返回 400；补充 2 条 RED→GREEN 回归测试。
- 代码审查追加：发现技能列表显式传入 `scope=private` 时原查询缺少当前用户归属条件，存在私有 skill 列表泄露；统一补上公开范围或当前用户 owner 条件，并增加结果级回归测试。
- 自动测试结果：本轮定向命令 9/9（rewrite 契约 3/3、技能创建/注入/列表 6/6）；`npm run gate:static` 退出码 0，1410/1410 测试、测试用户脚本 2/2、TypeScript、ESLint、Next build 79/79 通过；Lint 仍为 0 errors、42 个既有 warning。
- 受控本地浏览器：第二次 `npm run gate:browser` 通过，四个代表页 `/topics`、`/dashboard`、`/admin/content`、`/admin/collaboration` 1/1；本地生产环境无硬失败和控制台 error。`/dashboard` 最慢接口 663ms，超过 600ms 预算，仅作为 timing warning，未启用强制耗时失败。
- 生产/本地 API smoke：生产 `/api/health?check=supabase` 返回 200、`status=ok`、`supabase=up`；生产四个新 rewrite 路由对合法 UUID 仍返回 404，本地对应返回 401，证明代码修复尚未发布且本地路由已挂载。
- 浏览器边界：Chrome 已有 DYData 标签，但调试器返回 `Debugger unattached`，未取得生产登录态 DOM/Network/Console 交互证据；未用无登录新会话替代真实角色验收。
- 数据库/角色/写入：本轮未执行数据库写入、migration、schema cache 操作或提交推送；两条新增 migration、归档成员、生产真实写入链路和生产 rewrite 历史会话仍未验收/仍受既有阻断约束。
- 结论：R-5A-01 本地代码和门禁通过，生产待提交、部署和登录态历史会话复验；5A 继续暂不批准，不需要回退前四阶段。
