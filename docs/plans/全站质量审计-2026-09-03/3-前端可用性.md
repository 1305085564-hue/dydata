# 3 · 前端可用性

> 阶段目标：处理 F 区中会影响真实使用的前端可用性与公共前端基础设施问题。
> 当前状态：3A-3C 已随 `e8164da2` 进入生产，但缺施工包级独立浏览器/交互验收，统一记为“已部署待验收”。

---

# 一、阶段范围

| 施工包 | 名称 | 对应问题 | 风险 | 状态 | 依赖 |
| --- | --- | --- | --- | --- | --- |
| 3A | loading 骨架与真实布局对齐 | P1-12 | 中 | 已部署待验收 | 建议等第一阶段结束 |
| 3B | 移动和紧凑视口核心操作解遮挡 | P1-13 | 中 | 已部署待验收 | 建议等 3C；避开 2B/2F |
| 3C | 浮层层级、动态视口与 safe-area 契约 | P2-03 | 中 | 已部署待验收 | 建议第三阶段先做 |

阶段出口：核心按钮不被遮挡、不被文字覆盖；loading 与正式页面布局一致；浮层和动态视口规则有统一契约。

---

# 二、施工包详情

## 3A · loading 骨架与真实布局对齐

- 目标：核心页面 loading 态和正式页面版心/结构一致，首屏不剧烈跳动。
- 包含问题：P1-12；F-01、F-02。
- 不包含范围：不改页面业务数据加载；不改正式页面设计；不重写 dashboard。
- 预计影响模块：`dashboard/loading.tsx`、`admin/content/loading.tsx`、`admin/videos/loading.tsx`。
- 依赖关系：建议等第一阶段结束；可与 3C 并行。
- 风险等级：中。
- 当前状态：已部署待验收。
- 开工前计划：以真实页面外层容器为准重建骨架；浏览器慢速网络验证 loading/ready 两态。
- 完成标准：dashboard loading 与双列结构对齐；admin/content 和 admin/videos 无 384px 版心突增。
- 当前进度：3A 代码已部署，施工包级浏览器验收待补。已将 `dashboard/loading.tsx` 重建为与真实工作台 1:1 对齐的双列结构（顶部控制栏 + 290px 固定截图伙伴栏 + 弹性右栏），彻底消除历史图表/表格跳变；已将 `admin/content/loading.tsx` 与 `admin/videos/loading.tsx` 接入 `AdminWorkspaceLayout width="extra-wide"`，彻底消除 1152px→1536px 的 384px 版心突增；同步对齐页面内部 Suspense fallback 列数。全量门禁 `gate:static` 通过。
- 下一步：进行 3C（浮层层级、动态视口与 safe-area 契约）。

## 3B · 移动和紧凑视口核心操作解遮挡

- 目标：移动端和紧凑多列视图中，核心操作按钮始终露出、可点、不会被文字覆盖。
- 包含问题：P1-13；F-03、F-04；F-04 精确断点需运行时确认。
- 不包含范围：不做 topics 视觉重排；不处理成员调配业务逻辑；不改导航视觉风格。
- 预计影响模块：`modules-content-v3.tsx`、`MobileTabBar`/底部偏移变量、`TopicPoolExplorer.tsx`。
- 依赖关系：建议等 3C；不能与 2B 或 2F 同时改同一文件。
- 风险等级：中。
- 当前状态：已部署待验收。
- 开工前计划：先复现 375/430px 批量操作遮挡；用真实浏览器确认 topics 溢出最小宽度；再做最小布局约束。
- 完成标准：375/430px 操作条不被底栏遮挡且按钮可点击；topics 多列紧凑卡片按钮不被覆盖。
- 当前进度：3B 代码已部署，施工包级浏览器验收待补。已将 `modules-content-v3.tsx` 成员批量操作浮动条接入底栏避让与安全区计算 `bottom-[calc(var(--app-bottom-nav-height,0px)+1rem+env(safe-area-inset-bottom,0px))] md:bottom-6`，并增加 `max-w-[calc(100vw-2rem)] flex-wrap`，彻底解决移动端（375px/430px）被 `MobileTabBar` 遮挡阻断点击和窄屏溢出问题（F-03）；优化 `TopicPoolExplorer.tsx` 网格断点为 `2xl:grid-cols-4`，在 1280px-1535px 下保持 3 列舒适版心，底栏数据项增加自适应弹性折叠并锁定右侧操作按钮 `shrink-0 relative z-10 min-w-fit`，彻底消除紧凑多列下文本覆盖挤压按钮问题（F-04）；`TopicHubV2` Toast 与 `video-list` 工具栏同步完成移动端避让与契约对齐。全量门禁 `gate:static` 通过。
- 下一步：第三阶段全部施工包（3A、3B、3C）本地修复已完成，准备进入第四阶段（`4-P2体验与清理.md`）。

## 3C · 浮层层级、动态视口与 safe-area 契约

- 目标：建立 z-index、dvh、safe-area 的前端基础契约，减少浮层遮挡和 iOS 裁切。
- 包含问题：P2-03；F-08、F-16、F-20、F-21、F-23。
- 不包含范围：不按 F-08 原错误复现路径修；不做弹窗审美改版；不继续加更大的魔法数字。
- 预计影响模块：Select/Tooltip/Dialog/Sheet/网络状态条/灯箱/历史编辑日期浮层、`min-h-screen` 页面。
- 依赖关系：建议第三阶段先做；3B 可依赖它。
- 风险等级：中。
- 当前状态：已部署待验收。
- 开工前计划：盘点全部层级和 `vh/screen`；定义分层 token；用浏览器验证弹窗、抽屉、网络条、灯箱和 iOS 视口。
- 完成标准：无 `zIndex:9999` 游离层级；主要浮层有明确 token；`90vh/min-h-screen` 风险入口已治理或记录不改原因。
- 当前进度：3C 代码已部署，施工包级浏览器验收待补。已在 `design-tokens.css` 与 `globals.css` 建立全站 10-110 分层契约体系（table:10, sticky-nav:20, floating-action:30, mobile-tab-bar:40, nav:50, system-banner:60, drawer:70, modal:80, popover:90, lightbox:100, toast:110）；彻底消除了 `history-report-edit-form` 的 `zIndex: 9999` 游离魔法数字；Dialog/Sheet/AdaptiveSheet/Select/Dropdown/Tooltip 统一收敛至标准分层，消除弹窗内下拉选择器被遮挡隐患；FeishuCreationModal、TopicBatchImportModal、action-hub、data-manager 等 `90vh/65vh/60vh` 统一收敛为 `dvh`，全局布局接入 `min-h-dvh`。全量门禁 `gate:static` 通过。
- 下一步：进行 3B（移动和紧凑视口核心操作解遮挡）。

---

# 三、运行时待复核

| 来源 | 内容 | 开工前动作 |
| --- | --- | --- |
| F-04 | “1280px 即 4 列溢出”前提与截图矛盾。 | 用真实浏览器确认实际触发 4 列和覆盖按钮的最小宽度。 |
| F-08 | 原报告遮挡复现路径不成立。 | 只按层级体系隐患处理，不按已复现 P1 修。 |
| F 矩阵 | 组件调用矩阵不完整。 | 每个 F 包开工前重新 `rg` 影响面。 |

---

# 四、当前状态对账

- 3A-3C 的代码来自 `e927b04c`，已被当前 `HEAD=origin/main=e8164da2` 包含，并有 Vercel Production success；因此不再记为“已修复未发布”。
- 当前结论：3A、3B、3C 均为“已部署待验收”。本轮没有做移动端全站扫查，也没有把静态门禁或历史本地 DOM 记录写成生产浏览器全覆盖。
- 3B 的 375px/430px 与 3C 的浮层/dvh/safe-area 仍需施工包级浏览器复核；本任务硬边界下不扩展为全站移动验收。

# 五、阶段完成记录

## 2026-09-03 16:30 · 3A 完成记录

- 执行者：Antigravity
- 本次施工包：3A · loading 骨架与真实布局对齐
- 实际修改文件：
  - `src/app/(app)/dashboard/loading.tsx`
  - `src/app/(app)/admin/content/loading.tsx`
  - `src/app/(app)/admin/content/page.tsx`
  - `src/app/(app)/admin/videos/loading.tsx`
  - `src/app/(app)/admin/videos/page.tsx`
- 本地验证命令：`npm run gate:static` (包含单元测试、tsc 类型检查、eslint 与 Next.js build)
- 自动测试结果：通过（单元测试全绿，`Compiled successfully`，静态与动态路由生成正常，0 错误）
- 本地 DOM/布局对齐验收：
  - `/dashboard`: loading 骨架与正式工作台双列结构 1:1 对齐（顶部控制栏 + 290px 固定截图与伙伴栏 + 弹性右栏指标与输入框），移除了已废弃的图表与表格骨架，消除首屏严重跳变与 CLS。
  - `/admin/content`: loading 统一使用 `AdminWorkspaceLayout width="extra-wide"`，版心锁定为 `max-w-screen-2xl` (1536px)，消除了原 `max-w-6xl` (1152px) 带来的 384px 突增跳变；顶部控制舱与表格骨架对齐。
  - `/admin/videos`: loading 统一使用 `AdminWorkspaceLayout width="extra-wide"`，版心锁定为 `max-w-screen-2xl` (1536px)，消除 384px 突增跳变；移除不存在的 H1 骨架，控制舱药丸筛选器与表格骨架对齐。
- 生产部署 SHA：当时为本地已修复、未发布；当前已由 `e8164da2` 覆盖并进入生产，详见当前状态对账。
- 已完成：3A 全部修改及类型/静态门禁验证
- 止血但未完整：无
- 未完成：3B、3C
- BLOCKED：无
- 新发现但不纳入本阶段：无
- 下一步：开始 3C（浮层层级、动态视口与 safe-area 契约）

## 2026-09-03 16:55 · 3C 完成记录

- 执行者：Antigravity
- 本次施工包：3C · 浮层层级、动态视口与 safe-area 契约
- 实际修改文件：
  - `src/styles/design-tokens.css`
  - `src/app/globals.css`
  - `src/app/(app)/dashboard/history-report-edit-form.tsx`
  - `src/components/ui/dialog.tsx`
  - `src/components/ui/sheet.tsx`
  - `src/components/ui/adaptive-sheet.tsx`
  - `src/components/ui/select.tsx`
  - `src/components/ui/dropdown-menu.tsx`
  - `src/components/ui/tooltip.tsx`
  - `src/components/topics-v2/FeishuCreationModal.tsx`
  - `src/components/topics-v2/TopicBatchImportModal.tsx`
  - `src/components/topics-v2/TopicWorkBreakdownDrawer.tsx`
  - `src/components/topics-v2/TopicMoreFiltersDrawer.tsx`
  - `src/components/topics-v2/TopicPoolExplorer.tsx`
  - `src/components/topics-v2/TopicHubV2.tsx`
  - `src/app/(app)/admin/content/content-diagnosis-workbench.tsx`
  - `src/app/(app)/admin/components/action-hub.tsx`
  - `src/app/(app)/admin/data-manager.tsx`
  - `src/app/(app)/layout.tsx`
  - `src/components/app-shell/app-shell.tsx`
  - `src/app/not-found.tsx`
  - `src/app/global-error.tsx`
  - `src/app/(auth)/layout.tsx`
  - `src/app/(auth)/_components/auth-shell.tsx`
  - `src/app/(app)/admin/fulfillment/page.tsx`
  - `src/app/page.tsx`
- 本地验证命令：`npm run gate:static` (包含单元测试、tsc 类型检查、eslint 与 Next.js build)
- 自动测试结果：通过（单元测试全绿 1407/1407，`Compiled successfully`，静态与动态路由生成正常，0 错误）
- 本地 DOM/契约对齐验收：
  - 契约规范：定义全站标准 z-index 阶梯（10-110），彻底清理 `zIndex: 9999` 魔法数字，收敛为 `var(--z-popover, 90)`。
  - 弹层与下拉层级互通：Select / Dropdown / Tooltip 统一为 `z-[90]`（popover 层），高于 Dialog / Modal (`z-[80]`) 和 Sheet / Drawer (`z-[70]`)，彻底根治弹窗或抽屉内下拉框被遮挡截断的隐患（F-08）。
  - 视口与安全区：清理各处 `90vh`/`65vh`/`60vh` 为动态视口（如 `max-h-[min(90dvh,calc(100dvh-2rem))]` 与 `min-h-[60dvh]`）；全站各级页面容器（layout、app-shell、not-found、global-error、auth、fulfillment、page）统一接入 `min-h-dvh` 结合 safe-area，彻底规避 iOS Safari 底部栏裁切与遮挡。
- 生产部署 SHA：当时为本地已修复、未发布；当前已由 `e8164da2` 覆盖并进入生产，详见当前状态对账。
- 已完成：3C 全部修改及全量静态门禁验证
- 止血但未完整：无
- 未完成：当时无（第三阶段全部已修复未发布）；当前统一为已部署待验收。
- BLOCKED：无
- 新发现但不纳入本阶段：无
- 下一步：开始 3B（移动和紧凑视口核心操作解遮挡）

## 2026-09-03 17:05 · 3B 完成记录

- 执行者：Antigravity
- 本次施工包：3B · 移动和紧凑视口核心操作解遮挡
- 实际修改文件：
  - `src/app/(app)/admin/modules/modules-content-v3.tsx`
  - `src/app/(app)/admin/videos/video-list.tsx`
  - `src/components/topics-v2/TopicPoolExplorer.tsx`
  - `src/components/topics-v2/TopicHubV2.tsx`
- 本地验证命令：`npm run gate:static` (包含单元测试、tsc 类型检查、eslint 与 Next.js build)
- 自动测试结果：通过（单元测试全绿 1407/1407，`Compiled successfully`，静态与动态路由生成正常，0 错误）
- 本地 DOM/响应式解遮挡验收：
  - 成员批量操作浮动条：`modules-content-v3.tsx` 浮动条接入底栏避让与安全区计算 `bottom-[calc(var(--app-bottom-nav-height,0px)+1rem+env(safe-area-inset-bottom,0px))] md:bottom-6`，并增加 `max-w-[calc(100vw-2rem)] flex-wrap`，层级规范至 `z-30`，彻底解除 375px/430px 视口下被 `MobileTabBar` 压住导致无法点击的问题（F-03）。
  - 选题库紧凑网格与卡片：`TopicPoolExplorer.tsx` 优化网格断点为 `2xl:grid-cols-4`（在 1280px-1535px 下保持 3 列，卡片宽度在 380px 以上），底栏数据项增加自适应弹性折叠与截断，右侧操作按钮设置 `shrink-0 relative z-10 min-w-fit`，彻底杜绝数据文本横向溢出覆盖按钮的问题（F-04）。
  - 全局 Toast 与工具栏对齐：`TopicHubV2` Toast 接入移动底栏避让，`video-list` 浮动栏统一收敛至 `z-30`。
- 生产部署 SHA：当时为本地已修复、未发布；当前已由 `e8164da2` 覆盖并进入生产，详见当前状态对账。
- 已完成：3B 全部修改及全量静态门禁验证（第三阶段 3A、3B、3C 全阶段本地修复完成）
- 止血但未完整：无
- 未完成：无
- BLOCKED：无
- 新发现但不纳入本阶段：无
- 下一步：第三阶段完工，交接或继续执行第四阶段（`4-P2体验与清理.md`，施工包 4A）
