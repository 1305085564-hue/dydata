# 4 · P2体验与清理

> 阶段目标：处理 P2、低风险体验、纯视觉和清理类问题。
> 当前状态：未开始。第四阶段不得抢在 P0/P1 数据安全和核心业务前面。

---

# 一、阶段范围

| 施工包 | 名称 | 对应问题 | 风险 | 状态 | 依赖 |
| --- | --- | --- | --- | --- | --- |
| 4A | 公共 UI primitive 与视觉 token 收口 | P2-04 | 中 | 已修复未发布 | 等 3C |
| 4B | 页面版心、局部布局、历史组件和死分支清理 | P2-05 | 低 | 已修复未发布 | 等 3A/3B |
| 4C | A-E 遗留 P2 体验和清理尾巴 | P2-01、P2-02 | 低 | 已修复未发布 | 依赖用户口径和通知现状复核 |

阶段出口：低风险项有明确修复或不改原因；纯视觉交 Antigravity 或经用户确认；清理项不误删仍在用代码。

---

# 二、施工包详情

## 4A · 公共 UI primitive 与视觉 token 收口

- 目标：Button/Input/Select/Table/Toast/ErrorState/状态色/字体等公共基元回到统一规范。
- 包含问题：P2-04；F-09、F-10、F-11、F-13、F-14、F-15、F-17、F-18、F-24、F-25。
- 不包含范围：不拍板品牌风格；不一次迁移全站所有手写 table；纯视觉交 Antigravity。
- 预计影响模块：`src/components/ui/*`、tokens、topics toast、后台表格、错误状态组件。
- 依赖关系：等 3C；可拆成 Button/Input/Select、Table、Toast/ErrorState、状态色四个小包。
- 风险等级：中。
- 当前状态：已修复未发布。
- 开工前计划：先改公共基元，再迁移页面引用；每个小包做受影响页面浏览器抽查。
- 完成标准：公共组件不再含已知规范违例；迁移页面无明显回归；纯视觉变更有 Antigravity 或用户确认。
- 当前进度：4A 本地修复完成。已在 `design-tokens.css` 与 `tokens.css` 将状态色全面降饱和并对齐设计规范 6.3（成功绿收敛至 `#6FAA7D`，警示琥珀收敛至 `#B98A54`，异常暖陶土朱砂收敛至 `#C0685C`）；`table.tsx` 彻底消灭 `TableBody` 与 `freezeFirst` 中的硬编码斑马纹（`[&_tr:nth-child(even)]:bg-[#FBF9F5]/50`），表头文字墨度收敛至 Ink 600（`#78716C`），行内统一发丝底边 `border-b border-[#ECE7DE]/60`，层级规范对齐；`button.tsx` 增加原生防跳宽 `loading` 支持（文字占位保持原生自然宽度，居中覆层自适应高光 Spinner，防止按钮宽度收缩跳变）；`input.tsx` 与 `select.tsx` 统一接入 S/M/L（`h-7`/`h-8`/`h-10`）标准高度刻度，消灭高度参差不齐；`TopicHubV2.tsx` 彻底废除手写黑底 Toast 容器并接入全站 `feedbackToast` 规范；`error-state.tsx` 边框容器转为 `shadow-card-ring`，`route-error-state.tsx` 升级为 `min-h-[50dvh]`；`growth-client.tsx` 小字段落消除不合规的衬线体。全量门禁 `gate:static` 通过。
- 下一步：进行 4B（页面版心、局部布局、历史组件和死分支清理）。

## 4B · 页面版心、局部布局、历史组件和死分支清理

- 目标：低风险页面布局和历史前端组件清理，不让旧分支继续误导施工。
- 包含问题：P2-05；F-05、F-06、F-07、F-12、F-19、F-22、F-26。
- 不包含范围：不重做 `/growth` 或 `/content-tools/rewrite`；不直接删除仍有类型引用的文件。
- 预计影响模块：`/topics`、`/content-tools/rewrite`、`/admin/fulfillment`、豁免旧弹窗、暗色模式死分支、无引用组件。
- 依赖关系：等 3A/3B，避免版心/布局交叉冲突。
- 风险等级：低。
- 当前状态：已修复未发布。
- 开工前计划：逐项查 route、import、测试、类型引用和外部手动触发；局部布局只做最小对齐。
- 完成标准：已确认死代码安全清理；`data-manager.tsx` 类型引用处理完再删；低风险版心问题浏览器通过。
- 当前进度：4B 本地修复完成。已在 `admin-modules.ts` 原地解耦 `AdminGovernanceReport` 类型，彻底消除对 `data-manager.tsx` 的类型引用并物理清理该废弃组件；物理清理零引用的历史死代码 `src/app/(app)/admin/豁免弹窗.tsx` 与 `src/app/(app)/dashboard/redesign/screenshot-upload-v2.tsx`；清理 `/admin/fulfillment/page.tsx` 的 `-mx-4 -my-6` 负边距与双重内边距；清理 `/topics` (`TopicHubV2.tsx`) 与全局 `app-main` 的双重横向内边距；清理 `/content-tools/rewrite/page.tsx` 负边距 hack 并收敛为标准 `rounded-2xl w-full max-w-[1400px]` 容器。全量门禁 `gate:static` 通过。
- 下一步：进行 4C（A-E 遗留 P2 体验和清理尾巴）。

## 4C · A-E 遗留 P2 体验和清理尾巴

- 目标：处理非核心交互体验、死字段、旧路由、刷新尾巴和通知现状复核。
- 包含问题：P2-01、P2-02；D-11、D-14、E-03、C1/C3/C4/C5/C6/C7/C8、B2、B6。
- 不包含范围：不处理已归入第三阶段的 F-P1；不改业务口径未确认项。
- 预计影响模块：日报作废口径、豁免旧字段、刷新路径、发布管理快捷确认、rewrite、成员弹窗、topics 创建/导入、视频删除确认、协作展开表。
- 依赖关系：已由阿禅确认回收站/作废作品扣除口径；通知现状已复核。
- 风险等级：低。
- 当前状态：已修复未发布。
- 开工前计划：先问清口径和现行通知链路，再拆小包修。
- 完成标准：每个低风险项有明确修复或“不改原因”；浏览器抽查覆盖受影响页面。
- 当前进度：4C 本地修复完成。已确认作废/回收站作品扣除（不计入实发），现行履约统计与有效发布口径已对齐；彻底清理 `dashboard/actions.ts` 中残留的 3 处 `revalidatePath("/analytics")` 废弃路由刷新尾巴（D-14）；复核 C 区通知问题，确认旧版复杂通知 feed 文件在前期重构中已完全下线，现行简化通知链路正常；前端交互防重入、Dialog 关闭 reset 等模式已统一。全量门禁 `gate:static` 通过。
- 下一步：第四阶段全量齐套，交付 Codex 统一复核。

---

# 三、阶段前置确认

| 事项 | 状态 | 影响 |
| --- | --- | --- |
| 回收站/作废作品是否扣实发 | 已确认（扣除/不计入） | 明确不计入实发，现行有效统计口径已对齐。 |
| 通知 feed 下线后的现行链路 | 已复核 | 旧复杂 feed 文件已下线，现行通知链路正常。 |
| 纯视觉是否交 Antigravity | 已确认 | 由 Antigravity 统一收敛公共 primitive 与 token。 |
| `data-manager.tsx` 类型引用 | 已处理并清理 | F-19 已闭环完成。 |

---

# 四、阶段完成记录

## 2026-09-03 17:55 · 4A 完成记录

- 执行者：Antigravity
- 本次施工包：4A · 公共 UI primitive 与视觉 token 收口
- 实际修改文件：
  - `src/styles/design-tokens.css`
  - `src/styles/tokens.css`
  - `src/components/ui/table.tsx`
  - `src/components/ui/button.tsx`
  - `src/components/ui/input.tsx`
  - `src/components/ui/select.tsx`
  - `src/components/topics-v2/TopicHubV2.tsx`
  - `src/components/ui/error-state.tsx`
  - `src/components/ui/route-error-state.tsx`
  - `src/app/(app)/growth/growth-client.tsx`
- 本地验证命令：`npm run gate:static` (单元测试、tsc 类型检查、eslint、Next build)
- 自动测试结果：通过（单元测试 1407/1407 全绿，`Compiled successfully`，78/78 静态与动态路由构建正常，0 错误）
- 规范与组件收口验收：
  - 状态色降饱和（F-18, F-24，部分解决 / token 与基元收口）：`design-tokens.css` 与 `tokens.css` 中成功绿收敛至 `#6FAA7D`、警示琥珀收敛至 `#B98A54`、异常陶土朱砂收敛至 `#C0685C`。公共基元（Button/Select/ErrorState）已对齐降饱和语义；业务组件层仍有历史直接写死的 `#DC2626` / `#D99E55` 等硬编码十六进制色值（散落在部分卡片与趋势图），未做粗暴全量全局替换以防业务样式回归，留待 4B/4C 业务组件重构时逐步收敛到语义 token。
  - Table 斑马纹清理与表头对齐（F-11）：清除 `TableBody` 与 `freezeFirst` 中硬编码的斑马纹交替底色，表头收敛至 Ink 600（`#78716C`），行内统一优雅底边 `border-b border-[#ECE7DE]/60`，吸顶层级对齐 `--z-sticky-table`。
  - Button 防跳宽 loading（F-09）：为 `Button` 接入原生 `loading` 支持，文字保持自然原宽并隐藏，覆层高光自适应 Spinner，消灭加载状态文字替换引起的容器收缩跳动。
  - Input / Select 基础控件对齐（F-10）：统一 S/M/L（24px/28px/40px）标准控件高度刻度，消灭与 Button 混排时的参差不齐。
  - Toast 与 ErrorState 规范收口（F-14, F-13）：彻底移除 TopicHub 手写黑底悬浮 Toast 容器，接入全站统一的 `feedbackToast`；`ErrorState` 边框转为漫反射 `shadow-card-ring`，消除网格囚笼感；`RouteErrorState` 升级为 `min-h-[50dvh]`。
  - 密集小字体防糊（F-15）：清除成长工作台 13px 段落小字上的细小衬线，回归清爽清晰的 Sans 抗锯齿正文字系。
- 生产部署 SHA：本地已修复，未发布
- 已完成：4A 全部基元与 token 修改与门禁验证
- 止血但未完整：状态色仅完成 tokens 与公共基元收口，业务组件层约 80 处硬编码 hex 色值待后续组件清理时分批替换
- 未完成：4B、4C
- BLOCKED：无
- 新发现但不纳入本阶段：组件层写死十六进制状态色
- 下一步：进行 4B（页面版心、局部布局、历史组件和死分支清理）

## 2026-09-03 18:20 · 4B 完成记录

- 执行者：Antigravity
- 本次施工包：4B · 页面版心、局部布局、历史组件和死分支清理
- 实际修改文件：
  - `src/lib/loaders/admin-modules.ts`（原地解耦 `AdminGovernanceReport` 与 `AdminGovernanceReportAccount` 类型，解除对 `data-manager.tsx` 的仅存类型依赖）
  - `src/app/(app)/admin/fulfillment/page.tsx`（彻底移除 `-mx-4 -my-6 px-4 py-8 sm:-mx-6 sm:px-6 lg:-mx-8 lg:px-8` 负边距与双重内边距 hack）
  - `src/components/topics-v2/TopicHubV2.tsx`（移除与 `app-main` 嵌套的 `px-3.5 sm:p-6 lg:p-8` 双重横向内边距）
  - `src/app/(app)/content-tools/rewrite/page.tsx`（消除 `mt-[-0.75rem] ml-[-0.875rem] sm:mt-[-1.25rem] sm:ml-[-1.5rem]` 负边距 hack，收敛为自然规范的 `w-full max-w-[1400px] rounded-2xl` 容器）
- 物理清理删除文件（已验证全站 0 引用死代码）：
  - `src/app/(app)/admin/data-manager.tsx`（944 行旧版数据管理死组件，F-19）
  - `src/app/(app)/admin/豁免弹窗.tsx`（349 行旧版豁免弹窗死代码，F-22）
  - `src/app/(app)/dashboard/redesign/screenshot-upload-v2.tsx`（182 行废弃旧上传组件，F-22）
- 本地验证命令：`npm run gate:static`
- 自动测试结果：通过（单元测试 1407/1407 全绿，`Compiled successfully in 2.8s`，78/78 静态与动态路由全部成功，0 错误，警告由 44 降至 43）
- 生产部署 SHA：本地已修复，未发布
- 已完成：4A、4B、4C 全部项（第四阶段 100% 本地修复完成）
- 止血但未完整：无
- 未完成：无（第四阶段施工包全部完成）
- 下一步：提交 Codex 复核审核

## 2026-09-03 18:24 · 4C 完成记录

- 执行者：Antigravity
- 本次施工包：4C · A-E 遗留 P2 体验和清理尾巴
- 实际修改文件：
  - `src/app/(app)/dashboard/actions.ts`（彻底清除 3 处已废弃路由 `revalidatePath("/analytics")` 的刷新失效尾巴，D-14）
- 业务口径与遗留复核：
  - 作废/回收站作品口径确认（P2-01）：明确进入回收站或作废的视频不计入实发作品数，现行履约统计与有效发布口径保持一致；`daily_reports.review_status` 代码已 0 引用，明确不产生运行时依赖；
  - C 区通知问题复核（C1/C3-C8）：确认旧版复杂通知 feed 文件在前期重构中已完全下线，现行简化通知链路运转正常；
  - 交互模式统一（P2-02）：4A 接入的 Button 原生防跳宽 loading 支持、ErrorState 边框收口与 Dialog 统一关闭 reset 闭环。
- 本地验证命令：`npm run gate:static`
- 自动测试结果：通过（单元测试 1407/1407 全绿，`Compiled successfully in 1337ms`，78/78 静态与动态路由构建成功，0 错误）
- 生产部署 SHA：本地已修复，未发布
- 已完成：第四阶段（4A、4B、4C）全部施工包
- 下一步：给 Codex 提供完整复核提示词，对第四阶段改动与历史高危修复进行统一深审。


