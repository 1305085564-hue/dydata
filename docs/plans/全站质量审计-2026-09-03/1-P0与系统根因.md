# 1 · P0与系统根因

> 阶段目标：先处理 P0 和会影响后续验收正确性的系统级根因。
> 当前状态：1B、1C、1D 已部署并完成当前范围验收；1A、1E 已部署待施工包级独立验收。1C/1D 的生产补验不代表全站低频读取和日期链路均已覆盖。

---

# 一、阶段范围

| 施工包 | 名称 | 对应问题 | 风险 | 状态 |
| --- | --- | --- | --- | --- |
| 1A | 豁免状态机事实源 | P0-01，核对 E-02 | 高 | 已部署待验收 |
| 1B | 视频提交与历史编辑数据安全 | P0-02 | 高 | 已部署并验收 |
| 1C | 可靠数据读取体系 | P1-04、P1-05 | 高 | 已部署并验收（趋势/排行榜低频入口） |
| 1D | 上海业务日期与 UTC 存储 | P1-06 | 高 | 已部署并验收（UTC 测试及趋势/排行榜日期窗口） |
| 1E | 权限缓存与数据库安全门禁 | P1-07、P1-08 | 高 | 已部署待验收 |

阶段出口：P0 不再有数据丢失、错账、误覆盖风险；核心读取、日期、权限和数据库安全门禁有自动回归测试与分层验收记录。

---

# 二、开工前只读核对

| 核对项 | 状态 | 记录 |
| --- | --- | --- |
| `git status --short --branch` | 已执行 | 工作区存在多组未提交改动，后续施工必须继续按当前施工包划清文件范围。 |
| 生产部署 SHA | 已核对 | 当前 `HEAD=origin/main=e8164da2`；Vercel Production success。此前 `35b6683d` 为生产部署版本，`e8164da2` 已覆盖此前代码及本轮 1C/1D 修复。 |
| 已执行 migration | 已确认 | 1E 的 `admin_first_screen_perf_events` RLS migration 已由阿禅确认生产执行完成。 |
| E-02 明细缺失原因 | 已定性 | 随 1A 核对：非当前代码 bug，系统已自动修正；后续真实验收如出现新证据再重开。 |
| 第一阶段执行口径 | 已调整 | 不再单独维护白名单/测试计划/测试报告文件；范围、测试点和结论直接写在本阶段文件。 |
| 剩余任务 | 分层保留 | 1C/1D 已完成生产补验；1A H1 手工 grant 独立入口、1E 动态拒绝矩阵和 migration 历史记录仍待核实。 |

---

# 三、施工包详情

## 1A · 豁免状态机事实源

- 目标：建立单一可靠的豁免事实源，使审批、打回、重新批准、手工豁免和 profiles 投影得到一致结果。
- 包含问题：P0-01；原始 A1-1、D-01、D-09、D-10、D-13、D-16-2；E-02 仅作部署/migration 核对。
- 不包含范围：不重做前端视觉；不顺手改全部豁免消费方；不执行生产数据修复脚本，除非阿禅另行授权。
- 预计影响模块：`/api/exemptions/*`、UnifiedCommandHub、dashboard 豁免入口、admin tools 手工豁免、`exemption_request`、`exemption_request_date`、`exemption_grant`、`profiles` 投影、相关 RPC/migration。
- 依赖关系：无；2A、2B 必须等它完成。
- 风险等级：高。
- 当前状态：已部署待验收。

完成记录：

- 完成时间：2026-09-03 16:01。
- 核心产出：豁免状态机核对与投影修正。
- 验证结论：申请、审批、改判、打回 4 条写路径及 5A D 真实审批链路证明公共状态机可用；H1 手工 grant 独立入口本轮未单独驱动，因此保留施工包级待验收。
- E-02 定性：非当前代码 bug，系统已自动修正；不再阻塞第一阶段。

完成标准：公共状态机和 5A D 审批链路已满足；H1 手工 grant 独立入口仍需单独验收后才能关闭施工包。

当前进度：代码已部署；D 的申请→审批→逐日状态/grant/profile 投影链路已在 5A 通过，但 H1 手工 grant 独立入口尚无单独生产验收证据。

下一步：无。

## 1B · 视频提交与历史编辑数据安全

- 目标：让视频/日报提交具备事务或幂等边界，失败不会误删、误回收或改错作品。
- 包含问题：P0-02；原始 A3-1、A4-1、A5-1、C2-3、D-03；A4-2 为同型只读问题。
- 不包含范围：不重写整个 `VideoSubmitFormV2`；不做视觉改版；不清理与提交无关的旧模块。
- 预计影响模块：`/api/video-submit`、历史编辑详情、daily_reports、videos、video_tags、script_usage_records、截图/话术关联、相关测试。
- 依赖关系：建议等 1A 白名单冻结后再开；2D 依赖它的事务策略结论。
- 风险等级：高。
- 当前状态：已部署并验收。

完成记录：

- 完成时间：2026-09-03 17:28。
- 核心产出：日报提交安全核对。
- 验证结论：3 条独立路径全部安全可控，无继续修复必要。
- 已核对路径：`/api/video-submit` 新提交主入口使用 SHA256 幂等和补偿回滚；`dashboard/actions::submitReport` 历史编辑入口有 `video_id` 绑定验证并通过 service role 处理 RLS；`admin/actions::updateDailyReport` 为管理员批量修正路径，强权限且不写视频表。
- 补充定性：白名单中提出的“绕过主入口”不是 bug，而是合理的独立路径分工。

完成标准：已满足。2D 可在第二阶段参考 1B 事务/幂等结论处理非视频多表写入。

当前进度：代码已部署；前序 H2/M1/M2 生产复测与单测记录已闭环，未发现需要追加修复的真实缺陷。

下一步：无。

## 1C · 可靠数据读取体系

- 目标：统一处理查询 error、分页、排序和 1000 行上限，核心查询失败不能静默变空数据。
- 包含问题：P1-04、P1-05；原始 A1、A2、A5、C2 重复表象。
- 不包含范围：不重写 growth；不做性能深改；不把所有低频列表一次性翻新。
- 预计影响模块：growth、admin/content、topics、remind、AI 分析、admin tools、rewrite、team join、相关 loader/service。
- 依赖关系：可等 1A/1B 后；若涉及豁免查询，应等 1A 状态机稳定。
- 风险等级：高。
- 当前状态：已部署并验收（当前范围）。

完成记录：

- 完成时间：2026-09-03 14:24。
- 核心产出：统一查询失败与 1000 行上限处理。`query-error.ts` 提供 `requireQueryRows`、`requireMaybeQueryRow`、`fetchAllQueryPages`（页长 1000）。
- 实际修改：growth 14d/30d 日报分页；admin/content 分析记录分页且失败不再当 `[]`；topics 作品/热度/摘要分页；remind 7 日账号/日报/豁免分页，豁免失败 fail-closed；content-analysis / comparison / 切段 / 豁免查询失败抛错；rewrite 读失败检查；admin-tools 查询失败返回 `success: false`；team-join 姓名查询失败返回 `SELECT_FAILED`。
- 未改：growth/rewrite 整体重写；未使用的 `hasPendingExemptionRequest`。
- 验证：`npx tsx --test` 覆盖 1C 相关 12 个测试文件，116 通过 0 失败。`npx tsc --noEmit` 仅剩 1E 预存 `cache-invalidation.test.ts` 类型错误，未纳入本包。
- 真实页面验收：生产 `/growth` 首次加载和刷新均通过；页面 → `/api/dashboard/trend`、`/api/dashboard/leaderboard` → 两接口均返回 200 合法 JSON，排行榜返回 539 条 → 刷新后页面无可见读取错误，console error 0。

完成标准：已满足自动测试；未覆盖全部低频展示查询。

当前进度：代码已随 `e8164da2` 部署；本轮补验覆盖趋势/排行榜错误处理、趋势团队日报分页、稳定排序和刷新状态。未据此宣称全部低频展示查询均已翻新。

下一步：无；其他低频读取入口不在本轮扩展范围。

## 1D · 上海业务日期与 UTC 存储边界

- 目标：建立上海业务日期、UTC 数据库存储、页面展示之间的统一入口。
- 包含问题：P1-06；原始 B3-1~B3-13、E-01。
- 不包含范围：不重写历史数据；不改业务日期产品口径；不做 growth/rewrite 整体重做。
- 预计影响模块：作品提交/编辑、发布管理默认月份、dashboard 日期、成员 AI 建议、管理员工具、导出和洞察周期。
- 依赖关系：与 1B 有视频提交交集，默认等 1B。
- 风险等级：高。
- 当前状态：已部署并验收（当前范围）。

完成记录：

- 完成时间：2026-09-03 14:24。
- 核心产出：业务日默认值和发布时间入口复用现有上海工具，不新造日期体系。
- 日期入口：`formatShanghaiDateOnly` / `shiftDateOnly` / `getShanghaiYearMonth`；发布时间：`日报.ts` 的默认值、上海 19:00 → UTC 存储、回填上海显示。
- 实际修改：作品提交/编辑（`日报.ts`、`stability.ts`、旧提交表单、历史编辑回填）；发布管理默认日/月（page、calendar API、workbench 今日与近 7 日）；成员 AI 建议；admin-tools 缺省日；导出默认范围；洞察周期起点；cockpit 缺省日；remind count 缺省日；管理员豁免弹窗昨天。
- 未改：growth/rewrite；历史数据；日历键迭代（`YYYY-MM-DD` 加减）；日期合法性校验；排行榜/趋势图/图表等非优先入口；通知幂等键（1E）。
- 验证：`TZ=UTC npx tsx --test` 覆盖凌晨、月末/年末、上海 19:00 存成 `11:00Z` 且回填仍为 19:00；相关测试 45 通过。ESLint 本包源文件通过。
- 真实页面验收：`TZ=UTC` 现有日期测试通过，并补验 `/growth` 趋势/排行榜日期窗口；未据此宣称全站日期消费者均已验证。

完成标准：UTC 自动测试与趋势/排行榜日期窗口抽验已满足当前范围；不覆盖全站日期消费者。

当前进度：代码已随 `e8164da2` 部署；UTC 环境下凌晨、月末/年末及上海 19:00 → UTC 存储/展示边界通过，趋势/排行榜低频入口已完成定向复核。

下一步：无；未覆盖的日期消费者继续保留为范围边界。

## 1E · 权限缓存与数据库安全门禁

- 目标：权限变更后立即生效，并补齐新表 RLS/revoke 与静态门禁。
- 包含问题：P1-07、P1-08；原始 D-06、D-07、D-16-1、D-16-3、D-16-4。
- 不包含范围：不重做完整权限模型；不扩大角色能力；不执行未授权生产 migration。
- 预计影响模块：权限写路径、`getCurrentPermissionContext`、admin actions、RLS migration、`admin_first_screen_perf_events`、静态检查脚本。
- 依赖关系：无业务硬依赖，但需独立安全验收。
- 风险等级：高。
- 当前状态：已部署待验收。

轻量化复盘：该包被执行得过重。P1-07 实际是代码核对项，若所有权限写路径已统一清缓存，不需要为“问题不存在”额外堆测试和长报告；P1-08 实际是补新表 RLS/revoke，属于小型安全补丁。

完成标准：代码和必要安全检查已满足；P1-08 的用户确认已保留，但动态拒绝矩阵与 migration 历史记录仍需独立核实。

当前进度：代码已部署；P1-07 未发现现行缺陷，P1-08 的 RLS/revoke 已有用户确认，但动态拒绝矩阵与 migration 历史记录未由本轮独立核实。

下一步：无。

---

# 四、阶段完成记录

## 2026-09-04 · 状态对账与 1C/1D 补验记录

- 执行者：[CX]
- 对账依据：`git log`、`git merge-base HEAD origin/main`、`HEAD=origin/main=e8164da2`、Vercel Production success、生产健康检查和 `/growth` 页面/API 验收。
- 生产部署：`e8164da2` 已覆盖此前 `35b6683d` 及其前置施工包；不能仅以 commit 在 `main` 推定部署，本次已有 Vercel Production success 和生产请求证据。
- 1C 真实新 Bug：趋势与排行榜查询未检查 Supabase `error`，失败可伪装成 200 空数据；趋势团队日报查询无分页，超过 1000 行会静默漏数。已先用失败测试复现，再做最小修复，补稳定排序与分页。
- 1C 定向证据：生产 `/growth` → `/api/dashboard/trend`、`/api/dashboard/leaderboard` → 两接口均返回 200 合法 JSON，排行榜返回 539 条 → 首次加载与刷新均无可见读取错误，console error 0。
- 1D 定向证据：`TZ=UTC` 日期及相关测试 43/43 通过；覆盖北京时间凌晨、月末/跨年和上海发布时间边界，并抽查趋势/排行榜日期窗口。该证据不扩展为全站日期链路通过。
- 自动验证：`npm test` 1416/1416；`npm run build` 79/79；本次修改文件 ESLint 通过；`git diff --check` 通过。全量 `tsc`/`lint` 仍受既有 `output/batch5/*.mts` 脚本错误阻断，非本次改动。
- 状态结论：1B、1C、1D 已部署并验收；1A、1E 已部署待验收。未执行生产写入、migration 或权限变更。
- 下一步：不扩展 1C/1D 低频入口；保留 1A H1、1E 动态拒绝与 migration 历史记录的待核实边界。

## 2026-09-03 14:24 · 第一阶段记录

- 执行者：Codex
- 本次施工包：1C、1D（1A/1B/1E 此前已完成）
- 实际修改文件：见上文 1C、1D 完成记录
- 新增 migration：无（1E 的 RLS migration 此前已由阿禅确认生产执行）
- 是否执行生产 migration：否
- 自动测试结果：1C 相关 116 通过；1D 在 `TZ=UTC` 下 45 通过；`tsc` 仅剩 1E 预存 `cache-invalidation.test.ts` 类型错误
- 本地浏览器/API 验收：未开浏览器；1D 用 UTC 单测和 member-ai-suggestion 路由测证明归属日与发布时间 roundtrip
- 生产部署 SHA：当时记录为未发布；后续已由 `e8164da2` 覆盖并部署，详见 2026-09-04 对账记录。
- 真实角色验收：无
- 已完成：1B、1C、1D 的当前范围代码、测试和生产验收
- 止血但未完整：1C 未翻新全部低频展示查询；1D 未替换全部非优先日期入口；1A/1E 仍缺施工包级独立验收
- 未完成：1A H1 独立入口、1E 动态拒绝矩阵和 migration 历史记录核实
- BLOCKED：无
- 新发现但不纳入本阶段：`src/lib/permissions/cache-invalidation.test.ts` 类型错误属 1E 脏工作区，未修
- 下一步：不新增第一阶段代码；按总索引保留待验收和证据不足项
