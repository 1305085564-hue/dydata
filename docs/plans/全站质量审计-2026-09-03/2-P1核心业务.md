# 2 · P1核心业务

> 阶段目标：在第一阶段 P0 和系统根因稳定后，修复核心业务一致性问题。
> 当前状态：第二阶段进行中；2A 已修复未发布，下一步 2B。

---

# 一、阶段范围

| 施工包 | 名称 | 对应问题 | 风险 | 状态 | 依赖 |
| --- | --- | --- | --- | --- | --- |
| 2A | 豁免旁路消费方统一 | P1-01 | 高 | 已修复未发布 | 必须等 1A |
| 2B | 成员调配与 pending 豁免归属 | P1-02 | 中 | 未开始 | 必须等 1A |
| 2C | 履约申诉 RPC | P1-03 | 中 | 未开始 | 无硬依赖 |
| 2D | 非视频多表写入补偿治理 | P1-09 | 中 | 未开始 | 等 1B |
| 2E | 输入边界统一 | P1-10 | 中 | 未开始 | 无硬依赖 |
| 2F | 业务常量和状态集合收敛 | P1-11 | 中 | 未开始 | 避开 3B topics 文件 |

阶段出口：每个 P1 有对应修复或明确不改原因；自动测试、本地真实链路、生产和真实角色证据分层记录。

---

# 二、施工包详情

## 2A · 豁免旁路消费方统一

- 目标：所有“某天是否豁免/待审批/可重提”的消费方统一读取同一事实。
- 包含问题：P1-01；原始 D-05、D-08、D-15、B4-3。
- 不包含范围：不重做 1A 状态机；不做豁免弹窗视觉改版。
- 预计影响模块：dashboard、发布管理、remind、申请重叠校验、旧整单 RPC。
- 依赖关系：必须等 1A；不并行于 2B。
- 风险等级：高。
- 当前状态：已修复未发布。
- 开工前计划：复用 1A 事实源；逐个替换旁路消费方；旧 RPC 收权或删除前查 route/API/测试/外部触发；用部分批准/驳回日期做回归。
- 完成标准：dashboard、发布管理、remind、重提校验逐日结果一致；旧入口不再产生旧口径。
- 当前进度：已确认 1A 事实源语义：已获批/当日是否豁免读取 active `exemption_grant`，待审批/可重提读取 `exemption_request_date.status = 'pending'`，仅在无逐日明细的存量申请上兼容整单区间。已把 dashboard 首屏 pending 日期、dashboard Server Action 重提校验、REST 申请重叠校验、发布管理待审叠加、remind 免提醒判断、action center 待审日期展示统一到公共 helper。
- 下一步：未发布；进入 2B 前注意 `src/lib/permissions/cache-invalidation.test.ts` 仍有 1E 预存类型错误，会阻断全量 `tsc`。

## 2B · 成员调配与 pending 豁免归属

- 目标：成员转团队时 pending 豁免申请不会卡死，审批或重提都有明确路径。
- 包含问题：P1-02；原始 D-04。
- 不包含范围：不改成员角色体系；不处理 F-03 移动遮挡。
- 预计影响模块：admin/modules、成员调配 service、豁免审批列表、申请重提校验。
- 依赖关系：必须等 1A；不能与 3B 同时改 `modules-content-v3.tsx`。
- 风险等级：中。
- 当前状态：未开始。
- 开工前计划：转团队事务内定义 pending 申请迁移/撤销/重提策略；补错误文案和测试。
- 完成标准：有 pending 豁免时转团队后，旧团队/新团队审批和成员重提都不会卡死。
- 当前进度：未开始。
- 下一步：等待 1A 完成。

## 2C · 履约申诉 RPC

- 目标：申诉同意分支不再引用已删除字段，并能正确关闭申诉、更新履约记录。
- 包含问题：P1-03；原始 D-02。
- 不包含范围：不重做发布管理页面；不调整履约产品口径。
- 预计影响模块：`handle_fulfillment_appeal` RPC、发布管理申诉队列、成员抽屉申诉处理。
- 依赖关系：无硬依赖；migration 与其他包分开。
- 风险等级：中。
- 当前状态：未开始。
- 开工前计划：先查线上函数定义；新增 migration 修函数；补 approve 分支测试。
- 完成标准：同意 pending 申诉成功，无 `profiles.group_id` 引用；生产函数定义已核对或标未发布。
- 当前进度：未开始。
- 下一步：只读核对当前 RPC 定义。

## 2D · 非视频多表写入补偿治理

- 目标：注册、豁免申请、rewrite skill、成员生命周期等非视频多表写入不再留下不可见半成品。
- 包含问题：P1-09；原始 A3-2、A3-3、A3-4、A3-5、D-12。
- 不包含范围：不一次重写所有模块；不处理 1B 视频提交。
- 预计影响模块：注册入团、豁免申请、rewrite skill 创建、成员生命周期审计。
- 依赖关系：等 1B 的事务/幂等策略结论。
- 风险等级：中。
- 当前状态：未开始。
- 开工前计划：按风险拆子包，优先注册和豁免申请；每条链路做第二步失败注入。
- 完成标准：失败不永久残留，或残留有明确可恢复状态和可见错误。
- 当前进度：未开始。
- 下一步：等待 1B 结论，随后列子包顺序。

## 2E · 输入边界统一

- 目标：数字和长文本输入由前后端共同校验，不能入库污染统计或静默截断。
- 包含问题：P1-10；原始 B5-1~B5-6。
- 不包含范围：不做表单视觉改版；不改指标业务含义。
- 预计影响模块：日报指标、管理员日报编辑、审批反馈、豁免事由、案例驳回、选题文案。
- 依赖关系：无硬依赖。
- 风险等级：中。
- 当前状态：未开始。
- 开工前计划：后端先兜底，前端同步提示；共享数字/文本限制常量。
- 完成标准：负数、极大数字、超长文本被拒绝或阻断，不能入库或无声截断。
- 当前进度：未开始。
- 下一步：列出所有输入入口和当前校验位置。

## 2F · 业务常量和状态集合收敛

- 目标：topics 阈值、履约状态集合、展示口径等引用同一业务常量。
- 包含问题：P1-11；原始 B4-1、B4-2、B4-4、B7-1、B7-2、D-11、E-03。
- 不包含范围：不处理回收站/作废口径，等阿禅决策；不做 topics 卡片视觉修复。
- 预计影响模块：topics service、视频详情标签、发布管理统计、履约状态集合、相关测试。
- 依赖关系：不能与 3B 同时改 topics 卡片文件。
- 风险等级：中。
- 当前状态：未开始。
- 开工前计划：确定共享常量唯一入口，补硬编码回潮测试。
- 完成标准：修改共享阈值或状态集合后，所有引用方同步变化。
- 当前进度：未开始。
- 下一步：列出阈值/状态硬编码调用点。

---

# 三、阶段完成记录

```md
## YYYY-MM-DD HH:mm · 第二阶段记录

- 执行者：
- 本次施工包：
- 实际修改文件：
- 新增 migration：
- 是否执行生产 migration：
- 自动测试结果：
- 本地浏览器/API 验收：
- 生产部署 SHA：
- 真实角色验收：
- 已完成：
- 止血但未完整：
- 未完成：
- BLOCKED：
- 新发现但不纳入本阶段：
- 下一步：
```

## 2026-09-03 15:13 · 第二阶段记录

- 执行者：[CX]
- 本次施工包：2A · 豁免旁路消费方统一
- 实际修改文件：`src/lib/豁免.ts`、`src/lib/豁免.test.ts`、`src/lib/loaders/dashboard-page.ts`、`src/lib/loaders/dashboard-page.test.ts`、`src/app/(app)/dashboard/actions.ts`、`src/app/(app)/dashboard/actions.test.ts`、`src/app/api/exemptions/apply/route.ts`、`src/app/api/exemptions/apply/route.test.ts`、`src/lib/loaders/fulfillment-page.ts`、`src/lib/loaders/fulfillment-page.test.ts`、`src/lib/remind-submission.ts`、`src/lib/remind-submission.test.ts`、`src/app/api/remind/route.ts`、`src/app/api/remind/route.test.ts`、`src/lib/action-center/server.ts`、`src/lib/action-center/server.test.ts`。
- 新增 migration：无。
- 是否执行生产 migration：否。
- 自动测试结果：`npx tsx --test 'src/lib/豁免.test.ts' 'src/app/(app)/dashboard/actions.test.ts' 'src/lib/loaders/dashboard-page.test.ts' 'src/app/api/exemptions/apply/route.test.ts' 'src/lib/loaders/fulfillment-page.test.ts' 'src/lib/remind-submission.test.ts' 'src/app/api/remind/route.test.ts' 'src/lib/action-center/server.test.ts'` 通过，67/67；本次修改文件 ESLint 通过。
- 本地浏览器/API 验收：未启动浏览器；本包无可见布局改动，按规则用静态检查与定向测试验收。
- 生产部署 SHA：未部署。
- 真实角色验收：未执行。
- 已完成：公共 pending 日期 helper；dashboard、发布管理、remind、action center、重提/重叠校验统一到逐日 pending 明细；remind 已获批豁免读取 active grant。
- 止血但未完整：无。
- 未完成：生产发布和真实角色验收未做。
- BLOCKED：`npx tsc --noEmit --pretty false` 被 1E 预存 `src/lib/permissions/cache-invalidation.test.ts:24` 类型错误阻断，非 2A 文件。
- 新发现但不纳入本阶段：`exemption-orphan` 和 `admin/actions` 仍按整单 pending 处理孤儿申请/归属异常，归入 2B 语义范围，不在 2A 修。
- 下一步：2B · 成员调配与 pending 豁免归属。
