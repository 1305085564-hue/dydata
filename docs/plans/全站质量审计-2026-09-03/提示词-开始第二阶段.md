# 提示词：开始第二阶段

> 用途：把这份提示词交给后续 Codex/Claude，直接开工第二阶段。
> 当前阶段：全站质量审计 · 第二阶段：P1 核心业务一致性。
> 当前进度：第一阶段 1A–1E 施工包已完成；1C/1D 只在本地，未提交、未上线。第二阶段 2A–2F 全部未开始。
> 推荐开工：2A · 豁免旁路消费方统一。

---

# 一、必须先读

工作目录：`/Users/mac/Projects/dydata`

先读：

1. `AGENTS.md`
2. `docs/全站模块地图.md`
3. `docs/数据口径.md`
4. `docs/权限与安全说明.md`
5. `docs/工程运行事实.md`
6. `docs/plans/全站质量审计-2026-09-03/00-总清单.md`
7. `docs/plans/全站质量审计-2026-09-03/00-进度索引.md`
8. `docs/plans/全站质量审计-2026-09-03/1-P0与系统根因.md`（只读结论，不要重做）
9. `docs/plans/全站质量审计-2026-09-03/2-P1核心业务.md`

细节不够时再读 `00-总清单-详细备份.md`，不要把它当施工入口。

`2-P1核心业务.md` 开头可能仍写“第一阶段未完成前不开第二阶段”。以本提示词和 `00-进度索引.md` 为准；开工后把阶段文件状态改成进行中。

需要使用的 Skill：

- `17-根因调试恢复`
- `18-测试驱动开发`
- `22-接口边界设计`
- `supabase:supabase`
- 涉及真实页面验证时再用 `13-浏览器调试`
- 涉及生产 SHA / Vercel 部署时再用 `vercel:deployments-cicd` 或 `vercel:vercel-cli`
- 2A 高风险，模型强度按极强；2B–2F 较强，碰到 migration/RPC 升为极强

开工时先 `git status --short --branch`。工作区已经很脏：第一阶段 1C/1D 本地改动、1E 权限/通知清理、审计文档都未提交。只动当前施工包必要文件，不要把第二阶段改动混进 1C/1D/1E。

---

# 二、已完成，不要重做

| 施工包 | 完成时间 | 结论 | 后续要求 |
| --- | --- | --- | --- |
| 1A | 2026-09-03 16:01 | 豁免状态机核对完成；申请、审批、改判、打回 4 条写路径正确；E-02 不是当前代码 bug。 | 不要重做状态机。2A/2B 必须复用 1A 事实源。 |
| 1B | 2026-09-03 17:28 | `/api/video-submit`、`dashboard/actions::submitReport`、`admin/actions::updateDailyReport` 三条路径安全可控。 | 不要重做视频提交。2D 只处理非视频多表写入，参考 1B 的事务/幂等结论。 |
| 1C | 2026-09-03 14:24 | 核心读取失败不再伪装成空数据；高风险列表分页越过 1000 行。本地未提交。 | 不要重做读取体系。2A 消费方若有查询，沿用 1C 的 error/分页约定。 |
| 1D | 2026-09-03 14:24 | 业务日/默认月份/发布时间入口改用现有上海日期工具。本地未提交。 | 不要重做日期体系。新代码继续用 `formatShanghaiDateOnly` / `shiftDateOnly` / `getShanghaiYearMonth` 和 `日报.ts` 发布时间工具。 |
| 1E | 2026-09-03 | P1-07 核对未发现现行缺陷；P1-08 RLS migration 已由阿禅确认生产执行。`cache-invalidation.test.ts` 仍有预存类型错误。 | 不要重做 1E，也不要顺手修那个类型错误，除非它挡住本包测试。 |

不要修第三阶段、第四阶段、第五阶段。不要处理回收站/作废是否扣实发，那是阿禅产品决策，留给 4C。

---

# 三、第二阶段施工包

按这个顺序做，一次只做一个包。做完一个再开下一个。不要并行 2A 和 2B。不要同时改 `modules-content-v3.tsx` 或 topics 卡片文件（那是 3B 的互斥区）。

## 2A · 豁免旁路消费方统一

目标：所有“某天是否豁免 / 待审批 / 可重提”的读取，都走 1A 已经核对过的同一事实源。

包含问题：P1-01；原始 D-05、D-08、D-15、B4-3。

优先范围：dashboard、发布管理、remind、申请重叠/重提校验、旧整单 RPC。

执行方式：

1. 先在代码里找到 1A 的事实源函数/RPC，再 `rg` 旁路读取：整单区间、只读 `profiles` 投影、旧 RPC、本地日期集合。
2. 逐个替换消费方，不要重做审批写路径。
3. 旧 RPC 收权或删除前，查 route、API、测试、外部触发；有外部调用就先标出来，不要直接删。
4. 用“部分批准 / 部分驳回”的逐日数据做回归。

完成标准：dashboard、发布管理、remind、重提校验对同一天的结果一致；旧入口不再产出旧口径。

## 2B · 成员调配与 pending 豁免归属

目标：有 pending 豁免时把人转到别的团队，申请不会卡死；旧团队、新团队、成员本人都有明确的审批或重提路径。

包含问题：P1-02；原始 D-04。

不包含：不改角色体系；不处理移动端遮挡。

执行方式：在转团队事务里定义 pending 申请是迁移、撤销还是必须重提；补错误文案和测试。不要和 3B 同时改 `modules-content-v3.tsx`。

完成标准：转团队后，旧团队/新团队审批和成员重提都不会卡死。

## 2C · 履约申诉 RPC

目标：申诉同意不再引用已删除的 `profiles.group_id`，能关闭申诉并更新履约记录。

包含问题：P1-03；原始 D-02。

执行方式：先只读核对线上 `handle_fulfillment_appeal` 定义，再写 migration 修函数，补 approve 分支测试。生产执行 migration 必须阿禅在当前对话明确授权。

完成标准：同意 pending 申诉成功；无 `profiles.group_id` 引用；生产函数已核对，或明确标成未发布。

## 2D · 非视频多表写入补偿治理

目标：注册入团、豁免申请、rewrite skill 创建、成员生命周期等，第二步失败后不能留下不可见半成品。

包含问题：P1-09。不要重做 1B 视频提交。

执行方式：按风险拆，优先注册和豁免申请；每条链路做第二步失败注入。失败要么回滚干净，要么残留可恢复且错误对用户可见。

完成标准：失败不永久残留，或残留有明确可恢复状态和可见错误。

## 2E · 输入边界统一

目标：数字和长文本由前后端一起拦，不能入库污染统计，也不能静默截断。

包含问题：P1-10。

优先范围：日报指标、管理员日报编辑、审批反馈、豁免事由、案例驳回、选题文案。

不包含：表单视觉改版；不改指标业务含义。

完成标准：负数、极大数字、超长文本被拒绝或阻断。

## 2F · 业务常量和状态集合收敛

目标：topics 阈值、履约状态集合、展示口径引用同一套常量。

包含问题：P1-11。不处理回收站/作废口径。不要改 topics 卡片视觉，也不要和 3B 同时改 topics 文件。

完成标准：改一处共享阈值或状态集合，所有引用方一起变；硬编码回潮能被测试抓住。

---

# 四、执行纪律

1. 不要新建白名单、测试计划、测试报告等额外文档。进度只写进 `2-P1核心业务.md`，全局状态写 `00-进度索引.md`。
2. 不要写长施工报告。核对后发现“问题不存在”时，一句结论加关键证据即可。
3. 不要重做 1A–1E，不要修第三到第五阶段。
4. 不要修改数据库或执行 production migration，除非阿禅在当前对话明确授权。2C 尤其如此。
5. 不要提交或 push，除非阿禅在当前对话明确授权。
6. 阿禅不懂技术。需要选择时给出推荐和理由，等他决定。低收益高成本的方案要直接建议不做。
7. 改动必须前后端闭环。没有真实接口就说未接入，失败就明确失败，禁止假数据假装成功。
8. 高风险权限/登录/RLS/Auth 改动前先 grep 调用点，列完受影响文件再动手。
9. 统计口径拿不准就停，先抽查真实数据或问阿禅，不要猜。

---

# 五、交付格式

每完成一个施工包，只写短汇报：

```md
## 2A/2B/2C/2D/2E/2F 完成

- 实际修改：
- 测试结果：
- 真实页面/API 验收：
- 需要生产 migration：是/否
- 未覆盖边界：
- 下一步：
```

第二阶段全部完成后：

1. 更新 `2-P1核心业务.md`。
2. 更新 `00-进度索引.md`。
3. 明确第二阶段状态是“完成（本地）”还是还有未发布 migration。
4. 不要自动开第三阶段，除非阿禅当场要求。

现在从 **2A · 豁免旁路消费方统一** 开始。
