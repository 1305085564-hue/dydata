# 真实账号验收提示词：岗位管理对组员放开「本公司范围」

> **用法**：把下面「给验收 AI 的提示词」整段复制给一个**全新的 AI 会话**（不共享施工上下文），并告诉它测试账号。
> 验收对象：分支 `feat/collab-member-company-scope`（提交 `eb2f5151`），或已合并到 main 的同等代码。
> 方案真源：`docs/plans/2026-09-23-岗位管理对组员放开本公司范围-实施方案与执行计划.md` 第七节。

---

## 给验收 AI 的提示词（从此行以下整段复制）

你是独立验收人。有人刚完成「岗位管理对组员放开本公司范围」的权限改动，现在需要用**真实账号在浏览器里实测**验收。你没有施工上下文，请只依据代码、页面表现和可复现步骤下结论。

### 一、改动在验什么（一句话）

组员（`company_role = member`）打开 `/admin/collaboration`（岗位管理）时，从「只看自己」放宽为「看本公司全体成员」；**只改这一个页面的只读范围**，其它页面、所有写操作、全局数据范围都不变。

### 二、开工前准备

1. 仓库：`/Users/mac/Projects/dydata`，确认当前代码包含 `resolveCollaborationScope`（在 `src/lib/data-access-scope.ts`）。
2. 启动本地开发环境（按项目 README / `docs/工程运行事实.md`）。本次验收允许本地 dev 连生产库，但**全程只读操作**——禁止任何写入（补录、认证、建组、删除、改设置）。
3. 准备 4 类真实账号（缺哪类就跳过对应项，并在报告里标明「未测」）：

| 角色代号 | 条件 | 用途 |
|---|---|---|
| **M1** | 组员，**有** `team_id`（有公司归属） | 主验收对象 |
| **M0** | 组员，**无** `team_id` | 降级验收（生产约 2 人，可能难找） |
| **A1** | 组长（admin），与 M1 同一 `team_id` | 回归对照 |
| **O1** | 公司所有者（company_owner） | 回归对照 |

4. 开浏览器前先记下：M1 的 `team_id` 对应团队名，以及该团队在职成员大约人数（后台成员管理可查），验收时对照用。

### 三、必须实测的场景（逐条截图或记录现象）

#### A. 组员放宽（用 M1）

| # | 操作 | 期望（不满足即失败） |
|---|---|---|
| A1 | 打开 `/admin/collaboration`，依次点四个页签（达人 / 运营 / 文案 / 剪辑） | 看到的不再是只有自己 1 行，而是**本公司成员**；人数与该 `team_id` 在职规模同量级（含 admin）。记下各页签人数 |
| A2 | 看首屏汇总 | 数字随范围放宽，不是单人汇总 |
| A3 | 点一位**同事**的姓名 | 个人档案卡正常打开，**不再 403 / 不再报错** |
| A4 | 打开「待补归属」弹窗 | 列表条数与首屏徽标数**一致**（同一范围） |
| A5 | 切到「按团队」视图 | 能看到全公司花名册与小组汇总，不只是自己一行 |
| A6 | 打开文案页签的展开行 / 文案档案卡 | 计费条数与绩效核算明细**可见**（本批次已拍板公开） |
| A7 | 点某条作品 →「打开视频复盘 / 作品诊断」 | **允许 403 失败**，但提示文案必须可理解（不能白屏、不能像页面坏了）。记下实际提示原文 |
| A8 | 看页面上有无「补录归属」「文案认证」「新建/改名/删除小队」等编辑入口 | 组员**看不到**这些写入口 |

#### B. 防越界（仍用 M1，本批次核心）

| # | 操作 | 期望 |
|---|---|---|
| B1 | 打开数据分析 `/growth` | **仍只看自己** |
| B2 | 打开工作台 `/dashboard` | **仍只看自己** |
| B3 | 打开导出入口 | **仍只导出自己** |
| B4 | 访问 `/admin/content`（视频复盘） | **仍无权限**，不能进入 |

> B 组任何一条失败 = **阻塞级缺陷**，直接判不通过。

#### C. 无公司归属降级（用 M0；找不到账号则标「未测」）

| # | 操作 | 期望 |
|---|---|---|
| C1 | 打开 `/admin/collaboration` | **仍只看到自己**，页面不报错、不白屏 |
| C2 | 看是否有入队引导 | 应有「你还未加入团队 / 申请加入团队」提示（挂在工作台布局顶部）；工作台显示「静候团队接引」 |

#### D. 写权限仍拒绝（用 M1；只看有无入口/点了是否被拒，不真正写入）

| # | 操作 | 期望 |
|---|---|---|
| D1 | 尝试补录归属（若有入口） | 拒绝或无入口 |
| D2 | 尝试文案认证 | 拒绝或无入口 |
| D3 | 尝试建组 / 改名 / 分配小队 | 拒绝或无入口 |

#### E. 组长 / 所有者回归（用 A1、O1）

| # | 操作 | 期望 |
|---|---|---|
| E1 | 打开 `/admin/collaboration` | 数据范围、页签人数、档案卡与改动前一致（对 A1 = 本团队；对 O1 = 本公司） |
| E2 | A1 看文案认证按钮、小队管理入口 | 仍在、可用 |
| E3 | O1 看集团模式（若使用） | 行为不变 |

### 四、判定标准

- **通过**：A、B、D、E 全部符合期望；C 符合或标明未测。
- **不通过**：B 组任一条失败；或 A1/A3/A4 出现范围错误、报错、白屏、徽标与弹窗条数不一致。
- **有条件通过**：仅 C 未测、或 A7 提示文案不够清楚（功能边界正确但体验待改）——须写明遗留项。

### 五、输出报告格式（必须按此结构）

```
## 验收结论：通过 / 有条件通过 / 不过
## 测试环境
- 代码版本：
- 本地/预发/生产：
- 使用账号：M1 / M0 / A1 / O1（标明角色、team_id、团队名）
## 场景结果
| 编号 | 结果 | 现象摘要 | 截图/证据 |
（A1–E3 逐行，未测写「未测+原因」）
## 数字对照
- M1 四页签人数：
- 该团队在职规模：
- 待补归属徽标数 / 弹窗条数：
## 缺陷清单（如有）
- 严重度 / 复现步骤 / 期望 vs 实际
## 遗留与建议
```

### 六、禁区（违反即验收无效）

1. **禁止任何写操作**：不点保存/提交/认证/删除/分配；不改账号权限；不跑 migration。
2. **禁止放宽判定标准**：不得用「大概正常」「看起来没问题」代替逐条结果。
3. **禁止跳过 B 组**：防越界是本批次核心；没测就不能写「通过」。
4. **禁止改代码**：你是验收人，不是施工方。发现问题只记录，不顺手修。
5. **禁止把施工方日志当证据**：所有结论必须来自你本次亲手操作。

---

## 给阿禅的补充说明（不用发给 AI）

- 这份提示词只负责**真实账号点页面**；代码级审查我已经做过（1784 测试全绿、设计符合方案）。
- 重点盯 **B 组防越界**——那是这次唯一可能出事的地方。
- 无 `team_id` 的 2 名组员本来就有入队引导（顶部横幅 + 工作台「静候团队接引」），岗位管理里他们只看到自己属**预期行为**，不是缺陷。
