# 岗位归属双表一致性治理实施方案（修订版）

## 目标

修复历史手稿编辑会清空日报负责人、会误关联视频、以及日报和视频半成功的问题；保留 `videos` 与 `daily_reports` 的不同作品集合，岗位管理继续按日报行计量。

## 本轮冻结范围（批次 0-2）

- 做生产只读基线；
- 历史编辑加载未完成或接口错误时禁止保存；
- 区分“有绑定视频”和“合法无绑定视频”；
- 读取侧不再按账号+日期猜视频；
- 历史编辑改走服务端权限校验后的历史编辑原子 RPC；
- 原子 RPC 同时覆盖日报字段、标题/正文同步和三岗位归属；
- 不新建第三张归属表、不做全量历史回填、不改投稿主链路、不执行生产写库。

## 明确不采用的方向

- 不以 `videos` 或 `daily_reports` 作为全局唯一主表；
- 暂不新建 `content_attributions`：当前没有闭合的作品身份，新增事实层会形成第三份副本，且必须同步投稿入口、RLS 和历史灌入，短期风险更高；
- 不把“无视频”视为编辑失败；日报-only 记录必须仍能修改指标和岗位。

## 已核实的当前问题

1. `src/app/(app)/dashboard/history-report-edit-form.tsx:522-528,647-667,892-899`：负责人初始为 `unassigned`，详情异步加载，保存按钮只受 `isPending` 控制；详情未完成时可提交空负责人。
2. `src/app/api/video-submit/edit-detail/route-core.ts:125-140`：`daily_reports.video_id` 为空时按账号和日期寻找视频；0 条返回 404，多条返回 409，恰好 1 条则把猜中的视频返回给表单。
3. `src/app/(app)/dashboard/actions.ts:107-173`：当前历史编辑先写 `daily_reports`，再单独写 `videos`，不是事务。
4. `supabase/migrations/20260907113000_daily_reports_video_link.sql:154-205`：现有 `update_collaboration_attribution` 有事务，但无绑定视频时仍按账号+日期猜视频；只允许 `service_role` 执行，并且限制 `report_date >= 2026-07-27`，不能直接覆盖全部历史编辑。
5. 投稿主链路在 `src/app/api/video-submit/route.ts`，本轮不改，但后续若引入归属事实层必须纳入该入口。

## 目标行为

```text
编辑详情状态：
loading              -> 禁止保存
ready_with_video     -> 可改日报，并在同一事务同步绑定视频
ready_without_video  -> 可改日报；不写、不猜视频
error                -> 禁止保存，可重试
```

## 批次 0：只读基线

- [ ] 固化视频-only、日报-only、两者重叠、重叠但负责人不一致四类查询。
- [ ] 固化无视频日报中 404、409、恰好单视频三类数量。
- [ ] 固化有负责人值的无视频日报清单，作为止血前保护样本。
- [ ] 固化岗位管理作品数按 `daily_reports` 行计数的结果。
- [ ] 记录线上 RPC 版本、执行授权和历史编辑入口当前版本。

完成条件：查询可重复、无写操作、结果保存到本项目日志或验收报告。

## 批次 1：历史编辑止血

### 修改文件

- `src/app/(app)/dashboard/history-report-edit-form.tsx`
  - 增加 `loading | ready_with_video | ready_without_video | error` 状态；
  - `ready_without_video` 仍允许保存日报字段和负责人；
  - 只有用户明确选择“未指定”时才提交 `null`；未完成加载不能提交空值；
  - 错误状态显示可重试，不把失败缓存成可保存状态；
  - 保存按钮同时受加载状态和 `isPending` 控制。
- `src/app/api/video-submit/edit-detail/route-core.ts`
  - `daily_reports.video_id` 为空时直接返回合法的 `ready_without_video` 详情；
  - 删除按账号+日期猜视频的 404/409 分支；
  - 无视频时不读取视频专属快照、标签和负责人作为日报初值；
  - 保留真实 5xx/网络错误和绑定视频损坏的错误语义。
- `src/app/api/video-submit/edit-detail.ts` 及其测试
  - 扩展详情 DTO 表达“无绑定视频”；
  - 增加无绑定视频、绑定视频、多视频不再误猜的断言。
- `src/app/(app)/dashboard/history-report-edit-form.test.ts`
  - 增加四态保存门禁、无视频仍可保存、加载失败不可保存、明确清空与未加载区分。

完成条件：无视频日报不再被 404/409 卡死；恰好一天一视频时也不再隐式绑定；任何未完成或失败详情都不能写库。

## 批次 2：历史编辑专用原子写入

不要直接复用现有协作补录 RPC；新增版本化的历史编辑 RPC（通过 `supabase migration new` 创建 migration）。

建议函数：`update_history_report_edit_atomic`。

输入至少包括：

- `p_report_id`；
- `p_video_id` nullable；
- 日报可编辑字段（标题、正文、指标、发布时间等）；
- 三个负责人 UUID nullable；
- 调用方用户 ID/必要的服务端权限上下文。

事务规则：

- 锁定并校验日报归属、当前用户和可编辑范围；
- 必写 `daily_reports`；
- `p_video_id` 非空时，校验它等于日报绑定的 `video_id`，再同步 `videos.video_title`、`videos.content` 和三岗位负责人；
- `p_video_id` 为空时，只写日报，不猜视频、不创建绑定；
- 不限制 `report_date >= 2026-07-27`，覆盖历史编辑实际可见范围；
- 任一步失败整体回滚；
- 函数只授予 `service_role`，调用前由 Server Action 完成用户身份、账号归属和可编辑权限校验。

### 修改文件

- `src/app/(app)/dashboard/actions.ts`
  - 历史编辑路径切换到服务端 admin client + 新 RPC；
  - 保留现有用户权限检查；
  - 不再执行两次独立写入；
  - 处理无视频成功和事务失败两种返回。
- 新 migration：创建 `update_history_report_edit_atomic`，不修改旧 migration。
- 新增 `src/app/(app)/dashboard/actions-history-edit.test.ts` 或同目录定向测试：
  - 有视频时日报和视频一起成功或一起失败；
  - 无视频时日报成功且视频不变；
  - 旧日期可编辑；
  - 标题/正文仍同步到绑定视频；
  - 权限失败不执行 RPC。

完成条件：历史编辑不再绕过事务；不丢标题/正文同步；不误写无绑定视频；旧日期不被协作统计起点挡住。

## 暂缓批次 3-5

暂缓新归属表、双读切换和全量历史灌入，原因：

- 尚无闭合的跨表作品身份；
- 会引入第三份岗位副本；
- 投稿接口尚未纳入；
- 新表 RLS、历史灌入和回滚尚未形成闭环。

后续若仍要统一模型，必须先单独设计 `work_id`/作品身份，再决定是否新增父表或归属表。

## 验收命令

```bash
npm test -- src/app/api/video-submit/edit-detail.test.ts src/app/api/video-submit/edit-detail/route-core.test.ts src/app/(app)/dashboard/history-report-edit-form.test.ts src/app/(app)/dashboard/actions-history-edit.test.ts
npx tsc --noEmit --pretty false
```

此外必须做一次真实角色、真实数据的只读/定向写入验收；本地测试不能替代生产证据。生产 migration 和生产数据修复不在本轮自动执行。

## 风险和停止条件

- 无法证明 Server Action 的权限校验覆盖原子 RPC 时停止；
- 新 RPC 无法在本地/测试库证明回滚时停止；
- 详情 DTO 改动导致旧视频编辑表单无法区分无视频与错误时停止；
- 同一路径连续两次定向验证失败，停止并记录阻塞；
- 发现投稿主链路也存在同类猜视频时记入后续任务，不扩大本轮范围。

---

## 验收结论与下一步（WorkBuddy 复核，2026-09-22）

### 一、已交付部分：验收通过，但不能单独上线

| 项 | 结果 |
| --- | --- |
| 工作区改动 | 2 个文件、+22/−2（`history-report-edit-form.tsx` 15 行、其定向测试 9 行） |
| `npm test` | 1738 通过 / 0 失败（本地实跑复现） |
| `npx tsc --noEmit` | 通过（本地实跑复现） |
| 「详情未就绪即保存 → 署名被空值覆盖」 | 已堵住 ✅ |

**阻断项 1：门禁依赖的「合法无绑定视频」状态还没做，上线即锁死 188 条日报。**

`fetchVideoSubmissionEditDetail`（`video-submit-panel-v2.tsx:92-94`）对无绑定视频的日报抛的是「该账号该日期没有可编辑的原视频」（404）或「存在多条视频，无法安全编辑」（409）。三态门禁把这两类都判为 `error` → 保存按钮永不可用。历史手稿列表只展示每人最近 30 条，生产实测其中 **188 条是 `video_id` 为空的日报（98 条带署名）**。保存被禁用是**整张表单**，连指标都改不了，与本文档「批次 1 完成条件：无视频日报不再被 404/409 卡死」直接冲突。代码是三态（`loading | ready | error`），方案要四态。

**阻断项 2：错误状态的重试是假的。**

失败被 `editDetailCache.set(cacheKey, null)` 缓存（`history-report-edit-form.tsx:638`），重新打开读同一缓存立刻回到 `error`，组件 key 变化也不清缓存。文案「请重新打开后重试」不成立，实际只能刷新整页。同一个弹层文件里早有解法：`video-submit-panel-v2.tsx:835-880` 把「没有可编辑」判为**合法缺失**（另给文案），只有真错误才给「重新载入」按钮（`setEditDetailRequestVersion` 版本号重试）。**应复用该判定，不要新造状态机。**

**阻断项 3：详情接口是双调用方，改返回值会波及投稿弹窗。**

`/api/video-submit/edit-detail` 同时被历史手稿（`history-report-edit-form.tsx:610`）与投稿面板重传模式（`video-submit-panel-v2.tsx:380`）调用。若把「无绑定视频」改成合法 200：历史手稿可编辑日报侧 ✅，但投稿弹窗会从「未寻得该日期的视频底稿」变成能进表单 ❌（那里的目的是重传视频，没有视频就没有可编辑对象）。**DTO 层表达「无绑定视频」，由调用方决定呈现**——方案批次 1 缺这一条。

**阻断项 4（次要）：锁的是整表，不是署名字段。**

方案要求「只有用户明确选择未指定才提交 `null`」，这需要后端支持「字段未提交 = 保留原值」。当前 `formData` 传 `""` → 后端 `|| null`，无法区分「未提交」与「明确清空」。前端门禁只是第一道闸，**后端拒绝空值覆盖**这道闸仍缺。

### 二、批次 0 只读基线（2026-09-22 生产实测，可直接采用）

| 口径 | 数值 |
| --- | --- |
| 视频-only（active 且无日报） | 517 |
| 日报-only（`video_id` 为空） | 461 |
| 重叠（日报 `video_id` 指向 active 视频） | 1316 |
| 日报总数 / 视频 active 总数 | 1778 / 1833 |
| 无 `video_id` 日报细分 | 330 无同日视频 / 119 同日多条 / **12 恰好一条（会被猜中并写回）** |
| 无 `video_id` 且带署名 | 176（其中运营署名 174） |
| 历史手稿列表可触达的无 `video_id` 日报 | **188（含 98 条带署名）** |
| 两表署名不一致 | 9 |
| 绑定视频缺 24h 快照的日报 | 0（422 分支当前不影响生产） |
| 线上 `update_collaboration_attribution` 授权 | 仅 `postgres` / `service_role`（无客户端通道） |
| 日报 `data_source='manual'` | 290（归属补录已在使用该 RPC） |

比对条件与 `videoMatchesBizDate` 一致（上海时区比对 `published_at` 或 `uploaded_at` 同日）。

### 三、修正后的下一步顺序

0. **（阻断项，先做）** 读取侧「合法无绑定视频」+ 复用面板已有判定与重试，**与已交付的门禁同一批上线，不得拆开**。
   验收：188 条中任选 ≥3 条真实无视频日报，历史手稿可打开、可保存指标与署名；119 条「同日多条」不再 409；12 条「恰好一条」不再隐式绑定；投稿弹窗重传模式仍显示「未寻得视频底稿」；真 5xx 仍禁止保存且「重新载入」可恢复。
1. **后端第二道闸**：区分「字段未提交（保留原值）」与「明确清空（写 `null`）」。
   验收：不带署名三列的提交不改动三列；显式选「未指定」的提交清空三列。
2. **批次 2 新 RPC `update_history_report_edit_atomic`**（方案已定：覆盖日报字段 + 标题/正文同步 + 三岗位归属；无 7/27 门槛；仅 `service_role`；权限校验留在 Server Action）。
   验收：本地一次性 PG 演练「有视频两侧同成败 / 无视频只写日报 / 7/27 前日报可编辑 / 权限失败不执行」；回滚可证。
3. **数据治理**（9 条不一致对齐、188 条无视频日报署名保全）——需明确授权后执行，排在最后。

### 四、停止条件（追加）

- 读取侧改动未与门禁同批上线 → 停止发布；
- 投稿弹窗重传模式行为发生变化（无视频时能进表单）→ 停止；
- 无视频日报在历史手稿中仍不可保存 → 不进入批次 2。

---

## 施工完成记录（实施 + 验收，2026-09-22）

### 一、已交付

| 交付项 | 文件 | 要点 |
| --- | --- | --- |
| 读取侧补「合法无绑定视频」 | `src/app/api/video-submit/edit-detail/route-core.ts`、`route.ts` | 删除 `listActiveVideosByAccount` 与 `videoMatchesBizDate` 两个猜写相关件；日报无 `video_id` 时返回 `{ detail: null, unboundReport }`，不猜视频、不读视频专属快照/标签 |
| 无绑定视频 DTO | `src/app/api/video-submit/edit-detail.ts` | 新增 `UnboundDailyReportDetail` + `buildUnboundDailyReportDetail`：只暴露日报自身字段（含日报三署名），字段格式错误直接 422 |
| 两个调用方各自决定呈现 | `video-submit-panel-v2.tsx`、新增 `history-report-edit-detail.ts` | 投稿面板仍按「没有可编辑」处理（文案与「返回概览」动作不变）；历史手稿走新读取函数，拿到 `dailyReportOnly` 即视为可编辑状态 |
| 历史表单四态 | `history-report-edit-form.tsx` | `loading \| ready_with_video \| ready_without_video \| error`；失败不再落缓存、新增可见「重新载入」；无视频时署名初值取**日报自身三列**，`video_id` 不再从缓存里取 |
| 后端第二道闸 | `src/app/(app)/dashboard/actions.ts` | `parseAssigneeField`：字段缺失=保留原值，字段存在且空=明确清空；三列必须同时提交 |
| 历史编辑原子写入 | 新增 `supabase/migrations/20260922180000_history_report_edit_atomic.sql`、`src/lib/history-report-edit-rpc.ts` | 单事务写日报全字段 + 绑定视频标题/正文/三署名；无 `video_id` 只写日报；无 7/27 门槛；仅 `service_role`；函数缺失时降级为旧两次写入并 `console.warn` |

### 二、验证证据

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 定向 + 全量单测 | `npm test` | **1755 通过 / 0 失败**（基线 1738，新增 17 条） |
| 类型检查 | `npx tsc --noEmit` | 通过 |
| Lint（改动文件） | `npx eslint <改动文件>` | 0 error（仅 1 条预存 warning，位于未改动行） |
| migration 本地演练 | 一次性 PG16 集群 + 生产三表真实列结构：`output/history-edit-rpc-verify/run-verify.sh` | **25/25 PASS**：有视频两侧同成败、无视频只写日报且同日视频零改动、2026-07-01 旧日期可编辑、绑定视频不可用时整笔回滚、绑定不一致/作废稿/日报不存在均 raise 且未写库、`p_touch_assignees=false` 保留三列、`anon`/`authenticated` 无执行权限 |
| 真实角色 + 真实数据只读验收 | `.acceptance/verify-history-edit-detail-20260922.mjs`（Chromium，生产库只读） | **36/36 PASS**，3 条真实记录：`2026-08-04`（无绑定视频、三署名齐全）与 `2026-07-01`（无绑定视频、仅剪辑署名）由修复前的 404「没有可编辑的原视频」变为 200 + 可保存，署名回填正确；`2026-09-07`（有绑定视频）分支行为未变 |

截图：`docs/ux-audit-screenshots-2026-09-22-acceptance/verify-history-edit-{unbound-0804,unbound-0701,bound-0907}.png`

### 三、migration 执行与数据治理（2026-09-22 晚更新）

1. **生产 migration 已于 2026-09-22 18:11 执行完毕**（阿禅手动执行，`CREATE FUNCTION / REVOKE / GRANT / NOTIFY` 全过）。
   核验（只读）：函数存在、`prosecdef=true`、EXECUTE 仅 `postgres,service_role`（无 anon/authenticated）、23 个参数。
   接口层探针：service_role + 不存在的 ID → `P0002 日报不存在`（函数体可达、零写入）；anon → `42501 permission denied`（收口生效）。
   原子写入自此真正生效，降级分支不再触发。
   可复用执行器：`output/apply-history-report-edit-rpc.sh`（打码打印连接串 → 基线 → 单事务应用 → 核验 → 两只探针；已用 no-op 副本自检，未重放生产 DDL）。
2. **数据治理**：已完成一部分，明细见 §四 —— 补全 2 条零损失行；8 条冲突待业务裁定；461 条无视频日报署名底账已落。
3. 第 1 步上线后仍建议抽查：任选 3 条无视频日报确认可保存指标与署名（本次只读验收未点保存，避免写生产数据）。

---

## 四、数据治理执行记录（2026-09-22，阿禅授权「做吧」）

### 4.1 现状复核（只读，与批次 0 基线对比）

| 口径 | 数值 |
| --- | --- |
| 两表署名不一致（日报 `video_id` 指向的视频，非作废日报） | **10**（原基线 9，期间新增 1 条保存） |
| 其中「日报三列全空、视频侧有值」 | 2 |
| 其中「两侧都有值但不同」 | 8 |
| 无视频日报总数 / 带署名 | 461 / 176 |
| 无视频日报且同日视频恰好 1 条（历史上会被猜中的那批） | 18（带署名 12） |

**新增事实（方案未记录）**：

- `videos` 没有「署名来源」列，`audit_logs` 里也没有归属变更动作（只有 `update_report` × 4、`mark_fulfillment`、角色/权限类），**两表都没有 `updated_at`**。→ 不存在任何证据链能判定某条署名由谁、在哪一侧后写入。方向的裁定只能靠业务口径，不能靠数据推理。
- 12 条「带署名 + 同日恰好 1 条视频」逐条对照：**11 条日报与视频完全一致，仅 1 条不一致**（2026-09-06 付洋洋 vs 严一鸣/黄玉清/严一鸣）。→ **历史上并没有真的把署名猜写到别的视频上**，该风险的危害面是前瞻性的（代码已修），存量无损伤。

### 4.2 已完成：补全 2 条「日报三列全空」的日报

动作：把绑定视频的三列署名复制到日报。因为日报三列是 `NULL`，**不覆盖任何已有数据，零损失**。

| 日期 | 作品 | 补入署名 | 视频侧 |
| --- | --- | --- | --- |
| 2026-09-19 | 新股全场MVP | 雷洋 / 雷洋 / 雷洋 | 一致 |
| 2026-09-20 | 板块之间分歧转一致 | 张学 / 张学 / 张学 | 一致 |

这两条正是「视频复盘里有、岗位管理里查不到」的同一类。执行后该类不一致归零。

- 脚本：`output/history-attribution-repair/apply.sh`（`output/` 已 gitignore）
- 快照与回滚：`before-targets-*.csv`、`revert-source-*.csv`、`revert-*.sql`
- 执行方式：单事务 `UPDATE 2`

**触发器前置核查（避免误伤）**：`daily_reports` 上有两个非内部触发器 ——
`preserve_daily_report_manual_source`（BEFORE UPDATE，仅当 SET 含 `data_source` 时触发，会把 `manual` 钉住）与
`trg_daily_reports_clear_fulfillment_absent`（AFTER UPDATE/DELETE，仅当 SET 含 `user_id`/`report_date` 时触发，会清同人同日的 `absent` 履约记录）。
本次只写三个署名列，**两者都不会触发**，`data_source` 与履约标记均未被改动。

### 4.3 署名保全：底账已落，未做任何改写

`output/history-attribution-repair/inventory-no-video-reports.csv` —— 461 条无视频日报的 id / 账号 / 日期 / 来源 / 三列署名全量导出，作为可回滚底账。

结论：**没有发现需要修复的损伤**。含署名的无视频日报保持原样，不改写、不迁移。

### 4.4 已完成：8 条「两侧都有值但冲突」已按「以日报为准」对齐

> 2026-09-22 授权后执行。本节先保留裁定前的原始观察（含已否证的假设），再记执行结果与回滚方式。

**裁定口径（合并语义）**：每一列取非空的那一侧；两侧都非空时取日报侧。
依据：8 条中 5 条（08-18、09-05 大A祖训、09-06、09-15、09-18）日报侧信息量更大 —— 日报区分出了独立的剪辑人或脚本人，视频侧把该列复制成了另一个人；另 3 条对称无偏向。统一走同一口径，避免逐条二义。
特例：09-15 日报的脚本/运营为 `NULL` → 按同一口径取视频侧值**补全日报**（合并语义下不会把已有署名抹成 NULL，不制造新的信息丢失）。

裁定前的清单（两侧原值）：

| 日期 | 作品 | 冲突列 | 日报侧 | 视频侧 |
| --- | --- | --- | --- | --- |
| 08-07 | 炒股不需要复杂的技术，也能赚到米 | 运营 | 江季芸 | 张学 |
| 08-18 | 龙珠上位先过一关，看懂离悟道不远了 | 剪辑 | 文叶帆 | 曾文聪 |
| 09-04 | 尾盘选股法 | 脚本 | 严一鸣 | 张皓宇 |
| 09-05 | 大A祖训 | 剪辑 | 文叶帆 | 江季芸 |
| 09-05 | 柚子选股法 | 脚本 | 张皓宇 | 严一鸣 |
| 09-06 | 09.07盘前：外围AI硬件大涨，周一又该如何应对 | 剪辑 | 文叶帆 | 曾文聪 |
| 09-15 | 揭秘，板块之间的分歧转一致 | 脚本 / 运营 | ∅ / ∅ | 张学 / 张学 |
| 09-18 | 9.17 明日如何演绎 | 脚本 | 黎金钊 | 陈海潮 |

（其余列两侧一致，未列出；09-15 的剪辑为 日报 文叶帆 / 视频 张学，亦属冲突。）

**观察（供裁定参考，不是结论）**：

- 8 条中 5 条（08-18、09-05 大A祖训、09-06、09-15、09-18）的**日报侧信息量更大**——日报区分出了独立的剪辑人或脚本人，视频侧则把该列复制成了另一个人（09-18 视频侧三列全同）。按「不销毁已有区分度」的原则，方向应是日报 → 视频。
- 另 3 条（08-07、09-04、09-05 柚子）**完全对称**：日报侧把某列复制成了 A，视频侧复制成了 B，各自丢掉一列区分度。无任何偏向。
- **已否证的假设**：「视频侧是批量粗放赋值、日报侧更细」。对照实测 —— 冲突账号下 视频侧三列全同 255/317（80.4%）、日报侧 158/211（74.9%）；全库 视频侧 1596/1840（86.7%）、日报侧 1111/1323（84.0%）。两侧粗糙度基本相同，差 ~5 个百分点，**不构成判定依据**。
- 署名直接驱动文案计费与运营绩效，误改有实际成本 → 故不自动对齐（这是当时不自动对齐的理由；本次是在拿到明确裁定后才执行）。

**执行结果（2026-09-22）**

| 日期 | 作品 | 目标值（脚本 / 剪辑 / 运营） | 实际改动 |
| --- | --- | --- | --- |
| 08-07 | 炒股不需要复杂的技术 | 张学 / 江季芸 / 江季芸 | 视频 运营 张学 → 江季芸 |
| 08-18 | 龙珠上位先过一关 | 曾文聪 / 文叶帆 / 曾文聪 | 视频 剪辑 曾文聪 → 文叶帆 |
| 09-04 | 尾盘选股法 | 严一鸣 / 张皓宇 / 严一鸣 | 视频 脚本 张皓宇 → 严一鸣 |
| 09-05 | 大A祖训 | 江季芸 / 文叶帆 / 江季芸 | 视频 剪辑 江季芸 → 文叶帆 |
| 09-05 | 柚子选股法 | 张皓宇 / 张皓宇 / 严一鸣 | 视频 脚本 严一鸣 → 张皓宇 |
| 09-06 | 09.07盘前 | 曾文聪 / 文叶帆 / 曾文聪 | 视频 剪辑 曾文聪 → 文叶帆 |
| 09-15 | 揭秘，板块之间的分歧转一致 | 张学 / 文叶帆 / 张学 | 视频 剪辑 张学 → 文叶帆；日报 脚本/运营 ∅ → 张学 |
| 09-18 | 9.17 明日如何演绎 | 黎金钊 / 陈海潮 / 陈海潮 | 视频 脚本 陈海潮 → 黎金钊 |

- 执行：`videos` 侧 `UPDATE 8`；`daily_reports` 侧 `UPDATE 1`（仅 09-15）。核验：**两表署名不一致 = 0**。
- 脚本：`output/history-attribution-repair/sync-conflicts.sh`（带 `DRY_RUN=1` 只读预览）
- 快照 / 回滚：`conflict-before-*.csv`、`conflict-revert-*.sql`（一条 `-f` 即可整体回退两侧原值）
- 触发器：本次同样只写三个署名列，`daily_reports` 的两个触发器均未触发；`videos` 表无触发器（已核）。

**一处保留的疑点（已执行，但值得记）**：09-04「尾盘选股法」与 09-05「柚子选股法」在两侧呈**交叉串位** —— 一侧给 A 作品脚本=严一鸣、B 作品脚本=张皓宇，另一侧正好相反。这说明**其中一侧整体错位**（不是简单的漏填），而两张表都没有 `updated_at`、`audit_logs` 也没有归属变更动作，无法判定偏移发生在哪一侧。本次按「以日报为准」执行；若日后从其他线索确认视频侧才是原始录入侧，用 `conflict-revert-*.sql` 一条命令即可翻回。

---

## 五、三处「猜视频」来源的清除情况

| 来源 | 状态 |
| --- | --- |
| 读侧 `src/app/api/video-submit/edit-detail/route-core.ts` | 已清除（`20260922180000` 批） |
| 写侧 RPC `public.update_collaboration_attribution` | 已清除（`20260922190000`，见 5.1） |
| 投稿主链路 `src/app/api/video-submit/route.ts` | **核查结论：不存在**。`existingVideo` 只按 `id` 取（`route.ts:448-452`），编辑绑定另有校验，没有「按账号+日期挑一条视频」的写法 |

### 5.1 `20260922190000_drop_guessed_video_attribution.sql`（已在生产执行）

删掉的是这段：日报没有 `video_id` 时，取同账号同业务日期 `published_at`/`uploaded_at` 最新的一条 active 视频覆盖署名 ——
与读侧完全同构，命中唯一一条时会把署名写到**另一条作品**上。删除后只同步「日报自己绑定的那条 active 视频」；未绑定时不碰任何视频、返回 `videoUpdated=false`（`handlers.ts:115` 本来就有该分支与提示文案，属设计内状态，不是新增故障态）。

- 执行前逐行 diff 生产 `prosrc` 与新定义：**只少了那 12 行，其余逐字未动**。
- 执行后核验：函数存在 / `security_definer=true` / EXECUTE 仅 `postgres,service_role`（无 `anon`/`authenticated`）/
  `guesses_video_by_account_date` 由 `t` 变 `f`；探针 service_role → `P0002`（函数体可达、零写入）、anon → `42501`（收口有效）。
- 回滚：重跑 `20260907113000_daily_reports_video_link.sql` 里的 `create or replace` 段即可还原旧定义。
- 单测：`src/lib/collaboration-attribution-migration.test.ts` 已改为锁定最新定义（含「不再按账号+日期猜视频」的反向断言）；全量 `npm test` **1756 通过 / 0 失败**。
- 执行脚本：`output/apply-drop-guessed-video-rpc.sh`（4 步：基线 → 单事务应用 → 核验 → 接口层探针）。

---

## 六、仍然开放的项

1. **归属补录在「日报未绑定视频」时没有界面反馈**：后端返回 `videoUpdated=false`，但前端不消费该字段（已核：全仓仅 `handlers.ts:114` 与其单测出现该 key），因此用户不会知道「视频那侧没同步」。要不要补一条界面提示，属产品决策。
2. 老 RPC 的 `report_date >= '2026-07-27'` 统计起点是否需要随业务前移 —— 属业务口径，本轮未动。


