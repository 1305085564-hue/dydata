# 权限架构优化批次 6 验收总结

日期：2026-09-19
状态：**BLOCKED（文档收尾完成，生产变更等待选项 A 执行窗口）**

## 一、本批完成项

### 数据库直连与线上只读取证

- 通过 `/opt/homebrew/bin/psql` 直连线上 PostgreSQL 17.6。
- `supabase_migrations.schema_migrations` 共 159 行。
- `pg_policies` 全库 263 条，`public` schema 261 条；其中直接依赖 `visible_user_ids` / `active_visible_user_ids` 的策略 32 条。
- 保存六个目标函数及相关 helper 的完整 `pg_get_functiondef`、签名、owner、`SECURITY DEFINER` 属性和 ACL。
- 保存 profiles、team、`membership_status`、`archive_snapshot`、`company_role` 索引和 group-mode 会话基线。
- `role` / `company_role` 映射不一致数为 0；group-mode 会话 19 条，全部 revoked，active 未撤销 0，expired 0。

### 真实角色与 RLS

- 真实 authenticated company_owner：`data_scope=team`，visible 52，active_visible 44。
- 真实 authenticated admin：`data_scope=team`，visible 52，active_visible 44。
- 真实 authenticated member：`data_scope=self`，visible 1，active_visible 1。
- 三类身份跨团队读取均被拒绝。
- admin/company_owner 可在 `visible` 看到归档快照成员，但 `active_visible` 排除归档成员；member 不可见。
- 非本人调用 `company_role_for_user` 被 `42501 permission denied` 拒绝。
- invalid/revoked group-mode token 返回 inactive，且没有扩大 visible 范围。

### 兼容 migration 与本地回归

- 候选文件：`supabase/migrations/20260919095135_permission_v2_reconcile_compat.sql`。
- 已补上 `role/company_role` 冲突拒绝：冲突时抛出 `22023 role/company_role conflict`。
- `has_group_owner_qualification`、`has_permission`、`is_admin`、`is_owner`、`is_admin_or_owner`、`get_data_scope` 统一调用冲突拒绝入口。
- 在真实数据库连接内做单事务加载、签名/ACL/RLS/列检查后回滚；11 个函数签名存在，ACL、RLS 数量和 `company_role` 列均未改变。
- 未登记 migration 版本，未持久化生产变更。
- 本地 migration/集团模式定向测试 18/18 通过；核心七文件权限回归 63/63 通过；TypeScript 和 diff check 通过。

## 二、证据文件

- `2026-09-19-permission-live-inventory.txt`
- `2026-09-19-permission-function-defs-acl.txt`
- `2026-09-19-permission-rls-policies.txt`
- `2026-09-19-permission-profile-group-mode.txt`
- `2026-09-19-authenticated-role-rpc-rls.txt`
- `2026-09-19-group-mode-authenticated.txt`
- `2026-09-19-permission-v2-transaction-rehearsal.txt`
- `2026-09-19-permission-v2-body-compare.txt`

## 三、阻断点

1. `20260819130100`–`20260819130800` 已登记，但线上目标函数仍是 `20260819120000` v2 语义；当前账本和函数定义无法证明是登记未执行，还是后续链外覆盖。
2. 没有独立 Supabase branch/project；本批只有线上连接内的可回滚事务演练，不能冒充隔离环境演练。
3. 线上没有 active 未撤销或 expired group-mode 会话，因此 valid/expired token 的真实 authenticated 允许/拒绝证据尚未闭环；本批只完成 invalid/revoked token 拒绝证据。
4. 未执行生产 migration 登记、RLS 写修复、部署、Ready/SHA 回查、页面/API 刷新复验或 `company_role` 删除。

## 四、决策点

### 选项 A：接受现状后执行兼容 migration

- 基于事务演练和真实三角色/RLS 证据，执行单个兼容 migration。
- 风险：迁移漂移来源仍未解释，无法证明历史登记和线上函数体之间的完整因果链。
- 选择状态：**阿禅已选择 A**；本文件只记录选择，不在本批执行生产写入。

### 选项 B：等待漂移解释后再执行

- 等待 Supabase 审计日志、Dashboard 变更记录或 DBA 解释 `130100`–`130800` 与函数体的差异。
- 风险最低，但需要外部证据和时间窗口。

### 选项 C：终止本轮并保持兼容窗口

- 保持双列、当前函数、RLS 和 ACL 不变，等待独立隔离环境就绪后另立批次。
- 不执行任何生产数据库写入。

## 五、本批停止点

- 本批只完成证据整理、文档更新和证据提交。
- 不执行 `supabase db push`、不登记 migration、不修改 RLS、不部署、不删除 `profiles.company_role`、不 push 远程。
- 当前状态：**BLOCKED，等待下一轮按选项 A 单独执行并验收。**
