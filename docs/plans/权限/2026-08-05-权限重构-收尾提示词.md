# 权限重构收尾 — Codex 提示词 V2（实测错误清单）

> 目标：`npx tsc --noEmit` 零错误 + `npm run build` 通过
> 模型：Sonnet 即可（纯机械修复，无需判断）
> 状态：已跑过 tsc，只剩 30 个文件 123 处错误

---

## 提示词

```
权限系统重构已完成核心模块，现在收尾 tsc 剩余错误。只剩 30 个文件，错误全是机械性的——三类：

## 错误分布

- TS1117（70处）：对象字面量重复属性名 → 删除重复行
- TS2322/TS2345（35处）：mock 对象缺 dataScope 或有旧字段 → 加 dataScope: "all"，删旧字段
- TS2353（13处）：对象含已删字段（accessLevel/groupId/businessRole）→ 删掉
- 其他（5处）：见下方逐文件说明

## 非测试文件修复（3 个，优先处理）

### 1. src/app/api/admin/dashboard-alerts/[alertId]/execute/route.ts
行 210：`actorRole: auth.actor.role` 重复了两次 → 删除其中一行

### 2. src/app/api/admin/panels/modules/route.ts
行 44：`data.teamManagement.groups` 不存在了 → 删除整行 `groups: data.teamManagement.groups.filter(...)` 以及上方 spread 中传播的 groups 字段

### 3. src/app/api/rewrite/skills/route.ts
行 95：`permissionInfo?.role === "owner" || permissionInfo?.role === "owner"` 重复判断导致类型收窄冲突 → 改为 `const isOwner = permissionInfo?.role === "owner";`

## 测试文件修复（27 个，批量处理）

所有错误模式统一，按以下规则批量修：

### 规则 A：重复属性（TS1117）
对象字面量里有两行同名属性（通常是旧版 + 新版都保留了）→ 删除重复的那行（保留含新值的那行）

### 规则 B：mock 的 AdminActor 缺 dataScope（TS2322/TS2345）
所有 mock 的 actor 对象加：`dataScope: "all" as const`
删除已不存在的字段：`accessLevel`、`groupId`、`ledGroupIds`、`businessRole`

### 规则 C：mock 的 UserPermissionInfo 缺 dataScope（TS2322）
所有 mock 的 permissionInfo 对象加：`dataScope: "all" as const`
删除：`accessLevel`、`groupId`、`ledGroupIds`、`businessRole`

### 规则 D：mock 的 DataAccessScope 含旧字段（TS2353）
删除 `accessLevel`、`groupId` 字段
`kind` 值如果是 `"group"` → 改为 `"team"`

### 规则 E：旧导入名（TS2724）
`isProductionManagerBusinessRole` → 改为 `isProductionManagerRole`

### 测试文件清单（逐个过）

1. src/app/(app)/admin/权限管理.test.ts — 规则 A + B
2. src/app/(app)/admin/首屏取数.test.ts — 规则 A
3. src/app/api/admin/cockpit/queue-overview/route.test.ts — 规则 A
4. src/app/api/admin/collaboration/attribution/route.test.ts — 规则 A + B
5. src/app/api/admin/collaboration/person/route.test.ts — 规则 A + B
6. src/app/api/admin/content-observations/route.test.ts — 规则 B + D
7. src/app/api/admin/content/list/route.test.ts — 规则 A + B + C
8. src/app/api/admin/dashboard-alerts/[alertId]/execute/route.test.ts — 规则 A + B
9. src/app/api/admin/dashboard-alerts/route.test.ts — 规则 A + B
10. src/app/api/admin/execute-tool/route.test.ts — 规则 A + B
11. src/app/api/admin/fulfillment/_shared.test.ts — 规则 A + B
12. src/app/api/admin/member-ai-suggestion/route.test.ts — 规则 A + B
13. src/app/api/admin/modules/team-management/route.test.ts — 规则 A + B
14. src/app/api/admin/panels/analytics/route.test.ts — 规则 A + B + C
15. src/app/api/admin/panels/modules/route.test.ts — 规则 A + B
16. src/app/api/admin/settings/thresholds/route.test.ts — 规则 A + B
17. src/app/api/admin/sidebar-badges/route.test.ts — 规则 A + B
18. src/app/api/admin/system/settings/route.test.ts — 规则 A + B
19. src/app/api/admin/videos/list/route.test.ts — 规则 A + B
20. src/app/api/dashboard/sample-quality-check/route.test.ts — 规则 A + B
21. src/app/api/exemptions/review/route.test.ts — 规则 A + B
22. src/app/api/production/_shared.test.ts — 规则 A + E
23. src/app/api/violations/route.test.ts — 规则 A + B
24. src/lib/admin-tools/index.test.ts — 规则 B
25. src/lib/admin-tools/user-management.test.ts — 规则 B
26. src/lib/current-permission-context.test.ts — 规则 B + C
27. src/lib/data-access-scope.test.ts — 此文件已被删除，需要从头重写（见下方）
28. src/lib/loaders/admin-modules.test.ts — 规则 A + B
29. src/lib/loaders/analytics-page.test.ts — 规则 B + C
30. src/lib/team-management.test.ts — 已重写完毕，跳过
31. src/lib/topics/service.test.ts — 规则 B

## data-access-scope.test.ts 重写指南

此文件被删了需要补回。写一个新的测试文件覆盖核心逻辑：

```ts
// src/lib/data-access-scope.test.ts
// 测试 buildDataAccessScope 的三种 data_scope 路径（self/team/all）
// 测试 canAccessOwner / filterRowsByDataScope 工具函数
// mock 的 profile 使用新字段格式：{ id, role, permissions, data_scope, team_id }
// 不使用 group_id、access_level、business_role
```

## 验证

1. `npx tsc --noEmit` → 0 errors
2. `npm run build` → 成功
3. 如果 build 过程有 unused import lint 错误也一并修

## 约束

- 不改 src/types/index.ts
- 不改 src/lib/permission-utils.ts
- 不改 src/lib/permissions.ts
- 不改 src/app/api/admin/auth-helper.ts
- 不改 src/lib/data-access-scope.ts（除了重建测试文件）
- 不改 src/lib/team-management.ts（已完成）
- 不新建文件（除了 data-access-scope.test.ts）
```
