# 废弃代码清理方案

> 2026-07-25 · 目标：根治项目中所有废弃/未上线/孤立代码

---

## 前置说明

- 每个批次独立可执行，做完一批 build 验证一次
- 删文件前先 `git stash` 或建分支，方便回滚
- 所有删除操作用 `git rm`，保留 git 历史

---

## 批次一：video-review 模块（35+ 文件）

### ⚠️ 不能删的（有外部引用）

| 文件 | 原因 |
|------|------|
| `src/lib/video-review-thresholds.ts` | admin/settings、admin/content、admin/settings API 三处引用 |
| `src/app/api/video-submit/route.ts` | dashboard/video-submit-form.tsx 和 submission-calendar API 引用 |

### 可安全删除的文件清单

**页面（6 个）：**
- `src/app/(app)/video-review/page.tsx`
- `src/app/(app)/video-review/submit/page.tsx`
- `src/app/(app)/video-review/exemption/page.tsx`
- `src/app/(app)/video-review/archive/page.tsx`
- `src/app/(app)/video-review/manage/page.tsx`
- `src/app/(app)/video-review/loading.tsx`

**组件（19 个）：**
- `src/app/(app)/video-review/components/` 整个目录

**容器与服务（6 个 + 测试）：**
- `src/app/(app)/video-review/approved-list-data-container.tsx` + `.test.tsx`
- `src/app/(app)/video-review/exemption/exemption-workbench.tsx` + `.test.tsx`
- `src/app/(app)/video-review/manage/review-queue-data-container.tsx`
- `src/app/(app)/video-review/submit/submit-container.tsx`
- `src/app/(app)/video-review/submit/submit-workbench.tsx`
- `src/app/(app)/video-review/actions.ts`

**video-review 目录下的测试文件（6 个）：**
- `approved-list-data-container.test.tsx`
- `exemption/exemption-workbench.test.tsx`
- `components/video-review-admin-actions.test.ts`
- `components/video-review-workbench.test.tsx`
- `components/video-review-tabs.test.ts`
- `components/approved-list.test.tsx`

**API 路由（2 个）：**
- `src/app/api/work-submissions/` 整个目录
- `src/app/api/work-screenshots/` 整个目录

**lib（1 个）：**
- `src/lib/loaders/video-review-page.ts` + `.test.ts`

**middleware 清理：**
- 删除 `src/middleware.ts` 中 `isVideoReviewRoute` 相关逻辑（第 89、91、182 行附近）
- 删除 matcher 中 `/video-review/:path*`

**migration（2 个）：**
- `supabase/migrations/20260707090000_video_review_production_accounting.sql`
- `supabase/migrations/20260723090000_video_review_thresholds.sql`

> ⚠️ migration 文件删除不影响线上已执行的表结构，但建议保留注释说明"已执行，文件已清理"

---

## 批次二：旧版 rewrite API（19 个文件）

整套旧版 `/api/rewrite/` 已被 `/api/content-tools/rewrite/` 完全替代。

**API 路由（14 个）：**
- `src/app/api/rewrite/` 整个目录删除（含 routes.test.ts）

**lib（删除 5 个，保留 1 个）：**
- ❌ `src/lib/rewrite/api-helpers.ts`
- ❌ `src/lib/rewrite/documents.ts`
- ❌ `src/lib/rewrite/skills.ts`
- ❌ `src/lib/rewrite/generation.ts`
- ❌ `src/lib/rewrite/bootstrap.ts`
- ❌ 上述文件的 `.test.ts`
- ❌ `src/lib/rewrite/v2-backend.test.ts`
- ✅ **保留** `src/lib/rewrite/shared.ts` — 新版 content-tools/rewrite 的 5 个 API 路由都依赖它

> shared.ts 不依赖同目录其他文件，可安全单独保留。其测试文件 `shared.test.ts` 也保留。

---

## 批次三：孤立组件和废弃 API（12 个文件）

### 孤立组件（6 个，零导入）

- `src/components/pk/pk-selector.tsx`
- `src/components/pk/pk-battle.tsx`
- `src/components/profile-edit-dialog.tsx`
- `src/components/notifications/notification-bell.tsx`
- `src/components/admin-layout/today-todo-drawer.tsx`
- `src/components/content-tools/mockData.ts`

> `src/components/pk/` 目录只有这 2 个文件，可整个目录删除。
> `src/components/notifications/` 还有 `feedback-detail-dialog.tsx`、`feedback-notification-bridge.tsx`、`notification-panel.tsx`、`notification-store.tsx`，只删 `notification-bell.tsx`。

### 废弃 API 和孤立文件（7 个）

- `src/app/api/admin/conversion-hub/` 整个目录删除（weekly、advice、pipeline-counts 均无调用者，_shared.ts 仅内部引用）
- `src/app/api/violations/[id]/test/route.ts` — 无任何调用者
- `src/app/(app)/admin/ai-rewrite/ai-rewrite-client.tsx` — 无 page.tsx，无导入

> `conversion-hub/` 目录下还有 `pipeline-counts/` 和 `_shared.ts`。经排查 `pipeline-counts` 也无外部调用者，`_shared.ts` 仅被同目录路由引用。三个废弃路由 + `_shared.ts` + `pipeline-counts/` 可一起删除，整个 `conversion-hub/` 目录清空。

---

## 批次四：备份和调试产物

### 备份目录
- `备份/` 整个目录删除（含 video-submit-form.tsx.bak、video-submit-panel.tsx.bak、用户删除备份）

### 调试产物
- `output/` 整个目录删除（截图、美学对比、网络抓包）
- `ui-contrast-lab/` 整个目录删除
- `.logorythia/` 整个目录删除
- `.zcode/plans/` 删除（保留 `.zcode/` 如果有其他用途）

---

## 批次五：一次性脚本归档

将 `scripts/` 下的一次性数据修复脚本移到 `scripts/_archived/`：

```
scripts/check_date_conflict.ts
scripts/check_dup_accounts.ts
scripts/check_dup_data_dates.ts
scripts/check_nicks_now.ts
scripts/fix_dup_final_v2.ts
scripts/fix_remaining_dups.ts
scripts/merge-db.ts
scripts/verify-ai-assistant.sh
scripts/verify-v2-tables.mjs
scripts/measure-ttfb.mjs
scripts/test-appeals-endpoint.mjs
scripts/test-supabase-auth.mjs
```

> 移动而非删除，万一以后需要查历史数据修复记录。

---

## 批次六：小修补

### toSafeString @deprecated
`src/lib/admin-tools/utils.ts` 中的 `toSafeString` 被 4 个 admin-tools 内部文件调用。两个选择：
- **选项 A**：把 4 处调用改成 `toTrimmedString`，然后删除 `toSafeString`
- **选项 B**：去掉 @deprecated 标注，保留现状（改动最小）

建议选 A，趁清理一并处理。

### migration 编号冲突
`051_cleanup_deprecated_tables.sql` 和 `051_leader_daily_reports.sql` 共享编号。建议：
- 确认两个都已在线上执行过
- 如果是，重命名其中一个为 `051b_` 或合并注释

---

## 执行顺序建议

```
批次一 → build 验证 → 批次二 → build 验证 → 批次三 → build 验证
→ 批次四（无需 build）→ 批次五（无需 build）→ 批次六 → 最终 build
```

每批做完跑一次 `npm run build`，确认零错误再继续。

---

## 风险评估

| 批次 | 风险 | 说明 |
|------|------|------|
| 一 | 中 | middleware 改动需仔细，video-submit API 保留 |
| 二 | 低 | 已确认无外部调用者 |
| 三 | 低 | 已确认零导入 |
| 四 | 无 | 非代码文件 |
| 五 | 无 | 移动而非删除 |
| 六 | 低 | toSafeString 替换需逐一改 |
