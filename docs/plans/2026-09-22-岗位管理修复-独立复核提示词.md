# 岗位管理修复 · 独立复核提示词（2026-09-22）

> 用途：把下面「给复核 AI 的提示词」整段复制给一个**全新的 AI 会话**（不共享本次上下文）。
> 复核对象：`bfc30490 → ec253536` 这 6 个提交 + 方案文档。

---

## 给复核 AI 的提示词（从此行以下整段复制）

你是独立复核人。有人（另一个 AI）刚修完一批岗位管理（工种小队）的缺陷，现在需要你**独立验证他说的和实际做的是否一致**。你没有他的上下文，请只依据仓库代码、提交记录和可运行证据下结论。

### 一、复核范围

仓库 `/Users/mac/Projects/dydata`，按顺序看这 6 个提交：

```
bfc30490  feat(collab): 按团队视图改用视频复盘快照绩效指标      ← 批次 0（并行会话产物，先提交）
93a6664a  fix(collab): 批量分配改为如实返回部分结果并按 id 增量回滚  ← 批次 1
3e281f2a  fix(collab): 清掉岗位管理里残留的「编制」用户可见文案    ← 批次 2
d002aa67  fix(collab): 成员多选面板改文档流展开并补齐无障碍与全选清空对称 ← 批次 3
14f7cb1c  test(collab): 修正多选断言的假通过与弱断言并标注绩效口径同源  ← 批次 4
ec253536  docs: 记录岗位管理修复批次 3/4 落地、走查证据与偏离项
```

先读方案文档：`docs/plans/2026-09-22-岗位管理遗留问题-修复方案.md`（尤其「七、执行记录」里的 3 条偏离和未覆盖项）。

### 二、必须独立验证的 7 件事

1. **批量分配的部分成功语义**：`src/lib/work-groups.ts` 的 `assignWorkGroupMembers`——部分人失败时是否**如实返回 failures 而不是整体失败**？客户端（`work-group-manage-drawer.tsx`）拿到 failures 后是否**只还原失败的人**、而不是整表回滚？请自己构造用例跑 `npx tsx --test src/lib/work-groups.test.ts` 验证，不要只看代码。

2. **回滚是否真的按 id 逆操作**：`snapshotWorkGroupSlots` / `rollbackWorkGroupSlots` 是否可能误伤没参与本次操作的人？有没有「用渲染期快照整表覆盖」的残留？搜 `work-group-manage-drawer.tsx` 里所有 `setLocalRoster` / `setLocalGroups` 的写入点，确认是否都走 `commitRoster` / `commitGroups`。

3. **批次 3 的 5 项是否真修好**（重点看有没有「改了但没生效」）：
   - C1 下拉面板是否真的脱离绝对定位（搜 `absolute` / `z-50` 残留）
   - C2 `aria-controls` 与面板 `id` 是否真的能对上（`isOpen ? panelId : undefined` 这个写法在关闭态是否合理）
   - C2 Esc 是否只关面板——实测证据是「焦点在面板内搜索框时按 Esc，面板关闭、抽屉仍在」，但 `stopPropagation()` 挂在面板的 `onKeyDown` 上，**焦点在触发器按钮或抽屉其它位置时是否仍成立**需要你判断（Sheet 若在 document 上监听 keydown，未必拦得住）
   - C3「全选结果 / 清空结果」是否与实现一致（都只作用于筛选结果）
   - C4/C5 乐观新建组：拿不到 team_id 时不做占位、temp id 换真实 id 时 `setSelectedGroupId` 是否同步

4. **测试质量**：批次 4 说修了两条假通过/弱断言。请判断：
   - 新的 `/(?<!un)assignWorkGroupMemberAction\(/` 是否真的能挡住回潮？
   - `assert.doesNotMatch(manageDrawerSource, /next\/navigation/)` 是否只是「换个说法继续 grep 源码」？这类源码字符串断言**是否值得存在**，还是应该改成行为测试？
   - `work-group-workbench.test.ts` 全文 80+ 处 `readFileSync` + `assert.match` 属于源码 grep 式断言，请评估**其中哪些是真断言、哪些是假通过**（假通过 = 删掉被测代码后测试仍然通过）。

5. **口径是否被改坏**：批次 0 把「按团队」指标改成视频复盘 24h 快照口径（先加总再相除的加权口径）。请核对 `src/app/api/admin/collaboration/_shared.ts` 的 `buildPerformanceMetrics` 与 `docs/数据口径.md`、`src/lib/video-metrics.ts` 是否一致，**条均播放是否真的用「快照作品数」而不是 reportCount**。

6. **有没有引入死代码 / 冗余**：改动涉及的 6 个文件里有没有没人调用的新函数、留了没用的 prop、或者删了还在被引用的东西？跑 `npx tsc --noEmit` 和 `npx eslint` 复核。

7. **「编制」是否真的清干净**：`grep -rn "编制" src/` 看剩余项是否都只是代码注释/测试名，有没有会 toast 或显示给用户的漏网之鱼。

### 三、门禁命令（请自己跑，不要相信文档里的结论）

```bash
npx tsc --noEmit
npx eslint --no-cache <改动文件>
npm test                      # 期望 1770 通过 / 0 失败
```

### 四、禁区（违反即视为复核无效）

- **不要 push**（push main 会触发生产部署，必须阿禅明确点头）
- **不要动数据库、权限、RLS、不要加 migration**
- **不要顺手改代码**：你只做复核，输出问题清单即可；确需验证时可以跑命令、读代码，但不要修改业务代码
- 不要因为「文档这么写的」就采信，一切以代码和可运行证据为准

### 五、输出格式

按严重度分四档给结论，每条必须带**文件路径 + 行号 + 证据**：

| 档位 | 含义 |
|---|---|
| 完整修复 | 原问题确已修好，且有证据 |
| 止血未完整 | 表面好了但根因还在 / 只覆盖了一部分场景 |
| 预存问题 | 不是本次引入，但确实存在 |
| 新增问题 | 本次提交引入的新缺陷 |

最后回答一句：**这 6 个提交可以合并进 main 吗？**（可以 / 有条件可以（列出前置条件）/ 不可以（说明原因））
