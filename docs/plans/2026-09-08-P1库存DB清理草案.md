# P1 库存 DB/RPC 清理草案（2026-09-08 核查产出）

> 状态：仅核查与草案，未执行任何生产 SQL。执行前提：对应应用代码已具备部署条件（禁止数据库先行），逐条人工确认后再上。
> 取证方式：全仓 grep `src/`、`scripts/`、`supabase/migrations/`、`docs/`、`日志/`；migration 历史按文件名时间序核对最终定义。

## 一、核查结论修正（与待办清单原口径的差异）

1. **`violation_cases` 不是死表，禁止 DROP**。`src/lib/conversion-hub/service.ts` 的 `assertOpenCase` / `findOrCreateConversionCase` 仍在读写它（`purpose='conversion'` 的下粉话术案例），调用链：`/api/video-submit`（`follower_convert > 0 && script_text` 时调 `replaceDailyReportUsageRecord`）→ `prepareUsageRecord` → `violation_cases`。`src/app/api/video-submit/edit-detail/route-core.ts` 还按 `script_usage_records.case_id → violation_cases` 做 select join。模块地图"conversion-hub 被 video-submit 依赖不能删"的记载与代码一致；待办"6 张避坑案例表已无写入方"对 `violation_cases` 而言**不成立**（它同时是避坑案例表和话术转化案例表，后者仍活跃）。
2. **`queue-overview` 没有同名 RPC**。历史对象是已删除的 API 路由 `/api/admin/cockpit/queue-overview`（commit `0295de25` 删除，直接表查询、无 RPC）。待办所指"仍读 violation_cases 的 RPC 本体"实际是 `admin_cockpit_summary` 家族（见下）。
3. **`useRewriteV3Logic` 的 fetch 已不再是死链**。所列 rewrite 路由全部存在且 HTTP 方法匹配（`/api/rewrite/documents/[id]/history`、`/paragraphs`、`/revisions`、`/api/rewrite/paragraphs/user-edit` 等由 2026-09-03 `196f7288 fix: rewrite API路由修复与权限加固` 补齐）。原"约 8 处指向不存在路由（运行时 404）"口径作废。

## 二、可安全清理（DROP 草案，需人工确认 + 保留期核对后执行）

### 2.1 孤儿 RPC（代码零调用方，已核实）

```sql
-- 前置探测（只读）：确认线上确实无调用方可先查 pg_stat_statements / 控制台日志
-- drop 前必须逐条确认，禁止一把梭

-- (1) admin_analytics_first_screen —— 随 /admin/analytics 经营分析下线（2026-08-22）
drop function if exists public.admin_analytics_first_screen(
  uuid[], uuid, text, text, date, date, boolean, date, date
);

-- (2) admin_sidebar_badges_summary —— sidebar-badges 轮询链 2026-08-26 已删，RPC 无调用方
drop function if exists public.admin_sidebar_badges_summary(date, uuid[]);

-- (3) 旧指挥台队列 RPC 家族 —— 调用方（/api/admin/cockpit/*、admin-cockpit 组件）已于 2026-08-25 全删；
--     src 与其余 migration 均无引用（v2 变体亦无任何 SQL 调用方，一并清理）
drop function if exists public.admin_cockpit_summary(date);
drop function if exists public.admin_cockpit_summary_v2(date, text);
drop function if exists public.admin_pending_submissions_today(date);
drop function if exists public.admin_pending_submissions_today_v2(date, text);
drop function if exists public.admin_pending_videos_today(date, int);
drop function if exists public.admin_pending_violations(int);
```

注意：
- `admin_first_screen_perf_regressions` **保留**——现役 `/api/admin/first-screen-monitor` 在用。
- `is_admin_or_owner()` **保留**——履约链路多个现役函数仍在调用。
- drop 后需刷新 PostgREST schema cache。

### 2.2 真死表（全仓零引用，已核实）

```sql
-- 前置只读探测：行数与最近写入时间，与阿禅确认保留期后再 drop
-- select count(*) from public.knowledge_cases;  -- 及各子表

-- knowledge_cases 家族（2026-06-28 重构遗留；2026-08-19 曾专门补建基表但 src 无任何引用）
-- 子表对 knowledge_cases 有 FK，按序删除
drop table if exists public.case_usages;
drop table if exists public.knowledge_case_status_logs;
drop table if exists public.knowledge_case_taxonomy_links;
drop table if exists public.case_taxonomies;
drop table if exists public.knowledge_cases;

-- violation_test_records（055 违规系统 V1 遗留，src 零引用）
drop table if exists public.violation_test_records;

-- smart_alert_claims（smart-alert 2026-08-25 整体删除，表遗留；P0 对账确认线上仅有表/索引）
drop table if exists public.smart_alert_claims;
```

### 2.3 明确保留（勿动）

- `violation_cases`、`violation_events`、`script_usage_records`：话术转化链路现役（见一.1）。
- `review_violations` 权限键：豁免授权链路（`exemption-permissions.ts`、`conversion-hub` 权限判断、原子豁免 RPC `has_permission('review_violations')`）仍读写。
- middleware `/violations/:path*` matcher 与 `isViolationsRoute`：`src/middleware-clear-site-data.test.ts` 显式断言 matcher 包含该路径，且模块地图 B.3 记载"命中只会被正常登录闸机处理"；删除需同步改测试，属低风险但非零成本，待办已定"随 DB 清理一并处理"，本批不动。

## 三、执行顺序建议

1. 先只读探测（行数、最近写入、pg_stat 调用统计）→ 阿禅确认保留期。
2. 出一份 migration（只新增，不改旧文件），包含 §2.1 + §2.2，先随应用代码部署、再执行 SQL、刷新 schema cache。
3. 执行后回归：`/api/admin/first-screen-monitor` 手动触发一次（cron secret），确认无引用已删函数；导航栏豁免抽屉、履约、日报提交各抽查一次。
