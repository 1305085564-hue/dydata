---
name: 数据填报美学施工进度 · 2026-05-11 · P2
description: P2 管理端列表行 + 状态点 + 分页器 + 日历 + 数字档位收敛
type: project
scope: dydata
---

# 数据填报美学施工进度 · 2026-05-11 · P2

## 已完成（P2 · 管理端列表行 + 状态点 + 分页器 + 日历 + 数字档位收敛）

| 改动 | 文件 | 依据 |
|---|---|---|
| 所有卡内主数字 `text-[20/24]px font-semibold` → `text-[18px] font-medium`（Title 档） | hit-analyzer / personnel-analysis / time-analysis / ai-insight / analytics-modal-panel / 视频结论卡 / 视频结论卡-单卡 | 法典 C.3 |
| eyebrow `text-[11/12]px font-semibold tracking-wider` → `text-[10px] font-medium uppercase tracking-[0.25em]` | hit-analyzer（12 处）/ time-analysis | 法典 Eyebrow 标准 |
| submitter chip 行升级：bg-white + hover 整套 translate/shadow + transition 枚举列 | hit-analyzer.tsx:838-867 | 法典 E.3 列表行 |
| 分页器重构：当前页 `border-[#D97757]/40` 描边、其他 zinc-500、`h-7 min-w-7` + hover 升起 | hit-analyzer.tsx:submitter pagination | 法典 E.4 分页器 |
| 日期 Input 加 `[accent-color:#D97757]` 影响 native picker | hit-analyzer.tsx:date inputs ×2 | 法典 E.7 日历（native 回退） |
| 筛选文案样本列表行：`border-l-[2px] border-l-transparent hover:border-l-[#D97757]` + bg-[#FAFAFB] → hover:bg-white | hit-analyzer.tsx:stats.contents | 法典 E.3 + E.6 |
| personnel 人员卡语义左导轨：suggestion 四色（重点关注 Critical / 继续放量 Success / 波动异常 Pending / 保持观察 透明），移除全边框、改 `border-l-[2px]` | personnel-analysis.tsx:48-58 + 265-270 | 法典 B.3 四语义色 + E.6 左导轨 |
| time-analysis 热力网格配色：`#C9604D` → `#D97757`（Primary 暖橙），保留 `#D99E55` 作次高档；推荐窗 ring / pin 点 / 图例同步 | time-analysis.tsx:getIntensityClass + 推荐卡 + 图例 | 法典 B.3（#C9604D 仅用于异常/告警，不用于"正向信号"） |
| focus-visible ring 从 `ring-zinc-900/40` → `ring-[#D97757]/40`；pinned ring zinc-900 → `#D97757` | time-analysis.tsx:394-400 | 法典 禁蓝感 focus ring |

## Build / Lint 验收

- `pnpm build` Turbopack 在本机 node_modules symlink 环境报 panic（pre-existing 环境问题，非我引入），改跑类型检查 + lint
- `pnpm tsc --noEmit` 对 analytics/* 零新增错误（仅 pre-existing 测试文件 TS5097）
- `pnpm lint`：改动前后 **56 问题（29 errors / 27 warnings）** 数量一致，零新增
- analytics/* 的 3 个 warnings（react-hooks/exhaustive-deps × 2、`<img>` × 1）均为 pre-existing，不在本次扫盲范围

## Grep 清单（全部零命中）

| 模式 | 范围 | 命中 |
|---|---|---|
| `text-\[20px\]` | src/app/(app)/admin/analytics/ | 0 |
| `animate-pulse` | admin/analytics + admin/market + admin/content | 0 |
| `ring-blue / focus-visible:ring-blue / ring-zinc-900/40` | admin/analytics | 0 |
| `tracking-wider`（旧 eyebrow） | admin/analytics | 0 |

## P2 放弃项（记录 + 理由）

- **personnel-analysis 卡片里的 `text-[58px] font-semibold` 装饰数字**：它是 zinc-50 半透明位置偏移装饰（absolute -right-2 -top-4），承担"排名气泡"角色而非内容数字，保留不计入 Display 名额（法典 A.7 克制惊喜）
- **hit-analyzer 内 `<Input type="date">` 的 picker 面板本体**：base-ui/react 封装的 native date，picker UI 由浏览器渲染，只能通过 `accent-color` 影响选中高亮；shadcn `<Calendar/>` 换装需单独评估（施工方案未要求 P2 换实现）

## P3（整站 grep + 视觉回归）

按施工方案 §6 在新窗口展开：
1. 全站 `text-[20px] / text-[24px]` grep，定位所有 Display / Title 违反档位的文件
2. Storybook / Playwright 截图比对 analytics / market / content 三大管理端页面
3. 验证"每屏最多 1 处 Display（32px）"的唯一性
