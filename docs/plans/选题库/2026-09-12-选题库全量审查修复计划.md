# 选题库剩余验收项

> 状态：主体修复、生产 migration、真实角色闭环与发布已完成。已完成契约见 `docs/reference/选题库接口文档.md`，实施与验收证据见 `日志/2026-09-13.md`。本文只保留尚未消除的真实工作。

## 一、全仓静态门禁（范围外阻塞）

- [ ] 单独修复 `src/lib/animations.ts:229` 的 effect 内同步 `setState` lint 错误。
- [ ] 修复后重跑 `npm run gate:static`；只有命令退出 0 才能宣布全仓静态门禁通过。

## 二、全量迁移链重建（历史 migration 阻塞）

- [ ] 在独立数据库治理任务中修复历史 `062_ai_unified_config_view.sql` 对缺失 `public.rewrite_length_presets` 的依赖。
- [ ] 从空库重跑 `npx supabase db reset`，确认全部历史 migration 可重放。
- [ ] ~~不回写已上线的历史 migration，用新的前向修复迁移收口。~~（2026-09-13 审查修正：前向迁移救不了 db reset——重放时 062 本身先失败，新迁移轮不到执行。正确做法是直接编辑 062 删除引用 `rewrite_length_presets` 的 UNION 分支；该视图无应用代码消费，062 本就不可重放，回写无额外风险。动手前先查线上：`src/lib/rewrite/shared.ts` 仍在查该表，线上该表可能实际存活。详见 `docs/待办清单.md` P2。）

## 三、旧 `/claim` 兼容路由退场

- [ ] 获得可覆盖迁移周期的精确路由流量证据；当前 Vercel 套餐无法提供所需的 30 天精确路由指标。
- [ ] 确认零调用后删除 `/api/topics/sub-topics/[id]/claim`；在此之前保留废弃标记，不用猜测替代流量证据。

## 四、尚未获得的补充页面证据

- [ ] 生产数据达到 50 条以上或准备不污染业务数据的专用验收库后，再做一次真正跨页的 Slow 3G J/K 浏览器验收。当前生产可见选题只有 32 条，已完成同页 20 次连续 J/K 和 deferred promise 跨页自动化回归。
- [ ] 如后续具备专用 archived/未入团浏览器账号，补跑页面入团边界。当前已有生产 RLS 角色模拟与可执行路由测试，不创建或重置 Auth 账号来补证据。

## 五、独立工作树门禁

- [ ] 处理其他任务留下的 `日志/2026-09-12.md` 尾部空行后，重跑全局 `git diff --check`。Topics 范围的 `git diff --check` 已通过。
