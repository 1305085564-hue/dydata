# AI 管理助手清理错搬功能执行方案

> 生成时间：2026-05-08
> 执行代理：Kimi
> 背景：上一轮视觉改造时把文案助手（/content-tools/rewrite）的 Workspace Config（口语/专业/极简 tone + 深度推理/简短回答/引用来源 extras + session context）错搬到了 /admin/ai-assistant。
> 管理助手定位是「后台执行 Copilot」（查用户 / 改权限 / 重跑任务 / 诊断后台问题），根本不需要聊天口吻切换。
> 证据：前端 POST body 塞了 tone/model/extras，但后端 `src/app/api/admin/ai-assistant/` 和 `src/lib/admin-ai/` 完全没接收 — 死装饰。

## 管理助手应有的样子（对齐原设计）

按 `/Users/mac/.claude/projects/-Users-mac-Projects-dydata/memory/站内AI助手-完整方案.md`：

- 对话区（主气泡 + 明细卡）
- 确认卡（高危操作二次确认）
- 操作历史侧边栏
- 详情抽屉（点历史条目打开）
- 调试折叠区（仅 owner 可见）

× **不需要**：tone 切换、extras 开关、模型选择器、session context 提示框、Workspace 概念

## 要做的事

### Step 1：删整个 Workspace Config 面板

**删除文件**：
- `src/app/(app)/admin/ai-assistant/workspace-config-panel.tsx`（整个删掉）

### Step 2：清理 ai-assistant-client.tsx

**删除**：
- `import WorkspaceConfigPanel` 相关行
- `DEFAULT_CONFIG` 常量
- `const [config, setConfig] = useState<WorkspaceConfig>(...)`
- `const [configOpen, setConfigOpen] = useState(...)`（如有）
- 顶部状态栏里的 `{config.tone} · {config.model.split(...)}` 展示
- 顶部右侧的"配置栏"按钮
- 右侧 Workspace 栏整块（只保留左侧历史 + 中间对话的两栏布局）
- 任何与 config 相关的 prop 传递

**最终布局**：
```
┌──────────────────────────────────────────┐
│ 顶部 Header                              │
│ - 左：历史侧边栏展开/收起按钮 + 标题     │
│ - 右：「新对话」按钮                     │
├──────────────────────────────────────────┤
│ 左栏：History (200px, 可收起)            │
│ 中栏：Chat Panel (flex-1)                │
└──────────────────────────────────────────┘
```

### Step 3：清理 chat-panel.tsx

**删除**：
- `import type { WorkspaceConfig } from "./workspace-config-panel"`
- `config: WorkspaceConfig` prop
- 函数签名里的 `config` 参数
- POST body 里的 `tone: config.tone` / `model: config.model` / `extras: config.extras`（第 262-264 行）
- 底部 footer 里的 `{config.tone}` 和 `{config.model.split(...)}` 展示（第 522 / 583-586 行）
- 底部快捷键提示里任何提及配置/workspace 的文案

**保留**：
- 消息气泡渲染（user 右气泡 / AI 左气泡 / 确认卡 / 结果卡）
- 输入框（阿禅美学 rounded-[20px] + #D97757 发送按钮 不变）
- MarkdownContent 渲染
- AssistantDetailSections 渲染

### Step 4：API 层清理（防御性）

**文件**：`src/app/api/admin/ai-assistant/route.ts`

查 request body 解析部分，如果有 `tone / model / extras` 的解构或 zod schema 字段，一并删掉（避免未来接入时又误用这些概念）。如果本来就没接，跳过。

### Step 5：视觉层保持不变

× 不改颜色（#D97757、矿物色、#FAFAFB 全保留）
× 不改圆角（rounded-[14px] / rounded-[20px] / rounded-[10px] 全保留）
× 不改历史侧边栏内部样式
× 不改确认卡样式

**只是拿掉 Workspace 栏整块 + 顶部配置按钮 + 顶部 tone 标签**，其它视觉元素原封不动。

### Step 6：更新快捷键提示

chat-panel 底部的快捷键提示里，如果有「⌘K 切换工作区配置」之类，删掉或换成管理助手真正有用的提示（比如「Esc 关闭历史」「Enter 发送 / Shift+Enter 换行」）。

## 验收标准

1. `/admin/ai-assistant` 打开后，右侧没有 Workspace 面板，顶部没有"口语 · sonnet 4.6"这类标签，没有配置按钮
2. 只有两栏：左历史 + 中对话
3. 发消息链路正常（POST /api/admin/ai-assistant 成功返回）
4. 高危操作确认卡仍能弹出
5. 历史侧边栏正常展示
6. 详情抽屉点击后正常打开
7. 视觉层与上一轮完全一致（颜色、圆角、hover 微交互）
8. `npm run build` 通过
9. 既有测试（core.test.ts / presentation.test.ts / history-sidebar.test.ts / chat-errors.test.ts / assistant-detail-sections.test.tsx）全通过

## 不要做

× 不改 presentation 层（上一轮刚清理完，别动）
× 不改 migration / 数据库
× 不改 rewrite 工作台（那是文案助手的家，tone/extras 在那里是合理的）
× 不改阿禅美学标准
× 不自动 git commit / push
× 不动其它未提及的文件

## 回滚

- 改前先创建分支 `fix/admin-ai-remove-workspace-config`
- 如验收不过：`git checkout main` 即回滚
