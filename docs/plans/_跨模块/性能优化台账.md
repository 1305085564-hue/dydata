# 性能优化台账

> 这是性能优化历史 changelog，默认不随任务加载。
> 使用边界：本文件只记录已发生的测量和改动，不构成当前施工授权；新任务先按开工分诊命中现行文档，再按需查历史。
> 需要查历史实测数据时才读本文件。
> 最后更新：2026-08-30

---

| 日期 | 页面 | 首屏 | 请求数 | 最慢接口 | 归因 | 改了什么 | 执行 |
|---|---|---|---|---|---|---|---|
| 2026-08-30 | 全站 sweep 后四项修复：协作页人卡、admin/content 全量加载与状态去重、Topics 详情抽屉、导航豁免角标 | 修复后 `/admin/content` 页面请求数 5→2、判定由"单接口超标"转为通过（最慢接口 topic-library-status 20 条 1.09s，回到该页 1.2s 预算内）；协作页人卡 person 请求 2→1 次（1.91s，稳定 3.14s→2.55s）；Topics 详情抽屉请求 3→2（detail 1.79s + claims 1.33s，作品不再查两次） | /admin/content 首屏不再自动拉 full list（3.92s 冷缓存成本转为用户点击后触发）；topic-library-status 400 条重算只在点击加载全量后发生 1 次 | detail/claims/person/attribution 接口自身 1.3–1.9s 仍超 800ms 预算，属服务端既有延迟，本轮未动 | 导航豁免角标改为延后 2.5s 拉取 + 60s 模块缓存，不再与页面首屏争抢带宽；服务端注入方案会阻塞所有页面 shell TTFB，已评估并放弃 | ✅四项前端修复：`person-data.ts` 进行中 Promise 共享；`content-list.tsx` 删除 full list 自动加载 effect 改显式按钮，`content-page-client.tsx` 状态请求按 ID 签名去重 + AbortController + 入库操作后强制重算；`TopicWorkBreakdownDrawer.tsx` 作品统一读 detail.works 并删除 worksError 死代码；`nav-bar-client.tsx` 角标延后拉取。1255 测试、tsc、lint 0 error、生产构建全过，真实登录复测行为正确。仅本地生产构建，未部署 | ZC |
| 2026-08-30 | 全站 12 页 + 13 个抽屉/弹窗加载速度 sweep（company_owner 登录态） | 本地生产构建各页 FCP 0.37–1.23s、完整加载 0.6–1.4s，页面首屏与完整加载全部达标 | 每页 1–9 个业务请求，全部远低于预算；无 4xx/5xx、无控制台 error | `/admin/content` list 冷缓存 **3.92s**、topic-library-status **1.94s**（且随 full list 重复触发 2 次）；`/admin/videos` list **3.43s**；协作页 person 接口单击重复请求 2 次（1.24s+2.42s）；Topics 详情抽屉 detail/works/claims 1.28–1.54s；`/api/exemptions/pending` 全站每页 0.79–0.93s；dashboard 挂载即拉 operator-members 0.90s；growth trend/leaderboard 1.07–1.13s；rewrite bootstrap 1.78s；`/topics/[id]` 挂载 7 请求含 pool 1.70s | 慢点集中在“hydration 后自动追加重接口”与“弹层打开后的并行慢接口”，页面骨架本身不慢；重复请求两处（collaboration person、admin/content status）符合体检报告已记录的预存问题 | 本轮只诊断不改业务代码；新增一次性脚本 `scripts/perf-full-sweep.mjs`（未提交，可复跑）。仅本地生产构建，未宣称 Vercel 通过 | ZC |
| 2026-08-30 | 双层性能门禁首轮：`/topics`、`/dashboard`、`/admin/content`、`/admin/collaboration` | 最终正式命令本地生产构建 FCP：0.71s / 0.23s / 0.64s / 0.67s；完整加载 0.73s / 0.54s / 0.78s / 0.78s | 1 / 2 / 5 / 1；无重复业务请求、4xx/5xx 或控制台 error；Topics 为 0 个 `/api/topics/*` | 0.49s / 0.50s / **2.91s** / **0.94s** | 确定性正确性规则已稳定；admin/content 全量后台加载仍明显超 1.2s，协作页本轮略超 0.8s | ✅新增 `gate:static` 与 `gate:browser`；确定性规则硬阻断，时间预算先报告。浏览器产物不保存 trace/截图/视频/失败页面快照/登录态，只写去敏 JSON；仅本地生产构建，未宣称 Vercel 通过 | CX |
| 2026-08-30 | `/admin/content` topic-library-status 431 修复 | 本地生产模式：400 条状态 POST 约 1.24s | 页面先 20 条、full 后 400 条，各 1 次 | 400 条 POST 约 1.24s | 400 个 UUID 放入 GET URL 导致 HTTP 431，前端静默吞错 | ✅正确性已完整修复：固定 URL + POST JSON，GET 保留兼容，失败明确提示；真实登录复测两次均 200、无控制台错误。400 条仍超 800ms，保留性能债；未部署、未宣称 Vercel 通过 | CX |
| 2026-08-30 | 全站公共加载体检：`/topics`、`/dashboard`、`/admin/content`、`/admin/collaboration` | 本地生产模式登录态：Topics FMP 1.12s；Dashboard FMP 0.88s；admin/content DCL 0.66s；admin/collaboration DCL 0.77s（后两页 FMP 未稳定取得） | Topics 首屏 0 个 topics API；Dashboard 3 个挂载业务请求；admin/content 首屏后自动 full + 状态等请求；collaboration 2 个挂载请求 | Topics 详情 detail/works/claims 828–951ms；admin/content 全量状态 GET 返回 431 | 共性为 profile/accounts/scope 重复读取；局部问题分别是提前加载、重复作品查询、重复月报扫描、超长 GET | 本轮只诊断不改业务代码；新增 `docs/reference/全站公共加载链路体检-2026-08-30.md`，修正模块地图的 Topics 与缓存现状。Vercel 生产未宣称通过 | CX |
| 2026-08-30 | `/topics` 首屏 | 本地生产模式实测：首屏内容随页面返回，load/FCP 约 1.1s；此前数据完整可用约 2.1s | **0 个 `/api/topics/*` 首屏请求**；筛选 1 个 pool，刷新 2 个接口 | 筛选后的 `/api/topics/pool` 约 1.0s（登录/权限约 0.3s，选题查询约 0.1s） | 首屏原先分别拉 active、options、pool、my_claims，重复鉴权和统计；开发模式还会重复挂载请求 | ✅已优化：服务端页面复用已确认的身份/权限直接注入首屏数据；保留 `/api/topics/bootstrap` 作为客户端兜底；客户端请求去重；topics API 复用请求级用户上下文；刷新不再重复拉母题；新增契约/服务测试。仅本地生产模式验证，未写成生产通过 | CX |
| 2026-08-26（第二批） | `/topics` 首屏接口 | 复测（同环境 leader 账号）：active 中位 ~0.55s（首批 0.58s / 原始 1.04s），其余接口与首批持平 | 5 个不变 | `/api/topics/active` | 死数据拖慢：阿禅指出「今日聚焦」卡片 2026-08-15 已下线，但 active 接口仍在为其计算 focusTopics/worthRedoing/recentlyCreated 三块前端零消费的数据（全量扫 sub_topics + 作品快照排序），上一批优化保错了对象 | ✅已优化：active 接口只保留团队动态两组数据（claims+最近作品两路并行，works 查询去掉快照 join 和 limit*3 全量扫）；删除 service 的 buildFocusTopics/buildWorthRedoingTopics 及辅助函数、契约 V2FocusTopic/parseFocusTopic、类型 TopicFocusItem、相关测试用例；TodayFocusSection.tsx 更名 TeamActivitySection.tsx；接口文档/模块地图同步更新；1154 测试全过 | CC |
| 2026-08-26 | `/topics` 首屏接口 | 本地 dev server 真实登录态实测（leader 账号）：首屏 5 请求并行，最慢接口决定首屏 ≈ 优化前 1.04s → 优化后 0.58s（dev 含编译开销，线上更快）；浏览器整页全链路未实测 | 5 个（active / pool-all / pool-my_claims / options / comparison），符合 ≤20 红线 | `/api/topics/active` 优化前中位 ~1.04s | 后端重复查 + 串行：`loadActiveTopics` 7 个查询全部串行 await，且「active 作品+播放快照」全量扫了 3 遍（聚焦/值得再做/汇总各一遍）；选题池打分视图同表扫 2 遍；无 `[perf]` 埋点从未实测 | ✅已优化：active 的独立查询改 `Promise.all` 并行、作品表三查合一、汇总改本地计算；打分视图 works+summaries 合并为一次查询（select 补 content）；补齐 `[perf]` 埋点（`topics.active.*` / `topics.pool.*` / `topics.comparison.*`）；1156 项测试全过、tsc/lint 无新增错误；owner 账号与生产环境未复测。注：本批保留的三块死数据已在第二批删除 | CC |
| 2026-08-22 | 全站（字体） | LCP 未实测（无登录态浏览器环境） | 构建产物实测：生产 CSS/HTML 中不存在任何 fonts.googleapis/gstatic 引用 | 不适用 | `globals.css` 的 Google Fonts `@import` 在生产构建中被 CSS 管线剥掉（dev 保留），线上从未真实加载 Inter，一直走系统字体兜底 → 不构成首屏外部请求，**不迁移 next/font**；该行属死代码且在 dev 模式阻塞样式 | ✅已清理：删除死掉的字体 `@import`，dev 与生产行为对齐（都不再外联 Google Fonts）；构建通过、产物零外部字体引用 | CC |
| 2026-08-22 | `/admin/collaboration` | 浏览器全链路未实测；构建产物实测：路由首屏 19 个 client JS chunk 中 recharts 代码为 0 | 未实测（需登录态） | 未实测 | 个人档案卡弹窗组件静态 import recharts（约 370KB 懒加载块），但弹窗只在点击时打开，recharts 被打进首屏 bundle | ✅已优化：PersonalCard 改 `next/dynamic` 按需加载（ssr:false + loading 兜底），`prefetchPersonData` 抽到独立轻量模块 `person-data.ts`（不拖 recharts）；hover 预取数据时同步预载图表 chunk，交互链路（缓存命中、loading 骨架）不变；真实点击复测未做，标记未实测 | CC |
| 2026-08-22 | `/api/admin/next-day-review/batch` | 接口耗时未实测（需真实 AI 渠道调用） | 未实测 | 未实测 | 批量复盘对 ≤20 个视频逐个串行执行「鉴权 + AI 生成」，整批耗时 = 各视频耗时之和 | ✅已优化：改为有上限并发（并发度 3）的 worker pool，结果顺序与入参一致保持不变；新增 `[perf] next-day-review.batch` 埋点（DYDATA_PERF_LOG=1 时输出整批耗时）；提速幅度未实测 | CC |
| 2026-08-22 | `/api/admin/modules/member-emails`、`/admin/modules` loader | 未实测（需登录态） | 构建产物实测：Auth Admin API 调用从 N 次（每成员一次 getUserById 无上限并发）降为分页 `listUsers(perPage:1000)` 过滤 | 未实测 | 成员邮箱水化对 activeVisibleUserIds 逐个调 `auth.admin.getUserById`，N+1 扇出；单页 1000 条会截断大规模账号 | ✅已优化：改为 `listAllAuthUsers` 分页拉取后按 id 集合过滤，保留可见范围与归档语义；loader 补 `[perf] admin-modules.profilesAndHydration` / `[perf] admin-modules.monthlyPublishStats` 埋点供下次体检实测 | CC |
| 2026-07-18 | `/`、`/dashboard`、`/admin/content`、`/admin/analytics` | 浏览器耗时未实测；同机同版本 Turbopack 生产构建的初始资源 gzip：`/` 182.5→169.1 KiB（-7.3%）、dashboard 376.2→256.2 KiB（-31.9%）、admin/content 567.0→382.0 KiB（-32.6%）、admin/analytics 504.5→437.3 KiB（-13.3%） | 未实测（需真实登录态） | 未实测（本次只改前端阻塞与 bundle） | 全局组件和路由专用代码加载过早；鉴权读取重复且阻塞骨架；首页动态渲染和首次强制刷新 | ✅已优化：闲置弹窗/诊断图表动态加载；全局 Framer Motion 移出首屏；登录态请求级去重、导航查询并行；Dashboard 鉴权纳入 Suspense；首页静态化并取消首次强制刷新；Toaster 与路由 CSS 拆分；生产构建通过 | Codex |
| 2026-07-16 | `/growth` | 未实测（本次为功能重构非性能优化；新增 2 个轻量 count 查询已挂 `[perf]` 埋点 `growth.full.lifetimeStats` / `growth.full.activeProfiles`，待下次体检回填） | 未实测 | 未实测 | — | ✅已重构：三态状态机（累积/观察/成熟，按全历史份数+团队 active 人数判定）+ 断流双轴规则（断流只出横幅不降级，窗口算不出数才冻结）+ 趋势图诚实化（真实日期轴、缺数虚线、断流斜纹带、tooltip 去重、团队 <5 人隐藏 P70、互动分公式收进 info 浮层）+ 摘环比徽章；全量测试 627 项 621 过（剩 6 个为既有无关失败） | Kimi |
| 2026-07-14 | `/video-review`、`/video-review/submit`、`/video-review/exemption` | 修复前：子页首屏会额外并发预取 4 个兄弟路由 `_rsc`，单条约 625-654ms；首页截图链路大量 404 | 修复前子页额外 8 个 `_rsc` 预取；修复后本地真实登录态子页只剩自身静态资源 + `/api/exemptions/pending` | 修复前 `/video-review?_rsc=...` 1786ms；兄弟页 `_rsc` 625-654ms；截图签名路由单张约 1187ms | 前端请求太早 · tabs 默认预取兄弟路由；首页 loader 未收口；历史案例图错误走违规截图校验导致 404 | ✅已优化：首页收口到 `loaders/video-review-page`，approved list 限 24，submission 截图改批量签名；tabs `prefetch=false`；截图路由放行 `publish_drafts` 的本人/approved 历史图；本地真实登录态复测通过 | Codex |
| 2026-07-08 | `/dashboard` | 优化前本地 loader 870ms（非浏览器全链路） | 待补测 | `profiles` 492ms（⚠️此细项来自临时测量，dashboard-page.ts 内部尚无分步 `[perf]` 埋点，不符合本纲证据标准；下次体检需先补埋点复测） | 数据库重复查 · 夹带未用的团队审批数据 | ✅已优化：合并 3 次 profile 读为 1 次、删 teamReviewRequests 首屏查询及透传链、补回归测试 2/2 过 | Codex |
| 2026-07-08 | `/growth` | 优化前本地 initial 318ms / full 212ms（非浏览器全链路） | 待补测 | initial `daily_reports` 220ms；full `ai_insight_result` 208ms | 前端请求太早 · full 挂载即发；重复查 | ✅已优化：full 改真实 hydration 只补升级字段、不再重拉基础资料、补 owner_user_id 缺列兼容；测试 10/10 过 | Codex |
