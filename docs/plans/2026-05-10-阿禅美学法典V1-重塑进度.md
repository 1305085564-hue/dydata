# 阿禅美学法典 V1 全站重塑 · 项目进度文件

> 创建日期：2026-05-10
> 目的：**压缩上下文不丢关键信息**，为后续网页端前端进一步优化提供完整档案
> 状态：批次 1 + 批次 2 已全部交付并推 main（commit `0bcf223`）

---

## 一、项目目标

把 dydata（达人数据生产操作台）**整站前端视觉语言**从原先混乱的多色系/重字重/大圆角/毛玻璃风格，统一重塑为**阿禅美学法典 V1** 定义的低调克制审美体系。

**法典核心 13 条纲领**（详见 `/docs/阿禅美学标准-V1.md`）：
1. 留白优先 — 宁疏勿密，多用 zinc-50/white 呼吸空间
2. 层级靠灰阶 — zinc-400/500/700/800 四级文字灰度
3. 字号阶梯 — `10px uppercase tracking-[0.25em]` 次要 / `13px leading-[1.7]` 正文 / `20px semibold tracking-tight` 主标
4. 色值克制 — CTA 主色 `#D97757` 暖橙；语义色 `#6FAA7D`（成功）/ `#D99E55`（警示）/ `#C9604D`（异常）/ `#8AA8C7`（信息）
5. 圆角阶梯 — 按钮 `rounded-[10px]` / 输入 `rounded-lg` / 内卡 `rounded-xl` / 主面板 `rounded-2xl`；禁 `rounded-3xl+`
6. 阴影弥散 — 只用 `shadow-sm` (≤3% 透明度)；禁 `shadow-lg/xl/2xl` 和彩色 glow
7. Ring 细光环 — 统一 `ring-1 ring-zinc-950/5`；禁 `ring-3/4` 和彩色 ring
8. 交互低调 — `hover:-translate-y-[1px] active:translate-y-0` + `transition-[具体属性] duration-150 ease-[cubic-bezier(0.4,0,0.2,1)]`；禁 `transition-all`、`hover:scale-*`
9. 字重收敛 — 最高 `font-semibold`(600)，禁 `font-black/extrabold/bold`(≥700)
10. 状态点化 — 6px 小圆点 + 文字色替代大面积彩色背景
11. 空/加载/错态统一 — `<Skeleton />` / `<EmptyState>` / `<ErrorState>`，禁 `animate-spin` + `Loader2`
12. 细节从简 — icon `stroke-[1.5]` + size 统一
13. **禁区硬规** — 以上各条对应的禁用模式（批量 grep 可自检）

---

## 二、背景（重塑前的问题）

### 2.1 视觉层混乱
- 大面积彩底 `bg-red-50/bg-emerald-50/bg-amber-50` 四处开花
- 老调色板硬编码 hex 泛滥（`#FEF9C3/#E0E9FF/#D1FADF/#FEE4E2/#ECFDF3/#067647/#B42318/#92400E/#444CE7`）
- `font-black/extrabold` 重字重 + `shadow-lg/xl` 重阴影 + `backdrop-blur` 毛玻璃混用
- `transition-all` / `hover:scale-*` / `animate-spin` 低质感交互
- `rounded-[2rem]/rounded-[32px]/rounded-3xl` 过大圆角扁平感
- shadcn/ui 基础组件本身已偏离法典方向

### 2.2 单文件过度膨胀
- `production-control-system.tsx` 1611 行、11 个组件塞一起
- `video-submit-form.tsx` 1322 行 / `video-submit-panel.tsx` 1124 行
- 维护困难，视觉重塑无从下手

### 2.3 基础设施未统一
- `design-tokens.css` 仍有 oklch 色值
- `globals.css` 有径向渐变 body + glow-drift 动画
- auth layout 蓝色渐变 + `glass-card-static` 毛玻璃
- nav-bar-client 激活态 `bg-zinc-900 shadow-lg shadow-zinc-900/10` 过重

---

## 三、关键路径（两批次 7 代理交付）

### 批次 1 · 基础设施层（α，主进程执行）

```
α-1 Token 层重写
  ├─ design-tokens.css 完全重写（L0/L1/L2 底色 + D97757 暖橙 + 8-20px 圆角阶梯 + 150ms ease + 弥散阴影 ≤3%）
  └─ globals.css shadcn token 全部 hex 化（oklch 清零），body 去径向渐变改 L0 实色 13px/1.7

α-2 shadcn/ui 全组件重写（15 件 + 新增 Skeleton）
  ├─ button（D97757 默认 + 弥散阴影 + focus-visible:ring-1 ring-zinc-950/5）
  ├─ input/textarea/select（Soft Fill bg-zinc-100/70 + focus 变白）
  ├─ card/dialog/badge/tabs/checkbox/switch/table/sonner/empty/error/form
  └─ skeleton.tsx（基元新建）

α-3 auth 栈重构
  ├─ (auth)/layout 去蓝色径向渐变 + glow-drift 动画，bg 改 #F4F4F5
  └─ login/register/forgot/reset 四表改 max-w-sm 单栏极简

α-4 全站 layout
  ├─ nav-bar-client 去 bg-white/85 backdrop-blur → #FAFAFB；激活态 bg-white shadow-sm
  ├─ admin-sidebar 激活指示改 2px #D97757 左竖条
  └─ admin layout + AppShell 底色统一 #F4F4F5

α-5 验证：build 零 error + 禁区 grep 基础设施层全清零
```

**交付物**：commit 前 `npm run build ✓`，基础设施层禁区 grep 零命中

### 批次 2 · 页面层（β/γ/δ/ε/ζ′/η，6 个 Opus 代理）

```
β（独占）· production-control-system.tsx 拆分 + 重塑
  ├─ 1611 行 → 主文件 304 行 + dashboard/components/ 下 12 个新文件
  ├─ status-theme.ts / status-badge.tsx / dashboard-workspace-header.tsx
  │   alert-center.tsx / workflow-dashboard.tsx / data-report-stage.tsx
  │   quick-exemption-button.tsx / global-matrix.tsx / leader-dashboard.tsx
  │   review-detail-modal.tsx / leader-report.tsx / owner-report-list.tsx
  └─ STATUS_THEME 5 档映射法典色（#D99E55/#8AA8C7/#6FAA7D/#C9604D/zinc-400）

γ/δ/ε 并发三路
  γ dashboard 表单栈（11 文件）：video-submit-form/panel + dashboard-form + 日报/豁免/账号三弹窗 + checkpoint-tracker + video-tag-review-card + history-list + advice-panel + 导粉话术采集区
  δ growth 全栈（15 组件）：六维雷达 / diagnosis-card / status-card-grid / script-breakdown / growth-action-plan-panel / growth-pk-panel / advice-panel / ai-advice / benchmark-card / capability-grid / growth-insight-panel / sample-library / weakness-benchmark-grid
  ε admin 常规（9 子页面 + 辅助）：/admin + analytics/content/videos/violations/conversion-hub/modules/guidance/market/advice + admin-panel-launcher/audit-log-list/data-manager/export-button/generate-invite-button/permission-manager/submission-status/team-*/豁免* 等；**ε 越界顺手改了 ai-* 三模块部分文件**（后由 η 补全）

ζ′（轻量版，原 ζ 被中断后重派）· content-tools 全栈
  ├─ content-tools-client / topic-suggest / template-library / publish-recommend / utils
  └─ rewrite 子目录：ChatInputBar / RewriteHistory / RewriteInput / RewriteOutput / RewriteWorkbench / ConfigBar

η（Opus-4.7）· 全站最终扫描 + 残余 40+ 处修复
  ├─ violations 全栈 / dashboard 展示卡 / demo 子页 / charts / leaderboard / submission / screenshot-import / nav-bar-client / admin-secondary-nav
  ├─ lib/tailwind-utils.ts badgeClass 三档换法典色
  └─ 所有 Loader2/animate-spin 剩余点位改 Skeleton
```

**交付物**：commit `0bcf223`（180 files +5487/-5138），已推 main

---

## 四、验收结果

| 验证项 | 结果 |
|---|---|
| `npm run build` | ✓ Compiled successfully |
| 第 13 条禁区 grep 代码区 | 零命中（JSDoc 注释除外） |
| TypeScript tsc --noEmit | clean（仅遗留 test.ts allowImportingTsExtensions 历史配置） |
| eslint | 仅残余 pre-existing unused import warnings |
| git push main | `76e56f6..0bcf223 main -> main` |

---

## 五、已禁区清单（用于后续自检 grep）

```regex
font-black|font-extrabold|font-bold        # 字重 ≥700
transition-all                              # 模糊过渡
shadow-lg |shadow-xl|shadow-2xl             # 重阴影
backdrop-blur-                              # 毛玻璃
hover:scale-                                # 缩放交互
bg-red-50|bg-emerald-50|bg-amber-50|bg-blue-50|bg-sky-50|bg-rose-50|bg-indigo-50|bg-violet-50   # 彩底
#FEF9C3|#E0E9FF|#D1FADF|#FEE4E2|#FEF3C7|#ECFDF3|#067647|#B42318|#FDE68A|#FECDCA|#ABEFC6|#C7D7FE|#444CE7|#92400E   # 老 hex 调色板
Loader2 |animate-spin                       # 旋转加载
ring-3|ring-4                               # 粗彩 ring
rounded-3xl|rounded-\[2rem\]|rounded-\[32px\]|rounded-\[28px\]   # 过大圆角
```

**禁动区**（自检时排除）：
- `src/components/ui/` — α 成果
- `src/app/(app)/dashboard/components/` — β 成果
- `.ts` 纯逻辑 / `.test.ts` / server action / lib/schemas / types

---

## 六、项目结构地图（重塑后）

```
src/
├── app/
│   ├── (auth)/                 # [α-3] login/register/forgot/reset + layout
│   └── (app)/
│       ├── dashboard/
│       │   ├── production-control-system.tsx  [β 瘦身 304 行]
│       │   ├── components/                    [β 新建 12 文件]
│       │   ├── video-submit-form.tsx          [γ]
│       │   ├── video-submit-panel.tsx         [γ]
│       │   ├── dashboard-form.tsx             [γ]
│       │   └── ... (9 other γ 文件)
│       ├── growth/             [δ 全栈]
│       ├── admin/              [ε 9 子页面 + 辅助]
│       │   ├── ai-assistant/   [ε 部分 + η 补全]
│       │   ├── ai-channels/    [ε 部分 + η 补全]
│       │   └── ai-rewrite/     [ε 部分 + η 补全]
│       ├── content-tools/      [ζ′ 全栈]
│       └── violations/         [η]
├── components/
│   ├── ui/                     # [α-2] shadcn 法典组件（禁动区）
│   ├── growth/                 [δ 15 组件]
│   ├── content-tools/          [ζ′]
│   ├── nav-bar-client.tsx      [α-4]
│   ├── admin-layout/
│   │   ├── admin-sidebar.tsx   [α-4]
│   │   └── admin-main-area.tsx [α-4]
│   └── app-shell/              [α-4]
├── styles/
│   └── design-tokens.css       [α-1]
└── app/globals.css             [α-1]

docs/
├── 阿禅美学标准-V1.md          # 法典全文
└── plans/
    └── 2026-05-10-阿禅美学法典V1-重塑进度.md  # 本文件
```

---

## 七、遗留事项（非阻塞，后续优化可处理）

| 项目 | 类型 | 建议处理 |
|---|---|---|
| `ui-contrast-lab/` 根目录实验 | 独立目录 | 保留观察，不入主仓 |
| recharts `width(-1)` warning | 历史容器尺寸 | 在图表容器加 `min-h-[200px]` 之类兜底 |
| `.test.ts` allowImportingTsExtensions | tsconfig 历史 | 下次批量迁移 import 路径去掉 .ts 扩展 |
| 部分 eslint unused import warnings | pre-existing | 下次清理死 import |
| `src/app/(app)/dashboard/production-control-system.tsx` 304 行 > 280 | β 自陈超 24 行 | 若想再降可抽 `useAlerts(today)` / `useSopRefresh(...)` 两个自定义 hook |

---

## 八、后续优化可能方向（阿禅接手）

### 8.1 交互细节打磨
- 某些按钮 hover 位移 1px 是否够"手感"，可调 `transition-duration`（150 → 180ms 更顺滑但更慢）
- Focus ring `ring-1 ring-zinc-950/5` 在白底上是否够明显（可考虑 `ring-zinc-950/10`）
- 表单提交 loading → Skeleton 替换后，用户感知是否清晰（某些场景可考虑 disabled + opacity）

### 8.2 视觉节奏微调
- 当前行间距 `leading-[1.7]` 是否全站都合适（长文本区域可能需要更松）
- 状态点 6px 是否都够醒目（小卡片里可能需要 8px）
- 卡片间距 `space-y-5/6` 在宽屏上是否够松

### 8.3 数据可视化层
- recharts 当前用的 `#D97757/#8AA8C7/#6FAA7D/#D99E55/#C9604D` 5 色调色板，超过 5 条线可能不够用，可扩展次色阶
- 坐标轴字重、刻度密度、tooltip 样式是否统一
- Gauge/Radial 等特殊图表的色值落实

### 8.4 响应式 / 移动端
- nav-bar-client 在移动端是否够可用（滚动条隐藏已做）
- admin-sidebar mobileOpen 覆盖层是否 z-index 冲突
- dashboard 表单在小屏下的字段密度

### 8.5 深色模式（如需）
- 当前 token 层未准备 dark 变量组，若要加需复审整个色值系统

### 8.6 可访问性
- WCAG 对比度（法典 `#D97757` on 白底 / `#8AA8C7` on 白底需手动测过）
- 键盘焦点顺序 / `aria-label` 完整性
- 屏幕阅读器对状态点 + 文字色的理解

### 8.7 性能
- production-control-system 拆完后是否有多余 re-render（可 React DevTools Profiler 看）
- 大列表（history-list / violations）是否需虚拟滚动

---

## 九、关键 Git 节点

```
0bcf223  feat(ui): 阿禅美学法典 V1 全站重塑  ← 当前 HEAD
76e56f6  docs: add AI collaboration system and enhanced troubleshooting to AGENTS.md
fae0b68  feat(conversion-hub): 转化中心 P0+P1 全量闭环
f3a0f83  chore: sync local docs, scripts and admin layout component
0833aee  feat(ui): dashboard 表单降层优化 + 进度条强化 + 多模块 UI 迭代
```

---

## 九·附 · 外部验收发现与 V1.1 返工计划（2026-05-10 增补）

### 9A.1 外部反馈可取性判定（三路 Explore 现场实证后）

**Kimi 量化报告** —— 总体高度可取，仅粉彩底一条误报：

| 项 | Kimi 报告 | 现场实证 | 判定 |
|---|---|---|---|
| `text-zinc-950` | 192 | ✓ 192 / 10 文件 | **P0 真硬伤** |
| `text-zinc-900` | 未列 | 59 / 47 文件 | **P1 补录** |
| `font-bold` | 5 | ✓ 5 | **P0 真硬伤** |
| `bg-white border-zinc-200` | 359 | 417（其中真输入框违规 ~60-80） | **P0 部分准确**（只改输入框） |
| 粉彩底 | 说"有残留" | 0 命中 | 误报 |

**Gemini 视觉点评** —— 半对半错：

| 点评 | 判定 | 证据 |
|---|---|---|
| AI 洞察粉彩背景块 | **误判** | 现场是 `bg-zinc-50 + 左 2px 彩色导轨`，Gemini 看的是旧截图 |
| 主 CTA 集体失踪 | **部分属实** | dashboard/admin/violations 已锚 #D97757；content-tools 的"加载建议/推荐模板"仍灰色；growth 为纯展示页无按钮 |
| 图表默认科技蓝/亮橙 | **属实 P0 硬伤** | `components/charts/interaction-trend.tsx` + `result-trend.tsx` 用 `#3B82F6`/`#F97316`；`admin/analytics/video-analytics.tsx` 用 `#111827→#60a5fa` 蓝渐变 |
| 状态矩阵死灰 | **误判** | `global-matrix.tsx` 已用四色法典语义点 |

### 9A.2 V1.1 返工范围（P0 + P1）

**代理 A · 去黑化 + font-bold 归零**（Opus）
- `text-zinc-950 → text-zinc-800` 全量替换（禁动区 ui/ 除外）；重灾区 `admin/data-manager.tsx`(53) / `admin/ai-rewrite/ai-rewrite-client.tsx`(33) / `content-tools/rewrite/RewriteOutput.tsx`(9) 等
- `text-zinc-900 → text-zinc-800`（59 处 47 文件）
- `font-bold → font-semibold` 精修 5 处：`admin/ai-assistant/chat-panel.tsx:434` / `violations/[id]/components/usage-timeline.tsx:196` / `components/pk/pk-battle.tsx:178`（`text-[#007AFF]` 改 `text-[#8AA8C7]`）/ `components/submission/截图槽位卡.tsx:169,179`

**代理 B · 输入框 Soft Fill 化**（Opus）
- 只改"真输入框"违规（Input / SelectTrigger / Textarea / DatePicker 等表单原语）
- 默认态 `bg-white border-zinc-200` → `bg-zinc-100/70 border-transparent`
- Focus 态追加 `focus:bg-white focus:border-zinc-200 focus:shadow-sm`
- 重灾区：`admin/data-manager.tsx` 21 / `admin/analytics/hit-analyzer.tsx` 25 / `dashboard/video-submit-form.tsx` 15 / `dashboard/video-submit-panel.tsx` 15 / `admin/permission-manager.tsx` 10 / `admin/team-group-manager.tsx` 10 / `admin/豁免弹窗.tsx` 7
- 卡片容器的 `bg-white border-zinc-200` **不动**（法典允许）

**代理 C · 图表色板统一 + content-tools 主 CTA 注入**（Opus）
- 新建 `src/lib/chart-palette.ts` 导出 `CHART_PALETTE` 常量（法典 5 色 + 网格 `#E4E4E7` + 坐标轴文字 `#A1A1AA`）
- `components/charts/interaction-trend.tsx`：主线 `#3B82F6 → #D97757`、辅线 `#F97316 → #8AA8C7`
- `components/charts/result-trend.tsx`：同上
- `admin/analytics/video-analytics.tsx`：黑蓝渐变 → 暖橙渐变 `#D97757 → rgba(217,119,87,0.2)`
- 补充网格虚线统一 `#E4E4E7`、坐标轴 text-[10px] text-zinc-400
- `content-tools` 主操作按钮（加载建议 / 推荐模板 / 生成改写）升级为 `variant="default"` 暖橙 CTA

### 9A.3 验收三道门

1. `npm run build` 零 error
2. 自检 grep 全零命中（排除 ui/ demo/）：
```bash
grep -rn "text-zinc-950\|text-zinc-900" src/app src/components --include="*.tsx" --include="*.ts" | grep -v "src/components/ui/" | grep -v "src/app/demo/"
grep -rn "font-bold\|font-extrabold\|font-black" src/app src/components --include="*.tsx" --include="*.ts" | grep -v "src/components/ui/" | grep -v "src/app/demo/"
grep -rn "stroke=\"#3B82F6\"\|stroke=\"#F97316\"\|#111827.*#60a5fa" src/components src/app --include="*.tsx"
```
3. code-review skill 全量过

### 9A.4 已驳回点评

- ~~Gemini：AI 洞察粉彩底块~~ —— 误判，不动
- ~~Gemini：全域矩阵死灰~~ —— 误判，不动
- ~~Kimi：卡片容器 `bg-white border-zinc-200` 全改灰~~ —— 过度反应，仅输入框适用 Soft Fill

---

## 十、恢复上下文的最小信息集（压缩后读此节即可续）

1. **当前状态**：阿禅美学法典 V1 全站重塑已推 main（commit `0bcf223`），build 通过，禁区零命中
2. **法典文档**：`/docs/阿禅美学标准-V1.md`（13 条纲领）
3. **禁动区**：`src/components/ui/` + `src/app/(app)/dashboard/components/` + `.ts/.test.ts`
4. **自检命令**：`npm run build` + 第五节的禁区 grep
5. **下一步**：阿禅将对网页端前端做**进一步优化**，方向参考第八节（交互打磨 / 视觉节奏 / 数据可视化 / 响应式 / 可访问性 / 性能）
6. **工作方式**：主进程保持低上下文编排，所有子任务 offload 给 Opus-4.7 代理（模型 ID `claude-opus-4-7`）
