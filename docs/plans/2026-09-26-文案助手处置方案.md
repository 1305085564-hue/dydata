# 文案助手处置方案（修订版 v3）

> v1 经审查判定「**有条件通过，暂缓施工**」，列出 5 个阻断点 + 6 条质量问题。本版逐条修正，并追加 v1 遗漏的一条更严重冲突。
> 逐条对账见第十二节。
> 引用行号为 2026-09-26 实测值，施工时以内容匹配为准。

---

## 一、结论前置

| 项 | 结论 |
| :--- | :--- |
| 当前状态 | **已施工完成**（2026-09-26）：入口已摘、功能代码已删、数据表零动作；实测与未取证项见第十三节 |
| 阶段一 | **入口下线**（4 个文件；不删代码、不改 middleware、不动数据库） |
| 阶段二 | **删代码**（已施工完成）；施工前已完成第九节脏工作区隔离 |
| 数据库 | 本次**不做任何动作**（v1 的"统一置 `is_enabled = false`"不成立，见第七节） |
| 最大风险 | 误删 5 张共享表 → **截图 OCR + AI 配置中枢同时报废** |

---

## 二、v1 的致命遗漏：与既有拍板直接冲突

审查指出 v1 的"已确认下线"与所引审计依据不一致。追查后发现问题比审查描述的更严重——**冲突不在审计里，在项目事实文档里**。

### 2.1 既有拍板（现行有效，文档明写）

| 来源 | 原文要点 |
| :--- | :--- |
| `docs/全站模块地图.md:100` | 「**2026-08-26 阿禅拍板**：此页面后续整体重做。重做前只做故障修复，不做视觉/结构类优化；`src/lib/rewrite/` 仍承载主链路，**重做方案确定前不可当废代码删**」 |
| `docs/全站模块地图.md:106` | 「坑与连累：`src/lib/rewrite/` **不是可删的旧层**，当前仍承载主链路」 |
| `docs/全站模块地图.md:223` | 「`src/lib/rewrite/` 仍承载主链路，**不可当废代码删**」 |
| `docs/工程运行事实.md:26` | 「`/content-tools/rewrite` 仍确定后续整体重做，重做前只处理必要故障，**冻结性能深改和大范围清理**」 |
| `docs/工程运行事实.md:75` | 「`/content-tools/rewrite` 的结构性性能治理随整体重做处理，不在冻结期扩张范围」 |
| `docs/待办清单.md:39` | `/content-tools/rewrite` 待重做页面冻结 |
| `docs/待办清单.md:40` | 文案助手历史会话完整交互验收待办 |

### 2.2 审计结论（2026-09-26）的实际口径

| 来源 | 原文要点 |
| :--- | :--- |
| 审计 `:76` | 裁决：**「极简重构（砍掉 80% 通用聊天，做成爆款脚本洗稿机）」**——以重构为主 |
| 审计 `:80` | 「**如果做不到这一点，直接关停入口**」——关停是**兜底** |
| 审计 `:255-261` | 「文案改写怎么处理？」仍列为**待拍板事项 2** |

### 2.3 冲突判定

v1 把「代码删干净」写成"阿禅已确认"，但：

1. 项目事实（2.1）明确写着 **不可当废代码删**，且是阿禅本人 2026-08-26 的拍板；
2. 审计的主裁决是**重构**，删代码只是兜底路径；
3. 审计自己仍把该模块列为**待拍板**。

**结论：v1 的"代码删干净"缺少决策授权，不能作为施工依据。** 上一轮对话中阿禅的"可以"，是在未获知 2.1 这条既有拍板的情况下给出的答复，不构成对既有拍板的推翻。

### 2.4 决策补录（已完成，2026-09-26）

阿禅已明确：

> 文案助手不再进入重做队列，改为下线；2026-08-26 的「重做前不可当废代码删」**作废**。

据此：

1. 阶段一、阶段二**均已解锁**；
2. 2.1 中 `全站模块地图.md:100/106/223`、`工程运行事实.md:26/75`、`待办清单.md:39/40` 的「不可当废代码删 / 冻结」表述随本决策一并失效，须按 5.5 清单改掉；
3. **本文档为该决策的唯一记录载体**；施工时须把该决策同步进 `docs/全站模块地图.md` 与 `docs/工程运行事实.md`，否则施工完成后两份权威事实文档会与新现实冲突。

---

## 三、阶段划分

| 阶段 | 内容 | 前置 | 风险 | 交付的业务结果 |
| :--- | :--- | :--- | :--- | :--- |
| **阶段一** | 入口下线。不删代码、不改 middleware、不动数据库 | 无，可立即施工 | 极低 | 三端员工均看不到「文案助手」 |
| **阶段二** | 删代码 + 清连带引用 + 文档对账 | 已解除（2.4 已补录）；仍需第九节前置条件 | 中高（触及权限链路与中间件） | 仓库不再承载该模块代码 |

**推荐顺序**：决策已解锁，两阶段可在同一批内完成，但内部仍按「先摘入口 → 再删代码 → 最后清连带」的顺序推进，任一步出问题可单独回退；步骤间禁止提交（见第十节）。

若希望把风险再压一档，也可先只做阶段一（4 个文件）拿到「员工看不到」的业务结果，再单独做阶段二。阶段一还天然规避了审查的 P0-2：**不动 middleware 就没有这个矛盾**。

---

## 四、阶段一施工清单（4 个文件，可立即施工）

| # | 文件 | 行号 | 动作 |
| :--- | :--- | :--- | :--- |
| S1 | `src/components/nav-bar-items.ts` | 99–107、34 | 删 `use_ai_copy` 入口块；参数 `showAiCopywriting` 随之无用，一并删 |
| S2 | `src/components/nav-bar.tsx` | 31、58 | 删 `showAiCopywriting` 计算与传参 |
| S3 | `src/components/nav-bar-client.tsx` | 137、152、165、170 | 删 `showAiCopywriting` 属性、默认值、依赖数组项 |
| S4 | `src/app/(app)/content-tools/rewrite/page.tsx` | 全文 | 替换为极简「功能已暂停」提示页；保留 `metadata` 与登录/权限 redirect 逻辑，不再渲染 `RewriteWorkbenchV3` |

### 阶段一明确不改

`middleware.ts`、`api-path-validation.ts`、`usage-events/shared.ts`、`api-rate-limit.ts`、`perf-full-sweep.mjs`、`lib/rewrite/**`、`api/rewrite/**`、`api/content-tools/**`、数据库、权限键。

### 阶段一验收

- `npm run gate:static` 通过
- 三种身份（`member` / `admin` / `company_owner`）导航均无「文案助手」
- 无 `use_ai_copy` 的账号直达 `/content-tools/rewrite` → 跳 `/dashboard`（既有逻辑，未改）
- 有 `use_ai_copy` 的身份直达 → 显示「功能已暂停」，不渲染工作台
- 未登录直达 → 跳 `/login`；归档账号直达 → 账号拦截页（均沿用既有 middleware 逻辑）

---

## 五、阶段二施工清单（待 2.4 决策补录后解锁）

### 5.1 删除（43 个文件 / 9185 行生产代码 + 1944 行测试）

| # | 路径 | 文件数 | 说明 |
| :--- | :--- | :---: | :--- |
| A1 | `src/components/content-tools/` | 14 | `rewrite-v3/` 13 个（含 2 测试）+ 父级 `types.ts`。已核实 `types.ts` 仅被 `rewrite-v3/` 内部 `../types` 引用，可整目录删 |
| A2 | `src/lib/rewrite/` | 12 | 6 生产（含 2568 行的 `shared.ts`）+ 6 测试 |
| A3 | `src/app/api/rewrite/` | 13 | 12 路由 + `skills/route.test.ts` |
| A4 | `src/app/api/content-tools/` | 4 | 旧前缀壳（`_shared.ts` + 2 路由 + 1 测试） |

`/api/content-tools/rewrite/*` 是已被替换的旧前缀，业务逻辑全部转发给 `lib/rewrite/shared`，属纯冗余，与功能存废无关，一并清理。

### 5.2 修改（15 个不同文件；若阶段一已执行，则实际剩 12 个）

| # | 文件 | 行号 | 动作 |
| :--- | :--- | :--- | :--- |
| B1 | `src/components/nav-bar-items.ts` | 99–107 | 同阶段一 S1（阶段一已完成则跳过） |
| B2 | `src/components/nav-bar.tsx` | 31、58 | 同阶段一 S2（同上） |
| B3 | `src/components/nav-bar-client.tsx` | 137、152、165、170 | 同阶段一 S3（同上） |
| B4 | `src/app/(app)/content-tools/rewrite/page.tsx` | 全文 | 删除该文件（或保留阶段一提示页，见第十一节待拍板 3） |
| B5 | `src/lib/api-path-validation.ts` | 5、7–10 | 删 5 条 rewrite/旧壳 UUID 规则；**保留 4、6、11 行**其他模块规则（video-assets / notifications / topics） |
| B6 | `src/lib/usage-events/shared.ts` | 4、10 | 删 `rewrite_generate` 事件类型与 `/content-tools` 埋点前缀 |
| B7 | `src/middleware.ts` | 171–172 | 见 5.3 二选一 |
| B8 | `src/middleware.ts` | 290 | 见 5.3 二选一（与 B7 同文件） |
| B9 | `src/lib/api-rate-limit.ts` | 27 | **v1 漏项**：删 `/api/rewrite/generate` 的 10/分限流规则 |
| B10 | `scripts/perf-full-sweep.mjs` | 189、342 | **v1 漏项**：删 `/content-tools/rewrite` 性能扫描目标（含"历史对话面板"测试项） |
| B11 | `src/components/nav-bar-items.test.ts` | 38、54 | **护栏反转**：由「入口存在」改为「入口不存在」 |
| B12 | `src/lib/a11y-responsive-integration.test.ts` | 174、194–197、203–211、253–256 | 删 `use_ai_copy` fixture 与读 `rewrite-v3` 源码断言；保留 199–211 的反向守护意图——改成断言「无文案权限时底栏不出现该入口」 |
| B13 | `src/components/windows-adaptation-second-batch.test.ts` | 33 | 删对 `RewriteWorkbenchV3.tsx` 的存在性断言 |
| B14 | `src/lib/content-feedback-removal.test.ts` | 47 | 删对 `page.tsx` 的存在性断言 |
| B15 | `src/lib/usage-events/shared.test.ts` | 23–32 | 删 `rewrite_generate` 用例；补一条断言该事件类型已不被接受 |
| B16 | `src/app/(app)/layout.tsx` | 4 | 更新提及 content-tools 的注释 |

> B11–B15 是**防回归护栏**，不可整段删掉让测试变绿，必须改成断言「入口/事件/组件/页面已不存在」，否则以后被误加回来无人拦截。

### 5.3 middleware 处置：二选一（修正审查 P0-2）

**现状取证**：`isContentToolsRoute`（`:171`）汇入 `isProtectedAppRoute`（`:172`），该标志驱动两处保护——

- `:224` 未登录访问受保护页面 → 跳登录页
- `:240` 有 cookie 时校验 session 有效性，并读 `profiles.membership_status`（`:255-266`）拦截归档账号

v1 要求同时「保留 `/content-tools/:path*` matcher」又「删除 `isContentToolsRoute` 及其在 `isProtectedAppRoute` 中的引用」，**会让占位页脱离登录与归档拦截**，属方案自相矛盾。

| 方案 | 适用 | 动作 | 验收 |
| :--- | :--- | :--- | :--- |
| **甲（推荐）** 保留提示页 | 阶段一做法 | `middleware.ts` **完全不改**；matcher 与 `isProtectedAppRoute` 均保留 | 未登录直达 → 跳登录；归档账号直达 → 账号拦截页 |
| **乙** 直接删页 | 确认无人有书签 | 删 `:171` 定义、`:172` 引用、`:290` matcher | 直达 `/content-tools/rewrite` 得到**明确 404**，且未鉴权不可达 |

**禁止第三种组合**：删掉保护却保留 matcher。

### 5.4 测试文件

随目录删除（无需单独处理）：`src/lib/rewrite/*.test.ts`（6）、`src/app/api/rewrite/skills/route.test.ts`、`src/components/content-tools/rewrite-v3/*.test.ts`（2）、`src/app/api/content-tools/rewrite/_shared.test.ts`。

保留不动（属 `use_ai_copy` 权限契约，见 6.1 白名单）：`src/lib/permission-utils.test.ts`、`src/lib/company-permissions.test.ts`。

### 5.5 文档同步（8 份，含 v1 漏项）

| # | 文件 | 行号 | 动作 |
| :--- | :--- | :--- | :--- |
| C1 | `docs/全站模块地图.md` | 18、100、102–106、167、169、184、192、220、223 | 修订"待重做/不可删"表述；管理中心子项由 5 个改为 4 个（`:192`）；移除 `:184` 的 rewrite/generate 限流说明 |
| C2 | `docs/工程运行事实.md` | 20、26、75 | **v1 漏项，必须改**：`:20` 领域子目录删 `rewrite`；`:26`、`:75` 的"冻结/待重做"表述改为已下线 |
| C3 | `docs/待办清单.md` | 32、39、40 | **v1 表述过粗**：`:39` 冻结条、`:40` 历史会话验收条按新结论关闭；`:32` 涉及 `rewrite_length_presets`（051 已 `DROP` 但 `shared.ts` 仍查询，线上状态未核实）——删代码会消除该查询依赖，需据此重排 051/062 与代码收口的先后，**不可笼统划掉** |
| C4 | `docs/权限与安全说明.md` | 38、132、159、260 | 标注 `use_ai_copy` 已无页面承载 |
| C5 | `docs/reference/全站命名对照表-2026-09-01.md` | 18、30、46、86 | 标注路由已下线 |
| C6 | `docs/reviews/2026-09-26-…瘦身路线.md` | 20、66–80、239–240、260 | 标注第 3 条与待拍板事项 2 已落地 |
| C7 | `日志/2026-09-26.md` | — | 收尾必写（`[CX]` 前缀） |
| C8 | `docs/plans/2026-09-23-全站刷新感排查-前台数据录入.md` | — | 核实是否含 rewrite 条目并同步 |

文档收尾统一按 [文档维护入口](../文档维护入口.md) 核对受影响入口。

---

## 六、验收：允许/禁止残留清单（修正审查 P1-4）

v1 的"全站搜 `rewrite` 无残留"口径不成立——AI 配置中枢**真实读写** `rewrite_model_views` / `rewrite_model_routes`（`api/admin/ai-config/route.ts:210-301`），这是必须保留的共享能力。改为白名单 + 黑名单双向约束。

### 6.1 允许残留（白名单）

| 允许项 | 理由 |
| :--- | :--- |
| `src/app/api/admin/ai-config/**` | 共享模型配置的真实读写方，保留能力 |
| `src/app/(app)/admin/ai-config/**` | AI 配置中枢的页面、hooks 和模型绑定界面仍使用共享模型配置 |
| `supabase/migrations/**` | 历史 migration 不改（含 `20260714120000_add_usage_events.sql` 的 `rewrite_generate` 约束、`20260805120000_permission_v2.sql`、`20260819120000_company_role_and_group_mode.sql`、`20260915141335_unify_permission_functions.sql`） |
| `src/lib/permission-contract.ts`、`src/lib/permission-utils.ts`、`src/types/index.ts` | `use_ai_copy` 权限契约，划入更远批次，本次不动 |
| `src/lib/permission-utils.test.ts`、`src/lib/company-permissions.test.ts` | 上述契约的测试 |
| `src/lib/ai/provider-routing.ts`、`src/lib/ai/client.ts` | **陷阱文件**：被 OCR、`video_tag`、`member_ai_suggestion` 共用 |
| `docs/**`、`output/**`、`.worktrees/**`、`.playwright-mcp/**`、`tsconfig.tsbuildinfo` | 非运行时链路；不纳入下面的运行时代码扫描 |

### 6.2 禁止残留（黑名单）

- `src/lib/rewrite/**`
- `src/app/api/rewrite/**`
- `src/app/api/content-tools/**`
- `src/components/content-tools/**`
- `RewriteWorkbenchV3` 标识符
- `/content-tools/rewrite` 的功能请求与埋点（`rewrite_generate`）

### 6.3 判定口径

```bash
rg -n -i "content-tools|rewrite-v3|lib/rewrite|rewrite_generate|RewriteWorkbenchV3" \
  src scripts -g '*.ts' -g '*.tsx' -g '*.mjs'
```

**通过条件**：运行时代码命中只允许来自 6.1 中明确列出的保留路径，且不得命中 6.2 黑名单；`docs/`、`output/`、独立 worktree 和构建产物另行核对，不混入运行时代码结果。

---

## 七、数据库：本次不做任何动作（修正审查 P2-3）

v1 写"纯 rewrite 表统一置 `is_enabled = false`"——**不成立**。按实测表结构分四类：

| 类别 | 表 | 可用的停用手段 |
| :--- | :--- | :--- |
| 有 `is_enabled`（8） | `rewrite_model_views`、`rewrite_modes`、`rewrite_length_presets`、`rewrite_workflows`、`rewrite_workflow_steps`、`rewrite_model_routes`、`rewrite_fixed_modes`、`rewrite_skills` | 可置 false |
| 有状态字段（3） | `rewrite_document_revisions`（`status`）、`rewrite_generation_runs`（`status`）、`rewrite_messages`（`message_status`） | 语义是流程状态而非启停开关，置 false 无意义 |
| 有专用标记（3） | `rewrite_conversation_skills`（`is_active`）、`rewrite_document_paragraphs`（`is_locked`）、`rewrite_variants`（`is_adopted`） | 语义均非启停 |
| **无任何开关（3）** | `rewrite_conversations`、`rewrite_documents`、`rewrite_skill_versions`（仅 `published_at`） | **无法"停用"，只能原样保留** |

### 严禁删除（共享管线，误删即事故）

| 表 | 使用者 |
| :--- | :--- |
| `ai_providers`、`ai_provider_keys`、`ai_provider_key_models` | AI 配置中枢 + **截图 OCR**（`lib/ai/provider-routing.ts`、`lib/ai/client.ts`） |
| `rewrite_model_views`、`rewrite_model_routes` | AI 配置中枢「模型绑定」界面（`bindings-client.tsx`） |
| `ai_feature_bindings`、`ai_feature_config`、`ai_channels` | OCR、`video_tag`、`member_ai_suggestion`、`sample_quality_check` |

**最终结论**：因存在 3 张无任何开关的表，"统一停用"无法构成一条完整可执行的策略；且删表零收益、有风险、不可逆。**故本次对数据库不做任何动作**——不删、不改、不停用。

（若日后确需清理：仅允许按上表分类逐表定义策略，且必须先在测试环境验证 AI 配置页与截图 OCR 正常。）

---

## 八、验收：真实角色与链路（修正审查 P1-5）

按 `AGENTS.md`「浏览器验收分级」，本次触及**权限链路**（导航显隐 + middleware），适用「**完整验证**」，不能只靠 `npm run gate:static`。

### 8.1 静态门禁（必要非充分）

```bash
npm run gate:static    # test + tsc + lint + build
```

### 8.2 身份 × 路径矩阵（阶段一必做）

| 身份 | 主导航 | 直达 `/content-tools/rewrite` |
| :--- | :--- | :--- |
| `member`（无 `use_ai_copy`） | 无「文案助手」 | 跳 `/dashboard` |
| `admin`（有 `use_ai_copy`） | 无「文案助手」 | 显示「功能已暂停」，不渲染工作台 |
| `company_owner`（有 `use_ai_copy`） | 无「文案助手」 | 同上 |
| 未登录 | — | 跳 `/login` |
| 已归档账号 | — | 账号拦截页 |

### 8.3 接口与共享链路（阶段二必做）

| 检查项 | 期望 |
| :--- | :--- |
| `/api/rewrite/generate` | 不可用（404） |
| `/api/content-tools/rewrite/bootstrap` | 不可用（404） |
| `/admin/ai-config` | 正常加载；共享模型视图/路由区块可见可编辑 |
| **截图 OCR 冒烟** | 上传一张日报截图，OCR 正常出数——**证明共享模型路由未被破坏**（本次最关键的一条） |

### 8.4 发布前

```bash
npm run gate:browser    # 显式浏览器门禁
```

---

## 九、前置条件：脏工作区隔离（修正审查 P2-6）

当前 `git status` 有 **61 项未提交改动**，已包含一批 `/growth` 整页下线改动（删除 `growth/` 页面与组件、新增 `api/dashboard/leaderboard/route.test.ts`）及多份文档修改；另有 2 个 stash。

**风险**：本次改动与 `/growth` 批次混进同一个提交，出问题无法二分定位。

**前置条件（动工前必须完成）**：先把 `/growth` 批次单独收尾并提交（建议信息：`refactor(growth): 下线成长分析整页`），确认工作区干净后再开始本次施工。

> `.worktrees/collab-team-grouping/` 是独立 worktree，内含一份 rewrite 副本，本次不涉及、不影响。

---

## 十、施工顺序（阶段二）

| 步骤 | 动作 | 验收 |
| :--- | :--- | :--- |
| 0 | 完成第九节前置条件 | `git status` 干净 |
| 1 | 删 A3 + A4（接口层） | `npx tsc --noEmit` 通过 |
| 2 | 删 A2（后端逻辑层） | `npx tsc --noEmit` 通过 |
| 3 | 删 A1（前端组件层） | `npm run lint` 0 error |
| 4 | 清连带引用（B5–B10、B16） | 第六节判定口径通过 |
| 5 | 改测试护栏（B11–B15） | `npm test` 全绿，且护栏为「断言不存在」而非被删除 |
| 6 | 处理 middleware（B7/B8，按 5.3 选定方案） | 第八节 8.2 矩阵 |
| 7 | 全量静态门禁 | `npm run gate:static` |
| 8 | 真实角色与 OCR 冒烟 | 第八节 8.2 + 8.3 |
| 9 | 文档与日志收尾（C1–C8） | 按文档维护入口核对 |

**禁止中间提交**：步骤 1–6 之间不要 commit，避免留下半删状态。建议统一提交信息：`refactor(rewrite): 下线文案助手，保留数据表与共享模型配置`。

---

## 十一、规模数据与待拍板（修正审查 P2-1、P2-2）

### 11.1 规模实测（v1 数据有误，已重算）

| 项 | v1 | 实测 |
| :--- | :--- | :--- |
| 删除文件数 | 43 | **43**（正确） |
| 删除生产代码 | 9025 | **9185** 行 |
| 删除测试代码 | 1944 | **1944** 行 |
| 阶段二修改文件 | 12 | **15** 个不同文件（v1 的 B7/B8 同属 `middleware.ts` 已合并；另新增 B9/B10 两个漏项） |
| 阶段一修改文件 | — | **4** |

> v1 的 9025 漏算了 `src/components/content-tools/types.ts`（160 行）。

### 11.2 待阿禅拍板

| # | 事项 | 推荐 | 理由 |
| :--- | :--- | :--- | :--- |
| 1 | ~~是否补录 2.4 决策~~ | **已决**：2026-09-26 补录，作废 8-26 条款 | 见 2.4 |
| 2 | 阶段一现在做还是等 | 现在做 | 4 个文件、零风险、直接拿到业务结果 |
| 3 | 提示页还是直接 404 | 提示页（middleware 方案甲） | 老书签直达不会看到 404；且无需改 middleware |
| 4 | 数据库是否动作 | **不动作** | 3 张表无开关，"统一停用"不成立；删表有事故风险 |
| 5 | `use_ai_copy` 权限键 | 更远批次再清 | 涉及权限契约 + migration + 权限文档，高风险 |
| 6 | 旧壳 `/api/content-tools/rewrite/*` | 并入阶段二 | 纯冗余，与功能存废无关 |

---

## 十二、审查意见逐条对账

| 审查编号 | 问题 | 本版处理 |
| :--- | :--- | :--- |
| P0-1 | "已确认下线"与引用依据不一致 | **已修正并加重**：查明冲突源于 `全站模块地图:100/106/223`、`工程运行事实:26/75`、`待办清单:39/40` 的既有拍板；v1 作废。该冲突已由 2.4 决策（2026-09-26 补录）正式解除 |
| P0-2 | 占位页方案削弱 middleware 保护 | **已修正**：5.3 给出二选一并明令禁止第三种组合；阶段一因不动 middleware 而天然规避 |
| P1-3 | 漏了性能脚本 | **已补**：B10 `perf-full-sweep.mjs:189/342`；另自查补出 B9 `api-rate-limit.ts:27` |
| P1-4 | "全站无 rewrite 残留"口径不成立 | **已修正**：第六节建立白名单/黑名单双向判定 |
| P1-5 | 验收不足以证明高风险链路安全 | **已修正**：第八节补身份 × 路径矩阵、接口不可用、AI 配置中心、OCR 冒烟、`gate:browser` |
| P2-1 | 修改文件数 12 → 实为 13 | **已修正**：实测 15 个不同文件（含两个漏项） |
| P2-2 | 行数应重算 | **已修正**：9185 行生产 + 1944 行测试 |
| P2-3 | `is_enabled` 策略不适用无该字段的表 | **已修正**：第七节按四类分别定义，结论改为本次不动数据库 |
| P2-4 | `docs/工程运行事实.md` 未列入 C 类 | **已补**：C2，标注为必须改（否则施工后产生事实冲突） |
| P2-5 | `docs/待办清单.md` 表述过粗 | **已补**：C3，逐条对应 32/39/40 行，含 051/062 与代码收口顺序的关联 |
| P2-6 | 脏工作区边界未处理 | **已补**：第九节前置条件 |

---

## 十三、施工结果（2026-09-26）

- 阶段一 + 阶段二在同一批完成；middleware 采用**方案甲（零改动）**，提示页保留
- 删除 43 个文件 / 9185 行生产代码 + 1944 行测试；数据库**零动作**；`use_ai_copy` 权限键保留（后续批次）
- 实测（现有 dev server `:3000`，未登录 GET）：
  - `/content-tools/rewrite` → 307 跳 `/login` —— **保护未削弱**，方案甲成立
  - `/dashboard` → 307 跳 `/login` —— 基线不变
  - `/api/rewrite/generate`、`/api/content-tools/rewrite/bootstrap`、`/api/rewrite/documents/*`、`/api/rewrite/skills` → **全部 404**，已删接口确认不可用
  - `/api/admin/ai-config` → 401（存在且需鉴权）—— 共享模型配置未被误伤
- 静态：`tsc`、`lint` 在本次改动文件上零问题；`npm test` 1716/1722 通过
- **未取证（不得用静态检查冒充通过）**：三身份（member/admin/company_owner）导航显隐、提示页登录态渲染、归档账号拦截、截图 OCR 冒烟、`npm run gate:browser` —— 因同期另一代理的 `src/lib/video-review-thresholds.ts` 处于半成品状态、生产构建被其阻断（4 个构建错误全部指向该文件），浏览器门禁无法执行

**产出**：2026-09-26 `[CX]`｜状态：**已施工完成**
**本版**：v3（已吸收 v1 审查意见；v2 未单独保留）
