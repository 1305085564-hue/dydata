# 交接-2026-08-31-岗位管理服务岗口径

## 一、当前目标与状态

核心任务仍是上一份交接的延续:「岗位管理」数据统计与算法口径继续调整。本次已完成其中**数据归属层**的修复并上线,但用户明确过的「算法还要继续调整」(爆款判断、岗位评价逻辑)仍未开始,是下一阶段的第一优先级。

> 2026-08-31 22:02 续接修正：用户进一步确认运营存在「自己孵化自己的账号」，因此运营不再沿用下面一刀切的服务岗过滤。当前口径为：运营同时展示「自营孵化 / 服务账号」并拆分计数；文案、剪辑仍只统计给别人账号干的活。文案/剪辑岗位行已可展开查看当月全部账号和全部篇目。

本次完成的改动:

* 上一轮曾将运营/文案/剪辑三个服务岗 tab 都改为**只统计「给别人的账号干活」的日报**；其中运营口径已被上方续接修正取代，文案/剪辑继续有效。
* 触发原因:用户反馈「一堆运营岗,但都只运营了一个」。实查 8 月线上数据:33 个运营里 27 个是达人自运营自己账号刷屏,32 个文案里 26 个、30 个剪辑里 28 个同理。根因是第一轮重构把旧版「≥2 账号 + ≥1 个别人的账号」门槛整体拿掉时,顺带把「纯自干达人」也放进了服务岗列表。
* 已提交并推送 main:提交 `702d86ba`。全量 1266 项测试、tsc、78 路由生产构建全部通过。

## 二、用户关键原话

> "说错了,不是批量文案,而是批量运营"(前一句为:"目前我们的网站出现了大量的符合给至少一个其他人写文案的人,但他不满足要给两个人写文案这个要求啊,我刚刚一看一堆文案岗,但都只写了一个")

> "算法这里我要继续调整,目前还是有点问题"(上一份交接原话,仍然有效,本次未处理)

本次向用户提了两个选择题,用户选择:

1. 「运营/文案/剪辑 tab 要不要把只干自己账号活的人去掉」→ **只看给别人干活的(推荐项)**
2. 「只帮别人账号干过一两条的人还算吗」→ **算,照常展示(推荐项)**

## 三、已确认事实与待验证判断

### 已确认

* 服务岗归属字段不变:文案 `script_author_user_id`、剪辑 `video_editor_user_id`、运营 `operator_user_id`;达人 = `accounts.profile_id`。
* 新口径的过滤规则:日报的账号主人(`accounts.profile_id`)等于责任人本人 → 该条日报不计入服务岗列表与指标;账号 `profile_id` 为 null(未绑主人)时**按别人的账号计入**,避免漏掉真服务岗。
* 8 月线上真实数据(过滤前 → 过滤后):运营 33→6,文案 32→6,剪辑 30→2。真正的服务岗人员包括:张学(1 自有+1 别人)、吕琛(2 个别人的账号)、严一鸣(帮张皓宇 1 条)、梁聪敏(帮杜哲丞 1 条)、Test Admin(帮韦容强 2 条)等。
* 个人档案(`buildPersonPayload`)的 `records`/`currentMonth`/`trend` 保持全量(含给自己账号干的记录),仅 `operatorSummary` 因复用 `buildOperators` 而与运营 tab 同口径(纯自运营的人档案里不再显示运营摘要板块)。
* 达人 tab(`buildTalents`)、summary(`buildSummary`,健康条)、历史编辑链路均未改动。
* 上一轮遗留确认:`collaboration-workbench.tsx` 的 `react-hooks/set-state-in-effect` lint 错误是**预存问题**(HEAD 版本同样报),不是本次引入。
* 生产库混有测试数据:`Test Admin`、`00123`、`批次5-A组员2` 等测试账号的日报会出现在岗位列表,属于测试数据混入,尚未清理。

### 推测或待验证

* 爆款算法(≥3 万 + ≥3 条历史样本 + 最近最多 5 条均值×3)用户认为仍有问题,但具体是哪个维度未确认。上一份交接列的 9 个审查方向仍然有效。
* 过滤后岗位 tab 数字变小(如张学显示 1 条而不是 19 条),用户是否会困惑"指标只算给别人的"这一点,有待实际使用反馈。

## 四、已做决策与禁止重做

### 本次确定的

* 上一轮服务岗 tab 口径 = 「给别人的账号干活」；其中运营部分已被 22:02 的「自营孵化 / 服务账号」双口径取代。仍不恢复旧门槛「≥2 账号 + ≥1 个别人的」。
* 不给"帮别人干活"设最低条数门槛。
* 过滤实现在 `buildOperators`/`buildStaff` **函数内部**(不是调用层),所有调用方自动同口径,防止未来遗漏。
* 个人档案明细保持全量,不做过滤(它是个人工作完整视图,与岗位评价视图用途不同)。

### 继承上一份交接、仍然禁止

* 不要恢复「经手至少 2 个账号才展示」的旧规则;不要重建另一套岗位数据源;不要推翻岗位管理页面定位、单账号(别人的账号)人员展示、文案/剪辑具体作品查看、历史编辑完整链路(`GET /api/video-submit/edit-detail` + `POST /api/video-submit` mode=edit)。
* 前端明显的视觉/交互重做不要直接改,整理提示词交给 Antigravity;本次只做了文案微调(空状态提示、tooltip),属允许范围。
* 不要重做「岗位管理是什么」的调查,直接从算法真实结果和用户指出的异常案例继续。

## 五、当前关键上下文

* 核心改动文件:
  * `src/app/api/admin/collaboration/_shared.ts` — 新增 `isOtherAccount()`;`buildOperators`(current/previous 两个分组循环)与 `buildStaff`(分组循环)内跳过自账号日报;`buildCollaborationPageData`/`loadOperatorsData`/`loadStaffData` 保持原样(过滤已在聚合函数内部生效)。
  * `src/app/api/admin/collaboration/_shared.test.ts` — 新增 4 个测试:纯自运营过滤、混合归属只算别人的、无主账号计入、staff 过滤自写自剪。
  * `src/app/(app)/admin/collaboration/operator-tab.tsx` — 删除 `isSelfAccountOnly` 判定与「本人账号」标签(新口径下不可能出现);空状态与"负责账号" tooltip 文案更新。
  * `src/app/(app)/admin/collaboration/staff-tab.tsx` — 空状态文案更新。
  * `docs/数据口径.md` 第 11 节、`docs/reference/协作管理接口文档.md` B/C/D 部分 — 口径说明已更新。
* 22:02 续接改动：`buildOperators` 保留全部运营归属并输出 `selfOperatedAccountCount`、`serviceAccountCount` 与账号 `relation`；`buildStaff` 新增当月全部 `works` 并返回全部 `involvedAccounts`；两个岗位表均提供显式展开明细。
* `Test Admin` 已核实为旧测试管理员号（不是当前 `AI测试` 浏览器账号）：已清空其误挂在韦容强两条真实日报上的文案 1 次、剪辑 2 次、运营 2 次归属，日报与视频引用均回读为 0；账号已走正式成员生命周期归档并封禁。真实日报与作品未删除。
* 本次提交:`702d86ba feat(collaboration): 服务岗只统计给别人的账号干活的日报`(已 push main)。
* 爆款计算 `countHits()` 位置不变,服务岗的爆款数现在只统计给别人的账号干出来的作品(自然继承过滤)。
* 数据分析脚本(临时,已用完):scratchpad 下 `analyze-operators.mjs`/`analyze-staff.mjs`,走 `.env.local` 的 service role key,只读。node 需用绝对路径 `/usr/local/bin/node`;`npm run build` 能成功,直接 `node next build` 会因 Turbopack spawn 子进程环境问题失败。

## 六、待办与下一步

### 第一优先级:继续调整岗位算法(用户明确说"还要继续调整,目前还是有点问题")

本次只解决了"哪些人该出现在岗位列表"的归属层。爆款识别、播放类指标是否合理、岗位评价逻辑仍未动。上一份交接的 9 个审查方向(绝对播放 vs 相对基线、5 条均值被极端值拉高、统一 3 万门槛对不同体量账号的失真、新账号样本少、产量 vs 质量评价、共作品多岗归因、大号放大效应、环比可比性、四岗位是否该用不同评价逻辑)依然是下一步的审查清单。先拿真实数据做反例验证,再动代码。

### 第二优先级:清理生产库测试账号

`Test Admin` 已完成精确清理和归档。`00123`、`批次5-A组员2` 等测试夹具仍会出现在岗位列表，未获本轮扩大清理授权，继续作为预存问题保留；清理前必须逐个确认真实用途和精确影响范围。

### 第三优先级:预存问题

* `collaboration-workbench.tsx` 的 `react-hooks/set-state-in-effect` lint 错误(HEAD 上就存在,`useEffect(() => setTab(defaultTab), [defaultTab])`)。
* 工作区有 **Antigravity 的未提交改动**(`src/app/(app)/admin/fulfillment/fulfillment-workbench.tsx`、`page.tsx`,双视角工作台)和 `日志/2026-08-31.md` 的 An 记录行。按规则 Antigravity 改动必须用户确认后才能 push,本次提交刻意未包含这些文件。

## 七、历史摘要

「协作管理」→「岗位管理」第一轮重构(`53703e76`)完成:全量展示岗位人员、取消跨账号门槛、运营账号拆分、文案/剪辑具体作品查看、历史责任人完整编辑链路。爆款规则从「2 万 + 1 条样本」改为「3 万 + 3 条样本 + 最近最多 5 条均值×3」。

本轮(交接后继续):用户反馈岗位列表被"只干一个账号的人"淹没,实查确认为达人自干刷屏。经用户确认,服务岗 tab 改为只统计「给别人的账号干活」的日报并已上线。数据归属层至此闭环;下一阶段回到用户真正关心的「算法评价层」:爆款判断与岗位数据评价是否准确。

续接后再次修正：运营的自营孵化属于真实岗位工作，不应从运营身份中消失；当前通过关系拆分解决「要保留自营」与「不能把自营混成服务岗」的冲突。文案/剪辑仍维持给别人账号干活的服务岗口径。
