# 交接：历史日报完整编辑与岗位管理发布

## 一、当前目标与状态

- 用户最终要把“今日提交”里的历史记录与历史日期已提交作品，统一接入完整编辑表单，使历史文案、剪辑、运营责任人可查看、回填和修改；同时保留 Antigravity 已完成的岗位管理 UI 调整，最后统一提交并 push 到 `main`。
- 当前功能代码已完成，前后端改动都在共享工作区；尚未提交、尚未 push。
- 当前分支：`main`；`HEAD` 与 `origin/main` 都是 `53703e76 feat(collaboration): 重构为岗位月报`。
- 工作区仍有 16 个改动路径，见“当前关键上下文”。

## 二、用户关键原话

> “可以，但前端部分要给提示词，我让 Antigravity去做，刚刚你改的前端部分，我也让 Antigravity 去修复了并调了其他 ui，到时候一起你 push，执行吧”

> “Antigravity 完成了”

> “移交上下文给 glm”

用户约束：前端 UI 由 Antigravity 负责；Codex 负责逻辑、数据、接口和功能 bug；Antigravity 改动必须确认后才能 push；最终需要把两边改动一起提交。

## 三、已确认事实与待验证判断

### 已确认

- 历史记录入口在 `src/app/(app)/dashboard/video-submit-panel-v2.tsx`，历史列表在 `history-list.tsx`。
- 点击历史记录“查看并修改”会关闭历史列表，设置对应 `account_id` 与 `report_date`，进入 `editToday`，调用 `GET /api/video-submit/edit-detail`。
- 详情完整后渲染 `VideoSubmitFormV2`，标题为 `修改历史作品 · YYYY-MM-DD`；表单保留旧指标、截图、标签、文案、发布时间、导粉话术和三岗位责任人。
- 历史责任人来自 `assigneeProfiles`；已归档成员显示历史身份，未修改时服务端允许保留；改选只允许同队在职成员。
- 保存仍走 `POST /api/video-submit` 的 `mode: "edit"`；服务端 `edit-binding.ts` 会再次核对视频、日报、日期和 24h 快照绑定，详情加载失败时前端禁止保存。
- `buildSubmissionAssigneeColumns()` 在 `src/app/api/video-submit/edit-binding.ts` 中生成一组三岗位数据库列，`src/app/api/video-submit/route.ts` 对 `videos` 和 `daily_reports` 两张表复用同一映射，避免历史编辑只改一张表。
- 选题关联已有 `resolveEditTopicId()` 保护：编辑请求没有 `topic_id` 时保留原视频关联；已有回归测试，不需要重复改动。
- 旧的 `src/app/(app)/dashboard/history-report-edit-form.tsx` 与对应单测已删除；`dialog-layout.test.ts` 已移除对死组件的断言。
- `VideoSubmitPanelV2` 的浏览器缓存读取已从 SSR 初始状态移到 effect 异步调度，修复 hydration mismatch；历史详情 loading 也做了异步调度，避免新增 React Compiler lint 债。
- `/dashboard` 浏览器实测标题为“工作台 - DYData”，页面正常渲染，控制台 error 为空。
- 浏览器实测历史完整记录 `2026-08-28 / 测试一下`：进入后标题为 `修改历史作品 · 2026-08-28`，已回填两张截图、指标、视频标题/文案和“文案/剪辑/运营”三个责任人入口；未点击保存。
- 浏览器实测数据不完整的历史记录 `2026-07-15`：显示“无法安全加载原视频详情”和“编辑详情不完整：24h 指标 avg_play_duration 缺失”，提供“重新加载”“返回摘要”，没有开放保存；这是预期的防空值覆盖行为。
- `/admin/collaboration` 浏览器实测标题为“岗位管理 - DYData”，岗位页与真实数据正常渲染，控制台 error 为空；达人/运营/文案/剪辑 tab 均在页面中。
- `npm test`：`1260/1260` 通过。
- 定向历史编辑、状态、详情、对话框测试：`58/58` 通过；最近一次面板相关测试：`20/20` 通过。
- `npx tsc --noEmit --pretty false`：通过。
- `npm run build`：通过；Next.js 16.3.3 生产构建生成 78 个路由。
- `git diff --check`：通过。

### 推测或待验证

- 全量 lint 当前不能作为本批发布门禁：仓库现有 React Compiler 规则报错约 62 个，分布在 AI 配置、内容诊断、登录、选题库等未改模块。本次改动文件仍有旧 lint 问题（大表单和面板的 effect/未使用变量），不是本次历史编辑新增的逻辑错误。
- 需要在提交前再次检查最终 diff，确认没有 Antigravity 后续追加的未审 UI 逻辑；不要因为 lint 全站失败去大范围修 unrelated 模块。

## 四、已做决策与禁止重做

- 采用完整 `VideoSubmitFormV2` 编辑链路，不恢复旧的简化历史弹窗；原因是旧弹窗无法安全保留截图、指标、标签、导粉话术和岗位字段。
- 详情不完整时阻止保存，不用 0、null 或默认值覆盖历史数据。
- 不在本次任务改数据库结构、RLS、权限模型或 API 路径；沿用现有服务端绑定校验。
- 不替换 Antigravity 的 UI 设计；只允许修复确定的逻辑/运行时问题。
- 不执行真实浏览器提交，浏览器验收只读验证，避免修改测试库中的真实历史记录。
- 不要重复追查已确认的 `topic_id` 清空风险；服务端已有 `resolveEditTopicId` 与测试保护。

## 五、当前关键上下文

### 当前工作区改动

```text
 M AGENTS.md
 M docs/全站模块地图.md
 M src/app/(app)/admin/collaboration/collaboration-data-container.tsx
 M src/app/(app)/admin/collaboration/operator-tab.tsx
 M src/app/(app)/admin/collaboration/staff-tab.tsx
 D src/app/(app)/dashboard/history-report-edit-form.test.ts
 D src/app/(app)/dashboard/history-report-edit-form.tsx
 M src/app/(app)/dashboard/video-submit-form-v2-layout.test.ts
 M src/app/(app)/dashboard/video-submit-form-v2.tsx
 M src/app/(app)/dashboard/video-submit-panel-state.test.ts
 M src/app/(app)/dashboard/video-submit-panel-v2.tsx
 M src/app/api/video-submit/edit-binding.test.ts
 M src/app/api/video-submit/edit-binding.ts
 M src/app/api/video-submit/route.ts
 M src/components/ui/dialog-layout.test.ts
 M 日志/2026-08-31.md
?? docs/plans/上下文交接/交接-2026-08-31-历史编辑发布.md
```

### 重点文件和入口

- `src/app/(app)/dashboard/video-submit-panel-v2.tsx`
  - `handleHistoryReportOpen()`：设置账号、日期和 `requestedMode="editToday"`。
  - `fetchVideoSubmissionEditDetail()` effect：加载中/失败/成功三态。
  - `shouldShowHistoricalSubmittedCard`：历史已提交摘要；编辑时隐藏摘要并显示完整表单。
  - `VideoSubmitFormV2` 传入 `editDetail`、`initialBizDate`、`onCancel`、`onSubmitted`。
- `src/app/(app)/dashboard/video-submit-form-v2.tsx`
  - `createMetaFromEditDetail()`、`createEditableFieldsFromEditDetail()`、`createEditableSlotsFromEditDetail()` 完整回填。
  - `resolveVideoSubmitMode()` 会把带 `editPayload.video_id` 的 `editToday` 转成 API `mode: "edit"`。
  - `editDetailError` 或缺少完整 edit payload 时停止保存。
  - `mode === "editToday"` 且 `initialSummary` 存在时提交按钮文案为“保存修改”。
- `src/app/api/video-submit/edit-detail/route-core.ts`
  - 账号必须属于当前用户；日报、视频、24h 快照必须各自唯一且绑定当前账号/日期；只按原三岗位 ID 查询档案。
- `src/app/api/video-submit/edit-binding.ts`
  - 原视频/日报/快照绑定核对与三岗位列映射。
- `src/app/api/video-submit/route.ts`
  - 编辑写入 `videos` + `daily_reports`，保留历史指标/截图/topic 关联，校验新责任人。
- `docs/全站模块地图.md`
  - 已补充 2026-08-31 历史修改能力说明。
- `AGENTS.md`
  - 已补充浏览器验收分级规则；这是已有工作区改动的一部分，提交前确认是否按本次用户要求一起带上。

## 六、待办与下一步

1. GLM 接手后先执行 `git diff --stat`、重点文件 diff 和 `git diff --check`，确认 Antigravity 改动边界。
2. 再跑一次最小定向测试和 `npx tsc --noEmit --pretty false`；无需重复修改已通过的业务逻辑。
3. 若用户/GLM决定继续发布，运行 `npm test`、`npm run build`；记录全量 lint 的“预存失败”而不是修 unrelated 模块。
4. 检查 `日志/2026-08-31.md` 是否已有本次完整编辑记录；必要时只追加一行 `[CX]` 结论，避免覆盖其他 agent 日志。
5. 确认 Antigravity 改动后，统一 `git add` 本次相关文件，提交一个清晰 commit；根据项目规则 push `main`。
6. push 后必须核对远端 `main` 提交号，并向用户汇报文件、commit、测试、构建、浏览器结果；不要把 push 成功表述为 Vercel 已部署成功。

## 七、历史摘要

- 早前已将“协作管理”按用户真实用途改为“岗位管理”：按达人/运营/文案/剪辑统计考勤相关日报、账号、作品数和数据；爆款门槛改为播放至少 30,000、至少 3 条更早同账号样本且达到此前最多 5 条均值 3 倍。
- 早前用户反馈 Next.js `AdaptiveSheet` invalid hook call，根因是旧 Webpack dev 进程与更新后的 node_modules 依赖图混用；重启旧 dev 进程后 `/dashboard` 与 `/admin/collaboration` 正常。
- 同次排查发现 dashboard 首屏 hydration mismatch 来自 SSR 读取 `sessionStorage/localStorage`；已移到 effect，并有静态回归测试。
- 用户要求 Antigravity 负责前端 UI，Codex 负责后端和逻辑，最后两边改动一起 push；因此当前工作区故意保持未提交，等待最终确认。
