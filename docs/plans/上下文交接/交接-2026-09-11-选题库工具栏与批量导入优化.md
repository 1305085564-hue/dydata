# 选题库界面视觉降噪与交互优化上下文交接

> 生成时间：2026-09-11 22:10  
> 适用场景：新窗口/新 Agent 接手选题库（`/topics`）改动审查、测试及后续任务。

---

### 一、当前目标与状态

- **用户最终要什么**：
  打造符合 Claude 哲学的高质感、无噪声的选题库（`/topics`）工作台。
- **当前做到哪里**：
  视觉与交互已全部实现，已在真实浏览器环境（公司负责人「阿禅」账号登录态）完成截图与交互验证。
- **已完成什么**：
  1. **母题去重**：右上角冗余的母题多选下拉彻底拔除，统一由下方横向平铺栏承载。
  2. **批量导入收拢**：右侧独立的「批量导入」按钮收敛进「录入选题」统一弹窗内部（分段切换 Tab），外层仅保留唯一陶土橙 CTA「录入选题」。
  3. **视觉标准回归**：排除了因过度降噪导致的“字号变小、色深发灰”问题，文字回归深墨色（`#292524`），搜索框保留边框与微阴影。
  4. **工具栏重构**：
     - 视图切换（卡片 vs 表格）合并为单键，点击直接切换，Hover 动态提示「切换为表格视图 / 切换为卡片视图」；
     - 筛选下拉标准化：「最新」「时间」「更多」统一为纯文字 + 右侧 `ChevronDown` 箭头，去除了更多左侧多余的滑块图标。
- **当前还存在什么问题**：
  代码尚未提交（未 commit / 未 push），工作区处于已验证就绪状态，等待新 Agent 进行全面审查或后续任务。

---

### 二、用户关键原话

1. *"批量导入放入录入选题里面吧，录入选题，然后里面可以选择批量录入，符合 claude 哲学，设计好"*
2. *"筛选栏较于线上，是不是整体小了一号？色深和字号似乎都是"*
3. *"回到线上的标准吧"*
4. *"表格和列表这里现在是两个按键，直接改成一个，点击切换，划入显示字切换 xx，并有切换暗示，更多这里的下拉标识跟最新和时间保持一致吧，他现在没有点击的暗示"*
5. *"更多左侧那个还留着干嘛"*
6. *"你看这里的三个下拉加一个切换方这怎么样？可以不"*
7. *"这套你先实现看看"*
8. **部署铁律**：`AGENTS.md` 规定，Antigravity 改动**绝不能自动 push**，必须确认后才能 push。

---

### 三、已确认事实与待验证判断

- **已确认：**
  - 本地前端开发服务器（`localhost:3000`）正常运行中。
  - 使用 Playwright 登录阿禅账号（`1305085564@qq.com`）进入 `/topics` 实测：
    - 工具栏排布：`[ 搜索框 ] [ 最新 ⌵ ] [ 时间 ⌵ ] [ 更多 ⌵ ] | [ 切换方块 ] [ + 录入选题 ]`。
    - 点击切换方块，卡片视图与表格视图无缝切换，图标与 tooltip 状态联动正确。
    - 点击「录入选题」，弹窗顶部「单条录入」与「批量导入」分段切换流畅，文件拖拽解析、预览表格、错误校验链路完整。
  - 自动化契约测试全部通过：`npx tsx --test src/components/topics-v2/topics-v3-frontend-contracts.test.ts src/lib/a11y-responsive-integration.test.ts`（37 passed, 0 failed）。
  - TypeScript 类型检查完全通过：`npx tsc --noEmit`（0 error）。
- **推测或待验证：**
  - 无，所有链路均已由真实浏览器运行并验证通过。

---

### 四、已做决策与禁止重做

- **已确定的方案**：
  - **单键视图切换**：不恢复双并排按钮，保持紧凑单键；
  - **批量导入入口**：不恢复外层独立按钮，保持收拢在录入选题弹窗内部；
  - **更多筛选**：保持纯文字 + 右侧小箭头，不添加左侧装饰性图标。
- **禁止重做**：
  - 禁止在右侧工具栏重新添加母题下拉；
  - 禁止将深墨色字（`#292524`）再次调淡为发灰的弱化色。

---

### 五、当前关键上下文

#### 修改文件列表
- `src/components/topics-v2/TopicPoolExplorer.tsx`：工具栏、下拉组件、视图切换、样式复原
- `src/components/topics-v2/TopicCreateModal.tsx`：弹窗内嵌批量导入 Tab 及文件拖拽解析闭环
- `src/components/topics-v2/TopicHubV2.tsx`：解耦旧批量导入弹窗，注入统一入库链路
- `src/components/topics-v2/topics-v3-frontend-contracts.test.ts`：更新契约断言以匹配新布局
- `日志/2026-09-11.md`：改动时间线记录

#### 关键验证命令
```bash
# 1. 类型检查
npx tsc --noEmit

# 2. 契约与单元测试
npx tsx --test src/components/topics-v2/topics-v3-frontend-contracts.test.ts src/lib/a11y-responsive-integration.test.ts
```

---

### 六、待办与下一步

1. **第一优先级**：在新窗口中执行代码审查（依据上一轮提供的审查 Prompt），确认有无隐蔽缺陷或边界异常；
2. **第二优先级**：阿禅验收通过后，执行 Git commit，经阿禅确认后 push 到 `main` 分支；
3. **阻塞点**：无技术阻塞。
