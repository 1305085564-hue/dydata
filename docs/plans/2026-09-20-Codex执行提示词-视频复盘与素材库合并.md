# Codex 执行提示词：视频复盘与素材库合并

> 任务编号：2026-09-20-merge-content-videos  
> 预计工期：2-3 天  
> 项目路径：/Users/mac/Projects/dydata  
> 完成进度追踪：docs/plans/2026-09-20-视频复盘与素材库合并-完成进度.md

---

## 任务目标

**废掉素材库独立页面，保留视频复盘页面，用素材库的抽屉替换复盘抽屉，底部加双截图功能。**

核心方案：**视频复盘的列表 + 素材库的抽屉（底部加双截图）**

---

## 前置条件确认

1. **数据验证已完成**：见 `docs/plans/2026-09-20-数据验证报告.md`
2. **删除清单已确认**：
   - ✅ AI 诊断分析（零使用）
   - ✅ 素材评级 S/A/B/C（零使用）
   - ✅ 人工复盘状态字段前端显示（零使用）
   - ✅ 资产统计条（零使用）
   - ⚠️ 素材库页面的视频标签展示区块（只删展示，保留投稿流程）
3. **必须保留**：回收站（18 次真实使用）、双截图（85.4% 覆盖率）

---

## 实施步骤（共 5 个阶段）

### 阶段 2：列表筛选器迁移（0.5天）

#### 目标
把素材库的筛选功能搬到视频复盘列表

#### 具体实施步骤

**2.1 提取素材库筛选器组件**
- 源文件：`src/app/(app)/admin/videos/video-list.tsx`
- 找到筛选器相关代码（通常在组件顶部）：
  - 人员筛选下拉（profiles）
  - 账号筛选下拉（accounts）
  - 日期范围选择器（起止日期）
  - 关键词搜索输入框
  - 重置按钮

**2.2 迁移到视频复盘列表**
- 目标文件：`src/app/(app)/admin/content/content-list.tsx`
- 在列表顶部（现有 Tab 下方）插入筛选器
- 复用素材库的筛选逻辑和样式
- 保持视频复盘现有的 12 项指标列不变

**2.3 实现筛选功能**
```typescript
// 筛选状态管理示例
const [filters, setFilters] = useState({
  userId: '',      // 人员筛选
  accountId: '',   // 账号筛选
  startDate: '',   // 起始日期
  endDate: '',     // 结束日期
  keyword: '',     // 关键词
});

// 筛选逻辑
const filteredVideos = videos.filter(video => {
  if (filters.userId && video.user_id !== filters.userId) return false;
  if (filters.accountId && video.account_id !== filters.accountId) return false;
  if (filters.startDate && video.published_at < filters.startDate) return false;
  if (filters.endDate && video.published_at > filters.endDate) return false;
  if (filters.keyword && !video.video_title.includes(filters.keyword)) return false;
  return true;
});
```

**2.4 保留异常状态颜色标记**
确保列表中的异常状态颜色标记正常显示：
- 🔴 删稿/限流（红色 #C9604D）
- 🟠 腰斩/异常（橙色 #B98A54）
- 🟢 正常（绿色 #6FAA7D）

**2.5 URL 参数传递**
```typescript
// 使用 Next.js searchParams 传递筛选状态
const searchParams = useSearchParams();
const router = useRouter();

// 筛选变化时更新 URL
const updateFilters = (newFilters) => {
  const params = new URLSearchParams(searchParams);
  Object.entries(newFilters).forEach(([key, value]) => {
    if (value) params.set(key, value);
    else params.delete(key);
  });
  router.push(`/admin/content?${params.toString()}`);
};
```

#### 验收标准
- [ ] 筛选器 UI 与素材库一致
- [ ] 人员/账号/日期/关键词筛选功能正常
- [ ] 异常状态颜色标记清晰可见
- [ ] URL 参数可传递筛选状态（可分享链接）

#### Git Commit
```bash
git add src/app/(app)/admin/content/content-list.tsx
git commit -m "feat: 视频复盘列表增加筛选器

- 从素材库迁移筛选器组件
- 支持按人员、账号、日期、关键词筛选
- 保留异常状态颜色标记（红/橙/绿）
- URL 参数传递筛选状态

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 阶段 3：抽屉替换与双截图迁移（1天）

#### 目标
用素材库的抽屉替换复盘抽屉，底部加双截图功能

#### 具体实施步骤

**3.1 复制素材库抽屉组件**
```bash
# 复制抽屉组件文件
cp src/app/(app)/admin/videos/video-detail-dialog.tsx \
   src/app/(app)/admin/content/content-detail-dialog.tsx
```

**3.2 修改抽屉组件，保留核心功能**

在 `content-detail-dialog.tsx` 中保留：
- ✅ 完整数据指标展示（18 项指标）
- ✅ 视频文案查看和一键复制按钮
- ✅ 外链跳转到原视频按钮（"打开源视频网页"）
- ✅ 回收站操作按钮（移入回收站、恢复、永久删除）

**从复盘抽屉迁移的功能**：
- ✅ 选题库入库按钮（从 `content-diagnosis-workbench.tsx` 提取）

**3.3 提取并迁移双截图功能**

从 `src/app/(app)/admin/content/content-diagnosis-workbench.tsx` 提取截图对比代码：

```typescript
// 读取截图 URL
const trafficCurveUrl = snapshot?.traffic_curve_screenshot_url;
const retentionChartUrl = snapshot?.retention_screenshot_url;

// 在抽屉底部添加折叠面板
<Collapsible>
  <CollapsibleTrigger>
    <h3>手机截图对比</h3>
  </CollapsibleTrigger>
  <CollapsibleContent>
    <div className="grid grid-cols-2 gap-4">
      {/* 左侧：流量曲线图 */}
      {trafficCurveUrl ? (
        <div>
          <p className="text-sm text-gray-500 mb-2">流量曲线</p>
          <img src={trafficCurveUrl} alt="流量曲线" />
        </div>
      ) : (
        <div className="text-center text-gray-400">
          暂无流量曲线截图
        </div>
      )}
      
      {/* 右侧：留存脱落图 */}
      {retentionChartUrl ? (
        <div>
          <p className="text-sm text-gray-500 mb-2">留存脱落</p>
          <img src={retentionChartUrl} alt="留存脱落图" />
        </div>
      ) : null}
    </div>
  </CollapsibleContent>
</Collapsible>
```

**重要注意**：
- 字段名是 `traffic_curve_screenshot_url` 和 `retention_screenshot_url`（不是文档里的 `_url`）
- 流量曲线图覆盖率 38.3%，需要做空态提示
- 留存脱落图覆盖率 85.4%，不显示空态

**3.4 删除不用的功能**

在新抽屉中删除以下区块：
- ❌ AI 诊断分析区块（`<AIAnalysisSection />` 或类似组件）
- ❌ 多参照对比区块（`<ComparisonSection />` 或类似组件）
- ❌ 复制诊断话术按钮
- ❌ 素材评级 S/A/B/C 选择器（`<AssetRatingSelect />` 或类似组件）
- ❌ 人工备注输入框（`<AssetNoteInput />` 或类似组件）

**3.5 修改列表点击事件**

在 `content-list.tsx` 中：
```typescript
// 修改前（旧抽屉）
import { ContentDiagnosisWorkbench } from './content-diagnosis-workbench';

// 修改后（新抽屉）
import { ContentDetailDialog } from './content-detail-dialog';

// 点击行时打开新抽屉
<TableRow onClick={() => setSelectedVideo(video)}>
  ...
</TableRow>

{/* 渲染新抽屉 */}
<ContentDetailDialog
  video={selectedVideo}
  onClose={() => setSelectedVideo(null)}
/>
```

**3.6 测试所有功能**
- [ ] 点击列表任意视频，打开新抽屉
- [ ] 抽屉显示完整 18 项指标
- [ ] 视频文案可查看和复制
- [ ] 外链按钮正常跳转
- [ ] 选题库入库按钮正常工作
- [ ] 回收站操作正常（移入/恢复/永久删除）
- [ ] 双截图折叠面板显示正常（有截图时）
- [ ] 流量曲线图 38.3% 无图时显示空态提示
- [ ] 不再显示 AI 诊断、对比、评级等旧功能

#### 验收标准
- [ ] 新抽屉功能完整可用
- [ ] 双截图正常显示（有截图时），空态处理正确
- [ ] 所有删除的功能全仓 grep 零引用

#### Git Commit
```bash
git add src/app/(app)/admin/content/
git commit -m "feat: 用素材库抽屉替换复盘抽屉，底部加双截图

- 复制素材库抽屉组件为基础
- 保留完整指标、文案复制、外链、选题库、回收站
- 从复盘抽屉迁移双截图功能到底部折叠面板
- 流量曲线图 38.3% 覆盖率，做空态处理
- 删除 AI 诊断、多参照对比、评级、备注等零使用功能

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 阶段 4：素材库页面废弃与重定向（0.5天）

#### 目标
删除素材库独立页面，设置 301 重定向

#### 具体实施步骤

**4.1 修改素材库页面为重定向**

编辑 `src/app/(app)/admin/videos/page.tsx`：
```typescript
import { redirect } from 'next/navigation';

export default function VideosRedirectPage() {
  redirect('/admin/content');
}
```

**4.2 删除导航菜单中的"素材库"入口**

编辑 `src/lib/nav-bar-items.ts`：
```typescript
// 找到素材库的导航项，整个删除
// 大概是这样的结构：
{
  path: '/admin/videos',
  name: '素材库',
  icon: VideoIcon,
  permission: hasNavPermission('manage_videos'),
}

// 删除上面这个对象
```

**4.3 保留"视频复盘"作为唯一入口**

确认 `nav-bar-items.ts` 中视频复盘导航项存在：
```typescript
{
  path: '/admin/content',
  name: '视频复盘',
  icon: VideoIcon,
  permission: (user) => 
    hasNavPermission('review_content')(user) || 
    hasNavPermission('manage_videos')(user),
}
```

**4.4 保留素材库的 API 路由**

**重要：不要删除以下 API 文件**，新抽屉依赖它们：
- `src/app/api/admin/videos/list/route.ts`（查询用）
- `src/app/api/admin/video-assets/[videoId]/route.ts`（详情用）
- `src/app/api/admin/videos/[videoId]/lifecycle/route.ts`（回收站用）

**4.5 测试重定向和导航**
- [ ] 访问 `/admin/videos` 自动跳转到 `/admin/content`
- [ ] 导航菜单只剩"视频复盘"一个入口
- [ ] 新抽屉的所有功能正常（API 依然可用）

#### 验收标准
- [ ] 重定向正常工作
- [ ] 导航菜单无"素材库"项
- [ ] 抽屉功能不受影响（API 保留）

#### Git Commit
```bash
git add src/app/(app)/admin/videos/page.tsx src/lib/nav-bar-items.ts
git commit -m "refactor: 废弃素材库独立页面，设置重定向

- /admin/videos 重定向到 /admin/content
- 导航菜单删除"素材库"入口
- 保留所有 API 路由供抽屉使用
- 旧链接全部兼容

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 阶段 5：清理死功能代码（0.5天）

#### 目标
删除数据验证确认零使用的功能代码

#### 具体实施步骤

**5.1 素材库页面的视频标签展示区块清理**

⚠️ **重要边界**：只删素材库页面的展示区块，**不删标签表和投稿流程**

在 `video-list.tsx` 或 `video-detail-dialog.tsx` 中找到标签展示代码：
```typescript
// 类似这样的代码，删除它
<div className="tags">
  {videoTags.map(tag => (
    <Badge key={tag.id}>{tag.name}</Badge>
  ))}
</div>
```

**不要删除**：
- ❌ 不删 `/api/video-tags` 路由（投稿流程依赖）
- ❌ 不删 `video_tags` 表查询逻辑（后端需要）
- ❌ 不删投稿表单中的标签选择器

**只删除**：
- ✅ 素材库列表中的标签列展示
- ✅ 素材库详情抽屉中的标签区块
- ✅ 素材库 loader 中给前端用的 `videoTags` 派生逻辑（如果只是为了显示）

**5.2 素材评级清理**

删除详情抽屉的评级区块（应该在阶段 3 已删除，这里确认）：
```typescript
// 删除类似这样的代码
<Select value={assetLevel} onChange={setAssetLevel}>
  <option value="S">S 级</option>
  <option value="A">A 级</option>
  <option value="B">B 级</option>
  <option value="C">C 级</option>
</Select>
```

如果 `/api/admin/video-assets/[videoId]/route.ts` 中有 `asset_level` 写入逻辑，也删除：
```typescript
// 删除这样的代码
await supabase
  .from('videos')
  .update({ 
    asset_level: body.asset_level,  // 删除这行
    asset_note: body.asset_note      // 删除这行
  })
  .eq('id', videoId);
```

**数据库字段保留**：不删 `videos.asset_level` / `asset_note` 列

**5.3 资产统计条清理**

在素材库列表顶部找到统计条：
```typescript
// 删除类似这样的代码
<div className="stats">
  <span>已入库 {assetSummary.inLibrary}</span>
  <span>待整理 {assetSummary.pending}</span>
  <span>已评级 {assetSummary.rated}</span>
</div>
```

删除 loader 中的 `assetSummary` 派生逻辑（`admin-videos-page.ts`）：
```typescript
// 删除类似这样的代码
const assetSummary = {
  inLibrary: videos.filter(v => v.topic_id).length,
  pending: videos.filter(v => !v.asset_level).length,
  rated: videos.filter(v => v.asset_level).length,
};
```

**5.4 人工复盘状态字段前端显示清理**

删除复盘列表中的 `review_status` 显示：
```typescript
// 删除类似这样的代码
<Badge variant={video.review_status === 'completed' ? 'success' : 'warning'}>
  {video.review_status}
</Badge>
```

删除复盘抽屉中的"标记已复盘"按钮（如果还有）：
```typescript
// 删除类似这样的代码
<Button onClick={markAsReviewed}>
  标记已复盘
</Button>
```

**数据库字段保留**：不删 `videos.review_status` / `reviewed_at` / `reviewed_by` 列

**5.5 复盘旧抽屉代码清理**

删除整个旧抽屉文件：
```bash
rm src/app/(app)/admin/content/content-diagnosis-workbench.tsx
```

删除归因对比相关文件（如果存在）：
```bash
rm src/app/(app)/admin/content/use-content-comparison.ts
# 或其他相关文件
```

**保留后端 API**：
- ✅ 保留 `/api/admin/content-attribution/[videoId]`（未来可能用）
- ✅ 保留 `/api/admin/content-analysis`（未来可能用）

**5.6 全仓 grep 确认零引用**

对每个删除的功能，运行 grep 确认无残留引用：
```bash
# 检查评级相关
grep -r "asset_level" src/ --include="*.tsx" --include="*.ts"
grep -r "AssetRating" src/ --include="*.tsx" --include="*.ts"

# 检查复盘状态相关
grep -r "review_status" src/ --include="*.tsx" --include="*.ts"
grep -r "reviewed_at" src/ --include="*.tsx" --include="*.ts"

# 检查资产统计相关
grep -r "assetSummary" src/ --include="*.tsx" --include="*.ts"

# 检查旧抽屉相关
grep -r "ContentDiagnosisWorkbench" src/ --include="*.tsx" --include="*.ts"
grep -r "content-diagnosis-workbench" src/ --include="*.tsx" --include="*.ts"
```

如果 grep 返回结果，逐一检查并清理。

#### 验收标准
- [ ] 每个删除的功能点独立 commit
- [ ] 全仓 grep 确认零引用
- [ ] 所有测试仍然通过

#### Git Commit（分批 commit）
```bash
# Commit 1: 删除素材库页面的标签展示
git add src/app/(app)/admin/videos/
git commit -m "chore: 删除素材库页面的视频标签展示区块

- 只删素材库页面的标签展示
- 保留标签表和投稿流程（今天仍在产生数据）
- 保留 /api/video-tags 路由

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"

# Commit 2: 删除素材评级
git add src/
git commit -m "chore: 删除素材评级功能（零使用）

- 删除评级 UI 组件
- 删除 API 中的 asset_level 写入逻辑
- 保留数据库字段

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"

# Commit 3: 删除资产统计条
git add src/
git commit -m "chore: 删除资产统计条功能（零使用）

- 删除列表顶部统计条
- 删除 loader 中的 assetSummary 派生逻辑

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"

# Commit 4: 删除人工复盘状态字段显示
git add src/
git commit -m "chore: 删除人工复盘状态字段前端显示（零使用）

- 删除 review_status 显示
- 删除"标记已复盘"按钮
- 保留数据库字段

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"

# Commit 5: 删除复盘旧抽屉
git add src/
git commit -m "chore: 删除复盘旧抽屉代码（已被新抽屉替代）

- 删除 content-diagnosis-workbench.tsx
- 删除归因对比相关组件
- 保留后端 API

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 阶段 6：测试与验收（0.5天）

#### 目标
确保改造后功能完整可用

#### 功能验收清单

**导航与路由**
- [ ] 导航菜单只有"视频复盘"入口
- [ ] 访问 `/admin/videos` 自动跳转到 `/admin/content`

**列表功能**
- [ ] 列表顶部有完整筛选器（人员/账号/日期/搜索）
- [ ] 异常状态颜色标记清晰（红/橙/绿）
- [ ] 筛选功能正常（按人员/账号/日期过滤）
- [ ] 列表保持 12 项核心指标展示

**抽屉功能**
- [ ] 点击任意视频，打开素材库风格的抽屉
- [ ] 抽屉包含：完整指标 + 文案复制 + 外链 + 选题库 + 回收站
- [ ] 抽屉底部折叠面板显示双截图（如有）
- [ ] 流量曲线图空态提示正确显示
- [ ] 选题库入库按钮正常工作
- [ ] 回收站操作正常（移入/恢复/永久删除）

**删除功能确认**
- [ ] 不再显示 AI 诊断
- [ ] 不再显示多参照对比
- [ ] 不再显示评级功能
- [ ] 不再显示复制诊断话术
- [ ] 不再显示资产统计条
- [ ] 不再显示人工复盘状态

#### 回归测试
```bash
# 运行测试套件
npm test

# 手动测试权限
# 1. 用有 review_content 权限的账号登录
# 2. 确认能访问 /admin/content
# 3. 确认所有功能正常
```

#### 性能检查
- [ ] 列表加载时间 < 2 秒（本地测试）
- [ ] 抽屉打开时间 < 1 秒
- [ ] 筛选响应时间 < 500ms

#### 文档更新

**更新全站模块地图**

编辑 `docs/全站模块地图.md`：
```markdown
# 删除这行
| 视频管理 | admin/videos | manage_videos | 素材库、回收站、补录24h、评级 | An 全量视觉重塑 | ✅ P0-P2完成 |

# 修改视频复盘条目为
| 视频复盘 | admin/content | review_content / manage_videos | 
  列表（12项指标、筛选器、异常标记）+ 
  抽屉（完整指标、文案复制、外链、选题库入库、回收站、双截图）| 
  An 全量视觉重塑 | ✅ 已合并统一入口 |
```

**更新权限说明**

编辑 `docs/权限与安全说明.md`，在相关章节补充：
```markdown
#### review_content + manage_videos 权限说明
- `/admin/content`（视频复盘）：需要 `review_content` 或 `manage_videos`
- 导航显示：两者任一满足即可
- 抽屉回收站功能：需要 `manage_videos`
- 权限捆绑：目前 admin 角色两者都有，member 两者都无
```

**新增记忆文件**

创建 `~/.claude/projects/-Users-mac-Projects-dydata/memory/项目-视频复盘与素材库合并-2026-09-20.md`：
```markdown
---
name: 项目-视频复盘与素材库合并-2026-09-20
description: 视频复盘与素材库职责冲突解决：合并为统一入口，用素材库抽屉替换复盘抽屉
metadata:
  type: project
---

## 合并方案
- 废掉素材库独立页面 `/admin/videos`（重定向到 `/admin/content`）
- 保留视频复盘列表（12项指标 + 筛选器）
- 用素材库的抽屉替换复盘抽屉（底部加双截图）

## 删除的功能（零使用）
1. AI 诊断分析（3 条调试残留）
2. 素材评级 S/A/B/C（零使用）
3. 人工复盘状态字段前端显示（零使用）
4. 资产统计条（零使用）
5. 素材库页面的视频标签展示区块（只删展示，保留投稿流程）

## 保留的核心功能
- 列表：12项指标 + 筛选器（人员/账号/日期/关键词）+ 异常标记
- 抽屉：完整指标 + 文案复制 + 外链 + 选题库入库 + 回收站 + 双截图

## 关键数据
- 回收站：18 次真实使用，必须保留
- 双截图：留存图 85.4%，曲线图 38.3%（需做空态）
- 视频标签：6395 条，今天仍在产生，不能全删

**Why**: 两个模块职责冲突，33% 功能重叠，用户搞不清该去哪个页面。素材库名不副实（不存素材文件），复盘抽屉价值低（除双截图外零使用）。

**How to apply**: 后续新增功能时，统一在视频复盘页面实现，不再创建独立模块。
```

更新 `MEMORY.md` 索引：
```markdown
- [视频复盘与素材库合并](项目-视频复盘与素材库合并-2026-09-20.md) — 2026-09-20 合并为统一入口，用素材库抽屉替换复盘抽屉
```

#### Git Commit
```bash
git add docs/ ~/.claude/
git commit -m "docs: 更新文档，反映视频复盘与素材库合并

- 更新全站模块地图，删除素材库独立条目
- 更新权限说明文档
- 新增记忆文件记录合并过程

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

#### 最终验收

**阿禅验收清单**：
- [ ] 所有功能正常工作
- [ ] 无视觉 bug
- [ ] 性能符合预期
- [ ] 文档更新完整
- [ ] **批准 push main**

#### 完成标准
- 所有功能正常工作
- 无明显性能退化
- 无视觉 bug
- 文档更新完整

---

## 关键注意事项

### ⚠️ 必须遵守的边界

1. **不删标签表和投稿流程**：视频标签今天仍在产生数据，只删素材库页面的展示区块
2. **保留所有 API 路由**：新抽屉依赖素材库的 API，不要删除任何 `/api/admin/videos/*` 路由
3. **保留数据库字段**：不删 `asset_level` / `review_status` 等字段，只删前端显示
4. **保留后端 AI API**：`/api/admin/content-analysis` 和 `/api/admin/content-attribution/*` 保留，未来可能用
5. **字段名注意**：是 `retention_screenshot_url` 不是 `retention_chart_url`

### 🔧 技术细节

**双截图空态处理**：
- 留存图 85.4% 覆盖率：不显示空态（大部分有图）
- 流量曲线图 38.3% 覆盖率：必须显示空态提示

**数据库连接**：
- `.env.local` 的直连串已失效
- 真实可用通道：`aws-1-ap-southeast-1.pooler.supabase.com:5432`
- 见 `~/.claude/memory/` 中的数据库连接记忆文件

**颜色值统一**：
- 🔴 删稿/限流：#C9604D
- 🟠 腰斩/异常：#B98A54
- 🟢 正常：#6FAA7D

### 📊 工作量预估

| 阶段 | 预计工期 | 关键风险 |
|------|----------|----------|
| 阶段 2：筛选器迁移 | 0.5天 | 筛选逻辑复杂度 |
| 阶段 3：抽屉替换 | 1天 | 双截图迁移 |
| 阶段 4：重定向 | 0.5天 | 无 |
| 阶段 5：清理死代码 | 0.5天 | grep 遗漏 |
| 阶段 6：测试验收 | 0.5天 | 回归测试 |
| **总计** | **3天** | — |

---

## 执行要求

1. **按阶段顺序执行**：不要跳阶段，每个阶段完成后 commit
2. **每个阶段独立验收**：自己测试通过后再进入下一阶段
3. **删除功能分批 commit**：阶段 5 每删一个功能就 commit 一次
4. **保持代码质量**：
   - 遵循项目现有代码风格
   - 保持组件命名一致
   - 注释关键逻辑
5. **实时更新进度**：每完成一个阶段，更新 `docs/plans/2026-09-20-视频复盘与素材库合并-完成进度.md`

---

## 遇到问题时

1. **停下来，不要瞎改**
2. **grep 查找相关代码**
3. **读懂现有逻辑再动手**
4. **记录问题到完成进度的"问题记录"表**
5. **向阿禅报告阻塞问题**

---

## 完成后

1. 运行 `npm test` 确保测试通过
2. 本地手动测试所有功能验收清单
3. 更新完成进度文档，标记所有阶段为 ✅
4. 通知阿禅进行最终验收
5. 验收通过后，`git push origin main`

---

**祝施工顺利！有问题随时停下来询问。**
