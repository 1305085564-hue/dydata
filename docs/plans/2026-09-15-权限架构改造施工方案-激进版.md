# 权限架构改造施工方案（激进版 - 一次性执行）

**生成时间**：2026-09-15  
**执行方式**：原定一次性完成（4-6小时）；现按本文件「当前四步收口台账」分段收口
**执行者**：Codex  
**审查者**：Opus + 其他 AI 交叉审查

**进度更新**：2026-09-17；第 1 步 B1/B2 与第 2 步 B3 均已发布并按各自范围验收。第 3 步先有只读批次 `BLOCKED`，随后按阿禅确认的精确测试对象补齐受控写入、归档旧令牌与页面刷新，冻结范围内已完成；不存在非空靶行的路径继续明确标为“未覆盖”，不伪装为 PASS。**第 4 步外部交叉审查尚未进行，整案仍 `BLOCKED`，不能标记 `CLOSED`。**原施工步骤、工时和示例是历史材料，唯一当前收口口径是下方四步台账与第十一节。

---

## 零、从目标到最终完成的进度台账

> 状态口径：✅ 已有相应证据；⚠️ 已做一部分或证据与结论不一致；🔴 发现未收口的权限边界；⏳ 待完成。10.3 记录的是发布前历史快照，后续发布与验收以 10.5 为准；B3 独立执行的三条生产 migration 不能混作 B1/B2 的验收。

### 唯一当前收口台账（替代旧的“还差四步”口头清单）

| 步骤 | 当前判定 | 本步完成的可核对条件 |
|---|---|---|
| **1 应用权限缺口 B1/B2** | **✅ 指定范围已完成** | `694dba3b` 生产 Ready + 真实 member/admin/company_owner/集团态直调；无团队 admin 与逐个 AI 写工具属于明确未覆盖项，不能冒充已验，也不默认无限扩大为本步阻断。见 10.5。 |
| **2 数据库权限 B3（本对话施工）** | **✅ 已发布并做最小回查** | 三条 B3 migration 在生产历史可追溯；第二批 `20260916172444` 已收紧五张旧表、团队直连和子表范围，`d073dc25` 应用兼容补丁 Ready；元数据/匿名直调及公开入团目录冒烟通过。**这不是第 3 步的真实角色写入验收**。详见 11.2。 |
| **3 修复后生产验收** | **✅ 冻结范围已完成；部分无靶行路径未覆盖** | 11.3.1 只读批次如实保留 `BLOCKED`；11.3.2 经确认的真实账号补验：本人新增、同公司组长修改放行，跨公司更新/新增及换归属拒绝，归档旧令牌拒绝；数据库与硬刷新双向核对，测试行精确回收。R2 与旧批次失败不改写。 |
| **4 最终外部交叉审查（交 2–3 名 AI）** | **⏳ 待执行** | 只读复核最新代码/实际线上 SQL/原始验收证据，逐项指出阻断或通过；此前三份报告是发现问题的依据，不能代替修复后的最终审查。 |

**总完成条件**：第 1–3 步的冻结范围已具备生产证据；仅待第 4 步独立审查确认无未处理的权限/数据安全阻断，方可将整案改为 `CLOSED`。无非空靶行的路径、旧批次失败及 R2 待审批状态必须保留原样，审查者可判断其是否影响最终裁定；不临时长出第五步。

| 原始目标 / 施工步骤 | 当前状态 | 已有证据与未闭合边界 |
|---|---|---|
| 1. 权限契约成为固定角色权限的唯一来源 | ✅ | `permission-contract.ts` 与 `company-permissions.ts`、SQL 角色数组一致。 |
| 2. 页面与 API 共用身份解析核心 | ✅ | `getUserPermissions()`、`requireAdminActor()` 共用 `resolvePermissionCore()`；仅有请求内缓存。 |
| 3. 替换旧 `hasPermission()` 调用 | ✅ | 施工阶段全仓检查无旧调用；发布版以 `permissions[key]` / 固定角色契约判定。 |
| 4. 删除旧函数和个人权限写入口 | ✅ | 旧函数已删除；`updatePermissions` 及管理 AI 的个人权限写入口已移除。历史审计展示文案不构成写入口。 |
| 5. 后台页面权限注册表 | ✅ / ⚠️ | 现有 `/admin` 页面注册并对未知页面拒绝；**这不覆盖全部 API 与 Server Action**。B1/B2 已发布 `694dba3b` 并对 `execute-tool` 与团队架构动作加服务端门禁、生产真实账号直调通过（见 10.5）；`/admin/collaboration` 对 member 只读本人属确认设计。 |
| 6. 数据库统一 migration | ✅ 发布 / ⏳ 验收 | 两条权限函数 migration 与三条 B3 行范围 migration 已在线；第二批新迁移 `20260916172444` 关闭五表匿名暴露并加目标策略，元数据/匿名冒烟通过。实际角色正反向与受控写入仍属第 3 步。详见第十一节。 |
| 7. 静态测试与真实角色验收 | ✅ / ⚠️ | 原发布版 `gate:static` 1578/1578 + 三角色页面矩阵已有记录；B1/B2 补丁 `gate:static` 1594/1594、脚本 2/2、tsc、lint 0 错误、build 通过，且**已发布 `694dba3b` 后完成生产真实角色直调（B1/B2 重点项 PASS，见 10.5）**。遗留：B2 “组长无 team_id”子项仅单测、B1 各写工具未逐一实写；旧批次回收/刷新/覆盖面仍有第九、十节缺口。 |
| 8. 提交、迁移、部署 | ✅ | 原权限提交链及 B1/B2 `694dba3b` 已发布；B3 兼容补丁 `d073dc25` 与迁移历史版本对齐提交 `666745b2` 已精确快进推送，第二批 SQL 在线版 `20260916172444` 已执行，最终部署 Ready/SHA 见 11.2。 |
| 9. 生产写链路验收 | ⚠️ | 11 条核心权限/范围判断在测试批次中得到 PASS；另 1 条回收、1 条跨刷新复验不能按原文“全部四层 PASS”计。管理 AI 旧批只测 `/api/admin/ai-config`；**B1 已在本轮 10.5 对 `/api/admin/execute-tool` 完成生产真实账号直调（入口限制+范围+伪造集团）**，写工具逐一实写仍另批。 |
| 10. 2–3 名 AI 独立交叉审查 | ✅ 历史 / ⏳ 最终 | 旧三份已交并发现阻断；最终一次须在第 2、3 步后针对最新变更和证据重新审查，不以旧报告投票替代。 |
| 11. 最终权限目标：无跨团队/跨角色越权，证据闭环后 CLOSED | ⏳ | 第 1、2 步已发布，第 3、4 步尚未开始；第 2 步的元数据和匿名冒烟不替代真实角色正反向、可回收写操作与最终独立复核。**整体仍 BLOCKED。** |

**当前交付分层**：第 1 步 B1/B2、第 2 步 B3、第 3 步定点生产验收均已在各自冻结范围完成；第 4 步新证据的最终独立复核尚未做。因此总体仍为 **BLOCKED**，不得宣称“0 bug”“12/12 全 PASS”或“权限架构最终完成”。

---

## 一、执行前提

**原用户确认（2026-09-15 的施工前提，非当前状态）**：
- ✅ 今晚没人使用系统
- ✅ 接受一次性替换，执行完做交叉审查；“0 bug”是目标，不能代替实测结论
- ✅ 不需要分批次、不需要兼容层、不需要保留旧代码

**回滚说明**：原“一键硬回滚”设想已失效。现有多次提交、已执行的数据库 migration 和生产部署不能靠单条 `git reset --hard` 安全回滚；如需回滚，必须先定位目标部署与数据库兼容边界，制定独立恢复方案。

---

## 二、施工步骤（按顺序执行）

### 步骤 1：让 company-permissions 读取权限契约（✅ 已完成；原估时 30分钟）

**目标**：统一权限定义的唯一来源

**改动文件**：[src/lib/company-permissions.ts](../../../src/lib/company-permissions.ts)

**改动内容**：
```typescript
import { PERMISSION_CONTRACT } from "./permission-contract";

// ✅ 从统一契约读取
export const DEFAULT_PERMISSIONS_BY_COMPANY_ROLE = PERMISSION_CONTRACT.roles;
export const PERMISSION_KEYS_FOR_GROUP_MODE = PERMISSION_CONTRACT.groupMode.permissions;

// 其他函数保持不变，但内部改为读取 PERMISSION_CONTRACT
export function fixedPermissionsForRole(
  role: CompanyRole | UserRole | string | null | undefined,
  legacyPermissions: Permissions | null | undefined = null,
  groupMode = false,
): Permissions {
  void legacyPermissions;
  if (groupMode) {
    return Object.fromEntries(PERMISSION_CONTRACT.groupMode.permissions.map(k => [k, true]));
  }

  const companyRole = resolveCompanyRole(role) ?? "member";
  return Object.fromEntries(PERMISSION_CONTRACT.roles[companyRole].map(k => [k, true]));
}
```

**验证**：
```bash
npx tsc --noEmit
npx tsx --test src/lib/company-permissions.test.ts
```

---

### 步骤 2：统一身份解析逻辑（✅ 已完成；原估时 1小时）

**目标**：让 `getUserPermissions()` 和 `requireAdminActor()` 都调用同一个内部函数

**改动文件**：
1. [src/lib/current-permission-context.ts](../../../src/lib/current-permission-context.ts)
2. [src/lib/permissions.ts](../../../src/lib/permissions.ts)
3. [src/app/api/admin/auth-helper.ts](../../../src/app/api/admin/auth-helper.ts)

**新增函数**（在 current-permission-context.ts）：
```typescript
/**
 * 统一的身份解析逻辑
 * getUserPermissions() 和 requireAdminActor() 都调用这个
 */
export async function resolvePermissionCore(): Promise<{
  userId: string;
  companyRole: CompanyRole;
  membershipStatus: "active" | "archived";
  teamId: string | null;
  groupMode: boolean;
  permissions: Permissions;
  dataScope: DataScope;
  visibleUserIds: readonly string[];
  activeVisibleUserIds: readonly string[];
} | null> {
  const { user, authError } = await getCurrentUserContext();
  if (authError || !user) return null;

  const adminSupabase = createAdminClient();
  
  const [profileResult, groupModeState] = await Promise.all([
    adminSupabase
      .from("profiles")
      .select("id, company_role, membership_status, team_id")
      .eq("id", user.id)
      .single(),
    resolveGroupModeForUser(user.id, adminSupabase),
  ]);

  if (profileResult.error || !profileResult.data) return null;
  
  const profile = profileResult.data;
  const companyRole = resolveCompanyRole(profile.company_role);
  if (!companyRole) return null;
  if (profile.membership_status === "archived") return null;
  
  const groupMode = groupModeState.active;
  
  // ✅ 从权限契约获取权限
  const permissionArray = groupMode
    ? PERMISSION_CONTRACT.groupMode.permissions
    : PERMISSION_CONTRACT.roles[companyRole];
  
  const permissions = Object.fromEntries(permissionArray.map(k => [k, true]));
  
  // 数据范围
  const scope = await buildDataAccessScope(adminSupabase, user.id, {
    teamId: profile.team_id,
    profile: {
      id: user.id,
      role: companyRole,
      permissions,
      data_scope: null,
      team_id: profile.team_id,
      company_role: companyRole,
      group_mode: groupMode,
      membership_status: profile.membership_status,
    },
  });
  
  return {
    userId: user.id,
    companyRole,
    membershipStatus: "active",
    teamId: profile.team_id,
    groupMode,
    permissions,
    dataScope: scope.dataScope,
    visibleUserIds: scope.visibleUserIds,
    activeVisibleUserIds: scope.activeVisibleUserIds,
  };
}
```

**改造 getUserPermissions()**：
```typescript
export async function getUserPermissions(): Promise<UserPermissionInfo | null> {
  const core = await resolvePermissionCore();
  if (!core) return null;
  
  return {
    userId: core.userId,
    name: null,
    role: core.companyRole,
    permissions: core.permissions,
    dataScope: core.dataScope,
    teamId: core.teamId,
    companyRole: core.companyRole,
    membershipStatus: core.membershipStatus,
    groupMode: core.groupMode,
    hasGroupOwnerQualification: canEnterGroupMode(core.companyRole, core.membershipStatus),
  };
}
```

**改造 requireAdminActor()**：
```typescript
export async function requireAdminActor(
  options: RequireAdminActorOptions = {}
): Promise<RequireAdminActorResult> {
  const core = await resolvePermissionCore();
  
  if (!core) {
    return {
      actor: null,
      error: "未登录或账号已归档",
      statusCode: 401,
    };
  }
  
  if (options.requiredPermission) {
    if (!core.permissions[options.requiredPermission]) {
      return {
        actor: null,
        error: "权限不足",
        statusCode: 403,
      };
    }
  }
  
  return {
    actor: {
      userId: core.userId,
      role: core.companyRole,
      dataScope: core.dataScope,
      teamId: core.teamId,
      groupMode: core.groupMode,
      permissions: core.permissions,
    },
    error: null,
    statusCode: 200,
  };
}
```

**验证**：
```bash
npx tsc --noEmit
npx tsx --test src/lib/permissions.test.ts
```

---

### 步骤 3：批量替换 hasPermission() 调用（✅ 已完成；原估时 1小时）

**目标**：把 37 处旧的权限检查全部替换成新写法

**替换模式**：
```typescript
// ❌ 旧写法
if (!hasPermission(perm.role, perm.permissions, "manage_members")) {
  return { error: "无权限" };
}

// ✅ 新写法
if (!perm || !perm.permissions.manage_members) {
  return { error: "无权限" };
}
```

**需要改动的文件**（共 37 处）：

#### 高优先级（P0）
1. `src/lib/exemption-permissions.ts` (2处)
2. `src/lib/conversion-hub/service.ts` (1处)
3. `src/app/(app)/admin/actions.ts` (8处)
4. `src/app/(app)/admin/join-request-actions.ts` (1处)
5. `src/app/api/export/route.ts` (1处)
6. `src/app/api/production/_shared.ts` (2处)

#### 中优先级（P1）
7. `src/lib/analytics-access.ts` (11处)
8. `src/app/api/rewrite/skills/[id]/route.ts` (1处)
9. `src/app/api/rewrite/skills/route.ts` (1处)

**具体改动示例**：

**文件**：`src/lib/exemption-permissions.ts`
```typescript
// 改前
export function hasExemptionManagementPermission(role: UserRole | undefined, permissions: Permissions) {
  void role;
  return (
    hasPermission(role, permissions, "manage_fulfillment") ||
    hasPermission(role, permissions, "review_violations")
  );
}

// 改后
export function hasExemptionManagementPermission(permissions: Permissions) {
  return (
    permissions.manage_fulfillment === true ||
    permissions.review_violations === true
  );
}
```

**文件**：`src/app/(app)/admin/actions.ts`（8处类似改动）
```typescript
// 改前
export async function approveExemption(exemptionId: string) {
  const perm = await getUserPermissions();
  if (!hasPermission(perm.role, perm.permissions, "manage_fulfillment")) {
    return { error: "无权限" };
  }
  // ...
}

// 改后
export async function approveExemption(exemptionId: string) {
  const perm = await getUserPermissions();
  if (!perm || !perm.permissions.manage_fulfillment) {
    return { error: "无权限" };
  }
  // ...
}
```

**文件**：`src/lib/analytics-access.ts`（11处类似改动）
```typescript
// 改前
if (pathname === "/admin/settings") {
  return hasPermission(role, permissions, "manage_system");
}

// 改后
if (pathname === "/admin/settings") {
  return permissions.manage_system === true;
}
```

**验证**：
```bash
# 确认所有 hasPermission 调用已替换（除了测试文件）
rg -n 'hasPermission\(' src/ | grep -v ".test.ts"

npx tsc --noEmit
```

---

### 步骤 4：删除 hasPermission() 函数（✅ 已完成；原估时 10分钟）

**目标**：删除旧的权限检查函数

**改动文件**：[src/lib/permission-utils.ts](../../../src/lib/permission-utils.ts)

**删除内容**：
```typescript
// ❌ 删除这个函数
export function hasPermission(
  _role: UserRole | undefined,
  permissions: Permissions,
  key: PermissionKey,
): boolean {
  return permissions[key] === true;
}
```

**保留内容**：
```typescript
// ✅ 保留这些函数
export function hasCompanyPermission(role, key, groupMode) { ... }
export function fixedPermissions(role, legacyPermissions, groupMode) { ... }
export function hasAnyPermission(_role, permissions) { ... }
export function canUseAiCopywriting(role, permissions) { ... }
```

**验证**：
```bash
# 确认函数已删除
rg -n 'export function hasPermission' src/lib/permission-utils.ts

npx tsc --noEmit
```

---

### 步骤 5：创建路由权限注册表（✅ 页面层已完成；API/Action 不属于此表）

**目标**：集中管理所有后台路由的权限要求

**新建文件**：[src/lib/route-permissions.ts](../../../src/lib/route-permissions.ts)

```typescript
import type { PermissionKey } from "@/types";

/**
 * 路由权限配置表（单一数据源）
 * 所有 /admin/* 路由必须在此注册
 */
export const ROUTE_PERMISSIONS: Record<string, PermissionKey[]> = {
  "/admin/settings": ["manage_system"],
  "/admin/modules": ["manage_members"],
  "/admin/content": ["review_content"],
  "/admin/videos": ["manage_videos"],
  "/admin/fulfillment": ["manage_fulfillment"],
  "/admin/collaboration": ["view_conversion"],
  "/admin/ai-config": ["use_ai_assist"],
  "/admin": [], // 后台首页：任意 admin 权限即可
} as const;

/**
 * 检查用户是否有权限访问指定路由
 */
export function canAccessRoute(
  pathname: string,
  permissions: Permissions
): boolean {
  const required = ROUTE_PERMISSIONS[pathname];
  
  if (required === undefined) {
    console.warn(`Route ${pathname} not registered in ROUTE_PERMISSIONS`);
    return false;
  }
  
  if (required.length === 0) {
    // 任意权限即可（至少有一个权限为 true）
    return Object.values(permissions).some(v => v === true);
  }
  
  // 检查是否有任意一个权限
  return required.some(perm => permissions[perm] === true);
}
```

**改造 analytics-access.ts**：
```typescript
import { ROUTE_PERMISSIONS, canAccessRoute } from "./route-permissions";

// ✅ 使用路由表
export function canAccessAdminPath(
  pathname: string,
  role: UserRole,
  permissions: Permissions
): boolean {
  return canAccessRoute(pathname, permissions);
}
```

**验证**：
```bash
npx tsc --noEmit
```

---

### 步骤 6：创建 SQL 权限统一 migration（✅ 两条已发布；🔴 RLS 行范围另见第十节）

**目标**：让数据库权限函数也从统一契约读取

**新建文件**：`supabase/migrations/20260916000000_unify_permission_functions.sql`

```sql
-- 统一的权限获取函数（从 TypeScript permission-contract 同步）
CREATE OR REPLACE FUNCTION get_role_permissions(p_role text, p_group_mode boolean)
RETURNS text[]
LANGUAGE plpgsql
IMMUTABLE
AS $$
BEGIN
  -- 分组模式：全部权限
  IF p_group_mode THEN
    RETURN ARRAY[
      'view_analytics', 'export_data', 'view_conversion',
      'review_content', 'manage_fulfillment', 'manage_videos',
      'manage_members', 'review_violations', 'manage_system',
      'use_ai_copy', 'use_ai_assist'
    ];
  END IF;
  
  -- 按角色返回固定权限
  CASE p_role
    WHEN 'company_owner' THEN
      RETURN ARRAY[
        'view_analytics', 'export_data', 'view_conversion',
        'review_content', 'manage_fulfillment', 'manage_videos',
        'manage_members', 'review_violations', 'manage_system',
        'use_ai_copy', 'use_ai_assist'
      ];
    WHEN 'admin' THEN
      RETURN ARRAY[
        'view_analytics', 'export_data', 'view_conversion',
        'review_content', 'manage_fulfillment', 'manage_videos',
        'manage_members', 'review_violations', 'use_ai_copy'
      ];
    WHEN 'member' THEN
      RETURN ARRAY['view_analytics', 'export_data'];
    ELSE
      RETURN ARRAY[]::text[];
  END CASE;
END;
$$;

-- 重写 has_permission()，使用统一函数
CREATE OR REPLACE FUNCTION has_permission(perm text)
RETURNS boolean
LANGUAGE plpgsql
STABLE
SECURITY DEFINER
AS $$
DECLARE
  v_company_role text;
  v_group_mode_active boolean;
  v_permissions text[];
BEGIN
  -- 获取当前用户的 company_role
  SELECT company_role INTO v_company_role
  FROM profiles
  WHERE id = auth.uid()
    AND coalesce(membership_status, 'active') = 'active';
  
  -- 检查是否在分组模式
  v_group_mode_active := (
    SELECT EXISTS (
      SELECT 1
      FROM group_mode_sessions
      WHERE user_id = auth.uid()
        AND expires_at > now()
    )
  );
  
  -- 调用统一的权限获取函数
  v_permissions := get_role_permissions(v_company_role, v_group_mode_active);
  
  RETURN perm = ANY(v_permissions);
END;
$$;

COMMENT ON FUNCTION get_role_permissions IS '从 TypeScript permission-contract 同步的唯一权限定义';
COMMENT ON FUNCTION has_permission IS '检查当前用户权限（已统一为调用 get_role_permissions）';
```

**历史说明**：上面是 2026-09-15 的设计草稿，不是最终执行的 SQL；实际新增并执行的文件为 `20260915141335_unify_permission_functions.sql` 和 `20260916095530_fix_has_permission_inactive_false.sql`。禁止按此草稿再次执行或用它覆盖已上线函数。

---

### 步骤 7：运行测试（✅ 静态与定向测试；⚠️ 真实角色全链路未闭合）

**目标**：验证所有改动没有破坏现有功能

#### 7.1 TypeScript 编译测试
```bash
npx tsc --noEmit --pretty false
```

#### 7.2 单元测试
```bash
npx tsx --test src/lib/company-permissions.test.ts
npx tsx --test src/lib/permission-utils.test.ts
npx tsx --test src/lib/permissions.test.ts
npx tsx --test src/lib/current-permission-context.test.ts
npx tsx --test src/lib/data-access-scope.test.ts
```

#### 7.3 真实账号测试（必须）

**测试账号**（原 `.claude/projects/` 路径已失效；实际账号按 `docs/reference/测试账号.md` 的受控、gitignored 清单读取，严禁在文档记录密码）：
- 普通员工（member）
- 组长（admin）
- 老板（company_owner）

**测试场景**：

**场景1：豁免审批**
- member 试图审批别人的豁免 → 应被拒绝 ✅
- admin 可以审批本团队的豁免 ✅
- company_owner 可以审批全公司的豁免 ✅

**场景2：成员管理**
- member 试图删除其他成员 → 应被拒绝 ✅
- admin 试图删除其他团队成员 → 应被拒绝 ✅
- admin 可以管理本团队成员 ✅
- company_owner 可以管理全公司成员 ✅

**场景3：后台页面访问**
- member 访问 /admin/settings → 应被拒绝 ✅
- member 访问 /admin/content → 应被拒绝 ✅
- admin 访问 /admin/settings → 应被拒绝 ✅
- admin 访问 /admin/content → 可以访问 ✅
- company_owner 访问所有页面 → 都可以访问 ✅

**场景4：数据导出**
- member 可以导出自己的数据 ✅
- admin 可以导出团队数据 ✅
- company_owner 可以导出全公司数据 ✅

**场景5：AI 助手工具**
- member 试图使用 AI 助手 → 应被拒绝 ✅
- admin 试图使用 AI 助手 → 应被拒绝 ✅
- company_owner 可以使用 AI 助手 ✅

#### 7.4 修复发现的 bug

如果测试发现任何问题：
1. 记录问题现象
2. 定位问题代码
3. 修复问题
4. 重新运行测试
5. 直到所有测试通过

---

### 步骤 8：提交代码（✅ 施工提交链已发布；以下命令仅为历史记录，不要重跑）

**目标**：生成清晰的 commit message 和回滚指令

#### 8.1 检查改动
```bash
git status
git diff
```

#### 8.2 提交改动
> 下方是原计划的提交示例，包含后来被证据推翻的“真实账号全部通过”措辞；**不是实际提交内容，也不是当前可重跑的命令**。实际提交链见第零节和 Git 历史。
```bash
git add src/lib/permission-contract.ts
git add src/lib/company-permissions.ts
git add src/lib/current-permission-context.ts
git add src/lib/permissions.ts
git add src/lib/permission-utils.ts
git add src/lib/route-permissions.ts
git add src/lib/exemption-permissions.ts
git add src/lib/conversion-hub/service.ts
git add src/lib/analytics-access.ts
git add src/app/(app)/admin/actions.ts
git add src/app/(app)/admin/join-request-actions.ts
git add src/app/api/admin/auth-helper.ts
git add src/app/api/export/route.ts
git add src/app/api/production/_shared.ts
git add src/app/api/rewrite/skills/
git add supabase/migrations/20260916000000_unify_permission_functions.sql

git commit -m "refactor(permissions): 一次性统一权限检查体系

## 主要改动

1. 统一权限定义来源
   - 所有权限从 permission-contract.ts 读取
   - 删除 company-permissions.ts 中的重复定义

2. 统一身份解析逻辑
   - getUserPermissions() 和 requireAdminActor() 都调用 resolvePermissionCore()
   - 消除双入口导致的不一致

3. 批量替换权限检查（37处）
   - 删除 hasPermission() 函数
   - 改为直接检查 permissions[key]

4. 创建路由权限注册表
   - 所有后台路由权限集中管理
   - 新增路由必须注册

5. 统一数据库权限函数
   - 新增 get_role_permissions() 函数
   - has_permission() 改为调用统一函数

## 测试验证

- ✅ TypeScript 编译通过
- ✅ 单元测试通过
- ✅ 真实账号测试通过（member/admin/company_owner）
- ✅ 豁免审批权限验证正确
- ✅ 成员管理权限验证正确
- ✅ 后台页面访问权限验证正确
- ✅ 数据导出权限验证正确
- ✅ AI 助手工具权限验证正确

## 回滚指令

git revert HEAD

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

#### 8.3 推送到远程
```bash
git push origin main
```

---

## 三、改动文件清单

> 以下 17 文件是原估算，不是最终 diff。实际施工跨更多文件；以 `094a8def^..13bd0aa9` 的提交差异为准。不要将“待创建”视为当前待办。

### 新建文件（3个）
1. [src/lib/permission-contract.ts](../../../src/lib/permission-contract.ts) ✅ 已创建
2. [src/lib/route-permissions.ts](../../../src/lib/route-permissions.ts) - 已创建
3. 原拟 `20260916000000_unify_permission_functions.sql` 未采用；实际为 `20260915141335_unify_permission_functions.sql`，并追加 `20260916095530_fix_has_permission_inactive_false.sql`

### 重构文件（原清单实际列出 14 个）
1. [src/lib/company-permissions.ts](../../../src/lib/company-permissions.ts)
2. [src/lib/current-permission-context.ts](../../../src/lib/current-permission-context.ts)
3. [src/lib/permissions.ts](../../../src/lib/permissions.ts)
4. [src/lib/permission-utils.ts](../../../src/lib/permission-utils.ts)
5. [src/lib/exemption-permissions.ts](../../../src/lib/exemption-permissions.ts)
6. [src/lib/conversion-hub/service.ts](../../../src/lib/conversion-hub/service.ts)
7. [src/lib/analytics-access.ts](../../../src/lib/analytics-access.ts)
8. [src/app/(app)/admin/actions.ts](../../../src/app/(app)/admin/actions.ts)
9. [src/app/(app)/admin/join-request-actions.ts](../../../src/app/(app)/admin/join-request-actions.ts)
10. [src/app/api/admin/auth-helper.ts](../../../src/app/api/admin/auth-helper.ts)
11. [src/app/api/export/route.ts](../../../src/app/api/export/route.ts)
12. [src/app/api/production/_shared.ts](../../../src/app/api/production/_shared.ts)
13. [src/app/api/rewrite/skills/[id]/route.ts](../../../src/app/api/rewrite/skills/[id]/route.ts)
14. [src/app/api/rewrite/skills/route.ts](../../../src/app/api/rewrite/skills/route.ts)

**原估计**：3 个新建 + 14 个重构 = 17 个文件；不是最终施工范围。

---

## 四、验收标准

### 技术指标
- [x] TypeScript 编译零错误（发布前记录）
- [x] 单元测试全部通过（发布前记录：1578/1578）
- [x] 旧 `hasPermission()` 调用及函数已删除（施工阶段全仓检查）
- [x] 后台**页面**路由权限注册表已创建；API/Server Action 需各自审查，不归该表覆盖
- [x] SQL 固定角色权限函数已统一并发布；RLS 行范围另列阻断

### 功能验收
- [ ] 普通员工无法越权访问：`execute-tool/getTaskStatus` 的跨范围读取未闭合
- [ ] 组长只能管理本团队：成员列表/团队架构动作及直接 RLS 路径未闭合
- [ ] 公司所有者默认只管本公司、集团模式才跨公司：部分管理 AI 与 RLS 路径未闭合
- [x] 豁免审批的已测页面/API/RPC 路径符合当前范围；其他入口仍需补测
- [ ] 成员管理全入口权限正确：已测团队调配路径通过，但创建/删除团队与列表范围有反证
- [x] 已测后台页面访问符合注册表；不等于 API/Action 全覆盖
- [x] 已测导出文件无跨团队行；`>1000` 行完整性另列预存待办
- [ ] 管理 AI 所有工具权限正确：只测 `/api/admin/ai-config`，未测 `/api/admin/execute-tool`

### 代码质量
- [x] 固定角色权限定义集中在 `permission-contract.ts`；旧 RLS/helper 的行级语义仍待对齐
- [x] 身份解析逻辑统一到 `resolvePermissionCore()`
- [x] 应用层固定权限检查方式已统一
- [x] `/admin` 页面路由权限集中管理；不能推导出 API/Action 已集中管理

---

## 五、已知风险与缓解措施

### 风险 1：一次性改动太多，可能有遗漏
**缓解**：
- 步骤 7 有完整的测试清单
- 已完成三份独立 AI 交叉审查；分歧和阻断见第十节
- 多提交与已上线 migration 需独立恢复方案，没有安全的“一键回滚”

### 风险 2：测试账号可能不够全面
**缓解**：
- 测试 5 个核心场景（豁免、成员管理、页面访问、导出、AI助手）
- 测试 3 种角色（member/admin/company_owner）
- 交叉审查时补充测试

### 风险 3：SQL migration 可能影响线上数据
**缓解**：
- migration 只修改函数定义，不改数据结构
- 不删除任何表或字段
- 本轮函数已执行；后续 RLS 修复必须先核对线上实际策略、权限和契约，再单独发布验收

---

## 六、时间估算

- 步骤 1：30 分钟
- 步骤 2：1 小时
- 步骤 3：1 小时
- 步骤 4：10 分钟
- 步骤 5：30 分钟
- 步骤 6：30 分钟
- 步骤 7：1-2 小时（含修 bug）
- 步骤 8：30 分钟

**原始估算**：4.5-5.5 小时（不含交叉审查时间）；不是当前剩余工作量或完成证明。

---

## 七、执行后的交叉审查清单

**状态：三份外部审查已完成，结论分歧；最终裁决为 BLOCKED，详见第十节。下面是原始审查关注点，不表示全部通过。**

**Opus 审查重点**：
- [ ] 权限检查逻辑是否正确
- [ ] 有无遗漏的 hasPermission() 调用
- [ ] 路由权限表是否覆盖所有路由
- [ ] SQL 函数是否与 TS 契约一致

**其他 AI 审查重点**：
- [ ] 代码风格是否一致
- [ ] 有无潜在的安全漏洞
- [ ] 错误处理是否完善
- [ ] 测试覆盖是否充分

---

## 八、回滚边界（原“一键回滚”已作废）

现有多次应用提交、两条已执行 migration 和生产部署。单独 `git revert HEAD` 只能撤销最后一个提交，不能撤回数据库函数；`git reset --hard` 或强推还会覆盖其他人的提交，**不得按原草案执行**。发生线上事故时，先核对故障入口、生产部署 SHA、迁移和数据兼容性，再由独立恢复方案决定是否将 Vercel 切回兼容的 Ready 版本、追加向前修复 migration，或做受控代码回退。

---

**文档版本**：v1.1（激进版施工进度与交叉审查归并）

**最后更新**：2026-09-16
**执行者**：Codex  
**审查者**：Opus + 其他 AI

---

## 九、生产写链路验收结果

**验收日期**：2026-09-16  
**验收人**：[CC] QwenWork（千问办公）  
**环境**：生产 `https://dydata.cc`（Vercel READY，指向 13bd0aa9）· 真实测试账号 · Playwright 无头浏览器驱动页面与服务端接口 · 数据库经 service-role **只读**通道做前后差值校验（未通过该通道写库、未改函数/RLS、未跑新 migration）。  
**证据归档**：`output/acceptance/permw/`（探针脚本）与 `output/acceptance/permw/evidence/pm-2026-09-16/`（7 个场景 JSON，gitignored）；少量截图在 `output/acceptance/evidence/pm-2026-09-16/`。这些路径均为本机证据，不在 Git 提交内；外部 AI 若拿不到该机器的 `output/`，不得仅凭本节文字认定原始证据已独立复核。

**验收账号与归属（侦察确认）**：测试组长 `admin·深圳二部`、测试组员 `member·深圳二部`、阿禅 `company_owner·深圳二部`（三者同属深圳二部）；批次5-B组员 `member·页面1验证团队1124`（跨团队靶子）、批次5-A组员2 `member·深圳二部`（有 2 条真实报告，作 member 非空正例）。审批写链路走 `POST /api/exemptions/review`（与 /admin/fulfillment 页面按钮同一端点），团队写链路走 `/admin/modules` 抽屉 Server Action，导出走 `GET /api/export`，本批次“管理 AI”仅测 `POST /api/admin/ai-config`（`requireSystemActor→manage_system`）；**没有覆盖 `/api/admin/execute-tool` 管理 AI 工具执行入口**。

### 逐场景判定（原始批次结果；各行证据层级以实际记录为准）

| # | 场景 | 结论 | 已留证据与边界 |
|---|------|------|--------------|
| 1a | member 无管理权 | **PASS** | 页面：测试组员登录落 /dashboard；接口：`GET /api/exemptions/pending`→**403**；DB：无变化；刷新：无 |
| 1b | member 自审被拒 | **PASS** | 接口：member `POST /api/exemptions/review`→**403 无权限**；DB：R1 仍 `pending`、`grant=0`、`request_date=pending`；刷新复验：无落库 |
| 1c | admin 跨团队审批被拒 | **PASS** | 靶子：批次5-B组员(1124)真实 pending 申请 R2（**测试账号，非真实成员**）；接口：组长 `POST review(R2)`→**403「不能操作当前管理范围外的成员」**（DB RPC 42501）；DB：R2 仍 `pending`、b5b 无 active grant；刷新复验：无残留。读侧同证：组长 pending 列表含本团队 R1、不含跨团队 R2 |
| 1d | admin 本团队审批放行 | **PASS** | 靶子：测试组员自建可回收申请 R1（single/waive/2026-12-02）；页面：/admin/fulfillment；接口：组长 `POST review(R1)`→**200**；DB：R1 `request_status=approved`、`reviewed_by=组长id`、`exemption_grant` 1 条 active、`request_date=approved`；刷新：R1 移出 pending |
| 2a | admin 团队写入·跨团队被拒 | **PASS** | 页面：组长在 /admin/modules 抽屉把测试组员调配至「深圳一部」；接口：Server Action POST→200 但 `resolveMemberTeamTransfer` 判拒；DB：`team_id` 仍深圳二部（**未变**）；刷新复验：无残留 |
| 2b | admin 团队写入·本团队放行 | **PASS** | 页面：组长对测试组员点「移出团队」；接口：Server Action→200；DB：`team_id→null`；刷新：持久（组员变未分配） |
| 2c | owner 还原 | **终态已核对；自动步骤 FAIL** | `s2-admin-team-write.json` 的 owner 还原因页面定位超时标记 FAIL / NEEDS-MANUAL；后续外部只读核对见 DB `team_id` 回深圳二部、在职人数 44。人工/后续还原过程缺独立的页面→接口记录，不能把原始自动步骤改成 PASS。 |
| 3a | 导出·member 仅本人 | **PASS** | 非空正例：批次5-A组员2 `GET /api/export`→200，xlsx **2 行、提交人唯一=本人**；测试组员(0 报告)→0 行无外泄 |
| 3b | 导出·admin 仅深圳二部 | **PASS** | 组长→200，1000 行、31 位提交人**全部**属深圳二部（含归档历史归属），无任何其他团队提交人；行数<全库 1614 |
| 3c | 导出·owner 本公司 | **PASS** | 阿禅（未进集团模式）→与组长同为深圳二部范围（31 提交人、行数<全库），**未出现集团全量**，符合「默认本公司」契约 |
| 4a | 管理 AI·admin 被拒 | **PASS** | 组长 `POST /api/admin/ai-config` create→**403 无权限**；DB：目标 provider 未落库 |
| 4b | 管理 AI·owner 放行 | **PASS** | 阿禅 create→200，DB 出现临时 provider；随即 delete→200，DB 移除、`ai_providers` 总数 7→7 还原 |
| 5 | 刷新后状态 | **接口/DB 持久性通过；页面层 FAIL** | `s5-refresh.json` 原始结论为 FAIL：硬刷新后接口仍有临时 provider、DB 存在，但页面未找到；`s5b-persist.json` 虽改写为 PASS，`hardReload.inText=false`、`inInputValue=false`、`apiHas=true`。删除后接口/DB 无残留、两路拒绝后 DB 不变；不得写成“页面→接口→DB→刷新四层全通过”。 |

**计数与结论**：表格共 **13 行**，其中 11 行为核心权限/范围正反向检查、1 行为测试数据还原（2c）、1 行为跨场景刷新复验（5）。11 条核心检查有 PASS 记录；2c 原始自动化 FAIL/NEEDS-MANUAL，5 的页面层原始 FAIL。另 `s1-member-approval.json` 的“回收”原始 verdict 为 FAIL。最终数据库净状态与部分路径成功不能抹去这些过程失败；**本批次不能写“12/12 全 PASS、无 FAIL”**，也不能证明未覆盖的入口安全。

### 回收与残留
- 后续外部只读核对显示：测试组员团队归属已回深圳二部、深圳二部在职 44、两名测试组员无 active grant、临时 AI provider 已删且总数回到 7。**这证明核对时的净状态，不替代失败步骤的重跑或完整回收轨迹**。
- **残留**：两条自造测试豁免申请——R1（测试组员）现 `rejected`、R2（跨团队测试组员）现 `pending`。R2 仍会出现在有权者的待审批队列，不能笼统称“无业务影响”或“可忽略”；是否受控处理须先核对精确对象与业务审计要求，本轮未删除。

### 交付分层（区分预存问题 / 新发现 / 完整通过）
- **本批次已证实**：11 条核心权限/范围正反向检查在指定生产入口有真实账号、请求和数据库差值证据；并非“所有管理入口”或“每行四层”均已通过。
- **预存问题（范围外，只记录不修）**：`GET /api/export` 在筛选命中 >1000 行时，工作簿疑似被上游默认行上限截断——组长/owner 导出深圳二部 1429 条在职报告时，解析出 **1000 行**（远端 PostgREST 默认 `max-rows` 行为，非本轮权限改动引入）。**不影响权限正确性**（范围过滤正确、无跨团队泄漏），但导出完整性存疑，建议后续单列工程项核对。
- **本批次未覆盖及后续发现**：管理 AI 只测 `ai-config`，未测 `execute-tool`；成员管理只测团队调配，未测列表跨团队读取及创建/删除团队；直接 authenticated/RLS 路径未测。三份外部审查据此产生第十节的阻断发现，不能继续保留“无新发现问题、无 FAIL”的全局结论。
- 未触碰：`/auth/logout` 跳 localhost 既有问题、xlsx 高危依赖、其他 Supabase 安全顾问历史项，均按边界保留未动。

**边界声明**：全程未修改任何业务源码、未改数据库函数/RLS、未执行 migration；`git status` 确认 `src/` 无改动。

---

## 十、三份独立交叉审查的合并裁决（2026-09-16）

**最终裁决：BLOCKED，不是 CLOSED。** 三份报告均已收到：一份列出 4 个阻断项并建议 BLOCKED；一份建议 CLOSED 但确认 `getTaskStatus` 对 member 有跨范围读取，称其为“预存”；另一份建议 CLOSED 但确认第九节原始 JSON 的 FAIL/NEEDS-MANUAL、计数和证据路径问题。审查结论不能投票合并；按本方案“组员仅本人、组长仅当前公司、所有入口一致、生产四层可复核”的目标判断。**预存缺陷不是本次提交引入的回归，但仍阻断本方案的最终目标。**

### 10.1 决定性发现与当前证据等级

| 编号 | 状态 | 核到的事实 | 尚需验证 / 修复边界 |
|---|---|---|---|
| B1 管理 AI 工具入口 | ✅ 生产已验 PASS（2026-09-16，见 10.5） | 审查时 `execute-tool` 可由 member 借 `view_analytics` 调用，service-role 工具缺目标范围。补丁将入口与工具执行限为持 `use_ai_assist` 的 `company_owner`；`grantExemption` 改按 `manage_fulfillment`，从统一权限核心传入在职可见成员 ID，各查询/写工具校验目标，非集团模式拒绝全局缓存清理；二次确认时重新获取身份与范围。**已发布 694dba3b 并生产真实账号直调通过。** | 生产已核：member/admin 直接 403 无权限（且不写审计）、在职 owner 可用、跨公司成员按“管理范围外”拒绝、body 伪造 `groupMode` 无效。遗留：`getUserInfo` 命中 `exemption_grant.exemption_type` 列缺失（既有工具/表结构缺陷，非权限回归）；各**写工具**（kick/changeRole/delete/grant/retry/clear）未逐一跑真实写入（需受控对象另批）。DB 直连边界另属 B3。 |
| B2 成员管理范围与团队架构动作 | ✅ 生产已验 PASS（“组长无 team_id”子项仅单测覆盖，见 10.5） | 审查时普通 admin 的 `teamIds:null` 导致列表全团队可见，团队创建/删除只查 `manage_members`。补丁让 admin 只见可信 `team_id` 对应团队、无团队归属拒绝；创建/删除“团队=公司”需在职 `company_owner` 的有效集团模式，页面入口与 Server Action 同步收紧。**2026-09-16 阿禅确认此口径。已发布并生产直调通过。** | 生产已核：组长/公司模式 owner 的 `/admin/modules` 仅含本公司、剔除跨公司成员；createTeam/deleteTeam 经真实服务端动作请求验证——owner 有效集团模式放行（含建/删+回收、DB 前后值、刷新持久），member/admin/公司模式 owner 重放同一请求均返回 `无权限`、DB 不变；无集团 cookie 即不能伪造。**未在生产执行**：“组长 team_id 为空 → 不获得全范围”缺可安全使用的无团队 admin 账号，仅 `team-management.test.ts` 覆盖。RLS 直连仍单列 B3。 |
| B3 旧 RLS 的行范围 | 🔴 部分迁移，数据库修复未完成 | 已执行 `20260916133825_b3_rls_company_row_scope.sql`、`20260916134053_b3_fix_exemption_request_insert_team_scope.sql`，为 9 张表补 18 条限制策略，部分真实角色只读正反向通过。但首批生产矩阵还确认 **5 张 public 表 RLS OFF 且 anon/authenticated 具备四项读写 ACL**；另有已知子表宽放和跨公司 `teams` 目录。前两条 B3 migration 均未修复这些点。 | 第 2 步按第十一节定点修复并核实线上；第 3 步才是变更后的真实写入、归档操作者和可信集团态验收。`service_role` 不能代替普通角色请求。详见 `output/audit/2026-09-16-B3-生产RLS表权限函数权限矩阵.md` 与 `output/audit/B3-生产RLS行范围盘点-2026-09-16.md`。 |
| B4 第九节证据归档 | ⚠️ 文档必须纠正；部分验收需补证 | 原表 13 行却写 12 项；`s1` 回收 FAIL、`s2` 自动还原 FAIL/NEEDS-MANUAL、`s5` 页面硬刷新 FAIL；`s5b` 的 API 有值但页面检测仍无值。后续只读核对支持团队与临时 provider 的最终净状态，不能覆盖原始失败过程。原文 JSON 目录写错。 | 第九节已改为按真实证据分层。若仍要求“每场景页面→API→DB→刷新”四层全绿，需为 2c/5 补独立成功记录，并标注哪些旧批次被新证据取代；不能改写旧 JSON 或口头宣布 supersede。 |

**审查差异的处理**：第二份报告把 B1 定为“预存非阻断”，但它自己确认 member 可读跨范围任务；本计划的完成目标没有“预存越权可豁免”条款。第三份报告确认了 B4 的原始 FAIL，却建议 CLOSED；其“最终净状态可信”仅能支撑回收后的数据库状态。第一份报告对 B3 将部分豁免旧策略也概括成现行风险，范围偏宽；因此 B3 必须逐表核对生产有效策略，而不是预设 51 条全部有漏洞。以上纠偏不影响 B1、B2 的源码阻断结论。

### 10.2 收口顺序与停止点

**此处旧五项清单已由第零节「唯一当前收口台账」及第十一节覆盖**：第 1–3 步后续已有生产发布和验收记录，不再按这份历史顺序派工；当前仅待第 4 步只读独立交叉审查。每步的旧结论和未覆盖边界仍须保留，不自动改写为最终 `CLOSED`。

**排除项**：导出 >1000 行截断、退出跳 localhost、xlsx/qs 依赖、`getUserInfo` 预存列缺陷及其余与本次越权无关的历史安全顾问项，单独挂账；一旦有真实权限/数据破坏证据则不能以“预存”豁免。

### 10.3 B1/B2 本地补丁验证（2026-09-16，发布前历史快照）

- 代码范围：`/api/admin/execute-tool`、管理工具查询/写入目标校验、`/api/admin/member-ai-suggestion` 传可信目标范围、成员管理列表/候选范围、创建/删除团队 Server Action 与入口。未改 migration、RLS 或生产数据。
- 回归：权限定向 55/55；`npx tsc --noEmit --pretty false` 通过；`npm run gate:static` 通过（全量 1594/1594、测试账号脚本 2/2、lint 0 错误 47 警告、生产构建成功）。
- **当时的状态**：本地补丁完成、未推送/未部署；这不是当前状态。后续已发布与验收见 10.5；B3 两条迁移后的新缺口见第十一节。第九节的旧生产结果不能证明这次补丁。

### 10.4 决策与完成边界（2026-09-16）

- **已确认的业务决策**：团队即公司；创建或删除团队只允许在职 `company_owner` 处于服务端核验的有效集团模式时执行。普通组长、组员和公司模式下的所有者均拒绝。该决策确认的是 B2 权限口径，不是对本地 commit 的发布授权。
- **B1/B2 当前结论**：10.5 已证明指定应用入口的部署和真实角色正反向，因此第 1 步可单独判完成；无团队 admin 和逐个 AI 高风险写工具没有生产实写证据，属于明确未覆盖，不得扩大成“所有入口实写均 PASS”。
- **整份方案何时可判 CLOSED**：以第零节四步和第十一节冻结的数据库/生产/审查范围为准，不以原始方案“每条功能都全链路重跑”无限扩大；有未解决的实际越权/数据安全缺口仍 BLOCKED。

### 10.5 B1/B2 生产直调验收结果与事故（2026-09-16 [CC]）

**部署与 SHA**：`git push` 将 `origin/main` 从 `5297bc49` 快进到 `694dba3b`（仅该提交，脏文件/ B3 migration 未纳入）。Vercel 生产部署 `8babrBu7…`（URL `dydata-8ka3s27nh…`）`readyState=Ready`、`target=production`、alias=`dydata.cc`；GitHub commit status=success 绑定 `694dba3b`，据此确认 dydata.cc 正在提供该版本。**未执行 migration、未改生产函数/RLS**；DB 仅经 service-role **只读**通道核对前后值。
**发布门禁**：`gate:static` 全量 1594/1594 + create-test-users 2/2 + tsc + lint（0 错误/47 警告）+ build 全绿；`gate:browser` 1/1，仅本地 prod build 加载 4 页、不写业务数据。

**结论：B1、B2 的重点证明项在生产真实账号下全部 PASS**（明细与逐条请求/DB 前后/刷新见 `output/acceptance/b12-2026-09-16/evidence/RESULT.md`）：
- B1：member/admin `POST /api/admin/execute-tool` → 403 无权限且不写审计；在职 company_owner 可用（getTaskStatus/getAnomalousData success:true，数据限本公司）；跨公司成员 → “管理范围外”拒绝；body 伪造 `groupMode:true` 无效。
- B2 列表：组长与公司模式 owner 的 `/admin/modules` 仅含本公司 uuid，8 家外企 uuid 均 0、剔除跨公司成员，含同团队成员。
- B2 团队增删（真实服务端动作 + 1 个本轮空测试团队，净归零）：owner 有效集团模式建/删放行且回收、DB 前后值与刷新持久；member/组长/公司模式 owner 重放同一 createTeam/deleteTeam 请求 → `1:{"error":"无权限"}`、DB 不变；无集团 cookie 即不能建/删。
- 归档：归档测试账号无法建立会话（停留 `/login`、DB=archived），无法触达上述入口。

**未闭合的 B2 子项（如实标注，非 PASS）**：“组长 `team_id` 为空 → 不获得全范围”在生产缺可安全使用的无团队 admin 账号（改真实 admin team_id 属越权写），仅 `team-management.test.ts` 单测覆盖 → 生产 **BLOCKED/仅单测**。此外 B1 各**写工具**未逐一跑真实写入（需受控对象另批）。

**⚠️ 事故与恢复（验收脚本缺陷，非产品缺陷）**：Phase2 首次中止后残留测试团队；Phase2b 按团队名匹配删除按钮误命中，**删掉真实但当时显示为空的团队「武汉二」(a02ca897)**。经 owner 有效集团模式用应用 `createTeam` 重建同名团队（新 uuid `a4d1748b`）；终态 teams=9、原 9 个公司名称齐全、无测试团队、集团会话均已撤销。**纠正旧结论**：删除后才核对到 0 成员/指定表 0 引用，不能证明删除前完全无关联或“只有 uuid/created_at 差异”；线上外键确有 `ON DELETE CASCADE` / `SET NULL`，当前证据不足以断言无业务数据损失。第 3 步仅做一次有界的旧 ID/备份可用性只读对照，若发现实际丢失立刻按事故单独处理；不可反复删除团队“复验”。根因是未做到删除前唯一匹配并核对目标 uuid，后续受控写入必须加此护栏。

**边界**：本轮 B1/B2 应用层发布与生产直调到此为止。**整份方案仍 BLOCKED**（B3 `teams` 目录边界/写入/归档/可信集团验收、B4 原始失败分层与新补丁外部复核未闭合），不宣布 CLOSED。

---

## 十一、最终四步的冻结目标与交接（2026-09-16；当前执行依据）

### 11.1 第 1 步结论与第 2 步开工前的两项确认（历史决策，已确认并执行）

- **第 1 步已完成**：应用侧 B1/B2 的指定越权入口已发布并实测；10.5 的无团队 admin 和 AI 写工具逐个实写仍标注为未覆盖，不宣称所有历史功能都已复验。第 2 步不得借此扩大为“逐个重跑所有 AI 工具”。
- **待阿禅确认的建议口径 A——团队目录**：注册/申请加入团队仍可由现有 `/api/register-teams` 公开返回 `id/name`；**普通登录账号直连 `teams` 表只读本人所属公司**，不因目录用途而直读所有公司整行。集团所有者跨公司目录在服务端验证有效集团态后通过受控服务端入口读取。这样既保留入团选择，也关掉目前已复现的跨公司直接表读取。实施前核对是否有依赖直连全表的合法调用，必要时只调整该调用入口。
- **待阿禅确认的建议口径 B——集团态与数据库**：普通 `authenticated` 直连 RLS 始终只授予本公司（或成员本人），不从客户端请求参数/JWT 自称集团态获得跨公司表权限；确需跨公司的集团视角，由服务端核验有效集团会话后经受控 service-role 查询，再逐对象按角色/范围收口。`docs/权限与安全说明.md` A.3 当前写有“RLS/helper 同时检查集团模式”，若同意本建议，第 2 步须同步改成这条**分层边界**，不能留下口径冲突。若业务要求集团态能直接跨公司读写数据库，则是另一套可信签名/短时声明机制，须另定范围和工期，不能假装本批已覆盖。

**历史暂停点**：当时未收到上述两项确认前只可读盘点；后来已按“普通直连仅本公司、服务端核验集团态”口径施工。2026-09-17 阿禅针对目标项目与精确 SQL/隔离发布给出批准，此段不再是当前待办。

### 11.2 第 2 步——只收口 B3 已知数据库权限缺口（✅ 已发布，待第 3 步验收）

**已经完成、禁止重做**：生产 ref `gcrhhxaopomtposmahsw` 中两条统一权限函数 migration + 三条 B3 行范围 migration 已执行；前三批 B3 版本为 `20260916133825`、`20260916134053`、`20260916172444`。第一、二条 B3 对 9 张表新增 18 条限制策略；第三条覆盖下述四组。三份本地文件已在精确提交中版本化，线上历史、ACL/RLS/策略已回查；不能再次执行或盲跑 `supabase db push`。

**只处理以下四组**（每组先以生产实际对象、字段、有效策略和调用点为准，排除不存在或已被限制策略有效挡住的误报）：

1. **五张匿名暴露表，最高优先级**：`content_history`、`content_item`、`field_provenance`、`script_segment`、`submission_batch`。生产首批矩阵显示五表 RLS 均 OFF，`anon/authenticated` 均有 SELECT/INSERT/UPDATE/DELETE 表级权限；后续只读复核仍发现 `content_item` 存在非空数据，匿名 REST 可读。逐表核对线上真实字段/父子关系和是否有普通用户直连的合法读写；用新增 migration 开 RLS、撤掉匿名直接权限，默认普通角色拒绝；只有有证据的合法路径才补最窄的本人/父记录归属策略（写操作同时覆盖旧行 USING、新行 WITH CHECK），避免把站内调用误关掉。不得照抄与线上字段不符的早期建表 SQL。
2. **未被前两条 B3 migration 覆盖的已知策略族**：`team_join_requests`、`video_content_segments`、`video_metrics_snapshots`、`video_tags`、`exemption_request_date`。逐表核对现行多个 permissive 策略 OR 后是否仍让 active admin/owner 越公司读写、归档身份访问或借写入换目标归属；仅对可达缺口追加目标公司/父记录归属及 actor 在职限制。若某支已被父表 RLS 挡住，记录实际证明后不增加多余 SQL；不把历史报告中的推断直接当作线上 FAIL。
3. **团队目录**：按 11.1 确认口径，将普通直连 `teams` 限到本人所属公司，保留注册 `id/name` 的服务端公开目录与可信集团入口；核对读写调用的可用性，不新增普通组长建删团队的策略。
4. **已点名的旧 helper `owns_account(uuid)`**：核对生产 `SECURITY DEFINER`、`search_path`、`PUBLIC/anon EXECUTE` 与真实 RLS 调用；若无匿名合法用途，收回多余执行权并固定安全搜索路径，确保不破坏 `daily_reports` 等已有本人路径。五个统一权限函数的既有 ACL 只复核不重构。

**产物/发布门槛**：先存五表和上述对象的线上元数据快照、精确 migration 与最小回退/止损预案；定向本地/预备环境检查通过后，按新 migration 精确发布（本地/远端历史有分叉，**禁止盲跑 `supabase db push`**）。生产操作需阿禅明确批准准确 SQL；应用调用若被影响，只做相应最小适配，提交并核对 Vercel Ready/SHA 后再交第 3 步。生产回查必须证明五表 RLS/anon ACL 已关、目标策略/函数线上实际生效、迁移可追溯，并做只读或可安全恢复的最小可用性冒烟；**不要把第 3 步的完整真实角色验收伪记在第 2 步**。只精确 stage 本批文件，不卷入工作树其他脏文件；main push 另取当轮确认。

**第 2 步停止点**：上述已确认权限缺口上线并核查，给出明确改动清单和未处理阻断；若线上字段/调用与预期冲突、无法制定安全策略或缺生产授权，标 `BLOCKED` 并报告，不自行扩大到全库所有 51 条策略及其他历史安全顾问事项。

**2026-09-17 施工前快照（历史，已被下方生产证据取代）**：当时五张表 RLS OFF、匿名/登录角色四项授权全开；`owns_account(uuid)` 允许 PUBLIC/anon 执行、`search_path=public`。针对其余已知策略族，`exemption_request_date` 两条策略均经已加 B3 限制的父表 `exemption_request`，本批没有重复加子策略。原始待执行文件名 `20260916170729_b3_close_public_rls_and_related_scopes.sql` 的 SQL 与最终上线文件内容完全相同（SHA256 `83301ffced0565391adfa6dd6f35f71c925a0d52ba6e736db5f826ded8e7638f`）；应用待审批目标名称从服务端公开目录补全。主工作树施工时 `gate:static` 1608/1608；随后从远端生产基线隔离制作发布包，不带本地主分支另外两次维护提交。

**2026-09-17 生产发布与最小回查（第 2 步完成）**：阿禅批准后，从远端 `694dba3b` 隔离提交 `d073dc2528d40b104aa3510ae3eb433c42bdf6f8`，只含两条既有 B3 migration 的可追溯源码、新迁移、入团目录兼容代码/测试与权限说明；`git push origin HEAD:main` 快进成功。Vercel 生产 `dpl_JCJJKG9N9DC5b2Z6g3dydi8wSDMb` Ready，`dydata.cc` 别名命中该 SHA 后，通过受控迁移工具在 `gcrhhxaopomtposmahsw` 执行获批 SQL，线上 history 版本 `20260916172444`。随后只改源码文件名与说明的 `666745b250432eaf4491b049cc7c377dbf8c0d6e` 亦快进推送；最终 Vercel 部署 `dpl_H59z2D2btCNWq5x9VqyFHUNdUsHd` Ready，`dydata.cc` 别名命中该 SHA。隔离构建门禁：1599/1599 单测、账号脚本 2/2、类型与 lint 零错误、构建成功；`gate:browser` 1/1 通过；版本名对齐后 14/14 定向测试通过。生产回查：五表 RLS 均 ON、anon 对五表 S/I/U/D 均 false；authenticated 只保留 `content_item`、`script_segment` 的 SELECT，前者仅对在职且 `owner` 字段为本人的行放行、后者无放行策略；团队直连与入团申请限制策略、三张视频子表各四类策略均在；`owns_account` 为 definer + 空 search_path、PUBLIC/anon EXECUTE false、authenticated true。匿名 REST `content_item`、`script_segment` 为 401、`teams` 为 200 空数组；公开 `/api/register-teams` 为 200、9 条且只含 id/name。**第 2 步仅是发布和最小回查，不是第 3 步真实 member/admin/owner 写入、归档账号与可信集团态验收；整体仍 BLOCKED。**

### 11.3 第 3 步——新对话只做修复后的生产验收与证据归档

第 2 步完成后由本对话提供一次性提示词；新对话只按以下**有限矩阵**使用真实角色和存在的靶数据，明确 `PASS/FAIL/BLOCKED/未覆盖`，区分应用接口与普通 token 直连、不可用 service role 伪装成员：

| 固定场景 | 要证明的允许 / 拒绝 | 证据与安全边界 |
|---|---|---|
| 五张原暴露表 | 匿名直接读/写均拒；member 本人合法路径可用（如有），越过本人拒；只有 service-role 使用的表明确普通用户全拒 | 线上 ACL/RLS 快照 + 真实匿名/登录请求，靶表至少覆盖一个非空目标；不写真实成员数据。 |
| B3 九表与五个子表族 | member 仅本人；admin/默认 owner 同公司合法目标可读、跨公司非空目标拒；`teams` 直连按确认口径而公开入团目录正常 | 实际普通 token 请求和前后数据库对照；父子表按确有对象及产品入口抽样，未达路径明确未覆盖，不捏造空表 PASS。 |
| 受控写入 | 至少覆盖本人写与有权管理写的**允许**，以及跨公司写/换归属的**拒绝**；拒绝结果无副作用，允许结果回收后无残留 | 开始前约定唯一可回收对象、主键、基线及恢复方式；按实际新增策略选择代表性 INSERT/UPDATE（USING + WITH CHECK）和必要 DELETE；每次写后核数据库、可见页面再刷新。未备齐对象则 `BLOCKED`，绝不碰真实团队删建。 |
| 归档身份与集团态 | 有效旧 token 的 archived actor 直连拒；普通 owner 直连仍为公司范围，服务端可信集团态跨公司允许、退出后收回，伪造客户端参数无效 | 用已授权的真实测试账号/会话；若归档账号无法建立旧 token，标未验而非“登录失败=直连拒绝”；不为凑证据擅自更改真实角色。 |

**旧证据整理**：第九节原始 `s1/s2/s5` FAIL/NEEDS-MANUAL 不能改写；第 3 步只在对应修复路径做新证据并指出取代哪个旧结论。两条测试豁免申请中 R2 仍 pending：先核精确 ID 和审计要求，获得处理授权后通过安全业务流程回收或注明无法回收，不能静默删除。对误删重建的「武汉二」只做一次备份/旧 ID 的**只读**有界对照；能证明的写明，无法证明的明示事故遗留，发现实际数据损失另行上报，不用再次删团队验证。最终报告写回本文件并包含部署 SHA、迁移版本、原始请求/DB 前后/页面刷新和确切未覆盖项。

**第 3 步停止点**：上述冻结矩阵通过、测试数据回收到约定基线并可复核，或任一项 FAIL/BLOCKED 如实停下；不把 B1 每个破坏性 AI 工具、全库历史策略或导出上限并入这一轮。

#### 11.3.1 2026-09-17 修复后生产验收报告（CX）

**总裁定：第 3 步 `BLOCKED`，本报告不把整案改为 `CLOSED`，也不启动第 4 步。** 本轮没有改业务代码、执行 migration、部署、push 或提交；没有删除/重建真实团队。生产基线先于验收核对且一致：

| 项目 | 生产实况 | 结论 |
|---|---|---|
| Vercel `dydata.cc` | `READY`，SHA `666745b250432eaf4491b049cc7c377dbf8c0d6e` | `PASS` |
| Supabase | ref `gcrhhxaopomtposmahsw`，`ACTIVE_HEALTHY` | `PASS` |
| 最新 migration | `20260916172444`（`b3_close_public_rls_and_related_scopes`） | `PASS` |

真实会话使用了已登记的 member、admin、默认公司模式 `company_owner`；另使用了同公司非空靶账号和跨公司非空靶账号。凭据、token、邮箱均未写入报告。验收记录时间为上海时间 2026-09-17 01:46–01:49。

##### 一、五张原暴露表

靶表为 `content_history`、`content_item`、`field_provenance`、`script_segment`、`submission_batch`。线上快照显示五表 `relrowsecurity=on`；anon 对五表的 `SELECT/INSERT/UPDATE/DELETE` 均为 `false`。authenticated 仅保留 `content_item/script_segment SELECT`，其余三表没有普通用户直连读取权限。`owns_account` 为 `SECURITY DEFINER`，`search_path` 为空，执行权限仅 authenticated，不向 anon/public 开放。

| 角色与请求 | 非空靶数据/状态 | DB 与刷新证据 | 结论 |
|---|---|---|---|
| 匿名 `GET` 五表 | 五次均被拒：HTTP `401` | 无数据库变化 | `PASS`（匿名读边界） |
| 匿名写权限快照 | 五表 `INSERT/UPDATE/DELETE=false`，本轮不发送匿名 DML | 业务表计数无变化 | `PASS`（ACL 拒绝；未以写入冒险替代证据） |
| member `GET content_item` 越过本人 | 选取非空且非本人归属行；HTTP `200`、返回 `0` 行 | 无变化 | `PASS`（越界读拒绝） |
| member `GET content_item` 本人合法读 | 线上 4 行的 owner 都不是本轮真实 member，无法形成真实本人靶行 | 无变化 | `未覆盖`，不能用空结果作 PASS |
| member `GET content_history/field_provenance/submission_batch` | 三表均 HTTP `403`；它们是普通用户不可直连的 service-role-only 表 | 无变化 | `PASS`（普通 token 全拒） |
| member `GET script_segment` | HTTP `200`、`0` 行；线上没有非空可验靶行 | 无变化 | `未覆盖`（空表/空结果不作 PASS） |

本组没有页面写入路径；直接 Data API 的“页面刷新”不适用，且没有发起业务写入。五表计数保持：`content_history 0`、`content_item 4`、`field_provenance 0`、`script_segment 0`、`submission_batch 0`。

##### 二、B3 九表与指定子表族

九表按线上实际对象记为：`profiles`、`accounts`、`daily_reports`、`videos`、`exemption_request`、`exemption_grant`、`teams`、`ai_input_bundle`、`ai_insight_result`。指定子表族为 `exemption_request_date`、`team_join_requests`、`video_content_segments`、`video_metrics_snapshots`、`video_tags`。下表的“1/0”是非空靶行返回行数，不是表总量；全部请求均为普通真实 token 的直连请求。

| 角色/场景 | 非空靶数据与直连结果 | 结论 |
|---|---|---|
| member 本人 | `profiles` 1、`accounts` 1、本人 `exemption_request` 1、其 `exemption_request_date` 1、其 `exemption_grant` 1 | `PASS`（本人合法读） |
| member 同公司他人 | 同公司 b5a 的非空 `profiles/accounts/daily_reports/videos/exemption_request/date/grant/video_content_segments/video_metrics_snapshots/video_tags` 均返回 0 | `PASS`（越过本人拒绝） |
| admin 同公司 | 同一 b5a 非空账号、日报、视频、豁免申请/日期/授予及四类视频子表均返回 1 | `PASS`（管理范围允许） |
| 默认 company_owner 同公司 | 同一 b5a 非空账号、日报、视频、豁免申请/日期/授予及四类视频子表均返回 1 | `PASS`（默认公司范围允许） |
| member/admin/owner 跨公司 | b5b 的非空 `profiles`、`accounts`、R2 豁免申请均返回 0；`teams` 的跨公司靶队伍也均返回 0 | `PASS`（跨公司拒绝） |
| 跨公司日报/视频/授予 | 活跃跨公司没有非空日报、视频、授予靶行；不能用空结果代替 | `未覆盖` |
| 跨公司子表族 | `team_join_requests` 没有跨公司非空靶行；`video_content_segments` 九行均属本公司；其余跨公司父行亦不存在 | `未覆盖` |
| `teams` 普通直连 | 本公司队伍返回 1，跨公司队伍返回 0；member/admin/owner 均同样受公司边界约束 | `PASS` |
| `GET /api/register-teams` | HTTP `200`，9 个公开目录项；每项字段严格为 `id/name` | `PASS`（仅公开目录） |
| `ai_input_bundle` | 非空行 3；member/admin/owner 直连均 HTTP `200`、0 行 | `PASS`（普通用户拒绝 service-role-only 对象） |
| `ai_insight_result` | 非空行 3；member/admin/owner 直连均 HTTP `200`、0 行；无普通策略 | `PASS`（默认拒绝） |

同一批次的数据库前后总量完全相同：`profiles 82`、`accounts 80`、`daily_reports 1622`、`videos 1695`、`exemption_request 474`、`exemption_request_date 768`、`exemption_grant 313`、`teams 9`、`ai_input_bundle 3`、`ai_insight_result 3`、`team_join_requests 33`、`video_content_segments 9`、`video_metrics_snapshots 1666`、`video_tags 6201`。这证明本轮直连验收没有产生数据副作用，但不补足上述不存在非空靶行的覆盖缺口。

应用接口单独验收：member `GET /api/exemptions/pending` 为 `403`；admin/owner 为 `200`。owner 在 `/admin/fulfillment` 刷新前后结果均为 `200`，且默认态不包含跨公司 R2。以上应用接口结果没有用 service role 代替直连结果。

##### 三、受控写入

**结论：`WRITE_ACCEPTANCE_BLOCKED`。** 当前唯一明确的 R2 为 `b5cb7544-c2f1-405d-8776-61b33b298f0d`，现状为既有 `pending` 豁免申请、申请人属于跨公司测试账号；它不是本轮新建的专用可回收 fixture，且没有取得阿禅对该对象写入的明确确认。真实团队、成员和现有申请均不作为临时写入对象。

因此本轮没有执行本人允许写、有权管理写、跨公司写入或换归属拒绝测试，也没有执行回收/删除；没有可报告的写入前后差值、页面写后刷新或回收结果。数据库业务表计数保持不变，集团态临时会话在退出后也回到 0。不能把“没有写入”包装成写入 PASS；后续清理 R2 仍需另行授权，不能静默删除、重开或审批。

##### 四、归档身份与可信集团态

| 场景 | 请求与状态 | 页面/数据库证据 | 结论 |
|---|---|---|---|
| archived actor 旧 token 直连 | 当前测试账号材料没有可验证的 archived 账号/token；未用登录失败推断直连拒绝 | 线上虽有 archived profile，但没有会话证据 | `未覆盖/BLOCKED` |
| member/admin 伪进入集团态 | `POST /api/group-mode/enter` 均 HTTP `403`，`没有集团权限资格` | 无集团会话产生 | `PASS` |
| owner 普通态 | `GET /api/group-mode/status` 为 active `false`；`teams` 直连仍本公司 1、跨公司 0；pending 不含 R2 | `/admin/fulfillment` 刷新后仍不含 R2 | `PASS` |
| owner 伪造客户端参数 | `GET /api/group-mode/status?group_mode=true` 仍 active `false` | 未创建集团会话 | `PASS` |
| owner 服务端可信集团态 | `POST /api/group-mode/enter` 200；status active `true`；`GET /api/exemptions/pending` 200 且包含非空跨公司 R2 | `/admin/fulfillment` 刷新后仍为 200 且包含 R2 | `PASS` |
| owner 退出集团态 | `POST /api/group-mode/exit` 200；status active `false`；pending 不含 R2 | 页面再次刷新后仍不含 R2；`group_mode_sessions` active 数为 0 | `PASS` |

集团态是服务端会话状态；本轮没有把它当作普通数据库直连跨公司授权。退出后 R2 仍为 `pending`，申请、团队和业务表均无变化。

##### 五、旧证据、R2 与「武汉二」

- 第九节原始 `s1` 的 recovery `FAIL`（`checks.okRecycled=false`）保留；本轮只证明当前 R2 仍 pending 且按角色/集团态受控，不能替代原回收失败证据。
- 第九节原始 `s2` 的 owner restore `FAIL/NEEDS-MANUAL` 保留；本轮未做 owner restore，不改写旧结论。
- 第九节原始 `s5` 的页面层 `FAIL` 保留；本轮的集团态页面刷新只证明进入/退出后的当前 pending 可见性，不能替代旧 provider 页面渲染失败。旧 `s5b` 不作为改写依据。
- 本轮线上 ACL/RLS 快照、匿名请求、`teams` 直连和真实集团态证据，取代的是修复前“五表匿名暴露/B3 公开范围/teams 越界”的旧事实判断；不取代 `s1/s2/s5` 的失败或人工缺口。
- R2 已核对精确 ID 和当前状态，未处理、未删除；清理待另获授权。
- 「武汉二」只做了一次有界只读对照：旧 ID `a02ca897-b0ee-4890-ae24-f27944bd35bd` 当前不存在且 live 引用为 0；当前同名替代团队 ID 为 `a4d1748b-28e5-46df-a835-52d358d0c4ce`，存在且当前引用为 0。局部备份候选中未找到武汉专用数据库/业务行备份，因此只能证明“当前无旧 ID 悬挂引用、替代团队存在”，不能证明历史数据损失为 0；本轮未发现当前仍在发生的数据损失，也不再删建验证。

##### 六、停止与范围

本轮 `PASS` 的是：五表匿名/普通 token 边界（本人非空合法靶行缺失）、B3 已存在非空靶行的本人/公司/跨公司读边界、`teams` 直连、公开注册目录、普通角色集团态拒绝、owner 服务端集团态进入/退出和伪参数无效。`未覆盖/BLOCKED` 的是：member 本人 `content_item` 非空合法读、`script_segment` 非空靶行、活跃跨公司日报/视频/授予和部分子表族、archived 旧 token 直连、全部受控写入。因存在 `WRITE_ACCEPTANCE_BLOCKED` 和归档会话缺口，第 3 步在此停止；不扩展全库策略、B1 破坏性 AI 工具、导出上限或第 4 步交叉审查。

#### 11.3.2 2026-09-17 精确对象补验（CX；取代 11.3.1 的两项 BLOCKED，不改写旧记录）

阿禅确认两组精确生产写入及恢复后，仅使用批次 5 测试组员 `6abce492-dc2e-4376-b8de-0451c95de543`、跨公司测试组员 `61a9ca93-ddaf-467b-84eb-ed8c45e6dda9` 和测试组长 `71025a91-b33b-46bc-a04f-69cc06db7491` 的真实登录令牌。测试账号凭据、令牌不落盘。生产项目仍为 `gcrhhxaopomtposmahsw`，应用基线仍为 `666745b2`，迁移仍为 `20260916172444`；本批未改代码、函数、RLS 或 migration。原始脱敏证据在被忽略的 `output/acceptance/b3-supplemental-2026-09-17.json`，探针脚本同目录。

| 冻结测试 | 真实请求、数据库与页面证据 | 结论 |
|---|---|---|
| 本人允许新增 | 测试组员向 `accounts` 新增唯一主键 `c6bff6c3-d333-4726-98eb-338690131364`，HTTP 201，数据库归属为本人；验收前该主键 0 行、本人账号 1 行。 | `PASS` |
| 同公司管理允许修改 | 同团队测试组长仅修改上述行名称，HTTP 200 且返回目标 ID；数据库名称变为 `B3验收-0211-组长`。组员在生产 `/dashboard` 硬刷新后，账号菜单从 1 项变 2 项，并显示新名称。 | `PASS` |
| 越界更新拒绝 | 跨公司组员用普通令牌 PATCH 同一主键，HTTP 200 但返回 0 行；数据库名称未变。**200 空集不是写入成功**。 | `PASS` |
| 越界新增、换归属拒绝 | 跨公司组员向他人名下新建候选主键 `c516f3dd-4701-4dce-bdce-393aaad95cc1` 返回 403/`42501`，候选行仍 0；本人试图把测试行 `profile_id` 改给跨公司组员亦返回 403/`42501`，归属不变。 | `PASS` |
| 归档前旧令牌 | 在职测试组长先登录并以同一令牌读到本人 profile；仅将其 `membership_status` 暂改 `archived` 后，不刷新令牌直读本人 profile 与账号均 HTTP 200、0 行；恢复 `active` 后同一令牌再次读到本人 profile。全字段与原 profile 快照一致。此为 **RLS 旧令牌测试**，不代表完整业务归档/恢复流程验收。 | `PASS`（限定语义） |
| 回收与刷新 | 删除前逐一确认 9 张实际外键子表对测试主键均为 0，随后只按该主键和原归属删除新建行；候选主键 0 行。独立生产 SQL 回查两主键 0 行、原组员账号仍 1 行、组长 `active`、R2 仍 `pending`；同一 `/dashboard` 再次硬刷新后账号菜单恢复 1 项。 | `PASS` |

探针预检曾因 `advice_actions` 实际外键列为 `target_account_id`（非 `account_id`）中止两次，**均在任何写入前**；改用线上外键元数据核实后纠正脚本，再开始受控验收。第二次页面暂停验收完成后，脚本已输出并写入 `PASS` 证据，但等待用的计时器未自行退出，命令由操作员中断；这不是数据库回收失败，随后独立 SQL 与硬刷新已复核终态。两次测试写入均被精确回收，未留测试账号行。

**第 3 步裁定：冻结范围已完成，可交第 4 步只读审查；整案仍 `BLOCKED`。** 11.3.1 中不存在非空靶行的 `content_item` 本人读、`script_segment`、活跃跨公司日报/视频/授予与部分子表族仍是 `未覆盖`，不因本批账号写入改标 PASS。第九节 `s1/s2/s5` 原始失败、R2 待审批及「武汉二」历史损失无法证零的说明继续保留。若外部审查认为某项未覆盖属于必需安全门槛，应指出具体可利用路径或必须靶行，不能以模糊“全库没测完”无限扩围。

### 11.4 第 4 步——最后一次外部交叉审查与 CLOSED 判定

第 2、3 步均完成并取得新报告后，本对话再给 2–3 名外部 AI **同一份只读提示词**：复核四组数据库改动的线上定义与 source、`694dba3b` 应用边界、冻结矩阵的真实请求/数据库/刷新和旧失败分层；每条发现标阻断/非阻断/证据不足，须指出对象及证据，不重跑生产写入或顺手扩大范围。三份意见回到本对话后按证据裁决而非按票数裁决：**无未处理的实际越权、匿名暴露、核心写链路损坏或必需验收缺口**才改第零节和本节为 `CLOSED`；否则列出有限阻断及负责人决策，不创造新的模糊收口轮次。
