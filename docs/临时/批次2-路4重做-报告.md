# 批次 2 路 4（重做）整改报告

## 文件 1：ai-rewrite-client.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 1483 | `flex flex-1 items-start` | 改 min-w-0 | `flex min-w-0 items-start` |
| 1493 | `flex-1`（CardTitle 容器） | 改 min-w-0 | `min-w-0` |
| 1534 | `flex flex-1 items-start` | 改 min-w-0 | `flex min-w-0 items-start` |
| 1544 | `flex-1`（CardTitle 容器） | 改 min-w-0 | `min-w-0` |
| 1571 | `flex flex-1 items-start` | 改 min-w-0 | `flex min-w-0 items-start` |
| 1581 | `flex-1`（CardTitle 容器） | 改 min-w-0 | `min-w-0` |
| 1611 | `flex flex-1 items-start` | 改 min-w-0 | `flex min-w-0 items-start` |
| 1621 | `flex-1`（CardTitle 容器） | 改 min-w-0 | `min-w-0` |
| 1648 | `flex flex-1 items-start` | 改 min-w-0 | `flex min-w-0 items-start` |
| 1658 | `flex-1`（CardTitle 容器） | 改 min-w-0 | `min-w-0` |
| 1688 | `flex flex-1 items-start` | 改 min-w-0 | `flex min-w-0 items-start` |
| 1698 | `flex-1`（CardTitle 容器） | 改 min-w-0 | `min-w-0` |

## 文件 2：video-submit-form.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 1341 | `flex-1 h-10`（话题标签按钮） | 改 min-w-0 | `min-w-0 h-10` |
| 1502 | `flex flex-1 flex-col`（文案 motion.div） | 改 min-w-0 | `flex min-w-0 flex-col` |
| 1503 | `flex flex-1 flex-col`（文案容器） | 改 min-w-0 | `flex min-w-0 flex-col` |
| 1510 | `w-full flex-1`（textarea） | 改 min-w-0 | `w-full min-w-0` |

## 文件 3：ai-insight.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 118 | `space-y-2 flex-1`（Skeleton 区） | 改 min-w-0 | `space-y-2 min-w-0` |
| 145 | `space-y-4 flex-1`（最佳方向） | 改 min-w-0 | `space-y-4 min-w-0` |
| 176 | `space-y-4 flex-1`（最差方向） | 改 min-w-0 | `space-y-4 min-w-0` |
| 204 | `flex items-start gap-4 flex-1`（下期重点） | 改 min-w-0 | `flex items-start gap-4 min-w-0` |

## 文件 4：feature-detail-panel.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 39 | `flex min-h-[480px] flex-1`（空态占位） | 改 min-w-0 | `flex min-h-[480px] min-w-0` |
| 66 | `flex flex-1 flex-col`（motion.div 主容器） | 改 min-w-0 | `flex min-w-0 flex-col` |
| 154 | `flex flex-1 flex-col`（Textarea 容器） | 改 min-w-0 | `flex min-w-0 flex-col` |
| 182 | `min-h-[360px] flex-1`（Textarea） | 改 min-w-0 | `min-h-[360px] min-w-0` |

## 文件 5：dashboard-workspace-header.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 105 | `min-w-0 flex-1 space-y-2`（左侧标题区） | 改 min-w-0 | `min-w-0 space-y-2` |
| 270 | `flex flex-1 items-center`（工序条 stage） | 改 min-w-0 | `flex min-w-0 items-center` |
| 274 | `group flex flex-1 items-center`（工序按钮） | 改 min-w-0 | `group flex min-w-0 items-center` |

## 文件 6：hit-analyzer.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 743 | `min-h-0 flex-1`（散点图容器） | 改 min-w-0 | `min-h-0 min-w-0` |
| 1121 | `xl:min-h-0 xl:flex-1`（文案列表） | 改 min-w-0 | `xl:min-h-0 xl:min-w-0` |
| 1140 | `xl:min-h-0 xl:flex-1`（规律总结） | 改 min-w-0 | `xl:min-h-0 xl:min-w-0` |

## 文件 7：history-sidebar.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 143 | `flex-1 overflow-y-auto`（记录列表区） | 改 min-w-0 | `min-w-0 overflow-y-auto` |
| 184 | `flex-1 truncate`（描述文字） | 改 min-w-0 | `min-w-0 truncate` |
| 288 | `flex-1 overflow-hidden`（Dialog 内容） | 改 min-w-0 | `min-w-0 overflow-hidden` |

## 文件 8：leader-dashboard.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 345 | `min-w-0 flex-1`（审核队列内容区） | 改 min-w-0 | `min-w-0` |
| 395 | `min-w-0 flex-1 space-y-1`（豁免审批内容区） | 改 min-w-0 | `min-w-0 space-y-1` |

## 文件 9：action-hub.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 107 | `min-w-0 flex-1`（豁免待办内容） | 改 min-w-0 | `min-w-0` |
| 152 | `min-w-0 flex-1`（提交状态异常内容） | 改 min-w-0 | `min-w-0` |

## 文件 10：视频结论卡-单卡.tsx

| 行号 | 原 className 关键部分 | 操作 | 新 |
|---|---|---|---|
| 63 | `flex-1 space-y-4`（视频结论卡主体） | 改 min-w-0 | `min-w-0 space-y-4` |
| 170 | `flex-1 space-y-4`（干预结论卡主体） | 改 min-w-0 | `min-w-0 space-y-4` |

## 总计

- 改 min-w-0：39 处
- 改 w-auto：0 处
- 删除 flex-1：0 处
- 保留（合理）：0 处
- SKIP（不确定）：0 处

## 验收

```
$ npx tsc --noEmit 2>&1 | tail -10
src/app/(app)/admin/ai-assistant/chat-errors.test.ts(4,44): error TS5097...
（共 16 个 pre-existing test 文件错误，与本改动无关）

$ npm run lint 2>&1 | tail -10
✖ 60 problems (34 errors, 26 warnings)
（全部为 pre-existing 错误，无新增）
```

**结论**：清单内 10 个文件全部处理完毕，tsc / lint 无新增错误，仅存在改动前已有的 test 文件和 lint 问题。
